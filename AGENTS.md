# Agent 协作规则

本文件只保留通用开发约束。个人需求、产品设计、实施计划、逐 Task 报告和原始设备验收记录属于本地开发资料，不作为远端协作依赖。`docs/development/` 中列出的四份当前文档是本项目的版本化协作入口，应随工作推进保持同步。

## 开发文档与交接

`docs/development/` 当前只保留以下四份文档；其他资料放入 `docs/development/archive/<日期>-<主题>/`，只用于追溯。当前快照为 2026-09-27：

1. `开发状态-<日期>.md`：当前分支、基线、待开发项、阻塞项、下一步和必要检查命令。
2. `自动化测试说明-<日期>.md`：自动化命令、结果、覆盖范围和不能替代人工的边界。
3. `人工验收清单-<日期>.md`：真实桌面逐项验收的唯一执行入口与记录位置。
4. `功能完成度与验收状态矩阵-<日期>.md`：功能完成度、自动化/Windows/macOS 状态和发布阻塞项的唯一汇总。

当前四份文件必须分别只有一个最新日期版本：`开发状态-2026-09-27.md`、`自动化测试说明-2026-09-27.md`、`人工验收清单-2026-09-27.md`、`功能完成度与验收状态矩阵-2026-09-27.md`。归档目录只用于追溯，不作为接手入口，也不在其中继续维护“当前状态”。

- 开始接手时按上述 1 → 2 → 4 的顺序阅读；需要真机验证时再阅读 3。源代码、测试和公开文档仍按任务需要阅读。
- 行为或测试范围变更后，更新“开发状态”“自动化测试说明”和“功能完成度与验收状态矩阵”。人工验收后，更新“人工验收清单”和“功能完成度与验收状态矩阵”。
- 登记人工验收反馈前，必须先检索历史待开发项、已完成摘要和人工验收证据，明确判断该反馈是新问题、旧问题未修复、旧问题回归，还是已修复后的边界遗漏；再核查当前源码和测试事实。未完成真实复验时，不得把“代码已修复/已覆盖”写成“人工复验通过”。
- 当前文档只保留待推进事项和必要的已完成摘要。不得只写“部分完成”：必须拆成“已完成”和“仍需”，后者写明下一步或阻塞原因。
- 形成新的当前快照时，使用中文主题加 `YYYY-MM-DD` 命名，更新所有引用后将旧当前文档移入带日期的归档目录；不删除历史证据。
- 远端合并带来的旧版当前文档、人工验收记录、问题日志和版本范围参考，必须先吸收仍有效的结论，再移入归档；不得让旧英文/旧中文入口重新回到当前目录。
- 不将个人绝对路径、真实 Skill 内容、密钥、缓存、构建产物或用户未提交状态写入当前文档。

## 开始工作

- 先阅读 `README.md`、相关源码、测试和公开文档，确认任务边界。
- 发现需求、接口或架构冲突时先报告，不凭聊天记录或未提交文件擅自改范围。
- 保持变更聚焦，不顺手加入无关功能、重构或依赖升级。

## 实现与验证

- 行为变更采用 TDD：先写失败测试，再实现最小行为，最后重构。
- **前端改动（新增功能与缺陷修复均适用）动手前必须先加载 `frontend-design` skill**，按其方法完成设计与实现；涉及 React 代码时同时遵循 `vercel-react-best-practices` skill。修复 bug 不得绕过该要求。
- **前端行为的验收优先使用浏览器自动化**：`playwright-cli` 写 E2E；探查式验证用 `browser-use:web-gui-tester` 或 `agent-browser`。
- **人工验收清单按完整操作流程编排**：不要简单按 DEV 编号逐条罗列重复步骤；应将能在同一用户操作链路中验证的多个验收项合并为一条流程。每条流程用表格明确“覆盖项、前置素材、操作步骤、通过标准、结果、截图/日志证据和现场清理”，让验收者一次操作覆盖多个判据并能集中记录。开发待办仍保留 DEV 编号映射，避免合并流程后丢失追踪关系。
- 测试覆盖适用的成功、失败、取消、重复、恢复和权限受限路径。
- 不降低断言、删除测试或增加任意延时来掩盖问题。
- 完成变更后运行相关测试，以及仓库定义的格式、静态检查、类型检查和构建。
- 跨平台行为通过适配器和测试夹具隔离，考虑 Windows 与 macOS 的路径、权限、链接、大小写和换行差异。
- 生成的接口绑定必须由生成工具更新，不能手工维护重复契约。

## 界面呈现约定

- **路径展示**：任何面向用户的路径不得出现 `\\?\`、`\??\` 等 Windows 内部前缀，也不得出现同一目录因斜杠拼写（`\` 与 `/`）不同而重复展示；同一物理目录只展示一条，展示前按文件系统身份归并并统一斜杠风格。新增任何展示路径的界面必须遵守，存量界面发现违例一并修。
- **Agent 呈现**：**项目所有前端展示面**涉及 Agent 工作台厂商与类型时，统一使用「品牌厂商 logo + 用户可理解的展示类型徽标」组合呈现，不允许只在某个页面接入，也不允许纯文本 `client_id`/技术 ID 裸奔；例如 `codebuddy.code` 只展示为 CodeBuddy +「终端」，`codebuddy.workbuddy` 只展示为 CodeBuddy +「桌面端」。前端展示类型必须由统一 presenter 按用户视角归类，不能直接照搬后端 `ClientKind`：`cli/tui` 显示「终端」、`desktop` 显示「桌面端」、`ide_extension` 显示「IDE 插件」、`web` 显示「网页端」、`mobile` 显示「移动端」、`bot` 显示「机器人」、`headless` 显示「后台服务」、`acp` 显示「协议接入」、`shared_directory` 显示「共享目录」。同品牌、同 Skill 目录、不同展示类型的条目合并为一张卡片并标注类型，不得重复出卡；新增或修改 Agent 展示面时必须做全项目消费点检查。
- **共享 Agent 目录语义**：共享目录本身视为一个独立的 Agent 目录实体，在目录列表、技能关联和部署目标中只展示一份“共享目录”；支持/识别该目录的各品牌 Agent 作为该目录的关联方，不复制共享目录卡片。关系图谱必须把“Skill → 共享目录 → 可识别共享目录的 Agent”画成可区分的节点与关系，不能用 Skill 直接连多个 Agent 来替代共享目录拓扑。
- **集中库同名与部署命名边界**：集中库以 `SkillId` 作为实体身份，允许存在同名但内容、来源或用途不同的 Skill；当前可读目录名采用“原运行时名称 + 稳定识别后缀”的形式，不能据此把名称后缀写回 `SKILL.md`。Agent/项目的同一目标目录内，用户可见的有效 Skill 名称必须唯一，不能把两个同名目录直接部署到同一目标。部署入口名默认仍使用 Skill 的 `runtime_name`，并必须与副本/连接所呈现的 `SKILL.md:name` 一致。集中库内部目录后缀不参与 Agent/项目识别：软连接/目录联接的目标目录 basename 可以不同，Agent 看到的是目标位置上的连接入口名；实现上还必须确保连接目标中的 `SKILL.md:name` 与入口名一致。若产品允许用户为部署关系指定别名，别名必须是“该目标上的部署名称”，不是修改集中库 Skill；仅在受管复制模式下生成隔离副本，并同步改写副本 `SKILL.md` 的 `name` 以保持文件夹名与声明一致，记录原名/部署名/来源版本并支持回滚。符号链接、目录联接不得静默改写源文件；没有安全的别名部署能力时必须在预览阶段阻断并说明可选目标/处理方式，不能自动重命名或覆盖原 Skill。
- **技术标识不进界面**：前端展示以用户视角设计，不从技术角度出发。内部标识（SkillId、UUID、内容哈希、operation id、pairId、client_id 等）**一律不得出现在任何用户可见界面**——包括页面标题、图谱节点标签、列表与详情，也包括「技术详情/诊断信息」这类折叠区域：折叠只能用于收纳次要的**用户可读**事实（实现方式、能力说明、时间、规范化路径），不得成为展示原始 ID 的豁免通道。需要标识语义时用名称、版本、时间等用户可读信息。错误码/枚举值（`delete_skill`、`object.not_found` 等）不得作为独立的标签、标题或字段值出现；唯一例外是既有失败文案约定：本地化失败说明后括注错误码（如「操作失败（xxx）」）用于支持对账，且该说明必须先于码值、独立可读。技术对账与原始错误参数属于日志和导出诊断文件的职责，不属于界面。存量界面发现违例一并修。
- **多步流程分步呈现**：向导类多步流程的每一步必须是独立的呈现单元（子页面、弹窗或步骤面板），禁止把后续步骤的内容追加到长列表末尾靠滚动条发现；步骤切换由明确的「上一步/下一步」驱动，当前步骤内容应处于首屏可视范围。
- **抽屉与完整详情页的包含关系**：技能抽屉是快捷展示与快捷操作入口，但不是能力的唯一入口。抽屉中的所有内容和操作必须在技能完整详情页中同步可达；完整详情页可以提供抽屉没有的更多内容或操作。新增或优化抽屉能力时，必须同步检查详情页的字段、操作、状态反馈、权限边界和持久化结果，避免两处行为漂移。
- **调用方式只读边界**：Skill 的“调用方式/运行要求”在 SkillHub 内只展示声明与当前可读事实，不提供编辑或切换入口。模型调用、用户调用等开关由各家 Agent 自己管理；除非后续明确改变产品裁决，不得在抽屉或完整详情页新增修改调用方式的操作。

## 安全与数据

- 不提交 API Key、密码、令牌、个人路径、用户 Skill 内容、缓存、构建产物或其他敏感数据。
- 复制、链接、删除、恢复和迁移必须保留用户原文件，记录影响并提供可回滚路径。
- 外部路径、归档、网络内容和用户输入必须经过边界校验；不得执行未经校验的任意命令。
- 确定性安全检查不能被可选的 LLM 结果替代；不把“发现文件”描述为“Agent 一定能执行”。

## Git 协作

- `main` 应保持可构建、可测试；正常开发使用独立分支，只有明确授权时才直接修改 `main`。
- 并行任务使用独立 worktree，避免共享工作目录或文件集合。
- 独立任务使用清晰的英文动词开头提交信息；提交前运行 `git diff --check`。
- 合并或推送前确认测试结果、工作区状态和变更范围；不使用破坏性命令覆盖用户工作。
- 完成任务时报告修改文件、验证命令及结果、提交哈希、未解决问题和后续依赖。

## 完成标准

只有在行为有测试、实现符合边界、相关检查通过、变更已记录且风险已说明时，才能声明任务完成。
