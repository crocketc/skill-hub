# SkillHub Agent 协作规则

## 1. 项目定位

SkillHub 是一个面向 Windows 和 macOS 的本地 Skill 全生命周期管理工具，负责 Skill 的发现、导入、集中存储、版本管理、安全检查、搜索、部署、解除部署、备份和迁移。

当前仓库处于开发准备阶段，功能范围已经确认，正式代码实现尚未开始。

## 2. 权威文档

开始任何开发任务前，先阅读本文件和对应任务的实施计划。需求和设计以以下文档为准：

1. `docs/需求文档.md`：功能范围和业务规则的唯一需求来源。
2. `docs/产品与交互设计.md`：页面、交互、状态和操作流程。
3. `docs/Agent平台兼容性调研.md`：Agent 平台目录、项目级 Skill 和适配边界。
4. `docs/技术架构设计.md`：技术边界、模块职责和数据流。
5. `docs/superpowers/plans/`：按阶段拆分的实施任务、测试要求和验收门槛。

如果代码实现与需求或设计文档冲突，不要自行扩大或改变范围，先报告冲突并等待确认。

## 3. 开发方法

- 所有功能代码采用 TDD：先写能表达行为的失败测试，再实现最小功能，最后重构。
- 每个实施 Task 都要保持边界清晰，不能顺手加入未列入任务的功能。
- 测试必须覆盖成功、失败、取消、重复、恢复和权限受限等相关路径。
- 不要通过降低断言强度、删除测试或增加任意延时来掩盖问题。
- 每个 Task 完成后运行该 Task 的测试；每个阶段完成后运行对应集成测试。
- 代码、测试和文档中的用户可见文本应支持既定的 i18n 方向。

## 4. 并行 Agent 协作

- 只有互不依赖、不会修改同一组文件的任务才允许并行。
- 每个 Agent 只负责一个明确的 Task 或一个明确的模块边界。
- 开始前说明：任务编号、目标、允许修改的文件范围、依赖的接口和验收命令。
- 不要重写其他 Agent 正在负责的模块；发现边界冲突时停止并报告。
- 主 Agent 负责接口冻结、代码审查、合并、联调和最终验收。
- Agent 返回时必须说明修改文件、测试命令及结果、未解决问题和提交哈希。

## 5. Git 工作流

- `main` 是集成分支，保持可构建、可测试。
- 并行开发使用“独立分支 + 独立 worktree”，不要让多个 Agent 共享同一工作目录。
- 分支命名建议：`feat/<area>-<task>`，例如 `feat/catalog-task-03`。
- 每个 Task 独立提交，提交信息使用简短、明确的英文动词开头，例如 `feat: ...`、`test: ...`、`fix: ...`。
- 合并前必须通过任务测试和 `git diff --check`；合并后由主 Agent 运行阶段级联调测试。
- 个人项目不强制每个 Task 创建 GitHub PR，但不得绕过审查直接把未验证代码推到 `main`。
- 不得使用 `git reset --hard`、`git checkout --` 或其他破坏性命令覆盖用户工作，除非得到明确授权。

## 6. 文件和数据安全

- 不提交 API Key、密码、令牌、个人路径、用户 Skill 内容或其他敏感数据。
- 涉及 Skill 文件、软链接、复制、删除和恢复的操作必须可追踪、可回滚，并保留用户原文件。
- 不擅自删除、移动或重命名已有用户文件；需要变更时先确认精确目标和影响范围。
- 基础安全检查必须保持确定性；LLM 检查只能作为独立的可选增强，不能替代基础检查。
- 兼容性判断只负责识别和部署文件，不假定目标 Agent 一定会成功执行 Skill。

## 7. 跨平台要求

- V1 面向 Windows 和 macOS；不要把 Linux 专属行为作为默认前提。
- 文件路径、软链接、权限、大小写和换行符处理必须考虑 Windows/macOS 差异。
- 涉及平台差异的行为应通过适配器和测试夹具隔离，不要散落在界面层。

## 8. 完成标准

一个 Task 只有同时满足以下条件才算完成：

1. 行为有对应测试。
2. 实现符合需求、架构和当前 Task 边界。
3. 相关测试通过，且没有新增未解释的失败。
4. 变更已在独立提交中记录。
5. 向主 Agent 汇报文件、测试结果、风险和后续依赖。
