# GOAL 交接指令：复核后的人工验收与 v0.2.0 收口（2026-09-17）

> 本文件是交给下一位代理的作业指令。读完立即开工，不要向用户确认范围或排期——已授权你自行裁决并在执行中调整。
> 唯一例外：涉及**用户主观观感**的事项，登记到《人工验收清单-2026-09-17》并采用可回滚默认实现，不阻塞、不提问。
>
> **本轮与前几轮的根本区别**：上一轮代理自报「全部开发与自动化验收已完成，剩余仅为人工验收」。该结论已由另一位 agent 独立复核，**代码主体属实，但有三处需要打折**。你的第一优先级不是继续加功能，而是**把被打折的自动化证据补齐或修好**，然后才谈人工验收与发布。

---

## 1. 开工位置（不要新建分支或工作树）

```bash
cd C:/vibe_coding/skill-hub      # 本机唯一工作区（git worktree list 只有它）
git status                       # 必须干净
git log --oneline -5
git rev-parse HEAD
git ls-remote origin feat/v0.2.0-product-completion   # 两者必须一致
```

- 当前分支：`feat/v0.2.0-product-completion`，已推送并与 `origin` 同步；Task 10 的 worktree/分支已合并关闭删除。
- 上一轮功能提交止于 `b20b835`，收口文档提交为 `4bd52d5`（后者只改 5 个 md 文件，不含代码）。
- **本轮复核更正提交：`e3dc554`**（`docs: correct the 2026-09-17 snapshot after independent review`），本交接文档为紧随其后的 `docs: hand over …` 提交。
- **不要**为这批工作新建工作树——剩余工作是补证据与收口，不是并行开发。

## 2. 已核实的现状（本次交接实测，不是转述）

| 项目 | 实测结果 |
|---|---|
| 工作区 / 远端 | `git status` 干净；本地 HEAD == `origin/feat/v0.2.0-product-completion` |
| `cargo test --workspace --all-features` | 退出码 0；**142 个测试二进制、1098 通过 / 0 失败 / 0 忽略** |
| `cargo fmt --all -- --check` | 通过 |
| `pnpm verify:lifecycle` | 通过 |
| 前端测试文件数 | `apps/desktop/src` 下 `*.test.*` / `*.spec.*` 共 **165 个**（不是 296） |
| `pnpm exec playwright test --list` | **387 用例 / 35 spec**（独立点数，与上一轮一致） |
| `pnpm test:e2e` 全量复跑 | **387/387 通过、0 失败**（逐条 `ok` 计数；`.playwright-artifacts-*` 全空） |
| Windows Junction 修复 | **真修复**。逐字节核对：`SubstituteNameOffset=0`、`PrintNameOffset=substituteBytes+2`、`ReparseDataLength` 计入两个 NUL 终止符；`crates/skillhub-adapters/tests/deployment_filesystem.rs` 两项 `#[cfg(windows)]` 硬断言测试在本机**无提升权限**实跑通过 |
| Agent 发现根目录路径拼接 | **真修复**，修掉了 `%USERPROFILE%` 展开后混用正反斜杠的问题 |
| 敏感信息 | 本轮 325 行新增代码无个人路径、无密钥；`test_key.txt` 已被 `.gitignore` 覆盖且未提交 |

## 3. 上一轮报告中被证伪或打折的结论（必读，这是你的工作清单来源）

### 3.1 Task 12 与「关系治理链路」：0 失败 ≠ 验过

`crates/skillhub-application/tests/facade_relationship_governance.rs` 里有 **17 处** `available_capabilities().symlink` 守卫，命中时**提前 `return`**：

- **16 处完全静默**（无任何输出）；
- **1 处**（约第 1544 行）会 `eprintln!("skipping: …")`；
- 所以 Rust 全量结果里的 `ignored=0` 是假象——被跳过的用例**记为 passed，不记为 ignored**。

2026-09-16 基线的 18 项失败去向是：**17 项**改成静默 `return`，**1 项**（发现路径拼接）真实修复。

结论：**在这台无链接权限的 Windows 宿主上，关系迁移 prepare/commit/rollback、共享引用治理、批量取消链路没有执行任何断言。** 矩阵里 Task 12 已下调为「部分完成」，人工验收清单新增了 RC-15 强制取证。

### 3.2 一处断言被削弱

`governance_batch_requires_the_shared_impact_confirmation_per_row`：原来断言 `prepared_count == 1 && state == Prepared`，现在只断言「确认阻塞消失」，**行是否真的进入 `Prepared` 不再检查**。`AGENTS.md` 明令禁止降低断言。人工验收清单新增 RC-16。

### 3.3 Task 4「统一执行反馈已完成」是溢价结论

同一份《自动化测试说明》的异步入口审计表**自己**就写着：

- `skill-detail/nativeApi.ts`「即时写入/分析入口未统一」
- `removal/nativeApi.ts`「需逐入口核对」
- `security/nativeApi.ts`「部分已桥接」

但《开发状态》与矩阵却把 Task 4 标成「已完成」。以审计表为准，矩阵已下调为「主链路已完成、低频入口待收口」，人工验收清单新增 SR-9。

### 3.4 一处数字错误

「前端单元 296 文件」不存在。`pnpm test:frontend`（= `pnpm --dir apps/desktop test --run`，默认 vitest include）只运行 **165 个测试文件**；`1735` 这个用例数是对的。`296` 疑似把 `apps/desktop/src` 下约 298 个 `.tsx` 源码当成了测试文件数。四份文档与实施计划中的该数字已全部改正。

## 4. 剩余工作（按优先级串行，每项一次提交）

### P0 — 恢复证据完整性（不做完不要谈发布）

1. **RC-15：在有符号链接能力的宿主上补跑。** 优先 macOS，或 Windows 开发者模式 / 提升权限。
   `cargo test -p skillhub-application --test facade_relationship_governance`
   记录 17 项链路的**真实通过/失败集合**，写进《人工验收清单-2026-09-17》RC-15 行与矩阵。
2. **让「跳过」不再伪装成「通过」。** 把 16 处静默 `return` 改成至少可观测（`eprintln!("skipping: …")`）或在测试名/输出里可区分。允许用 `#[ignore]` + 显式原因，但**必须让 `ignored` 计数真实反映跳过数**。改完后全量结果里这两个数字应自洽。
3. **RC-16：恢复被削弱断言。** 把 `governance_batch_requires_the_shared_impact_confirmation_per_row` 恢复为 `prepared_count == 1 && state == Prepared`，并在有权限宿主上跑通。

### P1 — Task 4 低频入口收口

4. 逐个核对 `skill-detail/nativeApi.ts`（12 个调用点）、`removal/nativeApi.ts`（7 个）、`security/nativeApi.ts`（7 个）：判定每个是「用户可触发的写入」还是「API 边界/纯浏览」。属于前者的接入 `runTrackedOperation`（长流程）或 `instant` 模式（同步写入），并各补成功/失败/取消测试。改完后把矩阵里 Task 4 那行改回「已完成」，并同步更新审计表。

### P2 — 人工验收

5. 按《人工验收清单-2026-09-17》执行：`J-REL-1—4`、`RC-13/14/15/16`、`SR-1—9`。结论写回清单与矩阵后，才可评估发布。

### P3 — 可选

6. **E2E 收尾卡死**：本轮复核两次遇到「全部用例已输出 `ok`、进程却不退出、迟迟不打印汇总行」。已确认的规避方式：跑前确保 **5174 端口空闲**（配置 `reuseExistingServer: false` + `strictPort`），日志写仓库外，判定结果看逐条 `ok`/`not ok` 计数而非汇总行，结束后手动清理残留 `node` / `headless_shell`。根因未最终定位（疑似 vite webServer 子进程不随 runner 退出），不是本轮阻塞项。

## 5. 硬约束（来自 `AGENTS.md` 与既有裁决，不得违反）

- **行为变更必须 TDD**：先写失败测试再实现。**严禁**降低断言、删除用例、加任意延时、静默 `return` 让测试变绿——这正是本轮被打折的地方，不要重复犯错。
- **测试数只增不减**：前端 ≥165 文件 / 1735 用例；E2E ≥387。
- **绑定文件只由生成器更新**：`apps/desktop/src/api/bindings.ts` 不得手工改。注意正确口径是「生成前后 SHA256 一致」+ 再生成一次确认为不动点，**不是** `git diff --exit-code`（specta 会把 Rust 文档注释写进绑定，相对 HEAD 非空是正常结果）。
- **术语冻结**：动词层面禁用「部署」，用「添加到 Agent/项目」「从 Agent/项目移除」；名词层面保留「链接部署/复制部署/部署关系」；技术详情保留「部署/解除部署」「符号链接/目录联接/受管复制」。不得用「装配」「安装/卸载」。
- **中英双语文案键集对称**：改文案同时改 `zh-CN/common.json` 与 `en-US/common.json`。
- **不得**把自动化证据写成真机通过；**跳过不计为通过**；不得把设计稿或后端契约写成页面已完成。
- 当前文档只保留待推进事项与必要的已完成摘要，**不得只写「部分完成」**，必须拆成「已完成」+「仍需」。
- 不提交个人绝对路径、真实 Skill 内容、密钥、缓存、构建产物。

## 6. 阅读顺序（严格按此）

1. 本文件
2. `docs/development/开发状态-2026-09-17.md`（含「独立复核」结论与「下一步」）
3. `docs/development/自动化测试说明-2026-09-17.md`（含独立复跑复核与能力守卫说明）
4. `docs/development/功能完成度与验收状态矩阵-2026-09-17.md`（已下调 3 行完成度）
5. 真机操作时读 `docs/development/人工验收清单-2026-09-17.md`（RC-15/RC-16/SR-9 是新增项）
6. 复核前的四份文档快照：`docs/development/archive/2026-09-17-独立复核前快照/`（**仅追溯，不要从中恢复结论**）
7. 实施依据：`docs/superpowers/plans/2026-09-16-skill-relationship-module-and-unified-execution-implementation-plan.md`（任务 10 在 :355、任务 11 在 :389、任务 12 在 :442）
8. 上一轮交接指令已归档至 `docs/development/archive/2026-09-17-交接指令/`

## 7. 本机环境坑（Windows，务必先看，能省你几小时）

- **`cargo test` 与 `vitest` 不要并发跑。** 实测会互相抢 CPU，产生 5 秒超时**假失败**（复核时并发跑出现 3 项假失败，单项复跑 `OverviewPage.test.tsx` 为 25/25 全绿）。所有自动化电池请串行。
- **Playwright 会清空仓库根 `test-results/`。** 不要把验证日志重定向到那里（会当场被删，拿不到失败原因）；写到系统临时目录。
- **Bash shim 缺 coreutils**：`ls`/`head`/`sed`/`grep`/`dirname` 常不可用。文件操作优先用 Read/Write/Glob/Grep 专用工具，或绝对路径 Python/Node：
  `C:/Users/crock/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`
- **PowerShell 工具不回传 stdout**（只能拿退出码）。需要输出时重定向到文件再 `Read`。
- **Playwright 失败工件是未打包的 NDJSON 流**（`.trace`），不是 zip，可直接按行解析；`retain-on-failure` 只在失败时保留。
- **不要用「Python 能否 `os.symlink`」判断环境链接能力**，判断链接能力要看产品自己的 `available_capabilities()`。

## 8. minor 台账（发现一条记一条，不得悬空）

| 来源 | minor 内容 | 处置 | 提交 |
|---|---|---|---|
| 独立复核 | 16 处能力守卫静默 return，跳过伪装成通过 | 未处置 | — |
| 独立复核 | `governance_batch_…confirmation_per_row` 断言削弱 | 未处置 | — |
| 独立复核 | `skill-detail`/`removal`/`security` 三处入口未统一 | 未处置 | — |
| 独立复核 | 「前端单元 296 文件」数字错误 | 已处置（四份文档 + 实施计划已改正） | 见本轮 `docs:` 提交 |
| 独立复核 | 文档自相矛盾：「本地提交待推送」写在已推送的提交里 | 已处置 | 见本轮 `docs:` 提交 |
