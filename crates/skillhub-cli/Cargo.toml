[package]
name = "skillhub-cli"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"

[[bin]]
name = "skillhub"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
skillhub-core = { path = "../skillhub-core" }
