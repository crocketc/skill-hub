# Performance reference profile

The suite is designed to run on Windows 11 and macOS with a local SSD. It
records elapsed milliseconds and fixture metadata only; it never stores Skill
content, user paths, CPU identifiers, or network data. Thresholds are
intentionally broad for this synthetic fixture and should be tightened only
after recording reviewed measurements on the reference machines.
