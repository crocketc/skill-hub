# 交接指令：SkillHub v0.2.0 继续开发（2026-09-06 · N8 后）

> 用法：将分隔线以下全部内容作为 /goal 指令发给新会话的 Agent。
> 生成时间：2026-09-06 16:15，候选提交 `d46019f`（N8 批量来源更新检查）。Q1~Q17 全队列 + 原生缺口 N1~N8 已完成，本指令聚焦剩余缺口与深挖。

---

## 角色与总体目标

你是 SkillHub（Tauri 2 + React 18 + TypeScript，Rust 工作区 crates/）的开发 Agent。**持续自主开发，不要停下来向用户提问、不要请求人工验收**。遇到需要产品决策的点：选择最符合已有文档精神的方案，把决策与理由追加到 `docs/development/人工验收问题记录.txt`，然后继续。

工作目录：`C:\vibe_coding\skill-hub`，分支 `feat/v0.2.0-product-completion`。

**运行模式**：用户会自己决定何时停止。每个任务必须是完整可验证的闭环（测试 → 检查 → 提交推送 → 问题记录）；任何时刻停下工作区都应是干净的。若用户发出"准备停"类信号：20 分钟同步文档 + 输出最终报告后结束。

## 第一步（必做）

1. `AGENTS.md`
2. `docs/development/当前开发状态.md`（权威现状，HEAD 应为 `d46019f` 或更新）
3. `docs/development/前端页面功能与UI遗漏清单-2026-09-05.md`（FE-01~FE-18 逐行状态）
4. `docs/development/人工验收问题记录.txt`（全部反馈与修复记录；N8 节含最新决策）
5. `docs/development/v0.2.0-manual-acceptance-guide.md`（1c~1u 节为已实现待验收项）
6. `docs/development/缩放验收矩阵.md`（待人工执行）
7. 历史计划与任务报告已归档至 `docs/development/archive/`，需要考古时去那里找。

然后 `git status --short` 与 `git rev-parse HEAD` 确认分支状态（当前候选 `d46019f` 或更新；工作区应仅剩用户保留的 `apps/desktop/src-tauri/Cargo.toml` 未提交改动与未跟踪 `.workbuddy/`）。

## 硬性约束（不可违反）

1. **禁止触碰** `apps/desktop/src-tauri/Cargo.toml` 的未提交改动（用户保留的 `features=[]`）——不得提交、还原、叠加修改；新 Rust 依赖加到对应 crates/ 的 Cargo.toml。
2. **不合并 main、不创建/发布 tag、不做发布预检**。
3. 每任务独立提交，英文动词开头；提交前必跑：相关测试 → `pnpm --dir apps/desktop check` → `git diff --check`；改前端加跑 build；改 Rust 加跑 `cargo fmt -p <crate>` + `cargo check` + 相关测试。**以 check 退出码为准，测试通过不能替代**。
4. **TDD**：先写失败测试再实现；成功/失败/取消/重复路径都覆盖。
5. **禁止手写 bindings**：契约变更后 `SKILLHUB_WRITE_BINDINGS=1 cargo test -p skillhub-desktop generate_bindings` 再生成。
6. i18n：zh-CN 与 en-US 键集完全一致（有 parity 测试）；多个写入方并行后必须跑全量 typecheck 兜底。
7. 预览/读取类 UI 不得写文件；写操作必须显式确认；能力缺失时诚实降级并记录，不伪造。
8. 动手前查 `apps/desktop/src/api/bindings.ts` 与 `crates/skillhub-application/src/lib.rs`，不重复实现已有契约。

## 环境坑（实测，直接照做）

**MSVC 链接**：
```bash
MSVC="/c/Program Files (x86)/Microsoft Visual Studio/2022/BuildTools/VC/Tools/MSVC/14.44.35207"
export PATH="$MSVC/bin/Hostx64/x64:$PATH"
export LIB="C:\\Program Files (x86)\\Microsoft Visual Studio\\2022\\BuildTools\\VC\\Tools\\MSVC\\14.44.35207\\lib\\x64;C:\\Program Files (x86)\\Windows Kits\\10\\Lib\\10.0.26100.0\\um\\x64;C:\\Program Files (x86)\\Windows Kits\\10\\Lib\\10.0.26100.0\\ucrt\\x64"
export INCLUDE="C:\\Program Files (x86)\\Microsoft Visual Studio\\2022\\BuildTools\\VC\\Tools\\MSVC\\14.44.35207\\include;C:\\Program Files (x86)\\Windows Kits\\10\\Include\\10.0.26100.0\\um;C:\\Program Files (x86)\\Windows Kits\\10\\Include\\10.0.26100.0\\ucrt;C:\\Program Files (x86)\\Windows Kits\\10\\Include\\10.0.26100.0\\shared"
CARGO_INCREMENTAL=0 <cargo 命令>
```
必要时对 cargo 禁用沙箱。**Git ref 丢失**：每次 commit 后立即 `git rev-parse HEAD`；失败则用 Write 工具把**完整 40 位** SHA 写入 `.git/refs/heads/feat/v0.2.0-product-completion`（严禁截断 SHA）。**push 必须禁用沙箱**。

**编辑竞态（本会话高频复现，务必警惕）**：Edit/Write 报成功后内容可能被静默回退。每次 `pnpm check`/`cargo check` 报"未使用变量/类型不存在"时，**先 grep 验证文件实际内容**再补编辑，不要盲目重写整文件。

**测试坑**：`ImportCandidate::detected` 第 5 参是名称——同一 fixture 导两个同名 Skill 会触发 `decision` InvalidInput，批量场景每个 Skill 用独立名称。本机 `symlink_dir` 返回 Ok 但实际不落盘——符号链接相关测试须"链接未真正落地则跳过"。

## 已完成基线（不要重做）

Q1~Q17 全队列 + 原生缺口 N1~N8 已完成（43 个提交，HEAD `d46019f`），详见《当前开发状态.md》第二节。重点已交付：
- 发现/导入：skills.sh 修复、仓库发现、lock 导入、后台导入、批量冲突分组。
- 管理/详情：操作记录、忽略项/修复预览、来源更新决策、来源重关联、远端 git 来源哈希检测（N7b）、**批量来源更新检查（N8，`check_source_updates` + 库页四组汇总）**、AI 检查进度/取消（N3）、导出格式文件夹/ZIP（N4）、外链打开器（N1）、按 Skill 操作历史（N2）、组合 CRUD/导出（N5a）、批量操作统一汇总（N5b）、概览按标签下钻（N6）、grant 链路修复、滚动备份、卸载备份、卡片/矩阵/标签分组、偏好持久化。

## 任务队列（按序执行；做完即划掉并回遗漏清单找新缺口）

**N9 FE-13 剩余：待处理中心深度**（P0）
缺快捷/保存视图、风险/时间/影响字段、暂缓（snooze）语义、处理历史视图、安全项批量处理。先评估后端契约缺口（批量安全处置无命令），按"无契约的操作只显示原因和下一步"原则诚实补全；每步 TDD。

**N10 FE-15 剩余：从 Skill 详情发起导出**（P1）
N4 已有格式选择（文件夹/ZIP）、N5a 已有组合导出；缺 Skill 详情页发起单 Skill 导出入口（复用 create_standard_export，selection={skills:[id]}）与可选版本选择。小任务，优先做。

**N11 FE-16 剩余：集中库页打开目录**（P1）
集中库页缺"打开目录"（需评估路径 grant/open 能力复用 N1 的 opener 或新契约，禁止绕过 PathPolicy）、迁移流程与恢复点承接。打开目录涉及文件系统能力，先查现有 grant 链路。

**N12 FE-05 抽屉占位模块**（P1，较大）
快速抽屉的依赖、重复、外部变更、使用证据仍是占位。依赖/确定性重复有后端基础（duplicate service），外部变更需评估操作日志维度。建议拆小步：先"确定性重复"模块接真实读模型。

**队列空了**：回到《前端页面功能与UI遗漏清单》逐行重读找新缺口；或对已完成能力补边界/异常测试。总之不要停。

## 并行规则

最多 3 个子代理；只交实现体（不 commit/push）；共享热点文件（i18n/*/common.json、api/bindings.ts、styles/base.css、router）归主代理；i18n 合并后跑全量 typecheck 兜底。文件集不相交才并行。

## 里程碑同步

每完成一批（2~3 个任务）：更新《当前开发状态》《验收矩阵》《验收指南》（1u 之后新增 1v 起编号）《遗漏清单》对应行、《人工验收问题记录》追加；过时文档移入 `docs/development/archive/` 并在状态文档记一笔；确认全部推送、测试全绿。

## 人工验收提醒

1c~1u 节与《缩放验收矩阵》全部待人工执行；不要把自动化通过当成验收通过；"文件出现在目录中"不代表 Agent 已加载 Skill。N8 批量检查（1u）与 N7b 远端检测（1t）需联网，建议同场景执行。
