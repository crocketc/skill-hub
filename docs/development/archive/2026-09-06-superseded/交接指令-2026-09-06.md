# 交接指令：SkillHub v0.2.0 继续开发（2026-09-06）

> 用法：将分隔线以下全部内容作为 /goal 指令发给新会话的 Agent。
> 生成时间：2026-09-06，候选提交 `e1977b8d`。Q1~Q17 全队列已完成，本指令聚焦剩余缺口与深挖。

---

## 角色与总体目标

你是 SkillHub（Tauri 2 + React 18 + TypeScript，Rust 工作区 crates/）的开发 Agent。**持续自主开发，不要停下来向用户提问、不要请求人工验收**。遇到需要产品决策的点：选择最符合已有文档精神的方案，把决策与理由追加到 `docs/development/人工验收问题记录.txt`，然后继续。

工作目录：`C:\vibe_coding\skill-hub`，分支 `feat/v0.2.0-product-completion`。

**运行模式**：用户会自己决定何时停止。每个任务必须是完整可验证的闭环（测试 → 检查 → 提交推送 → 问题记录）；任何时刻停下工作区都应是干净的。若用户发出"准备停"类信号：20 分钟同步文档 + 输出最终报告后结束。

## 第一步（必做）

1. `AGENTS.md`
2. `docs/development/当前开发状态.md`（权威现状）
3. `docs/development/前端页面功能与UI遗漏清单-2026-09-05.md`（FE-01~FE-18 逐行状态）
4. `docs/development/人工验收问题记录.txt`（全部反馈与修复记录）
5. `docs/development/v0.2.0-manual-acceptance-guide.md`（1c~1k 节为已实现待验收项）
6. `docs/development/缩放验收矩阵.md`（待人工执行）
7. 历史计划与任务报告已归档至 `docs/development/archive/`（superpowers 计划/规格、task-reports、旧交接与规格），需要考古时去那里找。

然后 `git status --short` 与 `git rev-parse HEAD` 确认分支状态（当前候选 `e1977b8d` 或更新）。

## 硬性约束（不可违反）

1. **禁止触碰** `apps/desktop/src-tauri/Cargo.toml` 的未提交改动（用户保留的 `features=[]`）——不得提交、还原、叠加修改；新 Rust 依赖加到对应 crates/ 的 Cargo.toml。
2. **不合并 main、不创建/发布 tag、不做发布预检**。
3. 每任务独立提交，英文动词开头；提交前必跑：相关测试 → `pnpm --dir apps/desktop check` → `git diff --check`；改前端加跑 build；改 Rust 加跑 `cargo fmt -p <crate>` + `cargo check` + 相关测试。**以 check 退出码为准，测试通过不能替代**（前序 Agent 曾三次在 check 失败下误提交）。
4. **TDD**：先写失败测试再实现；成功/失败/取消/重复路径都覆盖。
5. **禁止手写 bindings**：契约变更后 `SKILLHUB_WRITE_BINDINGS=1 cargo test -p skillhub-desktop generate_bindings` 再生成。
6. i18n：zh-CN 与 en-US 键集完全一致（有 parity 测试）；多个写入方并行后必须跑全量 typecheck 兜底（曾发生键丢失竞态）。
7. 预览/读取类 UI 不得写文件；写操作必须显式确认；能力缺失时诚实降级并记录，不伪造。
8. 动手前查 `apps/desktop/src/api/bindings.ts` 与 `crates/skillhub-application/src/lib.rs`，不重复实现已有契约。

## 环境坑（实测，直接照做）

**MSVC 链接**：
```bash
MSVC="/c/Program Files (x86)/Microsoft Visual Studio/2022/BuildTools/VC/Tools/MSVC/14.44.35207"
export PATH="$MSVC/bin/Hostx64/x64:$PATH"
export LIB="C:\\Program Files (x86)\\Microsoft Visual Studio\\2022\\BuildTools\\VC\\Tools\\MSVC\\14.44.35207\\lib\\x64;C:\\Program Files (x86)\\Windows Kits\\10\\Lib\\10.0.26100.0\\um\\x64;C:\\Program Files (x86)\\Windows Kits\\10\\Lib\\10.0.26100.0\\ucrt\\x64"
export INCLUDE="C:\\Program Files (x86)\\Microsoft Visual Studio\\2022\\BuildTools\\VC\\Tools\\MSVC\\14.44.35207\\include;C:\\Program Files (x86)\\Windows Kits\\10\\Include\\10.0.26100.0\\um;C:\\Program Files (x86)\\Windows Kits\\10\\Include\\10.0.26100.0\\ucrt;C:\\Program Files (x86)\\Windows Kits\\10\\Include\\10.0.26100.0\\shared"
CARGO_INCREMENTAL=0 <cargo 命令>
```
必要时对 cargo 禁用沙箱。**Git ref 丢失**：每次 commit 后立即 `git rev-parse HEAD`；失败则用 Write 工具把完整 SHA 写入 `.git/refs/heads/feat/v0.2.0-product-completion`。**push 必须禁用沙箱**。

## 已完成基线（不要重做）

Q1~Q17 全队列已完成，详见《当前开发状态.md》第二节。重点已交付：skills.sh 修复、仓库发现、lock 导入、后台导入、操作记录、忽略项/修复预览、来源更新决策、导出/退出管理、Agent/项目/安全深度、偏好持久化、卡片视图/关系矩阵/标签分组、卸载备份、grant 链路修复、概览下钻。

## 任务队列（按序执行；做完即划掉并回遗漏清单找新缺口）

**N1 原生缺口：README/外链通用打开器**
前端 README 链接与"打开官方发布页"在打包 WebView 中可能无响应。新增契约 `open_external_url { url }`（仅允许 https，白名单校验），宿主用 rfd/tauri opener 打开；替换 `open_official_release` 的"仅校验不打开"占位。前端 RepoDiscovery/技能详情 README 链接改为走该契约。TDD：Rust 校验测试 + 前端调用断言。

**N2 原生缺口：按 Skill 操作历史查询**
新增查询 `list_skill_operations { skill_id }`（operation_repository 已有存储，按 skill 过滤需评估——若无 skill 维度则评估在操作记录里加 skill_id 列或诚实降级为全局过滤）。前端：Skill 详情/操作记录页承接。TDD。

**N3 原生缺口：AI 检查进度/取消**
`run_llm_safety_check` 运行中接 `cancel_operation`（契约已有），安全页 AI 卡加进度与取消入口。TDD。

**N4 原生缺口：导出格式（文件夹/ZIP）参数**
ExportInput 增加格式字段（默认文件夹保持兼容），export.rs 实现 ZIP 打包（复用 zip crate）。前端数据保护页导出表单加格式选择。TDD（Rust + 前端）。

**N5 FE-04 剩余：组合视图 / 批量操作统一汇总**
组合 CRUD/导出/部署入口；批量操作（导入/部署/删除）统一的可执行/跳过/冲突/失败汇总组件。较大 UI 建设，建议拆成 N5a/N5b 两个提交。

**N6 FE-02 剩余：指标级更细筛选下钻**
指标卡与柱形图下钻已有基础（`1721a35`）；补"按标签"等更细的下钻筛选（库页 URL 参数已支持 tags）。

**N7 Q17 更新检测（需决策）**
远端 GitHub 来源哈希对比：需先把 GitHub 来源做成长期来源记录（RelinkSource 或导入时记录 owner/repo/branch/directory——仓库导入现在来源指向临时目录）。先向用户提出哈希/坐标记录方案决策，再实现。算法参考 cc-switch `skill.rs` `check_updates`（源码在 `C:\project\cc-switch`），目录哈希 sha256 排序文件可移植。

**队列空了**：回到《前端页面功能与UI遗漏清单》逐行重读找新缺口；或对已完成能力补边界/异常测试。总之不要停。

## 并行规则

最多 3 个子代理；只交实现体（不 commit/push）；共享热点文件（i18n/*/common.json、api/bindings.ts、styles/base.css、router）归主代理；i18n 合并后跑全量 typecheck 兜底。文件集不相交才并行（参考：库页与发现页可并行；详情相关串行）。

## 里程碑同步

每完成一批（2~3 个任务）：更新《当前开发状态》《验收矩阵》《验收指南》（新增 1L 起编号）《遗漏清单》对应行、《人工验收问题记录》追加；过时文档移入 `docs/development/archive/` 并在状态文档记一笔；确认全部推送、测试全绿。

## 人工验收提醒

1c~1k 节与《缩放验收矩阵》全部待人工执行；不要把自动化通过当成验收通过；"文件出现在目录中"不代表 Agent 已加载 Skill。
