# SkillHub 当前交接

交接日期：2026-09-05  
状态：继续开发 P0；暂不进入全量人工验收或发布  
分支：`feat/v0.2.0-product-completion`  
当前远端提交：`d59b02d`（推送后以远端查询为准）  
功能基线提交：`d59b02d`  
工作目录：`C:\vibe_coding\skill-hub`（单一工作目录，非额外 worktree）

## 先读什么

1. `AGENTS.md`
2. [当前开发状态.md](./当前开发状态.md)
3. [前端页面功能与UI遗漏清单-2026-09-05.md](./前端页面功能与UI遗漏清单-2026-09-05.md)
4. `docs/需求文档.md` 和 `docs/产品与交互设计.md` 中与本任务对应的章节

不要使用已归档的 `docs/development/archive/发布版本需求差异审查-2026-09-02.md` 判断现状；它的 `v0.1.0/main` 基线已经失效。

## 当前代码状态

已接通且经近期自动化验证的用户路径：

- 首次初始化、可重新运行向导、Agent 识别和只读扫描。
- 扫描后的多来源批量导入、本机目录选择、冲突决策、导入进度与错误明细。
- 集中库 `skills` 内容物化、单 Skill 部署/解除部署、单个删除和带二次确认的批量删除。
- Agent 链接优先/复制回退、项目复制默认策略、窄窗口概览布局。
- 技能库的真实筛选、排序和关键状态读模型（FE-03，`ba9440e`）。
- Agent 按品牌的真实状态/受管部署统计列表与重新扫描，以及详情内对应的状态和部署数（FE-08 已完成此读模型部分，`239e8ec`、`104008c`）。
- 项目页的本机目录选择、已发现 Agent 关联、真实注册与详情页重新关联（FE-09 已完成此部分，`68abc75`、`da311fd`、`e58ab90`、`c32fc0d`）；注册不导入 Skill，也不写共享项目配置，取消关联不删除项目或 Agent。
- 待处理可按试用/安全/恢复筛选，且仅展示该类型真实支持的明确动作；执行期间会防重复与并发提交（`ec7fb49`、`4a90c02`、`1a545c4`）。
- 设置页可即时保存语言、主题、信息密度与三类自动检查；不再显示伪“测试提供商”操作（`10bca0e`、`0ae5a86`）。
- 技能库所选多个 Skill 可统一进入多 Agent 或注册项目部署，逐 Skill 预览全部成功后才允许提交；项目自动模式默认复制（`3041747`、`42854ea`）。

尚未完成的页面能力以遗漏清单为准。当前决策：先继续补齐 P0，不进行全量人工验收；每个独立功能仍须有相应自动化验证，涉及真实目录才做定向真机验证。下一轮优先顺序如下：

1. **FE-09 注册前只读扫描预览（下一项，固定优先）。** 在目录选择后、正式注册前完成“目录痕迹 → 候选 Agent → 只读扫描预览 → 确认关联”；预览阶段不得创建项目、导入 Skill 或写项目文件。随后再补忽略和解除管理。
2. **FE-08 剩余部分 / FE-10：Agent 管理与多目标部署的统一闭环。** 多 Skill → 多 Agent/注册项目已接通；后续补重新定位、忽略、自定义目录管理，以及从 Agent/项目反向发起入口与项目装配。
3. **FE-13 / FE-16：待处理中心和设置页。** 待处理已具备类型筛选与明确动作，设置已具备语言/主题/密度/自动检查；后续补暂缓/历史、集中库迁移/健康检查和更细的视图/自动化配置。

选择一个明确任务，避免将上述三个方向混在同一个提交中。

## 必须保留的本地状态

- `apps/desktop/src-tauri/Cargo.toml` 有用户未提交改动：`tauri` 依赖增加了空的 `features = []`。它当前不改变行为，且不属于已完成功能；不要提交、重置或删除，除非用户明确授权。
- `docs/development/` 和需求/设计文档被 `.gitignore` 忽略，仅存在当前设备。不要期待它们会通过远端分支同步。
- 本轮已完成 FE-01（`cd5eb7f`）、FE-07（`7a80f3e`）、FE-16（`c6b8d10`）三个独立提交并推送，当前分支应与 `origin/feat/v0.2.0-product-completion` 同步；接手时请先复核。
- 本会话曾出现沙箱丢弃 `.git/refs` 写入的现象（提交对象在、分支 ref 丢失导致 HEAD 暂时无法解析）。已通过直接写入完整 SHA 的 loose ref 恢复。若接手时 `git status` 显示大量新增文件或 HEAD 报错，先检查 `.git/refs/heads/feat/v0.2.0-product-completion` 是否存在且为完整 40 位 SHA。

## 已知验收边界

- 已修复先前人工验收中暴露的扫描/导入/路径/冲突/滚动问题，但还没有在最新候选提交上完成一轮全新的、全流程人工复验。
- 本轮新增入口均为前端复用既有契约：三分支向导的“使用已有库”分支依赖只读初始化扫描（会写扫描缓存，不创建受管对象）；备份恢复分支会真实执行恢复，人工验收请在临时集中库上进行。
- 真机 Agent/项目部署、备份恢复、导出文件、Windows/macOS 安装、签名更新和回滚均未完成发布级取证。
- 不能把自动化通过或“文件已写入目标目录”表述为 Agent 已加载、调用或验证可用。

## 推荐的开始步骤

```powershell
git status --short
git log -1 --oneline
pnpm --dir apps/desktop test --run <相关测试文件>
pnpm --dir apps/desktop check
git diff --check
```

涉及 Rust 契约时，先运行对应 crate 的定向测试；生成的 TypeScript bindings 必须由生成工具更新，不能手写重复契约。修改行为时遵循 TDD：先增加失败测试，再实现最小闭环。

## 提交和汇报要求

- 每项独立工作用清晰英文动词开头的提交信息；避免把用户的 `Cargo.toml` 改动混入。
- 完成后报告：修改文件、测试命令与结果、提交哈希、未解决风险和需要用户人工验收的动作。
- 未补齐完整质量门禁与真机发布证据前，不合并 `main`，不创建或发布 `v0.2.0` tag。
