# 交接指令：SkillHub v0.2.0（N9 前置修复后）

分支：`feat/v0.2.0-product-completion`，当前提交 `2fc5179`。先执行 `git status --short`：用户的 `apps/desktop/src-tauri/Cargo.toml` 和 `.workbuddy/` 不得触碰；N9 忽略规则前置修复已独立提交并推送。

必读顺序：`AGENTS.md` → `docs/development/当前开发状态.md` → `docs/development/功能完成度与验收状态-2026-09-06.md` → `docs/development/人工验收问题记录.txt` → `docs/development/v0.2.0-manual-acceptance-guide.md`。

当前修复的真实边界：忽略规则已改为 SQLite `settings` 持久化；待处理列表读取 exact_pending 与 exact_skill 规则，永久忽略或未到期的暂缓会隐藏事项，到期当天重现。`exact_path` 不影响待处理事项。不要把此基础修复说成“FE-13 完成”。

后续 N9 子任务提交前至少运行：

```powershell
cargo test -p skillhub-application --test facade
cargo test -p skillhub-storage
cargo clippy -p skillhub-storage -p skillhub-application --tests -- -D warnings
git diff --check
```

提交只包含当前 N9 子任务的代码与测试；不要包含 Cargo.toml、`.workbuddy/` 或 `docs/development/`（本地忽略资料）。推送后更新当前状态、验收矩阵和人工验收问题记录。

后续顺序：继续 N9 待处理中心深度（快捷/保存视图、风险/时间/影响、处理历史；无原生批量安全处置契约时必须诚实降级），然后 N10 单 Skill 详情导出、N11 集中库目录/迁移、N12 抽屉确定性重复读模型。i18n 保持“部分实现”直到全页面与错误反馈覆盖完成。
