
2026-09-04 / 候选提交 e6a3089

P0-01 初始化向导：默认集中库路径展示、全选可用 Agent、只读扫描、重新运行向导和完成后回到主界面均已验证。自定义集中库路径仍不能通过系统目录选择器更换，原因是 Tauri 原生 picker/grant 能力尚未接入；该项记录为未通过/待实现。

补充：在最新提交上，人工点击全选后扫描仍显示“无法完成扫描”。同机真实 facade 复现可完成扫描且 errors=0，需继续核对桌面运行实例、调用返回或超时边界；暂不判定 P0-01 扫描通过。

P0-02 发现/导入：当前只能手动输入本地目录路径，不能直接打开系统目录选择器；记录为未通过/待实现。技能库空态已提供“打开发现”入口。

---

2026-09-05 / 候选提交 c6b8d10 / FE-01、FE-07、FE-16 定向验收

通过项：
- 通过（1b-1 部分）：从备份恢复分支可正常返回（取消不提交）；“恢复并继续”能完成导入并完成初始化。
- 通过（1b-2 部分）：发现页显示上次扫描信息；“重新扫描”出现五类汇总。
- 通过（1b-3）：运行健康检查按钮正常，显示“未发现问题”。

问题（按序号记录用户原始反馈）：
1. 【全局-布局】100% 缩放下多页内容超出一屏但无滚动条，无法滚动；缩小比例后才有滚动。预期：滚动条由内容是否溢出决定，与缩放比例无关。
2. 【全局-导航】从主导航进入功能页后再点击跳转，页面上没有“返回上一页”，只能点主导航重新进入。预期：子页面提供返回/面包屑，能回到上一页。
3. 【FE-01-体验】备份恢复“恢复并继续”成功但偏慢。已确认为同步执行：UI 等待 commit_restore 完整结束，不是“先跳转后台继续”。待设计：恢复进度反馈或后台化（需契约扩展）。
4. 【FE-01-缺口】新建集中库不能自选系统目录（只有默认路径），应接入系统目录选择器；另：新建分支的“确认集中库”与“主题”页缺“上一步”按钮，只能继续/跳过。注：自选库根路径需新契约（complete_onboarding 目前只接受固定库根）。
5. 【FE-01-交互】识别 Agent 在 Agent 较多时较乱，希望按品牌分组，品牌下再分 CLI/桌面端，突出品牌层级。
6. 【FE-07-文案】五类汇总中“已有关系”与“疑似”含义不直观，需解释或改名+悬浮说明。含义：已有关系=已与客户端建立健康关联的目标目录数；疑似=扫描发现的内容指纹相同（疑似重复）的 Skill 数。
7. 【通过】发现页上次扫描信息显示正常。
8. 【设计+故障】本地发现的“导入 Skill”与在线发现的是同一导入向导，逻辑应区分：本地发现应融合目录解析、zip、URL、Git 地址、npx 来源解析；在线发现应是独立联网检索能力（可配置检索源，初期 skills.sh）。另：真实环境搜索报“搜索失败”。已定位根因：skills.sh API 已改为必须 Vercel OIDC 认证（匿名请求返回 401 authentication_required），桌面端无法自行获取该 token，需要策略决策（如部署 Vercel 代理）。
9. 【导入-Bug】初始化后批量导入识别到多个扫描地址，但来源框只显示第 1 个地址；多选/全选场景下不应只显示一个，除非用户手动自定义目录。
10. 【导入-Bug】“读取已选目录候选”在全选/多选时仍只识别第 1 个地址，逻辑有问题。
11. 【设计】处理冲突页需明确：冲突是待导入 Skill 与集中库对比；数量大时应全部校验；冲突明细要细化（谁和谁冲突、所属路径/品牌、版本）；应支持按冲突类别批量处理+单个处理。前后端均需设计。
12. 【设计】导入流程正常；大量 Skill 时希望支持后台静默导入，避免等待。
13. 【通过】健康检查显示“未发现问题”。

初步分诊：
- 前端可修（无需新契约）：#1 滚动、#2 返回导航、#9/#10 导入多来源 Bug、#6 文案+悬浮说明、#5 品牌分组（纯前端可先做分组展示）。
- 需 Rust 契约扩展：#4 自选库根、#3 恢复进度/后台化、#11 冲突明细校验、#12 后台导入、#8 在线检索策略。
- 需策略决策：#8 skills.sh 认证问题（外部 API 变更）。


【用户决策 2026-09-05】修复顺序：先 A 批（前端 Bug），再 C 批（契约扩展）；B 批（skills.sh 认证）记为技术债，等找到可行方法（如 Vercel 代理）再处理。

【A 批修复 2026-09-05 / 提交 a16f1fb，已推送】
- #1 已修复：主内容区改为 overflow-y auto，滚动条由内容溢出决定；布局契约测试已更新。
- #2 已修复：顶栏在子路由（Agent/项目详情、部署、安全、操作详情、数据保护）显示“返回”按钮，优先历史回退、无历史时回到父级页签。
- #5 已修复：识别 Agent 按品牌（profile_id）分组，品牌下标注客户端形态（命令行/桌面应用/…）。
- #6 已修复：五类更名“已关联目录”“疑似重复”并增加全部五类的悬浮说明。
- #9 已修复：多来源预选时来源框不再预填第一个地址，仅单来源或用户自选时显示。
- #10 已部分处理：采集后候选确认页显示每个来源目录的候选数；前端逻辑经测试验证支持多来源。若真机仍只识别第一个，候选页的分组计数可直接暴露问题来源，便于定位 Rust 侧（发现页技能候选 discover_import_candidates 按 root 独立检测，前端循环已验证）。
- #8 技术债：skills.sh API 需 Vercel OIDC 认证，桌面端无法直接调用；待定检索策略（Vercel 代理/抓网页/降级）。

【C 批修复 2026-09-05 / 提交 ac5b6b2 + 2f2be9b，已推送】
- #4 已修复（ac5b6b2）：新建集中库支持"选择其他目录"（系统选择器）→ 保存并重启生效；已初始化后拒绝变更（防误迁移）；create 分支第 1 步补"上一步"。新契约：set_library_root + restart_application。
- #3 已改善（2f2be9b）：恢复提交期间显示"正在恢复 N 个 Skill…（已用时 X 秒）"的诚实忙碌态；真正的后台恢复需要全局操作框架，记为后续。
- #11 已改善（2f2be9b）：冲突页按冲突类别分组（显示每类数量），支持"按类别批量处理"一键应用同一处理方式，单个仍可覆盖；每条冲突显示候选来源路径与所冲突的已有 Skill（复用 analyze_import 的 matches 数据，无需 Rust 改动）。
- #12 待办：后台静默导入需要把导入提交从向导局部状态提升为全局操作（进度、完成通知、离开页面继续），属于操作框架改造，暂缓。

【Q1 修复 2026-09-06 / 提交 8916f3a，已推送】
- #8 技术债（skills.sh 401）已修复：按发现模块实现规格 §5 将端点改为 https://skills.sh/api/search（legacy 响应形状：skills[]/{id,skillId,name,installs,source}+count+duration_ms），source 用 splitn(2,'/') 拆 owner/repo 并经仓库坐标白名单校验（新增 repo_ref 模块，§4 逐字保留+规格 §11 要求的 .lock/. 开头段拒绝），page_url 映射为 https://skills.sh/{source}/{skillId}，搜索超时 10s。对外契约 SourceSearchPage 形状不变，前端零改动。注意：规格 §4 函数与 §11 测试表存在两处不一致（seg.lock、.hidden/x），已按 §11（更强方向）补齐规则。

【Q2 新增 2026-09-06 / 提交 2e7176f，已推送】
- 仓库发现（规格标准版）已实现：crates/skillhub-adapters 新增 repo_discovery（§6.1 并行发现+单仓库失败仅告警、§6.2 下载/解压预算常量与函数逐字保留、§6.4 递归扫描+根目录 SKILL.md 用仓库名、§6.5 去重；GitHub zip 顶层包装目录按 git.rs 先例展平；sanitize_skill_source_path 逐字保留）。新契约：query= list_skill_repos / discover_repo_skills，command= add_skill_repo（upsert）/ remove_skill_repo / download_repo_skill（下载到 %TEMP%/skillhub-repo-skills/<id>/<name>，超 24h 残留尽力清理），bindings 已再生成。默认 4 仓库（anthropics/skills、ComposioHQ/awesome-claude-skills、cexll/myclaude、JimLiu/baoyu-skills）存 settings 表，首读自动播种。
- 前端发现页新增"仓库发现"卡片：仓库列表（启用开关=upsert、显式确认后移除、添加仓库）、"扫描仓库"结果列表（名称/描述/owner/repo@branch/README 链接）、"下载并导入"把下载目录以本地来源身份送入现有导入向导（下载本身不建受管对象，导入物化才写集中库）。
- 测试：adapters 集成 8 项（含 512MiB 预算/1 万条目/超长 symlink、分支回退、单仓库失败隔离、下载仅复制目标目录、穿越拒绝）+ lib 11 项；application facade 6 项（默认播种、upsert 排序、移除防重、非法坐标不联网、网络开关拦截）；前端 6 项。全量 Rust workspace 测试通过；前端 82 文件 468 项通过；check/build 通过。
- 验收提示：测试期间 discover 曾真实联网成功发现 cexll/myclaude 的 11 个真实 Skill（链路端到端可用）；正式验收请用网络开关=关来验证拦截路径。
- 已知边界：README 链接为普通 <a target=_blank>，打包后的 WebView 可能不响应点击（应用尚无通用外部打开器命令，open_official_release 也仅做校验）；发现查询为大流量操作（每仓库整仓下载），前端展示"正在扫描"诚实等待。

【Q3 完成 2026-09-06 / 提交 4db9341，已推送】
- FE-01 完成页汇总已实现：初始化完成后停留在汇总页，展示识别 Agent 数（含已选数）、扫描来源目录数、发现 Skill 数、跳过数（未变化+出错，诚实口径）；跳过扫描/跳过初始化/恢复分支按实际执行情况显示说明，不显示冒充的 0。
- 流程调整：完成后不再自动跳转，汇总页提供"打开批量导入"（携带扫描 roots，语义不变）与"进入主界面"两个显式按钮。
- 测试：OnboardingWizard 测试 16 项（新增计数正确、延迟进入、roots 传递、skip/restore 不误导）；i18n 双语 parity 通过。

【M2 批次 2026-09-06 / 提交 64ce4ede + 8320c6fe + 0febbda3，已推送】

【Q6 忽略项 UI + 修复预览 / 64ce4ede】
- 设置页技能库卡片新增：忽略项管理（list_ignore_rules 列表 + create_ignore_rule 三类主体 exact_path/exact_skill/exact_pending + 理由必填 + 确认后 remove_ignore_rule）；健康检查每个 finding 的"修复预览"（prepare_repair → RepairPlan 明细 → 显式"执行修复" → commit_repair），无可修复动作的 finding 不显示按钮，取消预览不写任何数据。
- 产品决策记录：DesktopPreferences 无"AI 输出语言"与"减少动效"字段，按队列指令诚实跳过（需要 Rust 契约扩展时再评估）；IgnoreSubject 实际形状为 {type, value}（非 payload），以 bindings 为准。

【Q4 操作框架地基 + 后台静默导入 / 8320c6fe】
- 验收反馈 #12 已修复：新增模块级全局操作跟踪器（platform/operationTracker，useSyncExternalStore 订阅），ImportWizard 提交循环接入——提交期间可离开页面/切换路由，循环继续并在全局指示器（应用壳 OperationIndicator，最近 3 条）显示进度与结果（成功/失败/跳过计数）。
- 提交中界面增加"可以离开此页面"提示；测试含"卸载后循环继续完成并写入 tracker"的关键用例（5 项 tracker + 3 项指示器 + 向导用例）。

【Q5 操作记录列表 + 恢复双页签 / 0febbda3】
- 恢复页改为"操作记录 / 备份恢复"双页签：操作记录页签合并两类事实并诚实标注来源——本会话后台操作（tracker）+ 持久化最近操作（BootstrapSnapshot.recent_operations，链接到既有操作详情页）；备份恢复页签承接原恢复确认流程并注明完整备份/恢复流程在数据保护页。
- 空态/读取失败均为诚实提示，不伪造历史；空记录与读失败分开呈现。

【M2 全量验证】前端 86 测试文件 492 项通过；pnpm check、pnpm build、git diff --check 通过。无 Rust 改动。i18n 并发合并期间曾丢失 HEAD 的 operations.completed 键（两语言），已从 HEAD 恢复并验证 parity。

【Q7 来源更新检查与决策（待与 Q8 一起提交推送）】
- Skill 详情"版本"区块新增"来源更新"面板：点击"检查来源更新"后按契约返回的五种状态如实呈现（已是最新/发现更新/发现更新且本地有修改/来源不可用/需要认证）；有更新时提供四个显式决策按钮（采用上游/保留本地/建独立分支/取消），对应 apply_source_update 的 UpdateDecision；应用后显示结果（含新版本号与"部署需要重新同步"提示）。
- 范围决策：IgnoreExternalChange 是部署维度契约（按 deployment_id），详情页当前无部署行 UI，入口留在部署相关页面（Q9/Q11）一并处理；批量更新检查无对应契约，不做批量入口（诚实边界）。

【M3 批次 2026-09-06 / 提交 4d327bd9 + 9a79a575，已推送（Q6 见 M2 批）】

【Q7 来源更新检查与决策 / 4d327bd9】
- Skill 详情"版本"区块新增"来源更新"面板（SourceUpdatePanel，5 项测试）：点击检查后按契约五种状态如实呈现（up_to_date/update_available/update_available_with_local_changes/source_unavailable/authentication_required）；有更新时提供显式决策按钮（采用上游/保留本地/建独立分支/取消）→ apply_source_update；应用后显示新版本与"部署需要重新同步"提示。
- 范围决策：IgnoreExternalChange 为部署维度契约（按 deployment_id），详情页无部署行 UI，入口留待部署相关页面（Q9/Q11）；批量更新检查无契约，不做批量入口（诚实边界）。

【Q8 批量导出入口 + 退出管理 / 9a79a575】
- 技能库批量栏新增"发起导出"：显式选择直接携带 skillIds 跳转数据保护页；"全部筛选结果"经分页枚举全部 id 后跳转，失败留在库页并提示。
- 数据保护页承接预填：逐技能 list_versions 预检并标注"可导出（当前版本）/无当前版本/版本读取失败"，不伪造可导出性；无 state 时行为不变。
- 退出管理分步：复选部署关系（list_deployments 全量）→ prepare_uninstall 预览影响（部署数/可用动作/preserves_central_library）→ 显式勾选动作（默认全不选）→ apply_uninstall_decision → 显示结果 phase；各步失败走结构化 alert。
- 诚实边界：ExportSkill.content 前端无法合法构造，仍按契约发 skills:[] 由后端从 selection+current 解析（Rust export_input 已有该逻辑）；后端将 backup/standard_export/remove_device_data/clear_credentials 暂标记 unsupported，违例组合显示后端结构化错误。
- 测试：Q7 5 项新增 + Q8 库页 3 项 + backup 7 项；M3 后全量前端 87 文件 507 项通过，check/build 通过。

【M4 批次 2026-09-06 / 提交 e7e648f6(Q12) + 1a9188d8(Q9) + 49eba85a(Q10) + 5b5083f3(Q11) + 33cd2219(i18n修复)，已推送】

【Q9 Agent 管理剩余 / 1a9188d8】
- 受管部署数按唯一 Skill 聚合（如"3 个 Skill · 5 条部署关系"）；自定义 Agent 新增/编辑/删除入口接通（删除需确认）；发现态 Agent 无管理入口（不伪装）。
- 诚实降级：重新定位目录、Agent 级忽略均无原生契约，详情页显示"需要原生契约支持，暂未提供"。
- 【重要发现】自定义 Agent 创建/编辑的 directory 契约只接受 grant_id，需宿主先 register_path_grant，而桌面宿主目前从不注册 grant（目录选择器只返回路径字符串）→ 运行时创建/编辑会被原生层拒绝并如实报错。需要后续原生任务补"选择器签发 grant + 宿主注册"链路。

【Q10 项目详情剩余 / 49eba85a】
- 项目详情新增"路径与访问状态"面板（device_path + 可访问/只读/不可访问徽标，快照无对应物理目标时显示"未记录"，读取失败显示"未知"）；装配计划按状态分组展示（已满足/已成功/待获取/冲突待选择/已跳过/失败），无计划显示诚实空态；注册预览候选按目录痕迹亲和度排序（同前缀/同根优先，悬浮说明注明依据）。
- 诚实降级：detach_management 需要 deployment_id，项目详情无部署记录落点，显示说明文案不伪造按钮。

【Q11 反向部署 + Agent 目标展开 / 5b5083f3】
- Agent 详情/项目详情新增"发起部署"按钮 → 跳转技能库并预选目标 → 勾选 Skill 后"统一部署"自动携带该目标（?target= 参数）。
- 批量部署页支持项目目标"展开关联 Agent（N 个）"一键加入；页面固定提示"批次逐个提交，不是原子操作：部分成功不会自动回滚"。

【Q12 安全结果深度 / e7e648f6】
- 发现项按基础/AI 分组，展示 file/line 证据（无定位显示"位置不可用"）；AI（llm）发现项处置接通（kind="llm"）；高风险处置必须经独立确认对话框（high_risk_confirmed 仅确认后为 true，native 层再加本地拒绝）；AI 卡显示发送范围说明（data_scope=explicit_selection 显示"仅发送显式选择的 Skill 内容"）；未配置 LLM 时运行入口禁用并说明原因。
- 降级：后端无批量处置命令，无批量入口；AI 检查运行中无进度/取消（cancel_operation 未接），仅按钮 loading。
- 流程问题记录：三个并行任务合并 common.json 期间曾丢失 agents.launchDeployment 键（合并时序竞态），已由 33cd2219 恢复；后续并行 i18n 合并需在完成后跑全量 typecheck 兜底。

【M4 全量验证】前端 88 测试文件 565 项通过；pnpm check、pnpm build、git diff --check 通过。无 Rust 改动。

【M5 前半批次 2026-09-06 / 提交 6b974017 + d14a274 + 2b5ed31e + 4bdf6cb + 6d95495b，已推送】

【Q13 来源重新关联 / 6b974017 + d14a274】
- Skill 详情元数据区块新增"来源重新关联"面板：输入本地/Git/HTTPS 地址（前端按规则分类构造 SourceDescriptor）→ relink_source；成功显示"已重新关联来源"，失败显示结构化错误。修复了接线时元数据三元表达式被破坏的渲染问题（d14a274）。
- 版本差异与回滚影响此前已在版本时间线接通（diff/impact 查询），本轮核实无缺口；按 Skill 的操作历史无契约，暂不做（全局操作记录已在恢复页提供）。

【Q14 抽屉真实数据 / 2b5ed31e + 4bdf6cb】
- 快速抽屉的快速视图从纯占位升级为真实读模型：get_skill 填充身份/描述/标签/许可证/生命周期/当前版本；按当前版本查询基础与 AI 检查状态；get_deployment_relations 填充部署关系（计数+名称）。无当前版本时检查如实显示 not_run；部署关系读取失败保持占位零值，不伪造。
- 流程问题记录：本次又在 check 失败的情况下完成了提交（快速视图测试 mock 类型错误），已补修复提交 4bdf6cb。重申纪律：提交前 pnpm check 必须显式确认通过。

【Q16 前半：滚动备份与保留策略 / 6d95495b】
- 数据保护页新增"滚动备份与保留策略"区块：保留份数输入 → run_rolling_backup（scope=full，decisions=[]）→ 显示"保留 X 份、清理 Y 份"；失败结构化 alert。附诚实缓存清理说明（仓库发现临时目录 24h 自动清理；集中库无手动清理缓存）。

【Q15 前半：概览指标下钻 / 1721a358，已推送】
- 概览指标卡升级为可点击链接：Skill 总数 → 技能库；Agent 数 → Agent 列表；项目数 → 项目列表；已部署数 → /library?deployment=deployed（复用库页既有 URL 筛选，返回时筛选状态保持）。柱形图下钻此前已存在（点击柱形进入对应 Agent/项目详情的部署视图），本轮核实无缺口。
- 剩余诚实记录：FE-04 卡片视图/组合视图/关系矩阵、FE-02 指标级更细筛选下钻仍属较大 UI 建设，未在本轮展开。

【M5 后半批次 2026-09-06 / 提交 a2735616 + 395f9c3 + 224d3bd + 6923fcf + 5daf27e，已推送】

【UI 偏好持久化契约 / a2735616】
- 发现并修复真实缺口：表格偏好/抽屉偏好在原生 facade 上从未实现（只有 unavailable），导致设置在真实桌面端不持久化。新增迁移 0007（ui_preferences 表）+ UiPreferenceRepository + 通用契约 get_ui_preference / set_ui_preference（原始 JSON 键值存储，值必须可解析、键非空）；bindings 已再生成。
- 前端 nativeSkillLibraryFacade 接通 load/saveTablePreferences 与 load/saveDrawerPreferences（缺失键时页面回退默认值并显示提示，不伪造）。

【FE-04 前半：技能库卡片视图 / 395f9c3→5daf27e】
- 库工具栏新增"卡片视图/表格视图"切换（经 ui_preferences 持久化，重启保持）；卡片展示技能名/用途/标签/部署数，点击卡片名打开抽屉。
- 过程记录：本轮两次在 check 失败下完成了提交（JSX 条件嵌套错误、mock 类型错误、测试语言断言、未用变量），均已即时补修复提交并重新全量验证。最终以 90 文件 574 项测试 + check + build + git diff --check 全绿收口。教训：必须以 check 的退出码为准，不能用"测试通过"替代。

【M5 全量验证】前端 90 测试文件 574 项通过；pnpm check、pnpm build、git diff --check 通过。

【Q17a 卸载备份 + FE-04 矩阵/标签分组 + Q16 缩放矩阵 2026-09-06 / 提交 bbff8267 + f89339e4 + a581e850 + 1ea80f4 + 79c5e8d6，已推送】

【Q17a 卸载备份（bbff8267 Rust + f89339e4 前端）】
- 退出管理的 backup 动作从"unsupported 拒绝"改为真实实现：apply 时先按 Full 范围创建完整备份包（BackupService.prepare/create/verify，落盘到集中库 .skillhub/backups/），成功后才执行 undeploy 等写动作；备份失败整体中止、不产生半卸载。message_code 区分 uninstall.decision_applied_with_backup。
- 测试：真实部署流（PrepareDeployment→CommitDeployment）后 apply [backup, undeploy_all] → 备份包 manifest 落盘 + 部署解除；无库根时整体中止且部署未动（2 项 Rust + 1 项前端参数断言）。
- 仍不支持（如实报错）：standard_export（走数据保护页导出）、remove_device_data、clear_credentials。

【FE-04 关系矩阵 + 标签分组（a581e850 + 1ea80f4 + 79c5e8d6）】
- 技能库视图模式升级为三选（表格/卡片/关系矩阵，经 ui_preferences 持久化）：矩阵为 Skill × 已部署目标一览（✓ 标记，列取真实部署关系，标签用已注册目标名，仅显示当前页 Skill 并有悬浮说明）。
- 卡片视图新增"分组方式"（不分组/按标签，持久化）：按标签分节展示，无标签技能归入"—"节。
- 新增持久化基建：get_ui_preference/set_ui_preference 通用契约 + 迁移 0007（ui_preferences 表）——表格/抽屉偏好在原生层首次真正持久化（此前为 unavailable 占位）。

【Q16 缩放验收矩阵（文档交付）】
- 新建 docs/development/缩放验收矩阵.md：15 个页面 × 100%/125%/150% × 布局/滚动/交互/键盘/诚实状态五维度，含历史问题回归点（100% 滚动条、概览窄窗口、150% 双页签换行）与回传格式。

【验证】前端 90 文件 577 项全绿；check/build/git diff --check 通过；Rust facade 卸载备份 2 项 + 既有全部用例通过。

【原生缺口修复：grant 链路 2026-09-06 / 提交 eb72ba5e，已推送】
- 自定义 Agent 创建/编辑的目录 grant 链路已打通（此前记录的"运行时创建/编辑会被原生层拒绝"缺口关闭）：
  1. 宿主 pick_local_directory 命令在用户选取目录后，将规范化路径注册为路径 grant（grant_id ≡ 规范化路径本身，与前端展示口径一致），并以 JSON 字符串返回 {path, grant_id}；
  2. CommandBridge 新增 with_local（桌面宿主注入具体门面）与 issue_grant_for_path/register_path_grant 转发；测试替身保持 None 不受影响；
  3. 启动时 re_register_custom_agent_grants：按已存储自定义 Agent 记录里的目录路径重新签发 grant（grant 注册表在内存中，重启后恢复），编辑既有 Agent 不再需要重新拾取目录。
- 测试（src-tauri path_grant_tests，桌面 crate 16/16 全绿）：签发后创建成功且 grant_id=规范化路径；未注册时创建报结构化错误（AgentProfileInvalidCapability，错误参数含 GrantNotAuthorized 原因）；模拟重启后经启动重注册，既有 Agent 可编辑改名。
- 设计说明：grant_id 即规范化路径（自签名），因此 Q9 时期前端"以路径作为 grant 标识"的发送方式在宿主注册后即为合法；目录选择器其余消费方（初始化库根、导入向导）仅用路径，行为不变。

【验证】桌面 crate 16 项测试全绿（含新增 3 项 grant 链路测试）；前端 90 文件 577 项全绿；check/build 通过。

【Q17b ~/.agents lock 发现与导入 2026-09-06 / 提交 e1977b8d，已推送】
- cc-switch 源码（C:\project\cc-switch）已回查：lock 解析规则逐条对齐——仅取 source_type=github 条目；分支回退 branch → source_branch → source_url（/tree/ 路径、#fragment、?branch=/?ref=）；空白分支视同缺失；解析失败/文件缺失一律空列表不阻塞。
- SkillHub 适配：adapters 新增 agents_lock 解析器（纯函数 5 项测试）；新查询 discover_agents_lock_skills（只读，解析 ~/.agents/.skill-lock.json，不动该文件）；bindings 再生成。
- download_repo_skill 扩展：空 directory = 整仓即 Skill（lock 条目常无 skill_path），测试覆盖整仓复制与空/非法目录区分（空→仓库根；穿越/绝对路径仍拒绝）。
- 前端发现页新增"~/.agents lock 导入"卡片（4 项测试）：扫描 → 列出 GitHub 条目（名称/repo@分支/仓库内路径）→ 逐条"下载并导入"复用既有仓库下载预算与导入向导；空态与错误如实呈现。
- 更新检测子项说明：SkillHub 的 check_source_update（本地来源目录哈希 vs 版本树哈希）已在 FE-11（4d327bd）实现并含三态判定；远端 GitHub 来源的哈希对比在"仓库发现导入以临时目录进入向导"的现状下缺少持久化远端坐标（来源记录指向临时目录），需先将 GitHub 来源做成长期来源记录（RelinkSource 或导入时记录 owner/repo/branch/directory）——已记录为后续契约增强，不在本轮强行实现。

2026-09-04 / 候选提交 e6a3089

P0-01 初始化向导：默认集中库路径展示、全选可用 Agent、只读扫描、重新运行向导和完成后回到主界面均已验证。自定义集中库路径仍不能通过系统目录选择器更换，原因是 Tauri 原生 picker/grant 能力尚未接入；该项记录为未通过/待实现。

补充：在最新提交上，人工点击全选后扫描仍显示“无法完成扫描”。同机真实 facade 复现可完成扫描且 errors=0，需继续核对桌面运行实例、调用返回或超时边界；暂不判定 P0-01 扫描通过。

P0-02 发现/导入：当前只能手动输入本地目录路径，不能直接打开系统目录选择器；记录为未通过/待实现。技能库空态已提供“打开发现”入口。

---

2026-09-05 / 候选提交 c6b8d10 / FE-01、FE-07、FE-16 定向验收

通过项：
- 通过（1b-1 部分）：从备份恢复分支可正常返回（取消不提交）；“恢复并继续”能完成导入并完成初始化。
- 通过（1b-2 部分）：发现页显示上次扫描信息；“重新扫描”出现五类汇总。
- 通过（1b-3）：运行健康检查按钮正常，显示“未发现问题”。

问题（按序号记录用户原始反馈）：
1. 【全局-布局】100% 缩放下多页内容超出一屏但无滚动条，无法滚动；缩小比例后才有滚动。预期：滚动条由内容是否溢出决定，与缩放比例无关。
2. 【全局-导航】从主导航进入功能页后再点击跳转，页面上没有“返回上一页”，只能点主导航重新进入。预期：子页面提供返回/面包屑，能回到上一页。
3. 【FE-01-体验】备份恢复“恢复并继续”成功但偏慢。已确认为同步执行：UI 等待 commit_restore 完整结束，不是“先跳转后台继续”。待设计：恢复进度反馈或后台化（需契约扩展）。
4. 【FE-01-缺口】新建集中库不能自选系统目录（只有默认路径），应接入系统目录选择器；另：新建分支的“确认集中库”与“主题”页缺“上一步”按钮，只能继续/跳过。注：自选库根路径需新契约（complete_onboarding 目前只接受固定库根）。
5. 【FE-01-交互】识别 Agent 在 Agent 较多时较乱，希望按品牌分组，品牌下再分 CLI/桌面端，突出品牌层级。
6. 【FE-07-文案】五类汇总中“已有关系”与“疑似”含义不直观，需解释或改名+悬浮说明。含义：已有关系=已与客户端建立健康关联的目标目录数；疑似=扫描发现的内容指纹相同（疑似重复）的 Skill 数。
7. 【通过】发现页上次扫描信息显示正常。
8. 【设计+故障】本地发现的“导入 Skill”与在线发现的是同一导入向导，逻辑应区分：本地发现应融合目录解析、zip、URL、Git 地址、npx 来源解析；在线发现应是独立联网检索能力（可配置检索源，初期 skills.sh）。另：真实环境搜索报“搜索失败”。已定位根因：skills.sh API 已改为必须 Vercel OIDC 认证（匿名请求返回 401 authentication_required），桌面端无法自行获取该 token，需要策略决策（如部署 Vercel 代理）。
9. 【导入-Bug】初始化后批量导入识别到多个扫描地址，但来源框只显示第 1 个地址；多选/全选场景下不应只显示一个，除非用户手动自定义目录。
10. 【导入-Bug】“读取已选目录候选”在全选/多选时仍只识别第 1 个地址，逻辑有问题。
11. 【设计】处理冲突页需明确：冲突是待导入 Skill 与集中库对比；数量大时应全部校验；冲突明细要细化（谁和谁冲突、所属路径/品牌、版本）；应支持按冲突类别批量处理+单个处理。前后端均需设计。
12. 【设计】导入流程正常；大量 Skill 时希望支持后台静默导入，避免等待。
13. 【通过】健康检查显示“未发现问题”。

初步分诊：
- 前端可修（无需新契约）：#1 滚动、#2 返回导航、#9/#10 导入多来源 Bug、#6 文案+悬浮说明、#5 品牌分组（纯前端可先做分组展示）。
- 需 Rust 契约扩展：#4 自选库根、#3 恢复进度/后台化、#11 冲突明细校验、#12 后台导入、#8 在线检索策略。
- 需策略决策：#8 skills.sh 认证问题（外部 API 变更）。


【用户决策 2026-09-05】修复顺序：先 A 批（前端 Bug），再 C 批（契约扩展）；B 批（skills.sh 认证）记为技术债，等找到可行方法（如 Vercel 代理）再处理。

【A 批修复 2026-09-05 / 提交 a16f1fb，已推送】
- #1 已修复：主内容区改为 overflow-y auto，滚动条由内容溢出决定；布局契约测试已更新。
- #2 已修复：顶栏在子路由（Agent/项目详情、部署、安全、操作详情、数据保护）显示“返回”按钮，优先历史回退、无历史时回到父级页签。
- #5 已修复：识别 Agent 按品牌（profile_id）分组，品牌下标注客户端形态（命令行/桌面应用/…）。
- #6 已修复：五类更名“已关联目录”“疑似重复”并增加全部五类的悬浮说明。
- #9 已修复：多来源预选时来源框不再预填第一个地址，仅单来源或用户自选时显示。
- #10 已部分处理：采集后候选确认页显示每个来源目录的候选数；前端逻辑经测试验证支持多来源。若真机仍只识别第一个，候选页的分组计数可直接暴露问题来源，便于定位 Rust 侧（发现页技能候选 discover_import_candidates 按 root 独立检测，前端循环已验证）。
- #8 技术债：skills.sh API 需 Vercel OIDC 认证，桌面端无法直接调用；待定检索策略（Vercel 代理/抓网页/降级）。

【C 批修复 2026-09-05 / 提交 ac5b6b2 + 2f2be9b，已推送】
- #4 已修复（ac5b6b2）：新建集中库支持"选择其他目录"（系统选择器）→ 保存并重启生效；已初始化后拒绝变更（防误迁移）；create 分支第 1 步补"上一步"。新契约：set_library_root + restart_application。
- #3 已改善（2f2be9b）：恢复提交期间显示"正在恢复 N 个 Skill…（已用时 X 秒）"的诚实忙碌态；真正的后台恢复需要全局操作框架，记为后续。
- #11 已改善（2f2be9b）：冲突页按冲突类别分组（显示每类数量），支持"按类别批量处理"一键应用同一处理方式，单个仍可覆盖；每条冲突显示候选来源路径与所冲突的已有 Skill（复用 analyze_import 的 matches 数据，无需 Rust 改动）。
- #12 待办：后台静默导入需要把导入提交从向导局部状态提升为全局操作（进度、完成通知、离开页面继续），属于操作框架改造，暂缓。

【Q1 修复 2026-09-06 / 提交 8916f3a，已推送】
- #8 技术债（skills.sh 401）已修复：按发现模块实现规格 §5 将端点改为 https://skills.sh/api/search（legacy 响应形状：skills[]/{id,skillId,name,installs,source}+count+duration_ms），source 用 splitn(2,'/') 拆 owner/repo 并经仓库坐标白名单校验（新增 repo_ref 模块，§4 逐字保留+规格 §11 要求的 .lock/. 开头段拒绝），page_url 映射为 https://skills.sh/{source}/{skillId}，搜索超时 10s。对外契约 SourceSearchPage 形状不变，前端零改动。注意：规格 §4 函数与 §11 测试表存在两处不一致（seg.lock、.hidden/x），已按 §11（更强方向）补齐规则。

【Q2 新增 2026-09-06 / 提交 2e7176f，已推送】
- 仓库发现（规格标准版）已实现：crates/skillhub-adapters 新增 repo_discovery（§6.1 并行发现+单仓库失败仅告警、§6.2 下载/解压预算常量与函数逐字保留、§6.4 递归扫描+根目录 SKILL.md 用仓库名、§6.5 去重；GitHub zip 顶层包装目录按 git.rs 先例展平；sanitize_skill_source_path 逐字保留）。新契约：query= list_skill_repos / discover_repo_skills，command= add_skill_repo（upsert）/ remove_skill_repo / download_repo_skill（下载到 %TEMP%/skillhub-repo-skills/<id>/<name>，超 24h 残留尽力清理），bindings 已再生成。默认 4 仓库（anthropics/skills、ComposioHQ/awesome-claude-skills、cexll/myclaude、JimLiu/baoyu-skills）存 settings 表，首读自动播种。
- 前端发现页新增"仓库发现"卡片：仓库列表（启用开关=upsert、显式确认后移除、添加仓库）、"扫描仓库"结果列表（名称/描述/owner/repo@branch/README 链接）、"下载并导入"把下载目录以本地来源身份送入现有导入向导（下载本身不建受管对象，导入物化才写集中库）。
- 测试：adapters 集成 8 项（含 512MiB 预算/1 万条目/超长 symlink、分支回退、单仓库失败隔离、下载仅复制目标目录、穿越拒绝）+ lib 11 项；application facade 6 项（默认播种、upsert 排序、移除防重、非法坐标不联网、网络开关拦截）；前端 6 项。全量 Rust workspace 测试通过；前端 82 文件 468 项通过；check/build 通过。
- 验收提示：测试期间 discover 曾真实联网成功发现 cexll/myclaude 的 11 个真实 Skill（链路端到端可用）；正式验收请用网络开关=关来验证拦截路径。
- 已知边界：README 链接为普通 <a target=_blank>，打包后的 WebView 可能不响应点击（应用尚无通用外部打开器命令，open_official_release 也仅做校验）；发现查询为大流量操作（每仓库整仓下载），前端展示"正在扫描"诚实等待。

【Q3 完成 2026-09-06 / 提交 4db9341，已推送】
- FE-01 完成页汇总已实现：初始化完成后停留在汇总页，展示识别 Agent 数（含已选数）、扫描来源目录数、发现 Skill 数、跳过数（未变化+出错，诚实口径）；跳过扫描/跳过初始化/恢复分支按实际执行情况显示说明，不显示冒充的 0。
- 流程调整：完成后不再自动跳转，汇总页提供"打开批量导入"（携带扫描 roots，语义不变）与"进入主界面"两个显式按钮。
- 测试：OnboardingWizard 测试 16 项（新增计数正确、延迟进入、roots 传递、skip/restore 不误导）；i18n 双语 parity 通过。

【M2 批次 2026-09-06 / 提交 64ce4ede + 8320c6fe + 0febbda3，已推送】

【Q6 忽略项 UI + 修复预览 / 64ce4ede】
- 设置页技能库卡片新增：忽略项管理（list_ignore_rules 列表 + create_ignore_rule 三类主体 exact_path/exact_skill/exact_pending + 理由必填 + 确认后 remove_ignore_rule）；健康检查每个 finding 的"修复预览"（prepare_repair → RepairPlan 明细 → 显式"执行修复" → commit_repair），无可修复动作的 finding 不显示按钮，取消预览不写任何数据。
- 产品决策记录：DesktopPreferences 无"AI 输出语言"与"减少动效"字段，按队列指令诚实跳过（需要 Rust 契约扩展时再评估）；IgnoreSubject 实际形状为 {type, value}（非 payload），以 bindings 为准。

【Q4 操作框架地基 + 后台静默导入 / 8320c6fe】
- 验收反馈 #12 已修复：新增模块级全局操作跟踪器（platform/operationTracker，useSyncExternalStore 订阅），ImportWizard 提交循环接入——提交期间可离开页面/切换路由，循环继续并在全局指示器（应用壳 OperationIndicator，最近 3 条）显示进度与结果（成功/失败/跳过计数）。
- 提交中界面增加"可以离开此页面"提示；测试含"卸载后循环继续完成并写入 tracker"的关键用例（5 项 tracker + 3 项指示器 + 向导用例）。

【Q5 操作记录列表 + 恢复双页签 / 0febbda3】
- 恢复页改为"操作记录 / 备份恢复"双页签：操作记录页签合并两类事实并诚实标注来源——本会话后台操作（tracker）+ 持久化最近操作（BootstrapSnapshot.recent_operations，链接到既有操作详情页）；备份恢复页签承接原恢复确认流程并注明完整备份/恢复流程在数据保护页。
- 空态/读取失败均为诚实提示，不伪造历史；空记录与读失败分开呈现。

【M2 全量验证】前端 86 测试文件 492 项通过；pnpm check、pnpm build、git diff --check 通过。无 Rust 改动。i18n 并发合并期间曾丢失 HEAD 的 operations.completed 键（两语言），已从 HEAD 恢复并验证 parity。

【Q7 来源更新检查与决策（待与 Q8 一起提交推送）】
- Skill 详情"版本"区块新增"来源更新"面板：点击"检查来源更新"后按契约返回的五种状态如实呈现（已是最新/发现更新/发现更新且本地有修改/来源不可用/需要认证）；有更新时提供四个显式决策按钮（采用上游/保留本地/建独立分支/取消），对应 apply_source_update 的 UpdateDecision；应用后显示结果（含新版本号与"部署需要重新同步"提示）。
- 范围决策：IgnoreExternalChange 是部署维度契约（按 deployment_id），详情页当前无部署行 UI，入口留在部署相关页面（Q9/Q11）一并处理；批量更新检查无对应契约，不做批量入口（诚实边界）。

【M3 批次 2026-09-06 / 提交 4d327bd9 + 9a79a575，已推送（Q6 见 M2 批）】

【Q7 来源更新检查与决策 / 4d327bd9】
- Skill 详情"版本"区块新增"来源更新"面板（SourceUpdatePanel，5 项测试）：点击检查后按契约五种状态如实呈现（up_to_date/update_available/update_available_with_local_changes/source_unavailable/authentication_required）；有更新时提供显式决策按钮（采用上游/保留本地/建独立分支/取消）→ apply_source_update；应用后显示新版本与"部署需要重新同步"提示。
- 范围决策：IgnoreExternalChange 为部署维度契约（按 deployment_id），详情页无部署行 UI，入口留待部署相关页面（Q9/Q11）；批量更新检查无契约，不做批量入口（诚实边界）。

【Q8 批量导出入口 + 退出管理 / 9a79a575】
- 技能库批量栏新增"发起导出"：显式选择直接携带 skillIds 跳转数据保护页；"全部筛选结果"经分页枚举全部 id 后跳转，失败留在库页并提示。
- 数据保护页承接预填：逐技能 list_versions 预检并标注"可导出（当前版本）/无当前版本/版本读取失败"，不伪造可导出性；无 state 时行为不变。
- 退出管理分步：复选部署关系（list_deployments 全量）→ prepare_uninstall 预览影响（部署数/可用动作/preserves_central_library）→ 显式勾选动作（默认全不选）→ apply_uninstall_decision → 显示结果 phase；各步失败走结构化 alert。
- 诚实边界：ExportSkill.content 前端无法合法构造，仍按契约发 skills:[] 由后端从 selection+current 解析（Rust export_input 已有该逻辑）；后端将 backup/standard_export/remove_device_data/clear_credentials 暂标记 unsupported，违例组合显示后端结构化错误。
- 测试：Q7 5 项新增 + Q8 库页 3 项 + backup 7 项；M3 后全量前端 87 文件 507 项通过，check/build 通过。

【M4 批次 2026-09-06 / 提交 e7e648f6(Q12) + 1a9188d8(Q9) + 49eba85a(Q10) + 5b5083f3(Q11) + 33cd2219(i18n修复)，已推送】

【Q9 Agent 管理剩余 / 1a9188d8】
- 受管部署数按唯一 Skill 聚合（如"3 个 Skill · 5 条部署关系"）；自定义 Agent 新增/编辑/删除入口接通（删除需确认）；发现态 Agent 无管理入口（不伪装）。
- 诚实降级：重新定位目录、Agent 级忽略均无原生契约，详情页显示"需要原生契约支持，暂未提供"。
- 【重要发现】自定义 Agent 创建/编辑的 directory 契约只接受 grant_id，需宿主先 register_path_grant，而桌面宿主目前从不注册 grant（目录选择器只返回路径字符串）→ 运行时创建/编辑会被原生层拒绝并如实报错。需要后续原生任务补"选择器签发 grant + 宿主注册"链路。

【Q10 项目详情剩余 / 49eba85a】
- 项目详情新增"路径与访问状态"面板（device_path + 可访问/只读/不可访问徽标，快照无对应物理目标时显示"未记录"，读取失败显示"未知"）；装配计划按状态分组展示（已满足/已成功/待获取/冲突待选择/已跳过/失败），无计划显示诚实空态；注册预览候选按目录痕迹亲和度排序（同前缀/同根优先，悬浮说明注明依据）。
- 诚实降级：detach_management 需要 deployment_id，项目详情无部署记录落点，显示说明文案不伪造按钮。

【Q11 反向部署 + Agent 目标展开 / 5b5083f3】
- Agent 详情/项目详情新增"发起部署"按钮 → 跳转技能库并预选目标 → 勾选 Skill 后"统一部署"自动携带该目标（?target= 参数）。
- 批量部署页支持项目目标"展开关联 Agent（N 个）"一键加入；页面固定提示"批次逐个提交，不是原子操作：部分成功不会自动回滚"。

【Q12 安全结果深度 / e7e648f6】
- 发现项按基础/AI 分组，展示 file/line 证据（无定位显示"位置不可用"）；AI（llm）发现项处置接通（kind="llm"）；高风险处置必须经独立确认对话框（high_risk_confirmed 仅确认后为 true，native 层再加本地拒绝）；AI 卡显示发送范围说明（data_scope=explicit_selection 显示"仅发送显式选择的 Skill 内容"）；未配置 LLM 时运行入口禁用并说明原因。
- 降级：后端无批量处置命令，无批量入口；AI 检查运行中无进度/取消（cancel_operation 未接），仅按钮 loading。
- 流程问题记录：三个并行任务合并 common.json 期间曾丢失 agents.launchDeployment 键（合并时序竞态），已由 33cd2219 恢复；后续并行 i18n 合并需在完成后跑全量 typecheck 兜底。

【M4 全量验证】前端 88 测试文件 565 项通过；pnpm check、pnpm build、git diff --check 通过。无 Rust 改动。

【M5 前半批次 2026-09-06 / 提交 6b974017 + d14a274 + 2b5ed31e + 4bdf6cb + 6d95495b，已推送】

【Q13 来源重新关联 / 6b974017 + d14a274】
- Skill 详情元数据区块新增"来源重新关联"面板：输入本地/Git/HTTPS 地址（前端按规则分类构造 SourceDescriptor）→ relink_source；成功显示"已重新关联来源"，失败显示结构化错误。修复了接线时元数据三元表达式被破坏的渲染问题（d14a274）。
- 版本差异与回滚影响此前已在版本时间线接通（diff/impact 查询），本轮核实无缺口；按 Skill 的操作历史无契约，暂不做（全局操作记录已在恢复页提供）。

【Q14 抽屉真实数据 / 2b5ed31e + 4bdf6cb】
- 快速抽屉的快速视图从纯占位升级为真实读模型：get_skill 填充身份/描述/标签/许可证/生命周期/当前版本；按当前版本查询基础与 AI 检查状态；get_deployment_relations 填充部署关系（计数+名称）。无当前版本时检查如实显示 not_run；部署关系读取失败保持占位零值，不伪造。
- 流程问题记录：本次又在 check 失败的情况下完成了提交（快速视图测试 mock 类型错误），已补修复提交 4bdf6cb。重申纪律：提交前 pnpm check 必须显式确认通过。

【Q16 前半：滚动备份与保留策略 / 6d95495b】
- 数据保护页新增"滚动备份与保留策略"区块：保留份数输入 → run_rolling_backup（scope=full，decisions=[]）→ 显示"保留 X 份、清理 Y 份"；失败结构化 alert。附诚实缓存清理说明（仓库发现临时目录 24h 自动清理；集中库无手动清理缓存）。

【Q15 前半：概览指标下钻 / 1721a358，已推送】
- 概览指标卡升级为可点击链接：Skill 总数 → 技能库；Agent 数 → Agent 列表；项目数 → 项目列表；已部署数 → /library?deployment=deployed（复用库页既有 URL 筛选，返回时筛选状态保持）。柱形图下钻此前已存在（点击柱形进入对应 Agent/项目详情的部署视图），本轮核实无缺口。
- 剩余诚实记录：FE-04 卡片视图/组合视图/关系矩阵、FE-02 指标级更细筛选下钻仍属较大 UI 建设，未在本轮展开。

【M5 后半批次 2026-09-06 / 提交 a2735616 + 395f9c3 + 224d3bd + 6923fcf + 5daf27e，已推送】

【UI 偏好持久化契约 / a2735616】
- 发现并修复真实缺口：表格偏好/抽屉偏好在原生 facade 上从未实现（只有 unavailable），导致设置在真实桌面端不持久化。新增迁移 0007（ui_preferences 表）+ UiPreferenceRepository + 通用契约 get_ui_preference / set_ui_preference（原始 JSON 键值存储，值必须可解析、键非空）；bindings 已再生成。
- 前端 nativeSkillLibraryFacade 接通 load/saveTablePreferences 与 load/saveDrawerPreferences（缺失键时页面回退默认值并显示提示，不伪造）。

【FE-04 前半：技能库卡片视图 / 395f9c3→5daf27e】
- 库工具栏新增"卡片视图/表格视图"切换（经 ui_preferences 持久化，重启保持）；卡片展示技能名/用途/标签/部署数，点击卡片名打开抽屉。
- 过程记录：本轮两次在 check 失败下完成了提交（JSX 条件嵌套错误、mock 类型错误、测试语言断言、未用变量），均已即时补修复提交并重新全量验证。最终以 90 文件 574 项测试 + check + build + git diff --check 全绿收口。教训：必须以 check 的退出码为准，不能用"测试通过"替代。

【M5 全量验证】前端 90 测试文件 574 项通过；pnpm check、pnpm build、git diff --check 通过。

【Q17a 卸载备份 + FE-04 矩阵/标签分组 + Q16 缩放矩阵 2026-09-06 / 提交 bbff8267 + f89339e4 + a581e850 + 1ea80f4 + 79c5e8d6，已推送】

【Q17a 卸载备份（bbff8267 Rust + f89339e4 前端）】
- 退出管理的 backup 动作从"unsupported 拒绝"改为真实实现：apply 时先按 Full 范围创建完整备份包（BackupService.prepare/create/verify，落盘到集中库 .skillhub/backups/），成功后才执行 undeploy 等写动作；备份失败整体中止、不产生半卸载。message_code 区分 uninstall.decision_applied_with_backup。
- 测试：真实部署流（PrepareDeployment→CommitDeployment）后 apply [backup, undeploy_all] → 备份包 manifest 落盘 + 部署解除；无库根时整体中止且部署未动（2 项 Rust + 1 项前端参数断言）。
- 仍不支持（如实报错）：standard_export（走数据保护页导出）、remove_device_data、clear_credentials。

【FE-04 关系矩阵 + 标签分组（a581e850 + 1ea80f4 + 79c5e8d6）】
- 技能库视图模式升级为三选（表格/卡片/关系矩阵，经 ui_preferences 持久化）：矩阵为 Skill × 已部署目标一览（✓ 标记，列取真实部署关系，标签用已注册目标名，仅显示当前页 Skill 并有悬浮说明）。
- 卡片视图新增"分组方式"（不分组/按标签，持久化）：按标签分节展示，无标签技能归入"—"节。
- 新增持久化基建：get_ui_preference/set_ui_preference 通用契约 + 迁移 0007（ui_preferences 表）——表格/抽屉偏好在原生层首次真正持久化（此前为 unavailable 占位）。

【Q16 缩放验收矩阵（文档交付）】
- 新建 docs/development/缩放验收矩阵.md：15 个页面 × 100%/125%/150% × 布局/滚动/交互/键盘/诚实状态五维度，含历史问题回归点（100% 滚动条、概览窄窗口、150% 双页签换行）与回传格式。

【验证】前端 90 文件 577 项全绿；check/build/git diff --check 通过；Rust facade 卸载备份 2 项 + 既有全部用例通过。

【原生缺口修复：grant 链路 2026-09-06 / 提交 eb72ba5e，已推送】
- 自定义 Agent 创建/编辑的目录 grant 链路已打通（此前记录的"运行时创建/编辑会被原生层拒绝"缺口关闭）：
  1. 宿主 pick_local_directory 命令在用户选取目录后，将规范化路径注册为路径 grant（grant_id ≡ 规范化路径本身，与前端展示口径一致），并以 JSON 字符串返回 {path, grant_id}；
  2. CommandBridge 新增 with_local（桌面宿主注入具体门面）与 issue_grant_for_path/register_path_grant 转发；测试替身保持 None 不受影响；
  3. 启动时 re_register_custom_agent_grants：按已存储自定义 Agent 记录里的目录路径重新签发 grant（grant 注册表在内存中，重启后恢复），编辑既有 Agent 不再需要重新拾取目录。
- 测试（src-tauri path_grant_tests，桌面 crate 16/16 全绿）：签发后创建成功且 grant_id=规范化路径；未注册时创建报结构化错误（AgentProfileInvalidCapability，错误参数含 GrantNotAuthorized 原因）；模拟重启后经启动重注册，既有 Agent 可编辑改名。
- 设计说明：grant_id 即规范化路径（自签名），因此 Q9 时期前端"以路径作为 grant 标识"的发送方式在宿主注册后即为合法；目录选择器其余消费方（初始化库根、导入向导）仅用路径，行为不变。

【验证】桌面 crate 16 项测试全绿（含新增 3 项 grant 链路测试）；前端 90 文件 577 项全绿；check/build 通过。

【Q17b ~/.agents lock 发现与导入 2026-09-06 / 提交 e1977b8d，已推送】
- cc-switch 源码（C:\project\cc-switch）已回查：lock 解析规则逐条对齐——仅取 source_type=github 条目；分支回退 branch → source_branch → source_url（/tree/ 路径、#fragment、?branch=/?ref=）；空白分支视同缺失；解析失败/文件缺失一律空列表不阻塞。
- SkillHub 适配：adapters 新增 agents_lock 解析器（纯函数 5 项测试）；新查询 discover_agents_lock_skills（只读，解析 ~/.agents/.skill-lock.json，不动该文件）；bindings 再生成。
- download_repo_skill 扩展：空 directory = 整仓即 Skill（lock 条目常无 skill_path），测试覆盖整仓复制与空/非法目录区分（空→仓库根；穿越/绝对路径仍拒绝）。
- 前端发现页新增"~/.agents lock 导入"卡片（4 项测试）：扫描 → 列出 GitHub 条目（名称/repo@分支/仓库内路径）→ 逐条"下载并导入"复用既有仓库下载预算与导入向导；空态与错误如实呈现。
- 更新检测子项说明：SkillHub 的 check_source_update（本地来源目录哈希 vs 版本树哈希）已在 FE-11（4d327bd）实现并含三态判定；远端 GitHub 来源的哈希对比在"仓库发现导入以临时目录进入向导"的现状下缺少持久化远端坐标（来源记录指向临时目录），需先将 GitHub 来源做成长期来源记录（RelinkSource 或导入时记录 owner/repo/branch/directory）——已记录为后续契约增强，不在本轮强行实现。

【文档同步与归档 2026-09-06】
- 《当前开发状态》《验收矩阵》《验收指南（1k 节）》《遗漏清单（FE-07 lock 卡片）》均已同步至候选 `e1977b8d`。
- 归档：当前交接-2026-09-05.md、skill-discovery-module-spec.md（实现规格）→ docs/development/archive/。
- 新建《交接指令-2026-09-06.md》：面向下一 Agent 的继续开发指令（N1 外链打开器、N2 按 Skill 操作历史、N3 AI 检查进度/取消、N4 导出格式、N5 组合视图/批量汇总、N6 更细下钻、N7 远端更新检测）。

2026-09-04 / 候选提交 e6a3089

P0-01 初始化向导：默认集中库路径展示、全选可用 Agent、只读扫描、重新运行向导和完成后回到主界面均已验证。自定义集中库路径仍不能通过系统目录选择器更换，原因是 Tauri 原生 picker/grant 能力尚未接入；该项记录为未通过/待实现。

补充：在最新提交上，人工点击全选后扫描仍显示“无法完成扫描”。同机真实 facade 复现可完成扫描且 errors=0，需继续核对桌面运行实例、调用返回或超时边界；暂不判定 P0-01 扫描通过。

P0-02 发现/导入：当前只能手动输入本地目录路径，不能直接打开系统目录选择器；记录为未通过/待实现。技能库空态已提供“打开发现”入口。

---

2026-09-05 / 候选提交 c6b8d10 / FE-01、FE-07、FE-16 定向验收

通过项：
- 通过（1b-1 部分）：从备份恢复分支可正常返回（取消不提交）；“恢复并继续”能完成导入并完成初始化。
- 通过（1b-2 部分）：发现页显示上次扫描信息；“重新扫描”出现五类汇总。
- 通过（1b-3）：运行健康检查按钮正常，显示“未发现问题”。

问题（按序号记录用户原始反馈）：
1. 【全局-布局】100% 缩放下多页内容超出一屏但无滚动条，无法滚动；缩小比例后才有滚动。预期：滚动条由内容是否溢出决定，与缩放比例无关。
2. 【全局-导航】从主导航进入功能页后再点击跳转，页面上没有“返回上一页”，只能点主导航重新进入。预期：子页面提供返回/面包屑，能回到上一页。
3. 【FE-01-体验】备份恢复“恢复并继续”成功但偏慢。已确认为同步执行：UI 等待 commit_restore 完整结束，不是“先跳转后台继续”。待设计：恢复进度反馈或后台化（需契约扩展）。
4. 【FE-01-缺口】新建集中库不能自选系统目录（只有默认路径），应接入系统目录选择器；另：新建分支的“确认集中库”与“主题”页缺“上一步”按钮，只能继续/跳过。注：自选库根路径需新契约（complete_onboarding 目前只接受固定库根）。
5. 【FE-01-交互】识别 Agent 在 Agent 较多时较乱，希望按品牌分组，品牌下再分 CLI/桌面端，突出品牌层级。
6. 【FE-07-文案】五类汇总中“已有关系”与“疑似”含义不直观，需解释或改名+悬浮说明。含义：已有关系=已与客户端建立健康关联的目标目录数；疑似=扫描发现的内容指纹相同（疑似重复）的 Skill 数。
7. 【通过】发现页上次扫描信息显示正常。
8. 【设计+故障】本地发现的“导入 Skill”与在线发现的是同一导入向导，逻辑应区分：本地发现应融合目录解析、zip、URL、Git 地址、npx 来源解析；在线发现应是独立联网检索能力（可配置检索源，初期 skills.sh）。另：真实环境搜索报“搜索失败”。已定位根因：skills.sh API 已改为必须 Vercel OIDC 认证（匿名请求返回 401 authentication_required），桌面端无法自行获取该 token，需要策略决策（如部署 Vercel 代理）。
9. 【导入-Bug】初始化后批量导入识别到多个扫描地址，但来源框只显示第 1 个地址；多选/全选场景下不应只显示一个，除非用户手动自定义目录。
10. 【导入-Bug】“读取已选目录候选”在全选/多选时仍只识别第 1 个地址，逻辑有问题。
11. 【设计】处理冲突页需明确：冲突是待导入 Skill 与集中库对比；数量大时应全部校验；冲突明细要细化（谁和谁冲突、所属路径/品牌、版本）；应支持按冲突类别批量处理+单个处理。前后端均需设计。
12. 【设计】导入流程正常；大量 Skill 时希望支持后台静默导入，避免等待。
13. 【通过】健康检查显示“未发现问题”。

初步分诊：
- 前端可修（无需新契约）：#1 滚动、#2 返回导航、#9/#10 导入多来源 Bug、#6 文案+悬浮说明、#5 品牌分组（纯前端可先做分组展示）。
- 需 Rust 契约扩展：#4 自选库根、#3 恢复进度/后台化、#11 冲突明细校验、#12 后台导入、#8 在线检索策略。
- 需策略决策：#8 skills.sh 认证问题（外部 API 变更）。


【用户决策 2026-09-05】修复顺序：先 A 批（前端 Bug），再 C 批（契约扩展）；B 批（skills.sh 认证）记为技术债，等找到可行方法（如 Vercel 代理）再处理。

【A 批修复 2026-09-05 / 提交 a16f1fb，已推送】
- #1 已修复：主内容区改为 overflow-y auto，滚动条由内容溢出决定；布局契约测试已更新。
- #2 已修复：顶栏在子路由（Agent/项目详情、部署、安全、操作详情、数据保护）显示“返回”按钮，优先历史回退、无历史时回到父级页签。
- #5 已修复：识别 Agent 按品牌（profile_id）分组，品牌下标注客户端形态（命令行/桌面应用/…）。
- #6 已修复：五类更名“已关联目录”“疑似重复”并增加全部五类的悬浮说明。
- #9 已修复：多来源预选时来源框不再预填第一个地址，仅单来源或用户自选时显示。
- #10 已部分处理：采集后候选确认页显示每个来源目录的候选数；前端逻辑经测试验证支持多来源。若真机仍只识别第一个，候选页的分组计数可直接暴露问题来源，便于定位 Rust 侧（发现页技能候选 discover_import_candidates 按 root 独立检测，前端循环已验证）。
- #8 技术债：skills.sh API 需 Vercel OIDC 认证，桌面端无法直接调用；待定检索策略（Vercel 代理/抓网页/降级）。

【C 批修复 2026-09-05 / 提交 ac5b6b2 + 2f2be9b，已推送】
- #4 已修复（ac5b6b2）：新建集中库支持"选择其他目录"（系统选择器）→ 保存并重启生效；已初始化后拒绝变更（防误迁移）；create 分支第 1 步补"上一步"。新契约：set_library_root + restart_application。
- #3 已改善（2f2be9b）：恢复提交期间显示"正在恢复 N 个 Skill…（已用时 X 秒）"的诚实忙碌态；真正的后台恢复需要全局操作框架，记为后续。
- #11 已改善（2f2be9b）：冲突页按冲突类别分组（显示每类数量），支持"按类别批量处理"一键应用同一处理方式，单个仍可覆盖；每条冲突显示候选来源路径与所冲突的已有 Skill（复用 analyze_import 的 matches 数据，无需 Rust 改动）。
- #12 待办：后台静默导入需要把导入提交从向导局部状态提升为全局操作（进度、完成通知、离开页面继续），属于操作框架改造，暂缓。

【Q1 修复 2026-09-06 / 提交 8916f3a，已推送】
- #8 技术债（skills.sh 401）已修复：按发现模块实现规格 §5 将端点改为 https://skills.sh/api/search（legacy 响应形状：skills[]/{id,skillId,name,installs,source}+count+duration_ms），source 用 splitn(2,'/') 拆 owner/repo 并经仓库坐标白名单校验（新增 repo_ref 模块，§4 逐字保留+规格 §11 要求的 .lock/. 开头段拒绝），page_url 映射为 https://skills.sh/{source}/{skillId}，搜索超时 10s。对外契约 SourceSearchPage 形状不变，前端零改动。注意：规格 §4 函数与 §11 测试表存在两处不一致（seg.lock、.hidden/x），已按 §11（更强方向）补齐规则。

【Q2 新增 2026-09-06 / 提交 2e7176f，已推送】
- 仓库发现（规格标准版）已实现：crates/skillhub-adapters 新增 repo_discovery（§6.1 并行发现+单仓库失败仅告警、§6.2 下载/解压预算常量与函数逐字保留、§6.4 递归扫描+根目录 SKILL.md 用仓库名、§6.5 去重；GitHub zip 顶层包装目录按 git.rs 先例展平；sanitize_skill_source_path 逐字保留）。新契约：query= list_skill_repos / discover_repo_skills，command= add_skill_repo（upsert）/ remove_skill_repo / download_repo_skill（下载到 %TEMP%/skillhub-repo-skills/<id>/<name>，超 24h 残留尽力清理），bindings 已再生成。默认 4 仓库（anthropics/skills、ComposioHQ/awesome-claude-skills、cexll/myclaude、JimLiu/baoyu-skills）存 settings 表，首读自动播种。
- 前端发现页新增"仓库发现"卡片：仓库列表（启用开关=upsert、显式确认后移除、添加仓库）、"扫描仓库"结果列表（名称/描述/owner/repo@branch/README 链接）、"下载并导入"把下载目录以本地来源身份送入现有导入向导（下载本身不建受管对象，导入物化才写集中库）。
- 测试：adapters 集成 8 项（含 512MiB 预算/1 万条目/超长 symlink、分支回退、单仓库失败隔离、下载仅复制目标目录、穿越拒绝）+ lib 11 项；application facade 6 项（默认播种、upsert 排序、移除防重、非法坐标不联网、网络开关拦截）；前端 6 项。全量 Rust workspace 测试通过；前端 82 文件 468 项通过；check/build 通过。
- 验收提示：测试期间 discover 曾真实联网成功发现 cexll/myclaude 的 11 个真实 Skill（链路端到端可用）；正式验收请用网络开关=关来验证拦截路径。
- 已知边界：README 链接为普通 <a target=_blank>，打包后的 WebView 可能不响应点击（应用尚无通用外部打开器命令，open_official_release 也仅做校验）；发现查询为大流量操作（每仓库整仓下载），前端展示"正在扫描"诚实等待。

【Q3 完成 2026-09-06 / 提交 4db9341，已推送】
- FE-01 完成页汇总已实现：初始化完成后停留在汇总页，展示识别 Agent 数（含已选数）、扫描来源目录数、发现 Skill 数、跳过数（未变化+出错，诚实口径）；跳过扫描/跳过初始化/恢复分支按实际执行情况显示说明，不显示冒充的 0。
- 流程调整：完成后不再自动跳转，汇总页提供"打开批量导入"（携带扫描 roots，语义不变）与"进入主界面"两个显式按钮。
- 测试：OnboardingWizard 测试 16 项（新增计数正确、延迟进入、roots 传递、skip/restore 不误导）；i18n 双语 parity 通过。

【M2 批次 2026-09-06 / 提交 64ce4ede + 8320c6fe + 0febbda3，已推送】

【Q6 忽略项 UI + 修复预览 / 64ce4ede】
- 设置页技能库卡片新增：忽略项管理（list_ignore_rules 列表 + create_ignore_rule 三类主体 exact_path/exact_skill/exact_pending + 理由必填 + 确认后 remove_ignore_rule）；健康检查每个 finding 的"修复预览"（prepare_repair → RepairPlan 明细 → 显式"执行修复" → commit_repair），无可修复动作的 finding 不显示按钮，取消预览不写任何数据。
- 产品决策记录：DesktopPreferences 无"AI 输出语言"与"减少动效"字段，按队列指令诚实跳过（需要 Rust 契约扩展时再评估）；IgnoreSubject 实际形状为 {type, value}（非 payload），以 bindings 为准。

【Q4 操作框架地基 + 后台静默导入 / 8320c6fe】
- 验收反馈 #12 已修复：新增模块级全局操作跟踪器（platform/operationTracker，useSyncExternalStore 订阅），ImportWizard 提交循环接入——提交期间可离开页面/切换路由，循环继续并在全局指示器（应用壳 OperationIndicator，最近 3 条）显示进度与结果（成功/失败/跳过计数）。
- 提交中界面增加"可以离开此页面"提示；测试含"卸载后循环继续完成并写入 tracker"的关键用例（5 项 tracker + 3 项指示器 + 向导用例）。

【Q5 操作记录列表 + 恢复双页签 / 0febbda3】
- 恢复页改为"操作记录 / 备份恢复"双页签：操作记录页签合并两类事实并诚实标注来源——本会话后台操作（tracker）+ 持久化最近操作（BootstrapSnapshot.recent_operations，链接到既有操作详情页）；备份恢复页签承接原恢复确认流程并注明完整备份/恢复流程在数据保护页。
- 空态/读取失败均为诚实提示，不伪造历史；空记录与读失败分开呈现。

【M2 全量验证】前端 86 测试文件 492 项通过；pnpm check、pnpm build、git diff --check 通过。无 Rust 改动。i18n 并发合并期间曾丢失 HEAD 的 operations.completed 键（两语言），已从 HEAD 恢复并验证 parity。

【Q7 来源更新检查与决策（待与 Q8 一起提交推送）】
- Skill 详情"版本"区块新增"来源更新"面板：点击"检查来源更新"后按契约返回的五种状态如实呈现（已是最新/发现更新/发现更新且本地有修改/来源不可用/需要认证）；有更新时提供四个显式决策按钮（采用上游/保留本地/建独立分支/取消），对应 apply_source_update 的 UpdateDecision；应用后显示结果（含新版本号与"部署需要重新同步"提示）。
- 范围决策：IgnoreExternalChange 是部署维度契约（按 deployment_id），详情页当前无部署行 UI，入口留在部署相关页面（Q9/Q11）一并处理；批量更新检查无对应契约，不做批量入口（诚实边界）。

【M3 批次 2026-09-06 / 提交 4d327bd9 + 9a79a575，已推送（Q6 见 M2 批）】

【Q7 来源更新检查与决策 / 4d327bd9】
- Skill 详情"版本"区块新增"来源更新"面板（SourceUpdatePanel，5 项测试）：点击检查后按契约五种状态如实呈现（up_to_date/update_available/update_available_with_local_changes/source_unavailable/authentication_required）；有更新时提供显式决策按钮（采用上游/保留本地/建独立分支/取消）→ apply_source_update；应用后显示新版本与"部署需要重新同步"提示。
- 范围决策：IgnoreExternalChange 为部署维度契约（按 deployment_id），详情页无部署行 UI，入口留待部署相关页面（Q9/Q11）；批量更新检查无契约，不做批量入口（诚实边界）。

【Q8 批量导出入口 + 退出管理 / 9a79a575】
- 技能库批量栏新增"发起导出"：显式选择直接携带 skillIds 跳转数据保护页；"全部筛选结果"经分页枚举全部 id 后跳转，失败留在库页并提示。
- 数据保护页承接预填：逐技能 list_versions 预检并标注"可导出（当前版本）/无当前版本/版本读取失败"，不伪造可导出性；无 state 时行为不变。
- 退出管理分步：复选部署关系（list_deployments 全量）→ prepare_uninstall 预览影响（部署数/可用动作/preserves_central_library）→ 显式勾选动作（默认全不选）→ apply_uninstall_decision → 显示结果 phase；各步失败走结构化 alert。
- 诚实边界：ExportSkill.content 前端无法合法构造，仍按契约发 skills:[] 由后端从 selection+current 解析（Rust export_input 已有该逻辑）；后端将 backup/standard_export/remove_device_data/clear_credentials 暂标记 unsupported，违例组合显示后端结构化错误。
- 测试：Q7 5 项新增 + Q8 库页 3 项 + backup 7 项；M3 后全量前端 87 文件 507 项通过，check/build 通过。

【M4 批次 2026-09-06 / 提交 e7e648f6(Q12) + 1a9188d8(Q9) + 49eba85a(Q10) + 5b5083f3(Q11) + 33cd2219(i18n修复)，已推送】

【Q9 Agent 管理剩余 / 1a9188d8】
- 受管部署数按唯一 Skill 聚合（如"3 个 Skill · 5 条部署关系"）；自定义 Agent 新增/编辑/删除入口接通（删除需确认）；发现态 Agent 无管理入口（不伪装）。
- 诚实降级：重新定位目录、Agent 级忽略均无原生契约，详情页显示"需要原生契约支持，暂未提供"。
- 【重要发现】自定义 Agent 创建/编辑的 directory 契约只接受 grant_id，需宿主先 register_path_grant，而桌面宿主目前从不注册 grant（目录选择器只返回路径字符串）→ 运行时创建/编辑会被原生层拒绝并如实报错。需要后续原生任务补"选择器签发 grant + 宿主注册"链路。

【Q10 项目详情剩余 / 49eba85a】
- 项目详情新增"路径与访问状态"面板（device_path + 可访问/只读/不可访问徽标，快照无对应物理目标时显示"未记录"，读取失败显示"未知"）；装配计划按状态分组展示（已满足/已成功/待获取/冲突待选择/已跳过/失败），无计划显示诚实空态；注册预览候选按目录痕迹亲和度排序（同前缀/同根优先，悬浮说明注明依据）。
- 诚实降级：detach_management 需要 deployment_id，项目详情无部署记录落点，显示说明文案不伪造按钮。

【Q11 反向部署 + Agent 目标展开 / 5b5083f3】
- Agent 详情/项目详情新增"发起部署"按钮 → 跳转技能库并预选目标 → 勾选 Skill 后"统一部署"自动携带该目标（?target= 参数）。
- 批量部署页支持项目目标"展开关联 Agent（N 个）"一键加入；页面固定提示"批次逐个提交，不是原子操作：部分成功不会自动回滚"。

【Q12 安全结果深度 / e7e648f6】
- 发现项按基础/AI 分组，展示 file/line 证据（无定位显示"位置不可用"）；AI（llm）发现项处置接通（kind="llm"）；高风险处置必须经独立确认对话框（high_risk_confirmed 仅确认后为 true，native 层再加本地拒绝）；AI 卡显示发送范围说明（data_scope=explicit_selection 显示"仅发送显式选择的 Skill 内容"）；未配置 LLM 时运行入口禁用并说明原因。
- 降级：后端无批量处置命令，无批量入口；AI 检查运行中无进度/取消（cancel_operation 未接），仅按钮 loading。
- 流程问题记录：三个并行任务合并 common.json 期间曾丢失 agents.launchDeployment 键（合并时序竞态），已由 33cd2219 恢复；后续并行 i18n 合并需在完成后跑全量 typecheck 兜底。

【M4 全量验证】前端 88 测试文件 565 项通过；pnpm check、pnpm build、git diff --check 通过。无 Rust 改动。

【M5 前半批次 2026-09-06 / 提交 6b974017 + d14a274 + 2b5ed31e + 4bdf6cb + 6d95495b，已推送】

【Q13 来源重新关联 / 6b974017 + d14a274】
- Skill 详情元数据区块新增"来源重新关联"面板：输入本地/Git/HTTPS 地址（前端按规则分类构造 SourceDescriptor）→ relink_source；成功显示"已重新关联来源"，失败显示结构化错误。修复了接线时元数据三元表达式被破坏的渲染问题（d14a274）。
- 版本差异与回滚影响此前已在版本时间线接通（diff/impact 查询），本轮核实无缺口；按 Skill 的操作历史无契约，暂不做（全局操作记录已在恢复页提供）。

【Q14 抽屉真实数据 / 2b5ed31e + 4bdf6cb】
- 快速抽屉的快速视图从纯占位升级为真实读模型：get_skill 填充身份/描述/标签/许可证/生命周期/当前版本；按当前版本查询基础与 AI 检查状态；get_deployment_relations 填充部署关系（计数+名称）。无当前版本时检查如实显示 not_run；部署关系读取失败保持占位零值，不伪造。
- 流程问题记录：本次又在 check 失败的情况下完成了提交（快速视图测试 mock 类型错误），已补修复提交 4bdf6cb。重申纪律：提交前 pnpm check 必须显式确认通过。

【Q16 前半：滚动备份与保留策略 / 6d95495b】
- 数据保护页新增"滚动备份与保留策略"区块：保留份数输入 → run_rolling_backup（scope=full，decisions=[]）→ 显示"保留 X 份、清理 Y 份"；失败结构化 alert。附诚实缓存清理说明（仓库发现临时目录 24h 自动清理；集中库无手动清理缓存）。

【Q15 前半：概览指标下钻 / 1721a358，已推送】
- 概览指标卡升级为可点击链接：Skill 总数 → 技能库；Agent 数 → Agent 列表；项目数 → 项目列表；已部署数 → /library?deployment=deployed（复用库页既有 URL 筛选，返回时筛选状态保持）。柱形图下钻此前已存在（点击柱形进入对应 Agent/项目详情的部署视图），本轮核实无缺口。
- 剩余诚实记录：FE-04 卡片视图/组合视图/关系矩阵、FE-02 指标级更细筛选下钻仍属较大 UI 建设，未在本轮展开。

【M5 后半批次 2026-09-06 / 提交 a2735616 + 395f9c3 + 224d3bd + 6923fcf + 5daf27e，已推送】

【UI 偏好持久化契约 / a2735616】
- 发现并修复真实缺口：表格偏好/抽屉偏好在原生 facade 上从未实现（只有 unavailable），导致设置在真实桌面端不持久化。新增迁移 0007（ui_preferences 表）+ UiPreferenceRepository + 通用契约 get_ui_preference / set_ui_preference（原始 JSON 键值存储，值必须可解析、键非空）；bindings 已再生成。
- 前端 nativeSkillLibraryFacade 接通 load/saveTablePreferences 与 load/saveDrawerPreferences（缺失键时页面回退默认值并显示提示，不伪造）。

【FE-04 前半：技能库卡片视图 / 395f9c3→5daf27e】
- 库工具栏新增"卡片视图/表格视图"切换（经 ui_preferences 持久化，重启保持）；卡片展示技能名/用途/标签/部署数，点击卡片名打开抽屉。
- 过程记录：本轮两次在 check 失败下完成了提交（JSX 条件嵌套错误、mock 类型错误、测试语言断言、未用变量），均已即时补修复提交并重新全量验证。最终以 90 文件 574 项测试 + check + build + git diff --check 全绿收口。教训：必须以 check 的退出码为准，不能用"测试通过"替代。

【M5 全量验证】前端 90 测试文件 574 项通过；pnpm check、pnpm build、git diff --check 通过。

【Q17a 卸载备份 + FE-04 矩阵/标签分组 + Q16 缩放矩阵 2026-09-06 / 提交 bbff8267 + f89339e4 + a581e850 + 1ea80f4 + 79c5e8d6，已推送】

【Q17a 卸载备份（bbff8267 Rust + f89339e4 前端）】
- 退出管理的 backup 动作从"unsupported 拒绝"改为真实实现：apply 时先按 Full 范围创建完整备份包（BackupService.prepare/create/verify，落盘到集中库 .skillhub/backups/），成功后才执行 undeploy 等写动作；备份失败整体中止、不产生半卸载。message_code 区分 uninstall.decision_applied_with_backup。
- 测试：真实部署流（PrepareDeployment→CommitDeployment）后 apply [backup, undeploy_all] → 备份包 manifest 落盘 + 部署解除；无库根时整体中止且部署未动（2 项 Rust + 1 项前端参数断言）。
- 仍不支持（如实报错）：standard_export（走数据保护页导出）、remove_device_data、clear_credentials。

【FE-04 关系矩阵 + 标签分组（a581e850 + 1ea80f4 + 79c5e8d6）】
- 技能库视图模式升级为三选（表格/卡片/关系矩阵，经 ui_preferences 持久化）：矩阵为 Skill × 已部署目标一览（✓ 标记，列取真实部署关系，标签用已注册目标名，仅显示当前页 Skill 并有悬浮说明）。
- 卡片视图新增"分组方式"（不分组/按标签，持久化）：按标签分节展示，无标签技能归入"—"节。
- 新增持久化基建：get_ui_preference/set_ui_preference 通用契约 + 迁移 0007（ui_preferences 表）——表格/抽屉偏好在原生层首次真正持久化（此前为 unavailable 占位）。

【Q16 缩放验收矩阵（文档交付）】
- 新建 docs/development/缩放验收矩阵.md：15 个页面 × 100%/125%/150% × 布局/滚动/交互/键盘/诚实状态五维度，含历史问题回归点（100% 滚动条、概览窄窗口、150% 双页签换行）与回传格式。

【验证】前端 90 文件 577 项全绿；check/build/git diff --check 通过；Rust facade 卸载备份 2 项 + 既有全部用例通过。

【原生缺口修复：grant 链路 2026-09-06 / 提交 eb72ba5e，已推送】
- 自定义 Agent 创建/编辑的目录 grant 链路已打通（此前记录的"运行时创建/编辑会被原生层拒绝"缺口关闭）：
  1. 宿主 pick_local_directory 命令在用户选取目录后，将规范化路径注册为路径 grant（grant_id ≡ 规范化路径本身，与前端展示口径一致），并以 JSON 字符串返回 {path, grant_id}；
  2. CommandBridge 新增 with_local（桌面宿主注入具体门面）与 issue_grant_for_path/register_path_grant 转发；测试替身保持 None 不受影响；
  3. 启动时 re_register_custom_agent_grants：按已存储自定义 Agent 记录里的目录路径重新签发 grant（grant 注册表在内存中，重启后恢复），编辑既有 Agent 不再需要重新拾取目录。
- 测试（src-tauri path_grant_tests，桌面 crate 16/16 全绿）：签发后创建成功且 grant_id=规范化路径；未注册时创建报结构化错误（AgentProfileInvalidCapability，错误参数含 GrantNotAuthorized 原因）；模拟重启后经启动重注册，既有 Agent 可编辑改名。
- 设计说明：grant_id 即规范化路径（自签名），因此 Q9 时期前端"以路径作为 grant 标识"的发送方式在宿主注册后即为合法；目录选择器其余消费方（初始化库根、导入向导）仅用路径，行为不变。

【验证】桌面 crate 16 项测试全绿（含新增 3 项 grant 链路测试）；前端 90 文件 577 项全绿；check/build 通过。

【Q17b ~/.agents lock 发现与导入 2026-09-06 / 提交 e1977b8d，已推送】
- cc-switch 源码（C:\project\cc-switch）已回查：lock 解析规则逐条对齐——仅取 source_type=github 条目；分支回退 branch → source_branch → source_url（/tree/ 路径、#fragment、?branch=/?ref=）；空白分支视同缺失；解析失败/文件缺失一律空列表不阻塞。
- SkillHub 适配：adapters 新增 agents_lock 解析器（纯函数 5 项测试）；新查询 discover_agents_lock_skills（只读，解析 ~/.agents/.skill-lock.json，不动该文件）；bindings 再生成。
- download_repo_skill 扩展：空 directory = 整仓即 Skill（lock 条目常无 skill_path），测试覆盖整仓复制与空/非法目录区分（空→仓库根；穿越/绝对路径仍拒绝）。
- 前端发现页新增"~/.agents lock 导入"卡片（4 项测试）：扫描 → 列出 GitHub 条目（名称/repo@分支/仓库内路径）→ 逐条"下载并导入"复用既有仓库下载预算与导入向导；空态与错误如实呈现。
- 更新检测子项说明：SkillHub 的 check_source_update（本地来源目录哈希 vs 版本树哈希）已在 FE-11（4d327bd）实现并含三态判定；远端 GitHub 来源的哈希对比在"仓库发现导入以临时目录进入向导"的现状下缺少持久化远端坐标（来源记录指向临时目录），需先将 GitHub 来源做成长期来源记录（RelinkSource 或导入时记录 owner/repo/branch/directory）——已记录为后续契约增强，不在本轮强行实现。

2026-09-04 / 候选提交 e6a3089

P0-01 初始化向导：默认集中库路径展示、全选可用 Agent、只读扫描、重新运行向导和完成后回到主界面均已验证。自定义集中库路径仍不能通过系统目录选择器更换，原因是 Tauri 原生 picker/grant 能力尚未接入；该项记录为未通过/待实现。

补充：在最新提交上，人工点击全选后扫描仍显示“无法完成扫描”。同机真实 facade 复现可完成扫描且 errors=0，需继续核对桌面运行实例、调用返回或超时边界；暂不判定 P0-01 扫描通过。

P0-02 发现/导入：当前只能手动输入本地目录路径，不能直接打开系统目录选择器；记录为未通过/待实现。技能库空态已提供“打开发现”入口。

---

2026-09-05 / 候选提交 c6b8d10 / FE-01、FE-07、FE-16 定向验收

通过项：
- 通过（1b-1 部分）：从备份恢复分支可正常返回（取消不提交）；“恢复并继续”能完成导入并完成初始化。
- 通过（1b-2 部分）：发现页显示上次扫描信息；“重新扫描”出现五类汇总。
- 通过（1b-3）：运行健康检查按钮正常，显示“未发现问题”。

问题（按序号记录用户原始反馈）：
1. 【全局-布局】100% 缩放下多页内容超出一屏但无滚动条，无法滚动；缩小比例后才有滚动。预期：滚动条由内容是否溢出决定，与缩放比例无关。
2. 【全局-导航】从主导航进入功能页后再点击跳转，页面上没有“返回上一页”，只能点主导航重新进入。预期：子页面提供返回/面包屑，能回到上一页。
3. 【FE-01-体验】备份恢复“恢复并继续”成功但偏慢。已确认为同步执行：UI 等待 commit_restore 完整结束，不是“先跳转后台继续”。待设计：恢复进度反馈或后台化（需契约扩展）。
4. 【FE-01-缺口】新建集中库不能自选系统目录（只有默认路径），应接入系统目录选择器；另：新建分支的“确认集中库”与“主题”页缺“上一步”按钮，只能继续/跳过。注：自选库根路径需新契约（complete_onboarding 目前只接受固定库根）。
5. 【FE-01-交互】识别 Agent 在 Agent 较多时较乱，希望按品牌分组，品牌下再分 CLI/桌面端，突出品牌层级。
6. 【FE-07-文案】五类汇总中“已有关系”与“疑似”含义不直观，需解释或改名+悬浮说明。含义：已有关系=已与客户端建立健康关联的目标目录数；疑似=扫描发现的内容指纹相同（疑似重复）的 Skill 数。
7. 【通过】发现页上次扫描信息显示正常。
8. 【设计+故障】本地发现的“导入 Skill”与在线发现的是同一导入向导，逻辑应区分：本地发现应融合目录解析、zip、URL、Git 地址、npx 来源解析；在线发现应是独立联网检索能力（可配置检索源，初期 skills.sh）。另：真实环境搜索报“搜索失败”。已定位根因：skills.sh API 已改为必须 Vercel OIDC 认证（匿名请求返回 401 authentication_required），桌面端无法自行获取该 token，需要策略决策（如部署 Vercel 代理）。
9. 【导入-Bug】初始化后批量导入识别到多个扫描地址，但来源框只显示第 1 个地址；多选/全选场景下不应只显示一个，除非用户手动自定义目录。
10. 【导入-Bug】“读取已选目录候选”在全选/多选时仍只识别第 1 个地址，逻辑有问题。
11. 【设计】处理冲突页需明确：冲突是待导入 Skill 与集中库对比；数量大时应全部校验；冲突明细要细化（谁和谁冲突、所属路径/品牌、版本）；应支持按冲突类别批量处理+单个处理。前后端均需设计。
12. 【设计】导入流程正常；大量 Skill 时希望支持后台静默导入，避免等待。
13. 【通过】健康检查显示“未发现问题”。

初步分诊：
- 前端可修（无需新契约）：#1 滚动、#2 返回导航、#9/#10 导入多来源 Bug、#6 文案+悬浮说明、#5 品牌分组（纯前端可先做分组展示）。
- 需 Rust 契约扩展：#4 自选库根、#3 恢复进度/后台化、#11 冲突明细校验、#12 后台导入、#8 在线检索策略。
- 需策略决策：#8 skills.sh 认证问题（外部 API 变更）。


【用户决策 2026-09-05】修复顺序：先 A 批（前端 Bug），再 C 批（契约扩展）；B 批（skills.sh 认证）记为技术债，等找到可行方法（如 Vercel 代理）再处理。

【A 批修复 2026-09-05 / 提交 a16f1fb，已推送】
- #1 已修复：主内容区改为 overflow-y auto，滚动条由内容溢出决定；布局契约测试已更新。
- #2 已修复：顶栏在子路由（Agent/项目详情、部署、安全、操作详情、数据保护）显示“返回”按钮，优先历史回退、无历史时回到父级页签。
- #5 已修复：识别 Agent 按品牌（profile_id）分组，品牌下标注客户端形态（命令行/桌面应用/…）。
- #6 已修复：五类更名“已关联目录”“疑似重复”并增加全部五类的悬浮说明。
- #9 已修复：多来源预选时来源框不再预填第一个地址，仅单来源或用户自选时显示。
- #10 已部分处理：采集后候选确认页显示每个来源目录的候选数；前端逻辑经测试验证支持多来源。若真机仍只识别第一个，候选页的分组计数可直接暴露问题来源，便于定位 Rust 侧（发现页技能候选 discover_import_candidates 按 root 独立检测，前端循环已验证）。
- #8 技术债：skills.sh API 需 Vercel OIDC 认证，桌面端无法直接调用；待定检索策略（Vercel 代理/抓网页/降级）。

【C 批修复 2026-09-05 / 提交 ac5b6b2 + 2f2be9b，已推送】
- #4 已修复（ac5b6b2）：新建集中库支持"选择其他目录"（系统选择器）→ 保存并重启生效；已初始化后拒绝变更（防误迁移）；create 分支第 1 步补"上一步"。新契约：set_library_root + restart_application。
- #3 已改善（2f2be9b）：恢复提交期间显示"正在恢复 N 个 Skill…（已用时 X 秒）"的诚实忙碌态；真正的后台恢复需要全局操作框架，记为后续。
- #11 已改善（2f2be9b）：冲突页按冲突类别分组（显示每类数量），支持"按类别批量处理"一键应用同一处理方式，单个仍可覆盖；每条冲突显示候选来源路径与所冲突的已有 Skill（复用 analyze_import 的 matches 数据，无需 Rust 改动）。
- #12 待办：后台静默导入需要把导入提交从向导局部状态提升为全局操作（进度、完成通知、离开页面继续），属于操作框架改造，暂缓。

【Q1 修复 2026-09-06 / 提交 8916f3a，已推送】
- #8 技术债（skills.sh 401）已修复：按发现模块实现规格 §5 将端点改为 https://skills.sh/api/search（legacy 响应形状：skills[]/{id,skillId,name,installs,source}+count+duration_ms），source 用 splitn(2,'/') 拆 owner/repo 并经仓库坐标白名单校验（新增 repo_ref 模块，§4 逐字保留+规格 §11 要求的 .lock/. 开头段拒绝），page_url 映射为 https://skills.sh/{source}/{skillId}，搜索超时 10s。对外契约 SourceSearchPage 形状不变，前端零改动。注意：规格 §4 函数与 §11 测试表存在两处不一致（seg.lock、.hidden/x），已按 §11（更强方向）补齐规则。

【Q2 新增 2026-09-06 / 提交 2e7176f，已推送】
- 仓库发现（规格标准版）已实现：crates/skillhub-adapters 新增 repo_discovery（§6.1 并行发现+单仓库失败仅告警、§6.2 下载/解压预算常量与函数逐字保留、§6.4 递归扫描+根目录 SKILL.md 用仓库名、§6.5 去重；GitHub zip 顶层包装目录按 git.rs 先例展平；sanitize_skill_source_path 逐字保留）。新契约：query= list_skill_repos / discover_repo_skills，command= add_skill_repo（upsert）/ remove_skill_repo / download_repo_skill（下载到 %TEMP%/skillhub-repo-skills/<id>/<name>，超 24h 残留尽力清理），bindings 已再生成。默认 4 仓库（anthropics/skills、ComposioHQ/awesome-claude-skills、cexll/myclaude、JimLiu/baoyu-skills）存 settings 表，首读自动播种。
- 前端发现页新增"仓库发现"卡片：仓库列表（启用开关=upsert、显式确认后移除、添加仓库）、"扫描仓库"结果列表（名称/描述/owner/repo@branch/README 链接）、"下载并导入"把下载目录以本地来源身份送入现有导入向导（下载本身不建受管对象，导入物化才写集中库）。
- 测试：adapters 集成 8 项（含 512MiB 预算/1 万条目/超长 symlink、分支回退、单仓库失败隔离、下载仅复制目标目录、穿越拒绝）+ lib 11 项；application facade 6 项（默认播种、upsert 排序、移除防重、非法坐标不联网、网络开关拦截）；前端 6 项。全量 Rust workspace 测试通过；前端 82 文件 468 项通过；check/build 通过。
- 验收提示：测试期间 discover 曾真实联网成功发现 cexll/myclaude 的 11 个真实 Skill（链路端到端可用）；正式验收请用网络开关=关来验证拦截路径。
- 已知边界：README 链接为普通 <a target=_blank>，打包后的 WebView 可能不响应点击（应用尚无通用外部打开器命令，open_official_release 也仅做校验）；发现查询为大流量操作（每仓库整仓下载），前端展示"正在扫描"诚实等待。

【Q3 完成 2026-09-06 / 提交 4db9341，已推送】
- FE-01 完成页汇总已实现：初始化完成后停留在汇总页，展示识别 Agent 数（含已选数）、扫描来源目录数、发现 Skill 数、跳过数（未变化+出错，诚实口径）；跳过扫描/跳过初始化/恢复分支按实际执行情况显示说明，不显示冒充的 0。
- 流程调整：完成后不再自动跳转，汇总页提供"打开批量导入"（携带扫描 roots，语义不变）与"进入主界面"两个显式按钮。
- 测试：OnboardingWizard 测试 16 项（新增计数正确、延迟进入、roots 传递、skip/restore 不误导）；i18n 双语 parity 通过。

【M2 批次 2026-09-06 / 提交 64ce4ede + 8320c6fe + 0febbda3，已推送】

【Q6 忽略项 UI + 修复预览 / 64ce4ede】
- 设置页技能库卡片新增：忽略项管理（list_ignore_rules 列表 + create_ignore_rule 三类主体 exact_path/exact_skill/exact_pending + 理由必填 + 确认后 remove_ignore_rule）；健康检查每个 finding 的"修复预览"（prepare_repair → RepairPlan 明细 → 显式"执行修复" → commit_repair），无可修复动作的 finding 不显示按钮，取消预览不写任何数据。
- 产品决策记录：DesktopPreferences 无"AI 输出语言"与"减少动效"字段，按队列指令诚实跳过（需要 Rust 契约扩展时再评估）；IgnoreSubject 实际形状为 {type, value}（非 payload），以 bindings 为准。

【Q4 操作框架地基 + 后台静默导入 / 8320c6fe】
- 验收反馈 #12 已修复：新增模块级全局操作跟踪器（platform/operationTracker，useSyncExternalStore 订阅），ImportWizard 提交循环接入——提交期间可离开页面/切换路由，循环继续并在全局指示器（应用壳 OperationIndicator，最近 3 条）显示进度与结果（成功/失败/跳过计数）。
- 提交中界面增加"可以离开此页面"提示；测试含"卸载后循环继续完成并写入 tracker"的关键用例（5 项 tracker + 3 项指示器 + 向导用例）。

【Q5 操作记录列表 + 恢复双页签 / 0febbda3】
- 恢复页改为"操作记录 / 备份恢复"双页签：操作记录页签合并两类事实并诚实标注来源——本会话后台操作（tracker）+ 持久化最近操作（BootstrapSnapshot.recent_operations，链接到既有操作详情页）；备份恢复页签承接原恢复确认流程并注明完整备份/恢复流程在数据保护页。
- 空态/读取失败均为诚实提示，不伪造历史；空记录与读失败分开呈现。

【M2 全量验证】前端 86 测试文件 492 项通过；pnpm check、pnpm build、git diff --check 通过。无 Rust 改动。i18n 并发合并期间曾丢失 HEAD 的 operations.completed 键（两语言），已从 HEAD 恢复并验证 parity。

【Q7 来源更新检查与决策（待与 Q8 一起提交推送）】
- Skill 详情"版本"区块新增"来源更新"面板：点击"检查来源更新"后按契约返回的五种状态如实呈现（已是最新/发现更新/发现更新且本地有修改/来源不可用/需要认证）；有更新时提供四个显式决策按钮（采用上游/保留本地/建独立分支/取消），对应 apply_source_update 的 UpdateDecision；应用后显示结果（含新版本号与"部署需要重新同步"提示）。
- 范围决策：IgnoreExternalChange 是部署维度契约（按 deployment_id），详情页当前无部署行 UI，入口留在部署相关页面（Q9/Q11）一并处理；批量更新检查无对应契约，不做批量入口（诚实边界）。

【M3 批次 2026-09-06 / 提交 4d327bd9 + 9a79a575，已推送（Q6 见 M2 批）】

【Q7 来源更新检查与决策 / 4d327bd9】
- Skill 详情"版本"区块新增"来源更新"面板（SourceUpdatePanel，5 项测试）：点击检查后按契约五种状态如实呈现（up_to_date/update_available/update_available_with_local_changes/source_unavailable/authentication_required）；有更新时提供显式决策按钮（采用上游/保留本地/建独立分支/取消）→ apply_source_update；应用后显示新版本与"部署需要重新同步"提示。
- 范围决策：IgnoreExternalChange 为部署维度契约（按 deployment_id），详情页无部署行 UI，入口留待部署相关页面（Q9/Q11）；批量更新检查无契约，不做批量入口（诚实边界）。

【Q8 批量导出入口 + 退出管理 / 9a79a575】
- 技能库批量栏新增"发起导出"：显式选择直接携带 skillIds 跳转数据保护页；"全部筛选结果"经分页枚举全部 id 后跳转，失败留在库页并提示。
- 数据保护页承接预填：逐技能 list_versions 预检并标注"可导出（当前版本）/无当前版本/版本读取失败"，不伪造可导出性；无 state 时行为不变。
- 退出管理分步：复选部署关系（list_deployments 全量）→ prepare_uninstall 预览影响（部署数/可用动作/preserves_central_library）→ 显式勾选动作（默认全不选）→ apply_uninstall_decision → 显示结果 phase；各步失败走结构化 alert。
- 诚实边界：ExportSkill.content 前端无法合法构造，仍按契约发 skills:[] 由后端从 selection+current 解析（Rust export_input 已有该逻辑）；后端将 backup/standard_export/remove_device_data/clear_credentials 暂标记 unsupported，违例组合显示后端结构化错误。
- 测试：Q7 5 项新增 + Q8 库页 3 项 + backup 7 项；M3 后全量前端 87 文件 507 项通过，check/build 通过。

【M4 批次 2026-09-06 / 提交 e7e648f6(Q12) + 1a9188d8(Q9) + 49eba85a(Q10) + 5b5083f3(Q11) + 33cd2219(i18n修复)，已推送】

【Q9 Agent 管理剩余 / 1a9188d8】
- 受管部署数按唯一 Skill 聚合（如"3 个 Skill · 5 条部署关系"）；自定义 Agent 新增/编辑/删除入口接通（删除需确认）；发现态 Agent 无管理入口（不伪装）。
- 诚实降级：重新定位目录、Agent 级忽略均无原生契约，详情页显示"需要原生契约支持，暂未提供"。
- 【重要发现】自定义 Agent 创建/编辑的 directory 契约只接受 grant_id，需宿主先 register_path_grant，而桌面宿主目前从不注册 grant（目录选择器只返回路径字符串）→ 运行时创建/编辑会被原生层拒绝并如实报错。需要后续原生任务补"选择器签发 grant + 宿主注册"链路。

【Q10 项目详情剩余 / 49eba85a】
- 项目详情新增"路径与访问状态"面板（device_path + 可访问/只读/不可访问徽标，快照无对应物理目标时显示"未记录"，读取失败显示"未知"）；装配计划按状态分组展示（已满足/已成功/待获取/冲突待选择/已跳过/失败），无计划显示诚实空态；注册预览候选按目录痕迹亲和度排序（同前缀/同根优先，悬浮说明注明依据）。
- 诚实降级：detach_management 需要 deployment_id，项目详情无部署记录落点，显示说明文案不伪造按钮。

【Q11 反向部署 + Agent 目标展开 / 5b5083f3】
- Agent 详情/项目详情新增"发起部署"按钮 → 跳转技能库并预选目标 → 勾选 Skill 后"统一部署"自动携带该目标（?target= 参数）。
- 批量部署页支持项目目标"展开关联 Agent（N 个）"一键加入；页面固定提示"批次逐个提交，不是原子操作：部分成功不会自动回滚"。

【Q12 安全结果深度 / e7e648f6】
- 发现项按基础/AI 分组，展示 file/line 证据（无定位显示"位置不可用"）；AI（llm）发现项处置接通（kind="llm"）；高风险处置必须经独立确认对话框（high_risk_confirmed 仅确认后为 true，native 层再加本地拒绝）；AI 卡显示发送范围说明（data_scope=explicit_selection 显示"仅发送显式选择的 Skill 内容"）；未配置 LLM 时运行入口禁用并说明原因。
- 降级：后端无批量处置命令，无批量入口；AI 检查运行中无进度/取消（cancel_operation 未接），仅按钮 loading。
- 流程问题记录：三个并行任务合并 common.json 期间曾丢失 agents.launchDeployment 键（合并时序竞态），已由 33cd2219 恢复；后续并行 i18n 合并需在完成后跑全量 typecheck 兜底。

【M4 全量验证】前端 88 测试文件 565 项通过；pnpm check、pnpm build、git diff --check 通过。无 Rust 改动。

【M5 前半批次 2026-09-06 / 提交 6b974017 + d14a274 + 2b5ed31e + 4bdf6cb + 6d95495b，已推送】

【Q13 来源重新关联 / 6b974017 + d14a274】
- Skill 详情元数据区块新增"来源重新关联"面板：输入本地/Git/HTTPS 地址（前端按规则分类构造 SourceDescriptor）→ relink_source；成功显示"已重新关联来源"，失败显示结构化错误。修复了接线时元数据三元表达式被破坏的渲染问题（d14a274）。
- 版本差异与回滚影响此前已在版本时间线接通（diff/impact 查询），本轮核实无缺口；按 Skill 的操作历史无契约，暂不做（全局操作记录已在恢复页提供）。

【Q14 抽屉真实数据 / 2b5ed31e + 4bdf6cb】
- 快速抽屉的快速视图从纯占位升级为真实读模型：get_skill 填充身份/描述/标签/许可证/生命周期/当前版本；按当前版本查询基础与 AI 检查状态；get_deployment_relations 填充部署关系（计数+名称）。无当前版本时检查如实显示 not_run；部署关系读取失败保持占位零值，不伪造。
- 流程问题记录：本次又在 check 失败的情况下完成了提交（快速视图测试 mock 类型错误），已补修复提交 4bdf6cb。重申纪律：提交前 pnpm check 必须显式确认通过。

【Q16 前半：滚动备份与保留策略 / 6d95495b】
- 数据保护页新增"滚动备份与保留策略"区块：保留份数输入 → run_rolling_backup（scope=full，decisions=[]）→ 显示"保留 X 份、清理 Y 份"；失败结构化 alert。附诚实缓存清理说明（仓库发现临时目录 24h 自动清理；集中库无手动清理缓存）。

【Q15 前半：概览指标下钻 / 1721a358，已推送】
- 概览指标卡升级为可点击链接：Skill 总数 → 技能库；Agent 数 → Agent 列表；项目数 → 项目列表；已部署数 → /library?deployment=deployed（复用库页既有 URL 筛选，返回时筛选状态保持）。柱形图下钻此前已存在（点击柱形进入对应 Agent/项目详情的部署视图），本轮核实无缺口。
- 剩余诚实记录：FE-04 卡片视图/组合视图/关系矩阵、FE-02 指标级更细筛选下钻仍属较大 UI 建设，未在本轮展开。

【M5 后半批次 2026-09-06 / 提交 a2735616 + 395f9c3 + 224d3bd + 6923fcf + 5daf27e，已推送】

【UI 偏好持久化契约 / a2735616】
- 发现并修复真实缺口：表格偏好/抽屉偏好在原生 facade 上从未实现（只有 unavailable），导致设置在真实桌面端不持久化。新增迁移 0007（ui_preferences 表）+ UiPreferenceRepository + 通用契约 get_ui_preference / set_ui_preference（原始 JSON 键值存储，值必须可解析、键非空）；bindings 已再生成。
- 前端 nativeSkillLibraryFacade 接通 load/saveTablePreferences 与 load/saveDrawerPreferences（缺失键时页面回退默认值并显示提示，不伪造）。

【FE-04 前半：技能库卡片视图 / 395f9c3→5daf27e】
- 库工具栏新增"卡片视图/表格视图"切换（经 ui_preferences 持久化，重启保持）；卡片展示技能名/用途/标签/部署数，点击卡片名打开抽屉。
- 过程记录：本轮两次在 check 失败下完成了提交（JSX 条件嵌套错误、mock 类型错误、测试语言断言、未用变量），均已即时补修复提交并重新全量验证。最终以 90 文件 574 项测试 + check + build + git diff --check 全绿收口。教训：必须以 check 的退出码为准，不能用"测试通过"替代。

【M5 全量验证】前端 90 测试文件 574 项通过；pnpm check、pnpm build、git diff --check 通过。

【Q17a 卸载备份 + FE-04 矩阵/标签分组 + Q16 缩放矩阵 2026-09-06 / 提交 bbff8267 + f89339e4 + a581e850 + 1ea80f4 + 79c5e8d6，已推送】

【Q17a 卸载备份（bbff8267 Rust + f89339e4 前端）】
- 退出管理的 backup 动作从"unsupported 拒绝"改为真实实现：apply 时先按 Full 范围创建完整备份包（BackupService.prepare/create/verify，落盘到集中库 .skillhub/backups/），成功后才执行 undeploy 等写动作；备份失败整体中止、不产生半卸载。message_code 区分 uninstall.decision_applied_with_backup。
- 测试：真实部署流（PrepareDeployment→CommitDeployment）后 apply [backup, undeploy_all] → 备份包 manifest 落盘 + 部署解除；无库根时整体中止且部署未动（2 项 Rust + 1 项前端参数断言）。
- 仍不支持（如实报错）：standard_export（走数据保护页导出）、remove_device_data、clear_credentials。

【FE-04 关系矩阵 + 标签分组（a581e850 + 1ea80f4 + 79c5e8d6）】
- 技能库视图模式升级为三选（表格/卡片/关系矩阵，经 ui_preferences 持久化）：矩阵为 Skill × 已部署目标一览（✓ 标记，列取真实部署关系，标签用已注册目标名，仅显示当前页 Skill 并有悬浮说明）。
- 卡片视图新增"分组方式"（不分组/按标签，持久化）：按标签分节展示，无标签技能归入"—"节。
- 新增持久化基建：get_ui_preference/set_ui_preference 通用契约 + 迁移 0007（ui_preferences 表）——表格/抽屉偏好在原生层首次真正持久化（此前为 unavailable 占位）。

【Q16 缩放验收矩阵（文档交付）】
- 新建 docs/development/缩放验收矩阵.md：15 个页面 × 100%/125%/150% × 布局/滚动/交互/键盘/诚实状态五维度，含历史问题回归点（100% 滚动条、概览窄窗口、150% 双页签换行）与回传格式。

【验证】前端 90 文件 577 项全绿；check/build/git diff --check 通过；Rust facade 卸载备份 2 项 + 既有全部用例通过。

【原生缺口修复：grant 链路 2026-09-06 / 提交 eb72ba5e，已推送】
- 自定义 Agent 创建/编辑的目录 grant 链路已打通（此前记录的"运行时创建/编辑会被原生层拒绝"缺口关闭）：
  1. 宿主 pick_local_directory 命令在用户选取目录后，将规范化路径注册为路径 grant（grant_id ≡ 规范化路径本身，与前端展示口径一致），并以 JSON 字符串返回 {path, grant_id}；
  2. CommandBridge 新增 with_local（桌面宿主注入具体门面）与 issue_grant_for_path/register_path_grant 转发；测试替身保持 None 不受影响；
  3. 启动时 re_register_custom_agent_grants：按已存储自定义 Agent 记录里的目录路径重新签发 grant（grant 注册表在内存中，重启后恢复），编辑既有 Agent 不再需要重新拾取目录。
- 测试（src-tauri path_grant_tests，桌面 crate 16/16 全绿）：签发后创建成功且 grant_id=规范化路径；未注册时创建报结构化错误（AgentProfileInvalidCapability，错误参数含 GrantNotAuthorized 原因）；模拟重启后经启动重注册，既有 Agent 可编辑改名。
- 设计说明：grant_id 即规范化路径（自签名），因此 Q9 时期前端"以路径作为 grant 标识"的发送方式在宿主注册后即为合法；目录选择器其余消费方（初始化库根、导入向导）仅用路径，行为不变。

【验证】桌面 crate 16 项测试全绿（含新增 3 项 grant 链路测试）；前端 90 文件 577 项全绿；check/build 通过。

【Q17b ~/.agents lock 发现与导入 2026-09-06 / 提交 e1977b8d，已推送】
- cc-switch 源码（C:\project\cc-switch）已回查：lock 解析规则逐条对齐——仅取 source_type=github 条目；分支回退 branch → source_branch → source_url（/tree/ 路径、#fragment、?branch=/?ref=）；空白分支视同缺失；解析失败/文件缺失一律空列表不阻塞。
- SkillHub 适配：adapters 新增 agents_lock 解析器（纯函数 5 项测试）；新查询 discover_agents_lock_skills（只读，解析 ~/.agents/.skill-lock.json，不动该文件）；bindings 再生成。
- download_repo_skill 扩展：空 directory = 整仓即 Skill（lock 条目常无 skill_path），测试覆盖整仓复制与空/非法目录区分（空→仓库根；穿越/绝对路径仍拒绝）。
- 前端发现页新增"~/.agents lock 导入"卡片（4 项测试）：扫描 → 列出 GitHub 条目（名称/repo@分支/仓库内路径）→ 逐条"下载并导入"复用既有仓库下载预算与导入向导；空态与错误如实呈现。
- 更新检测子项说明：SkillHub 的 check_source_update（本地来源目录哈希 vs 版本树哈希）已在 FE-11（4d327bd）实现并含三态判定；远端 GitHub 来源的哈希对比在"仓库发现导入以临时目录进入向导"的现状下缺少持久化远端坐标（来源记录指向临时目录），需先将 GitHub 来源做成长期来源记录（RelinkSource 或导入时记录 owner/repo/branch/directory）——已记录为后续契约增强，不在本轮强行实现。

【文档同步与归档 2026-09-06】
- 《当前开发状态》《验收矩阵》《验收指南（1k 节）》《遗漏清单（FE-07 lock 卡片）》均已同步至候选 `e1977b8d`。
- 归档：当前交接-2026-09-05.md、skill-discovery-module-spec.md（实现规格）→ docs/development/archive/。
- 新建《交接指令-2026-09-06.md》：面向下一 Agent 的继续开发指令（N1 外链打开器、N2 按 Skill 操作历史、N3 AI 检查进度/取消、N4 导出格式、N5 组合视图/批量汇总、N6 更细下钻、N7 远端更新检测）。

【文档归档补充 2026-09-06】
- docs/superpowers/（v0.1.0 时期实施计划/设计规格 45 件）与 docs/development/task-reports/（同期逐任务报告 68 件）归档至 docs/development/archive/ 对应子目录：内容为已落地实现的历史计划，活动文档（状态/清单/指南/交接指令）均无引用；两目录本就被 .gitignore 忽略，归档不影响远端。
- 注意：archive/superpowers/specs/2026-08-24 视觉系统设计为 v0.1 时期产物，若后续主题调整需参考视觉稿，可从归档取回。

【N1 原生缺口：外链通用打开器 2026-09-06 / 提交 0cf28a4，已推送】
- 决策：打包 WebView 中普通 `<a target="_blank">` 不触发系统浏览器，README 链接与"打开官方发布页"此前 inert。新增 `open_external_url` 契约，唯二规则：仅 https、且主机在白名单（github.com / raw.githubusercontent.com / docs.skillhub.dev / skillhub.dev / crates.io）；拒绝非 https、非白名单、含用户名密码、IP literal。理由：复用既有 external_link 校验范式、与 open_official_release 同源，避免引入无校验的任意打开器（XSS/钓鱼面）。
- 实现：core `validate_external_url` + `EXTERNAL_URL_ALLOWED_HOSTS`；application 新增 `ExternalUrlOpener` trait 与 `ExternalLinkService`（启动时宿主注入系统打开器 `open::that_detached`；无 opener 时返回 `ExternalLinkOpenerUnavailable` 结构化错误，不静默成功）；facade `open_external_url` 校验后调用 opener，`open_official_release` 从"仅校验不打开"占位改为真正打开。
- 前端承接：RepoDiscovery README 链接改走 `discoveryFacade.openExternalUrl`（确认对话框 → 命令）；Markdown 外链渲染器已接 `openExternalUrl` 现补真实调用；设置页"打开官方发布页"经 `open_official_release` 真正打开。i18n 复用既有 markdown 键，无新增键（parity 不变）。
- 测试（TDD 红→绿）：core external_link 8 项（白名单/非白名单/非 https/凭据/IP literal）；application facade 8 项（recording opener 断言 URL 到达、无 opener 拒绝、官方页真正打开、非官方拒绝）；desktop host external_link_tests 3 项（启动注册系统 opener、无 opener 拒绝、校验后打开）；bindings 经 `generate_bindings` 测试确认同步；前端 markdown/discovery 16 项测试通过（含 README 链接走确认→打开流程）。
- 验证：core/application/desktop 全量测试全绿；`cargo check`/`cargo fmt` 通过；前端 `pnpm check`/`pnpm build`/16 项测试通过。

【文档同步 2026-09-06 / N1 后】
- 《当前开发状态》（提交号→0cf28a4、累计 34 提交、原生缺口修复补 N1、剩余项 N1 划除、质量证据补 N1）、《验收指南（1L 节 + 进度提交号）》、《遗漏清单（FE-07 README 外链缺口划除）》均已同步至 `0cf28a4`。

【N2 原生缺口：按 Skill 操作历史查询 2026-09-06 / 提交 866bc7b，已推送】
- 决策（诚实降级）：评估结论——持久化操作日志（operations 表）当前是恢复日志：无 skill 维度列、object_results 无生产写入方、技能类变更（部署/删除/导入）不写该日志、bootstrap 的 recent_operations 恒为空。为无写入方的日志加 skill_id 列是纯机械改动且不产生用户价值，故按交接指令许可选择"诚实降级为全局过滤"：list_skill_operations 返回全局日志条目（operation_id/kind/phase/error_code），携带 filtered=false 与稳定限制码 skill_dimension_not_recorded；后续若日志补 Skill 维度，同一契约可无损升级为真过滤（前端已按 limitation 码显示说明）。
- 前端承接：Skill 详情"外部变化与操作历史"面板（getInsights 原为 unavailable 占位）接通真实数据——条目格式 `kind · phase[ · error_code]`、顶部固定限制说明、空日志显示"暂无操作日志记录。"空态；i18n 新增 operationHistoryEmpty / operationHistoryLimitation 两键（zh-CN 与 en-US 成对，parity 保持）。
- 测试（TDD 红→绿）：application facade_skill_operations 3 项（空日志诚实空态 / kind+phase+error_code 条目映射 / 对无关 Skill 不虚标按 Skill 过滤）；前端 nativeApi getInsights 映射 1 项 + InsightPanels 限制说明 1 项；全量 91 文件 585 项通过。
- 附带修复（环境发现）：本机环境 std symlink_dir 返回 Ok 但链接未实际落盘（沙箱/过滤卷特性），导致 path_policy 的 create_rejects_existing_symlink_ancestor_that_escapes_root 失败——两个符号链接测试加固为"链接未真正落盘则跳过"（creates_real_symlink helper），PathPolicy 产品代码无问题、未改动。

【Git 事故与恢复 2026-09-06】
- 会话中段 .git/objects/pack/*.pack 数据文件整体丢失（仅余 .idx 残骸），对象库不可读、git stash 触发损坏；经 git fetch origin 恢复对象、清理孤立 idx/multi-pack-index、重建分支引用后完全恢复。N1 改动当时全部在工作树中未受影响。
- 已知 ref 丢失 bug 在 N2 提交（866bc7b）后再次出现：按预案用 Write 工具把完整 SHA 写回 .git/refs/heads/feat/v0.2.0-product-completion 与 .git/refs/remotes/origin/... 后恢复正常。后续 Agent 每次 commit 后务必立即 rev-parse 校验。

【文档同步 2026-09-06 / N2 后】
- 《当前开发状态》（提交号→866bc7b、累计 35 提交、原生缺口修复补 N2、剩余项更新、质量证据 585 项、1c~1m）、《验收指南（1m 节 + 进度提交号）》、《遗漏清单（FE-06 按 Skill 操作历史划除）》均已同步至 `866bc7b`。

【N3 AI 检查进度/取消 2026-09-06 / 1b89ed4】
- 问题（FE-12 遗留）：run_llm_safety_check 在阻塞线程上同步执行，安全页既看不到进度也无法取消，重复点击会重发检查。
- 修复：facade 维护运行中 LLM 检查注册表并新增 list_running_llm_checks 查询；cancel_operation 接通取消令牌中止阻塞线程；安全页 AI 卡轮询进度 + 显式取消 + 诚实取消反馈，重复点击不重发，取消后可重跑。
- 顺带加固（同提交）：本机环境 symlink_dir 报 Ok 但链接未真正落盘，导致部署 apply 以无关校验错误失败且 deployment-target 测试偶发红——adapters apply 现在校验链接真正落地，未落地诚实返回 deployment.symlink_not_supported；capability 探测同样校验落地；相关测试在本机三连跑稳定。
- 自动化证据：facade_llm_cancel 3 项（运行中可见/取消生效/取消后可重跑）；前端全量 586 项通过；desktop 19 项（含 generate_bindings 同步）；cargo check 通过。真实"模型调用中途取消"的端到端体验属人工验收项（验收指南 1n 节）。
- 注意：unsupported 操作测试由 CancelOperation 改用仍为占位的 AcknowledgeRecovery（CancelOperation 已接通真实取消，不再返回 internal 错误）。

【N4 导出格式（文件夹/ZIP） 2026-09-06 / 964c134】
- 问题（原生缺口 N4）：标准导出只能产出文件夹树，分享需手动压缩。
- 修复：ExportInput 增加 format 字段（folder | zip，缺省 folder，旧载荷完全向后兼容）；ZIP 导出把相同 manifest.json + skills/<skill-id>/SKILL.md 打成单一 deflate 压缩包；敏感内容决策两种格式行为一致；数据保护页导出表单新增格式选择（zh/en 本地化）。
- 顺带修复（同提交）：迁移测试钉死 schema v6 的前序漂移（migration 0007_ui_preferences 落地后未同步，migrations.rs/database_upgrade.rs 共 6 处断言）；version-store 符号链接逃逸测试在本机"symlink 假成功"环境下改为校验链接真正落盘否则跳过。
- 自动化证据：storage export_uninstall 4 项（含 zip 归档布局/内容/无散落目录）+ core 兼容测试 + 前端备份模块 15 项（含 zip 选择传递）；前端全量 587 项；storage 24 套三连跑稳定；desktop 19 项（含 generate_bindings 同步）。
- 验收指南新增 1o 节（导出格式）；FE-17 行更新。

【Git 引用修复 2026-09-06 / N4 后】
- ref 丢失 bug 在 964c134 提交后再次出现；本次教训：修复引用时不要从 cut 截断输出复制 SHA（曾误写入 39 位截断 SHA 导致 "broken ref"），必须用 git rev-parse 取完整 40 位再写入 refs/heads 与 refs/remotes/origin 两处。

【N5a 组合 CRUD/导出入口 2026-09-06 / 73f3be3】
- 问题（FE-04 剩余）：组合仅有 create/list 契约，技能库页从未展示组合，导出组合无入口。
- 修复：新增 update_combination/delete_combination 契约（按名称定位；删除仅移除组合记录，Skill 不受影响；未知名称报 ObjectNotFound）；技能库页新增组合视图面板：列表（成员显示名）、新建（名称+成员勾选）、成员替换、内联确认删除、整组合标准导出。
- 边界决策：①组合契约沿用"名称"作为标识（combinations.name 无唯一约束，引入 id 会改变现有 wire 契约，故暂不支持重命名，需要时先补唯一约束+迁移）；②组合整体"部署"入口未做——逐成员部署需要 N5b 的批量可执行/跳过/冲突/失败汇总组件支撑，避免一次性弹 N 个确认框；③敏感内容组合导出如实透出后端 decision-required 错误。
- 自动化证据：facade_catalog_source 组合测试（更新/未知名称拒绝/删除/重复删除）+ 面板 3 项测试；前端全量 92 文件 590 项；desktop 19 项（generate_bindings 同步）。
- 验收指南新增 1p 节；FE-04 行更新。

【N5b 批量操作统一汇总 2026-09-06 / 79fa7d9】
- 问题（FE-04 剩余）：批量部署只有平铺结果列表，批量删除首错即停且只报一条笼统错误——部分完成的批次无法与整体失败区分，用户无法得知哪些 Skill 还需处理。
- 修复：新增共享 BatchOperationSummary 组件（成功/跳过/冲突/失败分组计数 + 逐项消息）；批量部署页提交结果改走该组件；批量删除循环改为逐项推进并逐项汇总（已删除的保持删除状态、失败项单列原因、库列表始终刷新避免显示已删行）。
- 边界决策：①"冲突"分组保留给预览阶段的目标冲突（提交前仍以告警列表展示，提交后映射为 conflict 状态）；②单 Skill 部署页的旧 DeploymentResults 保持不变（汇总仅用于批量场景）。
- 自动化证据：组件 2 项（分组计数/全成功态）+ 批量部署混合结果聚合 1 项 + 库页单项失败不丢弃其余结果 1 项；前端全量 93 文件 594 项；check/build 通过。
- 验收指南新增 1q 节；FE-04 行更新。

【N6 概览按标签下钻 2026-09-06 / c7eef74】
- 问题（FE-02 剩余）：概览只能按 Agent/项目下钻部署数据，Skill 只能进入未筛选的库页，而库页 URL 早已支持 tag 参数。
- 修复：BootstrapSnapshot 新增 tag_categories（SQL 按 skill_tags 聚合，无 Skill 的标签省略）；概览侧栏新增"按标签的 Skill 数量"清单（数量降序），条目跳转 /library?tag=<URL 编码名称>；共享明细列表增加可选 countLabel 参数以复用部署明细样式而不伪造图表维度。
- 边界决策：标签计数以库内 skill_tags 关系为准（与库页 facets 同源）；不做标签图表柱状化（标签数量可能很多，清单比图表更可访问）。
- 自动化证据：storage tag_chart 聚合测试 + 概览下钻导航测试；前端全量 93 文件 595 项；storage 24 套 / core 33 套 / desktop 19 项（generate_bindings 同步）。
- 验收指南新增 1r 节；FE-02 行更新。

【N7 决策与第一阶段 2026-09-06 / 6e0b9c5】
- 决策（原队列 N7 需用户决策项，按"最符合文档精神"原则先行决策并记录）：
  ①坐标记录方案：选择"导入即记录"——仓库发现下载时登记 UpstreamOrigin(url/branch/directory)，随导入候选回流，提交导入后写入 sources(kind=git, metadata_json={branch,directory}) + skill_sources(origin)，无需 schema 迁移、无需用户手动 RelinkSource；本地导入 Skill 仍可手动 RelinkSource 补录（后续可扩展）。
  ②哈希方案：与 cc-switch check_updates 对齐的可移植目录哈希（sha256，相对路径排序 + 文件内容）；本地哈希基于当前版本内容目录；远端按仓库分组下载 zip 提取后对 directory 计算哈希对比。
- 已交付（N7a，坐标记录闭环）：download_repo_skill 登记 → discover_import_candidates 盖章 → commit_import 落库（幂等，不动既有来源行）；本地导入无坐标不落库；前端零改动（候选原样回流）。
- 待实现（N7b）：check_source_update 支持 git/https 来源——拉取远端仓库归档、定位 directory、计算远端哈希与本地对比，返回 UpToDate/UpdateAvailable；预计复用 SourceFetcher + zip 提取 + 可移植哈希。
- 自动化证据：facade_upstream_origin 3 项（盖章 / 落库+git 描述符 / 本地不落库）；前端 93 文件 595 项；core 33 套 / storage 24 套 / application 13 套 / desktop 19 项。
- 验收指南新增 1s 节。

【N7b 远端来源更新检测 2026-09-06 / 6bdfc98】
- 功能：check_source_update 支持 git 来源（N7a 坐标）——解析 owner/repo → 仓库发现提供者拉分支归档 → 定位 directory → hash_tree 与当前版本对比（UpToDate/UpdateAvailable）；不可达/缺坐标/URL 非法一律诚实降级 SourceUnavailable。
- 顺带修复两个测试暴露的潜在缺陷：①revision_for_skill 对 NULL revision 直接崩溃（git 来源无 revision）——按 Option 读取；②仓库导入曾落两行 origin（临时本地目录 + git），for_skill 按 s.id 取行非确定（哈希 id 排序），取到临时目录行且目录已清理时 SourceUnavailable——现在 git 来源为唯一 origin，record_upstream 改 upsert 补写坐标。
- 实现细节：归档下载在独立线程跑 current-thread runtime（复用 LLM 检查的模式）；RepoDiscoveryProvider 移入 RwLock 以支持测试注入 fixture 服务器（set_repo_discovery_provider_for_tests）。
- 边界决策：无本地改动维度可判定（tree_hash 取自导入时清单），不一致时如实报 UpdateAvailable 而非 UpdateAvailableWithLocalChanges。
- 自动化证据：facade_upstream_check 3 项（UpToDate/UpdateAvailable/缺坐标降级，本地归档 fixture 服务器，四连跑稳定 0.2s）；core 33 套 / storage 24 套 / application 14 套 / desktop 19 项（generate_bindings 同步）；前端 93 文件全过 + check/build。
- 验收指南新增 1t 节（需联网人工验收）。

【N8 批量来源更新检查 2026-09-06 / d46019f】
- 来源：队列空后重读《前端页面功能与UI遗漏清单》，FE-11 剩余项"批量检查/升级"中"批量来源更新检查无契约"（状态文档第三节第 3 条同步记录）。
- 功能：新增查询 check_source_updates { skill_ids } —— 显式传入 Skill 列表，逐条复用单 Skill 检测（本地路径 + 远端 git 来源都支持）；返回 source_update_checks（每条 { skill_id, state }）。
- 边界决策：
  ①范围取显式 skill_ids 而非"全部 Skill"：网络归档下载成本高，由用户选择决定范围，避免隐式全量联网。
  ②单条失败按项降级：未知 Skill/网络关闭/来源缺失/远端不可达 → 该条 SourceUnavailable，批次绝不整体失败、不掩盖其余结果。
  ③只检查不批量升级：可升级项仍需逐个进详情页显式确认应用，避免静默覆盖本地修改（与"写操作必须显式确认"约束一致）。
  ④前端独立第四组"需要认证"（authentication_required），不与"来源不可用"混组，诚实区分。
- 前端：技能库页批量栏新增"检查来源更新"按钮（仅 facade 提供契约时挂载，预览 facade 诚实缺省）；新组件 SourceUpdateCheckSummary 按已是最新/可升级/来源不可用/需要认证四组展示并附诚实提示；显式选择与"全选过滤结果"都支持（复用 selectedSkillsForRemoval 的分页解析取名字）。
- 自动化证据：application facade_source_updates_batch 3 项（混合状态逐项 / 未知 Skill 降级不掩盖批次 / 空列表）；core 33 套 / application 15 套全过；desktop generate_bindings 同步；前端 94 文件 599 项 + check + build + diff --check 全过。
- 验收指南新增 1u 节（批量检查需联网，建议与 1t 同场景人工验收）。
【N9 前置缺陷 2026-09-06 / `2fc5179` 已推送】
- 复核发现：忽略规则服务原先以 `LocalIgnoreBackend { rules: Vec }` 保存，应用重启后规则消失；`list_pending_items` 直接读取 SQLite，不会读取忽略/暂缓规则。因此此前“忽略项重启后仍存在”和“暂缓可用”的结论均不成立。
- 根因证据：新增回归测试先失败——`ignore_rules_survive_reopening_the_application_database`、`deferred_pending_item_is_hidden_until_its_due_date`。
- 修复范围：新增 SQLite settings 持久化仓库；创建、读取、删除均走该仓库；待处理列表按 exact_pending/exact_skill 规则过滤，仅在 `today < defer_until` 时暂缓，到期当天自动重新出现。exact_path 不影响待处理派生项。
- 已通过：重启保留、暂缓隐藏、到期重现、原日期边界测试；application facade 全量 65 项、storage 全量通过。clippy 受既有 core `ExportFormat` 的 derivable_impls 阻断，未改无关文件。此项只是 N9 前置修复，不代表待处理中心完整实现。

【AR 批次修复 2026-09-07 / 提交 f327590 + 72889fa + dca4e69 + 3b4691a + d4a3b3e + 6ca7853，已推送】
针对《人工验收记录-2026-09-06.md》第 2 轮验收的 P0 全部与部分 P1 问题，逐项根因修复（TDD 先红后绿）：

- AR-020（f327590，Rust 契约+前端）：本地创建/仅本地目录来源的 Skill 不再被当作"有上游"。新增 SourceState::NoUpstream（bindings 再生成）；check_source_update 只把 git 来源（含 N7a 坐标）视为可检查上游，未知 Skill 报 object.not_found（批量侧仍按项降级）。测试：facade_source_update_semantics 2 项 + 重写 facade_catalog_source 1 项。
- AR-017（f327590）：①"采用上游版本"对 git 来源真实走归档下载管线创建新版本（与检查共用 fetch_remote_source_dir），不再报"remote acquisition is not configured"；②本地有修改状态不再提供"采用上游"按钮并说明原因；③新增共享 nativeErrors 模块统一解析 AppError 对象/JSON 字符串/宿主字符串，已知错误码映射可读文案，未知码回退通用文案并附原始码——[object Object] 根治。
- AR-008（72889fa + dca4e69）：set_library_root 已初始化冲突补 reason=library_root_locked 参数；onboarding 全部 catch 改用结构化错误呈现（含库根锁定专属文案），不再显示误导的"尚未连接到本机服务"。
- AR-002（dca4e69）：RestoreStep 的 prepare/commit 失败改为显示真实错误码与可读原因（真实备份若损坏会显示 checksum_mismatch 等具体码，不再吞掉）。
- AR-001（dca4e69）：applyCustomRoot 在持久化成功后立即回显所选目录（不再依赖重启完成），LibraryStep 区分"默认位置/已选择的集中库位置"。
- AR-007（dca4e69，宿主 Rust）：pick_local_directory 的 grant 签发失败降级为 grant_id:null（路径照常返回），选择器不再整体失败；canonicalize/非目录错误仍拒绝；前端选择器容忍 grant_id 缺失。
- AR-011（3b4691a）：仓库扫描中显示"已用时 N 秒"+ 每仓库 60 秒预算说明（aria-live）；失败按结构化原因呈现（如 network.disabled）。
- AR-014（3b4691a）：operationTracker 新增 hasRunningKind/useHasRunningOperation；后台导入进行中，发现页所有导入入口与向导提交按钮统一互斥锁定并诚实提示。
- AR-015（3b4691a）：/operations 路由改为 OperationsRecordsPage（会话 tracker + 持久化 recent_operations 双来源列表），不再调用恒空的 latest 详情；/operations/:id 详情路由保留。
- AR-023（3b4691a）：全局指示器行可手动关闭；完成/取消行 10 秒自动消失，失败行保留至手动关闭。
- AR-018（d4a3b3e）：Markdown 编辑分屏在 56rem 不再塌成单列（常见窗口下编辑+预览并排），40rem 以下才收起。
- AR-019（d4a3b3e）：编辑器提供三条明确路径：放弃修改并返回（确认后仅弃本机草稿）、保存（说明会创建新版本并设为当前）、另存为副本（禁用+说明需要新契约，不伪造）。
- AR-004（d4a3b3e）：健康检查按钮下方固定说明范围——只检查未完成应用操作；不含重名/重复检测（重复分析在导入时）、内容校验、部署一致性。
- AR-026（d4a3b3e）：退出管理区块补场景说明（何时用、默认保留集中库、不删 Agent 原有文件）。

自动化证据：前端 98 文件 632 项全绿 + check（eslint+tsc）+ build 通过；i18n zh/en 键集 parity 1307 键无差异；skillhub-desktop 21 项（含 generate_bindings 同步）；skillhub-application 16 套全过；cargo fmt --check / git diff --check 通过。clippy 基线阻断（core export/mod.rs derivable_impls）为既有问题未顺手修改。

待人工复验：全部进入《v0.2.0-manual-acceptance-guide.md》新增 1v 节（14 条回归步骤）。

【AR 第二批修复 2026-09-07 / 提交 8434ee1 + 0c6bda2 + 94535ae + dfa2f16，已推送】
- AR-009/010（8434ee1，子代理 E）：冲突条目四要素可读化（候选名+来源路径 / 原因完整句 / 已有 Skill 与差异 / 每个决策的影响说明），原始码退居"排查信息"小字；按原因类别筛选（带计数），批量应用仅作用于筛选集合，单条可覆盖。测试 +5。
- AR-024 + AR-005（0c6bda2，子代理 F）：批量操作栏按频率分组重排、同名去重、"部署…/发起导出"命名区分、删除独立分组降级视觉且 FORCE DELETE 保留；新增 BrandTag 组件（规范品牌拼写 + 确定性品牌色），应用于初始化识别页与 Agent 页。测试 +18。
- AR-025（94535ae，主 Agent 契约工作）：标准导出重设计——ExportSkill 增加 files（版本内全量文件 base64，单文件 16MiB/单版本 64MiB 诚实上限）、ExportInput 增加 output_dir（需宿主选择器签发的路径 grant，未授权路径拒绝 PathOutsideAllowedRoots）；两种格式都产出外层 ZIP（文件夹模式=完整 Skill 文件夹，ZIP 模式=独立可导入 Skill ZIP）；目录名用显示名+确定性冲突后缀，不再泄漏内部 ID；数据保护页新增输出目录选择器与诚实格式说明。测试：storage export_redesign 5 项 + facade 导出 4 项（TDD 先红后绿）。旧 export_uninstall 断言按新布局改写（原断言验证的是被验收推翻的旧结构）。
- AR-021（dfa2f16，主 Agent 契约工作）：①发现并修复重大断链——getVersions/getVersionDiff/getRollbackImpact/commitRollback 此前从未在原生 facade 实现，真实桌面端版本时间线/比较/回滚全部不可用；现已分别接通 list_versions、diff_versions、部署关系、set_current_version。②VersionResult 契约增加 created_at_epoch（清单文件 mtime，字符串化以过 Specta）与 sequence（按捕获时间序，最早=1；时间不可得诚实为 None）；时间线显示 vN 序号+捕获时间，当前徽标由 current 标记驱动，完整哈希不再作为版本名（时间未知回退短哈希+说明）。测试 +5。
- AR-016（dfa2f16，主 Agent）：忽略项创建入口移除"精确 Skill/精确待处理"自由输入，仅保留"路径忽略"；卡片补工作流说明（检查→修复预览→执行；路径忽略）；既有规则仍可见可删。测试 +1。

自动化证据：前端 99 文件 656 项全绿 + check + build；i18n parity 1307+ 键无差异（本批新增后实际以 parity 测试为准）；Rust core/storage/application/desktop 全套通过（含 generate_bindings 同步）；fmt/diff --check 通过。
待人工复验：指南 1x 节（8 条）。

【AR 第三批修复 2026-09-07 / 提交 6e1077e + 46d5402 + 835705d + 7941873 + 2260f36，已推送】
- AR-012（6e1077e，子代理 G）：发现页重构为"主页固定 4 模块卡片 + 独立子页 + 返回导航"；/discovery/{local,online,repo,lock} 四条子路由承载原有全部能力；onboarding 引导导入落点同步迁移。
- AR-003（6e1077e，子代理 G）：在线搜索命中提供"查看"（外部打开器）与"安装导入"（复用仓库下载管线进入导入向导）；无法识别的来源禁用安装（宁保守，不伪造）。
- AR-013（6e1077e，子代理 G，按授权决策执行）：lock 模块保留能力、视觉降级（末位卡片/次级样式/来源说明）；完整产品复盘留待后续。
- AR-022（46d5402，主 Agent）：组合管理独立子页 /library/combinations（列表/新建/成员维护/删除/导出集中承载），技能库页仅保留入口链接，不再挤压 Skill 明细视图；成员显示名经真实分页查询解析，失败诚实提示。
- AR-006（835705d，主 Agent）：①输入手动来源不再清空已选扫描来源（原行为正是混合导入断点），新增"添加到已选来源"追加动作；②候选门槛页每个来源行提供"移除"，仅剔除该来源候选，其余保留可继续，无需重读。
- AR-027 第一步（7941873，主 Agent）：设置页备份卡片更名"数据保护"并注明父子层级（备份/恢复/导出/退出管理为子能力）。完整设置分层重设计待产品设计阶段。
- i18n 审计基线（2260f36）：新增 scripts/i18n-cjk-audit.mjs 扫描脚本；基线 40 文件 190 行含 CJK（绝大部分为代码注释）；用户可见硬编码集中在 features/deployment/nativeApi.ts 等，已列为后续分批修复项。

自动化证据：前端 101 文件 674 项全绿 + check + build；i18n zh/en 键集 parity 无差异；git diff --check 通过。
待人工复验：指南 1y 节（8 条）。

【N9 待处理中心深度 + i18n 批次一 2026-09-07 / 提交 54e7519 + 8328d44 + 03d0ce2 + ce49e42，已推送】
- N9 后端契约（54e7519）：PendingItem 增加 due_date（试用到期日）/risk（严重级别映射 high/medium/low）/affected_deployments（当前生效部署关系数），全部由 list_pending 从真实行派生（serde default 兼容旧载荷）；bindings 再生成。测试：storage bootstrap_repository 新增 pending_items_expose_due_date_risk_and_deployment_impact。
- N9 前端深度（8328d44，子代理）：①每条事项显示到期日/风险徽标/影响面；②暂缓（7/30 天）与永久忽略走真实 exact_pending 忽略规则契约，暂缓到期自动重现；③处理历史卡片列出全部暂缓/忽略记录（理由/创建时间/截止日）并可撤销（remove_ignore_rule）；④保存视图经 ui_preferences 持久化类型筛选；⑤安全批量处置：批量暂缓直接执行+进度显示，批量永久忽略二次确认，批量转换/重查/恢复因无原生契约诚实不提供。测试 +14。
- i18n 批次一（03d0ce2 + ce49e42，主 Agent）：deployment 与 reconcile 的 nativeApi 不再抛硬编码中文句子，改抛稳定错误码（如 deployment.plan_stale），页面经 describeNativeError 呈现本地化文案+错误码；部署结果行文案改用 deployment.results.message.* 翻译键。
- 自动化证据：前端 101 文件 691 项全绿 + check + build；i18n parity 无差异；Rust core/storage/desktop 全过（含 bindings 同步）；git diff --check 通过。
- 待人工复验：指南 1z 节（6 条）。

【i18n 批次二 + 版本命名 2026-09-07 / 提交 f70cba4 + 3766f9b，已推送】
- i18n 批次二（f70cba4）：security/nativeApi（11 处）与 removal/nativeApi（7 处）的硬编码中文错误句全部改为稳定错误码（如 security.high_risk_confirmation_required、removal.undeploy_not_wired）；SecurityResults 与 SkillDetailPage 的删除/解除部署错误渲染接入 describeNativeError；新增 security.errors.generic 键。审计结论：platform/operationTracker.ts 与 features/operations/OperationsList.tsx 的 CJK 均为代码注释（非用户可见），projects/api.ts 为 dev-preview fixture（生产不可达），均无需替换。测试：security nativeApi 新增错误码断言（+1）。
- AR-021 用户可维护版本名（3766f9b）：迁移 0008 新增 version_labels 表；新命令 set_version_label（去首尾空白、非空、≤64 字符，重复命名即替换）；VersionResult 增加 label；时间线显示优先级 = 用户命名 > vN 序号 > 短哈希，并提供行内命名编辑器（保存/取消）。来源版本号抓取（GitHub release/tag）仍需网络策略与产品设计，见交接指令。
- 自动化证据：前端 101 文件 693 项全绿 + check + build；Rust core/storage/application/desktop 全过（含 bindings 同步、迁移 0008 升级测试）；git diff --check 通过。
- 待人工复验：指南 1aa 节（4 条）。

【i18n 批次三 + AR-021 来源版本 + N12 + N10 2026-09-07 / 提交 54e7519→5e5185e（含 1bb 相关 6 提交），已推送】
- i18n 批次三（1b5ff23）：import/nativeApi 的 8 处用户可见中文句改稳定码/翻译键（import.remote_download_not_wired、import.no_default_action、importWorkflow.commitMessages.* 等）；ImportSummary 消息经 t() 翻译并保留未知码原文；向导失败路径统一 describeNativeError。审计澄清：operationTracker/OperationsList 的 CJK 为代码注释、projects/api.ts 与 security/settings 的 fixture 为开发预览数据，生产不可达，无需替换。
- AR-021 来源版本号（83277c7）：RepoDiscoveryProvider.fetch_latest_release_tag——经 /releases/latest 302 重定向提取最新 tag（15 秒预算、无认证、无 release 诚实 None、生产域名出口断言）；check_remote_source_update 尽力附带 upstream_label（失败不影响检查结论）；面板优先显示。测试：adapters +2、facade +1（fixture 服务器支持 release 302）。
- AR-021 版本命名（3766f9b，见 1aa）：迁移 0008 version_labels + set_version_label（去空白/非空/≤64 字符）+ VersionResult.label + 时间线行内命名编辑器。测试：facade +1（命名/替换/空名拒绝）、迁移版本钉 8。
- N10 详情单 Skill 导出（aa7479e/5e5185e）：详情版本区导出入口携带 skill id；导出表单版本范围（当前版本/全部历史版本，经 listVersions 展开 History）。测试 +2。
- N12 确定性重复读模型（0fb37a5）：新查询 list_deterministic_duplicates——当前版本内容哈希相同的其他 Skill（联 display_name）；快速抽屉重复候选从恒空占位改为真实读模型，读取失败保持空。测试：storage +1、前端 +1。
- 自动化证据：前端 101 文件 695 项全绿 + check + build；Rust core/storage/application/desktop/adapters 全过（bindings 同步）；i18n parity 无差异。
- 待人工复验：指南 1bb 节（4 条）。

【AR-027 UI 打磨批次一 + N10/N12 收尾 2026-09-07 / 提交 f70cba4 + 3766f9b + 83277c7 + aa7479e + 5e5185e + 0fb37a5 + 3f5eff8 + 6eee3d1，已推送】
- i18n 批次三（1b5ff23）：import/nativeApi 8 处中文句→稳定码/翻译键；ImportSummary 消息 t() 翻译；向导失败路径统一 describeNativeError；nativeErrors 增加 import 码映射。审计澄清：operationTracker/OperationsList 为注释、projects/api.ts 为预览 fixture，无需替换。
- N12（0fb37a5）：list_deterministic_duplicates 查询（内容哈希相同互为确定性重复，联显示名）；抽屉重复候选接真实读模型。测试：storage +1、前端 +1。
- AR-021 来源版本（83277c7）：fetch_latest_release_tag（releases/latest 302 提取 tag，15 秒预算、无认证、无 release 诚实 None）；UpstreamCheckResult.upstream_label；面板优先显示。测试：adapters +2、facade +1。
- AR-021 版本命名（3766f9b）：迁移 0008 version_labels + set_version_label + VersionResult.label + 时间线行内命名编辑器。测试：facade +1、迁移版本钉 8。
- N10（aa7479e/5e5185e）：详情版本区导出入口 + 导出版本范围（当前/全部历史）。测试 +2。
- N11（3f5eff8）：宿主命令 open_local_directory（校验+系统文件管理器）+ 数据保护页"集中库目录"区块（BackupFacade.libraryPath）+ 恢复点自动管理说明。测试：桌面 +1、前端 +1。
- AR-027 UI 打磨批次一（6eee3d1）：设置页按盘点 1→6 分区顺序渲染（通用→数据保护→网络与 AI→自动化→技能库维护→应用更新），分区标题可见，密度控件并入通用分区。测试 +1（顺序断言）。
- 自动化证据：前端 102 文件 700 项全绿 + check + build；Rust core/storage/application/desktop/adapters 全过（含 bindings 同步、迁移 0008）；i18n parity 无差异。
- 待人工复验：指南 1bb/1cc 节。
