# SkillHub Task 交接模板

每个开发 Task 完成或暂停时，复制本模板为对应的 Task 报告，提交到仓库。交接内容不能依赖聊天记录。

## 1. 基本信息

- Task 编号：
- Task 名称：
- 状态：`待开始` / `进行中` / `待复审` / `已完成` / `已阻塞`
- 基线提交：
- 当前提交：
- 分支：
- Worktree：
- 负责 Agent：
- 复审 Agent：

## 2. 目标和边界

### 本 Task 要完成

- 

### 本 Task 明确不做

- 

### 允许修改的文件范围

- 

## 3. 依赖和接口

- 依赖的权威需求章节：
- 依赖的实施计划：
- 已冻结的 Rust 接口：
- 已冻结的 TypeScript bindings：
- 依赖的前序 Task 提交：

## 4. 实现结果

- 修改文件：
- 新增行为：
- 兼容性或平台差异：
- 数据迁移或持久化变化：
- 用户可见文本和 i18n 处理：

## 5. 测试和验收

### Task 测试

```text
命令：
结果：
```

### 阶段测试

```text
命令：
结果：
```

### 必要检查

- [ ] `cargo fmt --all -- --check`
- [ ] `cargo clippy --locked --workspace --all-targets --all-features -- -D warnings`
- [ ] `git diff --check`
- [ ] Specta bindings 漂移检查
- [ ] 前端 ESLint、TypeScript、Vitest、生产构建（如涉及前端）

## 6. 风险和未解决事项

- 

明确区分：代码缺陷、外部依赖、待用户决策、后续 Task 范围和暂不支持能力。

## 7. 交接给下一位 Agent

下一位 Agent 应先阅读：

1. `AGENTS.md`
2. `docs/development/当前开发状态.md`
3. 本 Task 报告
4. 对应需求、架构和实施计划

建议启动说明：

```text
你正在接手 SkillHub 的 <Task 编号>/<Task 名称>。
先阅读 AGENTS.md、当前开发状态、Task 报告和对应实施计划。
基线提交是 <提交号>，只允许修改 <文件范围>。
先运行 <基线测试命令> 确认环境，再按 TDD 开发。
完成后必须报告修改文件、测试结果、风险和提交哈希；未经复审不要合并 main。
```
