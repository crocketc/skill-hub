# SkillHub 开发状态

更新时间：2026-09-08
当前分支：`feat/v0.2.0-product-completion`
当前同步提交：`196d1ac`
浏览器验收基线：`eaee655`（`pnpm test:e2e` 22/22 通过）

## 已完成摘要

- 历史开发队列 Q1~Q17、N1~N12 与 AR-001~AR-027 的已授权修复均已有实现与自动化证据。
- 发现、导入、技能库、部署、组合、项目、操作记录、版本、数据保护、设置等页面流程已完成浏览器自动化预览验收。
- 当前分支与远端一致；本次之后的状态以 `git status --short`、`git rev-parse --short HEAD` 和实际测试结果为准。

## 待推进项

### [待人工验收 / 发布阻塞]

1. 在 Windows 与 macOS 真机取证：初始化、真实目录导入、部署/解除部署、删除保护、备份、恢复、导出、网络来源更新和系统浏览器。
2. 使用与候选提交对应的签名发布资产完成检查、下载、安装、重启与失败回滚；未完成前不得创建 `v0.2.0` tag。

### [待开发]

1. 完成 i18n 收尾：其余用户可见硬编码、后端错误、空态/弹窗/异步反馈与语言切换全流程。
2. 决定是否提供“已初始化后的集中库自动迁移”；当前安全路径为备份后导入新位置。
3. 评估后续产品增强：批量来源升级、跨来源版本比较、release 资产下载，以及设置页面的视觉打磨。它们不阻塞当前人工验收。

## 开始下一项工作前

1. 执行 `git status --short`，保留任何非本任务改动。
2. 阅读 `自动化测试说明-2026-09-08.md` 和 `功能完成度与验收状态矩阵-2026-09-08.md`，确认未覆盖边界。
3. 行为改动按 TDD 完成后，至少运行相关测试、`pnpm --dir apps/desktop check`、必要时 `pnpm --dir apps/desktop build` 与 `git diff --check`。
