# 人工验收清单（2026-09-09）

用途：只保留需要人在 Windows/macOS 真机上操作并判断的项目。浏览器自动化已经覆盖的内容不在此重复记录。
代码冻结基线：`27042e6`
执行方式：逐条完成，填写“结果、设备、版本、证据、问题编号”；未执行不得填写“通过”。

**（打钩的说明人工执行了验收测试，没打钩的没验证）**

## 一、设备与安装

- [ ] 人工-01 Windows 安装包可安装、启动、卸载；安装目录和快捷方式符合预期。
- [ ] 人工-02 macOS 安装包可安装、启动、卸载；权限提示和应用签名状态符合预期。
- [x] 人工-03 Windows/macOS 首次启动均能完成初始化，不因空目录或已有目录崩溃。
- [x] 人工-04 在不同系统缩放比例下，窗口、弹窗、表格、按钮和长文本没有明显截断或遮挡。

## 二、真实文件与权限

- [x] 人工-05 创建或导入技能后，确认真实文件出现在预期目录，重启应用后仍可见。
- [x] 人工-06 修改并保存技能/Agent/项目配置，确认内容真实写入且刷新后不丢失。
- [ ] 人工-07 对无权限目录、只读目录和不存在目录执行操作，确认提示可理解且不破坏已有文件。
- [ ] 人工-08 在 Windows 与 macOS 分别验证符号链接/链接类路径的读取、部署和失败提示。

## 三、部署、备份与恢复

- [ ] 人工-09 选择单个技能部署到真实目标，确认目标文件内容正确且重复部署行为可理解。
- [ ] 人工-10 选择组合技能执行整体部署，确认成员数量、目标结果和完成提示一致。
- [ ] 人工-11 在项目详情查看受管理部署并解除管理，确认解除后文件仍保留且界面进入空态。
- [ ] 人工-12 创建真实备份并导出到用户指定位置，确认文件可在文件管理器中找到且大小合理。
- [ ] 人工-13 使用备份恢复到空目录，确认技能、Agent、项目和设置恢复完整。
- [ ] 人工-14 构造同名冲突，逐项选择覆盖、保留两者和跳过；确认未完成决策不能继续，恢复耗时/进度没有虚假百分比。
- [ ] 人工-15 使用损坏或不兼容的便携数据恢复，确认被阻止并给出可行动提示，原数据不被覆盖。

## 四、更新与高风险操作

- [ ] 人工-16 使用真实签名更新包验证检查更新、下载、安装、重启和版本变化；失败时确认可恢复。
- [ ] 人工-17 执行删除、覆盖、解除管理等高风险操作，确认二次确认、取消和进行中反馈均正确。
- [ ] 人工-18 关闭或中断长耗时操作，确认状态可解释，重启后没有留下误导性的“已完成”。

## 五、平台体验与诚实提示

- [ ] 人工-19 在系统开启减少动效后，确认关键页面确实减少动画；关闭后恢复正常动效。
- [ ] 人工-20 验证内置 Agent 迁移、Agent 级忽略和 AI 独立输出语言的界面提示没有把暂不支持能力说成已完成。
- [ ] 人工-21 分别切换中文/英文，检查库、在线/本地发现、部署、恢复和高风险操作的关键提示没有截断、混用或遗漏。
- [ ] 人工-22 检查技能库统计/卡片事实、在线源“已在库”状态、本地扫描中状态和待导入提示与实际操作一致。

## 验收记录

| 编号 | 结果 | 设备/系统 | 应用版本 | 证据 | 问题编号 |
|---|---|---|---|---|---|
| 人工-04 | 技能库选择技能后点击删除所选skill，候选skill确认的页面全被覆盖了，实际应该用不上这个候选页面，只需要二次确认删除就够了，这个UI和交互要重新设计下，而且交互也不用让用户输入确认，只需要二次点击确认就可以了。操作删除之后结果弹窗提示成功失败多少条即可，不用每一条展示删除成功，然后这条删除动作记录应该收录到操作记录与恢复中，技能库这块页面应该返回到技能库。另外删除skill较多时，速度有点慢，应该给用户一个提示，比如正在执行删除，然后后台删除，页面回退到技能库列表。 | win11 | 0.2.0 |  |  |
| 人工 | 设置页面除了重新运行初始化向导，再加一个重置skillhub，所有设置回退到原始状态，技能库等也都删除，就跟第一次安装一样，点击重置后，弹出输出框，让用户输入：确认重置skillhub，删除所有数据并恢复到出厂状态。给一个指挥的提示，让用户可以复制黏贴到输入框中，点击确定，执行恢复出厂设置，然后重启软件，走初始化流程。 | win11 | 0.2.0 | | |
| 人工-03 | 新家集中库-选择其他目录-保存并重启提示：集中库初始化完成后不能更换库根目录。如需移动集中库，请使用数据保护中的备份/迁移流程。并无法下一步。返回上一步再再进默认选到我设置的这个用户目录，但是也无法下一步，用使用已有集中库也无法继续，是必须用户手工重启么？并且也无法再选择其他目录文件夹了，选择按钮置灰了。 | win11 | 0.2.0 | | |
| 人工 | 图标素材已经在项目中了吧，0.2.0版需要更换品牌图标为图标素材库中对应的图标logo，未识别或没有的保持当前颜色标签+首字母大写的方式。 | win11 | 0.2.0 | | |
| 人工-05 | 批量导入skill，我去掉一个默认的来源目录勾选，然后点击选择本地目录，其他的默认来源目录勾选都自动给我去掉了，这个批量导入应该同时支持2中方式，一种是扫描到的批量目录，用户可以选择勾选或不勾选，然后用户也可以点击选择本地目录，额外增加目录识别并和默认勾选的目录一同进行候选。另外点击读取已选目录候选按钮，仍然会弹出一个条目区域里面显示：类型  本地路径  目标  C:\Users\<本地用户>\.claude\skills  这个条目没什么用，下面已经有候选目录了。全选扫描来源按钮，应该点一下权限，再点一下，全不选，现在没有全不选的按钮。选择本地目录，点击解析来源后，也不需要显示：类型  本地路径  目标  C:\Users\<本地用户>\.claude\skills  这个条目   直接把候选目录添加到候选区域即可。这个批量导入只是本地的批量导入。候选来源目录区域，除了每个路径单独移除，也应该增加一个全部移除。  导入结果的展示不用把每一条都展示出来，应该给用户一个总览比如导入多少条成功，多少条失败，失败的可以单独展示哪条。导入技能后，技能库有了，但是概览中都是0 | win11 | 0.2.0 | | |
| 人工-06 | 技能抽屉修改别名和备注备注无法保存主，关闭后技能列表没显示，重新打开抽屉还是空的。抽屉中原始描述应该读取skill.md中头部description的内容，但是没有。 抽屉和完整skill信息中版本目前用的还是哈希值吧？之前我记得跟你说过要改这个逻辑，如果是本地创建的首次导入就按照1.0版本显示，如果是网络导入的，先看网上有没有具体的版本号，同步网络或者仓库的版本号，也方便后续升级skill。 查看完整技能详情页面，编辑别名、用途说明、标签、备注都无法保存，提示未能保存，草稿已保留。完整技能详情页面Markdown 工作区，点击编辑，源码编辑器与预览区域有重叠，不是左右分屏，按钮只有保存并创建副本，实际应该还有有保存为原文，不一定创建副本，可以让用户选。另外Markdown 工作区，如果skill.md文件内容很长，不能一直拉长窗口，还是应该有一个固定的高度，并增加滚动条。除了阅读、源码、编辑这3个按钮应该再加翻译的按钮，调用模型进行翻译，如果模型接入功能还没开发，可以先增加前端按钮，并标准后续版本支持。 | win11     | 0.2.0    |      |          |
|         |                                                              |           |          |      |          |
|         |                                                              |  |  | | |
|  |  |  |  | | |

发现问题后，记录到项目问题跟踪系统，并在《功能完成度与验收状态矩阵-2026-09-09》中更新对应状态；历史验收记录保存在 `docs/development/archive/`，不作为当前执行入口。
