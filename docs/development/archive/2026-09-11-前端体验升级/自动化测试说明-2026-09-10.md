# 自动化测试说明（2026-09-10）

更新时间：2026-09-10（集中库生命周期与当前候选自动化证据同步）
接手基线：`ff9ee2051225521345048ea67eb97f0de8dbc1bc`；Task 1～12 已独立提交，本文补记本次生命周期回归。
范围说明：本文记录当前代码的全量自动化验证结果与 LLM 能力的自动化覆盖边界。LLM 集成测试一律使用本地 mock HTTP server 与明显虚构的占位密钥，不访问真实网络、不使用真实凭据。

## 全量验证结果（闭环补齐轮，2026-09-10）

| 检查 | 结果 |
|---|---|
| `git diff --check` | 通过 |
| `cargo fmt --all -- --check` | 通过 |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | 通过（本轮起固定使用该变体，覆盖 feature-gated 测试） |
| `cargo test --workspace --locked` | 通过，720 个测试 0 失败 |
| 绑定漂移检查（`SKILLHUB_WRITE_BINDINGS=1 cargo test -p skillhub-desktop --lib generate_bindings` 后无变更） | 通过 |
| `pnpm --dir apps/desktop check`（eslint + tsc） | 通过 |
| `pnpm test:frontend` | 通过，108 files / 777 tests |
| `pnpm --dir apps/desktop build` | 通过 |
| `pnpm test:e2e` | 通过，56/56（含双语、品牌素材、300 Skill 规模、bootstrap unavailable 入口）；onboarding 2/2 |
| `node scripts/i18n-cjk-audit.mjs` | 通过，用户可见硬编码命中 0，15 条夹具豁免 |
| `node scripts/verify_atomic_test_catalog.mjs` | 通过，349 个唯一编号、62 条用户故事、10 条全局规则 |
| `node scripts/verify_release_readiness.mjs` | 通过 |
| `pnpm test:release` | 通过，9/9 |
| `node scripts/verify_frontend_lifecycle_scripts.mjs` | 通过 |

## TC-US001-M02 原始失败记录与生命周期修复（2026-09-10）

- 原始失败是“保存并重启”导致 `unknown`；该记录保留在人工清单中，不再作为当前产品流程。
- 当前实现删除 onboarding `restart_application` IPC 与前端 restart adapter，改为 `activate_library_root` 成功后直接进入兼容发现；更新器的独立 `app.restart()` 不在本流程范围内。
- `cargo test -p skillhub-desktop --lib`：Task 10 后 22/22；生命周期 adapter 与向导回归通过。

## 浏览器自动化补齐轮（2026-09-10）

把此前只能人工验收、但属于页面/交互层面的三项收进 Playwright E2E；本层验证在 Vite 预览夹具上执行，不证明 Tauri 原生行为。

- 新增 `tests/e2e/bilingual-core-flows.spec.ts` 12 条：用 Playwright `locale` 上下文（与 i18n 启动时读取 `navigator.languages` 一致）分别以 zh-CN/en-US 断言侧栏导航与技能库、详情、LLM 设置、安全检查、在线发现、导入向导的关键文案渲染。对应 TC-GR-09-M03 的自动化层；文案可理解性与视觉截断仍留真机人工。
- 新增 `tests/e2e/brand-assets.spec.ts` 3 条：`public/brand/agents/lobehub/` 全部素材经 HTTP 返回 200 + `image/svg` 并被真实浏览器解码；未知素材不被伪装成图片。组件层映射完整性已由 `BrandTag.test.tsx` 锁定，本层补真实加载。对应 TC-US008-06/07 与 TC-GR-09-M04 的资产部分。
- 新增 `tests/e2e/library-scale-300.spec.ts` 3 条：300 Skill（`?total=300&size=100`）下缓存首页 DCL < 2s（TC-GR-10 预算）、第二页可达、筛选仍可交互。`SkillLibraryPreview` 增加 DEV-only `?total=` 种子参数。原生磁盘 I/O 与数据库迁移性能仍留桌面取证。
- 本轮修复一个既有缺陷：`apps/desktop check`（tsc）在 HEAD 上实际失败——`BrandTag.test.tsx` 使用 `node:fs/path/process` 而 tsconfig `types` 缺少 `node` 且 `@types/node` 未安装（此前"check 通过"的记录依赖未提交的本地环境状态）。修复：新增 devDependency `@types/node@22.20.1` 并在 `apps/desktop/tsconfig.json` 的 `types` 中加入 `"node"`；修复后 check 通过。
- 原子目录同步：TC-GR-09、TC-GR-10 两行完成反向索引回填（生成器全局规则行现支持 evidenceOverrides），TC-US008-06/07 补浏览器层证据；待反向索引行数 291 → 289。

## LLM 能力自动化覆盖

后端（全部经本地 mock server，无真实网络）：

- 协议契约：`crates/skillhub-adapters/tests/llm_protocol_contract.rs`（5 协议族认证头、请求形状、结构化输出、模型列表路径、HTTP 状态到稳定错误码映射）、`llm_runner_contract.rs`（凭据解析、取消、超时）、`llm_safety_prompt.rs`、`duplicate_prompt.rs`、`security_masking.rs`（凭据遮盖保换行）。
- 门面与门控：`facade_llm_admin.rs`（供应商 CRUD、凭据只进 CredentialStore、默认解析、能力开关门控、网络关闭拒绝在线管理调用）、`facade_safety_flow.rs`（先基础检查并链接、复用未过期基础检查、证据遮盖）、`facade_translation.rs`（持久化、批量逐项、用户修订确认、需更新）、`facade_ai.rs`（未配置/能力关闭为 Info 级、LLM 失败回退确定性重复结果）、`facade_search_assist.rs`（扩展命中标记与回退）、`facade_import_ai_checks.rs`（逐对象成败、能力开关、确定性门不受影响）、`facade_llm_cancel.rs`。
- 存储层：`llm_provider_repository.rs`、`translation_repository.rs`。

前端与 E2E：

- `LlmProvidersSettings.test.tsx`（凭据状态/徽章渲染、草稿保存只经门面、预设填充、两级连接测试展示——仅模型级通过才显示"模型连接可用"）、`LlmCapabilitiesSettings.test.tsx`（默认全关、开关写回完整合并偏好、AI 输出语言独立持久化）。
- `tests/e2e/llm-settings.spec.ts` 经 dev-only `/__preview/settings-llm` 路由验证真实渲染路径。
- 能力开关默认全关、未配置返回 `llm.not_configured`（Info）等边界由 Rust 侧锁定；前端不可用门面抛错不静默。
- 闭环补齐轮新增（2026-09-10）：
  - 组件级：`MetadataPanel.test.tsx`（译文/原文/用途三分离、用户修订重译确认「替换译文」、无修订直接重译、意图带 `overwriteUserRevision`）、`SemanticDuplicatePanel.test.tsx`（不自动触发、运行后展示 AI 报告、失败回退确定性候选与失败码）、`OnlineDiscovery.test.tsx`（辅助关闭只发普通搜索、开启后扩展命中标记与提示、辅助失败回退普通搜索并提示）、`ImportWizard.test.tsx`（预检范围与跳过、逐对象报告、失败对象不丢其余、无能力时显示不可用）、`nativeApi.test.ts`（翻译/重复/确定性重复的原生映射与 `translate_description`/`save_user_translation_revision` 契约）。
  - E2E：`tests/e2e/llm-loops.spec.ts` 5 条——描述翻译重译闭环（`/__preview/skill-detail`）、语义重复分析（同路由）、AI 安全检查（`/__preview/security-llm`）、联网搜索 AI 辅助（`/__preview/discovery-online`）、导入 AI 预检（`/__preview/import-wizard`）；预览组件 `SecurityLlmPreview`、`OnlineDiscoveryPreview`、`ImportWizardPreview` 仅注册于 DEV 路由，使用本地夹具，不访问网络。
  - 后端：`translate_description` 新增 `overwrite_user_revision` 标志（用户修订需显式覆盖），`save_user_translation_revision` 空 hash 由后端按原文回填（前端不复算 FNV-1a）；`facade_translation.rs` 4 条、`facade_ai.rs`、`api_contract.rs`、`translation.rs` 同步覆盖。

## 原子用例与反向索引

- 完整目录：`docs/testing/原子测试目录-v0.2.0.md`（生成命令：`node scripts/generate_atomic_test_catalog.mjs`）。
- 当前共 349 条原子用例：325 条自动化候选（QA-001～QA-014 与 US-046/US-047 的 9 条分支已回填真实测试证据）、27 条人工验收入口用例（其中集中库生命周期新增 3 条）。
- US-046/US-047 已从"下一版本"移入 v0.2.0；US-047-05（排除范围）按边界说明行登记为"不适用（v0.2.0）"。
- 其余仍标"待反向索引"的行必须阅读测试真实断言后回填，不能按文件名推断。

## 不能替代人工的边界

- 自动化不能证明 Windows/macOS 真机安装包、签名、权限、真实文件写入、符号链接、系统缩放和系统减少动效效果。
- 自动化不能证明真实操作系统凭据存储（钥匙串/凭据管理器）的落盘隔离，也不能证明真实供应商服务的实际行为；对应人工用例为 TC-US047-M01/M02 与《供应商兼容矩阵》的真实验证列。
- 自动化 mock 不访问真实网络；真实供应商联调不能替代契约测试，反之亦然。
- 因此发布前仍需执行《人工验收清单-2026-09-10》，不得仅凭本文件宣称发布就绪。

## 执行约定

- 浏览器自动化依赖项目既有 Playwright 配置和本机浏览器运行时；浏览器运行时只用于测试，不进入应用打包产物。
- 新增或修改行为时，先补失败测试，再实现并运行相关检查；测试结果和覆盖边界同步更新本文。
- 测试夹具只能使用明显虚构的密钥；不将真实凭据、个人路径写入测试或本文。

## 人工验收反馈修复轮（2026-09-10，`3212165`～`75759b7`）

对 macOS 真机首轮人工反馈的五项问题按 TDD 修复（先失败测试后实现），回归结果：

| 检查 | 结果 |
|---|---|
| 目标测试（LlmProvidersSettings / nativeErrors / OnboardingBranches / ImportWizard / SourceInput / SettingsPage） | 通过，6 文件 / 67 测试 |
| `pnpm --dir apps/desktop test --run` | 通过，108 文件 / 790 测试（新增 13 条） |
| `pnpm test:e2e` | 通过，56/56 |
| `cargo test --workspace --locked` | 通过，0 失败（含 bootstrap_repository 新增 2 条：无快照为 0、按持久化发现快照计数） |
| 绑定漂移检查（`generate_bindings` 复跑无变更） | 通过（`BootstrapSnapshot` 新增 `discovered_agent_count` 已生成） |
| `pnpm --dir apps/desktop typecheck` / `check` | 通过 |
| `pnpm --dir apps/desktop build` | 通过 |
| `git diff --check`、`i18n-cjk-audit`、`verify_atomic_test_catalog` | 通过 |

新增/变更测试与契约：

- `nativeErrors.test.ts`：`input.invalid` + `existing_library_manifest_missing` 映射专属文案；其他 `input.invalid` 保持通用校验文案。
- `LlmProvidersSettings.test.tsx`：结构化 native error 不得显示 `[object Object]`（获取模型/测试草稿两条路径）；获取模型失败保留 endpoint/key/model 草稿并出现“可手动填写模型”提示；重试成功后提示清除；保存校验显示逐字段本地化必填提示且不调用保存。
- `OverviewPage.test.tsx`：概览同时断言“configured agents”（`agent_count`）与“discovered agents”（`discovered_agent_count`）两个口径。
- `ImportWizard.test.tsx` / `SourceInput.test.tsx` / `DiscoveryRoute.test.tsx`：onboarding 导入不渲染“添加到已选来源”、选目录自动进已选来源、主操作读取全部已选目录；普通导入混合来源测试未改动且保持通过。
- `router.test.tsx`：锁定生产入口 `MotionConfig reducedMotion="user"`（TC-GR-09-M02 的“跟随系统”契约，不引入应用内开关）。
- 边界：本轮全部为自动化/预览证据；LLM 真实凭据、真实服务两级连接、系统级减少动效观感、真实目录权限仍以《人工验收清单-2026-09-10》真机取证为准。

## 集中库生命周期回归（Task 1～12）

- Rust：`library_layout` 11 passed；`library_runtime` 4 passed；首次恢复 facade 目标/延迟激活与失败保持 Pending 用例通过；`restore_migration` 2 passed。
- 桌面绑定与适配器：Specta `generate_bindings` 通过；bootstrap API 6 passed；onboarding 四组测试 35 passed；RescanWizard 与 SettingsPage 9 passed；nativeErrors 与 i18n 15 passed。
- Tauri：`cargo test -p skillhub-desktop --lib` 22 passed；onboarding restart handler 已删除，更新器 restart 仍独立保留。
- E2E：`pnpm test:e2e -- tests/e2e/onboarding.spec.ts` 2 passed；验证首次入口不再显示重启 workaround。
- 边界：上述均为自动化或 Vite 预览证据，不能替代 Windows/macOS 真实桌面目录权限、路径持久化、恢复文件和视觉验收。

## 集中库生命周期控制器补修（2026-09-10）

| 检查 | 结果 |
|---|---|
| `pnpm --dir apps/desktop test --run src/features/onboarding/RescanWizard.test.tsx src/app/router.test.tsx src/features/settings/SettingsPage.test.tsx` | 通过，3 个文件 / 28 个测试 |
| `pnpm --dir apps/desktop typecheck` | 通过 |
| `pnpm --dir apps/desktop check` | 通过，ESLint + TypeScript |
| `git diff --check` | 通过 |
| `pnpm --dir apps/desktop test --run` | 通过，108 个文件 / 777 个测试 |
| `pnpm test:e2e -- tests/e2e/onboarding.spec.ts` | 通过，2/2；Playwright 使用固定 5174 独立端口、TCP 检查和受控 Vite 生命周期 |

补修覆盖：bootstrap 快照失败不暴露首次初始化分支；重新扫描取消/完成回调；完成重新扫描不调用 `complete_onboarding` / `activate_library_root`；Agent 发现和扫描失败显示错误并可重试。以上仍不能替代 Windows/macOS 真机验收。
