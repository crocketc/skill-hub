# 开发状态（2026-09-13）

更新时间：2026-09-15（Task 2 关系存储与多来源存证已完成代码/自动化闭环；其余关系治理链路与真机验收仍待推进）
当前分支：`feat/v0.2.0-product-completion`（本轮全部改动已直接落在此分支，当前 HEAD 见 Git 记录）。
本轮范围：2026-09-14 开发待办 D1—D5（安装版终端窗口、技能库工具区重排与视图缺陷、AI 已验证态、仓库管理页、一体化标题栏含范围扩展）。未宣称真机通过；未改变原文件保护、确定性安全检查优先、网络默认关闭与生成绑定边界。

## 2026-09-15 上游产品文档同步

- 已同步《需求文档》《用户故事》《产品与交互设计》《技术架构设计》和《Agent 平台兼容性调研》：当前版本的核心范围明确为导入、关系识别、冲突治理、部署、移除部署、复制副本/通用共享目录关系迁移、回退和待处理事项闭环。
- 已明确 `.agents/skills` 的目录节点与 Agent 识别能力是两个维度；Agent 页面、导入结果和部署操作必须复用规范化关系模型。图谱展示仍只登记为 v0.3.0，当前版本先提供其数据基础。
- 本次只更新需求与设计基线，没有把未实现行为标记为已完成；自动化测试说明、人工验收清单和完成矩阵待关系治理代码与测试落地后按实际结果更新。

## 2026-09-15 Task 2 关系存储与多来源存证

- 已完成：新增 0014 规范化关系迁移与目录节点、Agent 目录能力、来源关系、观察/部署关系、冲突成员/裁决、治理待办表；保留 0013 原表不变，并将旧存证、观察关系和 SkillHub 部署投影为兼容事实。旧观察关系在 `NameOnly`/`Diverged` 等不可靠状态下不填 `skill_id`。
- 已完成：`DirectoryRepository`、`RelationshipRepository`、`ConflictRepository`、`GovernanceTaskRepository` 及 `ProvenanceRepository` 独立 provenance ID/多来源历史；同一路径重复导入不覆盖规范化历史，兼容 API 仍返回最新投影；managed deployment 写入、移除和脱管同步关系投影。
- 已完成：0013→0014、同一 Skill 多来源、重复事实、共享目录多 Agent、冲突决定、治理待办状态转换、回退/恢复读取测试；`cargo test -p skillhub-storage` 全绿，fmt/diff check 全绿。
- 复审修复已完成：入口路径按 `target.path/runtime_name`（空 runtime 回退根目录）生成；关系冲突同步 `relation_id`；影响查询返回 deployments、source_relations、directory capabilities、directory nodes；0013/0014 双投影和 observed 状态同步均有事务边界；迁移后统一重算 `observed_path_key`。先写失败测试，定向 25 项及完整 storage 测试全绿。
- 明确 deferred：AI 分析记录表及持久化留给 Task 8；Task 2 只提供确定性的关系影响快照。0013 SQL、前端、IPC、application 未修改。
- 仍需：Task 3/4 继续实现关系分类、影响计算、应用层 prepare/commit/rollback 和前端闭环；Windows/macOS 真机仍需按人工验收清单复验。

## 2026-09-15 八条开发代办并行完成（代码/自动化闭环，待真机复验）

2026-09-14 记录的 8 条开发代办（OPT-20260914-01…08）由子代理分四批并行开发（每批≤3 个、独立 worktree），主代理逐一整合、修复并跑全量验证电池。每条的实现要点、证据与剩余风险见《自动化测试说明-2026-09-13》2026-09-15 节与《功能完成度与验收状态矩阵》OPT 表；此处只记状态与下一步。

| 待办 | 内容摘要 | 状态 |
|---|---|---|
| OPT-01 | 批量导入冲突分析真实进度（阶段/总数/已用时间/真实百分比或不确定进度）+ 取消分析 | 代码/自动化完成；真机复验待做 |
| OPT-02 | 通知历史消息本体与 action 链接点击先标已读再关抽屉/导航（`markRead`） | 代码/自动化完成；真机复验待做 |
| OPT-03 / D2-2c | 末列安全检查遮挡根因修复（移除 `scrollbar-gutter: stable` 钳制与 `min-width:max-content` 遗留） | 代码/自动化完成；Windows/macOS 真机复验待做 |
| OPT-04 / D5-9 | macOS 收起态取消 5rem 补偿，Logo 起点 4.25rem 实测收敛 | 代码/自动化完成；macOS 真机复验待做 |
| OPT-05 / D5-10 | macOS 展开态大 Logo 与主导航下移 8px（收起态/Windows 无影响已证明） | 代码/自动化完成；macOS 真机复验待做 |
| OPT-06 / D5-11 | macOS Dock 图标 1024 RGBA 母版重制（外层 87.5%/边距 6.25%），Windows ico 未动 | 代码/自动化完成；主标记 52.5% 未达 58%—64%（等比缩放固有），macOS Dock 观感复验待做 |
| OPT-07 | `~/.agents/skills` 通用目录归属 + 官方产品名/形态卡片合并 + 官方核验表 | 代码/自动化完成；真机复验待做；TraeWork/WorkBuddy 官方路径仍"官方未公开" |
| OPT-08 | 导入存证（迁移 0013）+ 已观察部署关系（指纹匹配）+ 原始文件清理确认门槛命令 | 代码/自动化完成；真机复验待做；迁移操作 UI 未做（契约就绪） |

### 本轮整合期修复（主代理）

- 存储测试基线与迁移 0013 同步：`migrations.rs`/`llm_provider_repository.rs` 的 schema 版本钉住 12→13、迁移列表补 13；`provenance_repository.rs` 补 `skills` 父行修复外键失败（提交 `4aaf68bc`）。未降低断言，仅随 schema 演进同步基线。
- 本机环境：工作区根 `node_modules` 存在 vitest 4 升级前残留包（pathval@2.0.1、tinyexec@0.3.2），按既定程序全新重装后生命周期校验恢复 57 包全过；非仓库文件变更。

### 仍需

1. 下一轮 Windows/macOS 人工验收：新增 H 节（八条代办复验原子用例）+ 既有 G 节 D5-8…D5-11、D2-2c、D3-1、D4 系列、B—F 节。自动化证据不得表述为真机通过。
2. OPT-20260915-01 导入与 Agent 部署关系治理闭环仍需实施：Task 2 已完成关系数据基础层；仍需关系分类/影响、单 Skill/批量导入治理、复制副本转管理链接、共享引用收敛、prepare/commit/rollback 和前端闭环。替换链接必须移除原入口并保留审计/回退路径；v0.3.0 仅实现图谱展示，不能把当前核心闭环留到 v0.3.0。
3. OPT-06 若 macOS Dock 复验仍嫌主标记偏小，需单独放大标记（非等比再加工），属新裁决项。

## 下一版本 v0.3.0 规划待办（设计登记，未进入当前实现）

| 待办 | 内容摘要 | 状态 |
|---|---|---|
| V0.3.0-SKILL-GRAPH-01 | Skill 关系图谱：读取当前版本已规范化的 Skill/版本、Agent、可识别目录、通用 `.agents/skills`、项目目录、导入存证、链接别名、复制部署、SkillHub 管理部署、冲突、目录优先级和治理待办，提供图谱展示与关系交互；不重新设计当前版本关系模型 | 已登记设计；待 v0.3.0 排期 |

### v0.3.0 关系治理设计边界

- `.agents/skills` 同时作为共享目录节点和“通用 Agent”卡片展示；具体 Agent 是否识别该目录以官方调研证据为准，不默认推断。
- 每个 Agent 页面按“可识别目录 → 目录中的 Skill → 与集中库关系”展开；共享目录与 Agent 自有目录同名 Skill 必须进一步引导用户选定纳入集中库管理的准确版本。
- 单 Skill 和批量导入都按关系类别批量确认，用户可展开、调整单项；冲突、识别不明确和共享影响均生成可追踪待办，不得静默跳过。
- 发现的外部链接明确记录为“非 SkillHub 创建的符号链接/目录联接”，展示源路径→目标路径；转为 SkillHub 管理链接时必须替换原入口，不能保留两个同名入口。
- 图谱首期以关系可追溯和治理入口为目标，不以自动删除或强制统一所有下属目录为目标。

## 2026-09-15 OPT-20260914-07 Agent 通用目录归属与产品形态卡片（已完成代码/自动化）

- 已完成：`~/.agents/skills` 与项目级 `.agents/skills` 归属收归新增的通用 `agent-skills` profile（伪客户端 `shared_directory` 类型）；各品牌 `.agents/skills` 条目改标 `shared_reference`，扫描/可用性/部署语义不回退，归属卡片只出现一次；profile 新增 `display_name`（逐客户端官方产品名）与 `shared_reference` 字段，schema/loader 同步校验；发现页卡片并列官方产品名与形态标签、同路径多形态合并、共享引用计数汇总；官方核验证据表写入《Agent平台兼容性调研》3.4 节；`codebuddy.workbuddy` 按既有 2026-09-05 Windows 真机确认登记 `~/.workbuddy/skills` 与项目级目录。
- 已完成：绑定经 `generate_bindings` 再生成（`shared_directory`、`display_name?`、`shared_reference?`）；Rust/前端/i18n/clippy/fmt/check 全绿（明细见《自动化测试说明》2026-09-15 节）。
- 仍需：真机复验发现页（通用目录卡片唯一、共享引用计数、品牌原生目录卡片、旧库快照重扫描后收敛）；TraeWork 与 WorkBuddy 官方本地目录仍无公开文本，维持“官方未公开/待测试确认”。

## 2026-09-14 macOS 标题栏验收待办

- 已完成代码与自动化：macOS 不再渲染右上角自绘最小化/最大化/关闭按钮，保留系统左上角红绿灯；侧栏折叠按钮下移避让红绿灯；收起侧栏时 Logo 与前进/返回控件整体右移。
- 仍需：在 macOS 源码启动中复验 G 节 D5-8—D5-11；Windows 保持原有三按钮与布局，不纳入本次改动。

## 2026-09-14 继续验收记录（不开发）

- 用户确认 Windows 应用图标与任务栏图标验收通过。
- 新增待办：主导航展开态大 Logo 与导航页签整体下移；macOS Dock（程序坞）图标改为与其他应用一致的圆角与视觉尺寸；收起态 Logo 与前进/返回控件右移距离收敛到用户截图红框位置。
- 方案记录：收起态取消额外 `5rem` 左侧补偿，只保留收起侧栏 `3rem` 加顶栏 `1.25rem` 内边距，令 Logo 起点约为窗口左侧 `4.25rem`，前进/返回按钮紧随其后。macOS 图标以 `1024×1024` RGBA（带透明通道）母版生成 `.icns`，外层圆角图形约占画布 `86%—88%`、四周保留约 `6%—7%` 透明边距，品牌主标记控制在画布 `58%—64%`，以同一 Dock 视图相邻应用为视觉基准复验。
- 本轮只记录问题，不修改代码；D5-9、D5-10、D5-11 同时纳入后续开发代办，等待人工验收接近完成后与既有优化项一起实施。当前开发代办共 8 条，D2-2c 与 OPT-20260914-03 为同一事项。
- 新增 Agent 归属与产品形态代办：扫描 `~/.agents/skills` 时登记为通用 Agent 目录，不挂到每个品牌；按桌面端、终端 CLI、IDE 插件、云端网页版等具体形态建立或合并 Agent 卡片，并在实施前联网核验品牌名称、共享目录与官方证据，更新《Agent 平台文件管理接入调研》后再开发。
- 新增导入与部署关系代办：扫描阶段识别各 Agent 已部署的 Skill；导入时将原始 Skill 文件夹或平台包复制到集中库，并在身份可可靠匹配时自动建立已观察部署关系。后续迁移到集中库后，以 SkillHub 软连接部署为主；原文件清理必须独立确认、保留来源/备份/回滚，不得因扫描或导入自动删除用户原文件。Trae 包形态及各平台导入规则列为实施前调研项。

## 2026-09-14 M-17-1 UI 优化实现

- 壳层改为“全高侧栏 + 内容区工作区”两列结构；页面标题、返回/前进固定在内容区顶栏左侧，技能库视图切换位于通知中心左侧，所有顶栏控件垂直居中。
- 顶栏高度收敛为 `3rem`；应用按钮统一 40px 点击区域、透明无框样式，窗口控制保留现有无框透明样式并移除关闭按钮红色特效。
- 展开态使用约 `2.75rem` 的大 Logo，在侧栏头部独立几何居中；折叠按钮向上、向左微调，与内容顶栏控件中心线对齐且不推动 Logo。收起态侧栏宽度收敛为 `3rem`，Logo 移入内容区顶栏并与实际存在的返回/前进箭头、标题依次向右排列，Logo 与箭头保持明确间距且不压叠。
- 按 TDD 补充 AppShell/Sidebar/base.css 契约与 app-shell-layout 几何 E2E；前端 144 文件 / 1433 测试、壳层 E2E 7/7、技能库/键盘 E2E 18/18、check/build 均通过。源码启动可开始新一轮真机复核；安装版无终端仍需 NSIS 单独验证。

## 2026-09-14 初始化扫描全局任务状态实现

- 点击“开始只读扫描”后立即置灰并改为“正在扫描…”，页面内显示扫描阶段、已用时间和进度条；当前原生扫描契约没有真实总数，因此页面和任务抽屉使用不确定进度，不显示伪造百分比。
- 后台交接保留原有“扫描仍在进行，可转入后台”流程；交接时保存原始开始时间，进入概览后由壳层顶栏在技能库视图切换之后、通知中心之前显示全局任务入口。
- 顶栏任务入口复用全局操作跟踪器，可承接后续导入等长任务；点击打开右侧“任务详情”抽屉。初始化扫描完成/失败状态显示 2 秒后自动收起，结果仍由通知中心承载。
- 已完成：ScanStep、backgroundScan、AppShell 集成测试；前端定向测试 3 文件/26 项通过，lint、typecheck、build、`git diff --check` 通过。仍需：源码启动后按 M-31-5/M-31-6 进行 Windows 真机验收。

## 2026-09-14 批量导入纳入全局任务状态

- 批量导入继续使用现有全局 `operationTracker`，顶栏任务入口与右侧详情抽屉显示真实的已完成/总数（例如 `1/42`），不改变导入页已有的当前进度和导入摘要。
- 导入页保留后台执行能力，并将提示明确为：可以离开页面，通过顶栏任务状态观察进度，完成后到通知中心查看结果。
- 已完成：ImportWizard 文案契约、TaskStatusIndicator 真实进度测试；导入向导 onboarding 变体进入后自动静默解析默认来源，解析中主按钮置灰显示“正在解析…”，全部完成后直接进入“继续选择候选”；来源确认框保留 fieldset/legend 语义并将标题放入内框；导入定向测试 90 项通过。仍需：源码启动后按 M-31-7 复验跨页面观察和完成通知，并复验 onboarding 来源行视觉布局。

## 2026-09-14 人工验收增量与新增优化待办

- M-29-1 导入向导优化已由用户源码启动复验通过并归档：默认来源自动静默解析、解析状态/候选数、完成后直接进入候选选择，以及来源确认框内布局均符合预期。
- D2-2a 技能库列设置拖动排序、D2-2b 配置快速抽屉模块拖动排序已由用户复验通过并归档；D2-2c 末列安全检查内容仍被纵向滚动条覆盖，保留为待修复/待复验项。
- 新增优化待办：批量导入冲突分析阶段增加真实进度；通知历史消息或其中链接被点击后自动标记已读；修复技能库安全检查末列与纵向滚动条的遮挡。详细方案见当前《人工验收清单》“验收后新增优化待办”。
- 下一轮人工验收将在另一台 macOS 上进行；当前文档不把 Windows 自动化或源码验证结果表述为 macOS 真机通过。

## 本轮已完成（开发待办 D1—D5，2026-09-14）

| 工作包 | 内容 | 审查结论 |
|---|---|---|
| D1 | `main.rs` 首行补 Windows 发布版窗口子系统声明 + 2 项防自匹配结构断言；`tauri dev` 控制台不受影响 | APPROVE |
| D2 | 工具区三带信息架构（检索/高级筛选/视图，六控件等高 Field 网格修复版本下拉悬空）；表格横向滚动取证修复（根因：fixed 布局纯 % 列宽使全列场景表格不溢出——列宽改 rem 比例+地板 + `scrollbar-width:auto`）；卡片分页 pinned 模型（分页常驻底部，批量条让位自洽） | APPROVE（6 Minor 已清偿/记录） |
| D3 | 三级连接测试结果按供应商持久化（settings KV + 七字段配置指纹）+ AI 四态检查消费（服务可达+模型可调用→"已验证"；配置/凭据变更指纹自动失效）；bindings 再生成 | APPROVE（3 Minor 记录在案） |
| D4 | 独立仓库管理页 `/discovery/repositories`（列表/启停/最近扫描/逐仓刷新/移除/URL 解析添加）；`refresh_skill_repo` 命令（网络开关+坐标复验）；逐仓扫描状态持久化；`SkillRepoView` 载荷；发现页管理入口；cc-switch（MIT）仅采纳设计思路，未复制代码 | REQUEST_CHANGES → 修复（e2e mock 换型 + metadata 补齐 + i18n/测试补强）→ scoped 复审 APPROVE |
| D5a+D5b | 一体化标题栏：无边框窗口机制（配置/Rust chrome/最小权限/平台封装/WindowControls 46px 三按钮）+ AppShell 集成（全高侧栏、内容区 3rem 顶栏、展开态 2.75rem Logo、40px 折叠按钮热区、收起态 3rem 侧栏、实际存在的返回/前进与标题依次右排、技能库视图切换同线、拖拽区五处、`LibraryViewModeContext`、macOS 避让）；窗口机制与接入同批落地（审查协调要求） | 均 APPROVE |
| 修复批次 | D4 Major/Minor、D2 五项 Minor、D5b 水合竞态加固（迟到结果不覆盖用户选择）、overview-preview 两条既有失败契约迁移、native-flow-preview mock 换型 | scoped 复审 APPROVE |

集成回归全绿：前端 144 文件/1430 测试、Rust 133 套件/850 测试、clippy/fmt/check/build、i18n 审计、生命周期策略、npm audit、`ci-local.mjs` 十步、定向 e2e（import-flow/llm-settings/discovery-cards 63 项、native-flow-preview 14、overview-preview 26、library-card-view 12 等）；`pnpm --dir apps/desktop tauri build` exe+NSIS 产物生成（末尾签名缺私钥为已知现状）。逐项命令与结果见《自动化测试说明-2026-09-13》"2026-09-14 开发待办 D1—D5 验证"。

## 上一轮已完成（六个任务 + 集成 + 逐页回归 + 修复轮，全部经独立审查/复审）

| 工作包 | 内容 | 关键提交 |
|---|---|---|
| T1 壳层与通知（新增01/02、M-17、M-10、M-04） | 全局通知中心（右上铃铛+未读数、2 秒 toast、会话历史抽屉、`useAppNotifications` 全局契约）；页面内容统一钉顶（`align-content:start`）；顶栏返回/前进图标按钮+子路由 fallback；减少动效双通道（系统 `prefers-reduced-motion` + 设置页"始终减少动效"开关）；折叠按钮主题化重绘 | `ff82ee9` `a788be0` `71a9110` `8f121a5` |
| T2 初始化（M-31、M-25） | 后台扫描交接：选择转后台后"完成初始化"立即提交并进入概览，扫描后台继续，完成后经全局通知提供结果入口；初始化 run_scan 释放共享 DB 锁（`872e511`）；重新发现完成动作移入底部稳定按钮区 | `e77099f` `81cae86` |
| T3 LLM（M-13、M-05、M-16） | 供应商预设/协议族全链路取证修复（draft→IPC→profile→adapter）；`explicit_selection` 等内部枚举改用户语言并附发送范围说明；连接测试错误集中 presenter/i18n；在线发现 AI 辅助四态可用性检查（可用/未配置/被禁用/连接未知），不可用不伪造 AI 结果 | `ca601d7` `bc5edf8` |
| T4 导入（M-29、M-14） | 已选来源分层：添加即显示、单删/多选删/全删、扫描为独立主动作并产出每目录候选数/失败/重试；重复目录去重并聚焦（含 Windows 大小写折叠 `9acf5f1`）；完全重复导入强制走冲突决策（M-14 根因修复）；导入结果接全局通知 | `d6687af` `8989c20` `3254843` `f65fd0f` |
| T5 技能库工作台（M-21、M-22、M-26） | 视图/关系/分组按钮移至工具栏右缘；表格独立滚动容器+末列可达；批量删除改底部滑动抽屉；偏好读取失败不再误报；别名不可改运行名+双名展示；抽屉内"我的用途"可编辑；通知接入 | `7f8d3e5` `f8f7a8a` `32ade62` `53c45c9` `56fcde1` `4649ffa` `c3d1aa2` |
| T6 发现与品牌（M-18、新增03、M-30） | 本地发现区与导入入口固定首屏（工作台 sticky 前置，Agent 盘点折叠后置）；SkillHub 应用图标进 Tauri bundle（icon/icns/NSIS）；Pi 与 DeepSeek Harness 官方 logo（lobe-icons，来源与许可记录于 `assets/branding/SOURCES.md` 与 `third_party/lobe-icons/`，来源不清则诚实回退） | `c50bc15` `9ee4b7c` `51cceb2` `af4bf18` |
| Wave 3 逐页回归 | 详情路由隐藏顶栏时通知中心入口以浮动铃铛保持可达（≥40px 几何锁定）；重复页内返回移除（Discovery 模块页、组合管理页；`/discovery/*`、`/library/combinations` 补壳层 fallback）；概览宽屏两行指标契约（hero 独占第一行、统计第二行，废除 5 卡单行横带）；绑定无漂移复核 | `7178323` `acdf466` `2a068bf` |
| 双审查+统一修复轮 | 全分支双审查（代码/安全 + 产品/UI）无 Critical；修复：M-29 去重大小写缺口、能力未开启文案重复渲染+死代码、已跟踪文档本机路径脱敏、嵌套 test-results 忽略；scoped re-review APPROVE | `9acf5f1` `5c35f94` `f7e8b00` `3410d34` |
| 基线修复与 CI 收尾 | 前端 3 项基线失败修复（`base.test.ts` ×2 断言同步 M-17-1 收敛后的 1.75rem、`router.test.tsx` ×1 按新增01-4 恢复顶栏改正向断言）；`skillhub-testkit` Windows 符号链接测试在本机安全层静默剥离链接的环境下按不支持跳过（防护逻辑未改）；`ci-local.mjs` 增加 `os error 5`/`LNK1104` 特征匹配自动重试；前端 1341/1341、Rust 132 套件 / 823 测试全绿；快进合并回 0.2.0 分支 | `e8fc2fb` |

## 收尾轮已完成（2026-09-13：审查遗留两条建议 + vitest 4 升级，全部 TDD/自动化闭环）

| 事项 | 内容 | 结果 |
|---|---|---|
| 1a 对话框选取路径去重 | `pickLocalDirectory` 复用与手动添加一致的去重：新增 `sameSourcePath` 同一性比较，重复（含仅大小写不同的 Windows 目录）聚焦已有条目、不新增、已扫描状态保持 | 前端组件测试 + 单元测试覆盖 |
| 1b POSIX 大小写折叠平台决策 | 裁决：按**路径形态**而非运行时 OS 决定——Windows 形态（盘符/UNC）折叠大小写；POSIX 路径精确比较。理由：macOS 卷可能被格式化为大小写敏感，折叠会误并两个真实目录；宁可让大小写不敏感卷上出现可手动移除的重复条目 | `directoryPicker.test.ts` 契约锁定 |
| vitest 4 升级 | 3.2.7 → 4.1.11（GHSA-82fw-gwwq-j7x9 修复版本 ≥4.1.11）；`vite.config.ts` 改用 `vitest/config` 的 `defineConfig`（vitest 4 不再增强 vite 的 UserConfig）；全量 140 文件 / 1346 测试在 vitest 4 一次通过 | 详见《自动化测试说明-2026-09-13》 |
| audit 阈值恢复 | 升级后 `pnpm audit` 全等级（info/low/moderate/high/critical）清零，`audit:frontend` 移除 `--audit-level=high` 恢复默认阈值并复跑通过 | `package.json` |
| 生命周期策略同步 | vitest 4 后 pathval 已脱离依赖图、tinyexec 升至 1.3.0；需清理 `node_modules` 全新重装（`pnpm install --force` 不清除 `.pnpm` 虚拟仓库残留）后，从 `docs/dependency-policy.md` 删除 `pathval@2.0.1#prepare`、`tinyexec@0.3.2#prepare` 两行过期记录；校验器 57 个包身份全过 | `docs/dependency-policy.md` |

## LLM 多供应商接入能力实现（独立计划，2026-09-13）

计划：《`docs/development/archive/superpowers/plans/2026-09-13-llm-provider-capability-implementation-plan.md`》（已实施完成并归档），基线 `4569d75`。按 TDD（红→绿→重构）逐任务推进，Task 0—9 已完成，Task 10 为文档与全量回归。

| 任务 | 内容 | 关键提交 |
|---|---|---|
| T1 证据台账 | cc-switch 只读取证 + 官方文档核验，形成四层证据结论 | `97e3813` `e92b5cd` |
| T2 领域模型 | 新增 `LlmCompatibilityProfile`、`LlmStructuredOutputStrategy`、`LlmReasoningPolicy` 与只读策略值对象；`LlmProviderConfig`/`LlmProfile` 显式携带兼容档，默认 `Generic` 不下探猜测 | `5b10f9b` |
| T3 预设声明与迁移 | 25 个预设声明兼容档 + `supported_protocols`；旧配置一次性幂等迁移（未知 ID 回退 `Generic`，endpoint/model/凭据/启用状态不变） | `a6910e3` |
| T4 请求规划器 | 唯一决策入口 `compatibility_policy(profile, protocol)`；`plan_request()` 纯函数产出请求形状，正常请求路径禁止按名称/模型/URL 猜厂商 | `d06a9a4` |
| T5 供应商策略 | OpenAI/Azure/Anthropic/Gemini 基线 + DeepSeek/GLM/Kimi + 其余直连 + OpenRouter/本地/Generic 的差异化参数（结构化策略、推理开关、模型目录） | `4218e96` |
| T6 三层连接测试 | `ConnectionTestResult` 拆成服务可达 / 模型可调用 / 结构化兼容三级；模型可调用但结构化失败只报能力警告，不再表述为“模型协议不兼容” | `cb6f08f` |
| T7 应用层与绑定 | `compatibility_profile` 贯通草稿→IPC→profile→adapter；草稿密钥只进内存覆盖层；历史行列出时携带迁移后兼容档；重新生成 `bindings.ts`（无手改） | `2d2b4f8` |
| T8 设置页 UI | 协议下拉按 `supported_protocols` 过滤、切换不覆盖已填地址/模型/密钥；移除“需开启路由”；`generic` 显示可折叠结构化策略高级项；三级报告；保存不要求测试通过；“在线”→“云端” | `d8a88cc` |
| T9 真实服务验证 | DeepSeek / GLM Coding Plan / LM Studio 真实链路验证（脱敏记录，临时脚本与凭据不入库） | 不产生提交 |
| T10 文档与回归 | 兼容矩阵与隐私说明形成 2026-09-13 快照、旧版归档；四份当前开发文档同步；全量验证电池 + 本地 CI | 见文档体系 |

### 四层契约（本轮核心变更）

一次请求的行为由四层共同决定，任一层不再单独决定“用什么参数发请求”：

1. **传输协议**：`open_ai` / `open_ai_compatible` / `open_ai_responses` / `anthropic` / `gemini` / `azure_open_ai`，只描述 HTTP 线形状（URL 后缀、认证头、响应信封）。
2. **供应商/产品线兼容档**：20 个内置档 + `generic`，描述同协议下不同产品的差异（结构化机制、推理开关、模型目录、凭据策略）。
3. **结构化输出策略**：`json_schema_strict` / `json_schema` / `json_object` / `prompted_json` / `gemini_schema`。
4. **推理策略**：`provider_default` / `disable_for_structured` / `effort_none_for_structured`。

核心事实：**OpenAI 兼容 ≠ 可下发 strict Schema**（DeepSeek Chat 只接受 `json_object`）；DeepSeek Chat 与 Responses 对推理控制不同；GLM 普通 API 与 Coding Plan 是两条独立产品线，Coding Plan 内部又分 Chat/Responses/Anthropic 三端点。因此矩阵按**产品线**而非按协议列能力。完整表见《LLM 供应商兼容矩阵-2026-09-13》。

### 三层连接测试语义

- **服务可达**：地址是否有任何 HTTP 响应（任意状态码均算可达），附耗时。
- **模型可调用**：一次低 token 的**纯文本**探测，证明认证、模型存在与信封可解析。
- **结构化输出兼容**：按当前兼容档策略发一次真实结构化请求。
- 未执行的层级显示“未验证”（info），不是失败也不是成功；模型级未通过时结构化级不下探；**模型可调用 ≠ 结构化兼容**；每级均有图标+文字，不单靠颜色；保存配置不要求测试成功。

### 真实验证与未验证边界

- 已验证（2026-09-13，经最终应用调用路径、脱敏）：DeepSeek Chat 面（模型列表 2 条、端点 87ms、模型+结构化通过，`json_object`）、GLM Coding Plan Chat 面（模型列表 10 条、58ms、`glm-4.6` 通过，`json_object`）、LM Studio 本地（`http://169.254.83.107:1234`，模型 8 条含 `qwen3.5-4b`、4ms、`json_schema`）。
- 纠正：DeepSeek `/v1/models` 实测返回 200（旧文档“无 /models”结论已过时），模型目录改以实测为准并保留手填回退。
- 未验证：DeepSeek Responses/Anthropic 面、GLM Coding Plan Responses/Anthropic 面、Kimi/Kimi Coding、其余直连供应商、Azure、真实凭据落盘与三级连接——保持“兼容”级别，不宣传为“已支持”。
- 验证脚本与真实凭据**未提交**；cc-switch 仅作参考实现取证，不等于 SkillHub 已支持。

### 状态语义（清除凭据 / 启用 / 部署标签）

- **清除凭据**：仅删除 OS 凭据存储中的密钥，保留供应商配置；**启用状态不变**（仍“已启用”），凭据显示“未配置凭据”；清除后模型级返回 `llm.auth_failed`/`credential.unavailable`，不伪装成健康状态。
- **启用/停用**与凭据相互独立；停用供应商不参与解析（`object_not_found`）。
- **部署标签**“云端/本地”表达部署类型（是否走网络、是否通常需凭据），不因连接测试结果改变；本地部署（Ollama/LM Studio）豁免凭据，允许 `http` 但仅限 loopback/私网/链路本地地址。

## 本轮设计决策（裁决记录，详见 SDD 台账）

1. **通知 Provider 归属**：`AppNotificationsProvider` 位于 AppShell 内、Router 之内（生产结构）；测试宿主必须同样把 Provider 放在 Router 内层，否则带跳转动作的 toast 因无 Router 上下文崩溃。
2. **返回入口唯一化**：壳层仅在存在子路由 fallback 时显示返回（`showBack = backFallback !== null`）；移除页内返回前先补 fallback。技能详情页页内返回**保留**——沉浸式详情无壳层返回，且其 Link 承载库内回跳状态语义，不是重复入口。
3. **概览两行指标取代 P1-07 单行指标带**：M-20 用户明确"窗口化两行布局为准，只缩放不换结构"；宽容器 hero 跨 4 列独占第一行，紧凑统计 `display:contents` 落到第二行。
4. **后端偏好同步延后**：reduced-motion 维持 localStorage 模式（与外观偏好一致），native `get/set_ui_preference` KV 留作未来同步通道。
5. **锁中毒语义**：凭据存储 mutex 中毒 `expect` 维持现状（桌面应用惯例，审查认可）。
6. **文档本地化策略修正**：四份当前文档与归档记录保持版本化（AGENTS.md 协作入口），其中个人本机路径脱敏为 `C:\Users\<本地用户>`；`github.com/crocketc/*` 为公开仓库 URL 非个人信息。

### 2026-09-14 D2/D5 开发前整体设计记录（frontend-design skill 统一设计，先行于编码）

7. **一体化标题栏分区（D5/M-17-1 收敛）**：壳层采用「全高侧栏列 + 内容区工作区列（顶栏+内容）」两列 grid；侧栏直通窗口顶部，内容区顶栏与页面融合，`--topbar-height` 收敛为 3rem。展开态使用约 2.75rem Logo，在侧栏头部独立水平居中，折叠按钮嵌在侧栏头部并拥有 40px 点击热区，同时向上、向左对齐内容顶栏控件中心线；收起态侧栏宽度为 3rem，侧栏 Logo 隐藏，缩小 Logo 迁入内容区顶栏折叠按钮右侧并保持不重叠。start = 收起态 Logo + 实际存在的返回/前进控件 + h1 页标题，三者固定向右排列；context = 上下文敏感控件区，技能库视图切换仅在 `/library` 与 `/__preview/skill-library` 渲染并右对齐到通知中心左侧；end = 验证状态 + 通知铃铛 + WindowControls。顶栏操作统一透明无框、40px 热区并垂直居中，关闭按钮不使用红色悬停。拖拽区覆盖 sidebar header、topbar-start、h1、context、topbar-end，按钮控件不加属性（点击不拖拽）。macOS 红绿灯避让改由侧栏头部让出系统按钮带。视图切换 state 上提为 AppShell 内 `LibraryViewModeContext`（Provider 包裹顶栏与 Outlet），水合与持久化语义不变。
8. **技能库工具区三层信息架构（D2）**：检索带（搜索 + 筛选/计数 + 清除筛选 + 结果摘要 + 分组切换 + 组合管理 + 收起视图栏，单行，右簇控件紧凑排列）→ 高级筛选带（六个过滤控件统一「标签在上、控件在下」Field 网格，等高对齐，修复版本下拉悬空——根因是旧 `.sh-skill-library__query-tools label{display:grid}` 契约未随组件迁移）→ 视图带（快捷视图 chips + 保存当前视图，可折叠）。功能语义不变；死代码 `.sh-skill-library__query-tools*`（base.css）与 `.sh-skill-library__view-toggle`（skills.css）随重排清理，相关 CSS 正则断言迁移为锁新契约（契约迁移，非删测试）。视图切换控件保持自包含 `sh-skill-library__mode-switch` 结构，便于 D5 按缝迁入标题栏。
9. **卡片视图分页固定底部（D2）**：卡片视图沿用表格视图「专属滚动区域 + 稳定页脚」模型，分页常驻可见；矩阵视图不动。
10. **表格横向滚动取证（D2）**：以真实行为为准重新取证——新增「全列开启 + 1280 宽」下滚动到最右、末列几何可达的断言，并锁定溢出时横向滚动条可见可交互；与 M-21-1 既有自动化结论冲突处按新证据修正。
11. **AI「已验证」态（D3）**：已验证 = 服务可达 + 模型可调用 两级通过（结构化级是能力警告，不阻断已验证判定）；按 provider 持久化最近一次三级测试结果 + 配置指纹（身份字段 + 凭据在场），读取时指纹不符即视为无结果；`enabled` 切换不进指纹（可用性身份与连接事实分离）。草稿测试带 id 时同样记录。
12. **D4 cc-switch 许可取证**：本地副本 `C:\project\cc-switch`（上游 farion1231/cc-switch）LICENSE 为 MIT（Copyright (c) 2025 Jason Young），许可兼容；沿用仓库既定取证边界（`docs/research/cc-switch-调研-2026-09-11.md`：不复制代码/界面/素材），仅采纳其仓库管理设计思路（URL 解析表单、按仓统计、独立管理面板），来源与许可随本轮文档记录。

## 仍需推进

1. **下一轮 Windows 人工验收**：D1—D5 与 M-17-1 UI 优化已完成代码与自动化闭环；《人工验收清单-2026-09-13》G 节需按源码启动复核 D5-1…D5-7、D2-1…D2-3、D3-1、D4-1…D4-4，D1-1/M-07-1 仍须 NSIS 安装包验证，并连同既有清单 B—F 节逐项执行。本轮代码与自动化证据不得表述为真机通过。已真机归档项：新增01-4、新增03-1、M-03-2、M-05-2、M-13-1、M-13-2（2026-09-14）。
2. 发布阻塞项更新：真机验收证据（Windows G 节与清单 B—F 节条目）、真实签名更新链路仍为阻塞；真实凭据落盘与三级连接（TC-US047）已于 2026-09-14 真机通过关闭。LLM 多供应商接入能力已代码与自动化闭环，真实按钮链路（GLM/DeepSeek）已真机通过；其余产品线/协议面保持“兼容”，不宣传“已支持”。
3. 架构深化任务维持 v0.2.0 冻结（2026-09-12 决策），发布合入 `main` 后启动；集中库初始化与生命周期计划（《`docs/superpowers/plans/2026-09-10-library-bootstrap-lifecycle-implementation-plan.md`》）仍处"计划完成、等待编码授权"，未排期。
4. 非阻塞技术债（2026-09-14 晚间遗留风险清理轮已修复三项：base.css 残留死选择器拆分删除、RepoManagerPage 启停/移除防抖、LLM 连接指纹纳入 custom_headers 摘要——注意指纹形态变更使既有已存测试结果一次性按"无结果"读取，失效方向安全；GR-10 性能预算用例加预热导航去噪，阈值不变。证据见《自动化测试说明》"遗留风险清理轮"节。仍余留：页面自带 h1 与顶栏 h1 并存（迁移前即如此，属跨页信息架构议题随后续任务处理）；"无凭据引用草稿"理论误导路径与 `delete_llm_provider` 无事务包裹（既有模式，不作为本轮范围）。
5. 基线失败已全部修复（2026-09-13 晚间 UI 收尾任务）：前端 3 项（`base.test.ts` ×2 断言同步为 M-17-1 收敛后的 1.75rem；`router.test.tsx` ×1 按新增01-4 恢复顶栏改为正向断言）与 Rust 2 项（`skillhub-testkit` Windows 符号链接测试在本机安全层静默剥离链接的环境下按不支持跳过）均已闭环；前端全量 140 文件 / 1346 测试、Rust 全量 132 套件 / 823 测试均 0 失败，`ci-local.mjs` 增加 Windows 文件锁抖动的特征匹配重试（详见《自动化测试说明-2026-09-13》“已知基线失败”）。

## 本轮 A 节增量处置（2026-09-13）

- 通过项已移至 `docs/development/archive/2026-09-13-A壳层与全局体验已通过/人工验收清单-A-已通过.md`；B 节及后续空白结果未改动。
- 组合管理仅调整壳层前端路由判定，恢复统一顶栏；未改组合管理后端逻辑。
- 详情页浮动铃铛增加滚动条缓冲边距，并检查确认普通页面没有第二类固定定位铃铛入口。
- 概览 hero/统计卡高度和宽屏内容区高度约束已调整；LLM 供应商刷新后会同步显示“已配置：供应商名”，无可用供应商时显示更明确的未配置说明。
- 本地 OpenAI 兼容部署在端点未带版本段时，连接测试自动使用 `/v1/chat/completions`；本地 HTTP 端点安全 allowlist 已覆盖回环、RFC1918 私网和 IPv4/IPv6 链路本地地址，因此 LM Studio 的 `169.254.83.107:1234` 与局域网 Ollama 地址均可进入配置校验；公网 HTTP 仍拒绝。

## 二次验收处置（2026-09-13）

### 已完成

- 详情页通知区按路由为浮动铃铛预留右侧空间；普通页面保持原顶栏通知布局，并补查通知入口未新增固定定位冲突。
- 侧栏折叠按钮改用 Lucide `Panel Left`，图标尺寸收敛为 14px、点击热区为 40px；现有 SkillHub 图形保留，仅重新生成带透明圆角的 Tauri PNG/ICO 资源。
- 概览页指标行统一等高；标签分布改为固定区域 ECharts 环图并保留可访问的文字等价列表，部署分布和待处理摘要纳入固定内容区，避免标签数量撑高页面。
- GLM 结构化响应兼容完整 Markdown JSON 包裹；空模型列表给出可操作提示；新增 OpenAI Responses API 协议；可见协议下拉收敛为 CC Switch 对齐的四类。DeepSeek 端点按 CC Switch 采用服务根地址，由协议适配器负责 `/chat/completions` 后缀。
- 已修复供应商新增草稿的模型列表获取：该动作现只校验供应商 ID、端点、部署和限额，不再在模型字段尚未选择时被完整任务配置校验拦截；保存与“测试此配置”仍要求模型名。
- 二次验收通过的新增02-3、M-05-1 已归档；失败/部分通过项仍保留在当前清单，结果改为代码修复后待真机复验；B 节及后续空白项未改动。

### 仍需

- 重新启动真实 Windows 桌面版本，复验新增01-4、新增03-1、M-17-1、M-03-2、M-05-2；尤其确认详情通知 toast 与铃铛、安装后任务栏圆角图标、概览 100% 无滚动，以及 GLM/DeepSeek 的“获取模型列表”和“测试此配置”按钮。
- 不把 GLM 底层诊断或自动化测试当作应用按钮的真机通过证据；真实凭据落盘、两级连接、macOS 和发布阻塞项保持不变。

## 2026-09-14 部分人工验收处置

### 已完成

- 本轮人工验收通过的 M-31-1…M-31-4、M-21-4、M-21-5 已从当前清单移出，原子记录归档至 `docs/development/archive/2026-09-14-人工验收通过/`。
- 当前清单中的空白结果条目未改动，仍作为下一轮人工验收入口。

### 仍需

- M-25-1：扫描预览动作已移入 footer；待源码启动后确认长列表滚动时底部动作持续可达。
- M-29-1：三段来源区已合并为统一来源确认列表，选中/添加后自动静默解析数量；本轮用户复验通过并已归档。M-29-2…M-29-4 仍按当前清单执行。
- D2-2a/D2-2b：列设置与快速抽屉模块指针捕获拖动排序本轮用户复验通过并已归档。D2-2c：安全检查末列仍被纵向滚动条覆盖，保留为待修复/待复验项。

## 已知边界

## 本次复验修复增量（2026-09-13）

- 2026-09-14 M-25-1：重新发现扫描预览移除“打开导入”流程按钮，动作统一进入固定 footer；首次初始化扫描正文动作保持不变。
- 2026-09-14 M-29-1：来源建议、已选目录和手动/本机选择目录合并为“来源确认”列表；选中或添加后静默解析候选数量，行内展示解析中/数量/失败与单目录重试，导入主流程仍需显式推进；用户复验通过并已归档。
- 2026-09-14 D2-2 修复轮：列设置和快速抽屉统一改为指针捕获拖动排序，避免 HTML5 原生拖放抢占 PointerEvent；D2-2a、D2-2b 用户复验通过并已归档。末列安全检查仍被纵向滚动条覆盖，拆为 D2-2c 待修复/待复验。
- 本轮定向回归 5 个文件 129 项、前端全量 145 文件 1437 项、`pnpm check:frontend` 与 `pnpm build:frontend` 均通过；生产构建仅保留既有大 chunk 警告。
- A 节失败项的前端修复已完成：技能详情恢复标准顶栏，侧栏品牌以全宽容器居中、折叠控件独立使用 40px 热区，概览移除 `100dvh` 反推高度；标签环图为每个标签显式分配主题兼容色。
- 概览窗口化高度不足时改为内容自然高度与唯一外层滚动，避免部署区与标签区相叠；高窗口保留满高布局。该临时兜底只作用于概览。
- LLM 目录改为产品线独立预设：GLM 常规 API/GLM Coding Plan（OpenAI Chat、Anthropic）、Kimi 开放平台/Kimi Code（OpenAI、Anthropic）、DeepSeek（OpenAI、Anthropic）均不再混用端点；同时补齐 Groq、Mistral、百度千帆等直连预设。
- 设置页接口格式下拉仅保留实际直连协议名称，移除“需开启路由”标注；SkillHub 不内置 cc-switch 式请求路由层。
- 模型列表兼容服务优先探测 `/v1/models`，模型列表上限为 15 秒；“测试此配置”对本地慢推理模型单独使用最多 60 秒，不影响正常任务超时。GLM Coding Plan 的 OpenAI Chat、模型列表、Anthropic 端点以及用户提供的 LM Studio 局域网模型列表/结构化 Chat 探针均返回 HTTP 200；此为底层服务诊断，且模型空值拦截已修复，仍待桌面按钮复验。

- 自动化全部为 Vitest/Playwright 预览层证据；真实磁盘 I/O、系统凭据、缩放、读屏、真机字体渲染、安装/卸载/签名仍属人工。
- Tauri 生产构建已执行成功仅证明可构建与打包配置正确，不构成安装真机验收。

## 接手入口

1. 本文；2.《自动化测试说明-2026-09-13》；3.《功能完成度与验收状态矩阵-2026-09-13》；4. 真机操作时《人工验收清单-2026-09-13》（仅含当前待验收项）。上一轮文档已归档至 `docs/development/archive/2026-09-13-验收反馈优化前/`。

```bash
git status --short
git log -5 --oneline
git diff --check
```

## 2026-09-15 Task 4 复审修复

已完成：应用层关系迁移已形成可恢复安全闭环：RelationBackupMetadata 持久化 prepared operation ID、备份路径、原路径、原始 fingerprint、原关系/表示/ownership/active，rollback 前重新读取并 re-hash 备份，绑定或 fingerprint 不符时只生成 NeedsRecovery/治理任务，不恢复文件、不写旧关系；失败恢复先以不跟随链接方式移除且仅移除本次可证明拥有的新入口再恢复备份；checkpoint/Verifying 在文件系统已改变后不吞恢复错误，恢复成功返回带 audit 参数的明确错误，恢复失败持久化 Failed/NeedsRecovery、治理任务和可重试错误详情；Failed rollback 会按持久化 phase/metadata 重试，成功才 RolledBack；prepared 事实、目标指纹和链接表示写入 operation journal，新 facade 可恢复 commit/rollback；终态幂等；链接能力不静默降级；路径授权使用已登记 directory node 与中心库根；RemovalImpact/Overview 补齐权限、inactive、共享影响及 subject/path/member facts 过滤；关系迁移不调用 original_migration，prepare 不写文件。

仍需：未执行真实 Windows/macOS 人工验收；Task 4 不包含前端 UI、Task 5/6 或 0013。

## 2026-09-15 Task 4 回退安全复审二次修复

已完成（有自动化证据）：`facade_relationship_governance` 通过 26 项定向测试，新增并覆盖 relation parent/central target 的 no-follow 受控父链快照、原始实体物理身份、commit 前 `source_relations` journal 重读比较、恢复后关系写回失败的 stage 化重试、DirectoryJunction fail-closed 治理任务，以及结构化损坏 backup metadata 错误；既有测试继续覆盖备份路径必须位于中心库受控根、备份实体类型与原表示一致、关系事实实时 TOCTOU、最终 RolledBack checkpoint 审计错误和可重试路径；实现只修改 Task 4 应用服务、Cargo 依赖与测试。

仍需：本机 Windows 链接策略未提供真实符号链接/Directory Junction 权限，依赖真实链接的 commit/rollback 用例未形成该环境的完整运行证据；需在具备链接权限的 Windows/macOS 设备执行，且不能将条件跳过记为通过。Task 5/6、前端和 0013 均未进入本轮。

## 2026-09-15 Task 4 阻断项闭环

已完成：关系迁移、导入写入和观察收敛共用 facade 内迁移级互斥；commit 在 Applying journal checkpoint（应用中日志检查点）后、备份校验后分别原子复核 `source_relations` 快照，消除文件操作期间的进程内 TOCTOU 窗口。关系 journal（日志）字段、备份可选字段和持久化 result 均严格解析，缺失或损坏时 fail-closed（拒绝继续）；回退关系写回使用显式快照恢复策略，不受普通观测写入的 ownership（所有权）降级保护阻断。新增并发提交、竞态复核、损坏字段和恢复写回重试回归，`facade_relationship_governance` 30/30 通过；应用、存储、核心相关测试及 workspace clippy（工作区静态检查）通过。

仍需：真实 Windows/macOS 人工验收，尤其符号链接、Directory Junction（目录联接）和权限边界；Task 5/6、前端和 0013 未进入本轮。
