# GOAL 交接指令：续接 Task 10，完成 v0.2.0 收口（2026-09-17）

> 本文件是交给下一位代理的作业指令。读完立即开工，不要向用户确认范围或排期——已授权你自行裁决并在执行中调整。
> 唯一例外：涉及**用户主观观感**的事项，登记到《人工验收清单》「用户裁决项汇总」节并采用可回滚默认实现，不阻塞、不提问。

---

## 1. 开工位置（不要新建分支/工作树）

```bash
cd /Users/chengcheng/projects/skill-hub/.worktrees/task10
git status          # 必须在 wt/task10-integration，且干净
git log --oneline -3
```

- 当前分支：`wt/task10-integration`，HEAD = `68d8566a`，已推送 `origin`（本轮交接前核对过 0/0）。
- 主分支：`feat/v0.2.0-product-completion` @ `f42e4535`，已推送，0/0 同步，**本轮 Wave 3 的四页全部在此**。
- 上游基线：`f42e4535`（你的分支基于它，Task 10 完成后才合并回主分支）。

## 2. 已核实的现状（本次交接实测，不是转述）

| 项目 | 实测结果 |
|---|---|
| `pnpm test:frontend`（工作树 @68d8566a） | **163 文件 / 1727 用例全绿**（基线 1720，+7） |
| `pnpm check:frontend` | 通过（ESLint `--max-warnings 0` + tsc） |
| `pnpm test:e2e` | **385 passed / 1 failed**；该 1 失败为并发 flake，见下 |
| Wave 3 四页 | 全部已合入并推送：图谱 `9357318e`、冲突 `4aef7abb`（含 Important 修复 `5de939b0`）、治理 `eecadda9`、概览 `f42e4535` |
| Task 4 统一执行桥 | 已落地：`platform/runTrackedOperation.ts`、`platform/operationTracker.ts`、`app/TaskStatusIndicator.tsx`（旧 `OperationIndicator` 已停用） |
| Task 5 模块外壳 | 已落地：`/relationships`、`/relationships/decisions`、`/relationships/governance` 三条路由已在 `app/router.tsx` |
| Task 10 切片① | 已提交 `29ac06ed`（概览 E2E 翻转 + 五页 h1 清扫，15 文件 +339/−49） |
| Task 10 切片② | **进行中**：已提交 `68d8566a`（任务 7 review minor：冲突决定失败改为 danger 通知 + 无 unhandled rejection，2 文件 +55/−4） |

**关于那 1 个 E2E 失败（务必按事实处理，不要误判为回归）**：

- 失败项：`tests/e2e/markdown-editor-layout.spec.ts:23` @1024px，`getByRole('textbox', { name: 'Markdown source' })` 未找到。
- 已验证：在基线 `f42e4535` 与工作树 `68d8566a` 上**单独复跑该 spec 均 7/7 通过**；全量 5 worker 并发下偶发。
- 定性：**并发 worker 争用导致的 flake，不是 Task 10 引入的回归**。不得为它降低断言、加延时或改跳过。若复现，先单独复跑确认，再按已有 flake 处置惯例记录。

## 3. 剩余工作（按此顺序串行，每片一提交）

### 切片②（续）：登记的 minors 清扫 —— 未完成

`68d8566a` 只清掉了「任务 7 review minor」。以下 **3 条 Task 9 切片复审 Minor 尚未处置**，且**只存在于上一轮代理上下文，未落任何文档**，本文件是它们唯一的书面记录，务必全部闭环：

1. **800x600 覆盖**：概览与关系三页在 800×600 小窗口下的布局覆盖（无溢出、无重叠、面板内部滚动）。
2. **关系命名空间失效验证**：关系命名空间失效时的行为验证（先盘清「命名空间」在当前实现里的具体所指，再补失败测试）。
3. **缩略带溢出兜底**：概览关系缩略带内容超长时的溢出兜底。

继续扫 Task 5—8 的复审 minor，发现一条记一条到本文件末尾的「minor 台账」，不得悬空。

### 切片③：异步入口逐项扫尾 —— 未开始

- **规模更正（重要）**：上一轮快照里的「163 项异步入口」是**误记**，163 是前端测试文件数。实测规模为 **约 125 个 `executeCommand(` / `.execute(` 调用点，分布在 26 个非测试源文件**（`apps/desktop/src/{features,platform,app}`）。
- 做法：先产出**入口清单表**（文件 → 调用点 → 是否已接入 `runTrackedOperation` → 分类：即时写入 / 分阶段异步 / 纯浏览），再一入口一测试。纯浏览操作**不得**写入操作日志。
- 当前已接入 `runTrackedOperation` 的源文件 13 个：`skill-detail/SemanticDuplicatePanel.tsx`、`overview/OverviewPage.tsx`、`security/SecurityResults.tsx`、`overview/RelationshipThumbnail.tsx`、`backup/DataProtectionPage.tsx`、`skills/SkillLibraryPage.tsx`、`deployment/BatchDeploymentPage.tsx`、`deployment/DeploymentDialog.tsx`、`relationships/governance/api.ts`、`relationships/governance/RelationshipGovernancePage.tsx`、`relationships/governance/nativeApi.ts`、`relationships/decisions/useConflictDecisions.ts`（+ `platform/runTrackedOperation.ts` 自身）。

### 切片④：进度事件订阅 + 取消缺口 + 通知覆盖面表格 —— 未开始

- 进度事件订阅：确认分阶段异步操作有真实的进度事件订阅（不得用假进度）。
- 取消缺口：逐个入口核对「用户取消」是否有确定结局（成功/失败/取消/需处理之一），不得出现点了取消却无记录也无反馈的入口。
- 通知覆盖面表格：产出「入口 → 结局类型 → 是否进通知中心 → 是否进操作记录」的完整表格，作为文档附件与人工验收 J 节依据。

### 切片⑤：孤儿键与 i18n 收尾 —— 未开始

- `zh-CN/common.json` 与 `en-US/common.json` **键集必须完全对称**（`apps/desktop/src/i18n/i18n.test.ts` 会断言）。
- 删除无调用的旧「采纳状态」文案、重复的底部操作指示器文案及样式。
- 跑 `node scripts/i18n-cjk-audit.mjs`。

## 4. Task 10 之后的收口（同属本轮）

1. **E2E 新场景**：4 个新 spec + ~20 桩（顶栏多任务分页、通知→操作记录、图谱深链与回退、冲突处理空态、治理批量部分失败、概览不溢出等，见计划「任务 10」第 2 条）。
2. **文档收口（当前最大欠账）**：`docs/development/` 四份文档**仍是 2026-09-16 快照，仍写着「Task 4—12 未开始」，严重滞后于代码**。必须：
   - 生成四份 `2026-09-17` 新快照（开发状态 / 自动化测试说明 / 人工验收清单 / 功能完成度与验收状态矩阵），旧快照移入 `docs/development/archive/2026-09-16-关系模块Task5-9快照/`，更新所有引用与 `AGENTS.md` 引用。
   - 《人工验收清单》**J 节 SR-1…SR-8 由「待实现」改为「可执行」**（页面已实现），RC-13/RC-14/RC-15 保持为真机取证入口。
3. **Task 11、Task 12 仍未开始**：上一轮 GOAL 的 DoD 第 1 条要求 Task 4—12 全部完成。这两项是否纳入 v0.2.0 本轮，需你在文档收口时给出明确结论并写清理由；若纳入则实施，若不纳入则在矩阵里标为「有意延后 + 理由」，不得静默挂账。
4. **`symlink_physical_id_for_path` 的 Windows 分支与注释不符**（《开发状态-2026-09-16》「仍需」第 8 条）：必须二选一——实施一次性兼容读取/存证迁移，或明确豁免并写清理由与风险，写进当前文档。不得继续挂账。
5. **Wave 5 全量验证电池**（计划「任务 10」最终验证电池，逐条 exit 0）：`pnpm test:frontend` / `pnpm check:frontend` / `pnpm build:frontend` / `pnpm test:e2e` / `cargo fmt --check --all` / `cargo clippy --workspace --all-targets --all-features -- -D warnings` / `cargo test --workspace --all-features` / `node scripts/i18n-cjk-audit.mjs` / `node scripts/verify_frontend_lifecycle_scripts.mjs` / `node scripts/ci-local.mjs` / 绑定重生成零漂移 / `git diff --check`。
6. **交付摘要**：完成后在最终回复里给出做了什么、测试结果、还剩哪些真机项、下一步给用户的操作。

## 5. 硬约束（来自 `AGENTS.md` 与既有裁决，不得违反）

- **行为变更必须 TDD**：先写失败测试再实现；覆盖成功/失败/取消/重复/恢复/权限受限路径。**严禁**降低断言、删除用例、加任意延时、静默 `return` 来让测试变绿。
- **测试数只增不减**：前端 ≥163 文件 / 1727 用例；E2E ≥386。
- **绑定文件只由生成器更新**：`apps/desktop/src/api/bindings.ts` 不得手工改，重生成前后必须零漂移。
- **术语冻结（先于任何文案落地）**：动词层面**禁用「部署」**，用「添加到 Agent/项目」「从 Agent/项目移除」；名词层面保留「链接部署/复制部署/部署关系」；技术详情保留「部署/解除部署」「符号链接/目录联接/受管复制」。**不得用「装配」**（已被 project assembly 占用）、**不得用「安装/卸载」**。Task 10 新增的顶栏与通知文案必须直接采用新词。
- **中英双语文案键集对称**，改文案同时改 `zh-CN/common.json` 与 `en-US/common.json`。
- **不得**把自动化证据写成真机通过；不得把设计稿或后端契约写成页面已完成；跳过不计为通过。
- **不得**为页面任务绕过 Task 1 冻结的投影自行拼关系事实，也不得为冲突决定或治理批处理另造第二套入口。
- 当前文档只保留待推进事项与必要的已完成摘要，**不得只写「部分完成」**，必须拆成「已完成」+「仍需」。
- 不提交个人绝对路径、真实 Skill 内容、密钥、缓存、构建产物。

## 6. 阅读顺序（严格按此）

1. 本文件
2. `docs/development/开发状态-2026-09-16.md`（注意内容滞后，但「仍需」第 8/9/10 条仍有效）
3. `docs/development/自动化测试说明-2026-09-16.md`
4. `docs/development/功能完成度与验收状态矩阵-2026-09-16.md`
5. 真机操作时再读 `docs/development/人工验收清单-2026-09-16.md`
6. 实施依据：`docs/superpowers/plans/2026-09-16-skill-relationship-module-and-unified-execution-implementation-plan.md`（任务 10 在 :355、任务 11 在 :389、任务 12 在 :442）
7. 上一轮交接指令已归档至 `docs/development/archive/2026-09-17-交接指令/`（仅作追溯，不作为接手入口）

## 7. minor 台账（在下面追加，切片②结束时不得留空行未处置）

| 来源 | minor 内容 | 处置 | 提交 |
|---|---|---|---|
| 任务 7 review | 冲突决定失败需 danger 通知、无 unhandled rejection、即时写入不占在途顶栏 | 已处置 | `68d8566a` |
| 任务 9 切片复审 | 800x600 覆盖 | 未处置 | — |
| 任务 9 切片复审 | 关系命名空间失效验证 | 未处置 | — |
| 任务 9 切片复审 | 缩略带溢出兜底 | 未处置 | — |
