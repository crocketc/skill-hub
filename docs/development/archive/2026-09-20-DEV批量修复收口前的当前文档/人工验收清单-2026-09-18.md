# 人工验收清单（2026-09-18）

更新时间：2026-09-20（追加 DEV 批量修复轮后的新增/变更判据：SR-6/10/D-11 复验预期更新、DEV-16/18/21/22/23 逐项判据；准-B 配方维持 2026-09-20 版）。本清单是真实桌面验收的唯一当前入口与记录位置；验收结论同步写回《功能完成度与验收状态矩阵-2026-09-18》。

## 2026-09-20 新增/变更判据（DEV 批量修复轮）

| 编号 | 功能 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| DEV-18-A | 预览失败可读 | 准-A 后：技能库 → `find-skills` → 添加到 Claude Code → 停在预览 | 失败行显示「目标目录已存在同名内容……可改用『纳入集中库管理』」；**全流程不出现 `[object Object]`**、不出现裸 SkillId；提交按钮保持禁用 | 待填 | 自动化已覆盖（E2E occupancy conflict 用例），真机复核以真实占用目录为准 |
| DEV-16-A | 用户字段三处一致 | 抽屉设别名/用途/标签 → 不重启依次查看：技能库列表行（标签列/用途列）、详情页身份区、抽屉 | 三处读值一致；详情页头部别名行仅在别名≠原名时出现且不显示 UUID | 待填 | |
| DEV-21-A | 目录联接可用 | 部署任一 Skill 到 Claude Code（宿主 junction:true）→ 查看计划模式 | 计划显示「目录联接」（而非一律托管复制）；提交后目标目录为 reparse point；移除部署后现场复原 | 待填 | 若计划仍显示托管复制，记录宿主 `available_capabilities()` |
| DEV-22-A | 计数一致 | 完成一次真实部署后依次查看：概览指标、Agent 页卡片计数、详情页状态栏、关系页 | 四处计数一致（部署关系条数相同）；概览「已配置部署目标 N 个」随 targets 表变化 | 待填 | |
| DEV-23-A | 关系页内导航 | 进入「关系」→ 用页签切换图谱/冲突处理/治理 | 三页签可达且计数与概览卡一致；概览卡片深链仍可用 | 待填 | |
| DEV-5-A | 路径展示 | 逐页检查 Agent 页/发现页/导入向导/部署目标/冲突与治理页的路径 | 无 `\\?\`/`\??\` 前缀；同一目录只出现一种斜杠风格 | 待填 | |
| J-REL-3 | 冲突工作台窄窗口内容判据 | 准-B 后 800×600 复测（判据不变） | 冲突成员列表可读、动作区可达、影响预览换行/滚动 | 待填 | 冲突处理页现可从关系页页签直达（DEV-23） |

## 被测产物与启动

| 项 | 内容 |
|---|---|
| 被测产物 | `C:\vibe_coding\skill-hub\target\release\skillhub-desktop.exe`（2026-09-19 21:37 构建，含全部已修复缺陷） |
| 启动方式 | **直接双击 exe**，无需任何环境变量 |
| 数据位置 | 数据库 `%APPDATA%\SkillHub\skillhub.sqlite`；部署目标为真实 Agent 目录（如 `C:\Users\crock\.claude\skills`）。**每项验收后按「前置准备」复原现场** |
| 截图/证据 | 存放目录自定，路径填入各行「备注/证据」列；不记录个人敏感内容 |

## 前置准备（素材制造与复原）

| 编号 | 素材 | 怎么操作 | 复原方式 |
|---|---|---|---|
| 准-A | 占用素材（SR-6 / SR-10 / D-11 共用） | 在 `C:\Users\crock\.claude\skills\` 下新建文件夹 `find-skills`，里面放一个任意文件（如 `说明.txt`） | 删除该文件夹，确认 `.claude\skills` 条目数回到验收前 |
| 准-B | 冲突素材（SR-4 / J-REL-3 共用） | 复制一个已有 Skill 的文件夹为同名/同内容副本，放入被扫描的 Agent 目录 → 「发现 → 本机发现 → 重新扫描」→ 观察是否生成冲突组（此前本机扫描为 `冲突 0`，能否产出以实际为准；产不出则 SR-4 / J-REL-3 记「未取得」）。**2026-09-20 进展：机制已验证可用**——find-skills 的同名不同内容冲突组已生成并在导入向导中被处理（见冲突处理页「已处理记录」，决定=保留为独立 Skill）；**SR-4 验收需要新的未处理冲突组**：换一个 Skill 重复上述步骤（副本 SKILL.md 改一行内容），导入时在向导中「跳过/不决定」，即可得到待处理组 | 删除副本 → 重新扫描，确认计数还原 |

## 验收项

### 导入与冲突

| 编号 | 功能 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| SR-3 | 导入提交 | 「发现 → 本机发现」→ 勾选 1 个候选 → 提交导入 → 观察顶栏任务；完成后对同一候选再次提交 | 只生成一次操作事实（一个顶栏任务、一条记录）；重复导入不重复建冲突组；原始文件保留在原目录 | 待填 | |
| SR-4 | 冲突人工决定 | 准-B 后进入「关系 → 冲突处理」→ 打开冲突组 → 做出保留/替换决定；随后再触发一次 AI 检查 | 已决定组不被 AI 结果覆盖；「跳过」不修改既有决定 | 待填 | |

### 统一执行反馈与失败处理

| 编号 | 功能 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| SR-5 | 取消长流程 | 技能详情/安全页运行 AI 安全检查 → 进行中点「取消」 | 顶栏任务与操作记录终态为「已取消」，不误报失败；页面状态一致 | 待填 | 若该入口无取消按钮，改用批量操作进行中取消并在备注说明 |
| 准-A 后 | SR-6 复验：失败与恢复 | 技能库 → 选 `find-skills` → 「添加到 Agent/项目」→ 勾选 Claude Code 目标 → 预览 → 确认添加 | ① 失败渲染逐目标行 + 可读中文文案；② 「操作记录」详情能回答哪个目标、哪条路径；③ 失败记「已撤销」终态，**重启应用不进恢复闸门**；④ 准-A 的外来文件原样未动 | 待填 | |
| 准-A 后 | SR-10 复验：失败文案可读 | 同 SR-6 场景，检查通知中心与顶栏失败原因 | 通知详情与顶栏失败原因均为「可读文案 + 错误码」，**全流程不出现 `[object Object]`** | 待填 | |
| 准-A 后 | D-11 新判据：规划期拒占用 | 技能库 → 同一 Skill → 添加到同一目标 → 停在「预览」阶段观察 | **预览/确认阶段即报「目标目录已存在…」类可读错误**（引导换名/检查目标），确认框不再显示「一切正常」；预览阶段不写盘 | 待填 | |

### 关系页窄视口

| 编号 | 功能 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| J-REL-3 | 冲突工作台窄窗口内容判据 | 准-B 后把窗口缩到 800×600 → 「关系 → 冲突处理」 | 冲突成员列表可读、动作区可达、影响预览长文本可换行/滚动；无根级横向溢出 | 待填 | 布局判据此前已通过，本项只补内容判据 |


| J-REL-2 判据更新 | 图谱内容判据随 DEV-15 更新（关系图谱任一视口） | 确认节点标签与边文本 | 节点标签为 Skill 展示名（不再是裸 UUID）；名称过长被省略号裁切仍算可读；详情面板时间为可读本地时间 | 待填 | 自动化侧 forceLayout/canvas 用例已覆盖坐标与交互；真机只肉眼复核名称与时间 |

## 结果写回

每完成一项：本表填「结果 + 备注/证据」→ 同步《功能完成度与验收状态矩阵-2026-09-18》对应行 → 全部完成后评估 v0.2.0 发布。
