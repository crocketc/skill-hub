# 矩阵历史电池结果归档（2026-09-18 macOS / 2026-09-18-19 Windows）

## 待开发项（非阻塞，2026-09-19 登记）

**DEV-1 跨卷端到端写入**（RC-14 剩余子项，改在 macOS 宿主补做）、**DEV-2 J-REL-3 内容判据**（由用户人工加入真实素材后复测）、**DEV-3「文件夹名 == SKILL.md name」校验**（**集中库侧必须先解决重名**：同名不同源的 Skill 在库内靠 `skill_id` 共存，但 `runtime_name` 相同，部署到同一 Agent 目标会撞 `UNIQUE(target_id, runtime_name)`）、**DEV-4 CSP 缺 `style-src`**（非阻塞缺陷，2026-09-19 登记；修法取舍 + `verify_release_readiness` 护栏）、**DEV-5 发现结果按物理目录去重**（仅斜杠拼写不同的重复目录只显示一个；归并放发现/快照层，**含发现页本机发现的 Agent 目录列表，全仓统一排查**，账目与规划层不受影响）、**DEV-6 技能库顶栏切换按钮位置回归**（3 个视图切换按钮出现在标题栏中间，挤占异步任务摘要区；需先溯源是回归还是漏改，再归位到通知旁 + 布局防回归用例）、**DEV-7 概览页布局调整**（技能关系区上移、部署关系分布下移、待处理摘要右下角；**用户已定：零标签显示空态面板、布局固定**；饼图缺失=零标签条件渲染，非回归）、**DEV-8 WorkBuddy 图标换官方绿色版**（前端零 workbuddy 图标映射，需官方素材）、**DEV-9 导入来源行复选框重复**、**DEV-10 导入全流程文案按用户视角重写**（「创建待办」等术语须说明后果）、**DEV-11 Agent 呈现统一「logo + 类型」并合并同品牌同目录卡片**（含受影响 Agent 列表与**部署目标选择列表**；合并卡内类型以「桌面端/终端」斜杠并列；**不可用目标不进默认列表**）、**DEV-12 导入向导状态跨导航保留**、**DEV-13 来源勾选后自动扫描恢复**（「未扫描」与文案承诺不符）、**DEV-14 注册本地项目抽屉 UI 重设计**（Agent 勾选列表布局已坏：复选框撑大、文本横向溢出；用户指定用 frontend-design 类 UI skill 落地）、**DEV-15 全仓移除面向用户的裸技术标识**（详情页/图谱节点等的 SkillId UUID；图谱节点改用名称；规则已入 AGENTS.md「技术标识不进界面」）、**DEV-16 Skill 用户字段数据链路断裂**（列表标签列空、详情页身份区不显示别名/用途，同一数据各视图投影不一致；详情页须完整显示并支持就地编辑）、**DEV-17 多步流程分步呈现**（添加向导计划区不追加列表尾部；规则已入 AGENTS.md）、**DEV-18 批量添加预览失败 `[object Object]`（高优先，当前阻塞人工验收）**（渲染缺陷掩盖真实原因，候选根因：版本 unknown 或 D-11 规划期错误；修渲染 + 查根因）、**DEV-19 Skill 操作动作全集统一**（列表批量栏/抽屉/详情页三处动作对齐，支持单独与批量；抽屉按钮疑似被裁撤需溯源；用 frontend-design 类 skill 设计）、**DEV-20 导入向导「关系治理/处理冲突」信息层级重设计**（突出识别结果与决策点；与 DEV-10/17 同批；流程功能已验证正常）、**DEV-21 目标链接能力被 profile 声明锁死**（全客户端只能托管复制；需基于 RC-13/14 真机证据重评 junction 声明，产品决策）、**DEV-22 部署成功后多处视图计数不一致**（Agent 页 0 skill、详情状态页 0 条、概览不更新，与关系页矛盾；排查计数口径与刷新链路；概览「已配置/已发现」文案歧义一并澄清）、**DEV-23 关系模块缺页内导航**（冲突处理/治理子页只能从概览卡片或直达 URL 进入，页内无任何入口；优先落地）、**DEV-24 技能图谱交互与视觉重设计**（画布可拖拽平移/缩放、内容整体居中、力导向参考 Obsidian；保留节点/边点击详情；详情面板裸 UUID 与 unix 时间戳一并修；技术选型 d3-force 或 sigma.js）。另有两条**界面呈现约定已写入 AGENTS.md**（路径展示：去 `\\?\` 内部前缀 + 统一斜杠 + 按物理目录归并；Agent 呈现：logo + 类型徽标 + 合并出卡），DEV-5 的路径问题（含 `\\?\` 泄漏）按该约定全仓统一修。十四项的现状与落地方式见《开发状态-2026-09-18》「待开发项（backlog）」。

## 收口电池结果（2026-09-18 复跑，macOS）
- `node scripts/ci-local.mjs`：**10/10 步骤通过**（格式化、依赖与许可策略、clippy `-D warnings`、Rust 测试、前端冻结锁安装、生命周期策略、依赖审计、lint+typecheck、前端全量测试、生产构建）。依赖策略输出既有重复依赖与间接 yanked crate 警告，策略检查结果为通过。
- Rust 全工作区：**142 个测试二进制、1096 通过 / 0 失败 / 0 忽略**（2026-09-17 于 `--all-features` 全量测得；D-7/D-8/D-9 修复引入 12 条平台无关用例后，Windows 侧 2026-09-19 复跑为 **144 目标 / 1110 通过**，macOS 未复跑、推算 1108）。日志内 `SKILLHUB-TEST-SKIP;` 计数 0。
- 前端单元全量：**165 个文件、1773/1773 通过**（该行是 2026-09-17 的测量值；此后新增 1 条取消路径回归测试 → 现为 **1774**，Windows 侧已实测 1774）。六轮累计自 1759 起 +14。`pnpm --dir apps/desktop check`（ESLint `--max-warnings 0` + tsc）通过。
- E2E 全量：**387 passed / 0 failed**（2.3 分钟，exit 0，进程正常退出）。
- `node scripts/i18n-cjk-audit.mjs`：user-visible（必须修）**0 条**，仅剩 7 个已审阅豁免的 DEV/夹具文件。
- `cargo fmt --all -- --check`、`git diff --check` 通过。
- 本轮新增代码经扫描无个人路径、无密钥、无真实 Skill 内容；未提交缓存或构建产物。

## Windows 宿主电池结果（2026-09-18 新增，Windows 11 家庭版 25H2 / build 26200，非管理员账号）

- Rust 全工作区：`cargo test --workspace --all-features` exit 0，**142 个测试目标 / 1098 通过 / 0 失败 / 0 忽略**；`--list` 计数 1098，逐条 `ok` 行数 1098。其中 **17 条为能力守卫跳过**（可见的 `SKILLHUB-TEST-SKIP;` 标记），真正执行断言 1081 条。
- **Rust 全工作区（2026-09-19 更新，含 D-7/D-8/D-9 的 12 条新增用例）**：**144 个测试目标 / 1110 通过 / 0 失败 / 0 忽略**，`--list` 计数 **1110**；`deployment_staging` 6/6、`facade_deployment_accounting` 6/6；17 条能力守卫跳过（执行断言 1093 条）。与修复前「1098 + 12」推算一致。该跑需**前台非沙箱 + `CARGO_INCREMENTAL=0`**（否则 `.d` 依赖文件写入报 `os error 5 拒绝访问`）。
- 关系治理套件：**58 通过 / 0 失败** + 17 条标记；`SKILLHUB_TEST_FORCE_NO_LINK_CAPABILITY=1` 下 18 条标记；`SKILLHUB_REQUIRE_SYMLINK_CAPABILITY=1` 下 **41 通过 / 17 失败**。
- 9 个 Windows 专属用例全部被编译、执行并逐个 `ok`（含 junction 模块级用例与 updater 的 NSIS 用例）。
- 前端：`pnpm install --frozen-lockfile`、`pnpm --dir apps/desktop check`、`node scripts/i18n-cjk-audit.mjs`（user-visible **0 条**，与 macOS 一致）全部通过；前端全量复跑 **165 个文件 / 1773 用例全通过**（早于取消路径回归测试）；清空 Vite 转换缓存后的**冷缓存**复跑 **165 文件 / 1774 用例全通过**（`transform 20.37s / setup 60.63s / import 80.31s / environment 164.69s`）。首次全量的 3 例 `OverviewPage.test.tsx` 非确定性失败已定位为宿主负载下的 5 秒超时并处置：逐用例预算放宽到 15 秒并写明测量依据，断言未改、无人为延时。
- 真机文件系统证据（RC-13/RC-14）：junction 落地为 reparse point 且可读透；来源目录条目与树哈希在转换前后完全一致；探针零残留；来源目录整体替换后仍可移除且父目录无残留。跨卷规则的决策层在本机**真实第二卷**上实测：`paths_share_volume` 跨卷为 `false`、能力探针跨卷 `junction:false`、转换规划器跨卷 **REFUSED**（保留原入口）、部署模式选择跨卷 **`ManagedCopy`**（放行）、探针零残留且第二卷根条目数 4 → 4（零跨界写入）。跨卷的端到端写入仍待一台带可写第二卷的宿主。
- **Rust 全工作区（2026-09-19 修复轮复跑）**：**144 个测试目标 / 1115 通过 / 0 失败 / 0 忽略**，`--list` 计数 **1115**；较 1110 **+5**，全部来自 `facade_deployment_accounting.rs` 本轮新增用例（`facade_operation_journal` 那条是改写）。17 条能力守卫跳过（日志实测 17 条 `SKILLHUB-TEST-SKIP;`，全为 `facade_relationship_governance` 符号链接守卫）→ 执行断言 **1098** 条。
- **前端（2026-09-19 修复轮复跑）**：**165 文件 / 1782 用例全通过**（较 1774 +8）；`pnpm check:frontend`（ESLint `--max-warnings 0` + tsc）无输出；`pnpm test:release`（`verify_release_readiness` 护栏）**10/10 通过**；`bindings.ts` 因新增 `RecentOperationSummary.targets` 与 `TargetOperationResult.residue` 必然变更，已重新生成并确认为不动点（SHA256 前后一致）。
- **E2E（2026-09-19 修复轮，Windows）**：受影响的 3 个 spec（`native-flow-preview` / `workflow-operations` / `workflow-recovery`）**48/48 `ok` / 0 `not ok` / 0 条 `error-context.md`**。**本机判定口径更新**：只要有用例失败，runner 会在收尾阶段挂住且**不打印 `not ok`**，进程挂死不能被当作「零失败下的收尾问题」；须反查 `test-results/<spec>/error-context.md`（存在即有失败，快照通常直接指出位置）。E2E 全量在本轮收口前未跑完。
- E2E 未在 Windows 上运行（非阻塞）；本机为新分支的干净克隆，无个人路径或密钥写入文档。
- **CSP 内联样式告警（2026-09-19 复现，非阻塞）**：release 产物在关系页与部署流程中持续打印 `Applying inline style violates the following Content Security Policy directive 'default-src 'self''` —— `tauri.conf.json` 只补了 `connect-src`，未声明 `style-src`，内联 `style=` 仍被拦。建议发布前修，并可考虑加进 `verify_release_readiness` 护栏。
