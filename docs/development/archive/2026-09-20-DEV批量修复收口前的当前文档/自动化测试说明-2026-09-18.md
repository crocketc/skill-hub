# 自动化测试说明（2026-09-18）

更新时间：2026-09-20（**追加 DEV 批量修复轮收口基线**：Rust 144 目标 / **1121** 通过、前端 **166 文件 / 1800 用例**、E2E 全量、`pnpm test:release` 10/10、bindings 不动点，详见下方「2026-09-20 批量修复轮」块）。此前更新（2026-09-19）内容保留在下文。本文记录本分支的自动化证据基线、入口审计和不能替代人工的边界。**自动化通过不构成 Windows/macOS 真机通过。**

## 2026-09-20 批量修复轮基线（Windows 实测，串行执行）

| 命令 | 结果 |
|---|---|
| `cargo test --locked --workspace --all-features`（前台非沙箱 + `CARGO_INCREMENTAL=0`） | exit 0，**144 个测试目标 / 1121 通过 / 0 失败 / 0 忽略**；17 条能力守卫跳过（`SKILLHUB-TEST-SKIP;` 计数 17）→ 真正执行断言 **1104** 条。较上批 1118 **+3**：`catalog_repository`（DEV-16 列表 tags 投影）、`discovery`（DEV-5 拼写变体归并）、`facade`（DEV-3 候选 frontmatter_name）各 +1 |
| `cargo clippy -p skillhub-adapters -p skillhub-application --all-targets --all-features -- -D warnings` | 通过 |
| `pnpm --dir apps/desktop check`（ESLint `--max-warnings 0` + tsc） | 无输出（exit 0） |
| `node ./node_modules/vitest/vitest.mjs run`（cwd=`apps/desktop`） | **166 个文件 / 1800 用例全部通过**（较上批 165/1782：+1 文件 `platform/displayPath.test.ts`；新增用例含 DEV-18 结构化错误透传 ×3、DEV-23 页签 ×2、DEV-16 投影 ×2、DEV-22 计数 ×2、DEV-13 自动解析 ×1、DEV-9 单复选框 ×1、DEV-3 前后端 ×2、DEV-12 恢复 ×1、DEV-17 分步 ×1、DEV-6 布局 ×1、DEV-7 空态 ×1、displayPath ×4，另有多处既有断言按新行为改写） |
| E2E 全量 `pnpm exec playwright test --config=tests/e2e/playwright.config.ts` | **389 用例全部逐条 ok / 0 not ok / error-context.md 计数 0 / exit 0**（净增 2：DEV-18 占用组合、DEV-23 关系页签；日志在仓库外 `Temp/e2e-final2.log`）。**startup-performance 单独复跑口径**：全量后单独执行 `pnpm exec playwright test … tests/e2e/startup-performance.spec.ts`，实测 exit 0、1 passed（2.9s，预算 2s 内达标）；该用例随宿主负载波动，全量翻红时必须单独复跑定性 |
| `pnpm test:release`（`verify_release_readiness` 护栏） | **10/10 通过**（DEV-4 起新增 style-src 断言，红绿实测：无 `style-src` → fail，声明后 PASS） |
| `node scripts/i18n-cjk-audit.mjs` | user-visible（必须修）**0 条**（新增 zh/en 键：relationships.sectionNavLabel、deployment.results.failure.targetExists 改写、overview.tags.emptyTitle/emptyHint、importWorkflow.candidates.nameMismatch、agentsCombined 改写，两侧对称） |
| bindings 不动点（DEV-3 有 DTO 变更） | `SKILLHUB_WRITE_BINDINGS=1` 生成前后 SHA256 一致（`47d79fb4…`），再生成一次为不动点；`ImportCandidate` 新增 `frontmatter_name?: string | null` |

**本批新增/改写的平台无关用例全部先红后绿**；既有断言改写（非放松）共 4 处：① `builtin_profiles.link_capabilities_are_true_only_when_officially_documented` → `…_when_documented_or_probe_verified`（DEV-21 双通道裁决）；② `facade_deployment_accounting.claude_code_target_never_selects_a_junction` → `…_follows_host_capabilities_for_link_selection`（按宿主探针推导期望模式）；③ `deployment_staging.catalog_exposes_declared_deployment_capability_for_a_client`（junction 声明随 DEV-21 翻转）；④ MetadataPanel/SkillDetailPage 的 P1-12「读值不重复展示」断言按 DEV-16 用户要求（身份区显示别名/用途读值）改写。

## 当前基线（2026-09-18 复跑，macOS 宿主，串行执行）

| 命令 | 结果 |
|---|---|
| `cargo test -p skillhub-application --test facade_relationship_governance --all-features` | **59 通过 / 0 失败 / 0 忽略**，日志内 0 条跳过标记（RC-15，2026-09-17 测得，之后 `.rs` 未改） |
| `SKILLHUB_TEST_FORCE_NO_LINK_CAPABILITY=1 … -- --nocapture \| grep -c '^SKILLHUB-TEST-SKIP;'` | **18**（负向对照：跳过路径可达且可计数） |
| `SKILLHUB_REQUIRE_SYMLINK_CAPABILITY=1 cargo test -p skillhub-application --test facade_relationship_governance --all-features` | **59 通过 / 0 失败**（跳过即失败，「通过」证明守卫真的执行） |
| 上述两个开关**叠加** | **18 失败 / 41 通过**（17 处符号链接守卫 + 1 处目录链接守卫全部接线） |
| `cargo test --workspace --all-features` | **142 个测试二进制、1096 通过 / 0 失败 / 0 忽略**，`SKILLHUB-TEST-SKIP;` 计数 0（2026-09-18 实测。此后新增 12 条平台无关用例，macOS 未复跑，推算为 **1108**；已实测的一端为 Windows 1110，见下） |
| `pnpm --dir apps/desktop test --run` | **165 个文件、1773/1773 通过**（该行是 2026-09-17 的测量值；此后新增 1 条取消路径回归测试，当前用例数为 **1774**——Windows 侧已实测 1774，macOS 未复跑）。六轮累计自 1759 起 +14：第四轮 +8、第五轮 +5 −1 死代码测试、第六轮 +2 |
| `pnpm exec playwright test --config=tests/e2e/playwright.config.ts`（全量，5 workers，日志写 `/tmp`） | **387 passed / 0 failed**（2.3 分钟，exit 0，进程正常退出） |
| `pnpm --dir apps/desktop check` | ESLint `--max-warnings 0` + tsc 通过 |
| `node scripts/i18n-cjk-audit.mjs` | user-visible（必须修）**0 条**；仅剩 7 个已审阅豁免的 DEV/夹具文件 |
| `node scripts/ci-local.mjs` | **10/10 步骤通过**：格式化、依赖与许可策略、clippy `-D warnings`、Rust 测试、前端冻结锁安装、生命周期策略、依赖审计、lint+typecheck、前端全量测试、生产构建。依赖策略输出既有重复依赖与间接 yanked crate 警告，策略检查结果为通过 |
| `cargo fmt --all -- --check`、`git diff --check` | 通过 |

### Windows 宿主实测（2026-09-18 新增，Windows 11 家庭版 25H2 / build 26200，非管理员账号）

| 命令 | 结果 |
|---|---|
| `cargo test --workspace --all-features` | exit 0，**142 个测试目标 / 1098 通过 / 0 失败 / 0 忽略**；逐条 `ok` 计数 1098；其中 17 条为能力守卫跳过（真正执行断言 1081 条） |
| `cargo test --workspace --all-features -- --list`（`: test$` 计数） | **1098**，与通过数一致 |
| `cargo test -p skillhub-application --test facade_relationship_governance --all-features` | **58 通过 / 0 失败 / 0 忽略** + 17 条 `SKILLHUB-TEST-SKIP;`；两项对照见「能力守卫在 Windows 的实测」 |
| 9 个 Windows 专属用例 | 全部被编译并执行，逐个输出 `test … ok`（含 `deployment::junction_windows::tests::creates_a_mount_point_with_the_raw_windows_api` 与 `updater::tests::windows_accepts_only_current_user_nsis_updater_artifact`） |

### Windows 工作区基线更新（2026-09-19 实测：144 目标 / 1110 通过）

D-7/D-8/D-9 三个部署链缺陷的修复带来两套新增用例（`deployment_staging` 6 例、`facade_deployment_accounting` 6 例，共 **+12**，均**不带 `#[cfg]` 门控**）。修复后在本宿主重跑全工作区：

| 命令 | 结果 |
|---|---|
| `cargo test --workspace --all-features` | exit 0，**144 个测试目标 / 1110 通过 / 0 失败 / 0 忽略**；其中 17 条为能力守卫跳过（真正执行断言 **1093** 条） |
| `cargo test --workspace --all-features -- --list`（`: test$` 计数） | **1110**，与通过数一致 |
| `deployment_staging`（新增） | **6/6 通过** |
| `facade_deployment_accounting`（新增） | **6/6 通过** |

即 **Windows 1098 → 1110**（142 → 144 个测试目标），与修复前「两端各 +12」的推算（Windows 1110 / macOS 1108）**完全一致**；macOS 侧尚未复跑。平台无关 + 两端共跑的 `#[test]` 池由 1105 增至 **1117**。

**注**：本机此前在 `--workspace` 全量上出现过 `os error 5 拒绝访问` 写 `.d` 依赖文件失败（Defender / incremental 目录锁）。规避方式为**前台非沙箱**执行并设 `CARGO_INCREMENTAL=0`；上述 1110 即在该条件下测得。

### Windows 工作区基线更新（2026-09-19 D-11 修复轮实测：144 目标 / 1118 通过）

D-11（规划期占用识别）修复带 +3 条平台无关用例（`facade_deployment_accounting` 11 → 14 例，其中 2 条既有用例同时改写了场景层面，见上节表），全工作区完整日志落盘于仓库外复测：

| 命令 | 结果 |
|---|---|
| `cargo test --locked --workspace --all-features`（前台非沙箱 + `CARGO_INCREMENTAL=0`） | exit 0，**144 个测试目标 / 1118 通过 / 0 失败 / 0 忽略**；17 条能力守卫跳过不变 → 真正执行断言 **1101** 条 |
| `facade_deployment_accounting` | **14/14 通过**（3 条新增 + 2 条改写场景 + 9 条既有） |
| `cargo clippy --locked --workspace --all-targets --all-features -- -D warnings` | 通过 |
| `cargo fmt --all -- --check` | **本机即红（存量）**：HEAD 的既有文件在另一版 rustfmt 下格式化，本机版本给出大量与本轮无关的重排 diff；本轮新增代码已按本机 rustfmt 口径手写对齐。处置口径见「已登记的测试边界与已知波动」 |
| `pnpm check:frontend` | 无输出（exit 0） |
| `pnpm test:release` | **10/10 通过** |

即 **Windows 1115 → 1118**，与「+3 条新增用例」逐字一致；`generate_bindings` 在全电池内通过 → `bindings.ts` 零漂移（本轮无 DTO 变更）。**E2E 未跑**：本轮为纯 application 层改动、零前端改动面，E2E 全程以 mock 替身 IPC，不经过本轮代码路径。

关键行为测试（全部先红后绿或有明确失败复现，细节见归档快照）：

- `facade_relationship_governance`：能力守卫可计数跳过；RC-16 用例断言 `prepared_count == 1 && state == Prepared`。
- `runTrackedOperation.test.tsx` / `nativeErrors.test.ts`：结构化 AppError 走错误码文案、未映射码回退 `tasks.notices.failureUnknown` 且带出原始码、普通 `Error` 自由文本不被错误码嗅探改写、`isStructuredNativeError` 形状判定。
- 待办处置桥接（`PendingPage.test.tsx`）：单条/撤销 `instant` 成功与失败通知、批量 `phased` 在途投影与计数成功通知、批量失败三端同源与部分成功刷新。
- 关系治理失败通知（`RelationshipGovernancePage.test.tsx`）：单条解除失败「行内 + 通知中心 + tracker 终态」同一份带码文案；批量拒绝在预览保持打开的同时进通知中心。
- E2E `workflow-pending`：失败反馈双通道断言（行内提示 + `notice-danger`，同文案、不含 `[object Object]`）。

## 统一执行反馈桥：当前口径

- **入口覆盖**：27 个桥接调用点 / 19 个文件全部经 `runTrackedOperation`；待办页处置（含批量与撤销）为最后收口的一处（`0bf619a7`）。**不存在已知未入桥的用户可触发写入。**
- **失败通知**：全部调用点走桥的失败通知；`RelationshipGovernancePage.tsx` 曾有 4 处 `errorNotice: () => null`（批量纳入主流程、单条纳入、单条解除、逐项回滚），已全部移除（`f9f7c4fc`；第四次更新登记时漏计为 1 处，已更正）。**不存在抑制失败通知的调用点。**
- **描述口径**：缺省 `describeError` 只对真正的结构化 `AppError` 走错误码文案（`isStructuredNativeError` 形状判定，防止把 `cannot write skill.md` 误嗅探成错误码），普通 `Error` 原样透出；显式传 `describeError` 的页面与其行内提示逐字同源。
- **边界登记**：筛选视图的 `set_ui_preference` 持久化保持页面静默（隐式 UI 状态保存，逐次弹通知是干扰；失败有 `role="status"` 行内提示）。

## 能力守卫：可观测、可反证的跳过

`crates/skillhub-application/tests/facade_relationship_governance.rs` 中 17 处符号链接守卫 + 1 处目录链接守卫统一走 `skip_or_continue_for_missing_capability`：跳过时把 `SKILLHUB-TEST-SKIP; 文件:行; 原因` 直接写入进程 stderr 文件描述符（绕开 libtest 对通过用例的 stderr 捕获），可用 `grep -c '^SKILLHUB-TEST-SKIP;' <日志>` 计数。`SKILLHUB_REQUIRE_SYMLINK_CAPABILITY=1` 使跳过变硬失败；`SKILLHUB_TEST_FORCE_NO_LINK_CAPABILITY=1` 为负向对照。**跳过不计为通过。**

## 用例数的跨平台归因（2026-09-18 已在 Windows 实测闭合）

macOS（`fee98343`）实测：`cargo test --workspace --all-features -- --list` 计 **1096** 用例 / **142** 个测试目标（135 `Running` + 7 `Doc-tests`），与全量跑出的 1096 通过一致。

**Windows 实测（2026-09-18，Windows 11 家庭版 25H2 / build 26200，MSVC stable，非管理员账号）：**

| 命令 | 结果 |
|---|---|
| `cargo test --workspace --all-features -- --list 2>/dev/null \| grep -c ': test$'` | **1098**（142 个测试目标，`--list` 输出无 doctest 条目） |
| `cargo test --workspace --all-features` | exit 0，**142 个测试目标 / 1098 通过 / 0 失败 / 0 忽略**；逐条 `test … ok` 计数同为 1098（列表数与通过数相等）；约 115 秒 |

即 **Windows 1098 / macOS 1096，两端都与修正后的平台门控推算完全一致**。上一版「Windows 应为 1100」的推算有 **2 个漏项**：

| 类别 | 数量 | 位置 |
|---|---:|---|
| Windows 专属 `#[test]` | 8 | `src-tauri/src/updater.rs` 1、`adapters/tests/discovery.rs` 1、`adapters/tests/watcher.rs` 1、`adapters/tests/deployment_filesystem.rs` 3、`core/tests/path_policy.rs` 1、`core/tests/external_change_hint.rs` 1 |
| Windows 专属测试**模块** | 1 | `adapters/src/deployment/junction_windows.rs` 的 `#[cfg(all(test, windows))] mod tests` |
| macOS 专属 `#[test]`（**上一版漏计**） | 1 | `src-tauri/src/updater.rs:312` 的 `#[cfg(target_os = "macos")] fn macos_accepts_app_tar_gz_updater_artifact` |
| Unix 专属 `#[test]`（**上一版记 5，实为 6**） | 6 | 上版 5 个（`storage/tests/projects.rs`、`adapters/tests/discovery.rs`、`adapters/tests/scanner.rs`、`adapters/tests/deployment_filesystem.rs`、`core/tests/deployment_planner.rs` 各 1）**加上** `application/tests/facade_relationship_governance.rs:965` 的 `#[cfg(unix)] prepare_refuses_conversion_when_the_relation_volume_cannot_host_links` |
| 两端都跑（不构成差额） | 2 | `src-tauri/src/updater.rs` 的 `#[cfg(any(windows, target_os = "macos"))]` 两例 |

修正后的推算（平台无关 + 两端共跑的 `#[test]` 共 **1105** 个）：

- Windows = 1105 − 6（Unix 专属）− 1（macOS 专属）= **1098**（实测 1098）
- macOS = 1105 − 9（Windows 专属）= **1096**（实测 1096）

**闭合结论**：「2 项未归因」已闭合为**上一版平台门控清点漏了 2 个条目**（1 个 `#[cfg(unix)]` + 1 个 `#[cfg(target_os = "macos")]`），不是被静默排除的用例。Windows 侧的集合级核对结果：

- 9 个 Windows 专属用例（8 函数级 + 1 模块级）在 `--list` 中**全部在场**，并在全量跑中逐个输出 `test … ok`；
- 6 个 Unix 专属与 1 个 macOS 专属用例**全部缺席**；
- `--list` 计数（1098）= 全量通过数（1098）= 逐条 `ok` 行数（1098）。

复现方式（两端命令相同）：

```bash
cargo test --workspace --all-features -- --list 2>/dev/null | grep -c ': test$'
cargo test --workspace --all-features
```

**计数口径提醒**：Windows 的 `1098 通过`（修复前）/ `1110 通过`（2026-09-19 修复后）都含 **17 条能力守卫跳过**的场景（详见下节），因此真正执行断言的分别是 **1081** / **1093** 条。macOS 侧同一命令的跳过标记计数为 0。

## 能力守卫在 Windows 的实测（2026-09-18 新增）

本宿主为非管理员账号（`IsInRole(Administrator)=False`，完整性级别 Medium），且**未开启开发者模式**。实测：

| 命令 | 结果 |
|---|---|
| `cargo test -p skillhub-application --test facade_relationship_governance --all-features` | **58 通过 / 0 失败 / 0 忽略** + **17 条 `SKILLHUB-TEST-SKIP;`**（Windows 少 1 个 `#[cfg(unix)]` 用例，故 58 而非 macOS 的 59） |
| 同上 + `SKILLHUB_TEST_FORCE_NO_LINK_CAPABILITY=1` | **58 通过 / 0 失败** + **18 条**标记（第 18 条即目录链接守卫，RC-16 场景） |
| 同上 + `SKILLHUB_REQUIRE_SYMLINK_CAPABILITY=1` | **41 通过 / 17 失败**（跳过变硬失败，17 处符号链接守卫全部接线） |

- 该宿主 `available_capabilities()` = `symlink: false, junction: true, copy: true`。
- 基线运行的 17 条标记**全部来自符号链接守卫**（其夹具自身要建真实符号链接）。逐条用例名：`committed_relation_is_rollbackable_when_link_capability_exists`、`managed_copy_conversion_replaces_the_copy_and_rollback_restores_the_managed_copy`、`shared_reference_conversion_repoints_one_alias_and_keeps_the_shared_body`、`rollback_rejects_a_replaced_relation_parent_before_removing_the_managed_link`、`commit_creates_and_verifies_the_staged_link_before_removing_the_original_entry`、`commit_failure_removes_new_link_before_restoring_and_preserves_central_fingerprint`、`rollback_retries_relation_writeback_after_filesystem_restore_failure`、`rollback_rejects_backup_metadata_tampering_without_restoring_or_writing_old_relation`、`rollback_rejects_a_backup_directory_replaced_by_a_symlink`、`rollback_rejects_an_original_symlink_backup_with_a_changed_target`、`rollback_refuses_to_remove_a_path_that_no_longer_matches_the_prepared_link`、`relation_migration_can_resume_from_a_new_facade_using_the_durable_journal`、`failed_relation_commit_re_reads_facts_on_retry_instead_of_returning_stale_failure`、`concurrent_commits_of_one_prepared_relation_are_serialized`、`governance_batch_cancelling_one_row_leaves_the_others_untouched`、`governance_batch_partial_failure_keeps_successes_with_per_item_retry_info`、`concurrent_governance_batch_commits_do_not_bypass_the_relation_lock`。
- **新证据**：基线只有 17 条而非 18 条，说明目录链接守卫**没有**跳过——RC-16 的 `governance_batch_requires_the_shared_impact_confirmation_per_row` 在 Windows 上以目录联接回退**真正执行了断言**。这是 RC-16 断言在 Windows 侧的首个执行证据。
- **守卫原因文本的可见性**：17 条标记可在 `cargo test` 的 stderr 中直接读到完整原因 `this host cannot create a symbolic link, so the guarded scenario is skipped and its assertions never run (RC-15)`。注意 **PowerShell 的 `2>` 重定向会把长行截断**（本次实测截在 74 字符，导致行号丢失）；需要完整标记请用 Git Bash 原生重定向后再读取原始字节。

### Windows 能力探测的真机边界（只能真机观察）

在本机非管理员账号上，`std::os::windows::fs::symlink_dir(target, link)` **返回 `Ok(())` 却没有创建任何东西**：紧随其后的 `symlink_metadata(link)` 报 `os error 2（系统找不到指定的文件）`。产品的 `probe_symlink` 会复核创建结果是否真的成为 reparse point，因此正确判定 `symlink = false` 并回退目录联接；若采信 `CreateSymbolicLink` 的返回值，转换会在用户目录里留下一个空目录（`filesystem.rs` 注释中描述的「报告成功但未落地」正是这一情形）。该行为在 macOS 上无法复现，属 Windows 真机独有证据。

### 关系四页面窄视口的自动化覆盖缺口（2026-09-18 登记）

E2E 全量 **387 用例 / 35 spec** 里有大量 800px 窄视口断言，但它们都落在 **DEV-only 的 `/__preview/*` 预览路由**上（`app-shell-layout.spec.ts`、`import-flow.spec.ts`、`library-card-view.spec.ts`、`discovery-cards.spec.ts`、`agents-projects-preview.spec.ts` 等，宽度集合多为 800/1024/1280/1440）。与四个关系路由直接相关的 `relationship-views.spec.ts` 只覆盖 `/__preview/agents/detail` 与 `/__preview/skill-detail/:id`，**没有对 `/`、`/relationships`、`/relationships/decisions`、`/relationships/governance` 的生产路由做窄视口布局断言**。

影响：J-REL-1—4 的窄视口判据目前**只能由真机/真桌面取证**（本轮已完成，见《人工验收清单》归档 R-3），没有可重复的自动化护栏。若要把它变成回归护栏，需要为生产路由补一条 800×600 的「根无横溢 + 主内容区不重叠 + 操作可达」E2E；本轮**未新增测试**（真机结论优先，且本机 0 条真实关系，断言会退化成空态检查）。

### 启动期控制台噪音：CSP 阻断 IPC 自定义协议（2026-09-18 发现，同日修复并加了护栏）

真桌面启动时控制台曾稳定复现（自动化可完整捕获，见《人工验收清单》归档 R-3 末条）：

```
log[error] Connecting to 'http://ipc.localhost/query_application' violates the following Content Security
  Policy directive: "default-src 'self'". Note that 'connect-src' was not explicitly set, so
  'default-src' is used as a fallback. The action has been blocked.
console.warning: IPC custom protocol failed, Tauri will now use the postMessage interface instead
```

原值是 `tauri.conf.json` 的 CSP 未显式声明 `connect-src`：功能上 Tauri 会回退 postMessage 并继续工作，但每次启动都多一次失败的协议往返。**修复（2026-09-18）**：CSP 改为 `"default-src 'self'; script-src 'self'; connect-src 'self' ipc: http://ipc.localhost"`，重跑 `tauri build` 后在真实 release exe 上冷启动复现测得 **`captured diagnostics (0)`**，且 `Network.*` 出现 3 组 `POST http://ipc.localhost/query_application → 200`。**今后以 release 产物为准的验收应看到 0 条 CSP 错误；若这 2 条错误重新出现，即该配置回归。**

**回归护栏**：`scripts/verify_release_readiness.mjs` 新增两条断言——`connect-src` 必须显式声明且同时含 `ipc:` 与 `http://ipc.localhost`；`script-src`/`default-src` 必须仍为 `'self'`（防止为了修 IPC 而放宽脚本来源）。`scripts/verify_release_readiness.test.mjs` 增加对应用例。红绿已实测：把 CSP 还原成旧值 → `not ok`；改回现值 → `ok`。命令：`node scripts/verify_release_readiness.mjs`、`node --test scripts/verify_release_readiness.test.mjs`（当前 PASS / 10 用例全通过）。

### 产出用于真机验收的 release 产物（2026-09-18 实测口径，两条坑）

1. **必须用 `tauri build`，不能只用 `cargo build --release`。** `tauri build --no-bundle -v` 原样打印出它实际执行的命令是 `cargo build --bins --features tauri/custom-protocol --release`。缺 `tauri/custom-protocol` 时产出的 exe **不内嵌/不伺服前端**：窗口能开，但页面停在 `chrome-error://chromewebdata/`、`document.querySelectorAll('main').length === 0`。本轮实测对照——坏产物 26,348,544 字节 / 正常产物 28,176,384 字节。**任何真机 UI 验收前都应先确认页面 URL 是 `http://tauri.localhost/` 且 `main=1`**，否则后续所有几何结论都是无效的。
2. **`tauri build` 可能卡在 `beforeBuildCommand` 不返回。** 本机实测一次 `pnpm --dir apps/desktop tauri build --no-bundle`：前端步骤已写出 `dist/assets`，但随后 **11 分 18 秒内无 cargo 进程、日志无新增**，最终只能手动终止（`tauri build -v --config` 覆盖后同一仓库 1 分钟内正常完成）。规避法是分两步：

   ```bash
   pnpm --dir apps/desktop build
   pnpm --dir apps/desktop tauri build --no-bundle --config '{"build":{"beforeBuildCommand":"echo frontend-already-built"}}'
   ```

   注意前端产物哈希不变时（例如只改 `tauri.conf.json`）也要**重跑一次 `tauri build`**：CSP、图标、窗口配置都是编译期内嵌的。

### 部署链路的自动化覆盖缺口（2026-09-18 登记，当日已按 TDD 闭合）

本机 Windows 实测 **142 个测试目标 / 1098 通过 / 0 失败**，但一次真实「添加到 Claude Code」两次提交都失败（详见《人工验收清单》归档 R-6）。1098 全绿与真机必失败并存，说明这段链路的断言强度不足，缺口有三处：

1. **没有任何测试真的去建物化目录。** `materialized_source()` 的物化路径含 `sha256:` 冒号，Windows 建目录必失败（`WinError 267`），但 macOS/Linux 上 `:` 合法，所以跨平台跑都绿。缺一条「物化目录路径在 Windows 上可创建」的断言（可直接对 `LibraryPaths` 派生出的路径做断言，或断言路径分量不含 `:`）。
2. **没有端到端的「部署后必须能读到部署记录」断言。** `deployments.target_id` 的外键指向生产从不写入的 `targets` 表；现有部署相关测试都在夹具里**手写** `INSERT INTO targets`（这正是全仓 `INSERT INTO targets` 只出现在测试中的原因）——夹具替生产补了那一行，于是外键永远不报错。缺一条从「新逻辑目标模型解析出 target」到「记录落库」的用例，且该用例**不得**预置 `targets` 行。
3. **没有测试覆盖「Agent profile 声明的能力」与「部署路径实际使用的能力」一致。** `builtin_profiles.rs` 断言了 profile 数据本身（含「所有客户端 `junction` 必须为 false」，且这条断言目前是绿的），但没有任何非测试代码读取它，于是产品不变量成立、部署行为却违反它。缺口是「行为层面」的用例，不是数据层面的。

**不要用放宽断言或加延时来掩盖**；这三条都应按 TDD 补成失败用例再修实现。

**→ 已闭合（2026-09-18，红→绿全程实测）。** 三条缺口各对应一条新用例族，全部先红后绿：

| 新套件 | 用例数 | 覆盖的缺口 | 红证据 | 绿证据 |
|---|---:|---|---|---|
| `crates/skillhub-core/tests/deployment_staging.rs`（+ 新模块 `core/src/deployment/staging.rs`） | 6 | 缺口 1：物化/中转目录名不得含 `:`、跨调用稳定、与 manifest 口径一致 | 首跑 2 失败（目录名含 `sha256:` 前缀） | 6/6 通过 |
| `crates/skillhub-application/tests/facade_deployment_accounting.rs` | 6 | 缺口 2 + 3：端到端「计划 → 提交 → 记录落库」，夹具**不预置** `targets` 行，断言提交后 `targets`/`deployments`/`deployment_relations` 三表有账；并断言 `junction: false` 的客户端目标永不出现目录联接模式（真实宿主能力 ∩ profile 声明） | 首跑 6/6 失败（junction 被选给 Claude Code、`operation.conflict`、`targets` 零行） | 6/6 通过 |

配套实现（与缺口一一对应）：D-7 中转目录名去 `sha256:` 前缀（core staging 模块 + application `materialized_source` 同口径）；D-8 新增 storage 层 `target_repository`（`Database::target_repository()`），提交部署前按物理目标登记 `targets` 行，写账失败暴露为 `operation.conflict` 而非静默丢账；D-9 目标能力取「profile 声明 ∩ 宿主探测」，`list_deployment_targets` 与规划器共用同一口径。前端配套：部署警告文案经 i18n 渲染（新键 zh-CN/en-US 对称，未知键回退原文），i18n + deployment 定向电池 66/66 通过、`check`（ESLint + tsc）无输出。

修复后受影响包回归（Windows 宿主，`--all-features`）：core + storage + adapters + application **133 个测试目标 / 1067 通过 / 0 失败**，desktop（src-tauri）**3 个测试目标 / 26 通过 / 0 失败**；`bindings.ts` 以「SHA256 前后一致 + 再生成一次为不动点」确认零漂移。两套新增用例均为平台无关（不带 `#[cfg]` 门控），两端工作区基线下次全量复跑时各 **+12**（推算 macOS 1108 / Windows 1110）。

**→ 推算已兑现（2026-09-19 实测）**：Windows 全工作区 **144 目标 / 1110 通过 / 0 失败 / 0 忽略**，`--list` 计数 1110，两套新用例各 6/6 通过——与「1098 + 12 = 1110」逐字一致（见上方「Windows 工作区基线更新」）。macOS 1108 为推算值、暂未复跑。

**→ 修复轮再复跑（2026-09-19 同日）**：R-9/R-10/R-11/R-12 修复落地后，Windows 全工作区再次复跑为 **144 个测试目标 / 1115 通过 / 0 失败 / 0 忽略**（`--list` 计数 1115；较 1110 **+5**，全部来自 `facade_deployment_accounting.rs` 本轮新增用例——`facade_operation_journal` 那条是**改写**而非新增）。口径不变：1115 含 **17 条能力守卫跳过**（本轮日志实测 17 条 `SKILLHUB-TEST-SKIP;`，全部为 `facade_relationship_governance` 的符号链接守卫）→ **真正执行断言 1098 条**。前端 **165 文件 / 1782 用例通过**（较 1774 **+8**；`RecoveryPage.test.tsx` 重写为 8 例、`pending/nativeApi.test.ts` +2、`DataProtectionPage.test.tsx` +1、`operations/nativeApi.test.ts` 重写；`deployment/nativeApi.test.ts` 与 `overview/OverviewPage.test.tsx` 仅补新必填字段、例数不变）。`pnpm check:frontend`（ESLint `--max-warnings 0` + tsc）无输出；`pnpm test:release`（`verify_release_readiness` 护栏）**10/10 通过**；受影响 E2E 3 spec **48/48 通过**。`bindings.ts` 因 `RecentOperationSummary.targets` 与 `TargetOperationResult.residue` 两个新字段而**必然变更**，已重新生成并确认为不动点（SHA256 前后一致）。

**环境注意（本机实测）**：`cargo test --workspace --all-features` 必须**前台 + 非沙箱 + `CARGO_INCREMENTAL=0`**——沙箱会拒绝写 `target/debug/deps/*.exp` 与 `*.d`（报 `os error 5 拒绝访问`），后台 shell 包装则会直接卡住。E2E 的 webServer（`pnpm --dir apps/desktop exec vite …`）在**后台 shell** 里会卡住不监听 5174，必须前台运行。

### 部署**失败**处理的自动化覆盖缺口（2026-09-19 登记，**同日闭合**）

1110 全绿之后，第二轮真机验收仍撞出 3 项发布阻塞（详见《人工验收清单》归档 R-9/R-10/R-11）。三者的共同特征是：**只在真实文件系统 + 真实数据库 + 真实IPC 的三方不一致时才暴露**，而现有自动化全部在「单次成功路径」或「夹具已预置好前提」的条件下运行，因此结构性地看不见它们。按 TDD 补测时，每条都必须先写成失败用例：

1. **没有任何用例覆盖「重复添加同一个 (Skill, 目标)」。** `deployments` 表带 `UNIQUE(target_id, runtime_name)`，而移除只把行改成 `state='removed'`（保留行）；现有部署用例每条都用**新**夹具数据库，从不经历「添加 → 移除 → 再添加」。缺一条：同一目标先 `commit_deployment` 再 `commit_undeploy` 再 `commit_deployment` 的端到端用例（当前必红：第二次提交会撞唯一约束并在**写盘之后**报 `deployment.target_exists`）。
2. **没有任何用例断言「失败时磁盘与账目必须一致」。** `LocalDeploymentBackend::apply_target` 先写盘后落库且失败不回滚。缺一条：注入落库失败后断言目标目录**不存在**（或已回滚），以及断言操作记录里的 `object_results` 携带失败目标与路径。当前必红。
3. **没有任何用例覆盖「失败应记终态还是 `needs_recovery`」。** `commit_deployment` 把任何非 `ObjectNotFound` 错误一律写成 `needs_recovery`；`facade_combinations.rs` 里已有类似的反例（`duplicate_create` 记 `rolled_back` 并断言 `error_code`），但部署链没有对应断言。缺一条：`target_exists`/`input.invalid` 这类**重试无法解决**的失败必须记终态，且**不得**让 `recovery_state` 离开 `clean`。当前必红。
4. **前端恢复路径的测试是「假绿」。** `runTrackedOperation` / 恢复相关用例都以**注入的 facade** 为替身，因此 `acknowledge_recovery` 这个服务端**未实现**的命令在测试里永远成功（`facade.rs:1474-1490` 反而用用例把「未实现」固化成了期望值）。缺一条：`/recovery` 的「确认恢复」必须调用 `resolve_recovery`（或断言它不调用未实现的命令）。当前必红。

**不要用放宽断言或加延时来掩盖**；这五条都应按 TDD 补成失败用例再修实现。另：`operations` 的失败渲染（`String(reason)` → `[object Object]`）也没有前端用例覆盖，同类缺口。

5. **没有任何用例断言「恢复（回滚）会把磁盘改回来」。** `LocalRecoveryBackend::resolve`（`application/src/lib.rs:9341-9368`）只执行 `UPDATE operations SET phase/state`，**不删任何产物**；部署路径也从没记录补偿信息（`application/src/lib.rs` 全文无 `inverse` 赋值，实测那次失败部署 op 为 `inverse: null` / `recovery_data: {}`）。缺一条端到端用例：制造一次「写了一半」的中断部署 → 执行 `resolve_recovery { action: "rollback_operation" }` → 断言目标目录条目数**回到操作前**、`deployments`/`deployment_relations` 无残留、`recovery_state` 回到 `clean`。当前必红。**这一条不补，前四条修完也只是「账目干净、磁盘仍留半成品」。**

**实施顺序与「必须先改写的既有断言」（2026-09-19 裁决后补）**：批次顺序固定为 **② 落库失败回滚磁盘产物 → ① 唯一约束允许 `removed` 行重新激活（**已定 upsert 复用旧行**；备选的分区唯一索引已排除——两个选项用户可见面无差别，只在库内卫生上分优劣）→ ③ 失败分类（可重试 vs 终态）→ ⑤ 恢复 = 撤销磁盘产物 + 结算账目 + 失败渲染改走 `describeNativeError`（统一回滚、用户可见文案用「恢复」，文案改写见下）**。前三步落在上面第 1—3 条缺口，第五步落在第 4、5 条缺口。**不能颠倒**：先做 ③ 会把 ① 的必发失败掩盖成终态；先做 ⑤ 会让用户看到「已恢复」而磁盘仍留孤儿。**两条现有断言把已废弃/未实现的命令当成了期望值，必须先改写（否则「修好」与「测试绿」互相矛盾）**：

- `apps/desktop/src/features/operations/nativeApi.test.ts:19-23` 现在断言「`acknowledgeRecovery` 会调用 `execute_command { type: "acknowledge_recovery" }`」——而该命令既未被实现、又按裁决要删除，等于把待删行为固化成了期望值。修复后应断言调用 `resolve_recovery`、`action = "rollback_operation"`、`operation_id` 为被选中的那条。
- `crates/skillhub-application/tests/facade.rs:1474-1490`（`unsupported_operations_return_a_structured_internal_error`）把 `AcknowledgeRecovery` 的 `execute.unsupported` 写成了期望值。裁决已定「不补齐 `acknowledge_recovery` 服务端实现」，因此该断言可保留为「该命令未被实现」的现状记录，但**应在注释里标明这是有意为之**，避免后来者误以为它是待修缺口。

第五步还牵连文案：`recovery.heading`「完成中断的操作」/`recovery.description`「确认操作状态后，再确认恢复并继续。」描述的是 `complete_operation`（把没走完的操作做到底），与「统一回滚」相悖，需改写成「恢复未完成的操作 = 撤销这次未完成的改动」并同步 zh/en 键对称；按钮 `recovery.acknowledge`「确认恢复」保留。改完跑术语专项测试（Task 11，7/7）与 `node scripts/i18n-cjk-audit.mjs`。

**另登记一条未覆盖项（非本轮，DEV-3）**：「文件夹名 == SKILL.md name」的生产校验当前完全不存在（只在文档与夹具里成立），因此没有任何用例覆盖它。将来在校验入口落地时必须同时补两类用例：**正例**（文件夹名与 `SKILL.md` frontmatter `name` 一致 → 放行）与**重名反例**（不同来源的同名 Skill → 集中库内可共存，但部署到同一 Agent 目标时 `runtime_name` 相同会撞 `UNIQUE(target_id, runtime_name)`，需给出明确处置而不是裸报 `deployment.target_exists`）。

**另登记一条未覆盖项（非阻塞，2026-09-19，R-12）**：数据保护 →「退出管理」的部署关系列表不过滤 `state='removed'`（后端 `list_deployments` 只按 `skill_id` 过滤，UI 原样渲染）——现有备份页用例只断言「会调用 `list_deployments` 查询」，没有断言**渲染结果**里不含已移除的部署，所以这条缺陷在任何测试下都不会变红。修消费侧过滤时同步补一条「`state='removed'` 的部署不出现在退出管理列表」的渲染用例。

### 部署失败处理缺口闭合（2026-09-19 修复轮，**已闭合**）

上文 5 条缺口按固定顺序全部先写成失败用例、再修实现转绿；R-12 的渲染缺口一并补上。落地清单：

| 缺口 | 落地用例 | 断言要点 |
|---|---|---|
| 1 重复添加 | `facade_deployment_accounting.rs::re_adding_a_removed_skill_reuses_the_same_position` | `commit → undeploy → commit` 后 `deployments` **仍只有一行**、`id` 未变、`state` 回到 deployed、`recovery_state == Clean`。实现侧 = `insert_sync` 改为 upsert 复用旧行（保留旧 `id`） |
| 2 失败时磁盘与账目一致 | `facade_deployment_accounting.rs::a_storage_failure_rolls_back_the_written_target` | 以 `BEFORE INSERT` 触发器强制落库失败 → 断言目标目录**不存在**、无残留。实现侧 = `apply_target::roll_back_applied` |
| 3 失败分类 | `facade_deployment_accounting.rs::a_rejected_deployment_is_terminal_and_keeps_startup_clean`、`a_rejected_deployment_records_the_failing_target_details` | `target_exists` 类失败记 `rolled_back` 且 `recovery_state` **保持 `Clean`**；操作记录携带目标与路径明细（`RecentOperationSummary.targets`） |
| 4 前端恢复路径「假绿」 | `operations/nativeApi.test.ts`（5 例）、`recovery/RecoveryPage.test.tsx`（8 例）、`pending/nativeApi.test.ts`（+2 例） | 断言调用 `resolve_recovery { action: "rollback_operation" }`；未声明的动作被拒；恢复页**列出全部候选**；失败渲染不出现 `[object Object]` |
| 5 恢复要撤销磁盘 | `facade_deployment_accounting.rs::rolling_back_recovery_removes_the_recorded_target` | 预置带 `recovery_data.pending_targets` 的 `needs_recovery` 操作 → `resolve_recovery` 后磁盘条目被删除、账目结算 |
| 既有断言改写 | `facade_operation_journal.rs::a_failed_deployment_commit_rolls_back_cleanly_and_retry_reuses_the_record` | 由「记 `needs_recovery`」**改写为「记 `rolled_back` + `recovery_state == Clean`」**——原断言固化的正是缺口 3 本身；「同一 prepared id 重试仍落到同一条记录并最终 `completed`」的断言保留 |
| R-12 渲染缺口 | `DataProtectionPage.test.tsx`「只列出仍然存在的部署，并用界面语言标注其状态」 | `state='removed'` 的行不进「退出管理」列表；`state` 经 i18n 渲染 |

E2E 侧连带修正一处**假接线**：`native-flow-preview.spec.ts` 的 mock 原本把 `list_recovery_candidates` 写在 `execute_command` 分支，而产品走的是**查询**——未命中时落到 `default: bootstrap_snapshot`，恢复页整页进错误态。该用例在本机的表现是**进程挂死而不是 `not ok`**（见下条），极易被误读成「用例只是慢」。

**E2E 失败即挂死（2026-09-19 本机新发现，判定口径更新）**：Windows 宿主上只要**有任一 E2E 用例失败**，runner 会在收尾阶段挂住且**不打印 `not ok`**，表现为「逐条 `ok` 出现、进程不退出」。因此进程挂死**不能**直接判定为「零失败下的收尾问题」；必须反查 `test-results/<spec>/error-context.md`——**该文件存在即说明有用例失败**，其页面快照通常直接指出失败位置（本轮即据此定位了上述 mock 假接线）。判定零失败的正确组合是：**逐条 `ok` 计数 == 用例总数 且 无 `not ok` 且 `error-context.md` 计数为 0**；`retain-on-failure` 的 trace 残留在被强杀时会留下，**不能**用来推断失败数。

上述口径在本次全量复跑中被验证过一次：全量输出 **386 `ok` / 0 `not ok` / 387 用例**，唯一缺失项 `startup-performance.spec.ts:3:5` 留下 1 份 `error-context.md`（即**真的失败过**，不是「没跑到」）。单独复跑该 spec 得 **1 passed（2.2s，`ok`）** → 判定为**宿主负载导致的启动性能预算 flake**（与 Rust 侧 `facade_search_assist` 的 flake 同类），非本轮改动引入。**注意这类用例会随宿主负载随机翻红，做发布判据时应单独复跑确认，不要直接计入失败数。**

**E2E 全量欠账闭合（2026-09-19 晚，D-11 修复后实测）**：`pnpm exec playwright test --config=tests/e2e/playwright.config.ts` 全量 **387/387 `ok` / 0 `not ok` / 0 条 `error-context.md`，用时 3.0m，exit 0，进程正常退出**（本次收尾未挂）；上一轮 flake 的 `startup-performance` 直接通过。跑前按口径确认了 5174 端口空闲。至此 E2E 侧无已知欠账；此后若再出现全量翻红，按上段口径先查 `error-context.md` 再定性。

**Release 产物重建（2026-09-19 晚，含 R-9…R-12 与 D-11 全部修复，提交 `e30c3217`）**：`pnpm --dir apps/desktop build`（15.8s）→ `pnpm tauri build --no-bundle --config '{"build":{"beforeBuildCommand":"echo frontend-already-built"}}'`（1m09s，规避法有效）→ `target/release/skillhub-desktop.exe`（2026-09-19 21:37）。核验：`dist/index.html` 引用的 5 个资源字面量全部存在于 exe 内（`missing_in_exe: none`）；`pnpm test:release` 复跑 10/10。**旧产物作废，真机验收一律用这份。**

### D-11 规划期占用识别（2026-09-19 转正登记，同日闭合）

长期跟踪项 D-11 转正：planner 是纯函数，从不读库和盘，而应用层解析出 `VerifiedTarget` 后**从未填充** `existing`（`core/src/deployment/model.rs:209` 硬编码空 `Vec`）→ 目标位置已被占用时规划仍返回 `change=create / conflicts=[]`，冲突拖到 commit 期才暴露。此前它只记录在工作区 MEMORY，未进四份当前文档，本轮按 TDD 闭合并同步登记（矩阵第 14 条）。落地清单：

| 场景 | 落地用例（`facade_deployment_accounting.rs`） | 断言要点 |
|---|---|---|
| 目标被外来目录占用 | `a_plan_for_an_occupied_destination_is_rejected_before_commit` | `GetDeploymentPlan` **规划期即 Err**（`deployment.target_exists`），外来内容零触碰、无账目行 |
| 同位置已有本 Skill 部署 | `replanning_a_deployed_skill_reports_noop` | 活跃受管行经 `attach_target_occupancy` 进入规划 → `change == NoOp` 且无冲突（重规划不再假装「全新创建」） |
| 移除后重添 | `replanning_after_undeploy_offers_a_clean_readd` | `removed` 行不算占用 → `change == Create`、无冲突（守护 upsert 语义不被过度矫正） |
| commit 期竞态（既有用例语义修正） | `a_rejected_deployment_is_terminal_and_keeps_startup_clean`、`a_rejected_deployment_records_the_failing_target_details` | 原场景「规划时已占用」如今在规划期即被拒、走不到 commit，直接沿用会让用例在**错误的层面**通过；改为「先规划（干净态）→ 放入外来目录 → 再提交」，保住终态分类与目标明细的 commit 期覆盖 |

实现侧（最小改动）：`get_deployment_plan` 在解析后调用新增的 `attach_target_occupancy`——活跃受管行取自 `deployments.list_all` 按 `managed && state ∈ {deployed, needs_recovery}` 过滤（**detached `managed=0` / `removed` 行不算占用**，退出管理后的既有目录由磁盘探测记 `Unknown` 并拒规划）；无行覆盖时对 `目标/runtime_name` 做 `symlink_metadata` 探测。commit 期 `filesystem.prepare` 的 `target_exists` 检查保留为「规划-提交竞态」兜底，不删除。

## 异步入口审计（125 次 / 26 个非测试源文件）

判定口径：`API边界` 是 facade/native API 内部调用，不单独创建顶栏任务；`纯浏览` 只读查询，不记操作日志；`已桥接` 是用户可触发的长流程并进入 `runTrackedOperation`。一个用户入口应有至少一个成功、失败、取消或恢复测试。

| 文件 | 调用点（行） | `runTrackedOperation` | 分类 |
|---|---:|:---:|---|
| `deployment/reconcileNativeApi.ts` | 40 | 否 | API边界 |
| `backup/nativeApi.ts` | 6, 11, 16, 20, 25, 30, 44, 49, 69, 74 | 否 | API边界；页面恢复提交已桥接 |
| `deployment/nativeApi.ts` | 147, 151 | 否 | API边界；页面部署提交已桥接 |
| `bootstrap/api.ts` | 84, 96, 135, 142, 152, 162, 176, 227 | 否 | API边界/启动读取 |
| `app/router.tsx` | 144 | 否 | 纯浏览/路由读取 |
| `agents/nativeApi.ts` | 152, 181, 192 | 否 | API边界；Agent 重扫描入口已桥接 |
| `discovery/api.ts` | 83, 134, 141, 151, 161, 171, 180, 200, 210, 220 | 否 | API边界；本机发现重扫描入口已桥接 |
| `import/nativeApi.ts` | 354, 365, 409, 420 | 否 | API边界；导入提交页面已统一桥接 |
| `markdown/nativeApi.ts` | 54, 70, 83 | 否 | API边界 |
| `skills/nativeApi.ts` | 217, 237, 249, 259, 334, 359, 362, 367, 375, 379 | 否 | API边界；页面批量长流程已桥接 |
| `projects/nativeApi.ts` | 42, 62, 84, 92, 103 | 否 | API边界 |
| `security/nativeApi.ts` | 63, 75, 110, 124, 134, 144, 154 | 否 | **已收口**：`set_finding_disposition` 与 `cancel_operation` 经发现项页面入桥；`run/recheck_llm_safety` 经 AI 检查入桥（可取消）。原 124/134/144/154 四个无调用点导出（`renameNativeSkill` 等）已删除（`58d8ec94`） |
| `skill-detail/nativeApi.ts` | 149, 175, 280, 289, 305, 322, 335, 353, 514, 536, 570, 583 | 否 | **已收口**：全部用户写入经面板/页面入桥；`analyze_semantic_duplicates`、`analyze_conflict` 此前已桥接 |
| `pending/nativeApi.ts` | 36, 56, 61, 64, 68, 72, 78, 103, 123 | 否 | API边界；**页面处置写入已全部入桥**（单条/撤销 `instant`、批量 `phased`） |
| `removal/nativeApi.ts` | 42, 46, 53, 66, 73, 85, 119 | 否 | **已收口**：prepare/commit 长流程入桥（带 operationId 关联）；`detach_management` 经项目详情页入桥 |
| `relationships/governance/nativeApi.ts` | 60, 75, 86, 98, 109 | 否 | API边界；治理页面调用已桥接，页面 4 处失败通知抑制已移除 |
| `relationships/decisions/decisionsApi.ts` | 54, 64 | 否 | API边界；同步决定按即时命令处理 |
| `settings/AiNetworkSettings.tsx` | 24 | 是 | 即时写入；`instant` 模式与失败回滚 |
| `operations/nativeApi.ts` | 35 | 否 | API边界/纯浏览 |
| `settings/api.ts` | 203, 215, 224, 233 | 否 | API边界 |
| `settings/AutomationSettings.tsx` | 15 | 是 | 即时写入；`instant` 模式与失败回滚 |
| `settings/llmApi.ts` | 196, 208, 217, 220, 223, 226, 239, 265 | 否 | API边界 |
| `settings/GeneralSettings.tsx` | 28, 39 | 是 | 即时写入；`instant` 模式与失败回滚 |
| `settings/nativeApi.ts` | 40, 74, 122, 136, 146, 152, 162 | 否 | API边界 |
| `settings/ViewSettings.tsx` | 15 | 是 | 即时写入；`instant` 模式与失败回滚 |
| `settings/SettingsPage.tsx` | 211 | 否 | 纯浏览；仅打开受限的官方发布页 |

## Windows 首次前端电池（2026-09-18 新增）

前端全量此前从未在 Windows 上记录过。在仓库根执行（Windows 11 家庭版 25H2，node v22.22.2 / pnpm 11.21.0）：

| 命令 | 结果 |
|---|---|
| `pnpm install --frozen-lockfile` | exit 0 |
| `pnpm --dir apps/desktop test --run`（第二次） | exit 0，**165 个文件 / 1773 用例全部通过**（34.9 秒）——与 macOS 基线数量**完全一致**（该次早于取消路径回归测试的加入） |
| `pnpm --dir apps/desktop test --run`（清空 `apps/desktop/node_modules/.vite` 后**冷缓存**复跑，2026-09-18） | exit 0，**165 个文件 / 1774 用例全部通过**（38.87 秒；`transform 20.37s / setup 60.63s / import 80.31s / tests 246.02s / environment 164.69s`）。用例数比上一行多 1，是新增的取消路径回归测试。这一跑的环境开销（setup 60.6s、environment 164.7s）比首次失败那跑更重，仍全绿 |
| `pnpm --dir apps/desktop test --run src/features/overview/OverviewPage.test.tsx`（定点复跑） | exit 0，**1 个文件 / 25 用例全部通过**（2.6 秒） |
| `pnpm --dir apps/desktop check`（ESLint `--max-warnings 0` + tsc） | exit 0，无输出 |
| `node scripts/i18n-cjk-audit.mjs` | user-visible（必须修）**0 条**；7 个已审阅豁免文件 / 52 行——与 macOS 一致 |

**首次全量的 3 个非确定性失败（已定位并处置，未降低断言、未加人为延时）**：第一次全量运行 exit 1，`Test Files 1 failed | 164 passed (165)`、`Tests 3 failed | 1770 passed (1773)`，全部集中在 `src/features/overview/OverviewPage.test.tsx`：

| 用例 | 失败摘要 |
|---|---|
| `keeps configured and discovered agent calibers distinct inside the combined frozen metric` | `Error: Test timed out in 5000ms.`（`OverviewPage.test.tsx:270`） |
| `drills the combined agent metric into the agent management workspace` | `Error: Test timed out in 5000ms.`（`OverviewPage.test.tsx:282`） |
| `links every pending summary item to the pending workbench` | `TestingLibraryElementError: Unable to find an accessible element with the role "link" and name "2 security findings"`；DOM 中只剩 `<output data-testid="location">/agents</output>`，即 `renderOverview()` 未产出内容（同一文件前两条已超时的连带结果，`OverviewPage.test.tsx:297`） |

**判定为非确定性、与宿主负载相关，不是跨平台功能缺陷**：同一次会话内第二次全量运行 exit 0（165/1773 全通过，34.9 秒），该文件单独运行 25/25 通过（2.6 秒）；两次运行的 vitest `setup` 阶段耗时分别为 158 秒 / 53 秒，首跑处于明显更重的负载下。报告原文是 `Test timed out in 5000ms`，即**超时**而非断言失败；失败与路径分隔符、大小写、换行无关。

**处置（2026-09-18 已落地，只改测试基建，未改产品代码、未动断言）**：`apps/desktop/vite.config.ts` 把逐用例预算从默认 5 秒放宽到 **15 秒**（`testTimeout: 15_000`），并在该文件内写明测量依据——165 个测试文件各建一次 jsdom 环境，冷缓存全量实测 `environment 164.69s / import 80.31s`（均为各 worker 内累加），5 秒预算在宿主争用下会被环境开销挤爆。**断言强度一条未改，也没有加入任何人为延时。** 验证方式是清空 `apps/desktop/node_modules/.vite` 后冷缓存复跑：**165 文件 / 1774 用例全通过**（见上表），且那一跑的环境开销比首次失败时更重。这是测试基建参数，不是产品行为变更。

## 已登记的测试边界与已知波动

- Windows junction 的真机证据已补齐（RC-13/RC-14，见《人工验收清单-2026-09-18》）：非管理员、未开开发者模式下按能力探测选择目录联接，原文件零误删、探针零残留、来源目录整体更新后仍可移除。跨卷**规则**的决策层也已在本机**真实第二卷**上实测（`paths_share_volume` 跨卷为 false、能力探针跨卷 `junction:false`、转换规划器跨卷拒绝并保留原入口、部署模式选择跨卷为 `ManagedCopy`、第二卷根条目数 4 → 4 零跨界写入）。**仍未取得**的是跨卷的**端到端写入**证据（该宿主没有「可写且用户可选」的第二块卷，且非管理员无法挂载）。另需重申范围：**跨卷只限制「纳入集中库管理」把复制改成链接，不限制部署**——跨卷部署走受管复制。只读目录、大小写与重启动仍需真机验证。
- Windows 全量的 `1098 通过`（修复前）/ `1110 通过`（2026-09-19 修复后）**不等于全部断言都跑过**：其中 17 条是能力守卫跳过（libtest 把早退记为 `passed`，靠 `SKILLHUB-TEST-SKIP;` 标记暴露），真正执行断言的分别是 1081 / 1093 条。报告 Windows 数字时必须同时给出跳过标记计数。
- **PowerShell 的 `2>` 重定向会截断长行**（本次实测截在 74 字符，`SKILLHUB-TEST-SKIP;` 标记的行号因此丢失）。需要完整日志请用 Git Bash 原生重定向，并用 Python 读取原始字节而不是 `Get-Content`。
- 前端测试中的 `useLibraryViewMode must be used within a LibraryViewModeProvider` 是上下文负例测试触发的 jsdom 日志；JSON 报告确认全量通过。
- 前端桥接测试会打印 React 的 `not wrapped in act(...)`（通知中心 toast 计时器在断言之外触发状态更新），未因此加延时或降低断言。
- 并发执行 `cargo test` 与 `vitest` 会产生超时假失败，必须串行。
- Playwright 会清空仓库根 `test-results/`，验证日志不要写在那里。E2E 进程可能在全部用例输出 `ok` 后不自行退出，判定请用逐条 `ok` / `not ok` 计数。
- **`cargo fmt --all -- --check` 在本机对 HEAD 即红（2026-09-19 发现，rustfmt 版本偏差）**：本机 rustfmt 对多个**本轮未触碰**的既有文件（`skillhub-core/src/application/deployment_service.rs`、`skillhub-core/tests/deployment_staging.rs`、`skillhub-storage/src/lib.rs`、`bootstrap_repository.rs`、application/test 的既有段落等）给出重排 diff——仓库上次格式化用的是另一版 rustfmt。**处置口径**：不要借此做全仓重排（无意义的大 diff）；新代码按本机 rustfmt 口径手写对齐即可。CI 步骤「Rust formatting」在本机跑绿前需先统一 rustfmt 版本。
