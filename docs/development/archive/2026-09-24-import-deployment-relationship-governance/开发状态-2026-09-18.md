# 开发状态（2026-09-18）

更新时间：2026-09-23（当前收口版）。本文件只保留当前开发状态、未完成事项和下一步；已完成明细、旧待办表和历史现场结论已归档至 `docs/development/archive/2026-09-21-人工验收清单整理/`。

当前分支：`feat/v0.2.0-product-completion`。开发遵循单一 worktree、顺序开发；当前未创建并行 worktree。

## 2026-09-22 验收记录

- macOS 15.6.1 ARM64；release 构建时间 `2026-09-22 20:18:26`；源码 HEAD `286199928fad1be0d08894570e629f65dc831be7`。
- `target/release/skillhub-desktop` 进程启动并打开本地数据库，但未出现可被桌面自动化层识别的窗口，无法取得窗口尺寸或页面证据；DEV-66—75 保持人工待验收，不判通过。
- 回查归档 DEV-55 后确认：导入向导仍把关系治理置于提交前，属于**旧问题未修复**。提交 `fa4c2ee2` 已改为“内容冲突 → 导入集中库 → 完成页选择现在整理或稍后处理”；导入只保留原目录并记录事实，链接整理继续由独立治理页执行。
- 本轮定向验证已通过：应用层关系治理 59/59，导入/完成页/发现路由/治理页四个前端测试文件 115/115，前端 lint、typecheck、生产构建及 Tauri 无打包构建均通过。提交 `fa4c2ee2` 后仍需重建 release，再继续人工流程 C、E。
- DEV-67 边界修正已提交为 `b9cb7c30`：移除关系页内部重复的“技能关系”标题，仅保留顶栏标题和三个子页签；关系布局 5/5、AppShell 21/21、lint、typecheck、前端 build 及 Tauri 无打包构建通过。`b9cb7c30` release 已于 `2026-09-22 23:37:25 +0800` 重建。

## 2026-09-23 导入恢复门禁回归

## 2026-09-23 Windows 概览高度复验

- **DEV-66 未通过 Windows 人工复验，不新增重复编号。** 当前 release 在物理 `2560×1600`、Windows 缩放 `175%` 的最大化窗口中，应用实际 client area 为 `1462×866`（`168 DPI`）；概览页出现页面级纵向滚动条。用户反馈 macOS 最大化时正常，但尚未留下可复核的 macOS 有效内容区尺寸，因此不能把平台差异写成通过证据。
- 源码不存在 macOS/Windows 专用的概览高度分支：壳层以 `100dvh` 建立可用高度，`#main-content` 固定为 `overflow-y: auto`；概览的指标、关系、主内容三带均为 `flex: 0 0 auto`，按自然高度展开。仅有的高度紧凑规则是 `@media (max-height: 44rem)`（约 `704px`），本机 `866px` 不会命中，却在扣除顶栏和页头后无法容纳全部自然高度内容，因而由页面根产生滚动。
- `tests/e2e/overview-height.spec.ts` 只测 `800×600`（会命中 `44rem` 紧凑规则）和 `1920×1080`（高度富余），遗漏了 `1462×866` 这类高 DPI 缩放后的有效窗口区间；因此“31/31 通过”不能证明该验收判据已覆盖。
- 修复已实施：紧凑阈值改为 `max-height:56rem`，继续以有效视口高度而非硬件分辨率控制密度；新增 `1462×866` 几何 E2E。概览高度 E2E 7/7、概览定向 Vitest 31/31、lint、typecheck、前端 build 与 Tauri 无打包 release 构建通过；新的 Windows release 已启动，等待人工复验。
- 本轮尝试全量 `pnpm test:e2e` 的 398 条回归未全绿，失败产物涉及导入治理、本地流程与 onboarding 等非概览范围；本次不把历史 `396/396` 写成当前 HEAD 的全量通过，也不将这些未归因失败混入 DEV-66 的完成结论，需另行分诊。

## 2026-09-23 Windows DEV-66 第二次人工复验

- **未通过，不再尝试第三次阈值微调。** 新版已消除滚动条，但最大化和窗口化都出现大面积底部空白；窗口化原本应保留自然排列和页面滚动，现也被不当地压缩。
- 根因已定位：本轮以 `@media (max-height:56rem)` 代替“是否最大化”的判断，CSS 无法区分“最大化后有效 client area 为 866px”和“任意窗口化为 866px”。该媒体规则同时压缩图表、卡片和标题；而 `.sh-overview__metrics`、`.sh-overview__relations`、`.sh-overview__content-grid` 全为 `flex: 0 0 auto`，`PageFrame`/内容区又 `align-content:start`，故压缩后的自然内容高度不会扩张填充 PageFrame 剩余轨道。
- 现有 E2E 仅证明 `scrollHeight <= clientHeight` 与模块未裁切，未断言“最大化时概览内容区占满剩余可用高度”，也未模拟/断言窗口化状态保持自然排列；35/35 相关 E2E 因而不能证明本条人工判据。
- 下一轮方案（未实施）：由 Tauri 窗口状态提供 `maximized` 语义，前端只在最大化时进入 fill 布局；窗口化恢复自然页面流及允许的页面滚动。最大化布局改为明确的可用高度轨道（而非继续缩小所有卡片），让主图表、标签/待办栏按剩余高度分配，同时保留数据完整、不可遮盖。先新增“最大化填满”和“窗口化不压缩”的失败用例，再实施。

## 2026-09-23 Windows DEV-66 第三次复验待执行

- 上述方案已实施：`windowChrome` 从 Tauri 原生窗口读取最大化状态并写入根样式类；仅最大化时概览取得剩余高度轨道，窗口化恢复自然页面流。状态切换后会触发图表重新量尺，避免旧 SVG 尺寸溢出新容器。
- 自动化已补“最大化填满/无滚动/无裁切”和“窗口化自然排列”断言；最大化 `800×600`、Windows `175%` 缩放后的 `1462×866`、`1920×1080` 均通过。相关 Vitest 65/65、Playwright 46/46、lint、typecheck、生产构建和 Tauri 无打包构建通过。
- 新 Windows release 已于 `2026-09-23 10:44:03 +0800` 生成并启动。**人工仍待验收**：请分别在最大化和窗口化状态确认内容填满、无重叠/遮盖/溢出，以及窗口化仍可自然滚动。

## 2026-09-23 Windows DEV-66 第三次复验反馈

- 页面和模块滚动条已消失，但截图显示两项**已修复后的边界遗漏**，登记为 **DEV-77**，不回退 DEV-66 的“最大化状态识别/根滚动”修复：① 最大化指标区固定为 5 列，实际只有 4 张次级指标卡，右侧留下整列空白；② 左侧部署图的 SVG 在 fill 网格切换后未随最终容器尺寸重测，越过面板边界并视觉压到右侧标签卡。
- 当前代码事实：`overview.css` 最大化规则使用 `grid-template-columns: repeat(5, ...)`，而数据模型固定为“1 张 hero + 4 张 compact stat”，hero 已跨整行；`DeploymentBarChartRuntime`/标签图运行时仅订阅 `window.resize`，没有 `ResizeObserver` 追随图表容器的最终几何尺寸。现有 E2E 只测模块外框与滚动高度，未测指标带的列占用、SVG 边界或左右卡片内容交叠。
- DEV-77 建议方案（待用户批准后开发）：最大化指标带改为四列次级指标轨道，hero 仍跨整行；图表运行时改用 `ResizeObserver` 在容器完成布局后重测，并补 E2E 断言“4 张指标共同填满指标带”“两个 SVG 均不越出各自图表容器”“左右模块内容矩形不相交”。
- 复现补充：离开概览页再返回后图表溢出消失，但仅切换到其他桌面应用再回来仍溢出。这排除了数据内容和页面根滚动作为主因，确认是**首次挂载/异步最大化布局/图表初始化的时序竞态**：重新进入会卸载并在最终容器几何下重建图表；失焦/回前台不重建也不触发容器观察。DEV-77 的 `ResizeObserver` 必须覆盖部署图和标签图，并在观察回调中安排布局稳定后的 resize，不能继续只依赖一次性 `window.resize`。

## 2026-09-23 DEV-77 实现与自动化验证

- 已实施：最大化指标带由 5 个空轨道修正为 4 个等宽次级指标轨道，hero 继续独占首行；部署图和标签图共享容器尺寸观察器，在 `ResizeObserver` 通知后的动画帧重新量尺，覆盖首次挂载、最大化布局完成及后续容器变化。
- 极矮最大化窗口仍完整展示全部内容：`800×600` 时首要指标改为同行紧凑呈现，部署明细改为两列，而窗口化页面不受此最大化规则影响。
- 新增单测、指标带填满及两个 SVG 容器边界 E2E。定向 Vitest 36/36、概览/预览/壳层 Playwright 47/47、lint、typecheck、生产前端构建和 Tauri 无打包构建通过；Windows release 已于 `2026-09-23 11:12:17 +0800` 重建并启动，人工复验仍待执行。

## 2026-09-23 概览人工验收通过

- 用户确认概览页滚动条、最大化填充、指标区空列、图表溢出及切页/失焦后的布局问题均已通过验收。
- DEV-66、DEV-77 关闭当前人工阻塞；相关代码、自动化和 release 证据保留在本文件及归档中。后续人工验收继续执行其它页面，不重新打开这两项。

## 2026-09-23 概览统计人工反馈

- 新登记 **DEV-78**：标签统计需要增加“无标签”，并保证分类总数等于技能库总数；部署关系切换到“项目”维度时，Agent 的 Skill 目录不得被计入项目。
- 当前代码事实：`tag_chart()` 通过 `tags JOIN skill_tags` 只统计有标签关联的 Skill；无标签 Skill 不会返回，多标签 Skill 会按标签关联次数重复计数，因此总和不具备 `skill_count` 不变量。`deployment_chart(Project)` 以 `COALESCE(t.project_id, 'project:none')` 分组，Agent 目标的 `project_id IS NULL` 行被错误纳入项目图表。
- 建议方案：后端输出可验证的互斥标签统计，补充稳定的“无标签”类别，并以技能总数断言分类和为 `skill_count`；项目维度只查询 `project_id IS NOT NULL` 的目标，Agent 维度保持按 `agent_id` 统计。补齐多标签、无标签、仅 Agent、仅项目及混合目标测试后，再构建 release 人工复验。

## 2026-09-23 概览标签展示反馈与 DEV-84 实现

- 新登记 **DEV-84**：macOS 窄窗口下标签环形图的标签说明显示不完整；标签数量较多时环形图也不适合继续承载全部分类。
- 用户裁定：该区域只展示“标签 → Skill 数量”，不重复展示 Skill 总数；点击具体标签继续直接跳转到对应标签筛选列表；一个 Skill 可以同时计入多个标签。
- 已实现：`tag_chart()` 按真实 `skill_tags` 关系分组，一个 Skill 的多个标签分别计数；未加标签的 Skill 不生成“无标签”分类。前端移除环形图，改为按数量降序的可点击横向条形列表；标签很多时只在列表区域滚动，不裁剪或合并标签。
- 自动化：Rust bootstrap 标签测试 13/13，概览页面/预览测试 32/32，lint、typecheck 通过；macOS/Windows release 的真实窄窗口和大量标签视觉验收待执行。

## 2026-09-23 技能图谱人工反馈

- 新登记 **DEV-79**：图谱默认节点和边距不适合阅读；缩放后 Agent 类型徽标未同步收缩；画布拖动会选中文本；用户调整的缩放、平移和节点位置在切页或切换 Skill 后未完整保留。
- 当前代码事实：`forceLayout.ts` 使用固定估算节点尺寸 `192×48`、最小节点间距 `12` 和按密度收缩的弹簧长度，`SkillGraphCanvas` 的节点 CSS 仍为 `max-width: 12rem`，AgentIdentity 内部徽标没有图谱专用缩放约束；画布仅设置 `cursor`，未设置 `user-select: none` 或拖动期间的选择抑制。
- 状态事实：图谱只通过 `useRelationshipsReturnState` 保存 `viewport`，且按 React Router `location.key` 隔离；节点拖拽的 `dragPositions` 和 `display` 设置未保存，普通离开页面也不会统一保存当前状态，因此返回同一 Skill 或切换 Skill 后会恢复默认布局。
- 建议方案：按真实画布尺寸和节点类型制定更小的节点/徽标尺寸与更长的最小边长，保证边标签位于可读区；拖动手势增加 `user-select: none`、指针捕获和拖动态选择抑制；将每个 Skill 的 viewport、节点位置和显示设置按稳定 SkillId 持久化并在返回时恢复，切换 Skill 时隔离状态。补充节点/边可读性、多视口缩放、拖动不选文案及跨页/跨 Skill 恢复测试。

## 2026-09-23 技能图谱第二轮人工反馈

- 新登记 **DEV-83**：DEV-79 的节点重叠/关系线遮挡已通过人工确认，但节点较少时默认视图仍被放大，用户要求至少缩小一半；拖动节点时出现被描述为空白遮挡区域的视觉异常。
- 问题分类：**DEV-79 已修复后的边界遗漏**，不回退已通过的节点/边不压叠结论；“空白区域”暂不能仅凭截图判定为真实遮罩，需要按拖动前、拖动中、释放后的 DOM/画布现场复现。
- 当前代码事实：`SkillGraphCanvas.fitToContent` 只用节点中心坐标包围盒计算缩放，未把真实 DOM 节点宽高、边标签和节点外间距计入；`forceLayout.ts` 仍使用固定估算尺寸与固定间距，节点数少时内容包围盒偏小，fit-view 会得到偏大的 zoom。节点 CSS 仍固定为最多 `8.5rem`，不会按节点数量自动缩放。
- 当前代码事实：节点拖动的每个 `pointermove` 都调用 `setDragPositions`，随后重新运行完整 `computeForceLayout`；被拖节点固定，其他未固定节点会在拖动过程中重新松弛并改变位置。画布只设置 `overflow: hidden`、指针捕获和节点 `user-select: none`，没有独立拖动态、拖动层或拖动期间冻结其余节点的机制；源码中没有额外的白色遮罩元素。因此截图中的空白更可能是拖动时布局/变换重算导致的内容移动或画布裁剪现象，需复现确认。
- 建议方案：新增低节点数 fit-view 失败用例，按实际节点几何和边标签净空计算缩放，并为少节点图谱设置至少 50% 的默认节点/徽标视觉尺寸下限；拖动时冻结非目标节点坐标，仅更新目标节点并用动画帧合并更新，增加明确的 `is-dragging`/`touch-action`/选择抑制样式。补充拖动前中后 DOM 矩形、画布边界和节点位置断言，确认无遮罩、无裁剪、无旧画面残留。

## 2026-09-23 DEV-83 实现与自动化验证

- 已完成少节点图谱收缩：fit-view 将节点实际估算尺寸纳入内容包围盒，少于等于 3 个节点时使用紧凑节点尺寸并限制默认缩放；画布节点、Agent 品牌徽标和类型徽标同步采用紧凑样式，避免节点很少时视觉过大。
- 已完成拖动稳定性修复：拖动开始时冻结当前布局快照，只更新被拖节点，避免每次移动重新松弛其它节点；画布与节点增加 `touch-action: none`、`user-select: none`、拖动态和取消事件，降低空白遮挡、旧画面残留、裁剪和文字选中风险。
- 自动化验证：关系图谱定向测试 **52/52**、前端 lint、typecheck、生产构建通过；Tauri 无打包 release 已重新生成 `target/release/skillhub-desktop.exe`。全量 Vitest 仍有仓库既有的 BrandTag、RelationshipGovernance、DirectoryMatrix、SkillQuickDrawer 和 router 测试失败，未归因于本次图谱改动，不能记为全量全绿。
- 当前状态：代码完成，DEV-83 与 DEV-79 的图谱人工判据均待用户基于本次 release 复验；本记录不将自动化通过写成人工通过。

- 回查 2026-09-19 归档 R-10，确认这是**旧问题回归**：导入前的安全拦截或输入无效没有写入集中库残留，却被操作日志记为“需要恢复”，从而在下次启动时错误阻断全部页面。
- 提交 `dfa4839f` 只在导入清理失败、可能残留文件时记录“需要恢复”；安全拦截和输入无效保留操作记录但记为“已撤销”。`cargo test -p skillhub-application` 全量通过；新版 macOS release 已于 `2026-09-23 00:28:15 +0800` 重建，人工复验待执行。

## 2026-09-23 技能部署流程 Agent 展示人工反馈

- 新登记 **DEV-80**：从技能抽屉点击“添加”进入技能部署流程后，目标选择列表仍展示 `agent-skills.shared-directory`、`openai.codex-cli`、`openai.codex-ide`、`anthropic.claude-code`、`google.gemini-cli` 等技术标识，没有统一的品牌 logo + 用户可理解类型徽标。
- 问题分类：**DEV-73/75 已修复后的边界消费点遗漏**，不重开原编号，也不影响已完成的统一展示实现结论。
- 当前代码事实：`DeploymentDialog.tsx` 和 `BatchDeploymentPage.tsx` 的目标选择、计划预览直接渲染 `target.label`；`DeploymentTarget` 仅有 `id/label/path/available/physicalId/modes`，没有品牌、类型、共享目录语义字段，因此该流程绕过了 `AgentPresentation`。部署预览/结果链路也继续传递裸 `label`，需一并检查，不能只修选择页。
- 建议方案：为部署目标建立统一的 Agent 展示投影，补齐结构化品牌、展示类型、共享目录和实例信息，并让单项/批量部署的选择、预览、提交结果及无障碍名称全部复用 `AgentPresentation`；共享目录按独立“共享目录”呈现，技术 ID 仅在折叠技术详情中保留。
- 自动化与人工验收：当前尚无 DEV-80 专项测试；后续补单项和批量部署渲染测试、技能抽屉到部署流程 E2E，人工确认所有目标主展示均为品牌 + 类型/共享目录，预览、提交、结果和键盘无障碍名称口径一致。

## 2026-09-23 部署预览影响展示与错误反馈

- 新登记 **DEV-85**：部署向导的“预览影响”原先只展示目标和技术字段；预览失败时还可能把 `deployment.no_current_version` 这类内部错误码直接展示给用户，批量结果也缺少清晰的影响摘要。
- 问题分类：**部署流程前端呈现与后端事实投影遗漏**，不是软连接、目录联接或托管复制后端能力缺失。后端已有只读规划、冲突/警告和写入验证，前端没有把目标目录、入口路径、实际操作和“源 Skill 不修改”完整呈现出来。
- 已完成：单项/批量预览统一使用影响卡片，展示品牌/类型目标、部署方式、目标目录、Skill 入口、创建或无需写入的影响说明；技术 ID 和实现细节折叠到技术详情；错误码统一映射为用户可读原因；批量预览增加可添加/被阻止摘要，提交后应用内通知包含成功、失败、跳过数量；颜色全部使用主题语义变量。
- 本轮边界修复：后端会同时返回 Skill 级和目标级模式告警，且符号链接/目录联接不可用会映射到同一用户文案；前端现在按目标去重并过滤已被卡片覆盖的 Skill 级告警，`托管复制` 等实现事实只保留在技术详情，不再出现重复行。
- 自动化验证：部署相关定向测试 5 个文件 / 91 项通过，native error（原生错误）映射、单项/批量预览、结果与原生 API 投影均有覆盖；lint、typecheck、前端生产构建通过。真实 macOS/Windows release 的单项、批量、失败提示和主题切换仍待人工复验。

## 2026-09-23 Agent 链接部署能力复查

- 新登记 **DEV-86**：Agent profile（能力配置档案）中多个本地技能目录仍登记为不支持符号链接，需要复核“官方明确支持”与“仅能发现技能目录”的边界。
- 复查结论：本轮以 skills.sh 的 `npx skills` 安装流程和 supported agents（支持的 Agent）清单为唯一判据。该流程默认使用符号链接，无法创建时才回退复制；因此已统一开放清单内且有本地目标的 Agent：共享 Agent Skills、Claude Code、Cline、CodeBuddy Code、Codex、Cursor、GitHub Copilot、Gemini CLI、Antigravity 应用/CLI、Grok Build、Hermes、Kimi Code、OpenClaw、OpenCode、Pi、Qoder/Lingma、Trae、Windsurf、ZCode。
- 边界结论：WorkBuddy、DeepSeek Harness、Comate、Antigravity IDE/SDK 不在 skills.sh supported agents 清单内，继续保持复制部署，不因目录可发现或品牌相似而放开链接部署。
- 自动化：新增 skills.sh 支持清单正向回归及 WorkBuddy、DeepSeek Harness、Comate、Antigravity IDE/SDK 负向回归；`cargo test -p skillhub-adapters`、`cargo test -p skillhub-core --test deployment_staging`、定向 rustfmt 和 `git diff --check` 均通过。无打包 release 已于 `2026-09-23 23:54` 重建并启动（PID 50175），真实各 Agent 链接落地、刷新、解除和失败回退仍需按人工流程 E/H 复验。

## 当前结论

- **代码完成**：DEV-2—65 的当前实现已完成；本轮包含导入冲突优先顺序、抽屉元数据快照刷新、图谱画布/图例/技术细节、Agent 品牌与类型展示、关系呈现和测试契约收口。
- **自动化完成**：Vitest 169 文件 / 1845 测试、Playwright 396/396、`cargo test --workspace`、前端 lint、typecheck、前端构建和 Tauri 无打包构建均通过。
- **外部阻塞**：DEV-1 需要 macOS 第二个真实可写卷；DEV-8 需要可信的官方 WorkBuddy 绿色 logo 素材。
- **人工验收进行中**：Windows/macOS release 真实操作链、截图、文件保护、权限、链接和跨卷证据按《人工验收清单-2026-09-18.md》继续执行。上一轮 DEV-2—65 的代码与自动化基线不因本轮新问题回退；本轮概览布局问题单独登记为 DEV-66。

## 2026-09-23 Agent 展示全项目复查

- 新登记 **DEV-81**：技能列表“已添加到 Agent”、技能抽屉关联关系以及若干关系/移除/项目选择界面仍未统一为品牌 + 用户可理解类型。
- 问题分类：**DEV-73/75/80 相关的全项目消费点与数据投影遗漏**，不重开已完成编号；DEV-80 继续只追踪部署流程目标选择链路。
- 当前代码事实：`SkillLibraryPage` 的 `SkillMatrix` 直接使用 `DeploymentTarget.label`；技能列表目标映射在 `skills/nativeApi.ts` 中把 `target.physical_id` 写入 `AgentDeployment.id`，技能抽屉关系又把部署记录的 `record.id` 写入 `AgentDeployment.id`，导致 `AgentDeploymentIcons` 即使调用 `AgentPresentation` 也拿不到真实 Agent 身份。技能详情 `RelationsPanel`、移除影响弹窗、项目 Agent 关联/注册选择仍直接渲染 `label`。
- 建议方案：建立统一的结构化 Agent/部署目标展示投影，明确区分逻辑目标 id、物理目录 id、部署关系 id、`agent_client_id` 和项目目标；所有 Agent 主展示统一复用 `AgentPresentation`，共享目录使用独立语义，物理/关系技术标识只进技术详情。完成全项目消费点审查，避免仅替换 JSX 组件但继续传错身份。
- 自动化与人工验收：当前尚无 DEV-81 专项测试；后续补技能列表、抽屉、矩阵、详情关系、移除影响、项目选择的渲染测试及全项目 Agent 展示回归，人工逐页确认品牌 logo、类型徽标、共享目录和无裸技术 ID。

## 当前待办

| 范围 | 当前状态 | 下一步 |
|---|---|---|
| DEV-2—65 | 代码与自动化完成，人工验收进行中 | 继续按人工清单 A—H 执行；上一轮通过项和不需要人工验收的内容只读归档，不重新打开。 |
| DEV-66 | 代码、自动化和概览人工验收已完成 | 最大化使用原生窗口状态和 fill 高度轨道；窗口化保留自然页面流。该项已关闭，后续不因其它页面问题重新打开。 |
| DEV-77 | 代码、自动化和概览人工验收已完成 | 4 列指标轨道、图表容器 `ResizeObserver` 和低矮窗口明细布局已通过人工复验；该项已关闭。 |
| DEV-78 | **人工验收通过，关闭**：标签统计补“无标签”且总数守恒；项目维度与 Agent 目录互斥 | storage bootstrap 13/13、前端 build 通过，已获 Windows release 人工通过 | 归入已完成摘要；除回归外不再重新打开。 |
| DEV-79 | 已完成代码修复：图谱状态按稳定 `SkillId` 保存，节点宽度/路径截断/边标签净空重算，图谱 Agent 改紧凑徽标 | 图谱页面、画布、布局、回退状态定向测试通过；lint、typecheck、生产构建和无打包 release 通过 | 用新版 release 复验节点/边说明不遮挡、图标不越界、切页及切换 Skill 返回后恢复各自缩放/位置/显示设置。 |
| DEV-80 | 已完成代码修复：按共享目录 `physical_id` 归并为单一选择目标，关联品牌下发至统一展示组件 | 应用层 facade 回归、绑定生成、部署前端定向测试、typecheck、生产构建和无打包 release 通过 | 验证同一路径仅出现一个“共享”目标，关联品牌可见，单项/批量预览和提交不回退。 |
| DEV-81 | 已完成代码修复：`AgentPresentation` 增加 compact 模式；紧凑列表仅显示品牌图标，tooltip/无障碍名称保留品牌+类型 | 统一展示、技能列表和部署定向测试通过；lint、typecheck、生产构建和无打包 release 通过 | 逐页验证紧凑列无裁切，悬停可读完整语义；详情与普通卡片仍显示完整品牌+类型。 |
| DEV-82 | 已完成并人工验收通过：部署目标改为专用紧凑响应式卡片网格，品牌+类型不再被宽泛 `span` 规则拉伸为整条色带；路径单行截断并保留完整悬停值。 | 部署页 22 项定向测试、lint、typecheck、生产构建和新版 release 人工复验通过。保留既有勾选、`?target=` 预选、预览、项目展开和不可用目标过滤逻辑。 | 无。 |
| DEV-83 | **代码已完成，待人工复验。**少节点图谱使用紧凑节点/徽标并按实际节点几何 fit-view；拖动期间冻结非目标布局并抑制选择/裁剪异常。 | 图谱定向测试 52/52、lint、typecheck、生产构建和 Tauri 无打包 release 通过；全量 Vitest 仍有既有非本次改动失败。 | 用新版 release 复验少节点尺寸、关系线、拖动前中后画布边界及非目标节点稳定性。 |
| DEV-84 | **代码已完成，待人工复验。**标签统计改为多标签分别计数；概览标签区改为可滚动横向条形列表，保留标签筛选跳转，不展示 Skill 总数。 | bootstrap 标签测试 13/13、概览定向测试 32/32、lint、typecheck 通过。 | 用 macOS/Windows release 验证窄窗口、长标签名、超过 6 个标签和滚动区域无裁剪。 |
| DEV-85 | **代码已完成，待人工复验。**部署预览展示实际影响、目标目录和入口路径；批量摘要、失败原因及应用内结果通知改为用户可理解文案，技术标识折叠展示。 | 部署定向测试 5 文件 / 91 项、lint、typecheck、前端生产构建通过。 | 用新版 release 验证单项/批量预览、预览失败、链接/复制部署、主题切换和应用内通知。 |
| DEV-86 | **已完成配置修正：按 skills.sh `npx skills` 支持清单统一开放链接部署；WorkBuddy、DeepSeek Harness、Comate、Antigravity IDE/SDK 保持复制部署。** | `builtin_profiles` 正向/负向链接能力回归、适配器完整测试、core 部署能力测试通过；无打包 release 已于 `2026-09-23 23:54` 重建并启动。 | 用真实支持清单中的代表 Agent 复验链接入口、内容刷新、解除部署和失败回退；同时确认上述负向边界不会出现链接选项。 |
| DEV-67—72 | 代码与自动化已完成（当前标题去重修正合并至 `b9cb7c30`）；人工复验待执行 | 图谱标题/图例/布局/控件/高度/工具栏问题均已按顺序修复；关系布局 5/5、AppShell 21/21、lint、typecheck、build 及 Tauri 无打包构建通过。下一步用 `b9cb7c30` release 进行图谱多视口人工复验。 |
| DEV-73 | 代码与自动化已完成（合并提交 `79b7772f`）；人工复验待执行 | 已建立统一 `AgentPresentation`，覆盖 Agent 列表、详情、发现、部署、技能关联、治理、图谱和 onboarding；全量 1864 测试、lint、typecheck、build 通过。下一步用新 release 复验全部展示面。 |
| DEV-76（R-10 导入恢复门禁回归） | 代码与自动化已完成（合并 `dfa4839f`）；人工复验待执行 | 无写入的安全拦截/输入无效不再生成恢复候选；应用层全量测试和操作日志定向测试通过。下一步按流程 C 验证失败后重启仍可正常进入应用，成功导入保持可用。 |
| DEV-1 | 外部阻塞 | 在 macOS 具备第二个可写卷后执行跨卷写入和回滚验收。 |
| DEV-8 | 外部阻塞 | 获得官方或可信授权的 WorkBuddy 绿色 logo 素材后再验收，不自绘替代。 |
| `cargo fmt --all -- --check` | 工具版本边界 | 当前仍会报告既有未触碰文件的 rustfmt 差异；不进行无关全仓重排，继续记录为已知边界。 |

| DEV-74（关联 DEV-49） | 代码与自动化已完成（合并提交 `f856fb23`）；仓库发现配置区和仓库管理页已接入共享 `RepositoryCard`，人工复验待基于新 release 执行。 | 已完成共享卡片投影、两页接入及 targeted tests 29/29；lint、typecheck、前端 build 通过。下一步重建并启动 release，人工验证卡片布局、操作和多视口。 |
| DEV-75（细化 DEV-73） | 代码与自动化已完成（合并提交 `66b52489`）；用户可见 Agent 类型已从 CLI/ACP 统一改为终端/协议接入。 | 已覆盖 AgentKindBadge、本机发现、部署/关系身份、初始化兼容性及中英文词条；下一步仅需基于新 release 进行人工复验。 |

| DEV-75（细化 DEV-73） | `AgentKindBadge`、发现工作台、部署/关系身份、初始化兼容性已统一使用“终端/协议接入”；中文/英文词条、组合徽标、`aria-label` 和说明 title 已补齐。 | 合并 `66b52489`；针对性 Vitest 62/62、lint、typecheck、前端 build 和 Tauri release build 均通过。 | 用户可见主展示不出现 CLI/ACP 裸缩写；多类型显示为“桌面端/终端”等可理解组合；待用户基于新 release 人工复验。 |
| DEV-55（旧问题未修复） | 提交 `fa4c2ee2` 已移除导入前关系治理步骤和强制决定；完成页提供“现在整理 Agent 副本 / 稍后再说”，独立治理页继续承担单项与批量链接整理。 | 应用层 59/59、四个前端定向文件 115/115、lint、typecheck、build、Tauri 无打包构建通过；默认并发全量 Vitest 的既有 `libraryViewContext` 测试隔离错误尚未形成全绿证据。 | 流程 C 验证原目录保持不动和完成页选择；流程 E 验证目录分类、链接整理、失败恢复和回滚。 |

## 已完成摘要

- 导入、冲突、关系治理、远程来源、安全检查、更新检测、部署规划、共享目录和图谱相关实现已有对应自动化覆盖。
- macOS 当前验证产物为 `target/release/skillhub-desktop`，已于 `2026-09-23 23:54 +0800` 从源码 HEAD `92386944` 重建并启动（PID 50175）；Windows 对应产物为 `target/release/skillhub-desktop.exe`。人工验收仍须记录实际平台、窗口尺寸和证据。
- 近期收口提交包括：`d08a54c1`、`c299d5a2`、`61aa5e29`、`f3a3bd42`、`ff1905a0`、`0e253cb7`、`a12b5a14`、`cecb2ab4`、`991bec51`、`49cf60ec`、`43f93dfe`。

## 下一轮开发代办

| 编号 | 新问题与代码事实 | 建议更改方案 | 完成判据 |
|---|---|---|---|
| DEV-66（第二次人工复验失败） | `56rem` 修复虽然消除了滚动，却把所有低高度窗口误判为最大化，极度压缩模块；三大区块 `flex:0 0 auto` 与顶对齐 PageFrame 又使剩余高度闲置。截图已确认最大化、窗口化均出现大面积空白。 | 不再调纯 CSS 高度阈值。接入 Tauri 可测试的 `maximized` 状态：仅最大化时采用 fill 轨道并把剩余高度分给图表/右栏；窗口化保留自然内容排列与页面滚动。补“最大化填满 + 无滚动”和“窗口化自然流 + 不误压缩”失败/回归用例。 | 最大化时所有模块完整、无压叠/裁切/溢出/滚动，且内容区有效填满 PageFrame；窗口化恢复可读的自然排列，页面滚动可用且无强制压缩；Windows/macOS release 分别人工通过。 |
| DEV-67—72 | 已修复并合并至 `47e3c7d9`：标题/页签合并、图例悬浮、画布自适应、控件真实生效、关系页 fill 高度和工具栏收纳均已完成。 | 定向关系测试、全量 Vitest、Playwright 关系回归、lint、typecheck、build 已通过；只剩 release 多视口人工复验。 | 图谱各项人工判据通过后关闭对应 DEV。 |
| DEV-73 | 已修复并合并 `79b7772f`：统一 `AgentPresentation` 覆盖所有主要 Agent 消费点，支持共享目录、同品牌多类型合并、未知类型和无障碍标签。 | 全量 170 文件/1864 测试、lint、typecheck、build 通过；只剩 release 全项目人工复验。 | 所有 Agent 主展示不出现裸技术 ID，品牌和类型一致；人工复验通过后关闭 DEV-73。 |
| DEV-76（R-10 导入恢复门禁回归） | 已修复并合并 `dfa4839f`：仅在导入清理失败且可能有文件残留时进入恢复；无写入的安全拦截/输入无效为已撤销。 | 应用层全量测试通过，门禁拦截的公共外观测试断言无恢复候选；待 release 重启人工复验。 | 失败导入不再阻断后续启动；真正残留仍保留恢复入口；成功导入不受影响。 |

| DEV-74（关联 DEV-49） | 历史方案已要求仓库发现和仓库管理复用卡片式呈现；当前实现已由 `f856fb23` 接入共享 `RepositoryCard`/view model，保留仓库坐标、分支、状态、扫描事实和操作。 | 已完成共享组件、响应式卡片布局、两页接入及渲染/操作测试；后续只做 release 重建和人工复验，不再重复开发。 | 两页面均以一致仓库卡片展示；所有启停/刷新/移除/扫描/重试/添加动作保持；窄屏无溢出，人工复验通过。 |
| DEV-78 | **人工通过，关闭。**标签互斥归类、“无标签”和 Agent/项目统计隔离均已验收。 | 无后续开发；仅在回归时重开。 | 已达成。 |
| DEV-79 | **旧问题未完全修复。**文字拖选已通过；当前 `location.key` 作为存储键导致不能按 Skill 恢复，布局的固定估算尺寸也未涵盖实际长路径和完整品牌徽标，关系说明仍被覆盖。 | 用稳定 `SkillId` 而非 history entry 保存 viewport、节点坐标和显示设置；基于渲染约束收紧节点/路径截断并扩大边标签净空；提供可随 zoom 收缩的图谱专用徽标。 | 节点、徽标和边说明互不遮挡；切页/切换 Skill 后各自视图恢复；拖动仍不选文字。 |

| DEV-80 | **旧问题仍有边界遗漏，已于本轮继续修复。**此前仅把 `shared_reference=true` 的引用归并，遗漏了同物理目录的“共享目录自身”归属条目，因此本体与引用组仍会并列。 | 现在先按 `physical_id` 收集全部逻辑目标；只要组内存在共享引用，即以 `agent-skills` 共享目录条目作为唯一可选目标，并把非 `agent-skills` 厂商品牌去重为关联信息。失败回归已覆盖“本体 + 两个引用”只返回一项。 | 同一路径只显示一次“共享”目标；可识别厂商作为关联类型，单项、批量、预览、结果一致。 |
| DEV-81 | **已修复后的紧凑布局遗漏。**表格/密集列表把完整 `AgentPresentation` 放进受限列，导致品牌/类型被裁切。 | 为统一组件增加显式 compact 变体，只显示小品牌图标；`title`、`aria-label` 和悬停说明呈现品牌与平台类型。仅在宽度受限的列表采用，详情保留完整组合。 | 紧凑列表无裁切，鼠标悬停和辅助技术可得完整语义；正常页继续显示品牌+类型。 |

## 继续工作顺序

1. 先按《人工验收清单》准备测试素材和 release 产物记录。
2. 依次执行流程 A—G；每条完成后立即写回《功能完成度与验收状态矩阵》。
3. 在具备对应平台和素材后执行流程 H，并区分“通过、未通过、阻塞、不适用”。
4. 发现问题时先回查归档证据，再判断新问题、未修复、回归或边界遗漏；不要覆盖历史记录。
