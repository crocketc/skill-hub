# 人工验收清单（2026-09-26）

> 最新状态：用户已确认通知玻璃视觉、文字选中高亮和概览 Agent 品牌柱条通过。仅这些视觉判据通过，人工验收流程 A—H 仍按清单逐项执行。当前标准产物 target/release/skillhub-desktop.exe；2026-09-26 16:58:02 +08:00；SHA-256 09D330B1913141F15291E226A26A7E9DDE6DDA106FBE354F9206DD35BEE8CD48。签名构建和前端静态检查通过；本轮未运行测试。

> 上一版记录：按参考图改为固定中性灰半透明玻璃（rgba(32,32,32,0.32) + blur(24px)），不使用主题色填充。多条通知折叠时点击卡片或按回车展开，保持未读状态；铃铛切换及 280ms 水平进出动效保留，仅由 SkillHub 减少动效设置控制。通知单测 31/31、定向 E2E 2/2、lint/typecheck 与签名 Tauri release 构建通过；Windows release 人工视觉待复验。历史产物：`target/release/skillhub-desktop.exe`；构建时间 2026-09-26 16:43:40 +08:00；34,387,968 bytes；SHA-256 `89A3A20C4F6B7FBE9546181F1B65283D807B9E7C743C2AE232BED2B4CAF5A3BB`；源码基线 HEAD `97f75054e88c016440c345f50e89395518e09de4` 加本轮修正。

更新时间：2026-09-26（**当前执行版**）。本文件是人工验收的唯一当前入口，只保留尚未完成的 Windows/macOS release 人工验收流程；已完成项（DEV-66、DEV-77、DEV-78、DEV-82 及概览相关判据）不再重复执行。上一版清单与历史现场结论已归档至 `docs/development/archive/2026-09-24-import-deployment-relationship-governance/`；9 月 24 日执行版归档至 `docs/development/archive/2026-09-26-notification-popover/`。

> **状态边界**：未完成真实操作、截图和日志复核，不得写"人工通过"；自动化通过只能记录为"自动化通过，人工待验收"。DEV-1 需要 macOS 第二个真实可写卷；DEV-8 需要可信的官方 WorkBuddy 绿色 logo 素材。

## 被测产物与记录规则

| 项 | 当前要求 |
|---|---|
| 被测产物 | 从最终 HEAD 重建的无打包 release：Windows `target/release/skillhub-desktop.exe`、macOS `target/release/skillhub-desktop`。若标准产物被运行中的程序占用，可用隔离 target-dir 构建。逐条记录平台、系统版本、窗口尺寸（含 OS 缩放后实际 client area）、语言、主题、产物构建时间与源码 HEAD。 |
| 当前 Windows 修复产物 | target/release/skillhub-desktop.exe；2026-09-26 16:58:02 +08:00；SHA-256 09D330B1913141F15291E226A26A7E9DDE6DDA106FBE354F9206DD35BEE8CD48；签名构建及静态检查通过。通知视觉、文字选择与 Agent 品牌柱条已获用户视觉验收；其他流程仍待验收。 |
| 证据记录 | 每条流程填写"结果、截图/日志证据、现场清理"；失败保留现场，不先清理再补写。证据不得包含密钥、个人 Skill 内容或个人绝对路径。 |
| 反馈分类 | 新问题、旧问题未修复、旧问题回归、已修复后的边界遗漏；登记前先回查归档与当前源码/测试事实。 |

## 前置准备与复原

| 编号 | 前置素材 | 准备方式 | 复原方式 |
|---|---|---|---|
| 准-A | 目标占用素材（DEV-18） | 在测试用 Agent 目录下创建与待部署 Skill 同名的目录和普通文件。 | 删除测试目录，确认原有条目未改变。 |
| 准-B | 同名不同内容冲突素材（DEV-54/58） | 复制一个测试 Skill，改动副本 `SKILL.md` 一行，放入测试扫描目录并重新扫描。 | 删除副本并重新扫描，确认计数恢复。 |
| 准-C | 远程来源素材（DEV-53） | 准备允许的 HTTPS 归档、允许的 Git 仓库，以及不可达、非 HTTPS、重定向、超预算和恶意归档样本。 | 删除下载、解包和测试库内容；保留失败日志引用。 |
| 准-D | 关系图与共享目录素材（DEV-59/60） | 准备多个 Skill、别名、长路径、不同 Agent 类型和同一共享目录被多个 Agent 识别的测试事实。 | 删除测试关系和测试部署，重新扫描确认源目录未被修改。 |
| 准-E | 元数据与安全素材（DEV-26—29、DEV-61/62） | 准备多标签、中英文逗号、长用途、翻译描述、上游更新、基础安全警告/高风险和无结果 Skill。 | 恢复原元数据；安全结果按产品允许的清理规则处理并记录。 |
| 准-F | 来源治理素材（本轮新增，DEV-87） | 一个已导入且保留来源副本的测试 Skill；一个可建立链接的 Agent 目录；一个权限受限（只读）目录；一条人为改名/移除的来源路径。 | 解除链接、恢复来源路径、删除测试副本，重新扫描确认计数恢复。 |

## 当前人工执行流程

每条流程合并可在同一操作链路中验证的多个验收项；"待验收"是初始状态，不代表通过。

| 流程 | 覆盖项 | 前置素材 | 操作步骤 | 通过标准 | 结果 | 截图/日志证据 | 现场清理 |
|---|---|---|---|---|---|---|---|
| A. 产物、启动与基础可用性 | DEV-2—7、10、14、17、20、23、25、30、33、37、38、41、42、49、64/65 | release 产物；标准窗口 800×600、1280×900、1440×900；记录 OS 缩放后实际 client area；键盘 | 启动后最大化打开概览，切页返回、失焦回前台、还原窗口；依次打开发现、技能库、详情、关系三页、仓库发现/管理、设置；切换中英文与主题；键盘完成导航、搜索、页签、抽屉、视图切换。 | 应用可启动；无根级横向溢出；其余入口清楚、焦点可见；主展示无裸技术 ID。 | 待验收（概览专项 DEV-66/77/78/82 已通过关闭，不再重复） | 记录启动、主题、键盘焦点证据。 | 关闭应用；清理测试数据。 |
| B. 本地来源、手动来源与远程获取 | DEV-50—53 | 准-C；本地测试目录；`npx skills add` 文本 | 解析本地路径、URL、Git 和 `npx`；本地路径进入导入向导；远程来源分别执行成功、失败、取消和重试。 | 只解析不执行命令；来源和重定向受策略限制；下载/解包有预算和路径保护；失败不写半成品且可重试。 | 待验收 | 保存各来源类型、取消/重试与失败清理证据。 | 删除测试下载与临时工作区。 |
| C. 导入安全、冲突优先与来源治理入口 | DEV-18、20、48、54—58、76、**DEV-87（本轮）** | 准-A、准-B；安全候选；准-F | 执行导入全流程：基础安全检查→冲突分析→只处理必须的内容冲突→提交→完成页确认"整理来源副本/稍后再说"两种选择；**导入后核对来源副本未被改动（逐字节/时间戳）**；打开治理待办深链与"整理来源副本"入口；对一次确定性安全拦截和一次输入无效提交关闭并重启应用。 | 基础安全未通过不可提交；无写入的拦截/输入无效不出现恢复闸门；导入不插入治理步骤且只复制到集中库；完成页两种选择可达；治理待办计数与"需处理"口径一致（需校验 + 需关注 + 已阻止）；重启后打开批次事实可查询、来源副本保持不变。 | 待验收 | 保存安全结果、冲突决策、完成页选择、来源副本前后对照、深链落地页与重启后状态。 | 删除准-A/B/F 素材与导入副本；重新扫描确认计数恢复。 |
| D. 元数据、翻译、调用策略、运行要求与更新 | DEV-26—29、46/47、65 | 准-E；有上游和无上游 Skill | 在列表、抽屉、完整详情三处编辑别名、用途、标签；翻译描述；查看调用策略与运行要求只读字段；执行上游检查。 | 三处读值一致；翻译有进行中/失败/取消/确认边界；调用方式只读、无编辑入口；更新状态不伪造。 | 待验收 | 保存三处对照与翻译状态证据。 | 恢复原元数据；清除筛选。 |
| E. 部署预览分组、关系治理、通知与回滚 | DEV-21—23、40—43、46、55、59/60、63、86、**DEV-88、DEV-92** | 准-D、准-F；skills.sh 支持清单可链接 Agent；只读目录；同名 Skill 两份 | 进入批量部署：核对四类处置分组（可执行/建议复制/无变化/被阻止）；确认被阻止 pair 不阻塞其余 pair 提交；对建议复制组确认回退后重新预览；单项取消勾选后重新生成预览；制造目标占用与来源路径缺失观察预览阻断；完成治理确认、刷新、解除与失败回退；核对负向 Agent 仅提供复制部署。由真实操作产生通知后，确认浮层贴齐顶栏下缘与窗口右侧、底色半透并模糊背景、最新通知下方最多两层错位卡片。保持 Windows“动画效果”关闭，确认 SkillHub 自己的“始终减少动效”关闭时仍从右向左滑入、向右滑出；打开 SkillHub 开关确认即时开合；关闭后确认滑动恢复。 |预览摘要和治理结果正确；支持清单 Agent 链接落地，负向 Agent 无链接选项；通知外框使用固定中性灰 16% 半透明底层与 6px 背景模糊，不混入主题色；卡片层叠在最新通知下方；点击多条通知的折叠卡片展开列表且保持未读，铃铛再次点击滑出关闭；动效只由 SkillHub 设置开关控制：关闭时左右滑动、开启时即时开合，即使 Windows 系统偏好为 reduce 也符合上述行为。 | 待验收（DEV-92 中性灰玻璃与堆叠交互新版 release 待人工复验） | 保存部署预览、回滚、消息浮层开/关截图；记录窗口尺寸、缩放、主题、Windows 系统动效偏好和 SkillHub 减少动效开关状态。用户原反馈截图仅作为问题来源，不代表修复后证据。 | 解除测试部署；恢复目标目录；重新扫描确认源目录未修改。 
| F. 安全检查与操作记录 | DEV-48、61/62、65 | 准-E | 从抽屉、详情和安全页查看基础/AI 状态；AI 运行中取消；核对顶栏任务、操作记录与列表两列。 | 抽屉操作在详情可达；AI 未配置不替代基础门禁；状态列与结果列分离。 | 待验收 | 保存运行、取消与列表对照证据。 | 清理测试任务与结果。 |
| G. 图谱与全项目呈现一致性 | DEV-11、15、24、31、34—39、44/45、60、64、79、83 | 准-D；长路径；800×600 和宽屏 | 搜索与拖拽/缩放图谱；对照 Agent 列表、详情、发现、部署、治理页的品牌 + 类型呈现；验证切页/切换 Skill 后图谱状态恢复。 | 节点/边与图例一致；Agent 使用品牌+用户视角类型；共享目录拓扑独立呈现；技术标识仅折叠技术细节；图谱状态按 Skill 恢复。 | 待验收 | 保存图谱全景、选中详情、窄屏与焦点证据。 | 清除搜索/筛选；关闭应用。 |
| H. Windows/macOS 平台专项 | DEV-1、4/5、8、14、21、25、30、37、42、**DEV-87/88 平台项** | 两平台 release；macOS 两个真实可写卷；真实 Agent 目录 | Windows：目录联接创建/删除、只读目录失败回退、来源清理、长路径；macOS：符号链接、大小写、跨卷写入（DEV-1 双卷）、Dock 图标；两平台分别执行 C/E 代表链路。 | 平台行为一致且符合清单；链接/联接失败时回退到复制需用户确认，不静默改写源文件；未提供素材时记外部阻塞。 | 待验收/待分类 | 每平台独立记录产物、视口、截图、日志。 | 恢复两平台测试目录与部署目标。 |

## 上一轮结转的人工待验收项

| 编号 | 内容 | 状态 | 下一步 |
|---|---|---|---|
| DEV-79/83 | 图谱节点/边可读性、拖动稳定性、按 Skill 状态恢复 | 代码与自动化完成；macOS release 窗口可见后复验 | 流程 G 一并执行 |
| DEV-84 | 概览标签条形列表窄窗口与大量标签 | 待 release 复验 | 流程 A 内核对 |
| DEV-85 | 部署预览影响卡片、失败文案、通知 | 待 release 复验 | 流程 E 内核对 |
| DEV-86 | skills.sh 支持清单链接部署边界 | 待 release 复验 | 流程 E/H 内核对 |
| DEV-74/75 | 仓库卡片、Agent 类型术语 | 待 release 复验 | 流程 B/G 内核对 |
| DEV-76 | 导入恢复门禁回归 | 待 release 复验 | 流程 C 重启场景一并验证 |

## 现场判定与结果写回

1. 每条流程开始前记录平台、系统版本、产物构建时间、源码 HEAD、窗口尺寸、语言、主题和测试数据来源。
2. 发现问题先保留现场，按四类登记，再补步骤、截图、日志和复现条件。
3. 未完成真实复验不得写"人工通过"。
4. 每条流程完成后填写本表"结果、截图/日志证据、现场清理"，再同步《功能完成度与验收状态矩阵-2026-09-26.md》。

## 本轮补充验收记录

| 判据 | 结果 | 证据与边界 | 现场清理 |
| --- | --- | --- | --- |
| DEV-92 通知玻璃视觉（16% 遮罩、6px 模糊） | 通过 | 用户明确反馈“现在可以，非常好”；仅确认该视觉项，不代表流程 E 全部通过 | 无新增清理 |
| 文字选中高亮、概览 Agent 品牌柱条 | 通过 | 用户明确回复“刚调整的这些问题我验收通过了”；记录为用户目视确认。未另行记录 OS、窗口参数与截图证据 | 无新增清理 |

以上局部通过项不等同于流程 A/E 完成；下一步继续流程 A 中未验的基础可用性判据。

本轮文字选择与品牌柱条产物：已由用户确认验收通过。产物时间、签名构建、静态检查及未运行测试等记录见本文件前文。

## I. 统一待办闭环（DEV-95，本轮下一项）

| 覆盖项 | 前置素材 | 操作步骤 | 通过标准 | 结果 | 截图/日志证据 | 现场清理 |
| --- | --- | --- | --- | --- | --- | --- |
| 五类待办来源、计数、跳转与完成后移出 | 测试 Skill 的安全发现及到期试用；一组未决冲突；需校验/关注/受阻关系；隔离测试环境中的恢复候选（没有恢复素材则该项待验，不人为破坏真实数据） | 从概览分类进入待办；核对数量与分类筛选；点“去处理”定位冲突/治理/恢复；实际处理一项后返回，核对待办消失、概览计数减少；试用转常规/安全重查，暂缓后撤销；切回窗口或点击刷新确认状态；恢复闸门触发时先按恢复页确认，完成后回待办核对。 | 同一冲突组/关系/操作各一项；不同问题不按同名技能误合并；正常与保留关系不列入；名称可读、路径规范；失败不报完成或假零；必需决策和恢复不能批量忽略；来源仍未解决时继续显示；概览与列表数量一致。恢复页（DEV-98 补充判据）：候选与摘要显示「本地化操作名：对象名」和本地化时间，不出现 operation id UUID 或原始 kind（如 delete_skill）；缺快照候选显示「未知操作」。 | 待验收（自动化通过） | 记录产物时间/HEAD、OS、窗口/缩放、语言/主题；保存处理前后计数及目标页、失败提示证据；恢复页保存候选列表与摘要截图。 | 恢复测试试用状态和忽略规则；按原治理/恢复流程复原测试目录；失败保留现场。 |

本轮通知玻璃、文字选中与品牌柱条的用户通过结论保持；DEV-95 为新增功能，需要单独执行本流程。

## J. 通知模块归属、删除影响再设计与内部标识退出界面（DEV-96/97/99，本轮新增）

| 覆盖项 | 前置素材 | 操作步骤 | 通过标准 | 结果 | 截图/日志证据 | 现场清理 |
| --- | --- | --- | --- | --- | --- | --- |
| --- | --- | --- | --- | --- | --- | --- |
| 通知卡片功能域徽标、操作结果可读性（DEV-96） | 无特殊素材；触发一次 Agent 重扫、一次部署或导入操作 | 触发 Agent 重扫，打开顶栏通知浮层；核对折叠态最新卡片是否带「发现」徽标与结果说明（如「已扫描 N 个 Agent、M 个部署目标」）；点「查看全部」展开，核对每张卡片都有功能域徽标（技能库/部署/导入/发现/关系治理/系统）；再触发一次批量类操作核对「成功 N、失败 M」摘要。 | 折叠态与展开态卡片均显示模块徽标；徽标文案与展开态分组一致；重扫通知不再只出现孤零零的「重新扫描」；批量操作成功通知带结果计数；失败通知仍可读且带深链。 | 待验收（自动化通过） | 保存折叠态/展开态浮层截图、触发操作前后的通知内容。 | 清空通知历史；撤销验收期间产生的忽略/标记。 |
| 批量删除影响页再设计（DEV-97） | 一个已部署到 Agent 的测试 Skill 和一个未部署的测试 Skill | 在技能库选中已部署 Skill 触发批量删除：核对说明文字只出现一次、部署行显示「品牌 + 类型」徽标（技术 client_id 不出现）、影响维度逐项分行（未知外部引用每条路径一行）；为每行选择处理方式后二次确认提交；再对未部署 Skill 触发删除：核对主按钮文案为「确认删除」而非「强制删除」，提交后从库中移除。 | 影响页能直接回答「删了会影响什么」；Agent 行为品牌+类型呈现；路径逐条可读且风格统一；文案与按钮语义一致（有部署关系→「继续删除」，无→「确认删除」）；取消部署确认标题显示品牌名（如「Claude」）而非 client_id；删除后目标 Agent/目录按所选处置落盘，原文件保留。 | 待验收（自动化通过） | 保存已部署/未部署两种删除影响页截图、提交后 Agent 目录状态证据。 | 恢复测试 Skill 与部署关系（可经备份导入回滚）；清理测试目录。 |
| 内部标识全面退出界面（DEV-99） | 一个可部署到 Agent 的测试 Skill | 走一遍添加 Skill（部署）流程：预览受阻场景时核对阻断原因是用户可读文案；展开「技术详情」核对只出现部署方式/回退方式/实现警告等用户可读事实，不出现 SkillId UUID、pairId、原始错误 code；提交后核对结果页只有展示名与结果文案。再切到操作记录页与恢复页（若存在记录）：核对条目标题为「本地化操作名：对象名」，错误说明为「操作未能完成（码值）」括注模式而非独立「错误码：xxx」行。 | 所有用户可见页面（含技术详情折叠区）不出现 SkillId/UUID/pairId/operation id/关系 ID；操作记录与恢复页无裸 kind（delete_skill 等）；错误码只作为失败说明的括注出现；界面语义全程用名称、版本、时间表达。 | 待验收（自动化通过） | 保存部署预览技术详情、结果页、操作记录页与恢复页截图。 | 清理测试 Skill 与部署关系。 |
