# 开发状态（2026-09-26）

> 最新验收状态：DEV-92 通知玻璃视觉（16% / 6px）已获用户确认；文字选中高亮与概览 Agent 品牌柱条也已获用户确认。仅这些视觉判据通过，完整流程 A/E 仍待执行。DEV-95 统一待办与 DEV-96/97/98/99/100/101（通知模块归属、删除影响再设计、恢复页可读标签、内部标识全面退出界面、junction 真机实证与 profile 更新、启动恢复闸门根因修复）代码与自动化完成，待人工验收流程 I/J。当前标准产物为 target/release/skillhub-desktop.exe（2026-09-27 00:27，34,399,744 bytes，SHA-256 73A257169BCD0A35C9656D9718313ADADF92E2DFA01280297EC858AC21FA2212）；签名构建通过。

> 上一版记录：按参考图改为固定中性灰半透明玻璃（rgba(32,32,32,0.32) + blur(24px)），不使用主题色填充。多条通知折叠时点击卡片或按回车展开，保持未读状态；铃铛切换及 280ms 水平进出动效保留，仅由 SkillHub 减少动效设置控制。通知单测 31/31、定向 E2E 2/2、lint/typecheck 与签名 Tauri release 构建通过；Windows release 人工视觉待复验。历史产物：`target/release/skillhub-desktop.exe`；构建时间 2026-09-26 16:43:40 +08:00；34,387,968 bytes；SHA-256 `89A3A20C4F6B7FBE9546181F1B65283D807B9E7C743C2AE232BED2B4CAF5A3BB`；源码基线 HEAD `97f75054e88c016440c345f50e89395518e09de4` 加本轮修正。

更新时间：2026-09-26（DEV-92 消息浮层收到真实桌面验收反馈并已修正；自动化通过，release 人工复验仍待做。同日验收中登记 DEV-96 通知模块与操作可读性，待开发）。本文件只保留当前开发状态、未完成事项和下一步；上一轮收口记录与历史现场结论已归档至 `docs/development/archive/2026-09-24-import-deployment-relationship-governance/`，9 月 24 日当前快照归档至 `docs/development/archive/2026-09-26-notification-popover/`。

当前分支：`feat/v0.2.0-product-completion`。开发遵循单一 worktree、顺序开发；当前未创建并行 worktree。

## 本轮范围：导入部署关系治理实施计划（15/15 完成）

按 `docs/superpowers/plans/2026-09-24-import-deployment-relationship-governance-implementation-plan.md` 完成全部 15 个任务，核心事实：

- **数据层**：新增迁移 `0019_import_source_governance.sql`（来源副本治理事实表）；真实 v18 夹具升级测试验证旧事实在新库可读且治理表齐全；崩溃恢复检查点（删除后前滚、删除前恢复为失败保留现场、无备份落入待恢复）均有测试。
- **应用层**：来源治理与治理历史仓储落地；`QueryOpenImportBatch` 补齐跨 facade 重启的打开批次恢复测试（重启后批次仍开放、提交后关闭）；部署预览/提交契约（`get_deployment_batch_preview` / `commit_deployment_preview`）覆盖伪造确认与过期确认不触碰文件系统的负向路径。
- **前端**：部署向导改为 pair 契约分组呈现（可执行/建议复制/无变化/被阻止四类处置组，blocked pair 不拖累其余 pair，确认回退需重新生成最终预览）；导入治理与向导彻底分离，完成页经深链进入治理待办；全部 Agent 展示面统一品牌 + 用户可理解类型徽标。
- **本轮发现并修复的真实缺陷**（均有红灯测试先行）：
  1. `describeDeploymentResult` 把 blocked 投影的稳定文案键（`committedBlocked`）当自由文本呈现给用户（`api.ts` 先按键翻译，未命中再走错误码路径）。
  2. `base.css` 的 `.sh-project-registration input { width: 100% }` 命中复选框，把项目注册勾选列表文本列挤压为 0（选择器排除 checkbox/radio，并允许品牌标签换行）。
  3. 概览最大化布局在窄容器（800×600）下 hero 未跨列（跨行声明只在 60rem 容器查询内），指标带被换行撑高并重新引入根滚动；已在最大化规则中对齐 M-20 契约（hero 恒跨整行）并对极矮最大化窗口补充密度压缩。
  4. E2E 对比度测量工具把 `color(srgb …)` 分量按 0–255 解析导致比率失真；已升级为正确解析 + 前景 alpha 合成 + 祖先不透明背景回溯，并按 WCAG 1.4.3 豁免禁用控件。

## 自动化基线（2026-09-25，含 DEV-87~94 全部变更）

| 命令 | 结果 |
|---|---|
| `cargo test --locked --workspace` | 通过：1276 通过 / 0 失败。 |
| `cargo fmt --all -- --check` | 未通过：既有未触碰文件与当前 rustfmt 版本存在格式差异（declared_requirements、catalog、relationship、storage 等约 20 处）。本轮未触碰这些文件，不做无关全仓重排，继续记录为工具版本边界。 |
| `pnpm --dir apps/desktop test --run` | 通过：1972 测试 / 0 失败（175 文件）。首跑与 cargo 并行时有 3 例超时类失败，单独重跑全绿，未复现。 |
| `pnpm check:frontend`（lint + typecheck） | 通过。 |
| `pnpm build:frontend` | 通过。 |
| `pnpm test:e2e` | **通过：404/404（3.3 分钟，2026-09-25 复验）**。 |

## 当前待办

| 范围 | 当前状态 | 下一步 |
|---|---|---|
| 人工验收 A—H | 流程 A 的概览项（DEV-66/77/78/82）已通过并关闭；其余流程仍待真实 release 证据 | 按《人工验收清单-2026-09-26.md》从最终 HEAD 重建 release 后执行；本轮新增来源副本治理、部署预览分组与回退确认判据已并入流程 C/E |
| DEV-79、DEV-83（图谱） | 代码与自动化完成 | macOS release 窗口可见后复验节点/边可读性、拖动稳定性与按 Skill 状态恢复 |
| DEV-84、DEV-85、DEV-86 | 代码与自动化完成 | 分别验证窄窗口标签列表、部署预览影响卡片与 skills.sh 支持清单链接部署边界 |
| DEV-1 | 外部阻塞 | 需要 macOS 第二个真实可写卷完成跨卷验收 |
| DEV-8 | 外部阻塞 | 需要可信官方 WorkBuddy 绿色 logo 素材 |
| DEV-87/88/89/90/91/94（2026-09-25 验收反馈开发项） | 开发与自动化验证完成 | 纳入 release 人工验收；自动化通过≠人工复验通过 |
| DEV-92 消息浮层（9/26 验收反馈复验） | 代码已完成；通知视觉获用户确认通过 | 定向自动化及签名构建已通过；堆叠/动效等流程级判据仍需按流程 E 复验 |
| DEV-93 通知历史持久化 | 下一版本规划（0.2.0 维持会话级，本轮未实施） | 下版本立项；契约五要素已为其铺路 |
| DEV-96 通知模块与操作可读性（2026-09-26 验收中反馈） | 代码与自动化验证完成（TDD 红灯先行） | 统一执行桥 `runTrackedOperation` 新增 `source` 参数并注入成功/失败/阻塞通知（调用方显式值优先）；多单位或部分失败的成功通知自动补「成功 N、失败 M」detail；通知卡片（折叠+展开）渲染本地化功能域徽标（缺省 System）；重扫调用点补 `discovery` 归属与结果事实。待人工验收流程 J |
| DEV-97 批量删除影响页再设计（2026-09-26 验收中反馈） | 代码与自动化验证完成（TDD 红灯先行） | ① 抽屉正文不再重复渲染流程说明（含旧 eyebrow 一并移除）；② 新增共享 `RemovalImpactPieces`（部署目标行 + 影响矩阵），单删/批量对话框与技能库抽屉三处收敛为一套实现；③ 影响矩阵逐维度分行、未知外部引用逐条路径呈现（固定版本保持计数，不暴露技术 ID）；④ 无部署关系时主按钮为「确认删除」，有部署关系为「继续删除」，`removal.retained` 措辞同步改写（术语守卫拦截「部署」用词后已修正）；⑤ 取消部署确认标题经 `readableAgentIdName` 显示品牌名，不再裸奔 client_id。待人工验收流程 J |
| DEV-98 恢复页码值裸奔（2026-09-26 验收中反馈） | 代码与自动化验证完成（TDD 红灯先行） | 用户验收流程 I 时发现恢复候选列表与摘要裸奔 operation id UUID 和原始 kind（`delete_skill`）。修复：桌面契约 `RecoveryCandidate`/`OperationState` 增加 `kind`/`objectName`/`createdAt` 展示字段（数据来自 recent_operations 快照，无 Rust 契约变更）；原生门面在 `listRecoveryCandidates`/`get` 补齐快照事实（快照读取失败不阻塞恢复本身）；新建共享模块 `operations/labels.ts`（kind 本地化映射 + 「操作名：对象名」标题 + epoch 秒时间格式化），OperationsList 同步收敛到该模块；恢复页单选项与摘要标题改为可读标签，缺快照的候选如实显示「未知操作」，真实错误码按既有「错误码：…」模式呈现。待人工验收流程 I（恢复页判据） |
| DEV-99 内部标识全面退出界面（2026-09-26 验收中反馈） | 代码与自动化验证完成（TDD 红灯先行） | 用户验收添加 Skill 流程时发现技术详情展示 SkillId UUID 与部署条目 pairId。修复：① 全项目排查——`DeploymentImpactCard` 移除 skillId/pairId/错误码/technicalError/logicalTargetIds 行，折叠区只留部署方式、回退方式与实现警告等用户可读事实（无方式事实时不渲染折叠区）；`DeploymentResults` 移除 skillId 块；`OperationProgress` 标题改「本地化操作名：对象名」并移除 operation id 行；`GraphDetailsPanel` 移除中心节点 ID 与关系 ID 展示。② 操作时间线/恢复页/操作进度中独立的「错误码：xxx」统一经 `localizeErrorCode`（keyedMessage → 既有失败文案，未映射走「操作未能完成（码值）」）映射为可读说明，码值仅作括注。③ 预览与 E2E 夹具 kind 对齐真实契约（import_skill 等），可达名不再出现裸 kind。④ AGENTS.md「技术标识不进界面」规则修订：ID 一律不得出现（含技术详情折叠区），错误码不得独立成值、仅允许失败文案括注。待人工验收流程 J（添加 Skill 技术详情判据） |
| DEV-100 junction 真机探针实证与 profile 更新（2026-09-26 用户真机验证） | 代码与自动化验证完成 | ① 跨客户端 junction 探针实验（探针 Skill 经 junction 接入 Kimi/Cursor/Trae/Gemini CLI/Copilot 及共享目录）全部通过：技能列表可见、调用返回探针字符串、共享目录与专属目录去重为一个条目；② 对应 profile（kimi/cursor/trae/google.gemini-cli/github-copilot/agent-skills）junction 升级 `true` 并移除未确认限制，链接部署选项将随之放开；Hermes/Antigravity（未安装）、豆包（未探针）维持不变；③ 豆包 profile 按用户真机确认登记四个目录（AppData 内置只读观察 `builtin=true`、`.user_skills`、工作区 `{project_root}/skills` project scope、共享目录），移除 `internal_skill_dirs_not_public_contract`；④ `builtin_profiles.rs` 验证名单与内置计数（5→6）、expectations 夹具同步；调研文档新增 5.25 实证记录，豆包/OpenClaw/Hermes「工作区技能」同构建模记为参照。cargo core+adapters 73 套件全绿。待人工验收：新产物上核对 Kimi 等目标出现「链接部署」且部署落盘正确 |
| DEV-101 启动恢复闸门反复触发（2026-09-27 换包复现） | 代码与自动化验证完成（TDD 红灯先行） | 用户换新 exe 正常打开仍见「正在恢复本地数据」。根因：删除/取消部署/部署/迁移四类流程的 `prepare` 只写账目行（`phase='prepared'`），可提交状态存于进程内存；用户取消确认框（或进程终止）后该行无人收尾，`LocalRecoveryBackend.list_candidates` 把它计为恢复候选，闸门每次启动必现（用户库中实测两条 delete_skill 卡住行）。修复：会话打开（`new_with_today`/`new_with_library` 两个构造路径）时把 `planned`/`prepared` 残留行结算为 `rolled_back`——四类 prepare 均为只读检查或内存计划、磁盘零副作用，无回滚需要；`applying` 及之后的行可能有真实磁盘效果，保持为恢复候选（携带 recovery_data 的报 NeedsRecovery，否则 InProgress，均闸住启动）。附带修复依赖旧行为的存量测试。用户库中两条卡住行已直接结算清零。workspace 151 套件全绿。待人工验收：取消一次删除/部署后重启，不再出现恢复闸门；流程 I 判据照旧 |
| `cargo fmt` 工具边界 | 既有未触碰文件存在 rustfmt 版本差异 | 单独安排一次纯格式化提交（不与行为变更混合），或统一工具链版本后全仓重排 |
| `cargo deny` licenses 边界 | `notify v8.2.0`（经 `gix` 引入）的 `CC0-1.0` 不在允许清单，干净 HEAD 同样失败 | 评估将 `CC0-1.0` 纳入 `deny.toml` 允许清单（FSF Free/Libre）或锁定替代依赖 |

## 已完成摘要：2026-09-25 验收反馈开发项（DEV-87~94）

全部自动化验证通过（见自动化基线 2026-09-25）；自动化通过≠人工复验通过，各项均待下次人工验收。

- **DEV-87 内置卡片标签融合**（467eb30a）：可访问状态徽标回到卡片右上角（StatusBadge 唯一右侧元素）；保留类型徽标；「内置」升级为琥珀差异色「内置 · 只读」徽标，正文只读说明段压缩。组件测试断言头部结构与徽标文案。
- **DEV-88 共享目录 chip**（d36071ff）：`shared_reference` 的 `.agents\skills` 路径行不再裸展示，渲染为可点击「支持共享目录」chip（info tint），跳转共享目录卡片；路径本体只在共享目录卡出现。链路：Rust `shared_reference` → `AgentView.sharedReferencePaths` → `AgentCardView.sharedPathKeys`（归一化去重）。
- **DEV-89 豆包工作 profile**（dd5520c3）：新增 `crates/skillhub-adapters/profiles/doubao.json`（`doubao-work.desktop`，仅声明共享目录 `{user_home}/.agents/skills`，shared_reference，受控复制；内置 AppData 目录经 2026-09-25 真机核实（106 个内置 Skill）不作为稳定公开契约，仅记入调研文档）；官方一手 PNG 图标（doubao.com CDN）逐字节入 `public/brand/agents/lobehub/doubao.png` 并登记 SOURCES.md + BrandTag 逐字节校验；调研文档豆包工作小节已更新。
- **DEV-90 关系页签高亮**（8a2428cb）：「技能图谱」NavLink 加 `end` 修正前缀误匹配；治理页签保持前缀匹配使 history 子页仍高亮。两个红灯用例覆盖。
- **DEV-91 仓库管理布局 + GitHub 图标化**（4e93facc + ed5141cb）：添加区改「仓库 URL + 分支」双字段卡片（分支默认 main，URL 带 `/tree/分支` 预填）；仓库卡改整宽横排卡 + 右侧图标操作组（刷新/外链/垃圾桶，删除保留 ConfirmDialog）；启停开关与最近扫描信息保留（裁决点按「功能保留、呈现收纳」执行）。仓库发现页 GitHub 文字按钮同步替换为右上箭头图标，其余不动。
- **DEV-92 消息中心**（b1375110，后经 2026-09-26 验收反馈更新）：`AppNotice` 增加 `source` 功能域枚举（library/deployment/import/discovery/governance/system，可缺省默认 system），18 个 notify 调用点补齐 source；浮层固定在顶栏下方右侧，半透明毛玻璃背景；折叠态为最新通知完整显示、其下至多两层旧卡片错位露出，可展开后按功能域分组；卡片含来源/标题/正文/时间/未读点/深链。水平滑动由 CSS keyframes 实现，减少动效只由 SkillHub 设置开关控制。Esc、外部点击、焦点返还保留。
- **DEV-92 验收边界遗漏（2026-09-26）**：查阅历史反馈及当前实现后，本次归类为既有视觉要求对齐与折叠交互边界遗漏，不新开 DEV 编号。本轮按参考图改为固定中性灰半透明玻璃（rgba(32,32,32,0.16) + blur(6px)），不使用主题色填充。多条通知折叠时点击卡片或按回车展开，保持未读状态；铃铛切换及 280ms 水平进出动效保留，仅由 SkillHub 减少动效设置控制。通知单测 31/31、定向 E2E 2/2、lint/typecheck 与签名 Tauri release 构建通过；Windows release 人工视觉待复验。此前已修正系统减少动效覆盖应用设置、铃铛重复点击不能关闭的问题。
- **DEV-93**：未实施（下版本，见待办表）。
- **DEV-94 操作记录要素统一**（30f74742 + eaf80d78）：两处真缺陷修复——epoch 秒时间解析失败不再回显原始值（10 位数字按秒转换，不可解析显示「—」）；kind 技术名本地化（import_skill→导入技能等 7 类，未知 kind 回退原文）。第二步扩展快照契约：`RecentOperationSummary` 新增 `object_name`（导入流程在 journal result 落 `{"object_name": runtime_name}`，prepare/commit/cancel/discard 四路径覆盖），快照组装宽松解析，specta 绑定由生成测试再生（非手写）；前端标题含对象名（「导入技能：Notes」，i18n objectTitle），部署类操作展示「成功 N、失败 M」与失败目标路径。

## 继续工作顺序

1. DEV-92 验收反馈代码修正已完成；不要将自动化通过登记为人工通过。
2. 通知玻璃视觉、文字选中高亮和 Agent 品牌柱条的局部视觉项已获用户确认；完整流程 A—H 按人工验收清单继续。
3. 按《人工验收清单-2026-09-26.md》复验半透明模糊底色、下方卡片层叠与水平进出场；保持 Windows 系统动画关闭，确认 SkillHub 开关关闭时仍滑动；再开启 SkillHub“始终减少动效”确认即时开合，然后关闭该开关复验恢复滑动，再继续 A—H。
4. 发现问题时先回查归档证据与当前源码，再分类登记；不覆盖历史记录。

## 本轮新增界面反馈（2026-09-26）

- 用户确认 DEV-92 当前通知玻璃视觉效果通过；不扩大为整个通知流程验收通过。
- 新增需求：文字选择使用淡主题色高亮并保留复制能力；强制颜色模式使用系统高亮。概览 Agent 柱条使用独立品牌色，多色 logo 取代表色、单色及未知品牌使用中性灰；项目维度保持原配色。源码与桌面构建已完成，人工验收待完成。

本轮文字选择与品牌柱条产物：已由用户确认验收通过。产物时间、签名构建、静态检查及未运行测试等记录见本文件前文。

## DEV-95 统一待办（2026-09-26）

- 分类：本轮新增产品需求。历史“待处理”实际仅接入试用到期与安全发现；冲突、关系治理和恢复查询原本独立。用户要求保留一级入口并补齐完整任务流。
- 已完成：前端通过已有只读查询统一汇总五类事项，保持后端事实为准；冲突按组、治理按活动关系、恢复按操作去重，不按 Skill 合并不同问题。治理只取 needs_validation / needs_attention / blocked；正常、保留及已完成事实不进入待办。
- 已完成：概览摘要与待办列表复用同一查询门面，概览分类链接带筛选；进入、返回、窗口恢复焦点、操作结束及发现/部署事实变化时刷新。任一事项来源读取失败时明确显示不可用并可重试，不显示假零。
- 已完成：冲突/治理/恢复使用携带对象的深链去处理；恢复预选候选、完成后刷新启动快照。启动恢复闸门仍优先要求恢复。必需决策与恢复不支持暂缓/忽略；安全及试用提醒保留现有操作，撤销忽略后同步刷新列表。
- 已完成：展示可读对象名和规范化路径；待办及提醒记录不裸露技术 ID。安全风险沿用检查事实，冲突/治理不伪造安全风险等级。
- 检查：相关组件/门面/路由测试 130/130；原生查询模拟的浏览器定向 E2E 2/2；前端静态检查结果与桌面构建另记。未改 Rust 契约或生成绑定。
- 仍需：Windows release 人工验收流程 I；本轮不将自动化结果写成人工通过。
