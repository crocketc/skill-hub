# 自动化测试说明（2026-09-26）

> 最新状态：DEV-96/97 完成——全量前端测试 1989/1989（178 文件）、定向 E2E 55/55、lint/typecheck 通过，签名 release 重建于 2026-09-26 21:43（标准路径 target/release/skillhub-desktop.exe；SHA-256 C929AC16E0D73F239B214AAA34F0CD97F39A02E512D3296FC1AD732E8E7E58A2；含 DEV-96/97/98 全部修正）；人工验收流程 I/J 待执行。用户已确认通知玻璃视觉、文字选中高亮和概览 Agent 品牌柱条通过；仅这些视觉判据通过，人工验收流程 A—H 仍按清单逐项执行。

> 上一版记录：按参考图改为固定中性灰半透明玻璃（rgba(32,32,32,0.32) + blur(24px)），不使用主题色填充。多条通知折叠时点击卡片或按回车展开，保持未读状态；铃铛切换及 280ms 水平进出动效保留，仅由 SkillHub 减少动效设置控制。通知单测 31/31、定向 E2E 2/2、lint/typecheck 与签名 Tauri release 构建通过；Windows release 人工视觉待复验。历史产物：`target/release/skillhub-desktop.exe`；构建时间 2026-09-26 16:43:40 +08:00；34,387,968 bytes；SHA-256 `89A3A20C4F6B7FBE9546181F1B65283D807B9E7C743C2AE232BED2B4CAF5A3BB`；源码基线 HEAD `97f75054e88c016440c345f50e89395518e09de4` 加本轮修正。

更新时间：2026-09-26。本文件只保留当前自动化命令、结果和不能替代人工的边界；上一轮批次记录已归档至 `docs/development/archive/2026-09-24-import-deployment-relationship-governance/`，9 月 24 日快照归档至 `docs/development/archive/2026-09-26-notification-popover/`。

> 自动化通过不构成 Windows/macOS release 真机通过。人工验收只按《人工验收清单-2026-09-26.md》执行并记录。

## 当前自动化结果（2026-09-24，分支 `feat/v0.2.0-product-completion`）

| 命令 | 结果 |
|---|---|
| `cargo test --locked --workspace` | 通过：1270 通过 / 0 失败。 |
| `cargo clippy --locked --workspace --all-targets --all-features -- -D warnings` | 通过。 |
| `cargo deny check advisories bans licenses sources` | **licenses 未通过**：`notify v8.2.0` 的 `CC0-1.0` 不在允许清单（经 `gix` 依赖链引入）。在干净 HEAD 复验同样失败，属既有依赖策略边界，本轮未变更任何依赖；advisories/bans/sources 通过。 |
| `cargo fmt --all -- --check` | 未通过：既有未触碰文件的 rustfmt 版本差异（工具边界，本轮未触碰相关文件，未做无关重排）。`git diff --check` 通过。 |
| `pnpm install --frozen-lockfile --ignore-scripts` | 通过。 |
| `pnpm verify:lifecycle` / `pnpm audit:frontend` | 通过。 |
| `pnpm --dir apps/desktop test --run` | 通过：1933 测试。 |
| `pnpm check:frontend` | 通过（ESLint 与 TypeScript）。 |
| `pnpm build:frontend` | 通过；保留既有大 chunk 提示。 |
| `pnpm test:e2e` | **通过：404/404（2.6 分钟，5 workers）**。当前 HEAD 首次全量全绿。 |

> `pnpm ci:local` 会在第一个失败阶段（Rust formatting 的既有工具差异）停止；其余阶段已按 `scripts/ci-local.mjs` 的定义逐条手动执行并记录如上（clippy、Rust 测试、依赖/生命周期/审计、前端 lint/typecheck/测试/构建均通过，licenses 策略为既有失败），不因此把 ci:local 写成整体通过。

## 2026-09-26 DEV-92 消息浮层验收反馈修正

| 命令 | 结果 |
|---|---|
| `pnpm --dir apps/desktop test --run src/ui/reducedMotion.test.tsx src/features/settings/GeneralSettings.test.tsx src/app/router.test.tsx src/ui/Drawer.test.tsx src/features/skills/SkillQuickDrawer.test.tsx src/ui/notifications.test.tsx` | 通过：111/111；验证系统 `prefers-reduced-motion: reduce` 不影响默认 SkillHub 动效，应用内开关持久化并控制 CSS/MotionConfig。 |
| `pnpm --dir apps/desktop test --run src/ui/notifications.test.tsx` | 本次通过：31/31；含堆叠卡片鼠标/回车展开并保留未读、展开后单条已读关闭及铃铛退出生命周期。 |
| `pnpm --dir apps/desktop test --run` | 前次全量基线通过：1970/1970（175 个测试文件）；本次新增通知用例后以通知文件 31/31 定向复验。首次全量出现 1 个部署预览焦点测试瞬时失败，单文件重跑通过，随后全量重跑通过。 |
| `pnpm exec playwright test --config=tests/e2e/playwright.config.ts tests/e2e/library-toolbar-notices.spec.ts tests/e2e/overview-preview.spec.ts --grep "notification popover|SkillHub's reduced-motion switch"` | 通过：2/2；在浏览器模拟 `prefers-reduced-motion: reduce` 时，通知仍有水平进出动画；SkillHub 开关开启后动画终止。 |
| `pnpm exec playwright test --config=tests/e2e/playwright.config.ts tests/e2e/library-toolbar-notices.spec.ts --grep "notification"` | 本次通过：2/2；真实浏览器灰色底层 alpha=0.32、24px blur、OS reduce 下水平进出动画及铃铛关闭、堆叠点击展开并保留未读。 |
| `pnpm test:e2e` | 前次全量基线通过：405/405；含 SkillHub 开关与 OS reduce 解耦、Reduced Motion 专项，以及抽屉布局回归。首次运行 1 个设置抽屉几何测试失败（测试仍依赖 OS reduce），修正为启用 SkillHub 自身开关后全量重跑通过。 |
| `pnpm check:frontend` | 通过（ESLint 与 TypeScript）。 |
| `pnpm build:frontend` | 通过；保留既有大 chunk 提示。 |
| `pnpm --dir apps/desktop tauri build` | 本次通过；NSIS 安装包、updater 包及签名生成。产物：`target/release/skillhub-desktop.exe`；构建时间 2026-09-26 16:43:40 +08:00；34,387,968 bytes；SHA-256 `89A3A20C4F6B7FBE9546181F1B65283D807B9E7C743C2AE232BED2B4CAF5A3BB`；源码基线 HEAD `97f75054e88c016440c345f50e89395518e09de4` 加本轮修正。 |

以上自动化与产物构建不能代替真实 release 视觉验收。新签名 Windows release 已在标准 `target/release/skillhub-desktop.exe` 构建并启动供验收；真实桌面截图与用户现场复验仍待记录，不能标记人工通过。

## 本轮新增/变更的测试范围

- **跨层回归（计划 15.8）**：v18 夹具升级、三个崩溃恢复检查点、watcher 根目录不匹配、部署确认伪造/过期负向路径均有既有测试；本轮补齐 `QueryOpenImportBatch` 的唯一空白——facade 重启后打开批次仍可恢复、提交后从开放列表消失（`facade_import_conflicts.rs`）。
- **E2E 契约更新**（对齐 pair 部署契约与品牌展示约定）：
  - `workflow-deployment.spec.ts`：处置分组（blocked 不阻塞其它 pair、确认回退先重新生成最终预览）、五桶摘要、单项排除后重生成、预览失败无分组无提交。
  - `import-governance.spec.ts`：治理与向导分离后的完成页深链（`task:preview-governance`）、整理来源副本 CTA、原始副本保持不变。
  - `relationship-views.spec.ts`、`onboarding-preview.spec.ts`、`agents-projects-preview.spec.ts`：品牌 + 用户可理解类型的可访问名（如 `OpenAI · 终端`），无裸 `client_id`/技术 ID。
  - `native-flow-preview.spec.ts`：原生 IPC 桩补 `get_deployment_batch_preview` / `commit_deployment_preview`，治理链接计数口径改为需处理状态并集（needs_validation + needs_attention + blocked）。
  - `overview-preview.spec.ts`：治理入口新契约 `Open needs-governance relations (N to handle)` 与带状态并集的深链；标签面板无内部滚动的现行等价断言（fb57b7b6 起为静态面板）。
  - `overview-height.spec.ts`：10/10（800×600、1462×866、1920×1080、窗口化对照、指标带填满、SVG 边界）。
- **修复的真实缺陷测试**：`committedBlocked` 文案键解析（`DeploymentResults.test.tsx` 红灯先行）、项目注册复选框列宽（E2E DEV-14 长标签场景）、800×600 最大化 hero 跨列与极矮密度（`overview-height`）、对比度工具 `color(srgb)` 解析与禁用控件豁免。

## 不能替代人工的边界

- 自动化不覆盖：真实用户目录与 Agent 文件保护、真实权限/只读目录、Windows 目录联接与 macOS 符号链接的真实落盘、跨卷行为、真实窗口/主题视觉、长路径与批量耗时、通知中心人工可读性。
- E2E 使用 DEV 预览路由与 mock 门面（`native-flow-preview` 经页内 IPC 桩），只能证明前端契约与几何，不能证明原生 IPC 与数据库的真实往返。
- 部署预览的确认指纹/过期裁决由后端测试覆盖语义，真实时间推进与多进程并发仍需 release 人工验证。
- 概览高度契约已覆盖三类有效视口，但仍不能替代真实 DPI 缩放下的最大化/窗口化人工复验（判据见人工清单流程 A）。

## 本轮文字选择与品牌柱条配色

仅修改选择样式及图表颜色映射；本轮未新增或运行测试。静态检查、产物构建通过；用户已对本轮视觉更改目视验收通过。

本轮文字选择与品牌柱条产物：已由用户确认验收通过。产物时间、签名构建、静态检查及未运行测试等记录见本文件前文。

## DEV-95 统一待办验证（2026-09-26）

| 命令/范围 | 结果与边界 |
| --- | --- |
| pnpm --dir apps/desktop test --run src/features/pending src/features/overview src/app/router.test.tsx src/features/recovery/RecoveryPage.test.tsx | 130/130 通过；覆盖五类源汇总、去重、完成状态消失、读取失败、禁止忽略必需事项、可读对象名、分类深链、返回刷新、概览同源计数和恢复后快照刷新。 |
| pnpm exec playwright test --config=tests/e2e/playwright.config.ts tests/e2e/native-flow-preview.spec.ts --grep "unified pending\|pending, recovery" | 2/2 通过；原生查询模拟下，概览→待办→冲突裁决→返回待办→概览计数减少闭环；并覆盖待办、安全和恢复入口。 |
| 人工边界 | 使用的是浏览器模拟事实，不代表 Windows/macOS 真机验收；启动恢复闸门、真实治理状态与目录权限回退须按人工流程 I/E/H 执行。 |

## DEV-96/97 通知模块归属与删除影响呈现（2026-09-26）

| 命令/范围 | 结果与边界 |
| --- | --- |
| `pnpm --dir apps/desktop test --run` | 全量 1989/1989（178 个测试文件）通过。 |
| 新增用例（TDD 红灯先行） | `runTrackedOperation.test.tsx`：流程 source 注入（成功/失败/阻塞通知）、调用方显式 source 优先、多单位或部分失败时补「成功 N、失败 M」detail 且不覆盖调用方 detail；`notifications.test.tsx`：折叠态/展开态卡片功能域徽标（缺省 System）；`BatchRemovalDrawer.test.tsx`（新文件）：流程说明只出现一次、部署行品牌+类型呈现且技术 client_id 不裸奔、未知外部引用逐条分行、无部署关系时按钮为「Delete」。 |
| 契约同步更新 | `BatchRemovalImpactDialog.test.tsx` 影响矩阵结构化断言与「Continue deletion」措辞；`SkillLibraryPage.test.tsx`/`SkillDetailPage.test.tsx` 下拉 accessible name 改为规范化路径标识；`terminology.test.ts` 守卫拦截 `removal.retained` 中「部署」用词后已改写为「目标」表述（守卫生效的实证）。 |
| `pnpm exec playwright test --config=tests/e2e/playwright.config.ts library-toolbar-notices native-flow-preview overview-preview` | 55/55 通过。 |
| `pnpm check:frontend` | 通过（ESLint 与 TypeScript）。 |
| 人工边界 | 徽标视觉密度、删除影响页信息层级和措辞可读性需按人工清单流程 J 真机复验；E2E 为 mock 门面，不证明真实 IPC 落盘。 |

## DEV-98 恢复页可读标签（2026-09-26）

| 命令/范围 | 结果与边界 |
| --- | --- |
| 新增用例（TDD 红灯先行） | `RecoveryPage.test.tsx`：恢复候选单选项显示「本地化操作名：对象名」（如「删除技能：Notes」），缺快照候选显示「未知操作」，operation id 与原始 kind 不出现在界面；摘要标题同口径并带本地化时间。 |
| 契约与共享模块 | `RecoveryCandidate`/`OperationState` 增加 `kind`/`objectName`/`createdAt` 展示字段（来自 recent_operations 快照，无 Rust 变更）；新建 `operations/labels.ts`（kind 本地化映射、objectTitle、epoch 秒时间格式化），`OperationsList` 同步收敛到该模块，消除双份映射。 |
| `pnpm --dir apps/desktop test --run` | 全量 1989/1989（178 文件）通过。 |
| `pnpm check:frontend` | 通过（ESLint 与 TypeScript）。 |
| `pnpm exec playwright test --config=tests/e2e/playwright.config.ts native-flow-preview --grep "recovery\|pending"` | 2/2 通过。 |
| 人工边界 | 恢复页文案可读性需在真实恢复现场按流程 I 复验；启动恢复闸门无轮询、恢复完成后需手动进入恢复页确认，为既有设计。 |

## DEV-99 内部标识全面退出界面（2026-09-26）

| 命令/范围 | 结果与边界 |
| --- | --- |
| 新增/更新用例（TDD 红灯先行） | `DeploymentDialog.test.tsx`：受阻 pair 的原始错误 code（`deployment.symlink_not_supported`）与技术详情折叠区均不出现；`BatchDeploymentPage.test.tsx`：skill UUID 全面缺席（含折叠区）、阻断原因保持用户可读文案；`DeploymentResults.test.tsx`：显示名为主文案且原始 Skill UUID 不再渲染。 |
| 存量断言对齐新契约 | 操作时间线/恢复相关测试夹具 kind 对齐真实契约（`import_skill`/`deploy_skill`/`delete_skill`）并补 `object_name`；「错误码：xxx」独立码值断言改为「操作未能完成（码值）」括注模式；E2E 链接可达名改为「本地化操作名：对象名」。 |
| `pnpm --dir apps/desktop test --run` | 全量 1989/1989（178 文件）通过。 |
| `pnpm check:frontend` | 通过（ESLint 与 TypeScript）。 |
| 定向 E2E（`pnpm test:e2e`） | `workflow-operations` + `workflow-deployment` 54/54；`import-deploy` + `native-flow-preview` + `workflow-recovery` 35/35（含修复占用冲突回归护栏：pair id 与错误对象不进首屏，折叠区计数为 0）。 |
| 人工边界 | E2E 前三跑中出现的「Cannot navigate to invalid URL」为本侧误用 `npx playwright` 未带 `--config`（baseURL 缺失），非产品缺陷；已确认标准脚本 `pnpm test:e2e` 全绿。技术详情折叠区内容需真机按流程 J 复验。 |

## DEV-101 启动恢复闸门反复触发（2026-09-27）

| 命令/范围 | 结果与边界 |
| --- | --- |
| 根因事实 | 四类流程（delete_skill/undeploy_skill/deploy_skill/migrate_original）的 `prepare` 均为只读检查或内存计划（磁盘零副作用），但会写 `phase='prepared'` 账目行；取消确认框或进程终止后该行无人收尾，`list_recovery_candidates` 将其计为恢复候选，启动闸门必现。用户库实测两条 `delete_skill` 卡住行。 |
| 新增用例（TDD 红灯先行） | `facade_operation_journal.rs::stale_planned_and_prepared_rows_settle_as_rolled_back_on_startup`：跨会话 prepared/planned 行在新会话打开时结算为 `rolled_back` 且闸门放行（Clean）；`applying` 行不受清扫影响并继续闸住启动（无 recovery_data 时报 `InProgress`，`NeedsRecovery` 保留给携带可回滚数据的行）。 |
| 实现点 | `LocalApplicationFacade::sweep_stale_journal`，在 `new_with_today` 与 `new_with_library` 两个持有 Database 的构造路径调用；附带修正依赖旧行为的存量测试（`facade_repairs_unfinished_operation_and_requires_valid_ignore_removal` 改为构造后种行）。 |
| `cargo test --locked --workspace` | 151 个套件全部通过、0 失败。 |
| 用户数据修复 | 应用未运行时直接结算用户库中两条卡住行（与启动清扫同一 SQL，等价幂等）；已复查 `planned/prepared/applying/verifying/needs_recovery` 全部清零。 |
| 人工边界 | 自动化不覆盖真实进程崩溃时序；闸门在真实 `applying` 中断（含 recovery_data 回滚）下的行为仍按流程 I 真机验收。 |
