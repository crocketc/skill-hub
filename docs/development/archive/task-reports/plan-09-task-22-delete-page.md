# Plan 09 Task 22：技能详情页删除完整闭环

## 状态

- 已完成并合并到 `main`。
- 代码提交：`dcd2a3d`。
- 验收收口：Windows/macOS 均已通过。

## 完成内容

- 技能详情页提供删除入口；预览路由不触发真实删除门面。
- 点击删除后读取集中库删除影响，展示活动部署关系和依赖项目。
- 每条部署关系必须选择处理方式后才能提交；没有部署关系时允许直接确认。
- 删除提交使用准备阶段返回的操作 ID，避免使用过期影响结果。
- 提交中禁用重复操作，失败显示可重试错误，取消关闭对话框。
- 删除成功后刷新技能库查询并返回原技能库上下文。
- 选项映射保持与领域契约一致：保留文件并解除关系、移除部署、保留为独立副本。

## 验证

Windows：

- 完整本地 CI：10/10 通过。
- `SkillDetailPage.test.tsx`：20/20 通过。
- `removal/nativeApi.test.ts`：4/4 通过。
- `RemovalImpactDialog.test.tsx`：1/1 通过。
- TypeScript、ESLint、生产构建：通过。

macOS：

- 提交 `dcd2a3d` 已同步到 `main`。
- 完整本地 CI：10/10 通过，60 个前端测试文件、336 个测试通过，安全审计 0 个漏洞，生产构建通过。
- 三个删除相关前端测试文件合计 25/25 通过。
- TypeScript：通过。
- 未修改源码、依赖、文档或 Git；仅保留 CI 生成的 `.gitkeep` 删除和本地 `.DS_Store` 未跟踪文件。

## 已知边界

- 删除入口已接入详情页，但删除后的项目引用替换、组合处理和备份恢复仍由后续批次负责。
- 不自动删除用户原始文件或未知外部内容，不提供平台专用导入包。
