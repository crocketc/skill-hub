# Plan 09 Task 29：Agent/项目与初始化扫描接入

## 状态

已完成。代码提交为 `e34d904`；Windows/macOS 验收均通过。

## 已实现

- 接入自定义 Agent、Profile 覆盖、发现快照、初始化扫描、目标扫描和 Skill 重扫。
- 接入项目注册、更新、标签、保存视图、共享项目配置和项目组装准备/提交。
- picker 路径必须由宿主预先登记 grant；扫描只发布文件系统事实，不声称 Agent 已授权或一定可执行。

## 验证

Windows/macOS 本地 CI 均为 10/10；facade 专项测试覆盖自定义 Agent、项目配置、扫描和项目组装，bindings、Clippy、格式检查通过。

## 边界

动态来源获取和未登记路径不会被隐式执行；组装失败保留明确失败状态供用户处理。
