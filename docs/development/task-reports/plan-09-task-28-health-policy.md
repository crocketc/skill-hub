# Plan 09 Task 28：健康、恢复与策略接入

## 状态

已完成。代码提交为 `9a96866`；Windows/macOS 验收均通过。

## 已实现

- 接入健康检查、修复计划、恢复候选与恢复处理。
- 接入调用策略查询、变更预览、提交和恢复原策略。
- 接入精确忽略规则的创建、查询和删除，保持结构化错误和显式动作。

## 验证

Windows/macOS 本地 CI 均为 10/10；ApplicationFacade 检查、Clippy、格式和 bindings 验证通过。

## 边界

当前数据库没有独立忽略规则表，因此忽略规则暂存于 facade 会话内；未修改数据库迁移。健康与策略判断不推断 Agent runtime 可用性。
