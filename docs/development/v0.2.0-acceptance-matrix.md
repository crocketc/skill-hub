# v0.2.0 验收矩阵（本地记录）

更新时间：2026-09-08（记录浏览器自动化 10/10 通过结果）
代码验收基线：`18bcccc`（本次验收基线包含 onboarding 流程适配、技能库/详情 E2E 与预览 fixture 过滤修复；后续同步提交只更新验收/状态文档）。
本轮新增提交（按批次）：M1 发现模块 `8916f3a` skills.sh 修复、`2e7176f` 仓库发现；M2 `4db9341` 完成页汇总、`64ce4ede` 忽略项+修复预览、`8320c6fe` 操作框架+后台导入、`0febbda3` 操作记录+双页签；M3 `4d327bd9` 来源更新、`9a79a575` 导出+退出管理；M4 `e7e648f6` 安全深度、`1a9188d8` Agent 管理、`49eba85a` 项目详情、`5b5083f3` 反向部署；M5 `6b974017`+`d14a274` 来源重关联、`2b5ed31e`+`4bdf6cb` 抽屉真实数据、`6d95495b` 滚动备份、`1721a358` 指标下钻、`a2735616` 偏好持久化、`395f9c3`→`5daf27e` 卡片视图、`bbff8267`+`f89339e4` 卸载备份、`a581e850`+`1ea80f4` 关系矩阵、`79c5e8d6` 标签分组、`eb72ba5e` grant 链路、`e1977b8d` lock 导入；N1~N8：`0cf28a4` 外链打开、`866bc7b` 操作历史、`1b89ed4` AI 取消、`964c134` ZIP 导出、`73f3be3` 组合 CRUD、`79fa7d9` 批量汇总、`c7eef74` 标签下钻、`6e0b9c5`/`6bdfc98` 远端更新、`d46019f` 批量来源更新检查；2026-09-07 AR 批次（34 提交 `f327590`→`5e5185e`）：AR-001~027 逐项修复与处置（onboarding 诚实错误/库根回显/选择器容错、来源更新 NoUpstream+tag 口径+版本命名、冲突可读化+按原因筛选、设置/健康/退出/Markdown 分区与决策、发现页重构、组合子页、导出重设计、批量栏重组、品牌标签、N9 待处理深度、N11 打开库目录、N12 确定性重复、i18n 三批次错误码化）；2026-09-07 后续批次（4 提交）：AR-021 tag 口径对齐、N10 版本范围、AR-027 分区重排。
此前已含：`cd5eb7f`、`7a80f3e`、`c6b8d10`、`a16f1fb`、`ac5b6b2`、`2f2be9b`。
状态含义：`[浏览器自动化]` 证明页面、路由和可注入数据下的交互；`[混合]` 还必须补充 Tauri/真实环境取证；`[桌面人工]` 必须在真实桌面环境完成。自动化证据不能代替真实桌面运行、安装和双平台取证；“未验证”不表示失败。

| 需求 ID | 用户动作 | 执行分流 | 自动化证据 | 当前人工状态 | Windows | macOS |
| --- | --- | --- | --- | --- | --- |
| P0-01 | 首次打开或重新运行初始化向导 | `[混合]` | `BootstrapGate`、`OnboardingWizard`、`OnboardingBranches`、Rust facade 测试（含 set_library_root）；2026-09-07 修复 onboarding 诚实错误/库根即时回显/选择器容错（`dca4e69`）、库根冲突 reason 参数（`72889fa`）；`pnpm test:e2e` 的 onboarding 用例通过 | 浏览器自动化通过：入口、分支选择、原生路径不可确认提示和禁用跳过；真实桌面仍待取证 | 待重新取证 | 待取证 | 待取证 |
| P0-02 | 本机发现、候选审阅、冲突决策与导入 | `[混合]` | Discovery/Import/`LocalDiscoveryWorkbench`/`RepoDiscovery` 页面和 native facade 测试（含五类汇总、按来源候选计数、冲突分组批量处理；`8916f3a` skills.sh legacy /api/search 契约测试；`2e7176f` 仓库发现 adapters 8+11 项、facade 6 项、前端 6 项测试） | 第 1 轮验收：扫描信息、五类汇总通过；多来源修复、冲突批量处理待第 2 轮验收；skills.sh 搜索已切到可用的 /api/search（`8916f3a`，待真机验证）；仓库发现 + 下载并导入（`2e7176f`）待人工验收 | 待取证 | 待取证 | 待取证 |
| P0-03 | 技能库、抽屉、详情、元数据与批量删除 | `[混合]` | SkillLibrary/SkillDetail/Removal 前端测试；`18bcccc` 新增浏览器 E2E 覆盖技能库搜索、卡片/标签分组、批量操作栏、快速抽屉、详情版本区及 Markdown 三模式；2026-09-07：抽屉确定性重复读模型（`0fb37a5`）、版本命名（`3766f9b`）、来源版本 tag（`83277c7`）、详情导出入口与版本范围（`aa7479e`）、设置分区（`6eee3d1`） | 浏览器自动化局部通过；AR-021/022/024/027 与真实数据、文件和 Tauri 行为仍待人工复验 | 待取证 | 待取证 | 待取证 |
| P0-04 | 部署、解除部署或删除 Skill | `[混合]` | Deployment/Removal 页面与 Rust facade 测试 | 未验证 | 待取证 | 待取证 | 待取证 |
| P0-05 | 安全检查、待处理、操作与恢复反馈 | `[混合]` | Security/Pending/Operations/Recovery 测试；`2fc5179` 规则持久化；2026-09-07 N9 深度（`8328d44`：暂缓/忽略/处理历史/保存视图/批量暂缓）与 /operations 记录页（`3b46a1`前序 `3b4691a`） | AR-015/016 已修复待复验（指南 1v-7、1x）；整体未验证 | 待取证 | 待取证 | 待取证 |
| P0-06 | 创建备份与敏感内容决策 | `[混合]` | BackupSettings 与 Rust backup facade 测试 | 未验证 | 待取证 | 待取证 | 待取证 |
| P0-07 | 校验备份与恢复预检/冲突决策 | `[混合]` | `DataProtectionPage.test.tsx`、Rust restore facade 测试 | 未验证 | 待取证 | 待取证 | 待取证 |
| P0-08 | 预检并导出临时 Skill | `[混合]` | `DataProtectionPage.test.tsx`、Rust export facade 测试；2026-09-07 导出重设计（`94535ae`：完整内容/显示名/输出目录/外层 ZIP/版本范围）+ 详情单 Skill 入口与版本范围（`aa7479e`） | AR-025 已修复待复验（指南 1w）；整体未验证 | 待取证 | 待取证 | 待取证 |
| P0-09 | 检查签名应用更新并安装/回滚 | `[桌面人工]` | release readiness 脚本及 Node 测试；检查/下载/签名/安装/重启/回滚的完整链路仍需真实资产 | 未验证；需要当前提交的真实签名资产 | 待取证 | 待取证 | 待取证 |

## 当前自动化质量证据

- 2026-09-08 浏览器 E2E：`pnpm test:e2e` 在 `18bcccc` 验收基线上执行，10/10 通过（`onboarding`、`keyboard-accessibility`、`startup-performance`、`import-deploy`、`edit-recover`、`library-preview` 4 条、`detail-preview`）。测试运行 Vite 预览页，浏览器通过不替代 Tauri/真实桌面证据。
- 本机已安装与项目 `@playwright/test 1.55.1` 匹配的 Chromium 测试缓存；该缓存属于测试环境，不在仓库中，也不会进入 Tauri 构建产物。

- `3f5eff8`（代码质量历史证据）：前端 102 文件 700 项 + check + build 全绿；Rust 五 crate 103 套全过（含 bindings 同步、迁移 0008）；i18n parity 无差异。`cargo clippy -D warnings` 受既有 core `ExportFormat` derivable_impls 阻断（`f327590` 后 ExportFormat 改名 / derive 保留），未修改无关文件。`141597a` 之后补充 onboarding 验收适配；`18bcccc` 新增技能库/详情浏览器验收、预览 fixture 搜索过滤和稳定并发配置。本轮浏览器 E2E 已按当前验收基线重新执行，前端全量质量检查仍按发布清单单独确认。
- `2fc5179`：application facade 全量 65 项、storage 全量通过（历史记录保留）。
- 历史质量记录按批次保留，不能替代当前代码基线的前端全量重跑。下一轮需用实际命令重新取证，并把浏览器自动化结果与桌面人工结果分开记录。
- `64ce4ede`：设置页忽略项管理 + 修复预览/执行（LibrarySettings 11 项测试，契约仅复用不新增）。
- `8320c6fe`：全局操作跟踪器（5 项）+ 指示器（3 项）+ 向导后台导入用例（含卸载后循环继续）。
- `0febbda3`：操作记录列表（4 项）+ 恢复双页签（3 项）。
- `4d327bd9`：Skill 详情来源更新面板（5 项，五态如实呈现 + 显式决策）。
- `9a79a575`：批量导出入口与退出管理分步（库页 3 项 + backup 7 项）。
- M4：安全深度（SecurityResults+FindingActions 17 项）、Agent 管理（agents 30 项，含唯一 Skill 聚合与自定义 Agent CRUD）、项目详情（projects 42 项，访问状态/装配分组/痕迹排序）、反向部署与目标展开（deployment 12 项）。M4 后全量前端 88 文件 565 项通过。
- 已知降级（UI 显示原因）：重新定位目录与 Agent 级忽略无契约；自定义 Agent 目录 grant 链路缺口（运行时报错，如实显示）；detach_management 在项目详情无落点。
- M5 前半：来源重关联面板（3 项测试）、抽屉真实读模型（2 项）、滚动备份保留策略（2 项）、概览指标下钻。M5 前半后全量前端 90 文件 572 项通过。
- `8916f3a`：skills.sh 搜索切至 legacy `/api/search`（匿名 200 可用），新增 repo_ref 坐标白名单模块（§4 逐字 + `.lock`/`.` 开头段按 §11 拒绝）。
- `2e7176f`：仓库发现标准版（repo_discovery 预算基线逐字保留、settings 仓库 CRUD、download_repo_skill 临时目录导入衔接）、bindings 已由生成工具再生成。
- `4db9341`：完成页汇总（诚实计数 + 显式出口按钮）。
- `ac5b6b2` 新增 set_library_root / restart_application 契约，bindings 已由生成工具再生成；Rust 新增 2 项 set_library_root facade 测试。
- `cargo check`、`cargo fmt`（仅本人 crate）通过。
- `pnpm --dir apps/desktop check` 通过；`pnpm --dir apps/desktop build` 通过（既存大 chunk 警告非阻断）。
- Rust 定向验证：`cargo test -p skillhub-storage --test library_layout`（7 项）和应用门面删除相关测试通过。
- 素材提交 `599f725` 只新增图标与营销资源，已执行 `git diff --cached --check`；未重复全量代码检查。

## 发布阻断项

- 未完成当前同一提交的 Windows/macOS 真实安装与启动取证。
- 未完成真实 Agent/项目部署、备份恢复、导出文件和迁移/E2E 证据。
- 未完成当前发布资产的签名校验、应用内更新安装和失败回滚取证。
- 前端遗漏清单中的 P0 页面能力尚未完成；在这些缺口关闭前，不创建或发布 `v0.2.0` tag。

详细的实际操作步骤见 [v0.2.0-manual-acceptance-guide.md](./v0.2.0-manual-acceptance-guide.md)。
