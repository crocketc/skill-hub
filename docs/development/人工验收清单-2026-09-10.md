# 人工验收清单（当前待验收项）

更新时间：2026-09-12

代码基线：以验收开始前实际构建的提交为准。

本文件只保留尚未完成的真实 Windows/macOS 桌面、文件系统、系统能力和真实服务验收项。上一轮已填写的完整清单（含结果、备注和历史上下文）已归档至：
`docs/development/archive/2026-09-12-新一轮验收切换/人工验收清单-2026-09-11-上一轮完整记录.md`。

## 验收前填写

验收日期：

平台/设备：

系统版本：

应用版本：

## 使用规则

- 每次只执行一项；“结果”填写 `通过`、`失败` 或 `阻塞`，未执行保持空白。
- 证据只记录真实设备上的截图、录屏、日志或文件路径；浏览器预览、Playwright、Vitest 不算真机通过。
- 使用专用测试目录和虚构 Skill，不使用真实 Skill、真实凭据或个人数据；破坏性操作前先复制测试目录。
- 失败或阻塞时保留证据并停止相关破坏性步骤，必要时在“备注/证据”中写明问题编号。

## 重新进入首次初始化

设置页的“重新打开初始化”只适用于已初始化应用：它会进入“重新发现 Agent 与 Skill”流程，不会重新显示首次初始化的“创建集中库 / 使用已有库 / 恢复备份”分支。

如需重新走首次初始化，请按以下步骤建立隔离状态：

1. 退出源码运行的 SkillHub，并确认任务管理器中没有残留的 SkillHub 进程。
2. 先保留原应用状态：将 `%APPDATA%\SkillHub` 重命名为 `%APPDATA%\SkillHub.acceptance-backup-YYYYMMDD-HHMMSS`。macOS 对应目录为 `~/Library/Application Support/SkillHub`。
3. 不要删除原集中库。首次“创建集中库”建议在向导中选择新的空目录，例如 `C:\SkillHub-acceptance-YYYYMMDD\library-first-run`；要测“已有库”或“恢复备份”时，再选择对应的测试目录。
4. 重新启动源码版本：`pnpm --dir apps/desktop tauri dev`。启动后应出现“选择初始化方式”页面，再按验收项选择创建、已有库或恢复备份。
5. 验收结束后如需恢复原环境，退出应用，确认新生成的 `%APPDATA%\SkillHub` 是本次测试目录后再移走，并将带时间戳的备份目录改回 `%APPDATA%\SkillHub`。不要凭路径猜测后直接删除。

只想测试已初始化应用的只读重新发现时，不需要隔离数据库，直接从设置页点击“重新打开初始化”即可。

## 固定验收记录表

以下顺序按用户实际使用旅程排列；M 编号保持稳定，便于和验收矩阵对应。

| 编号 | 验收项 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
|新增01|统一通知|---|---|新增统一的前端消息模块，所有同步、异步的结果通知都集中到一个消息中心中。见参考图样式。按钮固定在每个主页面右上角，可以点开滚动查看历史通知，位置如图。新消息以右上角从右向左滑动弹窗的方式展示消息内容，消息窗要固定宽度，高度度根据内容自适应。每一条消息停留展示2秒后自动向右滑动关闭，下面的消息往上顶。这个功能你可以看看有没有借鉴的方案，加载前端skill来实现。|（本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送）|
|新增02|每一页内容居上及返回按钮位置|---|---|查找并修复每一页及所有子页面中内容框是上下居中的情况，改为居上展示。每一页的子页面返回上一级页面的按钮统一下位置、样式。可以在顶部页签条增加向前和返回的按钮，这样内容区域就不用占空间了，看证据图标注。返回按钮也改为向左的箭头，统一。|举例：（本机证据截图，保存于用户本地，不随仓库推送）返回上一页按钮位置：（本机证据截图，保存于用户本地，不随仓库推送）|
|新增03|新增软件图标|||"assets\branding"此目录中是我为本项目创建的图标logo，需要更新进软件，现在软件启动后图片是一个纯黑的框。0.2.0版本需要加入这个图标。另外也看看软件内部哪些页面需要加入这个图标。|---|
| M-07 | 安装、启动、卸载 | 在 Windows 安装、启动、卸载；在 macOS 安装 DMG、检查 Gatekeeper、移除后重新安装 | 安装和启动正常；卸载/移除后用户数据按预期保留；重新安装可重新关联 | 版本发布前再验证 |  |
| M-28 | P1-05 旧库升级迁移 0011 | 用本轮之前版本产生的应用数据启动新版本（Windows；macOS 另行取证） | 打开无迁移错误；旧来源自动获得 `verified_upstream`/`local_only` 角色；搜索候选与来源功能正常；原数据无丢失 |  |  |
| M-31 | TC-US001 未覆盖分支补验 | 按“重新进入首次初始化”建立隔离状态，分别验证使用已有库、首次恢复、取消目录、空目录、恢复失败后重试，以及完成前返回并更换目录；已通过的创建集中库主流程不重复执行 | 完成前只记录选择，不创建或锁定集中库；最终提交才创建/打开所选库；取消、失败、重试、返回和切换路径状态准确；原应用状态可回滚；只读目录不被修改 | 新建库或者使用已有库，点击开始只读扫描很慢，弹出转为后台扫描，点击后点击完成初始化按钮。应该点击完成初始化后应该进入软件概览页面，等扫描完之后进行弹窗扫描结果再选择批量导入，但是实际并没有，仍然在等待初始化，等于点击完成初始化没有用。 |  |
| M-17 | P1-01/P1-02 壳层与控件统一 | 800×600～1280×900 调整导航与设置页；9 个主题查看品牌芯片；设置页技能库维护忽略项增删查、输入长值 | 折叠按钮为左上固定方形、可点击面积足够；设置导航固定仅内容滚动；单选/多选控件样式一致可辨；品牌文字对比度可读；忽略项无挤压、空态与错误可读 | 1、按钮太丑了，应该参考codex桌面版左上角的折叠按钮。<br />2、设置页中分页导航区域已经固定了，仅内容滚动验证通过。<br />3、数据保护、自动化、应用更新这3页的内容起始位置里离上面标题很远，有很大一片空白。每一页的内容起始位置应该是固定的，然后根据内容的多少进行滚动。 | （本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送） |
| M-03 | TC-GR-09 系统缩放与最小窗口 | 调整系统显示缩放和应用到最小窗口尺寸，检查标题、按钮、表格、长路径、弹窗 | 内容可读；页面可滚动；确认、取消、关闭始终可达 | 设置页面窗口化时分页导航改到了上面一行，这样不对，窗口化时应该100%比例全屏样式保持一致，分页导航还固定在左侧，内容在右侧滚动。大小可以缩放，布局不能变。<br />概览页窗口化时技能数据布局是对的，但是100%比例全屏布局变成一行了，应该以窗口化的概览页数据为2行展示布局为准，进行大小缩放即可，且应该网上顶到概览标题下面，不应该有空白区域，和窗口化时一致。 | （本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送） |
| M-04 | TC-GR-09 减少动效 | 开启系统“减少动效”，执行打开、切换、提交和错误流程 | 动效减少或关闭；状态仍有文字或非颜色反馈 | 设置页中就没有“减少动效”的设置，如果需要可以在通用分页中增加一个开关样式。参考自动化页的开关组件样式。 |  |
| M-05 | TC-GR-09 中英文 | 切换简体中文与 English，走查核心流程 | 无中英混用、截断或内部键名；核心操作含义一致 | 设置-网络与AI-数据范围显示：explicit_selection  这是什么意思？这个数据范围要展示什么？需要更直观。<br />供应商配置输入一个测试API key，点击测试此配置，提示是中英文混合。 | （本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送） |
| M-10 | P0-06 本轮回归与错误文案 | 走查概览、技能库、发现、详情、设置、导入、部署、移除、操作记录九个页面，中英文各一遍；窗口调整为 800×600、800×900、1024×900、1280×900 | 无内部键名、`[object Object]`、英文残片；页头与正文间距全应用一致；无重叠、无溢出 | 概览页100%全屏页头与正文间距有空白。<br />发现页100%全屏页头与正文间距有空白。<br />待处理页100%全屏页头与正文间距有空白。 | （本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送） |
| M-20 | P1-07 概览信息架构 | 100% 全屏与窗口化查看；逐个指标点击钻取 | 无大留白、指标卡高度协调；各指标钻取去向不重复且可达 | 概览页100%全屏页头与正文间距有空白。 | 概览页100%全屏页头与正文间距有空白 |
| M-27 | 密度与观感（P2-01、P2-02） | 100% 全屏对比概览/设置/发现三页页面节奏；长路径悬停 | 三页节奏一致、密度观感协调；长路径有完整 title 提示（如觉得过紧，在备注记录，按 token 调整） | 通过 |  |
| M-13 | P0-01/P1-03 LLM 配置闭环 | 按“端点→密钥→获取模型/测试”操作；未填密钥时观察按钮态与解释；保存后重启；执行清除凭据；用真实 GLM 凭据执行基础连接与模型级连接 | 前置不足禁用并解释；配置重启保留；清除后显示未配置；测试结果在抽屉内可读；凭据落盘进系统凭据管理器而非普通配置/日志/备份 | 供应商配置下拉菜单，选任何一个厂商，都显示手动配置。<br />使用真是api key仍然失败，我看cc-switch选择厂商后还有一个接口格式可选，是不是因为接口格式问题导致无法接入真是api，需要参考cc-switch源码中的配置厂商模型的实现进行修复，前端UI抽屉设计还挺好，继续保持。 | （本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送） |
| M-01 | TC-GR-08 只读目录 | 在 Windows/macOS 使用只读目录执行发现、导入、部署或移除 | 权限拒绝提示准确；原目录和原文件不被修改 | 暂不验证，版本发布前再验证，先验证功能 |  |
| M-02 | TC-GR-08 符号链接/目录联接 | 在 Windows/macOS 使用符号链接或目录联接，执行发现和移除 | 不重复计数；不越界删除用户原文件；异常有明确提示 |  |  |
| M-18 | P1-04/P1-06 发现页 | 本地扫描查看时间显示并“审查并导入”；在线页安装单个 Skill；仓库页制造失败并重试；查看品牌分组、不可用置底、排除操作；检查 ZCode 卡片可用性 | 时间为本地日期；导入携带本次扫描候选；单 Skill 安装不夹带同目录其他 Skill；失败逐仓可解释、可重试；排除=可撤销忽略规则且无磁盘删除入口；ZCode 依据 `~/.agents/skills` 显示可用 | 时间为本地日期验证通过。<br />本机发现页，发现agent比较多时，需要向下滚动到最后才找到本地发现区域和导入skill按钮，布局需要调整，使用前端相关skill调整。 |  |
| M-19 | P1-05 来源候选确认 | AI 搜索后将结果“登记为来源”或忽略；完成导入后查看来源状态；对已登记上游执行检查更新 | 候选不自动成为来源；确认并导入后来源为可验证上游且能检查更新；忽略后状态明确（当前版本忽略不可撤销） | AI还没接入，暂时无法验证。 |  |
| M-29 | P1-05 忽略候选不可撤销确认 | 发现页忽略一个候选后尝试恢复 | 界面如实呈现“忽略后当前版本不可撤销”，无虚假撤销入口 | 导入skill，从本地目录选择后点击添加到已选来源，但是前端选择来源那里还是空白的，必须点击读取已选目录候选才显示刚添加的目录。那这个添加到已选来源按钮对用户没反馈。应该添加一个目录，显示一个，添加一个显示一个，也能单独删除刚才添加的，或者全选删除目录。读取已选目录候选按钮触发的是扫描添加的目录有多少个skill这个动作，要区分开2个按钮的逻辑以及前端页面的反馈变化。 | （本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送） |
| M-14 | P0-03 导入冲突 | 用完全相同的 Skill 目录制造冲突；再造同名不同内容冲突；查看冲突对象展示 | 同内容显示“完全重复”；同名异内容显示名称冲突并给出 Skill 名称、别名、路径与详情入口；不以 UUID/哈希为主信息 | 我重复导入1个目录的所有skill，冲突检测显示没有需要处理的冲突。 |  |
| M-06 | TC-US046-M01 安全检查 | 使用包含敏感值的测试 Skill，执行基础安全检查和 AI 检查 | 确定性基础检查先执行；敏感值遮盖；原始 Skill 不被改写 |  |  |
| M-16 | P0-06 AI 回退文案三处 | 未配置 LLM 时分别执行：导入 AI 预检与跳过预检、在线 AI 搜索、详情“运行分析”、来源重连 | 全部为可读中英文案（无 `importWorkflow.errors.generic`、`[object Object]`），并说明确定性流程仍可继续 | 在线AI搜索，勾选AI检索实际没用，但是点击搜索仍然有结果，没有任何AI不可用的提示，应该点击AI搜索时就检查是否正确配置了AI，可以读取是否成功配置的记录吧？ | ， |
| M-21 | P1-08/P1-10/P1-11 技能库 | 卡片与表格分别执行搜索/筛选/分页/全选/批量删除；编辑别名与标签；卡片点击与“查看”分别验证；制造偏好加载失败与标签保存失败 | 两视图选择能力等价；表格末列横向可达；原名不变、双名展示；标签错误可读；批量条不遮挡分页；通知成功自动消退、危险常驻 | 首次打开技能库，仍然弹出提示：无法加载表格偏好，当前使用默认设置。如图。<br />视图切换及关系视图3个按钮可以放到顶部页签栏最右边，如图标注位置。<br />100%全屏比例下，表格末列仍然展示不全，没有左右滚动条。另外表格区域单独滚动，不随页面滚动，每页数量、上下翻页按钮的功能条压叠了表格<br />批量删除这次没有压叠，但是会跳转到表格最下面展示确认，有点丑，我想要的是从下到上的滑动抽屉，不要动原页面。<br />通知成功没有自动消退。参考新增的通知中心功能<br />抽屉中编辑别名，又把原名给改了，列表中也没有双名展示，需要修复。抽屉中我的用途，也要添加编辑按钮。如果用途中有用户设置的内容，列表中用途列就展示用户设置的内容，如果用途是空，就展示skill原始描述。<br />技能库页面设计还是不太好，搜索筛选区域和组合管理都不直观，如图，使用前端UIskill再设计一下吧，记一些视图切换按钮要放到最上面去，刚才其他条时有说明，一并考虑。 | <br />（本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送）（本机证据截图，保存于用户本地，不随仓库推送） |
| M-22 | P1-09 组合管理 | 从技能库入口进入；创建、重命名、删除组合；按名称与标签定位添加/移除成员；制造重名 | 组合视图直观展示成员；重名有明确错误；重命名生效；删除仅影响组合记录且有影响预览 |  |  |
| M-23 | P1-12 详情分层 | 打开详情页核对标题/描述/正文展示；查看确定性候选小节与 AI 分析层 | 描述不再多处重复；别名仅在编辑处展示；无 LLM 时确定性候选常显且 AI 层标注未运行 |  |  |
| M-24 | P1-13 项目页 | 注册本地项目抽屉全流程；查看卡片路径、访问徽章、关联 Agent、下一步；抽屉内联编辑 | 卡片信息完整可行动；徽章状态准确；无删除、健康分等后端未支持能力 |  |  |
| M-12 | P0-02/P1-14 Markdown 保存与联动滚动 | 编辑器双栏开关联动滚动；保存并创建副本；走“保存并替换”二次确认（观察默认焦点）；替换后查看版本回滚入口 | 联动无循环抖动且可关闭、重启记住选择；副本在库中可见且原 Skill 不变；替换有覆盖风险提示、可回滚 |  |  |
| M-15 | P0-04 部署流程 | 目标选择→预览→提交→结果；制造失败（如只读目标）并重试；每步返回上一步修改 | 失败有具体原因；预览有汇总与逐项结果；每步可回退；成功/失败数量与原文件边界清晰 |  |  |
| M-11 | P0-05 操作记录持久化 | 依次执行：创建 Skill、保存内容、保存副本、保存并替换、导入（含冲突决策与中途取消）、部署、取消部署、移除标签、移除部署关系、删除库中 Skill、移除忽略规则；打开操作记录页；重启应用后再查 | 每类操作出现对应记录，状态/时间/对象/失败原因可读；重启后仍可见；可恢复操作提供恢复入口；错误展示不含内部错误码 |  |  |
| M-25 | P1-15 重新发现完成动作 | 设置页进入只读重新发现，扫描后不滚动查看完成区 | 完成/重新扫描动作在固定操作区，无需滚动到列表底部 | 按钮不应该在滑动窗口内，应该在最下面按钮区域，与其他按钮同位置。如图标注。 | （本机证据截图，保存于用户本地，不随仓库推送） |
| M-26 | P1-15 四种移除语义 | 分别执行取消部署、移除标签、移除部署关系、删除库中 Skill，逐一核对名称、影响预览、确认与恢复说明 | 四者名称与入口唯一；影响预览与实际行为一致（含“保留目标文件”选项语义）；恢复方式明确；原文件保护不变 |  |  |
| M-30 | 0.2.0 Pi/DeepSeek Harness 文件接入 | Windows 分别准备 Pi 用户 `.pi/agent/skills`、`.agents/skills` 与项目 `.pi/skills`、`.agents/skills`；准备 DeepSeek Harness 用户 `.dsh/skills`、`.agents/skills` 与项目 `.dsh/skills`；分别执行发现、目录型 `SKILL.md` 扫描、复制/符号链接/目录联接部署、外部修改、解除部署和所有权检查；确认 DSH TUI/Web 目标共享逻辑目录；记录根级/扁平 Markdown、project trust、刷新和 runtime 观察结果 | 各 profile 仅发现约定范围内的目录型 `SKILL.md`；Pi 与 DSH 品牌/目标展示正确；发现、部署、解除部署、外部变化和所有权语义可解释；不把文件写入成功描述为 Agent 已加载/执行；不把未支持的根级/扁平 Markdown 当作已支持 | deepseek-harness和PI都没有适配的logo，PI可以参考cc-switch源码中的图标。deepseek应该可以网上找下鲸鱼的那个logo。目录倒是都识别到了没问题。 |  |
| M-08 | 备份与恢复 | 在 Windows 与 macOS 间双向备份恢复测试 Skill、版本、来源、项目和部署关系 | 业务数据恢复；系统凭据不进入备份；路径映射由用户确认 |  |  |
| M-09 | 更新包与签名 | 使用真实签名更新包，检查版本、Release 链接、SHA-256 和签名/公证状态 | 版本和链接准确；校验值一致；签名、未签名或未公证提示准确 | 版本发布前再验证 |  |
## 完成口径

当前表格中的 Windows/macOS 阻塞项全部有真实设备证据，且失败项关闭或明确豁免后，才能更新矩阵并评估 v0.2.0 正式发布。此前只能表述为“代码与自动化完成，人工/发布证据待完成”。
