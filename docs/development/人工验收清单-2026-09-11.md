# 人工验收清单（2026-09-11）

更新时间：2026-09-11

代码基线：以验收开始前实际构建的提交为准。

本文件同时作为后续人工验收的固定模板。每次新增验收只填写顶部信息和下方表格，不新增字段、不另建记录表；已完成内容归档，当前文件只保留待验收项目和本次结果。

## 当前状态

- 前端体验升级 Goal 的代码与浏览器自动化已完成；本清单只记录真实 Windows/macOS 桌面、文件系统、系统能力和真实服务证据。
- 2026-09-12：验收前开发第二轮已完成（P0-05 操作记录持久化、P0-06 错误可读性、桌面构建修复、P1-01～P1-15、P2-01～P2-03），全部为代码与自动化证据，未经真机验证；复测入口为下方固定表第 29 行起的下一轮验收项目，第 1–28 行历史记录保持不变。
- Windows 首轮记录的 P0-01～P0-06 均已有代码与自动化修复，仍需在真实桌面复测。macOS 既有部分记录仍需按新页面结构复测，并新增迁移 0011 旧库升级取证（第 47 行）。
- 2026-09-11 的用户授权 GLM 直连模型列表请求已成功（10 个模型）；它不是桌面真机验收，不能代替第 3–5 项的配置保存、系统凭据或两级连接证据。
- 已通过或历史原始记录见：`docs/development/archive/2026-09-11-人工验收旧清单/人工验收清单-2026-09-10.md`。

## 验收前填写

验收日期：

平台/设备：

系统版本：

应用版本：

## 使用规则

- 每次只执行一行；“结果”填写 `通过`、`失败` 或 `阻塞`，未执行保持空白。
- 证据只记录真实设备上的截图、录屏、日志或文件路径；浏览器预览、Playwright、Vitest 不算真机通过。
- 使用专用测试目录和虚构 Skill，不使用真实 Skill、真实凭据或个人数据；破坏性操作前先复制测试目录。
- 失败或阻塞时保留证据并停止相关破坏性步骤，必要时在“备注/证据”中写明问题编号。

## 重新进入首次初始化

设置页的“重新打开初始化”只适用于已初始化应用：它会进入“重新发现 Agent 与 Skill”流程，不会重新显示首次初始化的“创建集中库 / 使用已有库 / 恢复备份”分支。

如需重新走首次初始化，请按以下步骤建立隔离状态：

1. 退出源码运行的 SkillHub，并确认任务管理器中没有残留的 SkillHub 进程。
2. 先保留原应用状态：将 `%APPDATA%\SkillHub` 重命名为 `%APPDATA%\SkillHub.acceptance-backup-YYYYMMDD-HHMMSS`。macOS 对应目录为 `~/Library/Application Support/SkillHub`。
3. 不要删除原集中库。首次“创建集中库”建议在向导中选择新的空目录，例如 `C:\SkillHub-acceptance-YYYYMMDD\library-first-run`；要测“已有库”或“恢复备份”时，再选择对应的测试目录。
4. 重新启动源码版本：`pnpm --dir apps/desktop tauri dev`。启动后应出现“选择初始化方式”页面，再按验收项选择创建、已有库或恢复备份。
5. 验收结束后如需恢复原环境，退出应用，确认新生成的 `%APPDATA%\SkillHub` 是本次测试目录后再移走，并将带时间戳的备份目录改回 `%APPDATA%\SkillHub`。不要凭路径猜测后直接删除。

只想测试已初始化应用的只读重新发现时，不需要隔离数据库，直接从设置页点击“重新打开初始化”即可。

## 固定验收记录表

| 序号 | 验收项 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| 1 | T0 共享基础与主题 | 打开概览、技能库、发现、详情、设置、导入、部署、移除、操作记录；调整窗口到 800×600、800×900、1024×900、1280×900；切换 9 个主题 | 页面无重叠、无溢出；按钮/输入框/抽屉/弹窗可操作；Tab、焦点、禁用态、加载态正常；主题名称、顺序、持久化不变 | <br />左侧主导航区域折叠展开按钮现在是原型+箭头，希望改成方形的折叠展开按钮，位置调整到页面左上角固定，不用太大的图标<br />应该统一所有输入框的样式、下拉选项的样式，区分多选勾选和单选 |  |
| 2 | T1 设置分区导航 | 在 1280px 查看左侧分区导航，在 800–1024px 使用顶部选择器，逐项进入通用、数据保护、网络与 AI、自动化、技能库维护、应用更新 | 当前分区清晰；窄屏选择器可用且不遮挡内容；各分区可进入、返回 | 设置页面分区导航列表，应该固定，点击不同的列表，应该只需要页面变化，分区导航列表不应该总换位置，滚动条也应该值针对内容区域，除非分区导航列表太多放不下，才应该在分区导航区域增加单独的滚动条。其他没问题。<br />技能库维护页忽略项的UI设计有点丑。 |  |
| 3 | T1 供应商实体行与抽屉 | 查看供应商列表；新增、编辑、取消、关闭、保存；制造错误并重试；重启应用后再次查看 | 协议、端点、模型、启用、默认、凭据状态可读；长内容可换行；焦点恢复正常；凭据不回显 | <br />供应商配置中，获取模型列表是否应该先填写完API后才能点？顺序是不是应该调整下，API秘钥输入框放到API地址下面。点击测试此配置好像无效。我用了真实的zhipu GLM的key，点击获取模型列表提示：获取模型列表失败，可手动填写模型名称后保存。 |  |
| 4 | TC-US045-M01/M02 LLM 配置 | 保存 LLM 配置，重启应用，检查状态；执行清除凭据，再查看未配置状态 | 配置按预期保留；清除后凭据不可用且界面显示未配置；错误提示准确 | 没保存上LLM配置，也没看到清除凭证的操作在哪 |  |
| 5 | TC-US047-M01/M02 供应商凭据与连接 | 检查 Windows 凭据管理器或 macOS 钥匙串；执行真实供应商的基础连接测试和模型级连接测试 | 凭据落盘隔离、不出现在普通配置/日志/备份；两级连接结果准确，失败原因可定位 | 没验证到，可能需要先跑通保存LLM配置？ |  |
| 6 | T2 发现卡片 | 分别查看在线、仓库、lock 结果；检查不可安装项和 AI 回退状态 | 统一卡片样式；来源、状态、不可安装和 AI 回退语义清晰；操作可达 | 发现页面中4个区域框与上面标题中间有空白，这是特意设计么？<br />本地发现业：上次扫描： 1789114968，这是时间戳？应该是具体日期比较好吧。重新扫描，发现 58 个待导入候选，但是点击后面审查并导入进入到导入skill流程，没有把本次扫描结果带过来。是一个从0开始的导入skill流程。同样这个本地发现页面布局有很多空白，如何设计更美观？<br />在线发现页：勾选AI搜索辅助，但是我测试的时候LLM没配置好，点击搜索后没有任何提示。选择一个skill安装，它是下载到一个路径，如果在走导入skill这个固定流程，会把这个路径中其他的skill都识别出来，这里应该走单独的skill安装导入吧？再看看cc-switch的实现方案如何更好的与这个项目结合。<br />仓库发现页面和内部功能都不太好，再参考下cc-switch的实现方案以及前端。而且我点扫描仓库，提示都扫描失败了 |  |
| 7 | T2 卡片响应式与品牌回退 | 调整窗口宽度，查看 3/2/1 列；使用已知品牌和未知品牌数据 | 卡片无重叠；窄窗口操作区可达；品牌图标正常，未知品牌有首字母/颜色回退 | 通过，整体没问题，但还能美化一些，品牌图标再大一些，文字首字母大写对，但是文字的颜色看不清楚，可能文字颜色太浅了。每个品牌中的agent形式，直接显示出类型更友好，比如能识别出桌面端、命令行、IDE编辑器、IDE插件这种，你可以网上搜索下对应的类型。然后可用的按照品牌归类，所有不可用的放到单独一类，放到最下面。仅发现相关目录和不可用的可以提供排除和删除操作。如果同一品牌下不同类型共用同一个skills目录，那就合并卡片，展示上用例如：桌面端/IDE  这样展示。单独解决一下zcode不可用的问题。 |  |
| 8 | T3 概览 | 打开概览并调整窗口、缩放；对照首要指标、统计和图表明细 | 首要指标层级清晰；图表、明细和文字在缩放后仍可读 | 概览页，100%比例时，上面概览标题和下面技能总数的框中间隔了很大空白。技能总数下面几个数据框，几个已配置的agent、几个已发现的agent等，高度有点矮，不协调。窗口化时，概览页看着还行，100%比例全屏时，要调整下各块的大小和布局。而且几个已配置的agent和几个已发现的agent都跳转到Agent页面，有点重复了。需要再考虑下概览页保留哪些模块，跳转哪个页面。 |  |
| 9 | T3 Skill 库视图 | 打开 Skill 库，确认默认视图；通过“查看方式”切换卡片/表格；执行搜索、筛选、分页和批量选择 | 默认是卡片视图；表格可切换；两种视图的搜索、筛选、分页、选择均可用 | <br />通过<br />其他问题：技能库页面：卡片视图没有全选；搜索skill框上面显示：无法加载表格偏好，当前使用默认设置。重试。不知道这个是什么，有没有用。技能库页面上面功能区域太多了，挤压了下方技能库列表，看看再重新设计下，有些功能区域合并，并提供上下折叠按钮。比如最上面那一条的全部skill、活跃等等视图快捷筛选区域是否可以和搜索下来筛选区域金融融合，看看那怎么设计下。视图模式、分组方式属于偏页面设置的，可以固定放到页面右上角，换成图标方式切换。；组合管理是单独的一块功能，目前是单独页面，但是进入按钮应该设计放到技能库哪个位置，要考虑一下，并且组合管理的页面也要重新设计，组合管理应该包含组合的增删改查功能，应该支持通过标签筛选，或者名字搜索直接定位到skill添加到组合中，组合管理页面应该是基于一个组合的视图方式，要能直观看出一个组合当前包含哪些skill。；再技能抽屉中编辑别名，不应该修改skill的原名，技能库中卡片和列表应该同时展示原名和别名，别名可以小一点，技能抽屉中原始描述应该固定高度，内容太多的技能可以加上小滚动条，这样不会挤压下面区域。<br />卡片视图中，点击skill的卡片任意区域就应该弹出抽屉，查看按钮直接跳转当前skill的完整详情页，而不应该点查看是抽屉。<br />技能库列表表格视图，100%比例下列表框的左右滑动条没了，看不到后面的字段。<br />技能库抽屉中的各种标签好像还没链接后端功能，需要继续完善，链接具体的功能。 |  |
| 10 | T3 Skill 库批量操作 | 使用长名称、长路径、标签和不同状态的 Skill；选择至少 2 个执行批量删除 | 内容和按钮不溢出；二次确认清晰；影响矩阵完整且无压叠；原文件保护符合预期 | 批量删除的模块还是会和下一页下一页操作框、已选择的操作框有重叠覆盖，批量删除这个是否可以放在单独页面更好？批量删除后，最下面有有多出一个批量操作汇总（共 2 项）的文本框，又挤压了技能库列表了。就像技能库这一页的各种操作结果提示框，是否统一设置一种类型，比如从下往上的抽屉，定时1秒自动关闭，或用户随便点击任何地方关闭。 |  |
| 11 | T3 Skill 详情五区 | 打开详情页，依次查看身份、状态、正文、关系、生命周期；在窄窗口滚动分区导航并访问旧锚点 | 五区完整、层级清晰；旧锚点仍可达；窄屏导航可横向滚动；长正文可读 | skill完整详情页，需要重新设计，原名称应该在分页导航区域加粗更好看，也不用再单独展示描述了，因为下面有markdown区域，里面也有更详细的展示。身份也不用展示描述了。状态中也不用展示描述<br />详情中检查来源更新的逻辑是不是有问题，我测试一个网上下载的skill，也提示：此 Skill 没有可检查更新的上游来源（本地创建或仅登记了本地目录）。本地文件变化请在健康检查与外部变化中查看。这块是否要接入发现的功能联网检索找更好，需要重新设计下这块。<br />关系中点击运行分析，提示：分析失败：[object Object]；确定性候选仍然可用。 这个正常么？因为大模型我没配置。感觉这个提示不是很明确。 |  |
| 12 | T3 Agent/项目实体卡片 | 查看长路径、已知品牌、未知品牌；打开注册/编辑抽屉并取消、保存 | 长路径可读；品牌图标和未知回退正确；抽屉关闭、取消、保存、焦点恢复正常 | Agent页面通过，暂无问题<br />项目页面中注册本地项目的抽屉页面需要重新设计。项目中注册的项目卡片的内容几乎没有，要参考下最初需求，重新设计页面及功能。 |  |
| 13 | T3 Markdown 内容 | 查看标题、宽表格、图片占位、代码块、Mermaid；编辑后保存并触发校验 | 标题层级正确；宽表格可滚动；占位和代码块可读；保存/校验状态准确；滚动顺畅 | 阅读源码等都没问题。编辑还是有点问题，点击编辑后，左侧是源码编辑器，右侧是实时预览，这两块能联动滚动么？现在是两边各自滚动条，不高级。应该统一1个滚动条，左右内容对应。保存的按钮，之前说过，除了保存并创建副本的选项，还应该有一个保存替换的选项，直接修改原skill，提示用户后面如果更新可能会覆盖掉当前的改动，建议另存副本，然后让用户二次确认就行了。另外点了保存为副本提示：无法保存，本机草稿仍然保留。这块功能好像有问题。 |  |
| 14 | T4 初始化与重新发现 | 先按“重新进入首次初始化”步骤隔离应用状态；在完成初始化前反复返回第一步并更换目录；分别执行取消目录、空目录、已有库、首次恢复失败后重试；若发现旧版本已提前激活同一路径，重启后继续该路径；恢复原环境后再从设置页进入只读重新发现：TC-US001-M01～M05 | 完成初始化前只记录选择，不创建或锁定集中库；最终提交时才创建/打开所选库；旧版本遗留的同路径激活可继续完成，不同路径仍阻止切换；步骤状态、失败、取消、重试和底部操作区可用；只读目录不被修改；原应用状态可回滚 | 首次初始化没问题。<br />重新发现流程的最后一步点击开始只读扫描后，是在结果列表最下面有一个完成并进入批量导入的按钮。这个按钮应该放在外面，与完成重新扫描放一起，参考首次初始化的最后一步的实现方式。 |  |
| 15 | T4 导入流程 | 依次执行来源、候选、冲突、完成；制造错误并取消后恢复 | 四步流程清晰；冲突处理准确；底部操作区不遮挡；错误和取消后可恢复 | 处理冲突步骤中，冲突类型是否有优先级，我试了一模一样的skill进行冲突分析，分析冲突结果是名称重复，显示内容差异：运行时名称相同，但内容不同。实际内容是一模一样的。然后与已有skill冲突显示的好像还是哈希值？比如：与已有 Skill 冲突：`53ad610b-fb46-4c54-9d6c-30a3bb2949df``a5bf15f9-0c43-4991-81d7-ba342c5619c7``b6e1c8fc-3749-4b94-9907-43cc7a9d3006``c75168c7-a927-45cc-adf9-fc4b6f07ee1e``d8124708-8ffd-4080-9e41-1d4612396bf1`   需要更准确的信息，要给用户更明确具体跟哪个skill重复。<br />这个冲突分析的逻辑需要再重新设计下。 |  |
| 16 | T4 部署流程 | 选择目标，查看影响预览，提交部署，查看结果；检查批量风险 | 选择→预览→提交→结果完整；风险与提交动作相邻；操作区始终可达 | 部署skill的页面，第二步和第三步都是在第一步选择agent的最下面，还需要拉滚动条到下面，不合理，应该切换页面。点击预览部署，没有清晰的提示语。目前是只有托管复制么？不是应该优先软连接么？提交部署失败没有具体原因。另外每一步不能回退上一步修改。我还看到一个提示：每个 Skill 都会先单独预览；只有全部预览成功后才可提交。这是啥意思？如果多个skill，要一个一个预览？没必要吧，批量操作，应该也需要批量给结果。预览是什么作用？不能直接导入么？导入失败的单独列出来，成功的直接告诉用户结果数量就行了。 |  |
| 17 | T4 移除流程 | 选择 Skill 执行移除，查看影响预览并确认；检查取消路径 | 影响范围明确；确认/取消可用；原文件保护和结果反馈正确 | 没看懂这个移除是哪个流程，是移除标签还是取消部署？所以这个流程要设计的再清晰一些。 |  |
| 18 | T4 操作记录 | 查看时间线、进行中任务、失败码、恢复入口和长文本 | 状态、失败码、恢复入口和长文本清晰；刷新或重启后记录符合预期 | 操作记录里面没有任何记录，显示：尚未有任何操作记录。执行导入、部署等操作后会在这里显示。实际我测试各种功能操作了很多，所以这块功能要检查一下，应该记录用户所有的决策动作。 |  |
| 19 | TC-GR-08 只读目录 | 在 Windows/macOS 使用只读目录执行发现、导入、部署或移除 | 权限拒绝提示准确；原目录和原文件不被修改 |  |  |
| 20 | TC-GR-08 符号链接/目录联接 | 在 Windows/macOS 使用符号链接或目录联接，执行发现和移除 | 不重复计数；不越界删除用户原文件；异常有明确提示 |  |  |
| 21 | TC-GR-09 系统缩放与最小窗口 | 调整系统显示缩放和应用到最小窗口尺寸，检查标题、按钮、表格、长路径、弹窗 | 内容可读；页面可滚动；确认、取消、关闭始终可达 |  |  |
| 22 | TC-GR-09 减少动效 | 开启系统“减少动效”，执行打开、切换、提交和错误流程 | 动效减少或关闭；状态仍有文字或非颜色反馈 |  |  |
| 23 | TC-GR-09 中英文 | 切换简体中文与 English，走查核心流程 | 无中英混用、截断或内部键名；核心操作含义一致 |  |  |
| 24 | TC-US046-M01 安全检查 | 使用包含敏感值的测试 Skill，执行基础安全检查和 AI 检查 | 确定性基础检查先执行；敏感值遮盖；原始 Skill 不被改写 |  |  |
| 25 | 安装、启动、卸载 | 在 Windows 安装、启动、卸载；在 macOS 安装 DMG、检查 Gatekeeper、移除后重新安装 | 安装和启动正常；卸载/移除后用户数据按预期保留；重新安装可重新关联 |  |  |
| 26 | 备份与恢复 | 在 Windows 与 macOS 间双向备份恢复测试 Skill、版本、来源、项目和部署关系 | 业务数据恢复；系统凭据不进入备份；路径映射由用户确认 |  |  |
| 27 | 更新包与签名 | 使用真实签名更新包，检查版本、Release 链接、SHA-256 和签名/公证状态 | 版本和链接准确；校验值一致；签名、未签名或未公证提示准确 |  |  |
| 28 |  |  |  | 1、初始化流程中品牌 Logo 字感觉都是灰色的，不清晰。<br />2、导入skill页面，运行AI预检，当没配置大模型是提示：importWorkflow.errors.generic，应该是一个明确的提示才对。再点击跳过预检提示：已跳过 AI 预检；基础检查与确定性门照常生效，可以直接继续导入。importWorkflow.errors.generic，也有英文这句话。 |  |
| 29 | 本轮回归与错误文案走查（P0-06、P2-01） | 走查概览、技能库、发现、详情、设置、导入、部署、移除、操作记录九个页面，中英文各一遍；窗口调整为 800×600、800×900、1024×900、1280×900 | 无内部键名、`[object Object]`、英文残片；页头与正文间距全应用一致；无重叠、无溢出 |  |  |
| 30 | 操作记录持久化（P0-05） | 依次执行：创建 Skill、保存内容、保存副本、保存并替换、导入（含冲突决策与中途取消）、部署、取消部署、移除标签、移除部署关系、删除库中 Skill、移除忽略规则；打开操作记录页；重启应用后再查 | 每类操作出现对应记录，状态/时间/对象/失败原因可读；重启后仍可见；可恢复操作提供恢复入口；错误展示不含内部错误码 |  |  |
| 31 | Markdown 保存与联动滚动（P0-02、P1-14） | 编辑器双栏开关联动滚动；保存并创建副本；走"保存并替换"二次确认（观察默认焦点）；替换后查看版本回滚入口 | 联动无循环抖动且可关闭、重启记住选择；副本在库中可见且原 Skill 不变；替换有覆盖风险提示、可回滚 |  |  |
| 32 | LLM 配置闭环（P0-01、P1-03，TC-US045/US047） | 按"端点→密钥→获取模型/测试"操作；未填密钥时观察按钮态与解释；保存后重启；执行清除凭据；用真实 GLM 凭据执行基础连接与模型级连接 | 前置不足禁用并解释；配置重启保留；清除后显示未配置；测试结果在抽屉内可读；凭据落盘进系统凭据管理器而非普通配置/日志/备份 |  |  |
| 33 | 导入冲突（P0-03） | 用完全相同的 Skill 目录制造冲突；再造同名不同内容冲突；查看冲突对象展示 | 同内容显示"完全重复"；同名异内容显示名称冲突并给出 Skill 名称、别名、路径与详情入口；不以 UUID/哈希为主信息 |  |  |
| 34 | 部署流程（P0-04） | 目标选择→预览→提交→结果；制造失败（如只读目标）并重试；每步返回上一步修改 | 失败有具体原因；预览有汇总与逐项结果；每步可回退；成功/失败数量与原文件边界清晰 |  |  |
| 35 | AI 回退文案三处（P0-06） | 未配置 LLM 时分别执行：导入 AI 预检与跳过预检、在线 AI 搜索、详情"运行分析"、来源重连 | 全部为可读中英文案（无 `importWorkflow.errors.generic`、`[object Object]`），并说明确定性流程仍可继续 |  |  |
| 36 | 壳层与控件统一（P1-01、P1-02） | 800×600～1280×900 调整导航与设置页；9 个主题查看品牌芯片；设置页技能库维护忽略项增删查、输入长值 | 折叠按钮为左上固定方形、可点击面积足够；设置导航固定仅内容滚动；单选/多选控件样式一致可辨；品牌文字对比度可读；忽略项无挤压、空态与错误可读 |  |  |
| 37 | 发现页（P1-04、P1-06） | 本地扫描查看时间显示并"审查并导入"；在线页安装单个 Skill；仓库页制造失败并重试；查看品牌分组、不可用置底、排除操作；检查 ZCode 卡片可用性 | 时间为本地日期；导入携带本次扫描候选；单 Skill 安装不夹带同目录其他 Skill；失败逐仓可解释、可重试；排除=可撤销忽略规则且无磁盘删除入口；ZCode 依据 `~/.agents/skills` 显示可用 |  |  |
| 38 | 来源候选确认（P1-05） | AI 搜索后将结果"登记为来源"或忽略；完成导入后查看来源状态；对已登记上游执行检查更新 | 候选不自动成为来源；确认并导入后来源为可验证上游且能检查更新；忽略后状态明确（当前版本忽略不可撤销） |  |  |
| 39 | 概览信息架构（P1-07） | 100% 全屏与窗口化查看；逐个指标点击钻取 | 无大留白、指标卡高度协调；各指标钻取去向不重复且可达 |  |  |
| 40 | 技能库（P1-08、P1-10、P1-11） | 卡片与表格分别执行搜索/筛选/分页/全选/批量删除；编辑别名与标签；卡片点击与"查看"分别验证；制造偏好加载失败与标签保存失败 | 两视图选择能力等价；表格末列横向可达；原名不变、双名展示；标签错误可读；批量条不遮挡分页；通知成功自动消退、危险常驻 |  |  |
| 41 | 组合管理（P1-09） | 从技能库入口进入；创建、重命名、删除组合；按名称与标签定位添加/移除成员；制造重名 | 组合视图直观展示成员；重名有明确错误；重命名生效；删除仅影响组合记录且有影响预览 |  |  |
| 42 | 详情分层（P1-12） | 打开详情页核对标题/描述/正文展示；查看确定性候选小节与 AI 分析层 | 描述不再多处重复；别名仅在编辑处展示；无 LLM 时确定性候选常显且 AI 层标注未运行 |  |  |
| 43 | 项目页（P1-13） | 注册本地项目抽屉全流程；查看卡片路径、访问徽章、关联 Agent、下一步；抽屉内联编辑 | 卡片信息完整可行动；徽章状态准确；无删除、健康分等后端未支持能力 |  |  |
| 44 | 重新发现完成动作（P1-15） | 设置页进入只读重新发现，扫描后不滚动查看完成区 | 完成/重新扫描动作在固定操作区，无需滚动到列表底部 |  |  |
| 45 | 四种移除语义（P1-15） | 分别执行取消部署、移除标签、移除部署关系、删除库中 Skill，逐一核对名称、影响预览、确认与恢复说明 | 四者名称与入口唯一；影响预览与实际行为一致（含"保留目标文件"选项语义）；恢复方式明确；原文件保护不变 |  |  |
| 46 | 密度与观感（P2-01、P2-02） | 100% 全屏对比概览/设置/发现三页页面节奏；长路径悬停 | 三页节奏一致、密度观感协调；长路径有完整 title 提示（如觉得过紧，在备注记录，按 token 调整） |  |  |
| 47 | 旧库升级迁移 0011（P1-05） | 用本轮之前版本产生的应用数据启动新版本（Windows；macOS 另行取证） | 打开无迁移错误；旧来源自动获得 verified_upstream/local_only 角色；搜索候选与来源功能正常；原数据无丢失 |  |  |
| 48 | 忽略候选不可撤销确认（P1-05） | 发现页忽略一个候选后尝试恢复 | 界面如实呈现"忽略后当前版本不可撤销"，无虚假撤销入口 |  |  |

## 完成口径

当前表格中的 Windows/macOS 阻塞项全部有真实设备证据，且失败项关闭或明确豁免后，才能更新矩阵并评估 v0.2.0 正式发布。此前只能表述为“代码与自动化完成，人工/发布证据待完成”。
