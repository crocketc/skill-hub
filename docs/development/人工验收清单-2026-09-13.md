# 人工验收清单（当前待验收项）

更新时间：2026-09-13

代码基线：以验收开始前实际构建的提交为准（分支 `codex/acceptance-optimization-2026-09-13`）。

本文件只保留尚未完成的真实 Windows/macOS 桌面、文件系统、系统能力和真实服务验收项。上一轮完整清单（含用户反馈原文、结果与历史上下文）已归档至：
`docs/development/archive/2026-09-13-验收反馈优化前/人工验收清单-2026-09-10.md`。

说明：标注"复验"的条目是 2026-09-12 反馈中已由本轮代码与自动化闭环的部分，本轮需真实桌面确认后才能更新矩阵；此前未验证的条目原样保留。自动化通过不构成真机通过。

## 验收前填写

验收日期：

平台/设备：

系统版本：

应用版本：

## 使用规则

- 每次只执行一项；"结果"填写 `通过`、`失败` 或 `阻塞`，未执行保持空白。
- 证据只记录真实设备上的截图、录屏、日志或文件路径；浏览器预览、Playwright、Vitest 不算真机通过。
- 使用专用测试目录和虚构 Skill，不使用真实 Skill、真实凭据或个人数据；破坏性操作前先复制测试目录。
- 失败或阻塞时保留证据并停止相关破坏性步骤，必要时在"备注/证据"中写明问题编号。

## 重新进入首次初始化

设置页的"重新打开初始化"只适用于已初始化应用：它会进入"重新发现 Agent 与 Skill"流程，不会重新显示首次初始化的"创建集中库 / 使用已有库 / 恢复备份"分支。

如需重新走首次初始化，请按以下步骤建立隔离状态：

1. 退出源码运行的 SkillHub，并确认任务管理器中没有残留的 SkillHub 进程。
2. 先保留原应用状态：将 `%APPDATA%\SkillHub` 重命名为 `%APPDATA%\SkillHub.acceptance-backup-YYYYMMDD-HHMMSS`。macOS 对应目录为 `~/Library/Application Support/SkillHub`。
3. 不要删除原集中库。首次"创建集中库"建议在向导中选择新的空目录，例如 `C:\SkillHub-acceptance-YYYYMMDD\library-first-run`；要测"已有库"或"恢复备份"时，再选择对应的测试目录。
4. 重新启动源码版本：`pnpm --dir apps/desktop tauri dev`。启动后应出现"选择初始化方式"页面，再按验收项选择创建、已有库或恢复备份。
5. 验收结束后如需恢复原环境，退出应用，确认新生成的 `%APPDATA%\SkillHub` 是本次测试目录后再移走，并将带时间戳的备份目录改回 `%APPDATA%\SkillHub`。不要凭路径猜测后直接删除。

只想测试已初始化应用的只读重新发现时，不需要隔离数据库，直接从设置页点击"重新打开初始化"即可。

## 固定验收记录表

以下顺序按用户实际使用旅程排列；M 编号保持稳定，便于和验收矩阵对应。

| 编号 | 验收项 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| 新增01 复验 | 统一通知中心 | 触发成功/失败/危险通知；观察右上角铃铛与未读数、弹窗 2 秒自动消退、历史抽屉滚动查看；进入技能详情等隐藏顶栏的页面确认铃铛仍可达 | 所有同步/异步结果集中到消息中心；新消息右上滑入、固定宽度、2 秒滑出；历史可滚动查看；详情页通知入口可达 |  | 2026-09-12 反馈已实现为全局通知中心，本轮复验观感与位置 |
| 新增02 复验 | 每页内容居上与返回按钮 | 逐页检查内容是否居上；子页面用顶栏返回/前进图标按钮回退；确认正文无重复返回链接（技能详情页保留其页内返回属预期设计） | 内容容器统一居上；返回/前进在顶栏统一位置与样式（向左箭头）；正文无重复返回入口 |  | 本轮已移除发现子页/组合管理页的页内返回，由顶栏承载 |
| 新增03 复验 | 软件图标 | 安装/启动应用查看图标；检查窗口图标与内部页面品牌位 | 应用图标不再是纯黑框；启动器/任务栏/安装器显示新图标 |  | 图标已进 bundle；真机安装后确认 |
| M-31 复验 | 初始化后台扫描 | 按"重新进入首次初始化"建立隔离状态；新建库/已有库后开始只读扫描，弹出转后台时选择后台，点击完成初始化 | 点击完成后立即进入概览；扫描后台继续；完成后通知中心出现扫描结果并可跳转批量导入；失败/取消路径反馈准确 |  | 2026-09-12 反馈（完成初始化无效）已修复，本轮复验 |
| M-25 复验 | 重新发现完成动作 | 设置页进入只读重新发现，扫描后观察完成/重新扫描按钮位置 | 完成动作在底部稳定按钮区，不随列表滚动 |  | 已移入稳定操作区，本轮复验 |
| M-17 复验 | 折叠按钮与设置页布局 | 观察左上角折叠按钮观感；设置各分页内容起始位置 | 折叠按钮观感改善且可点击面积足够；数据保护/自动化/应用更新页内容起始位置贴顶、按内容滚动 |  | 本轮重绘折叠按钮并统一钉顶 |
| M-03 复验 | 缩放与最小窗口布局 | 系统缩放与最小窗口间切换；概览页窗口化与全屏对比 | 布局结构不变（左侧导航恒定）；概览指标以两行为准仅缩放；无空白横带 |  | 概览两行指标契约已落地 |
| M-04 复验 | 减少动效 | 系统减少动效开启/关闭 + 设置-通用"始终减少动效"开关 | 两通道均生效；动效减少时状态仍有文字/非颜色反馈 |  | 新增通用设置开关 |
| M-05 复验 | 中英文与文案可读性 | 查看 设置-网络与AI-数据范围；触发连接测试错误 | 无内部枚举（如 `explicit_selection`）泄漏；发送范围有用户语言解释；测试提示无中英混排 |  | 枚举已映射用户语言 |
| M-10 复验 | 九页错误文案与间距 | 走查九个页面，中英文各一遍；窗口 800×600～1280×900 | 无键名/`[object Object]`/英文残片；页头与正文间距一致无空白带；无重叠溢出 |  |  |
| M-20 复验 | 概览信息架构 | 100% 全屏与窗口化对比；逐个指标点击钻取 | 两行指标区紧贴标题下；无大留白；钻取去向不重复且可达 |  | 两行指标已实现，本轮复验全屏观感 |
| M-13 复验（UI 部分） | LLM 配置闭环 | 供应商下拉逐个选择观察预设回填；端点→密钥→获取模型/测试状态机；重启保留；清除凭据 | 选择厂商后显示对应预设而非一律"手动配置"；协议族/端点/模型推导正确；前置不足禁用并解释；测试结果抽屉内可读 |  | 预设与协议族链路已修复；真实凭据部分见 M-13（发布阻塞） |
| M-16 复验 | AI 回退与可用性提示 | 未配置 LLM 时：在线发现勾选 AI 检索后点击搜索；导入 AI 预检；详情分析；来源重连 | 勾选 AI 即提示可用性状态（可用/未配置/被禁用/连接未知），不可用时不出伪 AI 结果并提供设置入口；四条路径文案可读 |  |  |
| M-18 复验 | 发现页 | 本地扫描、在线安装单 Skill、仓库失败重试、品牌分组、排除、ZCode 卡片 | 本地发现与导入入口无需滚动即可见（Agent 多时折叠后置）；时间本地化；失败逐仓可解释；排除=可撤销忽略规则 |  | 工作台前置已实现 |
| M-19 | 来源候选确认 | AI 搜索后将结果"登记为来源"或忽略；导入后查来源状态；对已登记上游检查更新 | 候选不自动成为来源；确认导入后为可验证上游；忽略状态明确（当前版本忽略不可撤销） |  | 需先完成 M-16 的 AI 配置 |
| M-29 复验 | 导入已选来源反馈 | 添加多个本地目录（含仅大小写不同的重复路径）；观察已选列表即时显示；单删/多选删/全删；扫描后观察每目录候选数与失败重试 | 添加即显示、可单独删除；扫描为独立主动作；每目录候选数/失败/重试独立呈现；重复目录去重并聚焦已有项；返回/取消保留选择 |  | 分层与去重已实现 |
| M-14 复验 | 导入冲突 | 重复导入同一目录全部 Skill；再造同名不同内容冲突 | 完全重复显示"完全重复"并要求决策，不再显示"没有需要处理的冲突"；同名异内容给出名称/别名/路径与详情入口 |  | 根因已修复，本轮复验 |
| M-21 复验 | 技能库工作台 | 搜索/筛选/分页/全选/批量删除；卡片与表格切换；编辑别名与用途；制造偏好加载失败与标签保存失败 | 视图切换按钮在工具栏右缘；表格独立滚动+末列可达；批量删除为底部抽屉；首次打开无误报偏好错误；别名不改动原名且双名展示；用途列展示用户设置内容 |  | 七项反馈已闭环，本轮逐项复验 |
| M-30 复验 | Pi/DeepSeek Harness 品牌与文件接入 | 发现/部署流程查看 Pi 与 DeepSeek Harness 品牌图标；执行目录型 SKILL.md 发现、部署、外部修改、解除部署 | 两者有品牌 logo（不再缺失）；目录识别与部署语义正确；不把文件写入成功描述为 Agent 已加载 |  | logo 采用 lobe-icons 官方来源；文件接入语义复验 |
| M-22 | 组合管理 | 创建、重命名、删除组合；按名称与标签定位成员；制造重名 | 组合视图直观；重名有明确错误；删除仅影响组合记录且有影响预览 |  |  |
| M-23 | 详情分层 | 打开详情页核对标题/描述/正文；确定性候选小节与 AI 分析层 | 描述不重复；别名仅编辑处展示；无 LLM 时确定性候选常显且 AI 层标注未运行 |  |  |
| M-24 | 项目页 | 注册本地项目抽屉全流程；卡片路径/访问徽章/关联 Agent/下一步；内联编辑 | 卡片信息完整可行动；徽章准确；无删除、健康分等未支持能力 |  |  |
| M-12 | Markdown 保存与联动滚动 | 双栏联动滚动开关；保存并创建副本；保存并替换二次确认；版本回滚入口 | 联动无循环抖动可关闭重启记住；副本可见原 Skill 不变；替换有风险提示可回滚 |  |  |
| M-15 | 部署流程 | 目标选择→预览→提交→结果；制造失败并重试；每步返回修改 | 失败有具体原因；预览汇总+逐项结果；每步可回退；成功/失败数量与原文件边界清晰 |  |  |
| M-11 | 操作记录持久化 | 依次执行创建/保存/副本/替换/导入（含冲突决策与取消）/部署/取消部署/移除标签/移除部署关系/删除/移除忽略规则；重启后查操作记录页 | 每类操作有记录，状态/时间/对象/失败原因可读；重启可见；可恢复操作有入口 |  |  |
| M-06 | 安全检查 | 使用含敏感值的测试 Skill 执行基础安全检查和 AI 检查 | 确定性基础检查先执行；敏感值遮盖；原始 Skill 不被改写 |  |  |
| M-28 | 旧库升级迁移 | 用本轮之前版本的应用数据启动新版本（Windows；macOS 另行取证） | 无迁移错误；旧来源自动获得角色；搜索候选与来源功能正常；原数据无丢失 |  |  |
| M-01 | 只读目录 | Windows/macOS 只读目录执行发现、导入、部署或移除 | 权限拒绝提示准确；原目录和原文件不被修改 |  | 版本发布前必须验证 |
| M-02 | 符号链接/目录联接 | Windows/macOS 使用符号链接或联接执行发现和移除 | 不重复计数；不越界删除用户原文件；异常有明确提示 |  |  |
| M-07 | 安装、启动、卸载 | Windows 安装、启动、卸载；macOS DMG、Gatekeeper、移除重装 | 安装启动正常；卸载后用户数据按预期保留；重装可重新关联 |  | 版本发布前必须验证 |
| M-08 | 备份与恢复 | Windows 与 macOS 间双向备份恢复 | 业务数据恢复；系统凭据不进入备份；路径映射由用户确认 |  |  |
| M-09 | 更新包与签名 | 真实签名更新包检查版本、链接、SHA-256、签名/公证 | 版本链接准确；校验值一致；签名状态提示准确 |  | 版本发布前必须验证 |
| M-13（发布阻塞） | 真实凭据两级连接 | 使用真实供应商凭据执行基础连接与模型级连接；检查凭据落盘位置 | 凭据落盘进系统凭据管理器而非普通配置/日志/备份；两级连接成功且有证据 |  | 发布阻塞项，使用真实 API key 时执行 |

## 完成口径

当前表格中的 Windows/macOS 阻塞项全部有真实设备证据，且失败项关闭或明确豁免后，才能更新矩阵并评估 v0.2.0 正式发布。此前只能表述为"代码与自动化完成，人工/发布证据待完成"。
