# 人工验收清单（2026-09-18）

更新时间：2026-09-21（**当前执行版**）。本文件只保留尚未执行的 Windows/macOS release 人工验收流程；已完成的自动化证据、旧版验收表和历史现场结论已归档至 `docs/development/archive/2026-09-21-人工验收清单整理/`。本文件是人工验收的唯一当前入口。

> **状态边界**：DEV-2—65 的代码与自动化验证已完成，但本文件中的人工结果均未预填。DEV-1 需要 macOS 第二个真实可写卷；DEV-8 需要可信的官方 WorkBuddy 绿色 logo 素材。未完成真实操作、截图和日志复核，不得写“人工通过”。

## 被测产物与记录规则

| 项 | 当前要求 |
|---|---|
| 被测产物 | `target/release/skillhub-desktop.exe`；验收开始前记录构建时间，并确认源码 HEAD 与构建记录一致。macOS 使用同一源码提交生成的对应 release 产物。 |
| 平台记录 | 每条流程记录平台、系统版本、窗口尺寸、语言、主题、产物版本和源码 HEAD。 |
| 证据记录 | 每条流程填写“结果、截图/日志证据、现场清理”；失败保留现场，不先清理再补写。证据不得包含密钥、个人 Skill 内容或个人绝对路径。 |
| 反馈分类 | 新问题、旧问题未修复、旧问题回归、已修复后的边界遗漏；登记前先回查历史归档、当前源码和自动化证据。 |

## 前置准备与复原

| 编号 | 前置素材 | 准备方式 | 复原方式 |
|---|---|---|---|
| 准-A | 目标占用素材（DEV-18） | 在测试用 Agent 目录下创建与待部署 Skill 同名的目录和普通文件。 | 删除测试目录，确认原有条目未改变。 |
| 准-B | 同名不同内容冲突素材（DEV-54/58） | 复制一个测试 Skill，改动副本 `SKILL.md` 一行，放入测试扫描目录并重新扫描。 | 删除副本并重新扫描，确认计数恢复。 |
| 准-C | 远程来源素材（DEV-53） | 准备允许的 HTTPS 归档、允许的 Git 仓库，以及不可达、非 HTTPS、重定向、超预算和恶意归档样本。 | 删除下载、解包和测试库内容；保留失败日志引用。 |
| 准-D | 关系图与共享目录素材（DEV-59/60） | 准备多个 Skill、别名、长路径、不同 Agent 类型和同一共享目录被多个 Agent 识别的测试事实。 | 删除测试关系和测试部署，重新扫描确认源目录未被修改。 |
| 准-E | 元数据与安全素材（DEV-26—29、DEV-61/62） | 准备多标签、中文/英文逗号、长用途、翻译描述、上游更新、基础安全警告/高风险和无结果 Skill。 | 恢复原元数据；安全结果按产品允许的清理规则处理并记录。 |

## 当前人工执行流程

每条流程完成后，填写“结果”和“截图/日志证据”；“待验收”是初始状态，不代表通过。

| 流程 | 覆盖项 | 前置素材 | 操作步骤 | 通过标准 | 结果 | 截图/日志证据 | 现场清理 |
|---|---|---|---|---|---|---|---|
| A. 产物、启动与基础可用性 | DEV-2—7、DEV-10、DEV-14、DEV-17、DEV-20、DEV-23、DEV-25、DEV-30、DEV-33、DEV-37、DEV-38、DEV-41、DEV-42、DEV-49、DEV-64/65 | 当前 release；标准窗口 800×600、1280×900、1440×900；键盘 | 启动产物；依次打开概览、发现、技能库、详情、关系三页、仓库发现/管理、设置；切换中英文、默认/深色主题；用键盘完成导航、搜索、页签、抽屉、视图切换和返回。 | 应用可启动；无根级横向溢出；概览在最大化窗口中所有数据模块完整展示且不出现页面或模块滚动条；空态/加载/失败/返回入口清楚；焦点可见；关系页签、图谱搜索下拉、字段完整值提示可达；记录平台差异。 | **已记录新问题，转 DEV-66；本轮验收继续** | 用户现场截图：概览页整体滚动条已消失，但当前最大化分辨率下指标带、关系概览、明细/标签等模块仍出现内部滚动条并遮盖/裁切内容。代码核实：`overview.css` 为指标带、关系区、主内容区、待办区和标签明细设置了 `overflow-y: auto`；这是新一轮“概览全屏无模块滚动条、自适应完整展示”代办，不回写为上一轮 DEV-25 未完成。 | 保留当前现场和截图；DEV-66 修复后按同一窗口最大化状态与数据快照复验。 |
| B. 本地来源、手动来源与远程获取 | DEV-50—53 | 准-C；本地测试目录；`npx skills add` 文本 | 在发现页确认无旧 lock 入口；解析本地路径、URL、Git 和 `npx`；本地路径进入导入向导；远程来源分别执行成功、失败、取消和重试。 | 只解析不执行命令；来源和重定向受策略限制；下载/解包有预算和路径保护；成功来源可审阅；失败不写半成品且可重试；取消可恢复；来源坐标、版本/提交和哈希可追溯。 | 待验收 | 保存各来源类型、进度、取消/重试、失败清理和来源详情截图/日志。 | 删除测试下载、临时工作区和测试仓库；不删除用户原目录。 |
| C. 导入安全、冲突优先与关系确认 | DEV-18、DEV-20、DEV-48、DEV-54—58 | 准-A、准-B；安全候选；可选 LLM 供应商 | 先运行确定性基础安全检查，分别观察通过、失败、重试和阻断；分析冲突后确认“处理需要确认的冲突”，再点“继续确认关系影响”；测试保留原件不导入、复用已有、独立导入、记为待办；返回上一步重做决定；提交并重启。 | 基础安全未通过不可提交；证据和下一步可读；流程顺序为冲突→关系影响；人工决定不被 AI 覆盖；临时分析不伪装成历史；提交后冲突/待办可查询且刷新/重启不丢；主展示无裸 UUID 和 `[object Object]`。 | 待验收 | 保存安全结果、冲突决策、关系确认、提交结果、通知、操作记录和重启后状态。 | 删除准-A/准-B、导入副本；重新扫描确认计数恢复；清理或记录待办。 |
| D. 元数据、翻译、调用策略、运行要求与更新 | DEV-26—29、DEV-46/47、DEV-65 | 准-E；有上游和无上游 Skill；可用/不可用翻译供应商 | 在列表、抽屉、完整详情三处编辑别名、用途、标签；用中英文逗号保存并重启；执行描述翻译并测试取消/确认写入用途；查看调用策略和运行要求只读字段；执行上游检查和更新筛选；查看版本号与长字段提示。 | 三处读值一致且刷新；标签正确拆分并持久化；翻译有进行中/失败/取消/确认边界；调用策略不可编辑；运行要求来自真实声明，无声明有明确空态；更新状态不伪造；版本与来源语义一致；长字段可悬停或键盘查看完整值。 | 待验收 | 保存三处对照、翻译状态、更新前后版本/来源、筛选和完整值提示证据。 | 恢复原别名/用途/标签；清除筛选；按产品规则处理测试翻译记录。 |
| E. 部署、关系治理、计数与回滚 | DEV-21—23、DEV-40—43、DEV-46、DEV-55、DEV-59/60、DEV-63 | 准-D；可部署 Agent 目标；链接能力可用/不可用场景；同名 Skill 两份 | 预览链接/复制部署并提交；查看成功结果、治理入口、概览/Agent/详情计数；进入治理页执行确认、取消、失败、重试和回滚；进入图谱查看 Skill→共享目录→支持方 Agent；测试同一目标名称冲突和不同目标部署；解除部署并重启。 | 用户文案使用链接部署/复制部署和品牌+类型；目标占用/权限边界明确；计数和图表一致且无内部键；治理可独立进入；共享目录只出现一张实体卡并形成三段拓扑；同名目标预览阻断，不覆盖、不静默改名；回滚不损坏集中库和原文件。 | 待验收 | 保存预览、提交、治理、图谱、回滚和重启前后证据；真实路径只写入现场受控记录。 | 解除测试部署；恢复目标；删除测试关系；重新扫描确认源目录未修改。 |
| F. 安全检查与操作记录 | DEV-48、DEV-61/62、DEV-65 | 准-E；基础检查可运行 Skill；警告/高风险/无结果 Skill；可用 LLM 或记录不适用 | 从抽屉、完整详情和独立安全页查看基础/AI 状态；启动基础检查和 AI 检查；AI 运行中取消；观察顶栏任务、操作记录、列表“安全检查/检查结果”两列；重新打开页面并重启。 | 抽屉操作在完整详情可达；基础与 AI 结果独立；AI 未配置/跳过不替代基础门禁；取消终态一致且不误报失败；状态列与结果列分离，零结果不伪造；主展示无技术 ID。 | 待验收 | 保存运行、取消、失败、完成状态及列表前后对照。 | 取消或完成测试任务；按产品允许的规则处理测试结果并记录。 |
| G. 图谱与全项目呈现一致性 | DEV-11、DEV-15、DEV-24、DEV-31、DEV-34—39、DEV-44/45、DEV-60、DEV-64 | 准-D；长路径/长名称；多个 Agent 类型；800×600 和宽屏；默认/深色主题 | 搜索原名/别名并用鼠标、键盘、Escape、失焦；拖拽节点和平移空白区；缩放/适配视图；点击 Skill、Agent、目录、边查看详情；查看图例、路径、时间、类型徽标和技术细节；在 Agent、发现、技能库、抽屉、详情、治理页对照同一 Agent。 | 节点/边与图例编码一致；空白区可平移、节点可拖拽、控件在画布内；名称/路径/时间/关系类型可读；Agent 使用品牌+用户视角类型；共享目录拓扑清楚；技术标识仅在折叠技术细节；窄屏、长文本、键盘和无障碍不回归。 | 待验收 | 保存图谱全景、选中详情、图例、技术细节、窄屏和键盘焦点证据。 | 清除搜索/筛选；取消临时选择；关闭应用。 |
| H. Windows/macOS 平台专项 | DEV-1、DEV-4/5、DEV-8、DEV-14、DEV-21、DEV-25、DEV-30、DEV-37、DEV-42、D5-11 | Windows/macOS release；macOS 两个真实可写卷；真实 Agent 目录；官方 WorkBuddy 素材（若未提供则阻塞） | Windows 执行 A—G 代表流程，核查路径、权限、链接/目录联接、缩放和 800×600；macOS 重复关键流程并执行跨卷写入、Dock 图标、大小写/权限/路径检查；记录平台差异。 | 平台行为符合清单；跨卷写入仅在 macOS 双卷条件满足后判定；未提供 WorkBuddy 素材时 DEV-8 记外部阻塞；不得用 Windows 自动化替代 macOS 证据。 | 待验收/待分类 | 每个平台独立记录产物、系统版本、视口、截图、日志和清理结果。 | 恢复两平台测试目录、数据库和部署目标；删除临时卷测试数据。 |

## 本轮新发现：转下一轮开发，不改变上一轮验收基线

以下问题均来自本轮人工截图和操作反馈，已回查当前源码事实；不回写为上一轮 DEV-2—65 未完成，也不阻断当前人工验收流程继续执行。

| 新代办 | 人工现象 | 当前代码事实 | 建议更改方案 | 下一轮验收判据 |
|---|---|---|---|---|
| DEV-67 | 技能图谱页上方出现页面标题“技能图谱”，下方又有“技能图谱”页签，标题和页签重复。 | `RelationshipsLayout` 同时渲染 `PageHeader` 和 `RelationshipsNav`；导航项又使用 `relationships.graph.title` 作为图谱页签文案。 | 将关系模块标题区域改为一个带 `h1` 语义的加粗标题 + 同一行子页签；移除重复的路由级图谱标题，保留三页 `NavLink`、计数、`aria-current`、键盘和深链。 | 图谱页不再重复显示相同标题，冲突处理/关系治理仍可达，当前态、计数、键盘和深链保持。 |
| DEV-68 | “图谱阅读方式”占据大块独立区域，压缩图谱可视空间。 | `SkillGraphPage` 将 `GraphLegend` 作为图谱主体前的独立 `section` 渲染；`.sh-graph__legend` 使用整块网格布局和较大内边距。 | 把图例改为画布内右下角的紧凑悬浮按钮/图示面板，默认只展示线型和状态摘要；通过 hover/focus 或明确按钮展开详细说明，并设置边界避免遮挡关键节点和控件。 | 图例不再占用独立整行；右下角悬浮图示不遮挡节点、边、控件和详情，窄屏/键盘/无障碍仍可读。 |
| DEV-69 | 默认节点和边过大，节点互相压叠并盖住边，无法快速读出关系。 | `CANVAS_SIZE` 固定为 `960×640`；力布局使用固定 `SPRING_LENGTH`、`REPULSION`、边界和节点尺寸；节点 CSS `max-width: 12rem`，缺少按画布可用区域和节点数量的自适应密度策略。 | 让布局计算接收画布实际宽高、节点数量和节点估算尺寸；按密度动态调整节点间距/标签策略/初始缩放，先完成 fit-view 再渲染到可用区域；对过密上下文使用可展开聚合，不用互相覆盖或裁切隐藏边。 | 初次打开时按图谱区域尺寸、节点/边数量自动布局和缩放；节点与边尽量同时可辨，默认视图居中，放大后仍可探索，不能用裁切隐藏关系。 |
| DEV-70 | 放大、缩小、居中三个按钮现场点击无效；滚轮缩放和拖拽正常。 | `SkillGraphCanvas` 中三个按钮已有 `onClick`，分别调用 `zoomBy`/`fitToContent`；现有测试只验证回调被调用和按钮存在，未证明真实窗口中的视口视觉变化。 | 统一视口状态更新和画布 transform 的单一入口；为三个按钮增加真实 DOM 尺寸下的交互测试，断言按钮点击后的 `transform`/节点位置变化；检查覆盖层、事件冒泡、fit 时机和异步布局竞态。 | 在真实 release 窗口和浏览器自动化中证明三个按钮分别改变缩放、改变缩放方向并按当前画布重新居中；与滚轮、拖拽、节点选择互不回归，并保留可访问名称。 |
| DEV-71 | 技能图谱子页在最大化全屏下仍出现页面滚动条；期望与概览页一致，自适应占满可用空间。 | `RelationshipsLayout` 使用普通 `PageFrame`；图谱画布 `.sh-graph-canvas__surface` 固定 `min-height: 30rem`；关系页自身没有独立的 fill/可压缩高度契约。 | 给关系页增加 `fill` 高度链路：壳层、内容区、图谱页逐级 `min-height: 0`，图谱主体用剩余高度网格分配；移除固定最小高度对页面的强制撑开，详情面板在窄屏采用明确的可压缩/收纳策略。 | 最大化窗口下图谱页不出现页面滚动条；标题、子页签、工具栏、图谱区和详情区按可用空间自适应；800×600、1280×900、1440×900 及代表性设备均可读。 |
| DEV-72 | 搜索框与操作按钮区域被排成两行，留下大块空白。 | `.sh-graph__toolbar` 设置 `flex-wrap: wrap`；`.sh-graph__tags` 为纵向布局；搜索表单/标签和按钮没有统一的一行工具栏约束。 | 将搜索表单、标签输入和操作按钮改为统一的单行 flex/grid 工具栏，标签说明改为紧凑内联 label；为窄容器定义优先级收纳（筛选/显示设置进入紧凑菜单），避免依赖自然换行制造空白。 | 在满足最小可用宽度时搜索输入、标签输入、应用/筛选/显示设置/换 Skill 等操作统一一行；空间不足时按明确优先级收纳，不出现大块空白、重叠或键盘不可达。 |
| DEV-73 | Agent 页卡片标题仍显示 `agent-skills.shared-directory`、`anthropic.claude-code`、`cline.cli` 等技术字符串，没有统一为品牌 + 用户视角平台类型。 | `AgentListPage` 仅在品牌分组标题渲染 `BrandTag`，卡片标题直接渲染 `agent.instance`；已有 `AgentIdentity` 只在图谱/治理等部分消费点使用，列表卡片和详情标题仍未统一接入。 | 建立覆盖 `AgentView`、共享目录和自定义 Agent 的统一 presenter：品牌使用 `BrandTag`/官方 logo，类型按 `client`/证据映射为终端、桌面端、IDE 插件等；共享目录显示“共享目录”独立实体，不显示技术 ID；列表卡片、详情、发现、技能关联、部署治理和图谱全部复用同一 presenter。 | 所有前端 Agent 展示面不以 `client_id`、复合实例名或路径作为主标题；同品牌不同类型合并为一张卡并标注类型；共享目录只展示一份“共享目录”，无裸技术 ID，键盘/无障碍名称和深链保持。 |

## 现场判定与结果写回

1. 每条流程开始前记录平台、系统版本、产物构建时间、源码 HEAD、窗口尺寸、语言、主题和测试数据来源。
2. 发现问题先保留现场，按“新问题 / 旧问题未修复 / 旧问题回归 / 已修复后的边界遗漏”分类，再补步骤、截图、日志和复现条件。
3. 未完成真实复验不得写“人工通过”；自动化通过只能记录为“自动化通过，人工待验收”。
4. 每条流程完成后填写本表“结果、截图/日志证据、现场清理”，再同步《功能完成度与验收状态矩阵-2026-09-18》对应项。
