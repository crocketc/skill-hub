# 人工验收清单（2026-09-18）

更新时间：2026-09-21（代码与自动化验证已完成；本清单只记录真实 Windows/macOS 桌面仍需执行的操作链，不把自动化通过写成人工通过）。本清单是真实桌面验收的唯一当前入口与记录位置；验收结论同步写回《功能完成度与验收状态矩阵-2026-09-18》。

> **当前状态**：DEV-2—65 的代码和自动化验收已完成，人工 release 验收尚未执行；DEV-1 等待 macOS 第二个可写卷，DEV-8 等待官方 WorkBuddy 绿色 logo 素材。以下历史结果保持原样，后续人工复验时按“新问题/未修复/回归/边界遗漏”规则登记。

## 被测产物与启动

| 项 | 内容 |
|---|---|
| 被测产物 | `target/release/skillhub-desktop.exe`。当前产物已由 2026-09-21 Tauri 无打包构建生成；开始人工验收前仍须确认产物时间与源码 HEAD 一致。 |
| 启动方式 | **直接双击 exe**，无需任何环境变量 |
| 数据位置 | 数据库 `%APPDATA%\SkillHub\skillhub.sqlite`；部署目标为真实 Agent 目录（如 `<用户>\.claude\skills`）。**每项验收后按「前置准备」复原现场** |
| 截图/证据 | 存放目录自定，路径填入各行「备注/证据」列；不记录个人敏感内容 |

> **反馈登记规则**：每条新增反馈先对照历史待开发项、已完成摘要和既有验收证据，标注其与旧项的关系（新问题、旧问题未修复、旧问题回归或边界遗漏），再核查源码/测试事实。代码核查只能说明实现现状，不能替代 release 产物上的人工复验；未做人工复验的条目保持“待验收/待开发”状态。

## 前置准备（素材制造与复原）

| 编号 | 素材 | 怎么操作 | 复原方式 |
|---|---|---|---|
| 准-A | 占用素材（SR-6 / SR-10 / D-11 / DEV-18-A 共用） | 在 `<用户>\.claude\skills\` 下新建文件夹 `find-skills`，里面放一个任意文件（如 `说明.txt`） | 删除该文件夹，确认 `.claude\skills` 条目数回到验收前 |
| 准-B | 冲突素材（SR-4 / J-REL-3 共用） | 复制一个已有 Skill 的文件夹为同名副本，副本 SKILL.md 改一行内容，放入被扫描的 Agent 目录 → 「发现 → 本机发现 → 重新扫描」→ 导入时「跳过/不决定」，即得待处理冲突组（2026-09-20 已验证机制可用） | 删除副本 → 重新扫描，确认计数还原 |

## 验收项

### 导入与冲突

| 编号 | 功能 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| SR-3 | 导入提交 | 「发现 → 本机发现」→ 勾选 1 个候选 → 提交导入 → 观察顶栏任务；完成后对同一候选再次提交 | 只生成一次操作事实（一个顶栏任务、一条记录）；重复导入不重复建冲突组；原始文件保留在原目录 | **部分通过** | 2026-09-20：普通导入、重复导入、原始文件保留等其他判据暂未发现问题；完全相同 Skill 选择“复制导入”后，技能库出现两个副本，但冲突工作台未展示对应的已识别/已处理冲突历史，转 DEV-57。 |
| SR-4 | 冲突人工决定 | 准-B 后进入「关系 → 冲突处理」→ 打开冲突组 → 观察“不导入/复用已有 Skill/独立导入”三类决策层级与后果说明；作出人工决定；重新扫描/完成导入后重新进入冲突处理；随后再触发一次 AI 检查 | 三类选项含义清楚、主次突出；冲突项在导入提交后能进入冲突工作台；已决定组不被 AI 结果覆盖；“暂不处理/不导入”不修改既有决定；尚未提交的临时冲突不被冒充为历史 | **部分通过** | 2026-09-20：当前反馈确认导入冲突操作与说明仍是平铺层级，需 DEV-58 重做；复扫进入导入后冲突处理看不到冲突项，代码核查发现“仅提交后落库 + 导入完成未失效冲突查询”的同步断点；人工决定不被 AI 覆盖本身仍需按完整流程复验 |

### 统一执行反馈与失败处理

| 编号 | 功能 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| SR-5 | 取消长流程 | 从技能抽屉或完整详情页进入同一 Skill 的安全检查区；分别确认基础安全检查与 AI 安全检查入口；启动 AI 安全检查长流程 → 进行中点「取消」；再从两处重新打开查看状态 | 抽屉和详情页都能执行与查看安全检查；基础检查与 AI 检查入口/状态语义一致；AI 长流程可取消，顶栏任务与操作记录终态为「已取消」，不误报失败；取消后两处状态一致，基础检查结论不被 AI 结果替代 | **未通过/待修复** | 2026-09-20：技能详情页仅展示安全检查结果，抽屉仅展示基础/AI 状态，没有检查操作按钮；独立安全页虽有 AI 运行/取消入口，但基础检查未接入运行入口。转 DEV-61；完成后再复验 SR-5 |
| 预览后竞态素材 | SR-6 复验：提交后失败、撤销与恢复 | ① 确认 Claude Code 目标开始时为空；② 技能库选择 `find-skills`、勾选 Claude Code 并完成预览，确认“确认添加”可用；③ 不重新预览，在外部于目标位置创建同名 `find-skills` 目录/文件；④ 回到 SkillHub 确认添加；⑤ 检查失败结果、通知、顶栏任务、操作记录并重启 | ① 失败渲染逐目标行 + 可读中文文案；② 「操作记录」详情能回答哪个目标、哪条路径；③ 失败记「已撤销」或等价清理终态，**重启应用不进恢复闸门**；④ 外部制造的文件原样未动 | **待重新执行** | **原准-A“预览前占用”只能验证 D-11/DEV-18-A，用例 4 必须使用预览后竞态，否则规划期已阻断，无法验证 SR-6。** |
| 预览后竞态素材 | SR-10 复验：提交后失败文案可读 | 同 SR-6 竞态场景，检查通知中心与顶栏失败原因 | 通知详情与顶栏失败原因均为「可读文案 + 错误码」，**全流程不出现 `[object Object]`** | **待重新执行** | **与 SR-6 共用现场；预览前占用不计入本项。** |
| 准-A 后 | D-11 / DEV-18-A：规划期拒占用且文案可读 | 技能库 → 同一 Skill → 添加到同一目标 → 停在「预览」阶段观察 | 预览/确认阶段即报「目标目录已存在同名内容……可改用『纳入集中库管理』」类可读文案；**全流程无 `[object Object]`、无裸 SkillId**；提交按钮保持禁用；预览阶段不写盘 | **未通过（2026-09-20 复验）** | 阻断提交本身正确；但批量预览失败项仍把内部 Skill UUID（截图中的 `3896d85d-...`）作为主文案展示。`BatchDeploymentPage.tsx` 使用 `failure.skillId` 渲染失败项，需改为用户可读 Skill 名称，并保留技术标识在折叠技术细节中。属于 DEV-18 已修复项的剩余呈现缺口，不新增重复代办。 |

### 能力与计数（DEV 批量修复轮新增判据）

| 编号 | 功能 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| DEV-21-A | 链接/复制部署与清理闭环 | 部署任一 Skill 到 Claude Code（宿主实际支持链接）→ 查看预览 → 提交 → 查看成功结果、Skill 详情和关系治理 → 解除部署 | 预览主文案只显示用户可理解的「链接部署/复制部署」；技术实现类型折叠在技术细节中。提交后目标为链接入口（本机可为 junction/reparse point），并明确链接复用内容、复制占用额外空间；成功后有明确的查看/解除部署入口，解除后现场复原且集中库不受损 | **未通过/待修复** | 2026-09-20：实际部署成功且资源管理器显示链接图标，说明本机链接能力可用；但预览仍显示「目录联接/不支持符号链接」的技术化且相互矛盾文案，成功结果未发现清晰的解除部署/恢复入口。需统一为链接/复制用户语义，并补成功后的清理入口 |
| DEV-22-A | 计数与图表投影一致 | 完成一次真实部署后依次查看：概览指标、Agent 页卡片计数、Skill 详情状态栏、关系页和概览柱状图 | 四处计数一致（部署关系条数相同）；Agent 卡片显示已部署 Skill；概览「已配置部署目标 N 个」随 targets 表变化；图表显示用户可读的 Agent/项目名称，不出现内部 i18n key | **未通过/待修复** | 2026-09-20：概览计数可见，但 Agent 页未显示该部署；代码核查定位为 `agents/nativeApi.ts` 用逻辑目标 `target.id` 匹配部署记录中的物理 `target_id`。同时 `bootstrap_repository.rs` 返回 `deployment.dimension.agent`，`overview/api.ts` 未翻译，导致图表和明细显示内部键。属于 DEV-22-A 的投影回归，不是用户操作问题 |
| DEV-16-A | 用户字段三处一致 | 抽屉设别名/用途/标签 → 不重启依次查看：技能库列表行（标签列/用途列）、详情页身份区、抽屉 | 三处读值一致；详情页头部别名行仅在别名≠原名时出现且不显示 UUID | **通过（2026-09-20）** | 用例 7：列表、抽屉、详情三处用户字段一致。列表标签视觉和完整值提示另记 DEV-65 |

### 呈现与导航（DEV 批量修复轮新增判据）

| 编号 | 功能 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| DEV-23-A | 关系页内导航 | 进入「关系」→ 用页签切换图谱/冲突处理/治理 | 三页签可达且计数与概览卡一致；概览卡片深链仍可用 | **通过（2026-09-20）** | 用例 7：三页可达、计数与概览一致，概览深链进入正确子页；页签视觉样式另记 DEV-33 |
| DEV-5-A | 路径展示 | 逐页检查 Agent 页/发现页/导入向导/部署目标/冲突与治理页的路径 | 无 `\\?\`/`\??\` 前缀；同一目录只出现一种斜杠风格 | **未通过（2026-09-20）** | 用例 7：仍发现多处来源路径带 Windows 内部前缀；现有 `displayPath()` 未覆盖全部展示消费点，联动 DEV-31 |

### 关系页窄视口与图谱

| 编号 | 功能 | 怎么操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| J-REL-3 | 冲突工作台窄窗口内容判据 | 准-B 后把窗口缩到 800×600 → 「关系 → 冲突处理」（可从页签直达） | 冲突成员列表可读、动作区可达、影响预览长文本可换行/滚动；无根级横向溢出 | **无法验收（数据前置阻塞，2026-09-20）** | 用例 8：关系 → 冲突处理显示 0 项，但准-B/现场实际存在冲突，无法打开冲突组；未进入布局判据。与 DEV-58 的冲突事实落库、查询失效/复扫同步问题联动。DEV-2 的 E2E 仅覆盖同构冲突素材，不能替代真实工作台验收 |
| J-REL-2 判据更新 | 图谱内容判据随 DEV-15/24 更新 | 关系图谱（任一视口）确认节点标签与边文本，并打开详情内容框 | 节点标签为 Skill 展示名（不再是裸 UUID）；名称过长被省略号裁切仍算可读；详情面板时间为可读本地时间；内容框不以 relation_id、node_id、UUID、修订号等技术标识作为主展示 | **部分通过（2026-09-20）** | 用例 7：节点名称和本地时间通过；详情内容框仍显示技术标识，转 DEV-45。自动化侧 forceLayout/canvas 用例已覆盖坐标与交互 |

### 人工验收新增待开发项

| 编号 | 功能 | 发现现象 | 后续验收/开发判据 | 结果 | 备注/证据 |
|---|---|---|---|---|---|
| DEV-25 | 概览页响应式高度与无滚动条 | 全屏 100% 比例下概览页出现上下滚动条；技能关系框偏高，下方柱状图区域可压缩 | 按不同设备与分辨率重新调整各模块高度与布局约束：填满可视区域，所有概览内容直接展示，不出现页面上下或横向滚动条；需用代表性窗口尺寸实测并截图留证 | 待开发 | 2026-09-20 新版 release 人工验收反馈；暂不修改代码 |
| DEV-26 | 标签逗号拆分、数据库持久化与关联刷新 | 在技能编辑抽屉的标签输入框输入「测试，开发」并保存 | 显示为两个独立标签；保存后重新打开仍为两个标签；概览标签饼图自动更新，技能库列表、详情和其他标签相关位置不显示旧数据 | 待开发 | 代码核查：中文逗号解析缺失已确认；`set_metadata` 数据库写入链路在位；概览刷新缺口已确认。批量标签和关系图谱输入存在同类英文逗号限定，后续一并验证 |
| DEV-27 | 技能抽屉与完整详情页原始描述翻译并写入“我的用途” | 分别打开技能抽屉、技能完整详情页，在原始描述旁点击翻译；等待模型结果后分别测试确认与取消 | 两处均有翻译按钮；调用模型期间有进行中反馈；成功显示译文并询问是否写入“我的用途”；确认后用途保存且重新打开两处仍一致，取消不修改用途；失败显示可读错误且不产生半成品保存 | 待开发 | 代码核查：现有翻译命令/记录存储/详情页翻译入口已在位，但抽屉入口和两处统一的“写入我的用途”交互缺失；完整详情页必须覆盖抽屉能力 |
| DEV-28 | 调用策略与运行要求只读展示 | 分别查看技能抽屉和完整详情页的调用策略/运行要求，并尝试键盘遍历 | 可识别策略显示识别结果；无明确参数时默认“模型与用户”；运行要求来自 Skill/依赖文件的真实结构化声明；没有编辑、切换、保存控件；无声明时使用明确单一空态 | 待开发后复验 | 2026-09-20 产品裁决：不单独展示“调用声明/命令”；运行要求必须完成解析、持久化、查询和两处页面接入。SkillHub 不修改 Agent 调用开关。 |
| DEV-29 | 接入上游更新检测并精简技能库筛选区 | 为至少一个有已确认上游来源且确有新版本的 Skill、一个无上游 Skill、一个网络不可达场景执行更新检查；再使用“有更新版本”筛选，并观察宽/窄窗口布局 | 有更新的 Skill 能进入版本筛选结果并显示更新状态；无上游/不可达不伪造“有更新”；检查后列表与筛选状态刷新；筛选区不再显示虚假的禁用占位，列表和分页保持可达、无额外页面级滚动条 | 待开发 | 代码核查：后端 `check_source_updates` 已存在，但列表 `upgradeAvailable` 固定为 false；布局收敛与真实筛选需一并验收 |
| DEV-30 | 技能库搜索框宽度与工具栏空间重设计 | 打开技能库，分别在宽屏、窄屏和键盘操作下观察搜索框、筛选、结果摘要、分组、组合管理及后续按钮 | 搜索框相比当前版本明显收窄且仍易于使用；右侧功能有稳定空间、无拥挤/重叠；窄屏可合理折行或收纳；键盘焦点清晰，列表空间不被无意义的搜索宽度占用 | 待开发 | 代码核查：搜索框由 flex 吃掉工具栏剩余空间；与 DEV-29 同轮实现，单独验收 |
| DEV-31 | 关系图谱路径展示规范 | 进入关系 → 技能图谱，查看来源节点和右侧详情中的路径；同时检查其他关系页路径 | 不出现 `\\?\\`、`\??\` 等内部前缀；Windows 路径斜杠统一；同一路径不因展示组件不同而出现两种格式 | 待开发 | 代码核查：图谱 `GraphDetailsPanel` 直接展示 `node.path`，漏接 `displayPath()` |
| DEV-32 | 图谱空白区域左键拖拽平移 | 在图谱空白区域按住鼠标左键拖动；再分别拖动节点、点击节点/边、滚轮缩放并点击适配视图 | 空白区可平移视觉中心；节点仍可拖动；点击与缩放语义保持；适配视图仍能整体居中 | 待开发 | 代码核查：内部全尺寸 layer 使空白区 pointerdown 不满足当前 target 判断 |
| DEV-33 | 技能关系三页横向页签与标题层级 | 进入「技能关系」，观察图谱/冲突处理/关系治理的切换区域；点击三页并用键盘遍历，再从概览卡片深链进入 | 三页以横向页签呈现，不使用容易误认为标签的胶囊样式；当前子页标题不在页头和页签中重复；当前态、计数、键盘焦点、三页路由和概览深链均保持正确 | 待开发 | 与 DEV-23 的关系：代码核查确认 DEV-23 的路由导航能力在位，但当前实现是胶囊导航且 `PageHeader` 重复显示子页标题；DEV-23-A 尚无 release 人工通过证据 |
| DEV-34 | 图谱节点间距与关系文字避让 | 进入包含多个 Skill、Agent、来源和不同关系的图谱；观察初始布局、缩放、fit-view，并拖动节点后重新观察 | 节点框、关系文字和相邻节点不互相压叠；长名称、不同缩放比例和窄窗口仍可读；节点拖动、缩放、fit-view 不回归 | 待开发 | 与 DEV-24 的关系：代码核查确认已有力导向，但没有按节点实际尺寸和边文字做碰撞约束；截图为现象证据，尚未进行 release 复验 |
| DEV-35 | 图谱 Agent 节点品牌 logo 与类型徽标 | 在图谱中查看不同品牌、不同类型和未知品牌的 Agent 节点及右侧详情 | Agent 节点使用品牌 logo + 品牌名 + 类型徽标；未知品牌有可读回退；不以裸 client id 作为主视觉；无障碍名称和详情信息完整 | 待开发 | 与 DEV-11/AGENTS.md 的关系：代码核查确认图谱当前仍为纯文本 client id，未接入现有品牌视觉；暂未人工复验 |
| DEV-36 | 图谱关系类型视觉编码与图例 | 准备同时包含复制、共享目录、部署、冲突等关系及不同验证状态的图谱；观察边和图例，并切换筛选/选中边 | 不同关系类型具有稳定可辨识的线型/颜色/粗细组合；状态差异仍可辨识；图例与边标签一致；灰度或色觉不敏感时不只依赖颜色；选中态清晰 | 待开发 | 代码核查：当前仅按状态区分线型/颜色，关系类型全部已验证时会同色；页面暂无完整关系类型图例，尚未人工复验 |
| DEV-37 | 图谱与详情同高及画布内悬浮控制 | 在宽屏和窄屏分别进入技能图谱，观察图谱框、右侧详情框及放大/缩小/适配视图按钮；用鼠标和键盘操作按钮 | 宽屏两框顶部/底部对齐且高度一致；窄屏单列布局不溢出；三个控制按钮悬浮在图谱框右上角，不遮挡节点/边，鼠标、键盘焦点和画布拖拽均可用 | 待开发 | 与 DEV-24/DEV-34 的关系：代码核查确认当前画布控件在画布外，详情自然高度、画布固定 30rem，尚未 release 复验 |
| DEV-38 | 技能图谱搜索与筛选操作区 UI 重设计 | 进入技能图谱，分别操作 Skill 搜索、中心候选标签/应用标签、关系筛选、显示设置、换 Skill；再使用窄窗口和键盘遍历 | 操作区按“定位中心/关系筛选/显示设置/快捷切换”分组，主次明确；搜索与标签应用不散乱；当前筛选状态可见；弹层不被裁切、不遮挡关键内容；宽窄窗口和键盘焦点均可用 | 待开发 | 代码核查：当前所有控件平铺在同一 flex 工具栏，标签输入纵向块与搜索表单基线不一致；已按 frontend-design 登记，尚未 release 复验 |
| DEV-39 | 全项目 Agent 品牌与工作台类型统一视觉 | 分别查看 Agent 页、技能库列表/抽屉/详情、关系图谱/治理、发现/导入/项目中的 Agent 展示；重点检查 CodeBuddy Code、WorkBuddy、网页端、IDE 插件、后台服务和共享目录 | 不出现 `codebuddy.code`、`codebuddy.workbuddy` 等裸技术 ID；CodeBuddy Code 显示品牌 logo +「终端」，WorkBuddy 显示品牌 logo +「桌面端」，`web` 显示「网页端」；其他形态按统一用户语言徽标展示；同品牌同目录不同类型合并且类型不丢失；未知品牌可读回退；无障碍名称一致 | 待开发 | 与 DEV-11/DEV-35 的关系：代码核查确认统一规则已写入 `AGENTS.md`，但当前仍有多处消费点未接入；本次已将后端类型改为用户视角展示分类，尚未 release 复验 |
| DEV-40 | 关系治理新增关系入口语义与导航 | 在关系治理页点击新增关系入口；再分别从技能库、Agent 详情、项目详情发起添加并返回治理页 | 治理页明确只处理已有关系；新增入口不再进入无 Skill 的空批量添加页；通用入口先到技能库选 Skill，Agent/项目上下文能保留目标并进入添加流程；返回后筛选/来源上下文可恢复 | 待开发 | 与 DEV-19 的关系：代码核查确认当前 `/deploy` 链接缺少 `skill` 参数，截图中的“没有选择要添加的 Skill”可稳定解释；DEV-19 入口存在性已测，真实无上下文行为未测 |
| DEV-41 | 关系治理新增入口与选择操作区布局收口 | 进入关系治理页，观察新增关系入口；勾选/取消勾选关系并滚动到列表底部，在宽屏、窄屏和键盘操作下复验 | 新增关系入口与其他操作同一区域，以不同按钮颜色区分，不再占用独立大模块；选中后的数量、可执行/受阻摘要和批量操作位于页面底部，风格参考技能库批量操作条；操作条不遮挡末行/分页，动态换行后仍能让位，取消选择后隐藏；DEV-40 的导航、来源上下文和返回行为保持不变 | 待开发 | 代码核查：当前入口为独立 `sh-governance__deploy-entry`，选择摘要为列表上方 `sh-governance__toolbar`；尚未进行 release 人工复验 |
| DEV-42 | 关系治理单一滚动容器与用户视角信息架构 | 进入关系治理页，分别在全屏、矮窗口、宽屏和窄屏观察页面与关系列表滚动；查看可执行、待检查、已管理和受阻关系，展开技术细节并尝试键盘操作 | 页面本身不再与关系列表形成双层纵向滚动；纵向滚动只发生在关系列表，必要的横向滚动不影响操作；列名和值能直接说明“是什么关系、添加到哪里、当前发生什么、下一步怎么做”；不出现裸 client id 作为主展示；路径可读；“受阻”明确给出原因、影响和处理动作，技术信息可展开但不丢失；筛选、选择、批量/逐条操作及无障碍名称保持可用 | 待开发 | 代码核查：`.sh-app-shell__content` 与 `.sh-governance__list` 都可能纵向滚动；表格当前列为关系/来源/目标/影响/校验/操作，目标和阻塞内容仍明显偏技术实现；尚未进行 release 人工复验 |
| DEV-43 | 图谱待确认关系到治理操作的边上下文深链 | 在技能关系图谱中选择待确认关系边，点击“在关系治理中处理”；观察治理页筛选、定位和操作步骤，再返回图谱 | 自动带入该边两端的 Skill 与 Agent/项目上下文；只显示并定位这条关系，自动滚动/聚焦对应行；直接进入该关系的影响预览或明确的下一步操作，不要求用户在全量清单中重新寻找；返回仍回到图谱并保留原视觉上下文；边事实缺失时不伪造筛选并给出可理解的降级提示 | 待开发 | 代码核查：当前链接只带 `from=graph&relationId`；治理页解析但未传入 `listGovernance`，后端过滤器也不支持关系 ID；目前只能跳到治理页，尚未 release 人工复验 |
| DEV-44 | 技能图谱 Skill 节点同时展示别名与原名 | 准备至少一个有别名的 Skill 和一个无别名的 Skill，进入技能图谱；查看节点、右侧详情、搜索候选，并在选中/缩放后观察 | 有别名的节点同时显示清晰标注的“别名”和“原名”；无别名只显示一次；节点与详情不显示 SkillId/UUID 作为主名称；搜索、无障碍名称、节点跳转保持一致；长名称和缩放后仍能区分两种名称 | 待开发 | 代码核查：`nodeLabel()` 当前只返回候选 `display_name`；候选接口已有 `runtime_name`，但图谱节点 DTO 未携带，原名尚未展示；尚未进行 release 人工复验 |
| DEV-45 | 图谱详情面板去技术标识与 UI 信息架构重设计 | 在图谱中分别选中中心 Skill、相关 Skill、Agent、目录/来源、冲突节点和关系边；观察右侧详情，在宽屏/窄屏、长名称和键盘操作下复验 | 详情面板按用户任务分组展示名称、关系、来源/目标、状态、影响和下一步；Agent 使用品牌 logo+类型徽标；Skill 别名/原名规则一致；主内容不出现 relation_id、node_id、UUID、修订号等后端编码，技术细节可展开对账；路径规范；按钮有主次层级；选中对象切换、待校验/无数据、长内容、键盘焦点和无障碍名称均清晰可用 | 待开发 | 与 DEV-35/39、DEV-37/38、DEV-31、DEV-43 联动复验。代码核查：`GraphDetailsPanel` 直接显示 `relation_id`、`relationshipRevision`，节点名称缺失时回退 `node_id`；当前详情区为平铺事实列表，尚未进行 release 人工复验 |
| DEV-46 | 技能库列表/抽屉/详情 Agent 关系呈现与详情关系区重设计 | ① 技能库查看“已添加到 Agent”列；② 打开同一 Skill 抽屉；③ 打开完整详情的关系区、来源/存证区；④ 对比关系治理中的同一 Agent/目标 | 三处使用一致的品牌 logo+品牌名+用户视角类型徽标；解析失败不显示 `fs:volume-...` 等物理目标 ID；详情关系区能直接看懂关联对象、关系含义、当前状态、影响和下一步；路径/指纹/client/target/relation 等技术信息仅在“技术细节”中展开；布局不再是密集平铺，宽窄屏、长文本、键盘和无障碍正常 | 待开发 | **复验 DEV-39：未修复/未闭环。** 列表 `nativeApi.ts` 有原始 target ID 回退，抽屉复用旧文字标签组件，详情关系与存证仍直出技术字段；需与 DEV-11、DEV-35、DEV-39、DEV-42、DEV-45 联动验收 |
| DEV-47 | 技能详情页左侧栏名称/子导航层级恢复与列表版本标签一致性 | 打开一个有版本序号的 Skill 详情，再返回技能库对照同一 Skill；分别进入身份、关系、版本等子页签，并测试列表/抽屉/详情深链 | 左侧子导航栏顶部固定显示 Skill 名称/别名，下方紧接子页签按钮；不是放在页面整体顶部；内容区层级清晰，页签、深链、返回、相邻 Skill、键盘和窄窗口正常；同一 Skill 在列表、抽屉、详情和时间线均显示 `v1` 或同一可信版本号，不因列表字段缺失显示 unknown；确实无版本时才显示 unknown | 待开发 | **代码核查：当前名称在主内容区、子导航在左侧 rail，左侧栏顶部没有 Skill 名称；详情可由版本序号显示 v1，列表 `current_version_label` 为空时直接显示 unknown。** 需与 DEV-29、DEV-33、DEV-46 联动复验 |
| DEV-48 | 导入流程基础安全检查门禁与技能风险/待办摘要重构 | 导入一个新 Skill，观察基础检查运行、失败阻断和重试；再制造基础检查通过/失败、AI 未配置/跳过、关系待确认、来源有更新、无待办等状态并查看抽屉 | 导入流程内先运行确定性基础检查；检查未通过或存在安全风险时禁止导入，展示原因/证据/下一步并可重试；基础检查通过后才可提交；AI 安全检查可选，未配置或跳过不替代基础门禁；抽屉摘要只显示真实风险/待办，能看懂原因、影响和下一步并可跳转；无事项时使用紧凑空态 | 待开发 | **代码核查：导入链路未调用 `run_basic_check`，当前不存在基础检查失败阻断导入的门禁；候选固定为未检查；风险摘要仅显示高风险/待处理两个数字，真实 `pendingCount` 固定为 0。** 需与 DEV-19、DEV-22、DEV-29、DEV-42、DEV-46 联动复验 |
| DEV-49 | 仓库发现与仓库管理默认卡片视图统一 | 分别进入仓库发现、仓库管理；观察已配置仓库/仓库管理条目，并测试添加、启停、刷新、扫描、移除；覆盖未扫描、扫描中、失败、禁用、空列表、长仓库名和宽窄窗口 | 两个页面默认均为响应式仓库卡片，不是行列表；卡片清晰展示仓库名、分支、GitHub 来源、参与发现状态、最近扫描结果和候选数/失败原因；所有原有操作可见可用；卡片层级与在线发现一致，键盘焦点、窄屏换行和无障碍名称正常 | 待开发 | **代码核查：仓库发现已配置仓库使用 `sh-discovery-repos__repo` 行列表，仓库管理使用 `sh-repo-manager__row` 行列表；Skill 结果卡片已存在但未覆盖仓库条目。** |
| DEV-50 | 移除 `~/.agents lock` 发现模块 | 进入发现主页并检查入口、卡片数量和导航；直接访问旧 `/discovery/lock` 深链；分别验证本机、在线、仓库发现及普通导入 | 主页不再出现 lock 卡片或相关文案，旧 lock 路由有明确兜底；本机/在线/仓库发现入口和结果不受影响，普通导入仍执行 DEV-48 基础安全门禁；前后端契约、生成绑定、测试和文档引用已同步清理 | 待开发 | **产品决策已确认：该能力与现有发现流程重复，且清单不能保证精确版本恢复；统一开发阶段实施删除，当前验收阶段不改代码。** |
| DEV-51 | 本地导入向导移除手动来源输入并减少重复点击 | 进入本地导入向导；点击“选择本地目录”，观察来源加入、候选读取和进入候选选择；覆盖单/多目录、读取中、失败、取消、返回来源和键盘操作 | 本地导入只保留目录选择和已选来源管理，不再出现手动路径输入或重复“添加到已选来源”；候选读取完成后直接进入“选择候选”，不再额外点击“继续选择候选”；来源状态、失败重试、取消、返回编辑和候选选择不丢失，窄屏与无障碍正常 | 待开发 | **SR-3 关联优化：代码核查确认当前目录选择已自动加入来源，当前仍有手动输入和 `candidate_gate` 二次确认；手动来源识别改由 DEV-52 验收。** |
| DEV-52 | 手动来源解析发现模块替换 `~/.agents lock` | 进入发现主页，确认原 lock 卡片位置改为手动来源模块；分别输入本地路径、URL、Git 地址、`npx skills add` 和无效内容并点击解析；本地路径继续进入导入向导，非本地类型观察支持状态 | 主页卡片布局不变但不再出现 lock 文案；各输入类型显示正确的来源类型和目标；本地路径可进入现有导入流程；URL/Git/`npx` 在远程获取未实现前明确显示“已识别、暂不支持导入”，不伪造候选、不下载、不执行命令；错误、重复解析、键盘和无障碍状态清晰 | 待开发 | **产品方案已确认方向：本地导入去掉手动输入，手动来源独立为发现模块；代码核查确认非本地来源目前会返回 `import.remote_download_not_wired`。** |
| DEV-53 | 远程来源下载、解压、候选读取与安全导入闭环 | 在手动来源模块分别测试可用 URL/Git 来源、不可达来源、禁止重定向/域名、超大或恶意归档、取消和重试；成功来源继续完成候选选择、基础安全检查和导入；随后查看详情中的来源与版本信息并检查临时目录 | 远程来源有明确进度、取消、失败和重试；只允许通过来源/网络策略的地址；下载/解压受预算限制且无目录穿越、符号链接越界和临时残留；成功后候选可审阅，基础安全检查通过后才可导入；失败不写入半成品；记录来源坐标、可得的分支/提交、内容哈希并支持后续更新检测；`npx` 只解析、不执行命令 | 待开发 | **后续闭环代办：现有仓库发现已有受限下载/解压链路，但 URL/Git/`npx` 手动来源尚未接入；需单独进行安全、数据持久化、取消恢复和 release 真机验收。** |
| DEV-54 | 导入流程冲突优先与关系影响确认按需呈现 | 选择一个会产生关系影响的 Skill，分别制造有冲突/无冲突、选择复用/跳过/保留独立副本的场景；观察导入步骤顺序、关系影响确认、返回和最终提交 | 有冲突时先处理内容/名称/重复冲突，再显示导入后关系影响；无关系影响时不出现完整关系治理步骤；有关系影响但可安全默认时使用简洁提示，确需决定时才显示确认面板；页面明确说明影响对象、后果、默认动作和下一步；所有决定可返回修改且不丢失，基础安全检查仍先于最终提交 | 待开发 | **复验反馈：当前截图显示关系治理在冲突处理之前；代码核查确认 `governanceGroups` 会直接把流程切到 `governance`。** |
| DEV-55 | 导入完成后独立处理关系治理待办 | ① 直接进入关系治理页处理已有关系，确认无需重新导入；② 再导入一个会产生关系影响但基础安全检查通过的单个 Skill；③ 完成冲突处理并提交导入；④ 从完成页进入或稍后从图谱/详情进入关系治理待办；⑤ 分别验证保持现状、建立/转换管理关系、移除/回退等当前事实允许的选项及取消/失败恢复 | 导入流程和关系治理流程均可独立使用；导入成功后以“生成待办 → 立即/稍后治理”串成 Pipeline；导入向导不再出现难以理解的关系治理必经步骤，也不因普通关系影响阻塞 Skill 导入；完成页说明“已导入什么、发现什么关系影响、影响谁、下一步去哪里处理”；待办在重启后仍存在且可直接定位；治理选项明确说明改变对象、影响、可回退性，实际结果彼此不同；治理取消或失败不回滚已完成的普通导入，也不产生半成品；完成后待办状态和相关页面刷新；基础安全检查仍是导入提交前的独立门禁 | 待开发 | **新增代办：这是 DEV-54 的职责边界收口。代码已有独立治理路由及导入后治理待办的查询/跳转模型，但当前决策仍耦合在 `commit_import` 前，两个选项的用户语义也不构成清晰的决策差异。** |
| DEV-56 | 技能库无匹配结果时留在原列表工作区 | ① 进入技能库；② 在搜索框输入确定不存在的名称并执行搜索；③ 观察搜索框、筛选器、结果计数、视图切换和列表区域；④ 继续修改搜索词或点击清除搜索/清除筛选；⑤ 再切换表格/卡片视图并重复一次 | 无匹配时不跳转或替换成只剩空态的独立页面；原搜索框保持可编辑且焦点/输入可继续操作；筛选和视图控件仍可用；列表内容区显示紧凑、明确的无结果提示并提供清除入口；继续输入或清除后列表立即恢复，其他筛选和视图状态不丢失；真正的库为空、加载中和请求失败仍显示各自状态 | 待开发 | **新问题：代码虽未改变路由，但无结果分支提前卸载整个工作区，用户体验等同于跳转到“没有符合当前筛选条件的技能”页面；与 DEV-30 搜索区宽度/布局调整联动验收但不合并。** |
| DEV-57 | 完全相同 Skill 复制导入后的冲突历史 | ① 准备一个已在库中的 Skill 的完全相同副本；② 通过导入流程选择“复制导入”；③ 确认技能库中出现两个独立 Skill；④ 进入「技能关系 → 冲突处理」，分别查看待处理和已处理/已识别历史；⑤ 重启应用后再次查看，并从导入结果或技能库条目进入 | 导入流程能识别“内容完全一致”；选择复制后导入成功且不再次阻塞；冲突处理中能看到这次确定性重复事实的历史记录，明确原 Skill、复制 Skill、内容一致、复制保留决定、来源和时间；它不被误显示为仍待用户裁决；复用/跳过场景的状态分别正确；用户主视图不直接显示 UUID/哈希，技术细节可展开；重启后记录、计数和入口仍一致 | 待开发 | **复验结果：未闭环。** 当前代码已识别并落库，但冲突工作台只投影 `uncertain` 且未裁决项；完全相同且已复制导入的记录被过滤，导致技能库出现两个相同 Skill 而关系冲突处理中没有对应记录。关联 SR-3、既有 M-14 和 DEV-54/55。 |
| DEV-58 | 冲突人工决定页的决策层级与导入后同步 | ① 准备准-B 冲突；② 在导入流程观察三类用户选项、解释、颜色和选中态；③ 不提交时确认关系冲突页不应伪造临时历史；④ 提交后从导入结果进入「关系 → 冲突处理」；⑤ 重新扫描/重启后再次查看；⑥ 对已决定项运行 AI 分析 | “不导入/复用已有 Skill/独立导入”作为主决策文案，后果一眼可懂；导入提交后冲突事实可见，刷新/重启不丢；人工决定保持不变，AI 只能给建议不能覆盖；已处理项不重新阻塞 | 待开发 | **本次复验新增：视觉层级和提交后同步未通过/待修；根因已在 DEV-58 代码核查中定位，人工决定不被 AI 覆盖仍待验收** |
| DEV-59 | 共享 Agent 目录作为独立目录实体并只展示一张卡片 | ① 进入「发现 → 本机发现」并重新扫描；② 找到被多个 Agent 工作台引用的同一 `.agents/skills` 目录；③ 对照品牌专属目录和不同类型工作台；④ 在技能关联/部署目标中查找该目录；⑤ 执行展开、查看路径、排除/恢复（如可用） | 同一物理共享目录作为独立“共享目录”实体，仅一张卡片/一个目录目标；显示支持方数量/名称；不按 OpenClaw、ZCode 等各出一张重复卡；技能关联和部署目标也不复制目录；专属目录仍独立；路径规范、可用状态、排除操作和无障碍名称正确 | 待开发 | **复验反馈：未通过。** 截图已见同一共享目录按多个 Agent 重复出卡；代码虽有 `shared_reference` 前端聚合，但当前快照/投影未保证该标记有效，需追查数据源并修复全链路 |
| DEV-60 | 共享目录图谱拓扑 | ① 准备一个能复制/部署到共享目录的 Skill；② 确认同一共享目录至少被两个 Agent 工作台识别；③ 进入「关系 → 技能图谱」查看该 Skill；④ 点击共享目录节点和支持方 Agent 节点/边；⑤ 对照专属目录 Skill；⑥ 重启后再次查看 | 图谱呈现 Skill→共享目录→支持方 Agent 两段关系；共享目录是独立节点，支持方不是被伪装成 Skill 的直接部署目标；只有能力事实明确支持的 Agent 才连接；共享/专属目录、复制部署/目录识别关系有不同视觉编码和图例；节点详情、计数、筛选、治理入口和无障碍名称一致 | 待开发 | **新增登记：代码核查确认当前图谱未消费目录能力事实，尚不能证明上述拓扑已实现。** |
| DEV-61 | 技能抽屉/详情安全检查操作入口与取消长流程 | ① 从技能库打开抽屉，记录基础安全检查、AI 安全检查的状态和操作；② 打开完整详情页安全区做同样操作；③ 分别启动基础检查和 AI 检查；④ 在 AI 检查运行中取消；⑤ 关闭并重新打开抽屉/详情 | 抽屉中有的安全检查内容和操作详情页全部可达；两处均能运行基础安全检查和可选 AI 安全检查；AI 检查运行中有明确取消入口，取消后顶栏、操作记录和两处页面均为“已取消”；基础检查与 AI 结果独立展示，AI 不替代基础结论；运行中/失败/未配置/取消/完成状态一致，按钮具备键盘和无障碍名称 | 待开发 | **本次复验确认未闭环。** `SkillDetailPage` 只渲染 `SecurityEvidence` 结果，`SkillQuickDrawer` 的 `SecurityChecksModule` 只渲染状态；独立 `SecurityResults` 仅接入 AI 运行/取消，未提供基础检查运行入口。与 DEV-27、DEV-46、DEV-48 和 AGENTS.md 抽屉→详情包含关系联动 |
| DEV-62 | 技能库安全检查状态与结果拆列及密度优化 | ① 打开技能库并观察表头与多条 Skill；② 对照基础/AI 检查为未运行、通过、失败、运行中、未配置的条目；③ 对照有待处理/高风险结果与零结果条目；④ 打开同一 Skill 的抽屉和详情，确认完整结果与操作仍可达；⑤ 在窄窗口、键盘操作和横向滚动下复验 | “安全检查”列只显示基础/AI 状态标签，不再混入“待处理/高风险”统计；独立“检查结果”列展示真实的待处理、高风险等用户可理解指标，零结果有紧凑明确空态；状态列不因内容堆叠异常增高，表格仍可读可操作；抽屉/详情保留更完整结果和安全操作；不得把固定 0 当作真实结果；技术 ID 不进入主视觉 | 待开发 | **新登记。** `SkillTable.tsx` 当前只有一个 security 列，状态标签与结果指标混排；数据字段虽已分开，但列表投影未拆列，且 `pendingCount` 当前固定为 0。与 DEV-48、DEV-61 联动 |
| DEV-63 | 集中库同名 Skill 与 Agent/项目部署名称冲突 | ① 准备两个同名但用途/内容不同的 Skill；② 分别导入集中库；③ 对照集中库列表、详情和冲突处理记录；④ 将两者部署到不同 Agent/项目；⑤ 再尝试部署到同一 Agent/项目目标；⑥ 若有部署别名入口，分别用受管复制、符号链接/目录联接验证并检查 `SKILL.md:name`、原集中库文件、关系记录和回滚 | 集中库允许两个同名实体，并能用用途/来源/版本区分；不同 Agent/项目可分别部署；同一目标的相同有效名称在预览阶段阻断，不覆盖、不静默改名，提示可选择的处理方式；软连接/目录联接即使目标目录内部 basename 带集中库后缀，目标入口名仍必须与 `SKILL.md:name` 一致；若启用部署别名，仅受管复制可用，副本文件夹名与 `SKILL.md:name` 一致，原 Skill 内容和名称不变，部署关系记录原名/部署名并可回滚；链接/联接模式不得改写源文件且应明确禁用别名；所有页面不以 UUID/SkillId 作为主展示 | 待开发 | **新增边界规则。** 代码已确认集中库可用 `runtime_name--skill_id` 区分物理目录，版本对象保留原始 `SKILL.md`；当前部署计划 source path 指向 `versions/<SkillId>/<VersionId>`，不是可见目录 basename，目标入口仍按 runtime name；部署规划按目标路径 + runtime name 冲突，当前只有 name mismatch warning，没有部署别名机制。与 DEV-18、DEV-57 联动 |
| DEV-64 | 技能图谱搜索框内实时候选下拉 | 在技能图谱搜索框输入 Skill 原名/别名，观察候选展示、选择和清空 | 输入时在输入框附近以下拉呈现候选；支持鼠标/键盘选择、Escape/清空、加载/无结果/失败/失焦；不出现独立候选列表，不显示裸 UUID | 待开发 | 2026-09-20 已登记，待开发后复验 |
| DEV-65 | 技能库标签徽标与列表字段完整值提示 | 在技能库列表查看多标签、长用途、长调用方式、Agent/项目、版本和来源等字段；分别悬停、键盘聚焦并切换窄窗口 | 标签列按独立标签徽标呈现；所有可能截断的字段都能悬停或键盘聚焦查看完整值，并提供可访问名称；排序、筛选、密度和横向滚动不回归 | 待开发 | **2026-09-20 用例 7 新增。** 当前 `SkillTable.tsx` 的标签列仍为逗号拼接纯文本，完整值提示只覆盖部分字段，需统一列表字段的用户可读 tooltip/聚焦提示 |

### D5-11 / OPT-20260914-06：macOS Dock 图标尺寸复验补充（2026-09-20）

- **本次结果**：首次复验调整后的图标，圆角形状可接受，但与 Dock 邻近应用相比仍感觉略大。
- **分类**：既有人工验收项未关闭；不是新问题、不是回归，也不是自动化失败。当前未取得精确屏幕/视口尺寸，但该反馈已足以保留为光学尺寸待裁决。
- **证据边界**：Apple 规范提供 1024×1024 源图和系统缩放/遮罩基准，并没有固定 Dock 屏幕像素尺寸；当前自动化几何检查不能替代真机 Dock 对照。
- **下一步**：保留现有资产，制作一个仅增加透明外边距的可回滚候选；在相同 Dock 设置下与 Finder/TextEdit 对照后，再决定是否更新 `.icns` 和 release。Windows 图标不随本项联动修改。

## 2026-09-21 顺序开发新增人工流程

| 覆盖项 | 前置素材 | 操作步骤 | 通过标准 | 结果 | 截图/日志证据 | 现场清理 |
|---|---|---|---|---|---|---|
| DEV-50/51/52 | 新 release 产物；本地 Skill 目录；URL、Git 地址、`npx skills add` 文本 | 打开发现主页，确认无 lock 文案；打开手动来源模块，依次解析本地路径、URL、Git、`npx` 和无效输入；本地路径继续进入导入向导；直接访问旧 `/discovery/lock` | 主页仅有手动来源入口；本地路径沿用现有导入；远程来源显示真实支持/失败状态，不执行命令；旧路径明确回到普通发现页；键盘、窄屏和错误恢复可用 | 待验 | 记录产物版本、视口、截图和操作日志 | 删除测试导入/临时候选，不删除用户原目录 |
| DEV-53 | 可用 HTTPS ZIP/TAR；可用 Git 仓库；不可达地址；超大/恶意归档；网络开关 | 在手动来源中分别运行成功、不可达、重定向/域名阻断、超预算、取消、重试；成功来源继续候选审阅和导入；检查失败后无半成品、来源与版本事实可追溯 | 下载/解包有进度、取消、失败/重试；仅允许安全来源；无目录穿越/符号链接越界/临时残留；基础安全检查先于提交；`npx` 不执行 | 待验；自动化仅覆盖第一阶段 | 记录网络设置、来源 URL/Git、日志、临时目录清理结果和 release 截图 | 清理测试库/临时目录，保留失败证据路径 |
| DEV-59/60 | 多 Agent 识别同一共享目录的发现快照；含共享目录关系的 Skill | 重新扫描本机发现，确认共享目录仅一张卡；进入图谱查看 Skill→共享目录→支持方 Agent，点击节点/边并重启复验 | 共享目录是独立实体且不重复；只有明确能力事实的 Agent 被连接；节点/边名称可读，不出现裸技术 ID；布局、筛选和无障碍正常 | 待验 | 记录发现卡片、图谱三段拓扑和重启前后截图 | 删除测试关系或回滚测试部署 |
| DEV-64/65 | 含同名/别名 Skill、多标签、长用途/来源/版本值的 release 数据 | 图谱输入原名/别名并用鼠标、上下键、Enter、Escape、失焦操作；技能库悬停并键盘聚焦标签、用途、Agent/项目、版本、来源字段，切换窄窗口 | 候选下拉贴近输入框且不出现独立结果块；列表字段完整值可读、可访问；不显示 UUID/物理路径；筛选、排序、横向滚动不回归 | 待验 | 记录视口尺寸、键盘路径、字段完整值截图 | 清除筛选与测试数据 |

## 结果写回

每完成一项：本表填「结果 + 备注/证据」→ 同步《功能完成度与验收状态矩阵-2026-09-18》对应行 → 全部完成后评估 v0.2.0 发布。
