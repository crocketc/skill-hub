# 人工验收清单（2026-09-18）

更新时间：2026-09-23（**当前执行版**）。本文件只保留尚未执行的 Windows/macOS release 人工验收流程；已完成的自动化证据、旧版验收表和历史现场结论已归档至 `docs/development/archive/2026-09-21-人工验收清单整理/`。本文件是人工验收的唯一当前入口。

> **状态边界**：DEV-2—65 的代码与自动化验证已完成，但本文件中的人工结果均未预填。DEV-1 需要 macOS 第二个真实可写卷；DEV-8 需要可信的官方 WorkBuddy 绿色 logo 素材。未完成真实操作、截图和日志复核，不得写“人工通过”。

## 被测产物与记录规则

| 项 | 当前要求 |
|---|---|
| 被测产物 | 当前 macOS release 为 `target/release/skillhub-desktop`，已于 `2026-09-23 00:28:15 +0800` 从源码 HEAD `dfa4839f` 重建；Windows 使用 `target/release/skillhub-desktop.exe`。逐条记录实际平台、窗口尺寸、源码 HEAD 与产物路径。 |
| 平台记录 | 每条流程记录平台、系统版本、窗口尺寸、语言、主题、产物版本和源码 HEAD。 |
| 证据记录 | 每条流程填写“结果、截图/日志证据、现场清理”；失败保留现场，不先清理再补写。证据不得包含密钥、个人 Skill 内容或个人绝对路径。 |
| 反馈分类 | 新问题、旧问题未修复、旧问题回归、已修复后的边界遗漏；登记前先回查历史归档、当前源码和自动化证据。 |

## 前置准备与复原

| 编号 | 前置素材 | 准备方式 | 复原方式 |
|---|---|---|---|
| 准-A | 目标占用素材（DEV-18） | 在测试用 Agent 目录下创建与待部署 Skill 同名的目录和普通文件。 | 删除测试目录，确认原有条目未改变。 |
| 准-B | 同名不同内容冲突素材（DEV-54/58） | 复制一个测试 Skill，改动副本 `SKILL.md` 一行，放入测试扫描目录并重新扫描。 | 删除副本并重新扫描，确认计数恢复。 |
| 准-C | 远程来源素材（DEV-53） | 准备允许的 HTTPS 归档、允许的 Git 仓库，以及不可达、非 HTTPS、重定向、超预算和恶意归档样本。 | 删除下载、解包和测试库内容；保留失败日志引用。 |
| 准-D | 关系图与共享目录素材（DEV-59/60） | 准备多个 Skill、别名、长路径、不同 Agent 类型和同一共享目录被多个 Agent 识别的测试事实。 | 删除测试关系和测试部署，重新扫描确认源目录未被修改。 |
| 准-E | 元数据与安全素材（DEV-26—29、DEV-61/62） | 准备多标签、中文/英文逗号、长用途、翻译描述、上游更新、基础安全警告/高风险和无结果 Skill。 | 恢复原元数据；安全结果按产品允许的清理规则处理并记录。 |

## 2026-09-22 macOS release 尝试

- 环境：macOS 15.6.1 ARM64；产物构建时间 `2026-09-22 20:18:26`；源码 HEAD `286199928fad1be0d08894570e629f65dc831be7`。
- 结果：release 进程存在并打开本地数据库，但未出现可被桌面自动化层识别的窗口，无法继续页面操作；DEV-66—75 保持人工待验收，不判通过。
- 下一步：恢复可见且可操作的 release 窗口后继续 A、G 及 DEV-66—75；不以自动化测试、构建成功或后台进程存在替代人工通过。

## 2026-09-23 Windows DEV-66 复验记录

- 环境：物理显示器 `2560×1600` / `240Hz`；Windows 缩放 `175%`（`168 DPI`）；最大化 release 窗口的实际 client area `1462×866`。当前 Windows release 构建时间 `2026-09-23 09:42:57 +0800`。
- 结果：**未通过**。概览页出现页面级纵向滚动条；此项是 DEV-66 的既有“最大化时无页面或模块滚动条”判据未完全修复，不新建重复代办。用户口头反馈 macOS 最大化正常；未记录当时 release、缩放和有效内容区，不能替代 Windows 失败事实，也不能作为 macOS 通过证据。
- 修复与自动化：紧凑布局阈值已由 `44rem` 提升至 `56rem`，并新增 `1462×866` 几何 E2E；概览高度 E2E 7/7、定向 Vitest 31/31、lint、typecheck、前端 build 与 Tauri release 构建通过。新版 Windows release 已启动。
- 第二次复验：**未通过**。滚动条消失，但最大化和窗口化均出现大面积底部空白；窗口化本应保留自然排列与页面滚动，却被 `max-height:56rem` 误触发的紧凑规则压缩。该规则不能识别窗口是否最大化，且概览区块不消费 PageFrame 的剩余高度。
- 第三次复验产物：已于 `2026-09-23 10:44:03 +0800` 重新构建并启动 Windows `target/release/skillhub-desktop.exe`。代码已改为“原生最大化状态 + fill 高度轨道、窗口化自然页面流”；自动化覆盖最大化 `800×600`、`1462×866`、`1920×1080` 和窗口化对照。
- 现在执行：最大化时确认所有模块填满内容区且无滚动、压叠、遮盖或溢出；还原窗口后确认模块恢复自然排列、页面滚动可用且没有大面积强制留白。随后在 macOS 记录真实最大化 client area 复验同一判据。未完成真实复验前不得关闭 DEV-66。
- 第三次复验现场反馈：根滚动条已消失；但顶部 4 张次级指标未填满右侧一列，且左侧部署图 SVG 越过面板边界、与右侧标签卡视觉重叠。已登记为 DEV-77（已修复后的边界遗漏），本轮不改代码；请继续保留最大化截图并验收其它页面。
- 复现补充：从其他页面返回概览会重新挂载图表，溢出随之消失；仅切换到其他桌面应用再切回则仍溢出。此证据确认 DEV-77 图表问题属于首次挂载与最大化布局完成顺序的时序竞态；指标带右侧空列不受该操作影响。
- DEV-77 已实施并通过自动化：最大化四张次级指标改为共同填满一行；部署图和标签图以容器 `ResizeObserver` 在稳定布局后重新量尺；`800×600` 仍完整呈现全部明细。待本次 release 重建后人工验证，**不预填人工通过**。

## 2026-09-23 概览人工验收结果

- 结果：**通过**。用户确认概览页滚动条、最大化填充、指标区空列、图表 SVG 溢出，以及切换页面/切换其它应用后返回的布局问题均已解决。
- 关闭项：DEV-66、DEV-77。两项不再作为当前待验收项重复执行；后续若出现新的不同现象，先按历史证据分类为回归或新边界问题。

## 2026-09-23 概览统计问题登记

- 新问题编号：**DEV-78**。
- 人工现象：标签统计没有“无标签”类别，图中分类总数与技能库总数不一致；切换部署关系到“项目”时，Agent 的 Skill 目录被显示为项目部署。
- 源码核查：`tag_chart()` 使用 `tags JOIN skill_tags`，遗漏无标签 Skill，并会对多标签 Skill 按关联重复计数；项目部署查询使用 `COALESCE(t.project_id, 'project:none')`，把 Agent 目标的空 `project_id` 纳入项目维度。
- 建议更改：定义互斥标签归类规则并新增“无标签”类别，保证分类和等于 `skill_count`；项目维度仅统计 `project_id IS NOT NULL`，Agent 维度仅统计 Agent 目标；补充后端聚合和前端图表测试。

## 2026-09-23 技能图谱问题登记

- 新问题编号：**DEV-79**。
- 人工现象：默认图谱节点过大、边过短导致边描述被遮挡；缩小后 Agent 类型徽标发生溢出；按住画布拖动会选中节点文字；对某个 Skill 调整缩放、平移或节点位置后，切页或切换 Skill 返回未保留展示状态。
- 源码核查：布局固定使用 `192×48` 节点估算和按密度收缩的弹簧长度；图谱节点 `max-width: 12rem`，AgentIdentity 无图谱专用缩放规则；画布未设置拖动期间的文本选择抑制；返回状态仅保存按 `location.key` 隔离的 viewport，不保存节点位置和显示设置。
- 建议更改：按节点类型和实际画布尺寸缩小节点/徽标并拉长最小边长，确保边标签可读；增加拖动时的 `user-select: none` 和指针捕获保护；按稳定 SkillId 保存并恢复 viewport、节点坐标和显示设置，切换 Skill 时隔离状态。

## 2026-09-23 技能部署流程 Agent 展示问题登记

- 新问题编号：**DEV-80**。
- 复现入口：技能抽屉点击“添加” → 技能部署流程 → “选择目标”。
- 人工现象：目标列表显示 `agent-skills.shared-directory`、`openai.codex-cli`、`openai.codex-ide`、`anthropic.claude-code`、`google.gemini-cli` 等裸技术标识，没有品牌 logo 和“终端/IDE 插件/共享目录”等用户可理解类型。
- 分类：DEV-73/75 已完成后的部署流程消费点遗漏，不重开原编号。
- 源码核查：`DeploymentDialog.tsx`、`BatchDeploymentPage.tsx` 的选择列表和计划区域直接渲染 `target.label`；`DeploymentTarget` 缺少品牌、类型、共享目录等结构化展示字段；需继续检查部署结果链路。统一 `AgentPresentation` 已存在，但本流程未接入。
- 建议更改：补部署目标展示投影并让单项/批量选择、预览、提交结果及 `aria-label` 全部复用 `AgentPresentation`；共享目录显示独立共享目录徽标，裸技术 ID 仅放在折叠技术详情；补全从技能抽屉进入部署流程的消费点审查。

## 当前人工执行流程

每条流程完成后，填写“结果”和“截图/日志证据”；“待验收”是初始状态，不代表通过。

## 2026-09-23 Agent 展示全项目复查问题登记

- 新问题编号：**DEV-81**。
- 复现范围：技能列表“已添加到 Agent”列、技能抽屉“关联关系”、技能矩阵、技能详情关系、移除影响弹窗、项目 Agent 关联/注册选择，以及相关预览。
- 人工现象：部分位置显示裸目标/关系文字或出现错误徽标，未统一显示品牌 logo + 用户可理解类型；截图已确认技能列表和抽屉关联关系属于当前问题。
- 源码核查：`SkillMatrix`、详情 `RelationsPanel`、移除影响组件和项目 Agent 选择直接渲染 label；技能列表适配把 `physical_id` 当 Agent id，抽屉适配把部署关系 `record.id` 当 Agent id，统一 `AgentPresentation` 因此可能收到错误身份。
- 分类：DEV-73/75/80 相关的全项目消费点与数据投影遗漏，不重开原编号。
- 建议更改：先冻结逻辑目标 id、物理目录 id、部署关系 id、`agent_client_id` 和项目目标的边界，再建立统一展示投影；所有 Agent 主展示复用 `AgentPresentation`，共享目录独立呈现，技术标识只进折叠技术详情。

| 流程 | 覆盖项 | 前置素材 | 操作步骤 | 通过标准 | 结果 | 截图/日志证据 | 现场清理 |
|---|---|---|---|---|---|---|---|
| A. 产物、启动与基础可用性 | DEV-2—7、DEV-10、DEV-14、DEV-17、DEV-20、DEV-23、DEV-25、DEV-30、DEV-33、DEV-37、DEV-38、DEV-41、DEV-42、DEV-49、DEV-64/65、DEV-66、DEV-77 | `target/release/skillhub-desktop.exe`，`2026-09-23 11:12` 重建；标准窗口 800×600、1280×900、1440×900；另记录 OS 缩放后的实际 client area；键盘 | 启动产物后**首次**最大化打开概览；切至另一页再返回；切换至其他桌面应用后再回到 SkillHub；还原窗口并再次最大化。随后依次打开发现、技能库、详情、关系三页、仓库发现/管理、设置；切换中英文、默认/深色主题；用键盘完成导航、搜索、页签、抽屉、视图切换和返回。 | 应用可启动；无根级横向溢出；最大化概览完整填满可用内容区、无页面或模块滚动、无压叠/裁切/溢出；4 张次级指标共同填满指标带，部署图与标签图 SVG 始终留在自身卡片内；窗口化保持自然排列，允许页面滚动但不强制压缩；其余入口清楚、焦点可见。 | **通过。** 用户确认概览滚动条、填充、指标空列、图表溢出及切页/失焦回前台场景均正常。 | 自动化覆盖最大化 fill/窗口化自然流、指标带填满、SVG 容器边界；release 证据为 `2026-09-23 11:12` Windows 产物。 | 概览验收现场保留；DEV-66、DEV-77 已关闭，继续执行其它流程。 |
| B. 本地来源、手动来源与远程获取 | DEV-50—53 | 准-C；本地测试目录；`npx skills add` 文本 | 在发现页确认无旧 lock 入口；解析本地路径、URL、Git 和 `npx`；本地路径进入导入向导；远程来源分别执行成功、失败、取消和重试。 | 只解析不执行命令；来源和重定向受策略限制；下载/解包有预算和路径保护；成功来源可审阅；失败不写半成品且可重试；取消可恢复；来源坐标、版本/提交和哈希可追溯。 | 待验收 | 保存各来源类型、进度、取消/重试、失败清理和来源详情截图/日志。 | 删除测试下载、临时工作区和测试仓库；不删除用户原目录。 |
| C. 导入安全、冲突优先与后续治理入口 | DEV-18、DEV-20、DEV-48、DEV-54—58、DEV-76 | 准-A、准-B；安全候选；可选 LLM 供应商 | 先运行确定性基础安全检查，分别观察通过、失败、重试和阻断；对一次确定性安全拦截和一次输入无效提交关闭并重启应用；分析冲突后只处理必须的内容冲突并提交；确认导入完成页提供“现在整理 Agent 副本”和“稍后再说”，分别进入关系治理或返回技能库；重启后检查来源副本仍在且关系事实可查询。 | 基础安全未通过不可提交；无写入的拦截/输入无效保留为已撤销记录但不出现恢复闸门；流程不插入关系治理步骤；导入只复制到集中库、原目录保持不动；关系治理由完成页后独立进入，取消/失败不回滚普通导入；主展示无裸 UUID、关系 ID 和 `[object Object]`。 | 待验收 | 保存安全结果、冲突决策、失败后的重启与操作记录、导入完成页两种选择、通知和重启后状态。 | 删除准-A/准-B、导入副本；重新扫描确认计数恢复；清理或记录待办。 |
| D. 元数据、翻译、调用策略、运行要求与更新 | DEV-26—29、DEV-46/47、DEV-65 | 准-E；有上游和无上游 Skill；可用/不可用翻译供应商 | 在列表、抽屉、完整详情三处编辑别名、用途、标签；用中英文逗号保存并重启；执行描述翻译并测试取消/确认写入用途；查看调用策略和运行要求只读字段；执行上游检查和更新筛选；查看版本号与长字段提示。 | 三处读值一致且刷新；标签正确拆分并持久化；翻译有进行中/失败/取消/确认边界；调用策略不可编辑；运行要求来自真实声明，无声明有明确空态；更新状态不伪造；版本与来源语义一致；长字段可悬停或键盘查看完整值。 | 待验收 | 保存三处对照、翻译状态、更新前后版本/来源、筛选和完整值提示证据。 | 恢复原别名/用途/标签；清除筛选；按产品规则处理测试翻译记录。 |
| E. 部署、关系治理、计数与回滚 | DEV-21—23、DEV-40—43、DEV-46、DEV-55、DEV-59/60、DEV-63 | 准-D；可部署 Agent 目标；链接能力可用/不可用场景；同名 Skill 两份 | 从流程 C 的“现在整理 Agent 副本”或独立入口进入治理；分别验证品牌通用目录、共享 Agent 目录和独立 Agent 目录；执行确认、取消、失败、重试和回滚；进入图谱查看 Skill→共享目录→支持方 Agent；测试同一目标名称冲突和不同目标部署；解除部署并重启。 | 治理按物理目录一次执行：同品牌多端同目录显示“品牌通用目录”，行业共享目录显示“共享 Agent 目录”，不同目录分别治理；链接替换使用已验证临时链接、备份与恢复；成功后显示“已纳入 SkillHub 统一管理”，不展示内部键。 | 待验收 | 保存预览、提交、治理、图谱、回滚和重启前后证据；真实路径只写入现场受控记录。 | 解除测试部署；恢复目标；删除测试关系；重新扫描确认源目录未修改。 |
| F. 安全检查与操作记录 | DEV-48、DEV-61/62、DEV-65 | 准-E；基础检查可运行 Skill；警告/高风险/无结果 Skill；可用 LLM 或记录不适用 | 从抽屉、完整详情和独立安全页查看基础/AI 状态；启动基础检查和 AI 检查；AI 运行中取消；观察顶栏任务、操作记录、列表“安全检查/检查结果”两列；重新打开页面并重启。 | 抽屉操作在完整详情可达；基础与 AI 结果独立；AI 未配置/跳过不替代基础门禁；取消终态一致且不误报失败；状态列与结果列分离，零结果不伪造；主展示无技术 ID。 | 待验收 | 保存运行、取消、失败、完成状态及列表前后对照。 | 取消或完成测试任务；按产品允许的规则处理测试结果并记录。 |
| G. 图谱与全项目呈现一致性 | DEV-11、DEV-15、DEV-24、DEV-31、DEV-34—39、DEV-44/45、DEV-60、DEV-64 | 准-D；长路径/长名称；多个 Agent 类型；800×600 和宽屏；默认/深色主题 | 搜索原名/别名并用鼠标、键盘、Escape、失焦；拖拽节点和平移空白区；缩放/适配视图；点击 Skill、Agent、目录、边查看详情；查看图例、路径、时间、类型徽标和技术细节；在 Agent、发现、技能库、抽屉、详情、治理页对照同一 Agent。 | 节点/边与图例编码一致；空白区可平移、节点可拖拽、控件在画布内；名称/路径/时间/关系类型可读；Agent 使用品牌+用户视角类型；共享目录拓扑清楚；技术标识仅在折叠技术细节；窄屏、长文本、键盘和无障碍不回归。 | 待验收 | 保存图谱全景、选中详情、图例、技术细节、窄屏和键盘焦点证据。 | 清除搜索/筛选；取消临时选择；关闭应用。 |
| H. Windows/macOS 平台专项 | DEV-1、DEV-4/5、DEV-8、DEV-14、DEV-21、DEV-25、DEV-30、DEV-37、DEV-42、D5-11 | Windows/macOS release；macOS 两个真实可写卷；真实 Agent 目录；官方 WorkBuddy 素材（若未提供则阻塞） | Windows 执行 A—G 代表流程，核查路径、权限、链接/目录联接、缩放和 800×600；macOS 重复关键流程并执行跨卷写入、Dock 图标、大小写/权限/路径检查；记录平台差异。 | 平台行为符合清单；跨卷写入仅在 macOS 双卷条件满足后判定；未提供 WorkBuddy 素材时 DEV-8 记外部阻塞；不得用 Windows 自动化替代 macOS 证据。 | 待验收/待分类 | 每个平台独立记录产物、系统版本、视口、截图、日志和清理结果。 | 恢复两平台测试目录、数据库和部署目标；删除临时卷测试数据。 |

## 本轮新发现：转下一轮开发，不改变上一轮验收基线

以下问题均来自本轮人工截图和操作反馈，已回查当前源码事实；不回写为上一轮 DEV-2—65 未完成，也不阻断当前人工验收流程继续执行。

| 新代办 | 人工现象 | 当前代码事实 | 建议更改方案 | 下一轮验收判据 |
|---|---|---|---|---|
| DEV-67—72 | 已修复并合并至 `47e3c7d9`：标题/页签合并、图例悬浮、画布自适应、控件生效、关系页高度和工具栏收纳均已完成。 | 关系模块定向测试、全量 Vitest、关系 Playwright 回归、lint、typecheck、build 通过；人工待基于新 release 复验。 | 分别确认标题、图例、节点边可读性、按钮、无滚动布局和单行工具栏；记录 800×600、1280×900、1440×900 及最大化窗口证据。 | 图谱各项人工判据通过后关闭 DEV-67—72。 |
| DEV-73 | 已修复并合并 `79b7772f`：统一 `AgentPresentation` 覆盖列表、详情、发现、部署、技能关联、治理、图谱和 onboarding。 | 全量 170 文件/1864 测试、lint、typecheck、build 通过；支持未知类型、自定义 Agent、共享目录和同品牌多类型合并；人工待基于新 release 复验。 | 对照 Agent 列表、详情、发现、部署、治理、图谱和共享目录，确认主展示不出现技术 ID，品牌 logo、类型徽标和无障碍名称一致。 | 所有 Agent 主展示使用品牌 + 用户可理解类型，人工复验通过后关闭 DEV-73。 |

| DEV-74（关联 DEV-49） | 实现已完成（`f856fb23`）：仓库发现配置区和仓库管理页均接入共享 `RepositoryCard`，不再沿用截图中的列表行。自动化 targeted tests 29/29、lint、typecheck、前端 build 已通过；人工复验待执行。 | 基于新 release 分别进入仓库发现和仓库管理；检查卡片信息层级、启停/参与发现、扫描/刷新、重试、移除、添加和键盘焦点；覆盖宽窄窗口、长仓库名、未扫描/失败/禁用/空态。 | 卡片清晰展示仓库身份、分支、来源平台、状态、扫描事实和操作；两页视觉一致，无重叠/横向溢出，原有操作不回退；人工复验通过后关闭 DEV-74。 |
| DEV-75（细化 DEV-73） | 代码与自动化已完成（合并 `66b52489`）；本机发现页和 Cline 类型合并徽标已改为“终端”“协议接入”。 | 已核查并覆盖 `AgentKindBadge`、发现工作台、部署/关系身份、初始化兼容性及中英文词条；自动化 62/62、lint、typecheck、前端构建和 Tauri release 构建通过。 | 待基于新 release 人工复验：确认所有页面不再出现 CLI/ACP 裸缩写，组合类型和说明 title/无障碍名称可理解。 | 任何用户可见主展示不出现 CLI/ACP 裸缩写；组合类型显示为“IDE 插件/终端/无界面/协议接入”等可理解中文；Agent、发现、部署、图谱/治理口径一致。 |
| DEV-77 | 最大化概览：指标带右侧空列，部署图 SVG 越过左侧面板并与右侧标签卡视觉重叠。 | 已修复：4 列次级指标轨道；部署图和标签图监听容器 `ResizeObserver`，在稳定布局后的动画帧重测；低矮最大化时部署明细两列完整展示。 | 自动化已覆盖指标填满、SVG 容器边界、`800×600`、`1462×866`、`1920×1080` 和窗口化对照；用户已完成人工复验，确认首次进入、切页返回和失焦回前台均正常。 | **人工通过，DEV-77 关闭。** |
| DEV-78 | 标签统计缺少无标签且分类总数不等于技能库总数；项目部署统计混入 Agent Skill 目录。 | 待开发：冻结互斥标签归类规则、补无标签类别和总数守恒断言；项目维度排除 `project_id IS NULL` 的 Agent 目标。 | 自动化待补，release 人工验收待执行。 | 标签图分类和等于技能库总数；“无标签”可钻取；项目维度不显示 Agent 目录，Agent/项目切换口径独立。 |
| DEV-79 | 图谱节点/边距过大或过短、Agent 徽标缩放溢出、拖动选中文字、视图状态未按 Skill 保留。 | 待开发：调整布局与徽标尺寸；拖动时禁止文本选择；按 SkillId 持久化 viewport、节点位置和显示设置。 | 自动化待补，release 人工验收待执行。 | 节点与边标签可读；缩放后徽标不溢出；画布拖动不选文字；切页和切换 Skill 后各自状态正确恢复。 |
| DEV-55（旧问题未修复，2026-09-22 重新收口） | 归档已要求“导入完成 → 独立关系治理”，但旧向导仍在提交前插入治理步骤并要求决定。 | 提交 `fa4c2ee2` 已移除向导内治理步骤与强制决定；导入后提供“现在整理 Agent 副本 / 稍后再说”；导入仅复制并记录事实，治理单项/批量仍走独立工作台。应用层 59/59、前端定向 115/115 已通过，仍属人工待验收。 | 真实 release 验证导入后原目录不变、两种完成选择可达、治理失败或取消不影响已导入的集中库版本。 | 流程 C、E 均人工通过后关闭。 |
| DEV-76（R-10 旧问题回归） | 无写入的安全拦截和输入无效被错误记为“需要恢复”，重启后出现恢复闸门并阻断全部页面。 | `dfa4839f` 已将此类记录改为已撤销；仅清理失败且可能残留时保留恢复门禁。应用层全量测试和操作日志定向测试已通过，仍属人工待验收。 | 在 release 中分别触发安全拦截和输入无效，关闭重启并确认正常进入原工作区；再确认成功导入仍保留。 | 无写入失败不再阻断启动；操作记录可理解；真实残留仍有恢复入口。 |

| DEV-80 | 技能抽屉进入部署流程后，Agent 目标选择/计划展示仍是裸技术标识，未复用品牌 + 类型展示。 | 待开发：补结构化目标展示投影；单项/批量选择、预览、结果和无障碍名称统一复用 `AgentPresentation`；共享目录单独呈现。 | 自动化待补，release 人工验收待执行。 | 从技能抽屉进入部署流程，所有目标主展示均为品牌 logo + 用户可理解类型或共享目录；预览、提交、结果、键盘和无障碍名称一致，主展示不出现裸技术 ID。 |
| DEV-81 | 技能列表、抽屉关联、矩阵、详情关系、移除影响和项目 Agent 选择存在裸 label 或错误 Agent 身份投影。 | 待开发：统一逻辑/物理/关系/Agent 身份投影，所有 Agent 展示复用 `AgentPresentation`；共享目录单独呈现。 | 自动化待补，release 人工验收待执行。 | 上述所有页面跨页显示一致的品牌 logo + 类型/共享目录；主展示不出现裸技术 ID，路径和技术详情仍可追溯。 |

## 现场判定与结果写回

## DEV-78—81 修复后人工复验补充流程

| 覆盖项 | 前置素材 | 操作步骤 | 通过标准 | 结果 | 截图/日志证据 | 现场清理 |
|---|---|---|---|---|---|---|
| DEV-78 | 新版 release；含有无标签、多标签、Agent 目录和项目目录的测试数据 | 概览页记录技能总数；查看标签统计并逐项相加；切换 Agent/项目部署关系维度 | 标签包含“无标签”，分类总数等于技能库总数；项目维度不含 Agent 目录，Agent/项目统计互不污染 | **通过（2026-09-23）** | 用户已确认通过 | 不修改测试数据；关闭程序前保留证据 |
| DEV-79 | 新版 release；任一有多节点多边关系的 Skill | 打开图谱，观察默认节点/边；缩放、拖动画布和节点；切换其他页、切换其他 Skill 后返回 | 节点与边标签互不遮挡；Agent 徽标随缩放不越界；拖动不选中文本；每个 Skill 的视口、节点位置和显示设置正确恢复 | **待本轮复验**（旧结果：仅拖动禁选文字通过） | 记录缩放前后、返回前后截图及窗口尺寸 | 保留现场及当前偏好，待确认后清理 |
| DEV-80 | 从技能抽屉进入部署流程；至少一个被多个 Agent 识别的共享目录 | 依次查看目标选择、预览、提交结果；键盘聚焦并检查朗读名称 | 同一物理共享目录只出现一个“共享”品牌目标；可识别 Agent 厂商作为关联类型呈现；单项和批量一致 | **待本轮复验**（旧结果：同一路径重复三次；已补共享目录本体与引用整体归并） | 记录目标选择、预览和结果截图 | 取消或清理测试部署关系 |
| DEV-81 | 新版 release；存在多 Agent 部署关系的技能及窄列表列宽 | 依次查看技能列表、抽屉、矩阵、详情关系、移除影响、项目 Agent 选择/注册和目录预览；在紧凑列悬停徽标 | 正常区域显示品牌+类型；紧凑区域仅显示品牌小图标，悬停/无障碍名称提供品牌和平台类型；无裁切、无裸技术 ID | **待本轮复验**（旧结果：完整徽标被裁切） | 记录紧凑列及悬停提示截图 | 关闭抽屉、取消未提交选择、清理临时项目 |
| DEV-82 | 从技能抽屉进入部署流程；至少 6 个可写 Agent 目标 | 在宽窗口和窄窗口查看“选择目标”；鼠标与键盘勾选、取消、用 `?target=` 预选并进入预览 | 目标以紧凑小卡片网格呈现，宽屏多列、窄屏降列；路径单行截断且完整值可得；选择与预览行为不变，无横向溢出 | **通过**（新版 release 人工复验通过） | 记录宽/窄窗口、键盘焦点、路径提示和预览截图 | 取消未提交选择，关闭部署流程 |
| DEV-83 | 新版 release；节点较少且存在 Skill—Agent/来源关系的图谱 | 打开少节点 Skill 图谱，记录窗口尺寸；点击 fit-view/返回默认视图观察节点大小；拖动 Skill、Agent、来源节点，分别记录拖动前、中、后的画布和节点边界 | 少节点默认节点和徽标至少比当前版本缩小一半，关系说明仍可读；拖动时无空白遮罩、旧画面残留或画布裁剪；非目标节点不无故跳动，释放后位置和关系线一致 | **待本轮人工复验**（代码与自动化已完成；新版 release 已重建） | 记录窗口尺寸、拖动前/中/后截图和必要的 DOM/日志证据 | 退出图谱前保留视图状态，确认后再清理现场 |

1. 每条流程开始前记录平台、系统版本、产物构建时间、源码 HEAD、窗口尺寸、语言、主题和测试数据来源。
2. 发现问题先保留现场，按“新问题 / 旧问题未修复 / 旧问题回归 / 已修复后的边界遗漏”分类，再补步骤、截图、日志和复现条件。
3. 未完成真实复验不得写“人工通过”；自动化通过只能记录为“自动化通过，人工待验收”。
4. 每条流程完成后填写本表“结果、截图/日志证据、现场清理”，再同步《功能完成度与验收状态矩阵-2026-09-18》对应项。
