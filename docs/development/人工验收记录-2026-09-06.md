已修复已修复已修复已修复已修复已修复已修复已修复已修复已修复已修复已修复已修复已修复已修复已修复已修复已修复待复验（回归步骤见指南 1x/1y）||||||||||||||||||已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验（回归步骤见指南 1v）|已修复待复验 |已修复待复验 |已修复待复验 |已修复待复验 |已修复待复验 |已修复待复验 |已修复待复验 |已修复待复验 |已修复待复验 |已修复待复验 |已修复待复验 |已修复待复验 |已修复待复验 |已修复待复验 |# SkillHub 人工验收记录

验收日期：2026-09-06  
候选提交：`2fc5179`  
平台：Windows（源码启动）  
状态：进行中

## 1a. 初始化向导

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 主题选择 | 通过 | 主题选择与立即生效正常。 |
| 新建集中库目录选择 | 部分失败 | 可以通过系统选择器选择/新建本机目录；确认该目录为集中库后，界面仍显示默认集中库位置，未反映用户选择的目录。待确认是仅回显问题还是实际持久化/初始化目录错误。 |
| 初始化只读扫描 | 通过 | 扫描正常，可进入批量导入环节。 |
| 重复进入初始化向导 | 通过 | 可再次进入。 |

### 待处理问题

| ID | 优先级 | 问题 | 状态 |
| --- | --- | --- | --- |
| AR-001 | P0 | 集中库目录选择确认后仍显示默认路径；需核对实际库根、持久化值与 UI 回显是否一致。 | 已修复待复验（回归步骤见指南 1v） |

## 1d. 发现模块与初始化完成页

### 1d-1 在线搜索

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 检索 | 通过 | 可正常检索。 |
| 检索结果后续操作 | 重复问题 | 结果没有查看、安装或导入承接；与 AR-003 相同。期望参考 cc-switch 的卡片式“查看 / 安装”交互。 |

### 1d-2 仓库发现与发现页信息架构

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 扫描仓库 | 失败 / 体验阻断 | 点击后长时间没有任何结果或进度反馈，按钮保持置灰，用户无法判断是正在扫描、卡住还是失败。 |
| 仓库管理入口 | 交互缺口 | 添加/删除仓库不应与发现主页的摘要混在一起；期望独立按钮与独立管理页面。 |
| 发现页层级 | 产品/信息架构缺口 | 期望主页只保留四个固定大小的发现模块卡片、简介和入口按钮；每种发现方式进入独立子页承载详细搜索、配置、结果、导入操作，并提供返回上级按钮。希望参考 cc-switch 的交互方式。 |
| 发现页视觉 | UI 缺口 | 在具体交互迁往子页后，主页模块尺寸应尽量稳定，整体布局需要重新设计得更清晰、精美。 |
| `~/.agents lock` 导入 | 产品价值待决 | 验收者认为该模块与本机导入、在线检索、仓库安装重叠，当前用途不清晰；需评估是否保留、合并或降级展示。 |

### 1d-3 初始化完成页

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 完成页 | 通过（无新增问题） | 无新增问题；已记录的初始化、导入、路径问题仍适用。 |

### 新增待处理问题

| ID | 优先级 | 问题 | 状态 |
| --- | --- | --- | --- |
| AR-011 | P0 | 仓库扫描长时间无反馈、按钮持续置灰，缺少进度、取消、失败或超时后的明确状态。 | 已修复待复验（回归步骤见指南 1v） |
| AR-012 | P1 | 发现页缺少“首页摘要 → 独立发现子页 → 返回”的信息架构；仓库配置/管理与结果操作混杂在主页。 | 已修复待复验（回归步骤见指南 1y） |
| AR-013 | P2 | `~/.agents lock` 导入与其他导入方式重叠，产品价值和入口层级待决。 | 已决策执行：保留能力+视觉降级（末位次级卡片+说明），待复验（指南 1bb/1cc） |

## 1e. 后台导入、操作记录与忽略规则

### 1e-1 后台静默导入

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 导入状态与完成汇总 | 部分通过 | 第二次导入均为重复冲突，选择“独立导入”后导入耗时较长，但有进行中状态与最终汇总：“批量导入 Skill 已完成：成功 38、失败 0、跳过 0”。 |
| 导入期间重复提交 | 失败 / 交互缺口 | 导入进行中切换到其他页面再回发现页，导入按钮仍可再次操作。期望提交期间入口置灰/明确锁定，直至当前批次完成，避免重复导入。 |
| 查看操作记录 | 未完成闭环 | 完成提示提供“查看操作记录”链接，但操作与恢复的完整前后端能力尚未形成可用闭环。验收者要求后续完整开发。 |

### 1e-2 操作记录与恢复

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 打开操作记录 | 失败 | 页面显示：`operation was not found`。 |

### 1e-3 忽略项管理与修复预览

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 忽略规则语义 | 产品/交互问题 | 当前“精确 Skill 忽略”用途不清楚。验收者期望忽略应基于明确规则，或针对健康检查的某个发现维度/结果处理，而不是让用户直接输入精确 Skill。 |
| 健康检查与忽略/修复关系 | 需重新设计 | 期望将忽略规则、健康检查结果、修复预览和执行路径整合为一个可理解的工作流，重新设计并优化。 |

### 新增待处理问题

| ID | 优先级 | 问题 | 状态 |
| --- | --- | --- | --- |
| AR-014 | P0 | 后台导入进行中回到发现页仍可再次提交导入，缺少全局互斥/禁用与明确完成前保护。 | 已修复待复验（回归步骤见指南 1v） |
| AR-015 | P0 | “查看操作记录”后显示 `operation was not found`，完成通知无法进入有效记录。 | 已修复待复验（回归步骤见指南 1v） |
| AR-016 | P1 | 忽略项管理的“精确 Skill”语义不符合用户心智；健康检查、规则忽略、修复预览需合并重设计为可理解流程。 | 已修复待复验：自由输入入口移除（仅路径忽略）+工作流说明（指南 1x-8）；完整重设计待产品阶段 |

## 1f. 来源更新、Markdown 编辑与版本

### 1f-1 Skill 来源更新检查与决策

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 采用上游版本 | 失败 | 点击“采用上游版本”后显示：“来源更新操作失败：[object Object]”。错误未被结构化、可读地呈现。 |
| 本地创建 Skill 的更新检查 | 逻辑疑问 / 待核对 | 本地创建的 Skill 也能检查到更新版本；验收者认为没有明确上游来源时不应显示为可更新，需核对来源判定与真实数据。 |

### Markdown 工作区

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 编辑/预览布局 | 失败 / UI 缺口 | 用真实 Skill 内容编辑时，编辑区横跨页面并覆盖右侧实时预览；期望编辑与预览左右分屏、互不遮挡。 |
| 保存决策 | 交互/产品缺口 | 当前只有“保存并创建副本”。期望至少提供“返回且不保存”“保存覆盖当前 Skill”“保存为副本”三个明确路径，并解释各自影响。 |
| 另存副本语义 | 待产品明确 | 验收者认为“另存副本”应创建另一个 Skill；需明确新 Skill 身份、来源、版本与部署关系的规则。 |

### 版本展示与版本模型

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 版本展示 | 产品/UX 缺口 | 当前所有版本均显示完整哈希值，对普通用户不可读。 |
| 跨来源版本策略 | 需调研与重设计 | 期望联网导入 Skill 尽可能采用来源版本号/Tag/Release；本地 Skill 可让用户维护可读版本号，例如初始 `1.0`、更新后 `1.1`。需统一设计版本标识、内容哈希、来源版本与用户版本的关系。 |

### 新增待处理问题

| ID | 优先级 | 问题 | 状态 |
| --- | --- | --- | --- |
| AR-017 | P0 | “采用上游版本”失败且仅显示 `[object Object]`，来源更新错误不可读、无法继续处理。 | 已修复待复验（回归步骤见指南 1v） |
| AR-018 | P1 | Markdown 编辑区覆盖实时预览，未形成稳定左右分屏。 | 已修复待复验（回归步骤见指南 1v） |
| AR-019 | P1 | Markdown 编辑缺少不保存返回、覆盖保存、另存副本等明确决策及影响说明。 | 已修复待复验（回归步骤见指南 1v） |
| AR-020 | P0 | 本地创建且无明确上游来源的 Skill 也被识别为可检查更新，来源更新判定可能错误。 | 已修复待复验（回归步骤见指南 1v） |
| AR-021 | P1 | 版本直接显示完整哈希；缺少联网来源版本、用户可读版本和内容哈希的统一版本模型。 | 已修复待复验（回归步骤见指南 1x） |

## 技能库补充验收：组合视图与批量导入反馈

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 组合视图位置与占用 | 信息架构缺口 | 当前组合视图占用技能库主页面大量空间，挤压 Skill 列表；选中 Skill 后底部批量操作区进一步压缩可用空间。 |
| 组合管理形态 | 需完整重设计 | 倾向技能库默认保持“全部 Skill 明细视图”，仅保留“新建组合”入口；“组合管理”作为独立子页，按组合展示概要、说明、成员增删改查、导出、部署至 Agent/项目等能力，与全部 Skill 列表明确区分。 |
| 批量导入完成反馈 | 交互缺口 | “批量导入 Skill 已完成：成功 38、失败 0、跳过 0 [查看操作记录]”持续留在页面底部。期望改为可关闭的完成弹窗/通知（可自动消失），并在发现页提供“上次导入记录”区域承接历史；现有链接问题仍见 AR-015。 |

### 新增待处理问题

| ID | 优先级 | 问题 | 状态 |
| --- | --- | --- | --- |
| AR-022 | P1 | 组合视图与技能列表混排、占用过大；需重构为独立“组合管理”子页，覆盖成员、说明、导出与部署等完整使用场景。 | 已修复待复验（回归步骤见指南 1bb） |
| AR-023 | P1 | 批量导入完成反馈永久占据页面；缺少可关闭/自动消失通知与发现页“上次导入记录”承接。 | 已修复待复验（回归步骤见指南 1v） |

## 1f-2 批量导出入口

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 多选后的操作栏 | 交互缺口 | 当前按钮过多且有重复/语义不清：添加到、添加标签（重复）、运行安全检查、导出、归档、检查来源更新、发起导出、删除、清除选择。需要按日常使用频率重排、去重、分组；删除不应高亮为主要操作。 |
| 导出文件命名与目录 | 产品缺口 | 导出不应以内部 Skill ID 命名；应使用 Skill 原始名称。创建导出时应由用户通过本机目录选择器决定输出位置。 |
| 导出内容范围 | 产品缺口 | 导出必须包含每个 Skill 文件夹的全部内容，而不是仅 `SKILL.md`。 |
| 导出包结构 | 需重定义 | 多个 Skill 的整体导出始终应是一个外层压缩包。选择“文件夹”时，外层包解压后包含多个完整 Skill 文件夹；选择“ZIP 格式”时，外层包解压后包含多个独立的 Skill ZIP 包。后者用于 Trae、Cline 等可直接导入 ZIP Skill 的 IDE 型 Agent。 |

### 新增待处理问题

| ID | 优先级 | 问题 | 状态 |
| --- | --- | --- | --- |
| AR-024 | P1 | 多选 Skill 后批量操作栏按钮重复、过多且优先级不清；需按频率分组、去重、降低删除的视觉权重。 | 已修复待复验（回归步骤见指南 1x） |
| AR-025 | P1 | 标准导出不符合用户期望：内部 ID 命名、无本地目录选择、内容范围与多 Skill ZIP 嵌套结构需重新定义，并面向 IDE Agent 导入场景验证。 | 已修复待复验（回归步骤见指南 1w） |

## 1f-3 退出管理与设置层级

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 无部署关系时的退出管理状态 | 状态正常 / 说明不足 | 页面显示“当前没有部署关系”，预览影响与应用所选动作置灰；在当前无部署数据下符合状态，但用户不理解“退出管理”具体解决什么问题、何时需要使用。 |
| 退出管理说明 | 产品/文案缺口 | 需要面向普通用户说明这是解除 SkillHub 对已部署目标的管理、可选择保留集中库/处理目标关系等什么场景；不能只呈现技术动作。 |
| 数据保护与备份层级 | 信息架构缺口 | 当前“数据保护”位于设置的备份模块下。验收者认为层级应反转：数据保护是模块名，备份只是其中一个子功能。 |
| 设置整体模块划分 | 需完整重设计 | 应先基于需求与技术架构盘点所有设置项，定义上下级与分区，再设计前端 UI；不能以当前零散卡片作为最终结构。 |

### 新增待处理问题

| ID | 优先级 | 问题 | 状态 |
| --- | --- | --- | --- |
| AR-026 | P1 | “退出管理”缺少用户场景、影响与结果的清晰解释；无部署时虽正确禁用，但用途不可理解。 | 已修复待复验（回归步骤见指南 1v） |
| AR-027 | P1 | 设置页的模块上下级不合理：数据保护/备份关系倒置，整体设置项需按需求与技术架构重新盘点、分层和设计。 | 已修复待复验（回归步骤见指南 1cc） |

## 后续验收

## 1b. 恢复、发现与健康检查

### 1b-1 从备份恢复

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 选择既有备份并恢复 | 失败 / 未接入 | 重新运行初始化向导 → 从备份恢复 → 选择 `C:\Users\crock\SkillHub\\.skillhub\backups\skillhub-backup-39096-1788600107411879800` 后，显示：“此操作尚未连接到本机服务。不会创建目录、部署或导入任何技能。”验收时当前库已有 37 个 Skill，所选旧备份含 6 个 Skill。其他 1b-1 流程无异常。 |

### 1b-2 在线发现搜索

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 搜索结果 | 部分通过 | 可以检索到 Skill 结果。 |
| 搜索结果到导入的闭环 | 产品缺口 | 当前结果仅显示检索命中，与后续导入流程无关联。期望参考 cc-switch：结果以卡片展示，可“查看”（打开网页）或“安装”（进入明确的安装/导入流程）。 |

### 1b-3 集中库健康检查

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 运行检查 | 待确认 | 点击后立刻显示“未发现问题”。验收者观察到库中可能存在重名 Skill，疑问：重名是否应属于健康检查范围。 |
| 检查范围说明 | 产品缺口 | 界面未说明健康检查覆盖什么、不会覆盖什么、结果的用途。期望在按钮附近提供悬浮提示或等价说明。 |

### 新增待处理问题

| ID | 优先级 | 问题 | 状态 |
| --- | --- | --- | --- |
| AR-002 | P0 | 初始化向导的备份恢复显示“此操作尚未连接到本机服务”，无法恢复已选备份。 | 已修复待复验（回归步骤见指南 1v） |
| AR-003 | P1 | 在线发现搜索结果没有查看/安装或导入承接，搜索与导入是断裂流程。 | 已修复待复验（回归步骤见指南 1y） |
| AR-004 | P1 | 集中库健康检查范围、重复名是否覆盖及结果含义未向用户说明。 | 已修复待复验（回归步骤见指南 1v） |

## 后续验收

## 1c. 初始化后的导入、路径与冲突

### 1c-1 Agent 品牌分组

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 品牌分组 | 通过 | 初始化识别页与 Agent 页面均已按品牌分组；其余流程正常。 |
| 品牌视觉呈现 | UI 缺口 | 品牌名称未统一首字母大写；期望以具有品牌特征颜色的标签形式呈现，而不是当前普通文字分组。 |

### 1c-2 已选目录候选与混合导入

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 多路径候选读取 | 通过 | “读取已选目录候选”可识别所有已选路径。 |
| 已选路径调整 | 交互缺口 | 读取候选后无法直接临时取消其中一个路径或返回上一步调整；当前只能取消整个流程、重新点选路径、再重新读取。 |
| 手动补充路径 / 混合导入 | 交互缺口 | 多来源已选状态下无法手动追加目录并解析。期望导入流程支持“扫描来源多选 + 手动选择目录/其他来源”混合导入。 |
| 来源区域显示 | 逻辑/展示问题 | 未手动添加路径时，只要点击“读取已选候选路径”，仍出现“类型：本地路径；目标：`C:\Users\crock\\.claude\skills`”区域，且固定为扫描到的第一个路径。期望该区域仅在用户手动添加路径并点击解析后出现。 |
| 再次选择本地目录 | 失败 | 完成一次导入后重新到发现导入，选择本地路径时提示“无法打开目录选择器”。 |

### 1c-3 初始化后再次选择测试目录

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 选择测试目录并继续 | 失败 / 未接入 | 在之前 1c-2 导入后，重新运行初始化并选择测试目录，提示：“此操作尚未连接到本机服务。不会创建目录、部署或导入任何技能。”未执行保存并重启。 |

### 1c-4 备份恢复复验

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| 从备份恢复 | 失败 / 未接入 | 与 AR-002 相同：提示“此操作尚未连接到本机服务。不会创建目录、部署或导入任何技能。” |

### 1c-5 在线搜索与冲突决策

| 项目 | 结果 | 记录 |
| --- | --- | --- |
| skills.sh 搜索 | 通过 | 可检索到结果，之前搜索不可用的问题已消失。 |
| 冲突信息可读性 | 失败 / UX 缺口 | 批量导入发生已有 Skill 冲突时，界面直接显示 `import.same_runtime_name_conflict` 和长 UUID/拼接值，而非面向用户的冲突名称、原因、已有 Skill 信息与可理解的处理影响。示例候选为 `C:\Users\crock\\.claude\skills\pdf#pdf`。 |
| 批量冲突处理 | 交互缺口 | 期望先按冲突原因类别筛选，再为该类别选择处理方式；当前批量处理不够直观、不利于大量冲突审阅。 |

### 新增待处理问题

| ID | 优先级 | 问题 | 状态 |
| --- | --- | --- | --- |
| AR-005 | P2 | Agent 品牌分组缺少首字母规范化与品牌化彩色标签视觉。 | 已修复待复验（回归步骤见指南 1x） |
| AR-006 | P1 | 候选读取后无法局部调整来源；不能混合扫描来源与手动目录；未手动解析时错误显示首个扫描路径详情。 | 已修复待复验（回归步骤见指南 1y） |
| AR-007 | P0 | 已完成一次导入后再次选择本地路径，目录选择器报“无法打开目录选择器”。 | 已修复待复验（回归步骤见指南 1v） |
| AR-008 | P0 | 初始化中选择测试目录后提示“此操作尚未连接到本机服务”，且不保存/重启。 | 已修复待复验（回归步骤见指南 1v） |
| AR-009 | P1 | 导入冲突直接暴露内部 i18n 码、长 UUID/拼接值，缺少可读的冲突对象和影响说明。 | 已修复待复验（回归步骤见指南 1x） |
| AR-010 | P1 | 大量冲突缺少按原因筛选后再批量选择处理方式的高效审阅流程。 | 已修复待复验（回归步骤见指南 1x） |

## 后续验收

- 继续等待用户反馈。
