# 功能完成度与验收状态矩阵（2026-09-17）

更新时间：2026-09-17（**独立复核后第二次更新**）。自动化状态引用《自动化测试说明-2026-09-17》，桌面/系统状态引用《人工验收清单-2026-09-17》。任何一项未取得对应平台证据，都不能写成发布通过。

> 本次更新的依据：另一位 agent 的独立复核。相比上一版，**下调了 3 行的完成度**（Task 4、Task 12、关系治理链路），并修正「296 文件」数字错误。复核前版本见 `docs/development/archive/2026-09-17-独立复核前快照/`。

| 范围 | 代码/自动化 | Windows | macOS | 发布判断 |
|---|---|---|---|---|
| 关系投影、版本、图谱查询 | 已完成，既有 Task 1—3 证据归档 | 待真机 | 待真机 | 阻塞于真机 |
| 导入关系分组、复制副本/共享引用 | 已完成，Task 5—6 证据归档 | 待真机；链接权限需取证 | 待真机 | 阻塞于真机 |
| Agent 目录矩阵、Skill 详情关系视图 | 已完成；关系页面测试存在 | 待 J-REL | 待 J-REL | 阻塞于真机 |
| AI 冲突分析 | 已完成可选链路；真实凭据/服务仍按既有验收 | 待真机 | 待真机 | 非默认阻塞，真实链路需验收 |
| 冲突组生产写入链路（Task 10） | 已完成；持久化、重复导入、裁决保护有测试 | 待 SR-3/4 | 待 SR-3/4 | 阻塞于关键路径验收 |
| 统一异步执行反馈（Task 4） | **降级：主链路已完成（Agent/discovery 重扫描、导入提交、详情移除、四类即时写入）；`skill-detail`、`removal`、`security` 三处 nativeApi 的用户写入入口仍未统一** | 待 SR-1—7 | 待 SR-1—7 | 主链路可验收，低频入口待收口 |
| Task 9 复审小项 | 已完成自动化：布局、命名空间失效、溢出保护 | 待 J-REL-1—4 | 待 J-REL-1—4 | 需桌面确认 |
| Task 11 用户可见动作词收敛 | 已完成；术语专项测试 7/7 | 待 SR-8 | 待 SR-8 | 需桌面确认 |
| Task 12 关系测试夹具去链接依赖 | **降级为部分完成**：批量共享影响确认已改用无链接夹具；但 `facade_relationship_governance` 的 **17 项链路（迁移 prepare/commit/rollback、共享引用治理、批量取消）在本机被能力守卫提前 return，记为 passed 而非 skipped，未执行任何断言**（16 项静默） | RC-15 待验 | 需在有权限宿主补跑 | **不满足完成标准** |
| Task 12 附带：`governance_batch_requires_the_shared_impact_confirmation_per_row` 断言 | **削弱待恢复**：`prepared_count == 1 && state == Prepared` 退化为仅「确认阻塞消失」 | — | — | 违反 AGENTS.md 断言约束 |
| Windows junction 执行器 | 已修复 reparse buffer，且经独立逐字节复核确认为真修复；`deployment_filesystem.rs` 两项 `#[cfg(windows)]` 硬断言测试本机无提升权限实跑通过 | RC-13/14 待验 | 不适用 | 阻塞于 Windows 真机取证 |

## 当前发布阻塞项

1. Windows 真实 junction/符号链接权限与跨卷行为未验收。
2. 关系图谱、冲突工作台、治理清单的真实窄视口验收未完成。
3. 统一执行反馈的真实取消、恢复和通知深链验收未完成。
4. **`facade_relationship_governance` 的 17 项链路在本机未执行断言（静默 pass）；未取得有权限宿主/macOS 的实跑证据前，Task 12 不得标记完成。**
5. **`governance_batch_requires_the_shared_impact_confirmation_per_row` 的断言削弱未恢复。**
6. **`skill-detail` / `removal` / `security` 三处 nativeApi 的用户写入入口未统一，未取得对应测试前 Task 4 不得标记完成。**

## 收口电池结果（含独立复核修正）

- Rust 全工作区：`cargo test --workspace --all-features` 通过，142 个测试二进制、**1098 通过 / 0 失败 / 0 忽略**。**注意 `ignored=0` 是因为 17 项链路以 `return`（而非 skip 标记）跳过**，见上表。
- 前端单元全量：**165 个文件**、1735/1735 通过（上一版误写为 296 文件；`296` 疑似把 `apps/desktop/src` 下约 298 个 `.tsx` 源码当成了测试文件数）。
- 前端 E2E：独立点数 **387 用例 / 35 spec**，全量复跑 **387/387 通过、0 失败**，与上一轮报告一致（复现成功）。
- 前端生产构建通过（3820 modules transformed）；绑定生成 1/1 且按生成前后 SHA256 比对无漂移。
- `cargo fmt --all -- --check`、`pnpm verify:lifecycle`、`git diff --check` 通过。
- `ci-local`：10/10 步骤通过。依赖策略输出既有重复依赖与间接 yanked crate 警告，但策略检查结果为通过。
- 本轮 325 行新增代码经扫描无个人路径、无密钥；`test_key.txt` 已被 `.gitignore` 覆盖且未提交。
