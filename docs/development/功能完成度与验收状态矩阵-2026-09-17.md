# 功能完成度与验收状态矩阵（2026-09-17）

更新时间：2026-09-17（**第五次更新：待办处置入桥 + security 死代码删除**）。自动化状态引用《自动化测试说明-2026-09-17》，桌面/系统状态引用《人工验收清单-2026-09-17》。任何一项未取得对应平台证据，都不能写成发布通过。

> 本次更新的依据：上一版由另一位 agent 的独立复核下调了 3 行（Task 4、Task 12、关系治理链路）。第三次更新把这三处**当作待办逐项做完**（RC-15 取得 macOS 真实执行证据、能力守卫可观测且可反证、RC-16 断言恢复、Task 4 低频入口全部收口）；第四次更新继续收口扫描中暴露的**成类缺陷**（失败文案退化为 `[object Object]`，修在桥的缺省值上）并更正跨平台用例数的错误算术；第五次更新按用户裁决把两项「只登记」遗留落成代码：删除 security 四个不可达导出、把最后一个已知未入桥的用户写入（待办处置，含批量）接入统一执行桥。复核前版本见 `docs/development/archive/2026-09-17-独立复核前快照/`。

| 范围 | 代码/自动化 | Windows | macOS | 发布判断 |
|---|---|---|---|---|
| 关系投影、版本、图谱查询 | 已完成，既有 Task 1—3 证据归档 | 待真机 | 待真机 | 阻塞于真机 |
| 导入关系分组、复制副本/共享引用 | 已完成，Task 5—6 证据归档 | 待真机；链接权限需取证 | 待真机 | 阻塞于真机 |
| Agent 目录矩阵、Skill 详情关系视图 | 已完成；关系页面测试存在 | 待 J-REL | 待 J-REL | 阻塞于真机 |
| AI 冲突分析 | 已完成可选链路；真实凭据/服务仍按既有验收 | 待真机 | 待真机 | 非默认阻塞，真实链路需验收 |
| 冲突组生产写入链路（Task 10） | 已完成；持久化、重复导入、裁决保护有测试 | 待 SR-3/4 | 待 SR-3/4 | 阻塞于关键路径验收 |
| 统一异步执行反馈（Task 4） | **已完成**：主链路（Agent/discovery 重扫描、导入提交、详情移除、四类设置即时写入）之外，`skill-detail` 七处写入 + 来源更新、`removal` 的项目移除关系、`security` 的发现项处置/AI 检查取消均已接入 `runTrackedOperation`；单次写入用 `instant`（不占在途顶栏）、长流程用 `phased`；每入口有成功/失败断言，取消由可取消的 AI 检查覆盖。第四次更新补齐**失败反馈的可读性**：桥的缺省描述不再 `String(error)`，结构化 `AppError` 会渲染成含错误码的可读文案（一次性覆盖 27 个调用点 / 19 个文件），普通 `Error` 与自由文本原样透出。第五次更新把最后一个已知未入桥的用户写入——待办页处置（单条/批量/撤销）——也接入桥（`0bf619a7`），**当前不存在已知未入桥的用户可触发写入** | 待 SR-1—7、SR-10（含待办页处置的统一反馈取证） | 待 SR-1—7、SR-10（含待办页处置的统一反馈取证） | 自动化已完成，真机反馈仍待验 |
| Task 9 复审小项 | 已完成自动化：布局、命名空间失效、溢出保护（`4561fcda`、`4dc7f2f2`） | 待 J-REL-1—4 | 待 J-REL-1—4 | 需桌面确认 |
| Task 11 用户可见动作词收敛 | 已完成；术语专项测试 7/7 | 待 SR-8 | 待 SR-8 | 需桌面确认 |
| Task 12 关系测试夹具去链接依赖 | **已完成**：17 处符号链接守卫 + 1 处目录链接守卫改为**可观测且可计数**的跳过（`SKILLHUB-TEST-SKIP;` 写入进程 stderr，绕开 libtest 的逐用例捕获）；在具备符号链接能力的 macOS 上整套 **59 通过 / 0 失败 / 0 忽略 / 0 跳过**，关系迁移 prepare/commit/rollback 与共享引用治理链路逐条执行断言；负向对照（强制无链接能力）产出 18 条跳过标记，`REQUIRE_SYMLINK_CAPABILITY=1` 下无跳过，二者叠加 18 失败 | RC-13/14 仍需 Windows 凭据取证 | **已取得执行证据（`7853ae0f`）** | 自动化完成 |
| Task 12 附带：`governance_batch_requires_the_shared_impact_confirmation_per_row` 断言 | **已恢复**：重新断言 `prepared_count == 1 && state == Prepared`，并在 macOS 跑通；守卫改为「能建目录链接（符号链接或 junction）」，Windows 可回退 junction 的宿主仍执行（`4bc6b087`） | 不适用 | 已跑通 | 自动化完成 |
| Windows junction 执行器 | 已修复 reparse buffer，且经独立逐字节复核确认为真修复；`deployment_filesystem.rs` 三项 `#[cfg(windows)]` 测试本机不编译（macOS） | RC-13/14 待验 | 不适用 | 阻塞于 Windows 真机取证 |

## 当前发布阻塞项

1. Windows 真实 junction/符号链接权限与跨卷行为未验收（RC-13/RC-14）。
2. 关系图谱、冲突工作台、治理清单的真实窄视口验收未完成（J-REL-1—4）。
3. 统一执行反馈的真实取消、恢复和通知深链验收未完成（SR-1—7）；动作词桌面核对未完成（SR-8）；失败反馈的可读文案（不出现 `[object Object]`）未在真机确认（SR-10，自动化已覆盖代码路径）。
4. Windows 与 macOS 的 `cargo test --workspace --all-features` 用例数差额**已做源码侧归因，仍需一次 Windows 实跑闭合**：macOS 实测 1096（本机 `--list` 复核，142 个测试目标 = 135 `Running` + 7 `Doc-tests`）；平台门控为「Windows 专属 8 处 + Windows 专属测试模块 1 处 − Unix 专属 5 处」，故 Windows 应为 **1100**，而复核报告 1098 → **仍有 2 项未归因，且方向与上一版记录相反**。上一版「3 项 `#[cfg(windows)]` 不足以解释 2 项差额」是**错误的算术**（漏算 5 处 Windows 专属用例、1 个 Windows 专属模块和 5 处会反向抵消的 Unix 专属用例），本轮已更正。不影响功能判断，但在 Windows 实跑前不得写成「数字一致」。

> 上一版的第 4/5/6 条阻塞项（17 项链路未执行断言、RC-16 断言削弱、三处 nativeApi 未统一）**本轮已逐条关闭**，证据见上表与《自动化测试说明-2026-09-17》。

## 收口电池结果（本轮 macOS 复跑）

- Rust 全工作区：`cargo test --workspace --all-features` 退出码 0，**142 个测试二进制、1096 通过 / 0 失败 / 0 忽略**，日志内 `SKILLHUB-TEST-SKIP;` 计数为 **0**（即无任何被跳过的能力守卫场景）。上一轮在 Windows 宿主上为 1098，平台推算应为 1100，仍有 2 项未归因，见上方阻塞项 4 与《自动化测试说明-2026-09-17》。
- 前端单元全量：**165 个文件、1771/1771 通过**（第三次更新后 1759，第四次更新 +8；第五次更新 +5 桥接测试 −1 死代码测试。上一轮基线 1735，上一版曾把文件数误写为 296）。`pnpm --dir apps/desktop check`（ESLint `--max-warnings 0` + tsc）通过。
- E2E 全量（第五次更新复跑）：**387 passed / 0 failed**（2.9 分钟，exit 0，进程正常退出）；`workflow-pending` 的失败反馈用例已更新为「行内提示 + 通知中心」双通道断言。
- `node scripts/i18n-cjk-audit.mjs`：user-visible（必须修）**0 条**，仅剩 7 个已审阅豁免的 DEV/夹具文件。
- `cargo fmt --all -- --check`、`git diff --check` 通过。
- `node scripts/ci-local.mjs`：**10/10 步骤通过**（退出码 0，1 分 55 秒）——含 `cargo fmt --check`、`cargo deny check advisories bans licenses sources`、`cargo clippy --workspace --all-targets --all-features -- -D warnings`、`cargo test --locked --workspace`、前端冻结锁安装、生命周期策略、依赖审计、lint+typecheck、前端全量测试与**生产构建**（✓ built in 8.29s）。该次 `cargo test` 步骤同样报 **142 个 result 行 / 1096 passed / 0 failed / 0 ignored**，与 `--all-features` 全量一致。依赖策略输出既有重复依赖与间接 yanked crate 警告，但策略检查结果为通过。
- E2E 全量复跑：**387 passed / 0 failed（2.6 分钟，5 workers）**，387 行 `✓`、无失败产物目录；本轮进程正常退出，未复现上一轮的收尾卡死。
- 本轮新增代码经扫描无个人路径、无密钥、无真实 Skill 内容；未提交缓存或构建产物。
