# 功能完成度与验收状态矩阵（2026-09-18）

更新时间：2026-09-18（新快照）。自动化状态引用《自动化测试说明-2026-09-18》，桌面/系统状态引用《人工验收清单-2026-09-18》。任何一项未取得对应平台证据，都不能写成发布通过。

> 前置推导（2026-09-17 六轮收口）已归档于 `docs/development/archive/2026-09-18-六轮更新后快照/`：独立复核下调的 3 行（Task 4、Task 12、关系治理链路）已逐项补齐证据；桥的缺省失败描述成类修复；待办处置入桥；security 死代码删除；关系治理页 4 处失败通知抑制闭合（含登记漏计更正）。

| 范围 | 代码/自动化 | Windows | macOS | 发布判断 |
|---|---|---|---|---|
| 关系投影、版本、图谱查询 | 已完成，既有 Task 1—3 证据归档 | 待真机 | 待真机 | 阻塞于真机 |
| 导入关系分组、复制副本/共享引用 | 已完成，Task 5—6 证据归档 | 待真机；链接权限需取证 | 待真机 | 阻塞于真机 |
| Agent 目录矩阵、Skill 详情关系视图 | 已完成；关系页面测试存在 | 待 J-REL | 待 J-REL | 阻塞于真机 |
| AI 冲突分析 | 已完成可选链路；真实凭据/服务仍按既有验收 | 待真机 | 待真机 | 非默认阻塞，真实链路需验收 |
| 冲突组生产写入链路（Task 10） | 已完成；持久化、重复导入、裁决保护有测试 | 待 SR-3/4 | 待 SR-3/4 | 阻塞于关键路径验收 |
| 统一异步执行反馈（Task 4） | **已完成**：全部用户可触发写入入桥（主链路 + skill-detail 七处写入 + removal + security + 待办页处置，`0bf619a7`）；单次写入 `instant`、长流程 `phased`；每入口有成功/失败断言，取消由可取消的 AI 检查覆盖。失败反馈：桥的缺省描述渲染结构化 `AppError` 为含错误码的可读文案（27 个调用点），关系治理页 4 处失败通知抑制已移除（`f9f7c4fc`），失败详情与页面提示逐字同源 | **已取得部分真机证据（2026-09-18）**：SR-7 的「纯浏览零通知」通过（遍历 11 路由前后通知入口与通知集合完全一致，无未捕获异常）；SR-8 通过。SR-1—6、SR-7 的 operation id/深链半边、SR-10 仍待验 | 待 SR-1—7、SR-10 | 自动化已完成，真机反馈部分取得 |
| Task 9 复审小项 | 已完成自动化：布局、命名空间失效、溢出保护（`4561fcda`、`4dc7f2f2`） | **已取得部分真机证据（2026-09-18）**：800×600 真实窗口下概览单列无重叠、长内容内部滚动（550/1601）、根无横溢；其余三页布局同通过 | 待 J-REL | 布局已确认；内容相关判据待补 |
| Task 11 用户可见动作词收敛 | 已完成；术语专项测试 7/7 | **已完成（2026-09-18）**：真桌面 11 个路由文案核对——入口动作「添加到 Agent/项目」、移除类「移除/移除部署关系」，「部署」仅保留在技术名词（部署关系）中 | 待 SR-8 | 桌面文案已确认 |
| Task 12 关系测试夹具去链接依赖 | **已完成**：17 处符号链接守卫 + 1 处目录链接守卫可观测、可计数、可反证；macOS 上整套 59 通过 / 0 失败 / 0 忽略 / 0 跳过（`7853ae0f`） | **已取得执行证据（2026-09-18）**：Windows 58 通过 / 0 失败 + 17 条符号链接守卫标记；`SKILLHUB_REQUIRE_SYMLINK_CAPABILITY=1` 下 17 失败，证明守卫全部接线 | **已取得执行证据（`7853ae0f`）** | 自动化完成 |
| Task 12 附带：批量共享确认断言 | **已恢复**：`prepared_count == 1 && state == Prepared`，macOS 跑通（`4bc6b087`）；守卫兼容 junction 回退宿主 | **已取得执行证据（2026-09-18）**：目录链接守卫在该宿主上不跳过（基线 17 条标记而非 18 条），该用例以目录联接回退真正执行了断言 | 已跑通 | 自动化完成 |
| Windows junction 执行器 | 已修复 reparse buffer，经独立逐字节复核确认为真修复 | **RC-13 通过 / RC-14 部分通过（2026-09-18）**：非管理员、未开开发者模式下按能力探测选目录联接，落地为 reparse point、原文件零误删、探针零残留、来源目录更新后可移除；跨卷规则的**决策层**已在本机真实第二卷上实测（转换跨卷 → 拒绝并保留原入口；部署跨卷 → 受管复制、放行），仅「跨卷端到端写入」待补 | 不适用 | Windows 真机已取证，跨卷端到端写入待补 |

## 当前发布阻塞项

1. Windows 真实 junction/符号链接权限与用户文件保护**已取得真机证据**（RC-13 通过、RC-14 部分通过，2026-09-18）。**剩余**：跨卷的**端到端写入**验证（需一台带「可写且用户可选」第二个本地卷的 Windows 宿主；跨卷规则的决策层已在本机真实第二卷上实测，端到端写入未取得且未用替代手段伪造）。**范围澄清**：「跨卷」限制只作用于「纳入集中库管理」（复制改管理链接），**不影响部署**——跨卷部署实测选受管复制并放行。
2. 关系四页面真实窄视口：**J-REL-1 通过**；**J-REL-2—4 部分通过** —— 真实 OS 窗口 800×600 下四页的布局判据（根无横/纵溢出、跨滚动容器逃逸元素 0、主内容区重叠 0、不可达控件 0）全部通过，但内容相关判据（图谱节点/边文本、冲突成员列表与影响预览长文本、治理清单内部滚动）因本机**已配置 Agent 0 个、项目 0 个 → 关系/冲突/治理行数均为 0** 而未取得。补齐真实关系数据后复测即可，未用预览桩伪造（`/__preview/*` 仅 DEV 构建存在）。
3. 统一执行反馈：**SR-8 通过**、**SR-7「纯浏览零通知」通过**。**剩余**：SR-1—6（重扫描/发现重扫描/导入提交/冲突决定/取消长流程/失败恢复）、SR-7 的 operation id 与 `/operations/:id` 深链半边、SR-10 的真实 IPC 失败文案（均需真实写入操作，本机无已配置 Agent/项目，可用技能库既有 Skill 走「添加到 Agent/项目」补做）。
4. ~~Windows 与 macOS 的 `cargo test --workspace --all-features` 用例数差额~~ → **已闭合（2026-09-18）**：Windows 实测 **142 个测试目标 / 1098 通过 / 0 失败**，`--list` 计数同为 1098；差额归因为上一版平台门控清点漏了 2 个条目（1 个 `#[cfg(unix)]` + 1 个 `#[cfg(target_os = "macos")]`），非静默排除。两端与修正后推算（Windows 1098 / macOS 1096）一致。口径提醒：Windows 的 1098 含 17 条能力守卫跳过。细节见《自动化测试说明-2026-09-18》。
5. ~~**Windows 前端电池非确定性波动**~~ → **已处置（2026-09-18）**：3 例 `OverviewPage.test.tsx` 失败的报告原文是 `Test timed out in 5000ms`（超时而非断言失败），复跑 165/1773 全通过、该文件单独跑 25/25 通过。处置为逐用例预算 5 秒 → 15 秒（`apps/desktop/vite.config.ts`，附冷缓存 `environment 164.69s / import 80.31s` 的测量依据），断言未改、无人为延时；清空 Vite 缓存冷缓存复跑 **165 文件 / 1774 用例全通过**。非发布阻塞，已关闭。
6. **新发现缺陷（非发布阻塞，建议发布前修）**：`tauri.conf.json` 的 CSP `"default-src 'self'; script-src 'self'"` 未显式给 `connect-src`，`connect-src` 回落到 `default-src`，导致 Tauri IPC 自定义协议被 CSP 阻断、每次启动回退 postMessage，并常驻 2 条控制台 CSP 错误。Tauri 官方 CSP 参考值含 `"connect-src": "ipc: http://ipc.localhost"`。修法为一行配置，本轮只报告未改动。取证见《人工验收清单-2026-09-18》R-3 末条。

## 收口电池结果（2026-09-18 复跑，macOS）
- `node scripts/ci-local.mjs`：**10/10 步骤通过**（格式化、依赖与许可策略、clippy `-D warnings`、Rust 测试、前端冻结锁安装、生命周期策略、依赖审计、lint+typecheck、前端全量测试、生产构建）。依赖策略输出既有重复依赖与间接 yanked crate 警告，策略检查结果为通过。
- Rust 全工作区：**142 个测试二进制、1096 通过 / 0 失败 / 0 忽略**（2026-09-17 于 `--all-features` 全量测得，此后 `.rs` 未改动；今日 ci-local 的 `cargo test` 步骤再次复现）。日志内 `SKILLHUB-TEST-SKIP;` 计数 0。
- 前端单元全量：**165 个文件、1773/1773 通过**（该行是 2026-09-17 的测量值；此后新增 1 条取消路径回归测试 → 现为 **1774**，Windows 侧已实测 1774）。六轮累计自 1759 起 +14。`pnpm --dir apps/desktop check`（ESLint `--max-warnings 0` + tsc）通过。
- E2E 全量：**387 passed / 0 failed**（2.3 分钟，exit 0，进程正常退出）。
- `node scripts/i18n-cjk-audit.mjs`：user-visible（必须修）**0 条**，仅剩 7 个已审阅豁免的 DEV/夹具文件。
- `cargo fmt --all -- --check`、`git diff --check` 通过。
- 本轮新增代码经扫描无个人路径、无密钥、无真实 Skill 内容；未提交缓存或构建产物。

## Windows 宿主电池结果（2026-09-18 新增，Windows 11 家庭版 25H2 / build 26200，非管理员账号）

- Rust 全工作区：`cargo test --workspace --all-features` exit 0，**142 个测试目标 / 1098 通过 / 0 失败 / 0 忽略**；`--list` 计数 1098，逐条 `ok` 行数 1098。其中 **17 条为能力守卫跳过**（可见的 `SKILLHUB-TEST-SKIP;` 标记），真正执行断言 1081 条。
- 关系治理套件：**58 通过 / 0 失败** + 17 条标记；`SKILLHUB_TEST_FORCE_NO_LINK_CAPABILITY=1` 下 18 条标记；`SKILLHUB_REQUIRE_SYMLINK_CAPABILITY=1` 下 **41 通过 / 17 失败**。
- 9 个 Windows 专属用例全部被编译、执行并逐个 `ok`（含 junction 模块级用例与 updater 的 NSIS 用例）。
- 前端：`pnpm install --frozen-lockfile`、`pnpm --dir apps/desktop check`、`node scripts/i18n-cjk-audit.mjs`（user-visible **0 条**，与 macOS 一致）全部通过；前端全量复跑 **165 个文件 / 1773 用例全通过**（早于取消路径回归测试）；清空 Vite 转换缓存后的**冷缓存**复跑 **165 文件 / 1774 用例全通过**（`transform 20.37s / setup 60.63s / import 80.31s / environment 164.69s`）。首次全量的 3 例 `OverviewPage.test.tsx` 非确定性失败已定位为宿主负载下的 5 秒超时并处置：逐用例预算放宽到 15 秒并写明测量依据，断言未改、无人为延时。
- 真机文件系统证据（RC-13/RC-14）：junction 落地为 reparse point 且可读透；来源目录条目与树哈希在转换前后完全一致；探针零残留；来源目录整体替换后仍可移除且父目录无残留。跨卷规则的决策层在本机**真实第二卷**上实测：`paths_share_volume` 跨卷为 `false`、能力探针跨卷 `junction:false`、转换规划器跨卷 **REFUSED**（保留原入口）、部署模式选择跨卷 **`ManagedCopy`**（放行）、探针零残留且第二卷根条目数 4 → 4（零跨界写入）。跨卷的端到端写入仍待一台带可写第二卷的宿主。
- E2E 未在 Windows 上运行（非阻塞）；本机为新分支的干净克隆，无个人路径或密钥写入文档。
