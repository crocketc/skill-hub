# 开发状态（2026-09-10）

更新时间：2026-09-10
当前分支：`feat/v0.2.0-product-completion`
接手基线：`23338766ed2621633649e8870174fcf08d2e0875`（本次集中库生命周期计划的批准基线）
当前实现范围：Task 1～12 已按独立提交完成；当前正在同步自动化与真机验收证据。

## 已完成

- QA-001～QA-014 已全部修复并完成自动化回归（上一轮工作，含批量删除二次确认、Markdown 分屏/高度/所有权、品牌 Logo、元数据与版本标签等）；QA-015 确认为非 v0.2.0 范围。
- **v0.2.0 完整 LLM 能力接入已完成**（用户批准的全量范围，非最小范围），按 TDD 分十个阶段落地，共 10 个提交：
  1. `8ff8636` 领域模型、错误码、15 供应商预设、需求追踪与架构决策（`docs/llm/架构决策-2026-09-10.md`、`docs/llm/供应商兼容矩阵-2026-09-10.md`）。
  2. `0793791` OS 凭据存储、供应商配置持久化（迁移 0010）、偏好扩展、网络闸门同步。
  3. `45f0e16` 5 协议族适配器（OpenAI 原生/兼容、Anthropic、Gemini、Azure）、有界 runner、模型列表、两级连接测试、mock 契约测试。
  4. `8603dc3` 门面命令（保存/删除/启停/设默认/取模型/测连接）、能力分项门控（默认全关）、闸门接线、绑定再生成。
  5. `8e407b6` 描述翻译持久化、批量翻译（逐项失败不丢其他）、需更新标记、用户修订确认。（本提交曾遗漏 `translation_repository.rs` 源文件，已在推送前通过 fixup rebase 补入，中间提交均可独立构建。）
  6. `26b05da` AI 安全检查闭环：先跑需要更新的基础检查、证据遮盖明文凭据、版本化提示词、记录 basic_run_id。
  7. `372700b` 语义重复分析：确定性候选 + LLM 建议层，LLM 失败回退确定性结果并标注失败码。
  8. `76b1163` 联网搜索辅助：查询改写/扩展、扩展命中带来源标记、原始查询保留、失败回退普通搜索。
  9. `8d74320` 导入流程 AI 预检：用户发起/设置自动、逐对象成败展示、不绕过确定性门。
  10. `9495578` 前端 LLM 设置页（供应商管理卡片 + 能力开关卡片）、E2E 预览路由与用例、原子目录更新（US-046/047 正式入 v0.2.0）、用户文档 `docs/llm/用户配置与隐私说明-2026-09-10.md`、README 更新。
- 原子测试目录更新为 349 条（325 条自动化候选含已回填证据、27 条人工验收入口用例，新增集中库生命周期 3 条），覆盖 62/62 用户故事、10/10 全局规则；`node scripts/verify_atomic_test_catalog.mjs` 以生成目录为准。
- **LLM 闭环补齐轮完成（2026-09-10，完成验证器指出的四项偏差）**：
  1. 四项 LLM 功能前端用户闭环补齐：描述翻译（`skill-detail/nativeApi.ts` 接线 `translate_description`/`save_user_translation_revision`/译文展示；后端补 `overwrite_user_revision` 标志与空 hash 后端回填，绑定已再生成）、语义重复分析（新增 `SemanticDuplicatePanel` 挂载于详情"连接"分区）、联网搜索 AI 辅助（`OnlineDiscovery` 开关、扩展命中标记、失败回退提示）、导入 AI 预检（`ImportWizard` 冲突阶段可选预检与逐对象报告；AI 安全检查 UI 此前已存在，本轮仅补 E2E）。
  2. 新增 `tests/e2e/llm-loops.spec.ts` 5 条 E2E（描述翻译/语义重复/AI 安全检查/搜索辅助/导入预检）与三个 DEV-only 预览组件（`SecurityLlmPreview`、`OnlineDiscoveryPreview`、`ImportWizardPreview`）。
  3. 三份产品文档（需求文档、产品与交互设计、用户故事）头部加入带日期的 v0.2.0 实现状态注记，历史正文不重写。
  4. clippy 固定为 `--workspace --all-targets --all-features -- -D warnings` 变体，并修复该变体暴露的 3 个 feature-gated 测试文件缺省字段/参数问题。
- 闭环补齐轮全量验证通过：Rust 720 测试 0 失败、前端 764 测试（107 files）、E2E 37/37、fmt/clippy（--all-features）/构建/绑定漂移/i18n 审计/原子目录/发布就绪全部通过，详见《自动化测试说明-2026-09-10》。
- **浏览器自动化补齐轮完成（2026-09-10）**：把三项页面/交互层面的验收从"只能人工"收进 Playwright E2E——双语核心流程（`bilingual-core-flows.spec.ts` 12 条，Playwright `locale` 驱动 i18n）、品牌图标真实素材加载与解码（`brand-assets.spec.ts` 3 条）、300 Skill 规模下 2 秒预算/翻页/筛选（`library-scale-300.spec.ts` 3 条，TC-GR-10）；该轮 E2E 总数 37 → 55 全部通过。`SkillLibraryPreview` 新增 DEV-only `?total=` 种子参数。TC-GR-09/TC-GR-10 完成反向索引回填（待反向索引 291 → 289），TC-US008-06/07 补浏览器层证据。本轮发现并修复既有缺陷：`pnpm --dir apps/desktop check` 在 HEAD 上实际失败（`BrandTag.test.tsx` 依赖 Node 类型但 `@types/node` 未安装、tsconfig `types` 缺 `node`），新增 devDependency `@types/node@22.20.1` 并在 tsconfig `types` 加入 `"node"` 后通过；前端 764 测试复测通过。之后生命周期控制器补修新增 onboarding 2 条 E2E，当前完整 E2E 为 56/56，前端全量为 108 文件/777 测试。
- **集中库初始化与生命周期计划（2026-09-10）**：已完成 create/open-existing 目录语义、Pending/Active 运行时、facade 快照迁移、原地激活、首次恢复事务、Specta 绑定、三分支向导、只读重新发现和 onboarding 重启链路删除。首次恢复只有恢复与路径持久化成功后才发布 Active；失败保留 Pending，允许改选目标。自动化已覆盖，真实桌面仍需复测。
- **生命周期控制器补修（2026-09-10）**：bootstrap 快照加载失败现在只显示本地化 unavailable 状态，不暴露首次初始化分支；重新扫描增加“取消并返回设置”和“完成重新扫描”，且完成只触发路由回调，不调用初始化/激活命令。Agent 发现与扫描失败保留当前库路径和既有状态，并提供本地化可读错误与重试。定向前端回归 3 个测试文件 28/28、全量 Vitest 108 文件/777 测试、onboarding Playwright 2/2 通过；E2E 配置已固定独立 Vite 端口并由 Playwright 管理服务生命周期。

## 仍需推进

代码与自动化已完成；发布前仍缺真实设备证据：

- 人工验收入口已补充隔离数据库恢复方法、初始化与库迁移边界、逐步操作路径和 27 条人工用例的通过标准；尚未产生新的人工通过证据。
- TC-US001-M02 原“保存并重启”失败记录保留；当前步骤已改为“保存并继续”，无需重启，仍需在真实桌面确认保存后直接进入兼容发现并持续使用所选目录。
- 需重新复现人工反馈的“技能库多选删除影响页面压叠”问题：QA-001 已修复删除影响矩阵和二次确认逻辑，但当前没有针对该真实窗口布局的视觉 E2E，暂不能据此认定压叠问题已解决。

1. 按《人工验收清单-2026-09-10》恢复双平台真机验收（集中库新建/已有库/首次恢复/重新发现均需逐按钮复测）。
2. 完成 Windows 真机验收全部条目并回填证据。
3. 完成 macOS 真机安装、启动、真实写入、备份/恢复、导出、更新及权限/链接路径。
4. 验证真实签名更新包链路并同步验收矩阵。
5. 真实供应商联调验收后，将《供应商兼容矩阵》中"兼容"级别升级为"已支持"（契约测试不因真实验证而豁免）。

## 已知边界

- 当前无真实凭据环境，所有供应商支持级别最高为"兼容"；不得在发布物料中宣传"已支持"。
- LLM 能力默认全关；能力开启但网络总开关关闭时仍不发起任何请求。确定性安全检查不可被 LLM 结果替代，AI 层全部为建议性。
- 明确排除（命令契约与界面均无入口）：聊天、AI Skill 创作、自动改写、质量评分、兼容性判断、Embedding/向量索引/向量数据库、多模型自动路由、复杂成本管理、LLM 自动执行文件操作、恢复出厂、跨设备 Git 同步、NAS/SMB、流式。
- 不将真实凭据、用户 Skill 内容、个人路径写入测试、文档或提交历史；`.gientech/`、`test-results/` 等用户目录不提交。

## 接手入口

按以下顺序阅读：

1. 本文；
2. 《自动化测试说明-2026-09-10》；
3. 《功能完成度与验收状态矩阵-2026-09-10》；
4. 需要真实设备操作时再阅读《人工验收清单-2026-09-10》。

必要检查：

```bash
git status --short
git log -5 --oneline
git diff --check
```
