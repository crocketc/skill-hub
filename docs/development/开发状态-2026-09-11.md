# 开发状态（2026-09-11）

更新时间：2026-09-11（前端体验升级 Goal 完成；已关闭 LLM 设置与 Markdown 副本保存的自动化修复切片，当前未提交）
当前分支：`feat/v0.2.0-product-completion`
接手基线：`0a1a448`（前端体验升级 Goal 规划基线；上一阶段代码基线见《自动化测试说明-2026-09-11》）
当前实现范围：前端体验升级 Goal 的 T0 共享基础、T1–T4 页面迁移、T5 集成与独立审查全部完成；本轮初始化时机修复提交为 `44985da`，旧版本遗留激活状态兼容修复提交为 `fc243a2`。人工验收后的 P0-01/P0-02/P0-03/P0-04/P0-06 修复已改动桌面启动接线、LLM 管理命令、Markdown 副本命令、导入版本投影、部署目标错误投影、生成绑定、设置/Markdown/导入/部署 UI 与翻译；未改变 LLM 协议、数据库 schema、确定性安全检查或网络默认关闭边界。

## 人工验收 P0 修复（未提交）

- 生产桌面启动现在调用 `with_production_llm_runtime()`：同一 `NetworkGate` 注入 `HttpLlmTaskRunner`、`OsCredentialStore::native()` 和 LLM 管理器，消除“测试替身已注入、真实桌面没有 LLM runtime”的差异。
- 2026-09-11 已在用户授权下以真实 GLM 凭据直连 `GET https://open.bigmodel.cn/api/paas/v4/models`：请求成功，返回 10 个模型（首项 `glm-4.5`）。密钥未输出、未写入应用配置或日志；该证据仅证明服务端认证、网络与模型列表端点可用，不替代桌面配置持久化、系统凭据落盘或服务级/模型级连接验收。
- 新增 `clear_llm_provider_credential` 命令及设置页确认动作：仅删除本机凭据，保留供应商配置、模型、启用/默认状态；重复清除幂等，其他供应商凭据隔离。
- LLM 设置中的未配置、凭据、端点、鉴权、模型、限流、超时、协议等已分类错误映射为中英文可读文案；设置页不再直接显示 `[object Object]`、内部键或原始英文错误。
- Markdown 编辑器“另存为副本”已接入类型化命令：复制编辑内容为新的库内 Skill，原 Skill 与其当前版本不变；失败时保留本机草稿并提示检查集中库可写权限。
- P0-02 自动化覆盖：应用层临时库虚构 Skill 成功查询/原文不变与非法路径失败 2/2；Markdown 前端编辑器与 native facade 18/18；设置目录既有测试保持通过。
- P0-03 导入冲突自动化切片已完成：本地候选缺失哈希时只读计算规范内容树哈希；创建、导入提交、内容保存与 Markdown 副本保存会同步当前版本到 SQLite 的 `versions`/`current_pointers` 投影，避免真实已有 Skill 被误判为同名异内容。公开 `CreateSkill → AnalyzeImport` 集成测试覆盖“相同内容跨路径 → `exact_content`”和“修改内容 → 同名异内容”；前端冲突对象展示名称、运行时名和来源路径，不再以 UUID 作为主信息。
- P0-04 部署失败可诊断性已修复：目标级失败保留安全筛选后的错误码、严重度、参数与恢复动作（兼容原 `error_code`）；单个和批量结果统一映射为中英文可读文案，不再显示裸错误码、错误翻译键或对象。部署策略、预览/提交的非原子语义与步骤回退未在本切片改变。
- 本轮回归：`cargo test -p skillhub-application --test facade` 84/84、`cargo test -p skillhub-core --test import_conflicts` 3/3、`cargo test -p skillhub-core --test deploy_flow` 2/2、导入前端/native 测试 18/18、部署/翻译前端测试 30/30、`pnpm --dir apps/desktop check`、`cargo fmt --all -- --check` 与 `git diff --check` 通过。
- 定向自动化已通过：应用层凭据隔离/幂等契约 7/7，核心 API 契约 23/23，设置相关前端测试 69/69，`check`、i18n key parity、`cargo fmt --check` 和 `git diff --check`。尚未完成真实 Windows/macOS 凭据存储、GLM 手填模型与两级连接真机复测。

## 本轮初始化流程修复（`44985da`）

- 首次初始化的“新建集中库/使用已有集中库”选择在流程中只保存在前端状态；返回第一步后可以反复更换目录，不再提前调用 `activate_library_root`。
- 用户在最后完成初始化时才按当前选择调用一次创建/打开集中库；首次恢复分支保持原有恢复写入流程不变。
- 已补充失败测试、重新选择目录测试、最终提交时激活测试，并同步更新人工验收清单第 14 项；未修改 Rust、数据库或生成绑定。
- 针对旧版本已持久化且已激活的同一路径，新增前端兼容：锁定路径与当前选择一致时继续完成；路径不一致仍阻止切换并保留锁定错误。
- 本地自动化回归：前端单测 121 文件 / 971 测试通过，`check` 和 i18n 审计通过；相关 E2E 19/19 用例显示通过，但本机测试服务器清理阶段未退出，不能记为干净退出的 E2E 全绿。

## 已完成（本 Goal）

1. **T0 共享基础层**（`e083a8d`）：9 个主题完整锁定并补齐 `--ui-*` 语义 token（保留 `--color-*` 兼容别名）；新增 Field、Input、Select、CheckboxField、Switch、Icon（本地 SVG 注册表，与品牌 Logo 注册表分离）、IconButton（≥40×40 + aria-label）、PageFrame、PageHeader、首错聚焦辅助；Button 加载态（保留原文字 + aria-busy）；Drawer/ConfirmDialog 焦点管理；AppShell Skip Link 与唯一 main；`/__preview/ui-foundations` 展板；机械拆分 `settings.css`/`discovery.css`。
2. **T1 设置与 LLM**（`ec7c754`）：6 分区导航（1280+ 左侧 / 800–1024 顶部选择器）、右侧单列；供应商完整宽度实体行（类型/端点/模型/启用/默认/凭据状态）；新增/编辑抽屉（凭据不回显、焦点恢复）；逐字段必填校验与首错聚焦；删除确认；两级连接结果（服务/模型）分离展示，不合并；AI 能力开关与输出语言迁移，默认全关不变。
3. **T2 发现与共享卡片**（`eab3589`）：共享 `SkillCard`（`features/shared/skill-card/**`，已冻结）只含可证实字段；在线/仓库/lock 结果统一卡片化；3/2/1 列容器响应式；AI 扩展、回退、已在库、不可安装语义保留。
4. **T3-A 概览**（`59bc1bb`）：唯一首要指标 + 紧凑统计；1/2/4 列响应式；图表取色走 token、无动画路径、可访问明细。
5. **T3-B Skill 库**（`7fc1a75`）：默认增强卡片视图（消费共享 SkillCard），表格保留专业模式（单滚动所有者、比例列宽）；窄窗口筛选折叠并显示生效条件数；`SkillPagination` 两视图共用；300 Skill 预算测试继续通过；`nativeApi.loadViewMode` 未识别存储值回退改为 cards（集成提交 `d1c8d21`，含契约测试）。
6. **T3-C Skill 详情**（`95365d7`）：重组为身份/状态/正文/关系/生命周期五区，旧锚点映射保留；关系轨迹；窄窗口分区导航横向化；security 结果视觉迁移（判断逻辑未动）。
7. **T3-D Agent 与项目**（`9a6a2ab`）：响应式实体卡片、长路径可读、抽屉触发器焦点恢复、项目注册迁入抽屉（目录选择/重复提交语义不变）。
8. **T3-E Markdown**（`1624779`）：保守迁移；渲染标题层级映射（修复详情页多 h1）；宽表格滚动容器；图片稳定占位；保存/校验动态播报；grok-night 代码块/Mermaid 暗色调色板；DEV 路由构建期消除（+0.07 kB gzip 实测）。
9. **T4-A 工作流中心**（`3f2d36d`）：pending 按"风险—影响—建议操作"布局；操作记录结构化时间线（Intl.DateTimeFormat）；恢复页 Tabs 方向键/aria-controls；顶部锚定批量操作条不遮挡焦点。
10. **T4-B 初始化与重新发现**（`0503ecb`）：统一 WizardShell 步骤壳层；失败/取消/重复/恢复分支逐一保留；重新发现不触发初始化完成逻辑（既有契约测试继续锁定）。
11. **T4-C 导入**（`d411065`）：ImportShell 流程壳层（稳定步骤条与底部操作区、阶段切换焦点管理）；不重写 reducer/阶段机；AI 预检保持可选辅助。
12. **T4-D 部署与移除**（`7742be2`）：部署/移除统一"选目标→预览影响→提交→结果"；非原子批量风险紧邻提交动作；QA-001 影响矩阵与二次确认保留。
13. **T5 集成与审查**：每个 Task 合并后立即波次集成并跑完整 Green；独立审查两轮（渲染界面审查 + 代码范围审查），P0 项为零。审查发现的问题已逐项修复或记录处置（见下"审查问题处置"）。死 CSS 规则、死 i18n 键清理；`OperationIndicator` 关闭按钮字符图标替换为注册表图标。

## 审查问题处置（逐项）

已修复（TDD 或几何/E2E 断言锁定）：

- aurora 渐变扩散到主按钮：`.sh-button` 改为 `var(--ui-accent)` 实色（E2E 断言 9 主题 `background-image: none`）；渐变仅保留在 aurora 主题定义与主题预览。
- 控件高度阶梯：默认按钮 36→40px（标准 40/紧凑 32/主操作 44）。
- 圆角阶梯 6/8/12/16/22 → 4/6/10/14px（theme.test 锁定）。
- 侧栏折叠按钮 25×32 与保存视图删除按钮 45×32 → ≥40×40（规格 4.2）。
- `--ui-accent-strong` token 缺失（悬停色回退失效）→ 9 主题补齐。
- 设置页"重新发现"链接浏览器默认色 → 主题 token。
- 粘性条玻璃效果（分页栏/导入操作区 backdrop-filter）→ 实色表面；对话框遮罩 blur 移除。
- 悬停上浮（部署芯片 translateY）移除；侧栏折叠按钮装饰渐变+硬偏移阴影改 token。
- BrandTag 深色主题适配（品牌倾向色保留，外围底色/边界由语义表面色混合派生）。
- 侧栏标签省略号失效（flex 收缩）修复；窄窗口保存视图行换行防按钮被推出视口。
- 字号阶梯 token 归位（xl 20→22、2xl 24→28）；发现/待办/详情长文本 72ch 行宽。
- 移除流程错误态：图标+危险色 + `role="alert"`；预览夹具不再透传原始 i18n key。
- 发现页初始空白 → 搜索前引导文案（role=status）。
- 库预览 `?total=0` 可表达空库。
- 英文复数（1 projects/1 agents 等）→ i18next `_one/_other` 形式。
- 批量部署目标行对齐（复选框紧邻名称，状态靠右）。
- 测试接缝违规：`SkillDetailPage.test` 新增类名查询改为 role+DOM 关系断言；`OperationIndicator.test` 类名查询移除；`llm-settings.spec` 类定位器改为元素+文本过滤；`library-card-view.spec` 类计数断言改为滚动所有者几何断言。
- `OperationsList` 无证据 `useMemo` 回退（计划第 2 节：性能优化需前后证据）。

记录处置（非缺陷或属后续范围，不构成审查未关闭项）：

- 运行时图表容器 `aria-hidden` 且有明细列表文字等价（审查 B6 为误报）。
- `InvocationBadge` 禁用态 `×` 为 aria-hidden 状态字形且有文字标签，非正式操作（规格允许正文/状态字符除外）。
- `BatchOperationSummary` 状态胶囊以文字承载语义（不只靠颜色），保持现状。
- ImportShell 保留在 `features/import/`（单消费者 + RemovalShell 等价契约），泛化为 `ui/**` 列为可选后续。
- 字号 13px/16.38px 散点、800px 设置分区选择器滚动提示为已知小项，随 T5 真机视觉验收一并复核。

## features/* 迁移清单（T5 扫描结论）

| 目录 | 状态 |
|---|---|
| overview / settings / discovery / shared / skills / skill-detail / security / agents / projects / markdown / pending / operations / recovery / import / deployment / removal / onboarding / bootstrap（BootstrapGate 展示态） | 已迁移 |
| backup | 保守保留（无独立视觉重构，沿用共享工作流样式与 token 兼容层；备份 API 与业务语义未动） |
| dev-preview | 基础展板新增（DEV-only，生产构建消除） |

## 仍需推进

1. **完成并复测人工验收 P0**：LLM 设置生产接线、凭据清除、设置页分类错误文案、Markdown 副本保存、P0-03 导入冲突判定/展示与 P0-04 部署目标失败文案的代码与自动化切片已完成；GLM 服务端模型列表直连已通过，但仍需真实桌面验证配置持久化、系统凭据、手填 GLM 模型及服务级/模型级连接，以及副本真机查询、跨路径冲突展示和部署失败提示。P0-05 操作记录持久化已定位，待获设计确认后实现；导入 AI 预检与详情关系分析的内部键/对象错误仍需各自独立修复，不能误报为设置页已关闭。
2. **再完成 P1 工作流与信息架构**：固定导航与表单一致性，发现来源模型和扫描→导入上下文，Agent 适配器/ZCode，Skill 库/组合、详情、项目、Markdown 和重新发现/移除流程的任务重构。涉及产品取舍的项目先写简短设计决策，保持原文件保护、确定性安全检查与生成绑定边界。
3. **Windows/macOS 真机复测与发布证据**：按《人工验收清单-2026-09-11》复测修复项，并完成原表 19–27 的只读目录、链接/联接、缩放、减少动效、中英文、安全、安装、备份恢复、签名更新与真实凭据两级连接。自动化与预览不能替代真机取证。
4. 已验证的首次初始化流程维持现状；仅需按修复后版本重新取证，确认 Agent 识别步骤返回第一步时最终只创建/打开最后一次选择。`ImportShell/WizardShell` 泛化为 `ui/**` FlowShell、字号散点归位属于非阻塞后续，不应挤占 P0/P1。

## 已知边界

- 本轮全部为自动化/预览证据；不得将 Playwright/Vitest 结果描述为 Windows/macOS 真机视觉验收。
- 9 个主题的名称、顺序、持久化、system/light/dark 映射与注册调色板由 `theme.test.ts` 与 E2E 锁定；主题观感（字体、阴影、真实对比度）仍需真机确认。
- `.gientech/`、`test-results/` 等用户未提交内容保持未跟踪、未提交。

## 接手入口

按以下顺序阅读：

1. 本文；
2. 《自动化测试说明-2026-09-11》；
3. 《功能完成度与验收状态矩阵-2026-09-11》；
4. 需要真实设备操作时再阅读《人工验收清单-2026-09-11》。

必要检查：

```bash
git status --short
git log -5 --oneline
git diff --check
```
