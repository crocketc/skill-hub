# 开发状态（2026-09-16）

更新时间：2026-09-16（关系闭环计划 Task 5—10 全部完成；技能关系与统一执行计划 Task 1、Task 2、Task 3 已完成代码/自动化闭环；真机验收仍未开始）
当前分支：`feat/v0.2.0-product-completion`（本轮全部改动直接落在此分支，HEAD 见 Git 记录；本地与远端以推送后核验为准）。
本轮范围：`docs/superpowers/plans/2026-09-15-skill-management-closure-implementation-plan.md` 的 Task 5—10（单/批量导入关系分组确认、复制副本/共享引用纳入集中库管理、Agent 页与 Skill 详情关系视图、可选 AI 冲突分析、冲突组生产写入、端到端回归与文档收尾）。未宣称真机通过；原文件保护、确定性检查优先、网络默认关闭、生成绑定只由生成器更新等边界全程保持。2026-09-16 已确认下一阶段设计及实施计划：v0.2.0 增加技能关系一级模块（技能图谱、冲突处理、关系治理）及全项目统一异步执行反馈；实现尚未开始。

## 已完成（2026-09-16）

### Task 5：单/批量导入的关系分组确认

- 核心层按设计 §4.1 实现六类确定性分组：完全重复、内容一致的复制副本、同名不同内容、通用目录直接读取、通用目录链接引用、识别未知或共享影响未确认；判定顺序固定（同名冲突 → 已验证观察副本 → 完全重复 → 共享目录引用/读取 → 已知来源不建组 → 识别未知）。关系完全明确的普通导入不再生成治理组，避免受阻关系记录噪音。
- 分组携带结构化影响事实（来源路径、受影响 Agent）；界面文案（数量/受影响 Agent/回退方式）全部由客户端按 i18n 渲染，core 不产出散文。关系处理记录类型按分类一一映射，持久化稳定 detail 键。
- 导入向导在冲突分析之后插入治理阶段（无分组时跳过）；分组确认 + 单项覆盖，未显式确认不能提交；修复了流程步骤计算在无治理分组时的错位。经两轮独立复审（第二轮 APPROVE）。

### Task 6：复制副本/共享引用纳入集中库管理

- 核心纯函数转换计划 + 体积精确链接能力探针（在关系父目录内做创建→校验→清理探针，零残留）+ 同卷/跨卷事实（无法取卷身份时 fail-closed）。
- prepare 增加可行性闸门：链接不可用/跨卷 junction/需动共享本体时明确失败并生成受阻关系记录，原入口不动、不产生备份；绝不静默降级为复制覆盖。用户文案统一使用“纳入集中库管理”，技术详情再说明管理链接。
- commit 严格按「重扫 → 指纹比较 → 备份 → 暂建链接 → 验证 → 移除原入口 → 原子换入 → 再验证 → 关系落库 → 审计」执行；失败路径要么保留原入口，要么从恢复点回退；不存在双入口残留路径；外来同名条目拒删。顺带修复两个既有缺陷：关系影响范围漏 `link_target_directory_id`、备份路径校验穿透符号链接误判。
- 独立复审两轴均 MINOR 无阻断；Windows junction 执行器为既有桩（探针诚实报不可用），真机 junction 行为留给人工验收。

### Task 7：Agent 页目录矩阵与 Skill 详情关系视图

- Agent 详情新增目录矩阵：目录路径/角色/识别状态（supported/unknown/unsupported；绝不表述"已加载/一定会调用"，常显否定式免责句）/优先级证据/Skill 数/共享消费者/每 Skill 关系行（用户语义标签 + 移除影响入口 + 技术详情折叠）。
- 共享消费者只认"已确认支持"的登记能力与活动关系（unknown/unsupported 不推断）；其他 Agent 的关系行显式标注归属；未决受阻关系按关系计数展示。
- Skill 详情新增多来源存证、冲突处理记录、关系类型与移除影响入口（只读预览，转换执行归 Task 6 命令）；共享视图模型与移除影响组件在 relationshipGovernance 模块复用。独立复审发现四处缺口（硬编码顿号、unknown 计入消费者、归属未标注、受阻关系未渲染）全部修复并补测试。

### Task 8：可选 AI 冲突分析

- `AnalyzeConflictScope`（all/category/case/skill）+ `AnalyzeConflict` 命令；输出短结论（≤200 字符、证据/不确定点 ≤4 条、置信度 0—100）、推荐动作与保留成员（必须命中组成员）、失败码；畸形响应诚实报 `llm.invalid_structured_response`。
- 结果携带 `total_case_count`：范围超过单次请求上限（16 组）时如实上报总数，界面标注"已分析 N 组（范围内共 M 组）"，不假装覆盖全部。
- 新迁移 0015（`conflict_analysis_records`）持久化分析记录（含输入内容指纹、确定性基线、来源、失败码、采纳标记）；AI 绝不改写 `ConflictCase.user_decision`；已裁决组跳过并计数。
- capability 关闭/未配置供应商时只返回确定性基线 + 诚实失败码，不留伪造记录。修复 Task 5 遗留：面板 `aiAvailable` 改接真实 LLM 供应商配置信号（与设置页同一判定），"AI 未配置"提示生产可达。
- 独立复审 Standards APPROVE；Spec 轴三发现中"静默截断"已修复入库，其余两条属基线既有系统性缺口（见"仍需"第 2 条），按协作规则报告并登记，不擅自扩范围。

### Task 9：端到端回归、绑定漂移与文档收尾

- 新增 E2E：导入关系阶段全流程（5 步流程、确认门槛、单项覆盖、AI 不可用提示、关系入口）与关系视图（Agent 目录矩阵、Skill 详情关系区、移除影响预览）；预览桩补齐治理场景与确定性关系事实。
- 全量验证电池在主工作区复跑全绿（明细见《自动化测试说明-2026-09-16》）。
- 四份当前文档更新为 2026-09-16 快照，旧快照移入 `docs/development/archive/2026-09-15-关系闭环Task5前快照/`。

### Task 1（技能关系与统一执行计划）：关系投影、关系版本与图谱查询契约

固定三切片实施，每片在对应 seam 定向测试转绿后再进入下一片（提交 `0c0b526`、`f78616b`、`cd37f5b`、`4478039` 为切片 1）：

- 切片 1（core 纯投影，`crates/skillhub-core/src/relationship/graph.rs`）：`project_skill_relationship_graph` 只读取既有关系事实，产出单中心 + 一跳关联 Skill + 上下文叶子（来源/Agent/项目/目录/冲突）；Agent、项目、来源、目录都不得反向带出其他 Skill；只有 `uncertain` 且无人工决定的冲突才成为冲突节点；关系类型/状态筛选只隐藏展示内容，不改事实；同类上下文超过上限折叠为折叠节点且不留悬空边；输出顺序在输入反转时稳定。`crates/skillhub-core/tests/relationship_projection.rs` 13 项全绿。
- 切片 2（storage 关系版本与迁移）：新增 `0016_relationship_projection_state.sql`（单行单调 revision + `last_verified_at`，不建第二套关系事实表），`CURRENT_SCHEMA_VERSION` 升到 16，升级测试覆盖 `0015`→`0016`；关系版本只在事实写入时推进，写入点固定为 `relationship_repository`（capability/deployment/source 写入与释放）、`deployment_repository`（托管部署写入、解除集中库管理、释放、对账）、`provenance_repository`（导入存证、观察行释放与失效）、冲突 case 写入与明确决定；AI `AnalyzeConflict` 记录只写分析表，**不**推进结构版本。幂等重放（同能力、同关系事实、同裁决）与失败裁决不推进版本。
- 切片 3（query 契约与接入）：新增 `AppQuery::GetSkillRelationshipGraph`、`AppQuery::ListSkillRelationshipCandidates` 与对应结果 DTO（保留既有事实 ID、来源、状态、`relationship_revision`、`last_verified_at`，不放入渲染文案或推测性可执行结论）；应用层由关系仓库事实组装，查询路径**不**触发磁盘扫描、AI 或不必要的写操作；desktop invoke 边界接入 `get_skill_relationship_graph`，`apps/desktop/src/api/bindings.ts` 只由生成器更新且重生成零漂移。
- 验证命令与结果：`relationship_projection` 13/13、`relationship_repository` 7/7（本轮新增 3 项：provenance 写入、托管部署写入/解除管理/释放、观察行释放）、`facade_relationship_governance` 26/43（新增的图谱与候选查询集成测试通过；其余 17 项为链接能力环境既有失败，HEAD 基线 42 项同样 25 通过 / 17 失败且失败集合一致，非本轮回归）、`cargo test -p skillhub-storage --all-features` 33 套件全绿、工作区 `cargo test --workspace --all-features --no-fail-fast` 1042 通过 / 18 失败（17 项同上 + 1 项在零改动的 `skillhub-adapters`）、绑定重生成哈希不变、`cargo fmt --check --all` 与相关 crate `clippy -D warnings` 通过。工作区全量测试同时发现并修复一处遗漏：`llm_provider_repository` 硬编码的 schema 版本断言未随 0016 更新（已改为 16）。
- 未做（有意保留给后续任务）：任何前端页面、路由与导航（Task 5/6/7/8/9）、冲突显式决定 command（Task 2）、治理批处理（Task 3）、关系版本在前端的 query key 接入（Task 6）。本任务不改既有 `get_relationship_overview` 的消费者，不新增第二套关系表或操作日志。

### Task 2（技能关系与统一执行计划）：冲突「具体决定」契约与工作台查询

- core（`crates/skillhub-core/src/relationship/resolution.rs`）：新增 `ConflictDecision`（`keep_distinct` / `confirm_same_skill` / `centralize_management`；「暂不处理」**不是**决定，既不写结论也不写伪历史）、`ConflictResolutionRecord` / `ConflictWorkspace` / `ConflictWorkspaceCase` / `ConflictGovernanceHandoff` / `ConflictResolutionOutcome`，以及纯函数 `build_conflict_workspace`、`plan_conflict_decision`（含幂等重放与「已用其它决定处理过」拒绝）、`plan_conflict_governance_handoff`、`conflict_analysis_is_stale`、`decision_for_analysis_action`。`conflict_case_matches_scope` 由 application 私有实现上移到 core，使「一次分析」与「之后判定过期」使用同一选案口径。
- 工作台口径：默认队列只含 `uncertain` 且无人工决定的冲突；已确定（确定性结论）与已处理项进入历史、不回流当前队列；每条冲突携带最近一次分析与「是否因输入指纹变化而过期」，过期时不再给出可执行建议；累计处理数只统计写结论的决定，不含部署、解除部署与纳入集中库管理。
- AI 建议只映射到明确动作：`same_skill_version`→确认同一 Skill、`distinct_skill`→保留独立、`keep_uncertain`→不产生决定（界面只提供手动处理或暂不处理，不存在只写 `adopted` 的空按钮）。
- query/command：新增 `AppQuery::GetConflictWorkspace`（只读：不扫描、不调用 AI、不改关系版本）与 `AppCommand::ResolveConflictCase`（携带 `expected_relationship_revision`）。写结论的决定先校验冲突仍是待确认、且用户阅读时的关系版本未过期，再写入 `ConflictCase.user_decision/decided_at`（同一事务推进关系版本）；「纳入集中库管理」只按冲突成员路径/Skill 解析需要治理的关系边，返回携带 `conflict_id`/`relation_id` 的预览意图，**不**提交迁移、不写结论、不改文件，解析不到关系边时如实返回 `None`。该命令写入 `conflict_resolution` 语义操作记录（成功与转治理 `committed`、失败 `rolled_back` + 错误码）。
- `conflict_analysis_records.adopted` 只在具体动作完成后由决定写入（审计关联），不再存在任何独立的「采纳」入口。
- 验证命令与结果：`conflict_resolution` 10/10、`facade_ai` 全绿（新增 1 项「AI 分析绝不替换人工决定」）、`facade_relationship_governance` 新增 8 项定向全绿；绑定只由生成器重生成且新增类型齐全；`cargo fmt --check --all` 与相关 crate `clippy -D warnings` 通过。
- 未做（有意保留给后续任务）：冲突处理页面、类别导航与空态文案（Task 7）；治理影响预览与执行链路（Task 3/8）；统一异步执行桥对本次命令的顶栏、通知与父子任务接入（Task 4/10）；desktop 未为该查询新增专用 invoke（沿用通用 `query_application` / `execute_command`，与「即时写入」分类一致）。

### Task 3（技能关系与统一执行计划）：关系治理批处理

固定三切片实施（提交 `54ed6548` 清单纯投影、`65ef9f68` 契约与批量编排、`2f3205a5` facade 集成测试）：

- 切片 1（core 纯投影，`crates/skillhub-core/src/relationship/governance.rs`）：`project_relation_governance_ledger` / `project_relation_governance_ledger_with_names` 只读取已持久化的 `DeploymentRelationFact`，以**关系边**为单位产出清单行，不新增第二套关系表。四桶 `All` / `EligibleToCentralize` / `NeedsValidation` / `Blocked` 由 `RelationGovernanceReadiness`、`RelationGovernanceAction`、`RelationGovernanceBlocker`（9 种，含 `is_hard_block()` 区分硬阻断与「待用户确认」）与 `RelationGovernanceCounts` 结构化表达，界面文案仍由客户端按 i18n 渲染，core 不产出散文。影响范围直接复用既有 `calculate_removal_impact` 与 `RemovalFacts`，不另写一套影响计算。`crates/skillhub-core/tests/relationship_governance_batch.rs` 6 项全绿（四桶分组、共享影响待确认、保护共享目录主体、unknown/unsupported 识别分离、释放边不入清单、按桶/技能/Agent/文本/关系类型筛选 + 稳定排序）。
- 切片 2（契约与 application 编排）：core 新增 `AppQuery::ListRelationGovernance`（只读：不扫描磁盘、不调 AI、不推进关系版本）与 `AppCommand::PrepareRelationGovernanceBatch` / `CommitRelationGovernanceBatch` / `RollbackRelationGovernanceBatch`，结果 `RelationGovernanceBatchOutcome` 携带 `RelationGovernanceBatchItem`（逐项状态 `Prepared` / `Committed` / `RolledBack` / `Failed` / `Blocked` / `Cancelled` 与重试信息）与批次状态（`Prepared` / `Committed` / `PartiallyCommitted` / `Failed` / `Cancelled`）。application（`crates/skillhub-application/src/relationship_governance_batch.rs`）用**一个父** `relation_governance_batch` operation 映射每个子 `migrate_relation` operation，父子映射持久化在既有 operation 行的 `recovery_data.children` 中，**不新增第二本操作日志**。部分失败保留已成功项并逐项给出重试信息；取消通过复用单条 `rollback_relation_migration` 隔离在单行（prepared-but-uncommitted 未触碰文件系统）。
- 关键安全口径：`batch_row_is_executable()` 只放行「仅因共享影响未确认而受阻」的行，且必须由用户为该行显式提供确认令牌；其余 `NeedsValidation`（识别未知/不受支持）在批量里**保持拒绝**，不得被批量当作「已同意」。因此批量没有任何命令级旁路：提交仍逐行走 `prepare_relation_migration`，指纹校验、共享影响确认与对象锁（关系迁移锁）照常生效，批量也不改写部署/解除部署入口。
- 边界决策：`undeploy` 走 `DeploymentId` 命名空间，与 `relation_id` 无映射，故本任务批量只以 `relation_id` 为粒度复用关系迁移四段链路（符合「唯一事实与安全执行来源」约束）；解除部署入口收敛仍属页面任务（Task 8）。
- 验证命令与结果（本机 macOS 主工作区）：`relationship_governance_batch` 6/6、`facade_relationship_governance` **59/59**（基线 51 项 + 本轮新增 8 项：清单查询只读且按关系分组、未验证行受阻而可执行行仍执行、取消单行不影响其它行、部分失败保留成功项并带逐项重试信息、逐行共享影响确认、父操作关联每个子操作、并发提交不绕过关系锁、空选择被拒）、`undeploy_delete` 13/13、`skillhub-adapters` 1/1；`cargo fmt --all -- --check` 通过、四个 crate `clippy --all-targets --all-features -- -D warnings` 通过、绑定重生成不动点确认（重生成前后 SHA256 均为 `f5391709…`，新增 37 处治理批处理相关类型，绑定只由生成器更新）。
- 未做（有意保留给后续任务）：关系治理页面与解除部署入口收敛（Task 8）、统一异步执行桥对该批次的顶栏/通知/父子任务接入（Task 4/10）、关系版本的 query key 前端接入（Task 6）。本任务不改既有 `get_relationship_overview` 的消费者。

## 仍需

1. **真实 Windows/macOS 人工验收**（当前发布阻塞主线）：按《人工验收清单-2026-09-16》A—I 节逐项执行；技能关系模块与统一执行体验的待实施验收用例已登记在 J 节，待代码完成后执行。自动化证据不得表述为真机通过。
2. **冲突组生产写入链路——已按用户裁决实施（2026-09-16，Task 10）**：此前基线既有缺口经独立收口复审确认登记；用户裁决立即实施后，导入提交成功即把确定性冲突落成 `ConflictCaseFact`（UPSERT 不重复建组；ExactContent→same_skill_version 无需裁决；同名不同内容按用户决策映射 distinct_skill/same_skill_version/留待裁决；显式裁决刷新时间、未裁决不覆盖既有；Skip 不触碰），并经 `GetRelationshipOverview(scope=skill)` 端到端可查——Skill 详情冲突区与 AI 单 Skill 分析获得真实数据。独立复审双轴 APPROVE/MINOR 无阻断，主工作区全量电池复跑全绿。
3. AI 分析记录的 `mark_adopted` 仅作为具体处理动作完成后的审计字段，不提供独立的“采纳建议”空操作按钮；新冲突处理工作台需把 AI 建议绑定到明确动作，涉及文件时先展示影响预览，再异步执行并回写结果。该 UI 闭环随下一实施范围排期。
4. Windows/macOS 真机专项：符号链接/目录联接真实权限（M-02-1、Task 6 转换链路）、只读目录（M-01-1）、旧库升级（M-28-1，含 0013→0015 迁移链）。链接能力不足时的条件跳过不计为通过。
5. OPT-06 macOS Dock 图标主标记是否非等比放大的用户裁决项维持待裁决。
6. **技能关系与统一执行计划 Task 4—10 尚未开始**（Task 3 已于本轮完成，见上）：Task 4 统一异步执行桥、Task 5 关系模块路由与导航、Task 6 技能图谱页面、Task 7 冲突处理页、Task 8 关系治理页、Task 9 概览融合、Task 10 全量接入与验收。Task 1 已冻结的关系投影/查询契约、Task 2 已冻结的冲突工作台/决定契约、Task 3 已冻结的治理清单/批处理契约是它们的输入；页面任务不得绕过 Task 1 的投影自行拼关系事实，也不得为冲突决定或治理批处理另造第二套入口。下一步按串行后端主线进入 Task 4（统一异步执行桥），它是 Task 8 治理页面提交批次时顶栏/通知/父子任务反馈的前置。

## 接手入口

1. 本文；2.《自动化测试说明-2026-09-16》；3.《功能完成度与验收状态矩阵-2026-09-16》；4. 真机操作时《人工验收清单-2026-09-16》（只含当前待验收项）。上一轮文档已归档至 `docs/development/archive/2026-09-15-关系闭环Task5前快照/`。

```bash
git status --short
git log -5 --oneline
git diff --check
```

## 已知边界

- 自动化全部为 Vitest/Playwright 预览层与 Rust 集成测试证据；真实磁盘 I/O、符号链接/目录联接权限、系统凭据、缩放、读屏、安装/卸载/签名仍属人工。
- 本机（开发机）安全策略会静默剥离符号链接时，依赖真实链接的测试按既有策略条件跳过，不计为通过。
- v0.2.0 技能关系模块已纳入实施范围并按计划推进：**后端契约 Task 1、Task 2、Task 3 已完成**（关系事实投影、关系版本、图谱/候选查询、冲突工作台查询与显式决定、治理清单与批量安全执行），前端页面（Task 5—9）仍未实现，不能把设计稿或后端契约表述为页面已完成。已确认设计见 `docs/superpowers/specs/2026-09-16-skill-relationship-module-and-execution-experience-design.md`，按 `docs/superpowers/plans/2026-09-16-skill-relationship-module-and-unified-execution-implementation-plan.md` 串行推进。

### 下一实施范围（逐项标注当前状态）

- 技能关系一级导航与返回上下文（`/relationships`、`/relationships/decisions`、`/relationships/governance`）：**未开始**（Task 5）。
- 技能图谱查询投影、单 Skill 中心、原名/别名搜索、标签/关系/状态筛选、上下文叶子节点和不展示空画布规则：**后端契约已完成（Task 1）**，页面与交互未开始（Task 6）。
- 冲突处理对比工作台与明确的可执行处理方案；冲突不进入通用待处理中心：**后端契约已完成（Task 2）**——工作台查询、显式决定命令、AI 建议到具体动作的映射、治理预览意图；页面未开始（Task 7）。
- 关系治理关系清单与批量安全计划：**后端契约与批处理已完成（Task 3）**——只读清单查询、四桶分组、父子 operation 编排、逐行共享影响确认、部分失败保留成功项、取消隔离与逐项重试信息，且批量不绕过单条迁移的指纹校验/共享影响确认/关系锁；页面、解除部署入口收敛与顶栏执行反馈未开始（Task 8、Task 4）。
- 概览页保留既有指标、柱状图、饼图与通用待办，增加技能关系缩略入口；核心指标名称按设计稿冻结：**未开始**（Task 9；关系摘要数据源 Task 1 已就绪）。
- 全局异步执行状态、顶栏居中固定宽度摘要、任务分页窗口、通知中心和操作记录关联：**未开始**（Task 4、Task 10）。
- 统一异步执行桥对 Task 2 冲突决定命令的接入（顶栏、通知、父子任务）：**未开始**（Task 4/10）；该命令当前按「即时写入」分类走同步路径。
