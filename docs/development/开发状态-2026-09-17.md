# 开发状态（2026-09-17）

更新时间：2026-09-17。当前分支为 `wt/task10-integration`，本分支基于 `68d8566a`，尚未推送或合并。

## 已完成

- Task 9 复审小项：概览关系卡片在窄视口下受边界约束；关系图谱/冲突工作台/治理清单增加布局契约；关系 AI 可用性查询键归入正确的 `relationships` 命名空间并验证根键失效；缩略图与关系名保留长文本溢出保护。
- Task 10 冲突组生产写入链路已保留：导入提交写入稳定冲突事实，重复提交不重复建组，用户裁决受保护，Skip 不触碰既有裁决。
- Task 10 统一入口收口：Agent 重扫描和本机发现重扫描通过 `runTrackedOperation` 进入统一在途投影；导入提交、备份恢复、部署提交、关系治理批处理等既有长流程继续使用 tracker/统一桥接。
- 新增/更新测试覆盖：800×600 概览 E2E、关系布局契约、关系命名空间失效、缩略图溢出、Agent 重扫描、本机发现重扫描。
- 本轮独立提交：`4561fcd`、`38dd3b5`、`dee5fa9`。

## 仍需

- 125 个 `executeCommand(`/`.execute(` 命中点已完成清单化审计；其中 native API/facade 调用点不是独立用户入口，设置页即时写入、技能详情即时写入以及部分表单写入仍需后续统一桥接。审计表见《自动化测试说明-2026-09-17》；不得据命中总数宣称已全部接入。
- 进度订阅、取消确认、通知与恢复路径已完成差距登记，但仍需逐入口补齐证据和必要实现；同步即时命令不伪造可取消的后台阶段。
- 真实 Windows junction、符号链接权限、跨卷与测试夹具行为尚未在 Windows 真机完成运行取证；`symlink_physical_id_for_path` 与当前 Windows 期望值存在 mismatch，暂作为发布前阻塞项，不以 macOS 或类型检查替代。
- Task 11（用户可见动作词收敛）与 Task 12（关系测试夹具去链接依赖）本轮明确延期，不作为本分支隐式改动；延期原因与风险见验收矩阵。
- 四份当前文档已切换至 2026-09-17；2026-09-16 版本完整保存在 `docs/development/archive/2026-09-16-Task10收口前快照/`。

## 下一步

1. 控制器复核本分支三个聚焦提交，决定是否纳入主线。
2. 按《人工验收清单-2026-09-17》执行关系页面、操作反馈和 Windows 文件系统真机验收。
3. 若要发布 0.2.0，先解决 Windows mismatch 与链接能力证据，再决定是否拆出 Task 11/12 的独立变更。

## 必要检查命令

```bash
pnpm --dir apps/desktop test --run src/features/discovery/LocalDiscoveryWorkbench.test.tsx
pnpm --dir apps/desktop test --run src/features/agents/AgentListPage.test.tsx
pnpm --dir apps/desktop test --run src/features/overview/OverviewPage.test.tsx
pnpm --dir apps/desktop test --run src/features/relationships/responsiveLayout.test.ts src/features/relationships/decisions/useConflictDecisions.test.tsx
pnpm --dir apps/desktop check
pnpm exec playwright test --config=tests/e2e/playwright.config.ts tests/e2e/overview-preview.spec.ts --grep "800x600|compact overview panels|stacks chart" --workers=1
git diff --check
```

