# 开发状态（2026-09-17）

更新时间：2026-09-17（**独立复核后第二次更新**）。当前分支 `feat/v0.2.0-product-completion`。**功能代码 HEAD 为 `4bd52d5`**（上一轮收口提交）；本轮复核更正提交为 `e3dc554`，交接文档提交紧随其后。已推送并与 `origin/feat/v0.2.0-product-completion` 完全同步，工作区干净。Task 10 worktree 与分支已合并、关闭并删除。

> 本次更新的依据：另一位 agent 对上一轮「全部自动化通过、剩余仅为人工验收」的结论做了独立复核（复跑 + 逐提交代码审查）。复核确认了代码主体与主要功能真实可用，同时发现 **1 处数字错误、2 处完成度溢价、1 处断言削弱**，已按实际结果改写本文档、《自动化测试说明-2026-09-17》与《功能完成度与验收状态矩阵-2026-09-17》。复核前的原始文本完整保存在 `docs/development/archive/2026-09-17-独立复核前快照/`，只用于追溯，**不要从中恢复结论**。

## 已完成

- Task 9 复审小项：概览关系卡片在窄视口下受边界约束；关系图谱/冲突工作台/治理清单增加布局契约；关系 AI 可用性查询键归入正确的 `relationships` 命名空间并验证根键失效；缩略图与关系名保留长文本溢出保护。
- Task 10 冲突组生产写入链路已保留：导入提交写入稳定冲突事实，重复提交不重复建组，用户裁决受保护，Skip 不触碰既有裁决。
- Task 10 统一入口收口（主链路）：Agent/本机发现重扫描、导入提交、Skill 详情移除和四类设置即时写入均使用 `runTrackedOperation`；即时写入使用 `instant` 模式，仅给通知反馈，不伪造顶栏后台任务。失败时网络、视图、自动化、语言和主题均回滚。
- Task 11 已完成：入口动作统一为「添加到 Agent/项目」与「从 Agent/项目移除」；「部署」仅保留技术名词短语，专项测试 7/7 防回归。
- Windows Junction 写入已修复，且**经独立复核逐字节确认为真修复**：mount-point reparse 记录使用 `SubstituteNameOffset=0`、`PrintNameOffset=substituteBytes+2`、`ReparseDataLength` 计入两个 NUL 终止符，布局正确（原实现误用了符号链接的 `Flags` 布局）。`crates/skillhub-adapters/tests/deployment_filesystem.rs` 中两项 `#[cfg(windows)]` 硬断言测试在本机（无提升权限）实跑通过。
- Windows Agent 发现根目录已按路径组件拼接，消除了 `%USERPROFILE%` 展开后混用正反斜杠导致的断言差异；discovery 专项测试 14/14 通过。
- 新增/更新测试覆盖：800×600 概览 E2E、关系布局契约、关系命名空间失效、缩略图溢出、Agent 重扫描、本机发现重扫描。
- 本轮新增提交：`a9ba3de`、`e148ae7`、`c9b3890`、`5281684`、`277113f`、`343ca6c`、`16a1c6f`、`f39268d`、`d67e3a8`、`1344277`、`b20b835`，以及收口文档提交 `4bd52d5`（该提交本身只改 5 个 md 文件，不含代码）。
- 独立复跑的自动化结果：`cargo test --workspace --all-features` 退出码 0，**142 个测试二进制、1098 通过 / 0 失败 / 0 忽略**；`cargo fmt --all -- --check` 通过；`pnpm verify:lifecycle` 通过；E2E 独立点数 **387 用例 / 35 spec**，全量复跑 **387/387 通过、0 失败**（复现成功，详见《自动化测试说明-2026-09-17》）。

## 仍需（独立复核后确认）

- **Task 12 降级为「部分完成」。** 批量共享影响确认确实已改用无链接夹具；但真实共享链接转换相关的 **17 项测试**（`crates/skillhub-application/tests/facade_relationship_governance.rs`）在 `available_capabilities().symlink` 为假时**提前 `return`，被记为 passed 而不是 skipped**（这正是全量结果里 `ignored=0` 的原因）。其中 **16 项完全静默、连一行输出都没有**，只有第 1544 行那 1 项会打印 `skipping:` 提示。在这台无链接权限的 Windows 宿主上，关系迁移 prepare/commit/rollback 与共享引用治理链路**没有执行任何断言**。这不能算「通过」，必须在有链接权限的宿主或 macOS 上补跑。
- **Task 4「统一执行反馈已完成」为溢价结论。** 同一份《自动化测试说明》的异步入口审计表里自己就写着 `skill-detail/nativeApi.ts`「即时写入/分析入口未统一」、`removal/nativeApi.ts`「需逐入口核对」、`security/nativeApi.ts`「部分已桥接」。这三个文件的用户写入入口仍未统一，Task 4 应记为「主链路已完成、低频入口待收口」。
- **一处断言被削弱，待恢复。** `facade_relationship_governance::governance_batch_requires_the_shared_impact_confirmation_per_row` 从 `prepared_count == 1 && state == Prepared` 退化为只断言「确认阻塞消失」，行是否真的进入 `Prepared` 不再检查。AGENTS.md 明令禁止降低断言，虽在代码注释与文档散文里有交代，但不足以抵消。
- **上一轮文档的数字错误已修正。** 原文写「前端单元 296 文件」，实际 `pnpm test:frontend`（= `pnpm --dir apps/desktop test --run`，默认 vitest include）只运行 **165 个测试文件**；`1735` 这个**用例数**是对的。`296` 疑似把 `apps/desktop/src` 下约 298 个 `.tsx` 源码文件当成了测试文件数。同一错误同时出现在实施计划与自动化测试说明中，已一并改正。
- 同步即时命令继续使用 `instant` 模式，不伪造可取消的后台阶段；纯浏览和 API 边界不创建操作日志。
- 真实 Windows 符号链接权限、跨卷与测试夹具行为仍需真机记录；自动化已验证 Junction 实现，但不替代实际权限与用户文件保护验收。

## 下一步

1. 按《人工验收清单-2026-09-17》执行真机验收。**RC-15 必须记录无权限宿主下 `facade_relationship_governance` 的真实跳过/失败集合，不得把能力守卫的静默 pass 写成产品通过。**
2. 在有链接权限的宿主（或 macOS）补跑上述 17 项链路；同时把「静默 return」改为显式 skip 标记或至少保留可观测输出。
3. 恢复 `governance_batch_requires_the_shared_impact_confirmation_per_row` 的原始断言。
4. 收口 `skill-detail`、`removal`、`security` 三处 nativeApi 的用户写入入口后，再把 Task 4 记为完成。
5. 人工验收结论写回清单与完成度矩阵后，方可评估发布。

## 必要检查命令

```bash
# 复核能力守卫的实际覆盖面：17 处守卫，其中 16 处静默 return
rg -n "available_capabilities\(\)" crates/skillhub-application/tests/facade_relationship_governance.rs

# 复核前端测试文件数（应为 165，不是 296）
pnpm --dir apps/desktop test --run --reporter=dot

pnpm --dir apps/desktop test --run src/features/discovery/LocalDiscoveryWorkbench.test.tsx
pnpm --dir apps/desktop test --run src/features/overview/OverviewPage.test.tsx
pnpm --dir apps/desktop check
pnpm exec playwright test --config=tests/e2e/playwright.config.ts tests/e2e/overview-preview.spec.ts --grep "800x600|compact overview panels|stacks chart" --workers=1
git diff --check
```

> **环境提醒（本机实测）**
> - `cargo test` 与 `vitest` **不要并发跑**：会互相抢 CPU 并产生 5 秒超时假失败（复核时并发跑出现 3 项假失败，单项复跑 25/25 全绿）。所有自动化电池请串行执行。
> - Playwright 运行会**清空仓库根 `test-results/`**，不要把验证日志重定向到该目录（会当场被删），请写到仓库外或系统临时目录。
> - **E2E 进程常在全部用例跑完后不自行退出**：本轮 387 项全部输出 `ok` 后，进程挂起 9 分钟以上、未打印汇总行，需手动结束残留的 `node` / `headless_shell` 进程。判定通过与否请以日志里的逐条 `ok` / `not ok` 计数和 `test-results/.playwright-artifacts-*` 是否为空为准，不要等汇总行。
> - 跑 E2E 前务必确认 **5174 端口空闲**（`webServer.reuseExistingServer=false` 且 `strictPort`），否则会出现「全部用例快速失败 + 进程挂死」的现象。
