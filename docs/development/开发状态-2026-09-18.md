# 开发状态（2026-09-18）

更新时间：2026-09-23（当前收口版）。本文件只保留当前开发状态、未完成事项和下一步；已完成明细、旧待办表和历史现场结论已归档至 `docs/development/archive/2026-09-21-人工验收清单整理/`。

当前分支：`feat/v0.2.0-product-completion`。开发遵循单一 worktree、顺序开发；当前未创建并行 worktree。

## 2026-09-22 验收记录

- macOS 15.6.1 ARM64；release 构建时间 `2026-09-22 20:18:26`；源码 HEAD `286199928fad1be0d08894570e629f65dc831be7`。
- `target/release/skillhub-desktop` 进程启动并打开本地数据库，但未出现可被桌面自动化层识别的窗口，无法取得窗口尺寸或页面证据；DEV-66—75 保持人工待验收，不判通过。
- 回查归档 DEV-55 后确认：导入向导仍把关系治理置于提交前，属于**旧问题未修复**。提交 `fa4c2ee2` 已改为“内容冲突 → 导入集中库 → 完成页选择现在整理或稍后处理”；导入只保留原目录并记录事实，链接整理继续由独立治理页执行。
- 本轮定向验证已通过：应用层关系治理 59/59，导入/完成页/发现路由/治理页四个前端测试文件 115/115，前端 lint、typecheck、生产构建及 Tauri 无打包构建均通过。提交 `fa4c2ee2` 后仍需重建 release，再继续人工流程 C、E。
- DEV-67 边界修正已提交为 `b9cb7c30`：移除关系页内部重复的“技能关系”标题，仅保留顶栏标题和三个子页签；关系布局 5/5、AppShell 21/21、lint、typecheck、前端 build 及 Tauri 无打包构建通过。`b9cb7c30` release 已于 `2026-09-22 23:37:25 +0800` 重建。

## 2026-09-23 导入恢复门禁回归

- 回查 2026-09-19 归档 R-10，确认这是**旧问题回归**：导入前的安全拦截或输入无效没有写入集中库残留，却被操作日志记为“需要恢复”，从而在下次启动时错误阻断全部页面。
- 提交 `dfa4839f` 只在导入清理失败、可能残留文件时记录“需要恢复”；安全拦截和输入无效保留操作记录但记为“已撤销”。`cargo test -p skillhub-application` 全量通过；新版 macOS release 已于 `2026-09-23 00:28:15 +0800` 重建，人工复验待执行。

## 当前结论

- **代码完成**：DEV-2—65 的当前实现已完成；本轮包含导入冲突优先顺序、抽屉元数据快照刷新、图谱画布/图例/技术细节、Agent 品牌与类型展示、关系呈现和测试契约收口。
- **自动化完成**：Vitest 169 文件 / 1845 测试、Playwright 396/396、`cargo test --workspace`、前端 lint、typecheck、前端构建和 Tauri 无打包构建均通过。
- **外部阻塞**：DEV-1 需要 macOS 第二个真实可写卷；DEV-8 需要可信的官方 WorkBuddy 绿色 logo 素材。
- **人工验收进行中**：Windows/macOS release 真实操作链、截图、文件保护、权限、链接和跨卷证据按《人工验收清单-2026-09-18.md》继续执行。上一轮 DEV-2—65 的代码与自动化基线不因本轮新问题回退；本轮概览布局问题单独登记为 DEV-66。

## 当前待办

| 范围 | 当前状态 | 下一步 |
|---|---|---|
| DEV-2—65 | 代码与自动化完成，人工验收进行中 | 继续按人工清单 A—H 执行；上一轮通过项和不需要人工验收的内容只读归档，不重新打开。 |
| DEV-66 | 代码与自动化已完成（合并提交 `91f7b1c0`）；人工复验待执行 | 已调整概览区域高度分配和溢出策略，定向测试 31/31、lint、typecheck、前端 build 通过；下一步用新 release 进行最大化、多视口人工复验。 |
| DEV-67—72 | 代码与自动化已完成（当前标题去重修正合并至 `b9cb7c30`）；人工复验待执行 | 图谱标题/图例/布局/控件/高度/工具栏问题均已按顺序修复；关系布局 5/5、AppShell 21/21、lint、typecheck、build 及 Tauri 无打包构建通过。下一步用 `b9cb7c30` release 进行图谱多视口人工复验。 |
| DEV-73 | 代码与自动化已完成（合并提交 `79b7772f`）；人工复验待执行 | 已建立统一 `AgentPresentation`，覆盖 Agent 列表、详情、发现、部署、技能关联、治理、图谱和 onboarding；全量 1864 测试、lint、typecheck、build 通过。下一步用新 release 复验全部展示面。 |
| DEV-76（R-10 导入恢复门禁回归） | 代码与自动化已完成（合并 `dfa4839f`）；人工复验待执行 | 无写入的安全拦截/输入无效不再生成恢复候选；应用层全量测试和操作日志定向测试通过。下一步按流程 C 验证失败后重启仍可正常进入应用，成功导入保持可用。 |
| DEV-1 | 外部阻塞 | 在 macOS 具备第二个可写卷后执行跨卷写入和回滚验收。 |
| DEV-8 | 外部阻塞 | 获得官方或可信授权的 WorkBuddy 绿色 logo 素材后再验收，不自绘替代。 |
| `cargo fmt --all -- --check` | 工具版本边界 | 当前仍会报告既有未触碰文件的 rustfmt 差异；不进行无关全仓重排，继续记录为已知边界。 |

| DEV-74（关联 DEV-49） | 代码与自动化已完成（合并提交 `f856fb23`）；仓库发现配置区和仓库管理页已接入共享 `RepositoryCard`，人工复验待基于新 release 执行。 | 已完成共享卡片投影、两页接入及 targeted tests 29/29；lint、typecheck、前端 build 通过。下一步重建并启动 release，人工验证卡片布局、操作和多视口。 |
| DEV-75（细化 DEV-73） | 代码与自动化已完成（合并提交 `66b52489`）；用户可见 Agent 类型已从 CLI/ACP 统一改为终端/协议接入。 | 已覆盖 AgentKindBadge、本机发现、部署/关系身份、初始化兼容性及中英文词条；下一步仅需基于新 release 进行人工复验。 |

| DEV-75（细化 DEV-73） | `AgentKindBadge`、发现工作台、部署/关系身份、初始化兼容性已统一使用“终端/协议接入”；中文/英文词条、组合徽标、`aria-label` 和说明 title 已补齐。 | 合并 `66b52489`；针对性 Vitest 62/62、lint、typecheck、前端 build 和 Tauri release build 均通过。 | 用户可见主展示不出现 CLI/ACP 裸缩写；多类型显示为“桌面端/终端”等可理解组合；待用户基于新 release 人工复验。 |
| DEV-55（旧问题未修复） | 提交 `fa4c2ee2` 已移除导入前关系治理步骤和强制决定；完成页提供“现在整理 Agent 副本 / 稍后再说”，独立治理页继续承担单项与批量链接整理。 | 应用层 59/59、四个前端定向文件 115/115、lint、typecheck、build、Tauri 无打包构建通过；默认并发全量 Vitest 的既有 `libraryViewContext` 测试隔离错误尚未形成全绿证据。 | 流程 C 验证原目录保持不动和完成页选择；流程 E 验证目录分类、链接整理、失败恢复和回滚。 |

## 已完成摘要

- 导入、冲突、关系治理、远程来源、安全检查、更新检测、部署规划、共享目录和图谱相关实现已有对应自动化覆盖。
- macOS 当前验证产物为 `target/release/skillhub-desktop`，已于 `2026-09-23 00:28:15 +0800` 从源码 HEAD `dfa4839f` 重建；Windows 对应产物为 `target/release/skillhub-desktop.exe`。人工验收仍须记录实际平台、窗口尺寸和证据。
- 近期收口提交包括：`d08a54c1`、`c299d5a2`、`61aa5e29`、`f3a3bd42`、`ff1905a0`、`0e253cb7`、`a12b5a14`、`cecb2ab4`、`991bec51`、`49cf60ec`、`43f93dfe`。

## 下一轮开发代办

| 编号 | 新问题与代码事实 | 建议更改方案 | 完成判据 |
|---|---|---|---|
| DEV-66 | 已修复并合并 `91f7b1c0`：概览区域改为按可用高度分配，移除导致遮盖/裁切的内部滚动承接；新增多视口高度契约测试。 | 已完成最小布局修复和 31/31 定向测试；后续只需重建 release 并人工确认不同最大化窗口、数据密度和平台差异。 | 最大化窗口下概览不出现页面或模块滚动条，数据无遮盖/裁切，Windows/macOS release 人工复验通过。 |
| DEV-67—72 | 已修复并合并至 `47e3c7d9`：标题/页签合并、图例悬浮、画布自适应、控件真实生效、关系页 fill 高度和工具栏收纳均已完成。 | 定向关系测试、全量 Vitest、Playwright 关系回归、lint、typecheck、build 已通过；只剩 release 多视口人工复验。 | 图谱各项人工判据通过后关闭对应 DEV。 |
| DEV-73 | 已修复并合并 `79b7772f`：统一 `AgentPresentation` 覆盖所有主要 Agent 消费点，支持共享目录、同品牌多类型合并、未知类型和无障碍标签。 | 全量 170 文件/1864 测试、lint、typecheck、build 通过；只剩 release 全项目人工复验。 | 所有 Agent 主展示不出现裸技术 ID，品牌和类型一致；人工复验通过后关闭 DEV-73。 |
| DEV-76（R-10 导入恢复门禁回归） | 已修复并合并 `dfa4839f`：仅在导入清理失败且可能有文件残留时进入恢复；无写入的安全拦截/输入无效为已撤销。 | 应用层全量测试通过，门禁拦截的公共外观测试断言无恢复候选；待 release 重启人工复验。 | 失败导入不再阻断后续启动；真正残留仍保留恢复入口；成功导入不受影响。 |

| DEV-74（关联 DEV-49） | 历史方案已要求仓库发现和仓库管理复用卡片式呈现；当前实现已由 `f856fb23` 接入共享 `RepositoryCard`/view model，保留仓库坐标、分支、状态、扫描事实和操作。 | 已完成共享组件、响应式卡片布局、两页接入及渲染/操作测试；后续只做 release 重建和人工复验，不再重复开发。 | 两页面均以一致仓库卡片展示；所有启停/刷新/移除/扫描/重试/添加动作保持；窄屏无溢出，人工复验通过。 |

## 继续工作顺序

1. 先按《人工验收清单》准备测试素材和 release 产物记录。
2. 依次执行流程 A—G；每条完成后立即写回《功能完成度与验收状态矩阵》。
3. 在具备对应平台和素材后执行流程 H，并区分“通过、未通过、阻塞、不适用”。
4. 发现问题时先回查归档证据，再判断新问题、未修复、回归或边界遗漏；不要覆盖历史记录。
