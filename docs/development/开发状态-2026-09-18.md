# 开发状态（2026-09-18）

更新时间：2026-09-21（当前收口版）。本文件只保留当前开发状态、未完成事项和下一步；已完成明细、旧待办表和历史现场结论已归档至 `docs/development/archive/2026-09-21-人工验收清单整理/`。

当前分支：`feat/v0.2.0-product-completion`。开发遵循单一 worktree、顺序开发；当前未创建并行 worktree。

## 当前结论

- **代码完成**：DEV-2—65 的当前实现已完成；本轮包含导入冲突优先顺序、抽屉元数据快照刷新、图谱画布/图例/技术细节、Agent 品牌与类型展示、关系呈现和测试契约收口。
- **自动化完成**：Vitest 169 文件 / 1845 测试、Playwright 396/396、`cargo test --workspace`、前端 lint、typecheck、前端构建和 Tauri 无打包构建均通过。
- **外部阻塞**：DEV-1 需要 macOS 第二个真实可写卷；DEV-8 需要可信的官方 WorkBuddy 绿色 logo 素材。
- **人工验收进行中**：Windows/macOS release 真实操作链、截图、文件保护、权限、链接和跨卷证据按《人工验收清单-2026-09-18.md》继续执行。上一轮 DEV-2—65 的代码与自动化基线不因本轮新问题回退；本轮概览布局问题单独登记为 DEV-66。

## 当前待办

| 范围 | 当前状态 | 下一步 |
|---|---|---|
| DEV-2—65 | 代码与自动化完成，人工验收进行中 | 继续按人工清单 A—H 执行；上一轮通过项和不需要人工验收的内容只读归档，不重新打开。 |
| DEV-66 | 新一轮待开发（本轮人工新发现） | 概览页最大化窗口下页面级滚动条已消失，但多个数据模块仍有内部滚动条并遮盖/裁切内容。修复为按可用高度自适应各区域，最大化状态下概览不出现任何模块滚动条；补充不同设备/分辨率自动化与人工复验。 |
| DEV-67—72 | 新一轮待开发（本轮人工新发现） | 技能图谱页标题/页签重复、图例占用整块空间、节点和边密度不适配、缩放/居中按钮现场无效、最大化窗口有滚动条、工具栏换行留白。建议方案已写入当前人工清单和本表下方“下一轮开发代办”；暂不修改代码，待本阶段人工验收结束后统一梳理。 |
| DEV-1 | 外部阻塞 | 在 macOS 具备第二个可写卷后执行跨卷写入和回滚验收。 |
| DEV-8 | 外部阻塞 | 获得官方或可信授权的 WorkBuddy 绿色 logo 素材后再验收，不自绘替代。 |
| `cargo fmt --all -- --check` | 工具版本边界 | 当前仍会报告既有未触碰文件的 rustfmt 差异；不进行无关全仓重排，继续记录为已知边界。 |

## 已完成摘要

- 导入、冲突、关系治理、远程来源、安全检查、更新检测、部署规划、共享目录和图谱相关实现已有对应自动化覆盖。
- 当前构建产物为 `target/release/skillhub-desktop.exe`；人工验收开始前仍需记录构建时间并核对源码 HEAD。
- 近期收口提交包括：`d08a54c1`、`c299d5a2`、`61aa5e29`、`f3a3bd42`、`ff1905a0`、`0e253cb7`、`a12b5a14`、`cecb2ab4`、`991bec51`、`49cf60ec`、`43f93dfe`。

## 下一轮开发代办

| 编号 | 新问题与代码事实 | 建议更改方案 | 完成判据 |
|---|---|---|---|
| DEV-66 | 本轮人工截图显示概览整体无页面滚动条，但模块内部仍滚动并遮盖/裁切内容。源码 `apps/desktop/src/features/overview/overview.css` 当前对指标带、关系区、主内容区、待办区和标签明细使用 `overflow-y: auto`，与“最大化窗口下概览完整展示且无任何模块滚动条”的新验收目标冲突。 | 重新定义概览的可用高度分配和数据密度策略；优先调整区域轨道和内容摘要，避免以模块滚动承接布局不足；数据确实超出物理空间时采用摘要/分页等明确产品规则。 | 在最大化窗口下按可用空间自适应，概览不出现模块滚动条、遮盖或裁切；覆盖多视口和 Windows/macOS release。 |
| DEV-67—72 | 本轮人工截图与操作反馈已核实源码事实：`RelationshipsLayout` 同时渲染页头和页内导航；`GraphLegend` 为独立整块；画布固定 `960×640`、节点最大宽度 `12rem`、力布局参数固定；三个控制按钮虽有 `onClick`，但缺少真实视觉效果自动化证据；关系页无独立 fill 高度契约；工具栏允许换行且标签区纵向布局。 | DEV-67 合并标题与页签；DEV-68 将图例改为紧凑右下角悬浮；DEV-69 让布局接收容器尺寸和图谱密度并动态 fit；DEV-70 统一视口状态入口并补真实交互断言；DEV-71 建立关系页 fill 高度链路；DEV-72 重做单行工具栏与窄屏收纳。当前只记录，不修复。 | 分别完成页签/标题合并、紧凑图例、自适应节点边布局、真实缩放/居中按钮、最大化无滚动布局和单行工具栏；补齐多视口自动化及 release 人工复验。 |

## 继续工作顺序

1. 先按《人工验收清单》准备测试素材和 release 产物记录。
2. 依次执行流程 A—G；每条完成后立即写回《功能完成度与验收状态矩阵》。
3. 在具备对应平台和素材后执行流程 H，并区分“通过、未通过、阻塞、不适用”。
4. 发现问题时先回查归档证据，再判断新问题、未修复、回归或边界遗漏；不要覆盖历史记录。
