# 开发状态（2026-09-18）

更新时间：2026-09-18（新快照：2026-09-17 六轮收口完成后重立当前入口）。当前分支 `feat/v0.2.0-product-completion`，**功能代码 HEAD 为 `f9f7c4fc`**（随后是文档同步提交）。本轮直接在当前分支收口，未新建分支或工作树。

> 2026-09-18 追加：**Windows 宿主取证轮**（起点 `40396938`，与 `origin` 同步，工作区干净）：Rust 用例数跨平台差额闭合、RC-13/RC-14 真机取证、Windows 首次前端电池。结论已并入本文件的「已完成 / 仍需」与《自动化测试说明-2026-09-18》《人工验收清单-2026-09-18》《功能完成度与验收状态矩阵-2026-09-18》。
>
> 2026-09-18 追加：**GUI 取证通道建立轮**（HEAD `9c5a4223`）。此前登记为「无 GUI 操作信道」的真机阻塞**已解除**：真实 exe + WebView2 远程调试可驱动真桌面，前提是给 WebView2 传 `--no-sandbox`（宿主上 Chromium 沙箱会杀掉 GPU 进程 → 窗口全白）。据此完成 **J-REL-1（通过）**、**J-REL-2—4（部分通过：布局判据全过，内容判据因本机 0 条真实关系未取得）**、**SR-8（通过）**、**SR-7 只读半边（纯浏览零通知，通过）**。通道用法与边界、以及顺带发现的 CSP 缺陷，见《人工验收清单-2026-09-18》R-3。

> 2026-09-18 追加：**CSP 修复轮**（起点 `1da8e3de`）。修复 `apps/desktop/src-tauri/tauri.conf.json` 缺 `connect-src` 导致每次启动阻断 Tauri IPC 自定义协议、回退 postMessage 的缺陷，并用真实 release 产物复验（冷启动 `captured diagnostics (0)`、`POST http://ipc.localhost/query_application → 200`、`invoke` 正常）；同一口径已固化为 `verify:release` 回归护栏（旧值会红）。另确立两条 release 真机验收的环境事实：**必须走 `tauri build`**（CLI 实际执行 `cargo build --bins --features tauri/custom-protocol --release`），以及 `tauri build` 可能卡在 `beforeBuildCommand` 的规避法。取证见《人工验收清单-2026-09-18》R-3 末条。

> 2026-09-18 追加：**部署链三缺陷修复轮（D-7/D-8/D-9）**。按 R-6 取证登记的三个发布阻塞缺陷全部 TDD 修复（先红后绿，红因逐条对准缺陷本体）：**D-7** 物化/中转目录名去 `sha256:` 冒号前缀（core 新增 `deployment::staging`，application 同口径）；**D-8** 提交部署前按物理目标登记 `targets` 行（storage 新增 `target_repository`，写账失败暴露为 `operation.conflict`）；**D-9** 目标能力取「Agent profile 声明 ∩ 宿主探测」，`list_deployment_targets` 与规划器共用同一口径。新增两套回归（core `deployment_staging` 6 用例、application `facade_deployment_accounting` 6 用例，均平台无关）；前端警告文案 i18n 化（zh/en 键对称）。受影响包回归 133+3 个测试目标全绿、`bindings.ts` 零漂移；**真实 release 产物复验**：`needs_recovery` 残影处置清零 → Claude Code 目标规划不再出现目录联接（自动选受管复制）→ 真实部署成功落盘且三表记账完整 → 走产品自身移除链路复原现场（20 条 Skill、deployment 转 removed、零 `needs_recovery`）。

> 上一快照（2026-09-17，含六轮更新的完整推导过程：独立复核更正、RC-15/RC-16 证据、Task 4 收口、桥的缺省失败描述成类修复、待办处置入桥、security 死代码删除、关系治理页失败通知抑制闭合）整体归档于 `docs/development/archive/2026-09-18-六轮更新后快照/`，只用于追溯，**不要从中恢复结论**。交接指令 `GOAL-交接-2026-09-17-复核后收口与人工验收.md` 的全部开发项已吸收完毕，移入 `docs/development/archive/2026-09-17-交接指令/`。

## 已完成（摘要）

- **CSP `connect-src` 缺陷已修复并复验（2026-09-18）。** `tauri.conf.json` 的 `csp` 补上 `connect-src 'self' ipc: http://ipc.localhost`；重跑 `tauri build` 后，在同一份 release exe 上以 CDP 冷启动复现测得 `captured diagnostics (0)`、`cspRelated=0`，且 `Network.*` 出现 3 组 `POST http://ipc.localhost/query_application → 200`（回退状态下不会产生该请求）。回归护栏进 `scripts/verify_release_readiness.mjs` 与 `scripts/verify_release_readiness.test.mjs`，红绿实测：旧值 `not ok`（`connect-src` 为 `undefined`）、新值 `ok`；`node scripts/verify_release_readiness.mjs` = PASS，`node --test scripts/verify_release_readiness.test.mjs` = 10/10 通过。
- **统一异步执行反馈（Task 4）彻底收口。** 全部用户可触发的写入都在 `runTrackedOperation` 上：主链路（Agent/发现重扫描、导入提交、详情移除、四类设置即时写入）、`skill-detail` 七处写入 + 来源更新、`removal` 的项目移除关系、`security` 的处置/AI 检查取消、待办页处置（单条/撤销 `instant`、批量 `phased`，`0bf619a7`）。**不存在已知未入桥的用户可触发写入。**
- **失败反馈口径完全统一。** 桥的缺省失败描述可读（结构化 `AppError` 走错误码文案，普通 `Error` 原样透出，一次性覆盖 27 个调用点，`3a060e58`）；关系治理页 4 处 `errorNotice: () => null` 失败通知抑制全部移除（登记时漏计为 1 处，已更正，`f9f7c4fc`）；用户写入失败一律进通知中心，详情与页面行内提示逐字同源，未知错误码带出原始码。
- **死代码清理完成。** `security/nativeApi.ts` 四个无调用点导出（`renameNativeSkill` 等）及其测试已删除（`58d8ec94`），全仓无残留引用。
- **RC-15/RC-16 证据闭合（macOS 侧）。** 关系治理链路 59 通过 / 0 失败 / 0 跳过，能力守卫可观测、可计数、可反证（`7853ae0f`）；被削弱的批量断言已恢复并在 macOS 跑通（`4bc6b087`）。Windows 侧仍需 RC-13/RC-14 取证。
- **RC-13/RC-14 Windows 真机取证完成（2026-09-18，非管理员、未开开发者模式）。** 该账号 `available_capabilities()` = `symlink:false / junction:true / copy:true`，产品按能力探测选择目录联接：junction 落地为 reparse point、可解析为源目录、读透内容；转换前后来源目录条目与树哈希完全一致（原文件零误删）；探针零残留（`.skillhub-link-probe-*` 计数 0）；来源目录整体替换后 `remove_owned` 仍返回 Ok 且父目录无残留。另测到一条只能真机看到的边界：`std::os::windows::fs::symlink_dir` 返回 `Ok(())` 却未创建任何东西（`symlink_metadata` 报 os error 2），产品「必须真正落地」的校验因此正确判定符号链接不可用。跨卷真机场景未取得（该宿主仅一个卷且非管理员）。逐项记录见《人工验收清单-2026-09-18》。
- **Windows 首次前端电池（2026-09-18）。** `pnpm install --frozen-lockfile`、`pnpm --dir apps/desktop check`、`node scripts/i18n-cjk-audit.mjs`（user-visible 0 条）全部通过；前端全量复跑 **165 文件 / 1773 用例全通过**（该次早于今日新增的取消路径回归测试），与 macOS 数量一致。首次全量的 3 例非确定性失败已定位并处置：**逐用例预算 5 秒 → 15 秒**（`apps/desktop/vite.config.ts`，依据 165 个文件各建一次 jsdom 环境的冷缓存实测 `environment 164.69s / import 80.31s`），断言一条未改、无任何人为延时；清空 Vite 转换缓存冷缓存复跑 **165 文件 / 1774 用例全通过**。
- **自动化基线（2026-09-18 复跑，macOS 宿主，串行执行）。** `node scripts/ci-local.mjs` **10/10 步骤通过**；前端全量 **165 文件 / 1773 用例通过**；E2E 全量 **387 通过 / 0 失败**（exit 0，进程正常退出）；`pnpm --dir apps/desktop check`（ESLint `--max-warnings 0` + tsc）、`node scripts/i18n-cjk-audit.mjs`（user-visible 0 条）、`git diff --check` 通过。Rust 全工作区 **142 个测试目标 / 1096 通过 / 0 失败 / 0 忽略**（`.rs` 自该测量后未改动，今日 ci-local 的 `cargo test` 步骤再次复现）。逐条命令与边界见《自动化测试说明-2026-09-18》。

## 仍需

1. **真机验收（发布阻塞主线，部分完成）。** Windows junction/符号链接权限与用户文件保护已取得真机证据（RC-13 通过、RC-14 部分通过，见《人工验收清单-2026-09-18》）；跨卷规则的**决策层**已在本机真实第二卷上实测，仅「跨卷端到端写入」待补。关系四页面窄视口：**J-REL-1 通过**，**J-REL-2—4 部分通过**（真实窗口 800×600 下布局判据四页全过；内容相关判据因本机「已配置 Agent 0 个 / 项目 0 个 → 关系、冲突、治理行数均为 0」未取得）。统一执行反馈：**SR-8 通过**、**SR-7「纯浏览零通知」通过**。**仍未完成**：跨卷端到端写入（需一台带可写第二卷的 Windows 宿主）、J-REL-2—4 的内容判据、SR-1—6、SR-7 的 operation id/深链半边、SR-10。唯一执行入口是《人工验收清单-2026-09-18》，逐项记录后写回《功能完成度与验收状态矩阵-2026-09-18》。
   **范围澄清（2026-09-18）**：「跨卷」规则只作用于「纳入集中库管理」（把已有复制改成管理链接），**不影响部署**——跨卷部署实测走受管复制并放行（「集中库在 D 盘、agent/项目在 C 盘」是正常场景）。根因是 NTFS 目录联接在操作系统层面无法跨卷，与产品策略无关。
   **补做 J-REL-2—4 内容判据 / SR-1—6、SR-10 的前置（2026-09-18 已实测，结论：当前不可补）**：需要至少一条真实关系。已按用户指示用真实 **Claude Code** 目标（`~/.claude/skills`，发现快照中 available+writable）走产品自身的部署链路（CDP → `window.__TAURI_INTERNALS__.invoke` → `get_deployment_plan`/`prepare`/`commit`），**两次真实提交全部 `committed: false`**：受管复制报 `io_kind: NotADirectory`；规划器默认的目录联接**磁盘上建成、记账为零**（`deployments`/`deployment_relations`/`targets` 仍为 0，操作落 `needs_recovery`）。根因即矩阵阻塞项 7、8、9。**正确顺序是「先修 8、7 → 再补 J-REL-2—4」**，不要再去试部署。现场已清理（`~/.claude/skills` 回到 20 条、库内空残留已删）。**不要**用 `/__preview/*` 预览桩替代——它只在 DEV 构建存在，release 产物里没有这些路由。完整取证见《人工验收清单-2026-09-18》R-6。
2. ~~**Windows 宿主实跑一次 Rust 全量**，闭合用例数差额~~ → **已完成（2026-09-18，Windows 11 家庭版 25H2 / build 26200，非管理员账号）**：`cargo test --workspace --all-features` exit 0，**142 个测试目标 / 1098 通过 / 0 失败 / 0 忽略**；`--list` 计数同为 **1098**，与逐条 `ok` 数一致。差额已归因为**上一版平台门控清点漏了 2 个条目**（`application/tests/facade_relationship_governance.rs:965` 的 `#[cfg(unix)] prepare_refuses_conversion_when_the_relation_volume_cannot_host_links`，以及 `src-tauri/src/updater.rs:312` 的 `#[cfg(target_os = "macos")] macos_accepts_app_tar_gz_updater_artifact`），不是被静默排除的用例：9 个 Windows 专属用例全部在场并逐个 `ok`，6 个 Unix 专属与 1 个 macOS 专属全部缺席。修正后推算 Windows 1098 / macOS 1096，两端与实测一致。**口径提醒**：1098 中含 17 条能力守卫跳过（真正执行断言 1081 条）。细节见《自动化测试说明-2026-09-18》。
3. **可选：E2E 进程收尾卡死根因。** 既有规避手段可靠（见自动化测试说明环境提醒），非发布阻塞，不为它改断言或加延时。Windows 侧 E2E 本轮未跑（非阻塞）。
4. ~~**Windows 前端电池非确定性波动**~~ → **已处置（2026-09-18）**：3 例失败的报告原文是 `Test timed out in 5000ms`（超时，不是断言失败），同会话第二次全量 165/1773 全通过、该文件单独跑 25/25 通过，两次 `setup` 耗时 158 秒 / 53 秒 → 判定为宿主负载相关。处置为把逐用例预算放宽到 15 秒并写明测量依据（`apps/desktop/vite.config.ts`），**未动任何断言、未加人为延时**；清空 Vite 缓存冷缓存复跑 **165 文件 / 1774 用例全通过**（该跑环境开销比首次失败时更重）。详见《自动化测试说明-2026-09-18》「Windows 首次前端电池」。

## 下一步

1. ~~**先修三个 Windows 部署阻塞缺陷（2026-09-18 新发现，见 R-6），再继续真机验收。**~~ → **已修复并真机复验（2026-09-18）**，明细：
   - **D-7**：中转目录名含 `sha256:` 冒号 → 已改为去前缀 digest（core `deployment::staging` 新模块 + application `materialized_source` 同口径），`core/tests/deployment_staging.rs` 6 用例（先红后绿）。
   - **D-8**：`deployments.target_id` 外键指向生产从不写入的 `targets` 表 → 提交前新增 storage 层 `target_repository` 登记物理目标行，写账失败暴露为 `operation.conflict`；`application/tests/facade_deployment_accounting.rs` 6 用例端到端断言三表落账（夹具不预置 `targets` 行）。
   - **D-9**：目标能力忽略 profile 声明 → 改为「profile 声明 ∩ 宿主探测」，`list_deployment_targets` 与规划器共用同一口径；`junction: false` 的 Claude Code 在真机规划中不再出现目录联接（自动选受管复制）。
   - 真机复验（release 产物 + CDP）：2 条 `needs_recovery` 残影处置清零 → 真实部署成功（`~/.claude/skills/find-skills` 为真实目录、`SKILL.md` 可读、`targets`/`deployments` 行就位）→ 产品自身移除链路复原现场（20 条 Skill、deployment=removed、零 `needs_recovery`）。R-6 全文见《人工验收清单-2026-09-18》。
   - **后续**：按清单继续剩余真机验收项（跨卷端到端写入、**J-REL-2—4 内容判据（前置已解除，可重试）**、SR-1—6、SR-7 深链半边、SR-10），逐项记录后写回清单与矩阵，再评估 v0.2.0 发布。
2. **桌面真机取证的前置已就绪（2026-09-18）。** `tauri build --no-bundle` 产物 `target/release/skillhub-desktop.exe` 已重建并**核对了内置前端与源码构建一致**（`dist/index.html` 引用的 5 个资源全部以字面量存在于 exe 内）。旧的「必须先重建」提醒不再适用：`target/debug/skillhub-desktop.exe` 仍是 2026-09-14 的旧产物，**不要再用它**。
   启动方式（真桌面驱动）：
   ```
   WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS="--no-sandbox --remote-debugging-port=<port> --remote-allow-origins=*" \
     target/release/skillhub-desktop.exe
   ```
   不加 `--no-sandbox` 时宿主 Chromium 会连杀 9 次 GPU 进程并判定 `GPU process isn't usable. Goodbye.`，窗口全白、页面停在 `about:blank`（**宿主环境问题，不是产品缺陷**）。不要改用 `--in-process-gpu` 绕过：页面能渲染，但切到 `/relationships` 后承载调试端口的浏览器进程整体卡死，会误判成「应用挂死」。窗口尺寸用 `Browser.getWindowForTarget {targetId}`（必须带 `targetId`）→ `Browser.setWindowBounds`；进入深链必须走应用自身导航，**CDP 的 `Page.navigate` 会让新文档加载完资源但应用 bundle 不执行**。完整说明与边界见《人工验收清单-2026-09-18》R-3「GUI 取证通道」。
3. ~~**CSP 缺 `connect-src`**~~ → **已修复并复验（2026-09-18）**。`tauri.conf.json` 的 `csp` 已补 `connect-src 'self' ipc: http://ipc.localhost`；重跑 `tauri build` 后 CDP 冷启动 `captured diagnostics (0)`，IPC 回到 `http://ipc.localhost` 并返回 200。回归护栏加进 `scripts/verify_release_readiness.mjs` + 对应用例（红绿已实测，全量 `verify:release` PASS、10 用例全通过）。
   **本轮同时确立两条真机 release 验收的环境事实**：① **必须用 `tauri build`**——CLI 实际跑的是 `cargo build --bins --features tauri/custom-protocol --release`，只用 `cargo build --release` 产出的 exe 不伺服内嵌前端（页面停在 `chrome-error://chromewebdata/`）；② `tauri build` 可能**卡在 `beforeBuildCommand` 不返回**（实测 11 分钟无 cargo 进程、无输出），规避法是前端单独 `pnpm --dir apps/desktop build` 后，用 `--config '{"build":{"beforeBuildCommand":"echo frontend-already-built"}}'` 覆盖跳过。
4. 用例数差额已闭合，矩阵阻塞项 4 已关闭；Windows 与 macOS 的 Rust 基线数字可直接引用（Windows 1098 / macOS 1096）。

## 必要检查命令

```bash
# RC-15 三项证据（本机为 macOS，具备符号链接能力）
cargo test -p skillhub-application --test facade_relationship_governance --all-features            # 59 通过 / 0 忽略
SKILLHUB_TEST_FORCE_NO_LINK_CAPABILITY=1 cargo test -p skillhub-application --test facade_relationship_governance \
  --all-features -- --nocapture | grep -c '^SKILLHUB-TEST-SKIP;'                                    # 18（负向对照）
SKILLHUB_REQUIRE_SYMLINK_CAPABILITY=1 cargo test -p skillhub-application --test facade_relationship_governance --all-features  # 59 通过 / 0 跳过

# 交叉验证：两个开关叠加必须是 18 失败，否则说明守卫没接上
SKILLHUB_REQUIRE_SYMLINK_CAPABILITY=1 SKILLHUB_TEST_FORCE_NO_LINK_CAPABILITY=1 \
  cargo test -p skillhub-application --test facade_relationship_governance --all-features

# Windows 用例数（两端命令相同；2026-09-18 实测 Windows 1098 / macOS 1096）
cargo test --workspace --all-features -- --list 2>/dev/null | grep -c ': test$'
cargo test --workspace --all-features

# Windows 能力守卫三项对照（非管理员、未开开发者模式的宿主）
cargo test -p skillhub-application --test facade_relationship_governance --all-features -- --test-threads=1   # 58 通过 + 17 条 SKILLHUB-TEST-SKIP;
SKILLHUB_TEST_FORCE_NO_LINK_CAPABILITY=1 cargo test -p skillhub-application --test facade_relationship_governance --all-features -- --test-threads=1   # 58 通过 + 18 条
SKILLHUB_REQUIRE_SYMLINK_CAPABILITY=1 cargo test -p skillhub-application --test facade_relationship_governance --all-features -- --test-threads=1      # 41 通过 / 17 失败

# 前端
pnpm --dir apps/desktop test --run                                                 # 165 文件 / 1774 用例（Windows 实测 1774）
pnpm --dir apps/desktop check
node scripts/i18n-cjk-audit.mjs
pnpm exec playwright test --config=tests/e2e/playwright.config.ts                  # 387 用例
node scripts/ci-local.mjs
git diff --check
```

> **环境提醒（本机实测）**
> - `cargo test` 与 `vitest` **不要并发跑**：会互相抢 CPU 并产生 5 秒超时假失败。所有自动化电池请串行执行。
> - Windows 侧的 `1098 通过` 含 17 条能力守卫跳过（libtest 把早退记为 `passed`），报告时必须同时给出 `SKILLHUB-TEST-SKIP;` 计数；真正执行断言的是 1081 条。
> - **PowerShell 的 `2>` 重定向会截断长行**（实测截在 74 字符，会丢掉 `SKILLHUB-TEST-SKIP;` 标记里的行号）。需要完整日志请用 Git Bash 原生重定向，并用 Python 读原始字节。
> - 前端全量在 Windows 上的 `OverviewPage.test.tsx` 曾出现 5 秒超时假失败（负载相关）。**已处置**：逐用例预算现为 15 秒（`apps/desktop/vite.config.ts`，附冷缓存环境开销的测量依据），断言强度未变、无任何人为延时。若再遇到超时，先清空 `apps/desktop/node_modules/.vite` 冷缓存复跑，**不要**再放宽预算或降低断言。
> - Playwright 运行会**清空仓库根 `test-results/`**，验证日志不要重定向到该目录，写到仓库外或系统临时目录。
> - **E2E 进程可能在全部用例跑完后不自行退出**：判断通过与否以日志里的逐条 `ok` / `not ok` 计数和 `test-results/.playwright-artifacts-*` 是否为空为准，不要等汇总行。
> - 跑 E2E 前务必确认 **5174 端口空闲**（`webServer.reuseExistingServer=false` 且 `strictPort`），否则会出现「全部用例快速失败 + 进程挂死」。
> - `pnpm exec tsc` 对 `runTrackedOperation` 泛型推断：当 T 只出现在带隐式参数的回调里时会落到 `unknown`，必要时显式写 `runTrackedOperation<number>`。
