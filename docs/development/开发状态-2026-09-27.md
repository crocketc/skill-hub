# 开发状态（2026-09-27）

> 最新验收状态：**DEV-101 已获用户真机确认通过**（2026-09-27 换新产物打开直接进入主界面，恢复闸门不再出现；"取消删除后重启不再闸门"的回归判据仍待验）。DEV-92 通知玻璃视觉、文字选中高亮与概览 Agent 品牌柱条此前已获用户确认。DEV-95—101（统一待办、通知模块归属、删除影响再设计、恢复页可读标签、内部标识全面退出界面、junction 真机实证与 profile 更新、启动恢复闸门根因修复）代码与自动化完成，人工验收流程 I/J 及链接部署核对待执行。当前标准产物 target/release/skillhub-desktop.exe（2026-09-27 00:27，34,399,744 bytes，SHA-256 73A257169BCD0A35C9656D9718313ADADF92E2DFA01280297EC858AC21FA2212）；签名构建通过，未打包运行供验收。

更新时间：2026-09-27。本文件只保留当前开发状态、未完成事项和下一步。上一快照（2026-09-26，含 DEV-87—101 的完整逐项记录与产物史）已归档至 `docs/development/archive/2026-09-27-验收反馈修复批次/`；更早归档见该目录内说明。

当前分支：`feat/v0.2.0-product-completion`。开发遵循单一 worktree、顺序开发；当前未创建并行 worktree。代码未提交（等用户验收流程 I/J 后一起提交）。

## 自动化基线（2026-09-27）

| 命令 | 结果 |
|---|---|
| `cargo test --locked --workspace` | 通过：151 个套件全绿 / 0 失败。 |
| `pnpm --dir apps/desktop test --run` | 通过：1989 测试 / 0 失败（178 文件，2026-09-26）。 |
| `pnpm check:frontend`（lint + typecheck） | 通过。 |
| `pnpm test:e2e`（定向） | workflow-operations + workflow-deployment 54/54；import-deploy + native-flow-preview + workflow-recovery 35/35（2026-09-26/27）。全量 404/404 基线为 2026-09-25。 |
| `pnpm --dir apps/desktop tauri build`（带签名） | 通过；NSIS + updater 签名生成。 |

## 当前待办

| 范围 | 当前状态 | 下一步 |
|---|---|---|
| 人工验收 A—H | 待真实 release 证据（概览专项 DEV-66/77/78/82 已关闭） | 按《人工验收清单-2026-09-27.md》执行 |
| 人工验收 I/J | 待执行；DEV-101 打开正常已确认 | 流程 I（含 DEV-98 恢复页判据、DEV-101 取消重启判据）、流程 J（DEV-96/97/99 判据） |
| DEV-100 链接部署人工核对 | 代码与 profile 完成 | 新产物上核对 Kimi 等目标出现「链接部署」且部署落盘正确；Hermes/Antigravity/豆包 junction 待装机后补测 |
| DEV-79/83/84/85/86（图谱与预览承载项） | 代码与自动化完成 | macOS release 窗口可见后复验；流程 A/E/G 一并核对 |
| DEV-1、DEV-8 | 外部阻塞 | DEV-1 需 macOS 第二真实可写卷；DEV-8 需可信官方 WorkBuddy 绿色 logo 素材 |
| `cargo fmt` 工具边界 | 既有未触碰文件存在 rustfmt 版本差异 | 单独安排一次纯格式化提交（不与行为变更混合），或统一工具链版本后全仓重排 |
| `cargo deny` licenses 边界 | `notify v8.2.0`（经 `gix` 引入）的 `CC0-1.0` 不在允许清单，干净 HEAD 同样失败 | 评估将 `CC0-1.0` 纳入 `deny.toml` 允许清单（FSF Free/Libre）或锁定替代依赖 |
| DEV-93 通知历史持久化 | 下一版本规划（0.2.0 维持会话级） | 下版本立项；契约五要素已为其铺路 |

## 已完成摘要：2026-09-26/27 验收反馈批次（DEV-95—101）

全部自动化验证通过（见基线）；自动化通过≠人工复验通过。逐项完整记录见归档快照，此处只留口径：

- **DEV-95 统一待办**：五类来源（试用/安全/冲突/治理/恢复）同源汇总、去重、对象深链、返回与操作完成刷新；必需事项不允许忽略；概览与列表同查询门面。
- **DEV-96 通知模块归属**：`runTrackedOperation` 注入 `source`；多单位/部分失败补「成功 N、失败 M」；卡片（折叠+展开）功能域徽标。
- **DEV-97 删除影响再设计**：共享 `RemovalImpactPieces` 收敛三处呈现；矩阵逐维度分行；无部署→「确认删除」、有部署→「继续删除」。
- **DEV-98 恢复页可读标签**：候选与摘要按「本地化操作名：对象名 + 本地化时间」；共享模块 `operations/labels.ts`。
- **DEV-99 内部标识全面退出界面**：部署影响卡/结果页/操作进度/图谱详情移除全部内部 ID；错误码仅允许失败文案括注（`localizeErrorCode`）；AGENTS.md 规则修订（ID 不进折叠区、错误码不独立成值）。
- **DEV-100 junction 真机实证与 profile 更新**：六处 junction 探针（Kimi/Cursor/Trae/Gemini CLI/Copilot/共享目录）用户真机全部通过、去重正常；对应 profile junction 升级 `true`；豆包 profile 登记四个真机确认目录（AppData 内置只读、`.user_skills`、工作区 `{project_root}/skills`、共享目录）；调研文档新增 5.25。
- **DEV-101 启动恢复闸门根因修复**：四类流程 prepare 只写账目行且取消无人收尾 → 会话打开时把上个会话残留 planned/prepared 结算为 rolled_back（prepare 磁盘零副作用）；applying 及之后行保持恢复候选。用户库两条卡住行已清零；**用户真机确认打开正常（2026-09-27）**。

## 继续工作顺序

1. 用户按《人工验收清单-2026-09-27.md》继续流程 I/J（新产物已含 DEV-96—101 全部修正与 junction 放开）；DEV-101 的「取消删除后重启」回归判据顺手验证。
2. 发现问题时先回查归档证据与当前源码，按四类登记；不覆盖历史记录。
3. 流程 I/J 通过后一起提交本批未提交变更（含 profile、清扫修复、文档滚动）。
