# SkillHub 当前开发状态

更新时间：2026-09-09（功能收口 + UI 收口完成，全量自动化冻结于 `27042e6`）
当前分支：`feat/v0.2.0-product-completion`  
远端状态：以 `git rev-list --left-right --count` 为准  
当前验收提交：`27042e6`（全量自动化冻结基线）  
功能代码基线：`27042e6`

> **2026-09-09 收口批次（7 个提交，c325fc5→27042e6）**：v0.2.0 剩余开发全部完成，交付至"人工验收就绪"。
> - **质量债务（`44bb254`，独立提交）**：清除 `cargo clippy -D warnings` 全部存量阻断（含历史 `ExportFormat` derivable_impls 与 clippy 1.97 新增 lint）；修复 macOS 首跑暴露的两处存量测试缺陷——symlink 部署所有权证明此前锚定"链接指向的目标目录 inode"，集中库整体替换来源后合法移除被误拒，现改为锚定链接自身身份（ManagedCopy/Junction 语义不变）；`facade_llm_cancel` 两个用例的 `local.run_until(...)` 缺失 `.await`，断言体此前从未真正执行，且 fixture 的库目录 TempDir 在 setup 函数返回时即被删除（ENOENT）——已修复并使断言真正生效；`sanitize_skill_source_path` 测试按平台区分反斜杠语义。顺带把 repo 发现/lock 两处不可达的中文兜底错误消息改为稳定英文码式消息。
> - **i18n（`c5f5c57`）**：审计脚本升级为剥除块注释 + 显式豁免清单（fixture/mock 数据逐项注明理由）+ 非豁免命中退出码 1。冻结基线上用户可见中文命中 **0**（原 38 文件/172 行命中经逐项核实：绝大多数为注释，其余为测试专用 fixture）。zh/en 键集 parity 测试保持通过。
> - **功能小缺口（`27042e6`）**：①组合整体部署——组合面板新增"部署组合"入口，成员以 `?skill=` 预选进入既有批量部署页（复用 `5b5083f3` 反向部署模式，零新写语义）；②项目详情"解除管理"——新增 `ProjectManagedDeployments` 区块接入既有 `detach_management` 契约（仅列本项目 managed 副本，显式确认后解除，成功后刷新并汇报）；③向导恢复分支逐项冲突决策——不再静默全部 skip，复用 `CommitRestore.decisions` 既有契约逐项要求覆盖/保留两份/跳过决策，invalid_portable_data 阻断提交（与数据保护页规则一致）；恢复进度维持诚实降级（同步契约无进度事件，保留已用时状态）。
> - **已知降级复核**：自定义 Agent 重新定位实际可经编辑表单完成（提示改为指引）；内置 Agent 重定位、Agent 级忽略、AI 独立输出语言维持诚实降级（理由记录于《功能完成度与验收状态》）；减少动效由系统 `prefers-reduced-motion`（MotionConfig reducedMotion="user" + CSS）承接，视为已覆盖。
> - **UI 收口（`27042e6`，全部仅消费既有查询/字段）**：技能库统计摘要条 + 卡片事实行（当前版本/可升级/高风险）；在线发现"已在库"标记（复用 `listSkills` 查询按名保守比对）+ 搜索中播报 + 诚实空态；本机发现"发现待导入"横幅 + "审查并导入"入口（进入既有向导）+ 扫描中播报 + aria-busy；高风险流程（数据保护动作、单/批量部署）补齐进行中反馈；导入向导原有四态未动。
> - **每项均补组件测试**；已确认决策（批量升级不做、库根迁移移出、双语-only、UI 收口入开发阶段、发布路径）已记录于《功能完成度与验收状态》。

> **冻结自动化证据（2026-09-09 单次采集于 `27042e6`）**：`cargo test --workspace --locked` 111 套 641 项 0 失败；`cargo clippy --workspace --all-targets --all-features -- -D warnings` 通过；`cargo fmt --all -- --check` 通过；`generate_bindings` 无漂移；`pnpm test:frontend` 103 文件 714 项；`pnpm --dir apps/desktop check`/`build` 通过；`pnpm test:e2e` 22/22；`node scripts/i18n-cjk-audit.mjs` 用户可见命中 0（豁免 15 行 fixture）；`node scripts/verify_release_readiness.mjs` PASS + `pnpm test:release` 通过。

> 2026-09-07 单日完成《人工验收记录-2026-09-06.md》全部 27 项 AR 问题的处置并推送 19 个提交（`f327590`→`2260f36`）：
> - **P0 九项**：AR-001/002/007/008（onboarding 诚实错误+库根回显+选择器 grant 容错）、AR-011（扫描进度/结构化失败）、AR-014（导入全局互斥）、AR-015（/operations 真实记录页）、AR-017（远端采用真实下载+错误结构化）、AR-020（NoUpstream 语义）。
> - **P1/P2 十八项**：AR-003/004/005/006/009/010/012/013/016/018/019/021/022/023/024/025/026/027——含发现页重构（主页+4 子页）、在线搜索查看/安装闭环、混合导入与来源局部调整、冲突可读化+按原因筛选、组合管理子页、标准导出重设计（全量内容/显示名/输出目录 grant/外层 ZIP）、版本时间线断链修复+可读序号、Markdown 分屏与保存三决策、批量栏重组、品牌标签、健康检查范围与退出管理说明、设置层级第一步。
> - **重大断链发现**：getVersions/getVersionDiff/getRollbackImpact/commitRollback 从未接通原生 facade（此前"已实现"结论有误），本轮已全部接通真实契约。
> - **契约变更**（均经生成工具同步 bindings）：SourceState+NoUpstream、VersionResult+created_at_epoch/sequence、ExportSkill+files、ExportInput+output_dir、库根冲突 reason 参数。
> - **并行规则实践**：G/E/F/B/C 五个子代理按"文件集不相交 + i18n 分 section + 主 Agent 持有共享契约"并行；每次合并跑 parity 兜底，未再发生键丢失。
>
> 自动化证据（最终批次）：前端 101 文件 674 项 + check + build；i18n parity 无差异；Rust core/storage/application/desktop/adapters 全套通过（含 generate_bindings 同步）；fmt/diff --check 通过；clippy 受既有 core export/mod.rs derivable_impls 阻断（未顺手修改）。
> 回归准备：《验收指南》新增 1v/1w/1x/1y 四节共 35 条回归步骤，并已补充浏览器自动化/混合/桌面人工分流；《人工验收记录》26 条 AR 标记"已修复待复验"，AR-013 记录决策执行。

> **2026-09-08 浏览器自动化验收**：在 `eaee655` 基线上执行 `pnpm test:e2e`，22/22 通过（既有 10 条，加 `native-flow-preview` 12 条）。新增覆盖原生桥接预览下的发现、部署目标、组合、项目、操作记录、技能详情、来源更新和版本比较/回滚预览；Playwright Chromium 仍只是测试环境缓存，不进入仓库或 Tauri 安装包。该结果只关闭页面/交互自动化证据，真实 Tauri、文件系统、系统弹窗、网络、AI 运行和签名资产仍按指南保留人工验收。

> **2026-09-07 追加批次（5 个提交，54e7519→ce49e42）**：N9 待处理中心深度完成——后端 PendingItem 契约增加 due_date/risk/affected_deployments（真实派生）；前端暂缓（7/30 天，真实 exact_pending 忽略规则，到期自动重现）、永久忽略+确认、处理历史卡片（可撤销）、保存视图（ui_preferences）、安全批量处置（批量暂缓直执行+进度、批量永久忽略二次确认；批量转换/重查/恢复因无原生契约诚实不提供）。i18n 批次一：deployment/reconcile nativeApi 硬编码中文句改稳定错误码+describeNativeError 映射，部署结果文案改翻译键。前端 101 文件 691 项全绿；回归步骤见指南 1z 节。

> **2026-09-07 第三批追加（2 个提交，f70cba4/3766f9b）**：i18n 批次二（security 11 处 + removal 7 处错误句改稳定码，页面接 describeNativeError；operationTracker/OperationsList 的 CJK 为注释、projects/api.ts 为预览 fixture，均无需改）；AR-021 用户可维护版本名落地（迁移 0008 version_labels + set_version_label 契约 + 时间线行内命名编辑器，用户命名 > vN 序号 > 短哈希）。前端 101 文件 693 项全绿。回归步骤：指南 1aa 节。

> **2026-09-07 第四批追加（3 个提交，f70cba4→5e5185e）**：i18n 批次二（security 11 处 + removal 7 处错误句→稳定码，页面接 describeNativeError；operationTracker/OperationsList 为注释、projects/api.ts 为预览 fixture，无需替换）；AR-021 来源版本号抓取（RepoDiscoveryProvider.fetch_latest_release_tag 经 /releases/latest 302 提取 tag，15 秒预算、无认证、失败诚实 None；UpstreamCheckResult.upstream_label 随检查返回，面板优先显示可读 tag）；N10 详情单 Skill 导出（详情版本区导出入口携带 skill id 进导出流程 + 导出表单版本范围选择：仅当前版本/全部历史版本，历史经真实版本列表展开）。前端 101 文件 695 项全绿。回归步骤：指南 1aa 补充与 1z 节。

> **2026-09-07 第五批追加（1 个提交，0fb37a5）**：N12 确定性重复读模型落地——新查询 list_deterministic_duplicates（当前版本内容哈希相同的其他 Skill，联显示名），快速抽屉重复候选不再恒空占位。前端 101 文件 696 项全绿。回归：指南 1bb 节第 3 条。

> **2026-09-07 第六批追加（N11 + AR-027 盘点，3f5eff8）**：N11 集中库打开目录落地——宿主新命令 open_local_directory（canonicalize+目录校验，platform 文件管理器打开，只读），数据保护页新增"集中库目录"区块（显示库根+打开按钮+恢复点自动管理说明+迁移流程指引）；BackupFacade.libraryPath 经引导快照读取。AR-027 设置分层盘点文档完成（现位于 docs/development/archive/2026-09-09-superseded/设置分层盘点-AR-027.md：11 个偏好字段+8 项独立能力逐项对照需求 §5.39/5.40/5.42/5.44/§6，两层结构+分区设计，落地状态与 UI 打磨建议）。测试：桌面 crate +1（目录校验）、前端 backup +1（打开目录）。前端 102 文件 699 项全绿。

> **2026-09-07 第七批追加（AR-027 UI 打磨批次一，6eee3d1）**：设置页按盘点 1→6 分区顺序渲染（通用→数据保护→网络与 AI→自动化→技能库维护→应用更新），分区标题可见，密度控件并入通用分区；NetworkStoragePlaceholder 保留在末位（云同步为大版本方向，需求 §2.4/§4.2）。测试：SettingsPage 顺序断言 +1。前端 102 文件 700 项全绿。

> **2026-09-07 第八批追加（AR-021 取数口径对齐，e15a008）**：check_remote_source_update 的比较基线从分支归档改为优先最新 release/tag 归档（/archive/refs/tags/{tag}.zip；无 release 回退分支），upstream_label 与检查结论来自同一 tag 快照；apply_source_update 的远端捕获走同一 tag 管线；provider 新增 download_tag_skill_directory（共用下载预算/展平/路径防护）与 tags 落点断言。测试：adapters tag 下载 +1、facade 双路由口径对齐 +1（分支内容≠基线、tag 内容=基线 → UpToDate，证明口径对齐）。Rust 103 套全绿。

## 剩余项（诚实清单，非本轮可闭合）

1. **真实桌面取证**：双平台真机取证与签名更新取证为仅剩发布门槛项（第 3/4 条），由用户人工执行；浏览器自动化可覆盖部分已全部通过。
2. ~~AR-021 完整版本模型~~ 剩余可选增强（release 资产下载）已并入后续版本候选，不阻塞 v0.2.0。
3. **已知诚实降级**（详见《功能完成度与验收状态》复核结论）：内置 Agent 重定位、Agent 级忽略、AI 独立输出语言、恢复进度（同步契约无事件）。
4. ~~i18n 审计修复~~ 已完成（双语全覆盖，审计清零 + 豁免有据）。
5. **发布门槛**：第 1/2/5 条已满足；第 3/4 条待双平台真机取证与签名更新取证，未完成前不得创建 v0.2.0 tag（《功能完成度与验收状态》）。


## 一、当前结论

**任务队列 Q1~Q17 与原生缺口 N1~N8 已全部处理完毕，但这不等于 v0.2.0 已完成或可发布。** 已完成仅表示这些已定义任务有实现和自动化证据；真实桌面闭环、双平台取证与剩余产品缺口仍未关闭。当前浏览器 E2E 已完成 22/22；前端全量质量检查、桌面人工和双平台取证仍需分别记录。

工作树状态以 `git status --short` 为准；本次同步不触碰用户未提交改动。功能完成度和人工验收的唯一汇总见《功能完成度与验收状态-2026-09-06.md》；后续开发从真实桌面取证和剩余产品缺口继续，而非直接发布。

## 二、已完成并已推送的重点能力（本轮累计 42 个提交）

### 初始化、发现与导入
- 三分支初始化向导 + 自选库根 + 完成页汇总（`cd5eb7f`/`ac5b6b2`/`4db9341`）。
- 本机发现工作台（`7a80f3e`）；skills.sh 搜索修复为 legacy `/api/search`（`8916f3a`，401 技术债关闭）。
- **GitHub 仓库发现标准版**（`2e7176f`）：预算安全基线逐字保留（§6.2 全部上限常量）、默认 4 仓库、`download_repo_skill`（空 directory = 整仓即 Skill）→ 临时目录 → 现有导入向导（集中库仍是 SSOT）。
- **~/.agents lock 发现与导入**（`e1977b8d`）：解析规则对齐 cc-switch（github-only、分支三级回退），发现页 lock 卡片只读列出条目、逐条下载并导入。
- 批量导入冲突分组批量处理、多来源候选计数（`a16f1fb`、`2f2be9b`）。

### 操作、管理与详情
- 全局操作框架 + **后台静默导入**（`8320c6f`，验收反馈 #12 关闭）；操作记录列表 + 恢复双页签（`0febbda`）。
- 忽略项管理 + 修复预览/执行（`64ce4ed`）；来源更新检查与显式决策（`4d327bd`）；来源重关联面板（`6b97401`）。
- Agent：受管部署按唯一 Skill 聚合 + 自定义 Agent CRUD + 管理边界诚实降级（`1a9188d`）；项目：访问状态 + 装配分组 + 候选痕迹排序（`49eba85`）；反向部署 + 项目展开关联 Agent（`5b5083f`）；安全证据分组/AI 处置/高风险确认/发送范围说明（`e7e648f`）。
- 批量导出入口 + 退出管理分步预览（`9a79a57`）；滚动备份 + 保留策略 + 缓存说明（`6d95495`）。

### 原生缺口修复
- **批量来源更新检查**（`d46019f`，N8）：新增查询 `check_source_updates { skill_ids }`——显式列表逐条复用单 Skill 检测（本地 + 远端 git 来源）；单条失败按项诚实降级 SourceUnavailable，批次不整体失败；前端库页批量栏新增"检查来源更新"入口与四组结果汇总（已是最新/可升级/来源不可用/需要认证），只检查不批量升级（应用仍在详情页显式确认）。详见《人工验收问题记录》N8 节。
- **grant 链路**（`eb72ba5e`）：目录选择器签发路径 grant（grant_id ≡ 规范化路径）+ 启动时按已存 Agent 重注册——自定义 Agent 创建/编辑恢复可用。
- **外链通用打开器**（`0cf28a4`）：新增 `open_external_url` 契约（仅 https + 主机白名单校验），宿主注入平台打开器真实打开；`open_official_release` 从"仅校验不打开"占位改为真正打开；RepoDiscovery README 链接与 Markdown 外链渲染改走该契约（打包 WebView 中不再 inert）。
- **按 Skill 操作历史查询**（`866bc7b`）：新增 `list_skill_operations` 查询读取持久化操作日志；因日志暂无 Skill 维度，以 `filtered=false` + `skill_dimension_not_recorded` 限制码诚实降级为全局日志，Skill 详情"外部变化与操作历史"面板接通真实数据并附限制说明与空态。
- **AI 检查进度/取消**（`1b89ed4`）：facade 注册运行中 LLM 检查并新增 `list_running_llm_checks` 查询；`cancel_operation` 接通取消令牌中止阻塞线程；安全页 AI 卡轮询进度 + 显式取消入口 + 诚实取消反馈，重复点击不再重发。
- **导出格式（文件夹/ZIP）**（`964c134`）：`ExportInput` 增加 `format` 字段（缺省 folder 完全向后兼容），ZIP 导出把相同 manifest + skills 目录打成单一 deflate 压缩包，敏感内容决策两种格式行为一致；数据保护页导出表单新增格式选择（zh/en 本地化）。
- **远端 git 来源更新检测**（`6bdfc98`）：`check_source_update` 支持 git 来源——解析 owner/repo → 经仓库发现提供者拉取分支归档 → 定位坐标目录 → 与当前版本 tree_hash 对比（一致 UpToDate / 不同 UpdateAvailable）；远端不可达/坐标缺失/URL 非法一律诚实降级 SourceUnavailable。顺带修复两个潜在缺陷：`revision_for_skill` 对 NULL revision 崩溃；仓库导入曾同时记录临时本地目录与 git 两个 origin（取行非确定）——git 来源现为唯一 origin，`record_upstream` 以 upsert 补写坐标。**仓库导入长期上游来源**（`6e0b9c5`）：新增 `UpstreamOrigin`（仓库 URL/branch/directory）；`download_repo_skill` 下载即登记坐标，扫描导入候选自动盖章并随 prepare/commit 原样回流，提交导入后写入 `sources`（kind=git，metadata_json 记 branch/directory，幂等）+ `skill_sources` origin——GitHub 来源自此成为长期来源记录，远端更新检测有了数据基础；本地导入不落库。**概览按标签下钻**（`c7eef74`）：`BootstrapSnapshot` 新增 `tag_categories`（按标签聚合 Skill 数，无 Skill 的标签省略）；概览侧栏新增"按标签"清单，点击进入 `/library?tag=<name>`（复用库页既有 URL 参数）；共享明细列表支持自定义计数标签。**批量操作统一汇总**（`79fa7d9`）：共享 `BatchOperationSummary` 组件把批量结果聚合为成功/跳过/冲突/失败分组计数与逐项明细；批量部署页接入；批量删除由首错即停改为逐项推进并汇总（单项失败不掩盖其余结果，库列表始终刷新）。**组合 CRUD 与导出入口**（`73f3be3`）：新增 `update_combination`/`delete_combination` 契约（按名称替换成员；删除仅移除组合记录、Skill 不受影响，未知名称报 ObjectNotFound）；技能库页新增组合视图面板：列表（成员显示名）、新建（名称+成员勾选）、成员维护、内联确认删除、整组合标准导出（敏感内容组合如实报 decision-required 错误）；面板仅在 facade 提供组合方法时挂载。

## 三、剩余项（历史批次清单，2026-09-09 前的记录；最新口径见文首"剩余项（诚实清单）"）

1. **组合视图批量译文/用途说明批量维护**（FE-04 剩余，较大 UI 建设）。
2. **FE-11 剩余**：批量升级（应用侧）无契约——N8 已交付批量检查；升级仍逐 Skill 在详情页显式确认（决策见人工验收问题记录 N8 节）。
3. 已知诚实跳过：AI 输出语言、减少动效（DesktopPreferences 无字段）。
4. 其余缺口以《功能完成度与验收状态-2026-09-06.md》为准（FE-01/05/08/09/10/13 等 P0/P1 深度项）；旧逐页审查底稿已归档，仅供考古。
5. **i18n 尚未完成**：现有成果是语言偏好持久化、部分页面资源与键集 parity 测试；全路由硬编码、后端错误映射、空态/弹窗/异步反馈覆盖以及切换语言全流程验收均未完成，不能标为“国际化完成”。
6. **N9 仍未完成**：本次只修复了“忽略/暂缓规则不持久化且不生效”的基础缺陷；待处理中心的快捷视图、风险/时间/影响字段、处理历史和安全项批量处置仍需独立设计、契约与验收。

## 四、人工验收状态

- 第 1 轮人工验收 13 条反馈已全部修复归档；第 2 轮起按验收指南 **1c~1u 节**定向执行（1c 全局体验；1d 发现/完成页；1e 后台导入/操作记录/忽略项；1f 来源更新/导出/退出管理；1g Agent/项目/反向部署/安全；1h 来源重关联/抽屉/滚动备份/下钻；1i 偏好持久化/卡片视图；1j 卸载备份/矩阵/分组/缩放矩阵；1k lock 导入；1L 外链打开器；1m 操作历史；1t 远端更新检测；1u 批量来源更新检查）。
- 真实 Agent/项目部署、备份恢复、导出、双平台安装、签名更新仍属"待人工取证"。

## 五、最近质量证据

- **当前最终证据见文首"冻结自动化证据"（2026-09-09 于 `27042e6`）**；以下为历史批次记录，不能替代当前基线。历史全量前端证据：`92 个测试文件、590 项`（早期批次）、N8 记录 `94 个文件、599 项`——均为各自批次的当时计数。
- Rust：全 workspace 测试通过；桌面 crate 16 项（含 grant 链路 3 项）；卸载备份 facade 2 项；adapters 新增 agents_lock 5 项 + repo_discovery 整仓 1 项。
- 各提交均为独立闭环（相关测试 + check + build + diff --check + 推送 + 记录）。
- N1 外链打开器：core external_link 8 项 + application facade 8 项 + desktop host external_link_tests 3 项（启动注册 / 无 opener 拒绝 / 校验后打开）；bindings 经 `generate_bindings` 测试确认同步；前端 markdown/discovery 16 项测试通过。
- N2 操作历史：application facade_skill_operations 3 项（空日志 / 条目映射 / 不虚标按 Skill 过滤）；前端 skill-detail 64 项（含面板限制说明）。
- N3 AI 检查进度/取消：application facade_llm_cancel 3 项（运行中可见 / 取消生效 / 取消后可重跑）+ unsupported 操作测试改用仍为占位的 `AcknowledgeRecovery`；前端安全页进度轮询与取消测试通过（全量 586 项）。
- 符号链接诚实降级加固（`1b89ed4`）：adapters `apply` 在 `create_dir_link` 返回 Ok 后校验链接真正落地，未落地返回 `deployment.symlink_not_supported`（此前会以无关校验错误失败）；`available_capabilities` 探测同样校验落地；`deployment_filesystem` 7 项与 deployment-target 查询测试在本机三连跑稳定。
- 环境发现：本机环境 `symlink_dir` 返回 Ok 但实际未创建链接（沙箱/过滤卷特性）——`path_policy` 两个符号链接测试已加固为"链接未真正落盘则跳过"（`866bc7b`），PathPolicy 本身无需改动。
- N4 导出格式：storage zip-archive 测试（归档布局/内容/无散落目录）+ core 兼容测试（缺 format 字段默认 folder）+ 前端格式选择测试（prepare/create 均传 format）；`964c134` 同时修复迁移测试钉死 schema v6 的前序漂移（0007 迁移后未同步）与 version-store 符号链接测试同类环境问题。

## 六、工作区与协作注意事项

- 工作区状态以 `git status --short` 为准。2026-09-09 时用户未提交改动仅剩未跟踪的 `.gientech/` 与 `test-results/`——保持不提交、不还原、不叠加；历史上出现过的 `apps/desktop/src-tauri/Cargo.toml` 本地改动、`.workbuddy/`、`apps/desktop/src/platform/restart.ts` 若再次出现，同样按用户文件保留。
- `docs/development/` 下的进度/验收文档为**入库维护**的协作资料（早期"本地忽略"的说法已过时）；`archive/` 为只读追溯区。
- **归档历史**：`当前交接-2026-09-05.md`、`skill-discovery-module-spec.md（实现规格）.md`（Q1/Q2/Q17 已消费完毕）、`docs/superpowers/`（v0.1.0 实施计划 45 件）、`docs/development/task-reports/`（同期任务报告 68 件）于 2026-09-06 前后移入 `archive/`；旧交接指令与旧前端审查底稿入 `archive/2026-09-06-superseded/`。**2026-09-09 再归档**：《交接指令-2026-09-07.md》（被本文件与《功能完成度与验收状态》取代）与《设置分层盘点-AR-027.md》（任务已完成）入 `archive/2026-09-09-superseded/`。
- ~~clippy 被 `crates/skillhub-core/src/export/mod.rs` derivable_impls 阻断~~ 已于 `44bb254` 清除，全工作区 clippy 现为绿灯。
- i18n 并行合并后必须跑全量 typecheck 兜底（曾发生键丢失竞态）；提交前以 `pnpm check` 退出码为准。

## 七、下一位 Agent / 验收人的起点

1. 人工验收：按《v0.2.0-manual-acceptance-guide.md》**1dd 节**开始（基线 `27042e6`），随后 1v~1cc 与 P0 核心流程；结果回填《人工验收记录-2026-09-06.md》。
2. 修复循环：验收发现问题 → 独立分支提交修复 → 重跑受影响自动化 → 更新冻结证据（命令/提交/计数）→ 复验。
3. 进度唯一口径：《功能完成度与验收状态-2026-09-06.md》。不得把"队列完成""自动化通过"表述为"软件完成"；发布门槛第 3/4 条（双平台真机、签名更新取证）未闭合前不得创建 `v0.2.0` tag。
