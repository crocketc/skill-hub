# 自动化测试说明（2026-09-09）

更新时间：2026-09-09
接手基线：`e88fe4c2d3e17f5f738532f0a137d58bec39720c`

## 冻结结果

以下结果是 `27042e6` 的历史冻结证据，不能自动继承为当前基线的通过结果。`e88fe4c` 之后的结果将在完成反向索引和缺陷修复后重新执行并回填。

| 检查 | 结果 |
|---|---|
| `cargo test --workspace --locked` | 通过，111 个 suite / 641 个测试，0 失败 |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | 通过 |
| `cargo fmt --all -- --check` | 通过 |
| 生成绑定漂移检查 | 通过 |
| `pnpm test:frontend` | 通过，103 files / 714 tests |
| `pnpm --dir apps/desktop check` | 通过 |
| `pnpm --dir apps/desktop build` | 通过 |
| `pnpm test:e2e` | 通过，22/22 |
| `node scripts/i18n-cjk-audit.mjs` | 通过，可见用户界面硬编码命中 0，15 个测试夹具豁免 |
| `node scripts/verify_release_readiness.mjs` | 通过 |
| `pnpm test:release` | 通过 |

## 自动化覆盖

- 路由、项目、技能库、Agent、备份恢复、部署、发现、国际化和核心 Rust 适配器均有单元或集成测试。
- Playwright E2E 覆盖可在浏览器中稳定复现的导航、核心交互、加载/空态、错误反馈和双语界面路径。
- 自动化已覆盖组合整体部署、项目解除管理、恢复逐项冲突决策及主要 UI 收口逻辑的可验证部分。

## 原子用例与反向索引

- 完整目录：`docs/testing/原子测试目录-v0.2.0.md`。
- 当前共有 335 条原子用例：314 条自动化候选、21 条真实设备人工用例，覆盖 62/62 条用户故事和 10/10 条全局规则。
- QA-002～QA-010 对应的稳定用例已回填真实测试文件和断言名；其余自动化证据仍标记为“待反向索引”。必须阅读测试中的行为和断言后，才能回填具体文件、测试名称和覆盖结论；不能按测试文件名推断。
- QA-001～QA-015 对应缺口先建立最小失败测试，再做单一修复；每条证据回填到稳定用例编号。
- 目录完整性检查：`node scripts/verify_atomic_test_catalog.mjs`。

## 本轮增量证据

| 范围 | 命令 | 结果 |
|---|---|---|
| QA-002～QA-006 相关前端回归 | `pnpm --dir apps/desktop exec vitest run src/app/DiscoveryRoute.test.tsx src/features/bootstrap/BootstrapGate.test.tsx src/features/overview/OverviewPage.test.tsx src/features/import src/features/discovery src/features/onboarding` | 通过，21 files / 144 tests |
| QA-007 前端回归 | `pnpm --dir apps/desktop exec vitest run src/features/skills src/features/deployment src/features/skill-detail` | 通过，28 files / 257 tests |
| QA-007 契约回归 | `cargo test -p skillhub-core -p skillhub-application` | 通过，49 个 suite 全部通过 |
| 前端静态与类型检查 | `pnpm --dir apps/desktop check` | 通过 |
| 原子目录完整性 | `node scripts/verify_atomic_test_catalog.mjs` | 通过，335 个唯一编号、62 条用户故事、10 条全局规则 |

QA-006 关闭证据：`DiscoveryRoute.test.tsx` 直接断言存在成功导入结果时调用共享启动快照刷新（含变异验证：移除刷新调用后测试变红），全部失败结果不调用刷新。

QA-007 关闭证据：`nativeApi.test.ts` 断言抽屉补丁合并进 `set_metadata` 全量契约且不清空标签/作者/许可证（`SkillResult` 契约新增 author 后由生成工具再生成绑定）；`SkillQuickDrawer.test.tsx` 断言成功后快速视图与技能库列表各重取一次、失败和能力缺失时显示告警并回滚乐观更新。

QA-008 关闭证据：领域层 `catalog_rules.rs` 证明用途独立于原文/译文并可清空；`catalog_repository.rs` 往返测试证明 user_purpose 持久化；`skill-detail/nativeApi.test.ts` 证明 saveMetadata 经 `set_metadata` 全量契约保存且不丢字段。

QA-009 关闭证据：`crates/skillhub-application/tests/facade.rs` 新增 `commit_import_reads_frontmatter_description_into_the_catalog`，红→绿证明导入落库读取头部 description。
QA-010 关闭证据：`facade.rs` 新增 `get_skill_exposes_capture_sequence_as_current_version_label` 与 `named_version_label_takes_priority_over_capture_sequence`（变异验证：标签恒为 None 时两测皆红）；前端 `nativeApi.quickView.test.ts` 与 `skill-detail/nativeApi.test.ts` 证明展示层用可读标签而检查查询仍用原始哈希。

上述是增量证据，不替代最终全量验证。

## 下一版本规划记录

- 下一版本需要实现完整的大模型接入能力；范围至少包括模型配置、调用链路、权限与数据发送边界、失败/取消/限额反馈，以及与确定性安全检查的协同。
- 上述能力不属于 v0.2.0 当前验收范围，具体供应商、接口和测试矩阵待下一版本需求澄清后确定。

## 不能替代人工的边界

- 自动化不能证明 Windows/macOS 真机安装包、签名、权限、真实文件写入、符号链接、系统缩放和系统减少动效效果。
- 自动化不能替代真实备份介质、升级包、系统更新链路及不同设备上的可恢复性确认。
- 因此发布前仍需执行《人工验收清单-2026-09-09》，不得仅凭本文件宣称发布就绪。

## 执行约定

- 浏览器自动化依赖项目既有 Playwright 配置和本机浏览器运行时；浏览器运行时只用于测试，不进入应用打包产物。
- 新增或修改行为时，先补失败测试，再实现并运行相关检查；测试结果和覆盖边界同步更新本文。
- 自动化命令、缓存目录、构建产物和个人绝对路径不写入当前文档。
