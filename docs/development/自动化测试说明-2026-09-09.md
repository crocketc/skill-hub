# 自动化测试说明（2026-09-09）

更新时间：2026-09-09
接手基线：`e88fe4c2d3e17f5f738532f0a137d58bec39720c`

## 冻结结果

以下结果是 `27042e6` 的历史冻结证据，不能自动继承为当前基线的通过结果。`e88fe4c` 之后的结果将在完成反向索引和缺陷修复后重新执行并回填。

| 检查 | 结果 |
|---|---|
| `cargo test --workspace --locked` | 通过，111 个 suite / 641 个测试，0 失败 |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | 通过 |
| `cargo fmt --all -- --check` | 通过 |
| 生成绑定漂移检查 | 通过 |
| `pnpm test:frontend` | 通过，103 files / 714 tests |
| `pnpm --dir apps/desktop check` | 通过 |
| `pnpm --dir apps/desktop build` | 通过 |
| `pnpm test:e2e` | 通过，22/22 |
| `node scripts/i18n-cjk-audit.mjs` | 通过，可见用户界面硬编码命中 0，15 个测试夹具豁免 |
| `node scripts/verify_release_readiness.mjs` | 通过 |
| `pnpm test:release` | 通过 |

## 自动化覆盖

- 路由、项目、技能库、Agent、备份恢复、部署、发现、国际化和核心 Rust 适配器均有单元或集成测试。
- Playwright E2E 覆盖可在浏览器中稳定复现的导航、核心交互、加载/空态、错误反馈和双语界面路径。
- 自动化已覆盖组合整体部署、项目解除管理、恢复逐项冲突决策及主要 UI 收口逻辑的可验证部分。

## 原子用例与反向索引

- 完整目录：`docs/testing/原子测试目录-v0.2.0.md`。
- 当前共有 322 条原子用例：301 条自动化候选、21 条真实设备人工用例，覆盖 62/62 条用户故事和 10/10 条全局规则。
- 当前自动化证据统一标记为“待反向索引”。必须阅读测试中的行为和断言后，才能回填具体文件、测试名称和覆盖结论；不能按测试文件名推断。
- QA-001～QA-015 对应缺口先建立最小失败测试，再做单一修复；每条证据回填到稳定用例编号。
- 目录完整性检查：`node scripts/verify_atomic_test_catalog.mjs`。

## 本轮增量证据

| 范围 | 命令 | 结果 |
|---|---|---|
| QA-002～QA-006 相关前端回归 | `pnpm --dir apps/desktop exec vitest run src/features/bootstrap/BootstrapGate.test.tsx src/features/overview/OverviewPage.test.tsx src/features/import/ImportSummary.test.tsx src/features/import/ImportWizard.test.tsx src/features/import/SourceInput.test.tsx src/features/onboarding` | 通过，11 files / 77 tests |
| 前端静态与类型检查 | `pnpm --dir apps/desktop check` | 通过 |
| 原子目录完整性 | `node scripts/verify_atomic_test_catalog.mjs` | 通过，322 个唯一编号、62 条用户故事、10 条全局规则 |

上述是增量证据，不替代最终全量验证。QA-006 尚缺发现路由触发快照刷新的集成断言。

## 下一版本规划记录

- 下一版本需要实现完整的大模型接入能力；范围至少包括模型配置、调用链路、权限与数据发送边界、失败/取消/限额反馈，以及与确定性安全检查的协同。
- 上述能力不属于 v0.2.0 当前验收范围，具体供应商、接口和测试矩阵待下一版本需求澄清后确定。

## 不能替代人工的边界

- 自动化不能证明 Windows/macOS 真机安装包、签名、权限、真实文件写入、符号链接、系统缩放和系统减少动效效果。
- 自动化不能替代真实备份介质、升级包、系统更新链路及不同设备上的可恢复性确认。
- 因此发布前仍需执行《人工验收清单-2026-09-09》，不得仅凭本文件宣称发布就绪。

## 执行约定

- 浏览器自动化依赖项目既有 Playwright 配置和本机浏览器运行时；浏览器运行时只用于测试，不进入应用打包产物。
- 新增或修改行为时，先补失败测试，再实现并运行相关检查；测试结果和覆盖边界同步更新本文。
- 自动化命令、缓存目录、构建产物和个人绝对路径不写入当前文档。
