# 自动化测试说明（2026-09-11）

更新时间：2026-09-11（前端体验升级 Goal 完成；初始化流程时机修复已提交）
代码基线：`44985da`（Goal 历史完成基线 `b00770c`，Goal 起点 `0a1a448`）
范围说明：本文记录前端体验升级 Goal 的全量自动化验证结果与覆盖边界。LLM 集成测试仍全部使用本地 mock server 与明显虚构的占位密钥，不访问真实网络。

## Goal 最终全量验证（T5，提交 `b00770c`）

| 检查 | 结果 |
|---|---|
| `git diff --check` | 通过 |
| `pnpm --dir apps/desktop test --run` | 通过，120 文件 / 968 测试（Goal 起点 108 文件 / 790 测试） |
| `pnpm --dir apps/desktop check`（eslint `--max-warnings 0` + tsc） | 通过 |
| `pnpm --dir apps/desktop build` | 通过（chunk >500kB 警告为基线既有；DEV 预览路由构建期消除） |
| `pnpm exec playwright test`（完整 E2E 套件，`--output` 指向任务专用 /tmp 目录） | 通过，330/330（Goal 起点 56 条） |
| `node scripts/i18n-cjk-audit.mjs` | 通过，用户可见硬编码命中 0（dev-only 展板为已评审豁免项） |
| `node scripts/verify_atomic_test_catalog.mjs` | 通过，349 个唯一编号、62 条用户故事、10 条全局规则 |
| 9 主题锁定（`theme.test.ts`） | 通过：名称/顺序/持久化映射/注册调色板/AA 对比度/圆角阶梯/`--ui-*` 完整性 |
| 后端边界核验 | `git diff 0a1a448..HEAD` 中 crates/**、src-tauri/**、生成绑定、`package.json`、锁文件零改动 |

## 新增自动化覆盖（按 Task）

- **T0**：`theme.test.ts` 主题契约（41→51 断言）；`ui/**` 新组件测试（Field/Input/Select/CheckboxField/Switch/Icon/IconButton/PageFrame/焦点辅助/Button 加载态/Drawer/ConfirmDialog）；`AppShell` Skip Link；`tests/e2e/ui-foundations.spec.ts`（含 9 主题 accent/canvas/color-scheme、图标按钮 40px、主按钮 40px 实色断言）。
- **T1**：设置分区导航、供应商实体行、抽屉、逐字段校验与首错聚焦、删除确认、两级连接分离、能力开关 Switch 化；`tests/e2e/settings-layout.spec.ts`（17 条，含四宽度布局几何与 9 主题）。
- **T2**：`features/shared/skill-card` 契约测试；发现卡片化单测（在线/仓库/lock/诚实省略）；`tests/e2e/discovery-cards.spec.ts`（23 条，3/2/1 列断点、长文本、9 主题）。
- **T3-A**：概览首要指标层级、空/零值状态锁定；`tests/e2e/overview-preview.spec.ts`（20 条）。
- **T3-B**：默认卡片视图、筛选折叠与生效数播报、表格单滚动所有者、分页共用；`tests/e2e/library-card-view.spec.ts`；`library-preview`/`library-scale-300` 适配（300 Skill 2s 预算、翻页、筛选在卡片与表格双视图断言）。
- **T3-C**：五区结构、分区导航、hash 激活、关系轨迹、安全结果徽标；`tests/e2e/skill-detail-zones.spec.ts`（23 条）。
- **T3-D**：实体卡片、失败重试、抽屉焦点恢复、项目注册抽屉；`tests/e2e/agents-projects-preview.spec.ts`（21 条）。
- **T3-E**：标题层级映射、图片占位、播报区、暗色调色板、40rem 临界；`tests/e2e/markdown-workspace-preview.spec.ts`（14 条）。
- **T4-A**：风险-影响-操作布局、批量条不遮挡焦点、时间线、Tabs 键盘；`tests/e2e/workflow-pending/operations/recovery.spec.ts`（52 条）。
- **T4-B**：WizardShell 步骤契约、失败/跳过/重复/恢复分支独立测试；`tests/e2e/onboarding-preview.spec.ts`（19 条）；既有 `onboarding.spec.ts` 继续 2/2。
- **T4-C**：ImportShell 流程契约、门槛语义、长路径与 60 条目；`tests/e2e/import-flow.spec.ts`（27 条）。
- **T4-D**：部署/移除流程壳层、部分失败、非原子风险邻接、焦点恢复；`tests/e2e/workflow-deployment.spec.ts`（34 条）。
- **T5 集成层**：`nativeApi.loadViewMode` cards 回退契约测试；`OperationIndicator` 注册表图标断言；`RemovalImpactDialog` 错误态图标断言；发现页初始引导断言；i18n 中英键集合一致性继续锁定。

## 初始化时机修复验证（提交 `44985da`）

- 先补失败测试锁定“在 Agent 步骤前不激活集中库”，再将创建/打开调用移动到最终完成动作；覆盖创建、已有库、失败重试、重复提交和返回后重新选择目录。
- `pnpm --dir apps/desktop test --run src/features/onboarding/OnboardingBranches.test.tsx src/features/onboarding/OnboardingWizard.test.tsx src/features/onboarding/RescanWizard.test.tsx src/features/bootstrap/api.test.ts`：通过，4 文件 / 49 测试。
- `pnpm --dir apps/desktop test --run`：通过，121 文件 / 969 测试。
- `pnpm --dir apps/desktop check`、`node scripts/i18n-cjk-audit.mjs`、`git diff --check`：通过。
- `pnpm test:e2e tests/e2e/onboarding-preview.spec.ts`：19/19 用例均显示 `ok`；本机 Playwright/Vite 清理阶段不退出，进程被手动终止，故不记为干净退出的 E2E 通过。
- `pnpm --dir apps/desktop build`：受现有 `node_modules` 依赖链接异常阻塞；锁文件要求 `d3-contour@4.0.2` 使用 `d3-array@3.2.4`，当前安装树解析到 `d3-array@2.12.1`，未修改 `package.json` 或锁文件。

## 波次集成记录

每个 Task 提交后由 Goal 主 Agent 串行合并、应用翻译/路由清单并运行完整 Green（单测 + check + build + 相关 E2E + i18n 审计 + `git diff --check`），全部通过后才进入下一任务。合并顺序：T0 → T1 → T2 → T3-E → T3-A → T3-D → T3-C → T4-B → T4-A → T3-B → T4-C → T4-D。

集成期修复并回归的基线问题：

1. `ui/Icon` 注册表 SVG 无默认描边导致消费方不可见（`785513f`，先失败测试后修复）。
2. T0 展板 33 处硬编码中文导致 i18n 审计基线失败（`84ca07a`，按脚本既定机制豁免并记录理由：DEV-only、生产构建消除）。
3. `native-flow-preview.spec.ts` 详情页断言未随 T3-C 五区重组适配（集成层修复，与 `library-preview` 已建立的同模式等价适配）。

## 独立审查与修复验证

两个独立审查 Agent（渲染界面审查、代码范围审查）在 T5 完成后执行，问题逐项处置（详见《开发状态-2026-09-11》审查问题处置小节）。全部修复后在提交 `b00770c` 复跑上表全量验证，均为通过。

## 不能替代人工的边界

- 自动化不能证明 Windows/macOS 真机安装包、签名、权限、真实文件写入、符号链接、系统缩放、原生下拉框、真实字体渲染、滚动手感和读屏体验；本 Goal 涉及的页面结构变化已整理进《人工验收清单-2026-09-11》并需按新布局复测。
- 主题对比度已按 AA 阈值锁定 token 值，但真实桌面的观感（阴影、字体、光标）仍属人工。
- 300 Skill 预算为 Vite 预览层证据；原生磁盘 I/O 与数据库迁移性能仍需桌面取证。
- 因此发布前仍需执行《人工验收清单-2026-09-11》，不得仅凭本文件宣称发布就绪。

## 执行约定

- 浏览器自动化依赖项目既有 Playwright 配置（固定 5174 端口）和本机浏览器运行时；并行任务在各自 worktree 内以临时端口覆盖运行，端口改动不进入提交。
- 新增或修改行为时，先补失败测试，再实现并运行相关检查；测试结果和覆盖边界同步更新本文。
- 测试夹具只能使用明显虚构的密钥；不将真实凭据、个人路径写入测试或本文。
