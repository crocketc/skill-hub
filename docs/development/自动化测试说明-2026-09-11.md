# 自动化测试说明（2026-09-11）

更新时间：2026-09-12（追加批量导入恢复阻塞修复验证；0.2.0 Pi / DeepSeek Harness 适配、前端性能优化轮、遗留项处置轮与第二轮验证表见下文）
代码基线：`8412e91`（0.2.0 Pi / DeepSeek Harness 适配；前端性能优化轮基线 `1e06b34`，明细见《开发状态-2026-09-11》）。
说明：本文件只记录自动化命令、结果、覆盖范围与边界；真实 Windows/macOS 桌面、文件系统、系统能力与真实服务证据以《人工验收清单-2026-09-10》的当前待验收项为准。上一轮填写结果已归档；自动化通过不构成真机通过。

## 0.2.0 Pi / DeepSeek Harness 适配验证（2026-09-12）

本次新增 Pi coding agent 与 DeepSeek Harness 的目录型 `SKILL.md` 前后端适配。后端 profile、发现路径、存储 allowlist 和前端品牌映射均通过自动化验证；没有新增 IPC 字段或生成绑定。Pi 根级 `.md`、DeepSeek Harness 扁平 `<name>.md`、项目祖先搜索、runtime loading、trust/scope、链接和刷新仍不在本次实现承诺内。

| 命令 / 观察 | 结果 | 边界 |
|---|---|---|
| `cargo test -p skillhub-adapters --test builtin_profiles --test discovery` | 通过（15 个测试） | 内置 profile 契约与 Pi/DSH 实际路径展开 |
| `cargo test -p skillhub-adapters` | 通过 | 适配器全量单测/集成测试 |
| `cargo test -p skillhub-storage` | 通过 | 内置 profile 存储 allowlist 与存储回归 |
| `cargo test -p skillhub-application` | 通过 | 应用 Facade 与快照链路回归 |
| `pnpm test:frontend` | 通过（Vitest：1188 passed） | 130 个测试文件，含 Pi/DSH 部署图标与品牌标签 |
| `pnpm --dir apps/desktop check` | 通过 | ESLint `--max-warnings 0` 与 TypeScript |
| `pnpm --dir apps/desktop build` | 通过 | Vite 生产构建 |
| `node scripts/i18n-cjk-audit.mjs` | 通过（user-visible 0） | CJK 硬编码审计 |
| 本机 `pi --version` / `dsh --version` | `0.85.0` / `0.1.1-rc.2` | 仅证明命令可用，不证明 runtime 加载或执行 |
| 本机用户目录观察 | Pi `.pi/agent/skills`、`.agents/skills` 与 DSH `.dsh` profile 存在 | Windows 部分事实，不等同完整文件接入验收 |

仍需按《人工验收清单》执行 Windows 的真实发现、复制/链接部署、外部变化、解除部署和 TUI/Web 共享验证；macOS 全部待验证。

## 批量导入恢复阻塞修复验证（2026-09-12，工作区未提交）

根因是 AI 导入预检查为了读取候选而创建的临时 `prepared` 操作没有在预检查结束后结清，导致下一次启动把已完成导入误判为仍在恢复；同时旧入口使用普通 `<a>` 指向记录页，启动门阻塞时无法进入真正的恢复页面。本轮修复保留操作记录，通过 `rolled_back` 结束仅用于预检查的临时记录，并允许 `/recovery` 穿过启动门以执行恢复操作。

| 命令 | 结果 | 覆盖范围 |
|---|---|---|
| `cargo test -p skillhub-application --test facade_import_ai_checks` | 通过（3 个测试） | AI 预检查成功、逐对象失败、能力关闭均不留下启动阻塞 |
| `cargo test -p skillhub-application` | 通过（全部测试） | 应用 Facade、操作日志、恢复快照及导入链路回归 |
| `vitest run src/features/bootstrap/BootstrapGate.test.tsx` | 通过（7 个测试） | 启动阻塞文案、恢复状态与 `/recovery` 路由可达性 |
| `vitest run` | 通过（130 个测试文件，1189 个测试） | 前端全量回归；存在既有 React `act` 警告，无失败 |
| `tsc --noEmit` / `vite build` | 通过 | 前端类型检查与生产构建 |
| `cargo fmt --check --all` / `git diff --check` | 通过 | Rust 格式与变更空白检查 |

边界：以上是代码、自动化和现有本地库状态修复证据；新构建启动后的真实桌面批量导入仍需按人工清单 M-10、M-11、M-16 复测，不能由自动化结果替代。

## 前端性能优化轮验证（2026-09-12 执行，代码状态 `1e06b34`）

| 命令 | 结果 | 说明 |
|---|---|---|
| `pnpm test:frontend` | 通过（vitest：1186 passed） | 两处测试随行为适配：MarkdownWorkspace 编辑 tab 断言改 `findByRole` 等待懒加载 chunk；其余为既有回归。MemberPicker 防抖、并行 IPC 的逐项结果/首错即停语义由既有测试锁定 |
| `pnpm check:frontend` | 通过（eslint `--max-warnings 0` + `tsc --noEmit`） | 静态与类型检查 |
| `pnpm build:frontend` | 通过（vite build ✓） | 生产构建产物见《开发状态》首包体积对比：入口 gzip 601→203 kB，vendor 与路由/按需 chunk 拆分生效 |
| `pnpm test:e2e` | 通过（363 passed / 0 failed） | 懒加载路由下九页几何/主题/工作流矩阵全绿，含悬停导航、编辑器、抽屉拖拽相关路径 |
| `git diff --check` | 干净 | — |

边界：本轮未跑 `cargo test`（零 Rust 改动）；bundle 体积差异来自 vite 构建产物统计，非真机加载计时；Suspense 停顿、拖拽手感等感知性能仍属人工验收。

## 遗留项处置轮全量验证（2026-09-12 执行，代码状态 `5907353`）

| 命令 | 结果 | 说明 |
|---|---|---|
| `pnpm test:frontend` | 通过（vitest：1186 passed；第二轮末 1182） | 新增：未知码兜底、组合竞态守卫、LLM 模型字段清结果、通知焦点回退等反回归测试 |
| `pnpm check:frontend` | 通过（eslint + tsc） | 静态与类型检查 |
| `node scripts/i18n-cjk-audit.mjs` | 通过（user-visible 0） | 豁免清单不变 |
| `pnpm build:frontend` | 通过（vite build ✓） | 生产构建 |
| `pnpm test:e2e` | 通过（363 passed / 0 failed；第二轮末 359） | 新增：F9 同步开关两态、discovery 节奏三视口；品牌对比度改为全芯片采样 |
| `cargo test --workspace` | 通过（130 个测试二进制全绿） | 含迁移 0012 去重/唯一索引/版本基线、`get_skill_source` 退役后的来源诊断入口 |
| `cargo fmt --check --all` / `git diff --check` | 干净 | — |

处置明细与逐项提交哈希见《开发状态-2026-09-11》"遗留项处置轮"。

## 第二轮全量验证（2026-09-12 执行，代码状态 `8c73585`）

| 命令 | 结果 | 覆盖范围 |
|---|---|---|
| `pnpm test:frontend` | 通过（vitest：1182 passed；上一阶段 971） | 全部前端单测与组件测试，含本轮新增（见下节） |
| `pnpm check:frontend` | 通过（eslint `--max-warnings 0` + `tsc --noEmit`） | 静态检查与类型检查 |
| `node scripts/i18n-cjk-audit.mjs` | 通过（user-visible (must fix): 0） | CJK 硬编码审计；本轮唯一新增登记豁免为 `features/projects/ProjectsPreview.tsx` 的 DEV-only 预览夹具（`import.meta.env.DEV` 门控、生产构建消除，按 T0 先例在审计脚本登记理由） |
| `pnpm build:frontend` | 通过（`tsc --noEmit` + vite build ✓） | 生产构建。本轮起点时该命令因 node_modules 损坏失败（见"根因记录"），重装后恢复，零 manifest/锁文件改动 |
| `pnpm test:e2e` | 通过（359 passed / 0 failed，约 2.8 分钟；上一阶段 330） | Playwright 全量（固定 5174 端口预览构建）：九页几何/主题矩阵、工作流四步、导入/部署/移除、操作记录页、来源候选闭环、密度节奏与 1600×900 技能库几何锁定 |
| `cargo test --workspace` | 通过（exit 0，在最终 Rust 状态执行） | 全部 crate：operations 读写与恢复语义、迁移 0011 回填向后兼容、search_candidates 仓储与命令、RenameCombination 重名/歧义、journal 接线、ZCode 路径候选夹具 |
| `cargo fmt --check --all` | 通过 | Rust 格式 |
| `git diff --check` | 干净 | 空白错误 |
| `node scripts/verify_atomic_test_catalog.mjs` | 沿用通过（349 唯一编号；本轮未触及该体系） | 原子测试目录可追溯 |

## 分任务证据要点（本轮新增，均经独立实现→审查→修复循环）

- **P0-05 操作记录**：创建/保存/副本/替换/导入（prepare、commit、冲突决策、cancel）/部署/解除部署/删除/卸载/忽略规则移除全部持久化到 SQLite `operations`；测试全部经公开 `ApplicationFacade` 写入并经 `GetBootstrapSnapshot`/操作查询取证，无手工 SQL 插入；needs_recovery 仅用于可恢复的 prepared 状态；错误列仅允许白名单错误码。
- **P0-06 错误可读性**：`describeNativeError`/`keyedMessage` 覆盖导入 AI 预检、在线 AI 搜索、详情关系分析、来源重连四处；反回归测试断言界面不出现键名/`[object Object]`/裸码；AI 不可用文案明确"确定性流程仍可继续"；i18n 中英键集合一致性继续锁定。
- **P1-05/P1-06 来源模型**：迁移 `0011_source_roles.sql` 对旧 git 行向后兼容回填；confirm 候选永不自动绑定为上游（测试锁定）；ZCode 主候选改为 `~/.agents/skills`（与 codex-cli 一致），`~/.zcode/skills` 降为 lower_priority_copy，适配器夹具回归。
- **P1-09 组合**：`RenameCombination` + TargetExists 防重名 + 同名歧义 OperationConflict；四个组合命令均写操作日志；前端组合面板增删改查与成员定位有组件测试。
- **P1-08/P1-11 技能库**：卡片/表格等价选择、共享选择模型；批量条遮挡根因修复以 1600×900 几何 E2E 锁定；NotificationCenter 成功 6 秒自动消退（aria-live polite）、危险常驻（role=alert）有断言。
- **P1-14/P1-15**：同步滚动"回声消费+原位跳过"双机制防循环测试 + localStorage 持久化断言；保存并替换默认焦点取消、覆盖风险与版本回滚说明有断言；重新发现完成动作固定操作区以几何断言锁定"不滚动即可完成"；四种移除对象文案与后端行为逐字对齐（pinning 断言）。
- **P1-01/02/03/07/12/13、P2-01/02**：折叠按钮几何、设置双滚动所有者、LLM 状态机禁用解释、抽屉内测试结果、概览钻取去向唯一性、详情分层、项目页诚实缺省、`--page-gap` 节奏断言均有对应单测/E2E；明细见各任务报告（SDD 归档）。

## 本轮发现并处置的问题

1. **桌面构建阻塞（d3 blur2 缺失）**：诊断结论为 node_modules 损坏（junction 丢失 + hoist 回退使 d3-array 2.12.1 遮蔽 3.2.4），锁文件正确；干净重装修复，无 manifest/锁文件变更。不得通过降级依赖或跳过检查"修复"。
2. **既有 E2E 过时（3 条）**：native-flow-preview 2 条、llm-loops 1 条，根因是前期任务行为变更后 spec 夹具/断言未同步；以 spec-only 提交修复（`5499221`、`f6e356a`），断言只增强未放松，经轻量审查通过。
3. **CJK 审计基线失败（1 处）**：`ProjectsPreview.tsx` 预览夹具中文，按 T0 先例登记 DEV-only 豁免（`8c73585`），并经路由门控核实生产构建不含。

## 不能替代人工的边界

- 测试夹具覆盖的文件系统行为不能证明真实磁盘权限、符号链接/目录联接、大小写与只读目录行为（人工清单 M-01～M-02）。
- LLM 相关测试全部使用本地 mock/虚构密钥；2026-09-11 用户授权的 GLM 直连模型列表成功（10 个模型）只是服务端认证证据，不证明系统凭据落盘、配置持久化与两级连接（人工清单 M-13）。
- 在线发现/仓库扫描测试不访问真实网络；真实 URL、权限与失败原因需真机取证（人工清单 M-18）。
- 缩放、最小窗口、减少动效、读屏、真实字体渲染、安装/卸载/签名/备份恢复链路全部未自动化（人工清单 M-03～M-09）。
- 迁移 0011 已有自动化覆盖，但 macOS 旧库升级仍需真机取证（人工清单 M-28）。

## 复跑入口

```bash
pnpm test:frontend
pnpm check:frontend
node scripts/i18n-cjk-audit.mjs
pnpm build:frontend
pnpm test:e2e
cargo test --workspace
cargo fmt --check --all
git diff --check
```

并行任务临时改 E2E 端口时只允许本地覆盖，端口改动不进入提交；`/test-results/` 等产物已由 `.gitignore` 忽略，不提交。

## 上一阶段摘要（前端体验升级 Goal，基线 `b00770c`/`fc243a2`）

- T0–T5 全部合并（42 提交）；9 主题契约、共享控件、五区详情、工作流壳层、导入/部署/移除流程、初始化时机（最终完成才创建/打开集中库）与旧激活状态兼容均有测试锁定；120 文件/968 测试、E2E 330/330、CJK 审计 0、原子目录 349 通过。
- 上一阶段 P0 修复（生产 LLM runtime 接线、清除凭据命令、错误映射、Markdown 副本、P0-03 冲突、P0-04 部署可诊断性）的定向证据保留在 `archive/2026-09-11-人工验收问题梳理/` 与 git 历史；其中构建阻塞问题在本轮根因确诊并关闭。
