# 自动化测试说明（2026-09-13）

更新时间：2026-09-14（D1—D5 与 M-17-1 UI 优化完成 + 修复批次 + 集成回归全绿）
代码基线：分支 `feat/v0.2.0-product-completion` 当前工作树。
说明：本文件只记录自动化命令、结果、覆盖范围与边界；真实 Windows/macOS 桌面、文件系统、系统能力与真实服务证据以《人工验收清单-2026-09-13》为准。自动化通过不构成真机通过。

## 2026-09-14 开发待办 D1—D5 验证（本轮）

六个工作包（D1 安装版终端窗口、D2 技能库工具区重排与视图缺陷、D3 AI 已验证态、D4 仓库管理、D5 一体化标题栏 = 窗口机制 D5a + AppShell 集成 D5b）+ M-17-1 UI 优化 + 修复批次，全部 TDD 红→绿，每包经独立审查（审查者非实现者）。

### 分包证据

| 包 | 内容 | 关键证据 |
|---|---|---|
| D1 | `main.rs` 首行补 `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` + 2 项结构断言（按 `#[cfg(test)]` 切分防自身字面量假绿 + 首行断言） | 红 2 failed → 绿；`cargo test -p skillhub-desktop` 全绿；审查 APPROVE |
| D2 | 工具区三带信息架构（检索/高级筛选/视图）；表格横向滚动取证修复（见"取证记录"1）；卡片分页 pinned 模型（含批量条"双重预留压扁滚动区"缺陷修复） | 组件+e2e 双证据：1280 全列溢出可滚且末列几何可达、800/1600 无回归、六筛选控件等高（≤1px）；审查 APPROVE（6 Minor 已清偿或记录） |
| D3 | 三级连接测试结果持久化（settings KV `llm_connection_test_state` + `LlmConnectionIdentity` 七字段指纹）+ AI 四态消费（服务可达+模型可调用→"已验证"） | storage 4、application 14、core 2、OnlineDiscovery 39 用例；bindings 再生成（+`LastConnectionTestView`）；审查 APPROVE |
| D4 | 独立仓库管理页 `/discovery/repositories` + `refresh_skill_repo` 命令（网络开关/坐标复验）+ 逐仓扫描状态持久化（`skill_repo_scan_state`）+ `SkillRepoView` 载荷 | facade_repo_discovery 13、storage 15、RepoManagerPage 15、api 30 用例；审查 REQUEST_CHANGES → 修复后 scoped 复审通过 |
| D5a | 无边框窗口机制：conf（visible:false / Overlay / hiddenTitle）、`apply_main_window_chrome`（Windows set_decorations(false)+show，双启动路径共用 setup）、6 项 core:window 权限（无 core:default）、`windowChrome.ts` 平台封装、`WindowControls` 46px 三按钮（规格照计划） | lib 24 + windowChrome 8 + WindowControls 6 用例；审查 APPROVE |
| D5b | AppShell 改为全高侧栏 + 内容区顶栏两列结构；展开态 2.75rem Logo 独立居中、折叠按钮与顶栏控件对齐；收起态侧栏 3rem、Logo 迁入内容区顶栏左侧，实际存在的返回/前进与 h1 依次右排；context（技能库视图切换，右对齐）/end（验证 + 铃铛 + WindowControls）三分区、3rem 高度、透明无框统一控件、拖拽区、`LibraryViewModeContext` 保持同步、macOS 侧栏头部避让 | 红绿新增契约 → 全绿；app-shell-layout 7/7；审查 APPROVE |
| M-17-1 UI 优化 | 展开态 2.75rem Logo 几何居中且不受按钮推动，折叠按钮与通知/窗口按钮中心线对齐；收起态侧栏 3rem，Logo 移至内容区顶栏并与实际存在的返回/前进控件、标题依次右排且保持间距；窗口控制保持当前透明无框且不使用红色 hover | AppShell/Sidebar/base.css 契约 + app-shell-layout 几何断言；审查随本轮实现 |
| 修复批次 | D4-M1（native-flow-preview mock 换 `SkillRepoView` + 补 `__TAURI_INTERNALS__.metadata`）、D4 m1-m3、D2 五项 Minor、D5b 水合竞态加固、overview-preview 两条既有失败契约迁移 | 全量 1430 绿 + 定向 e2e 绿；scoped 复审通过 |

### 集成回归（2026-09-14 全量电池）

| 命令 | 结果 |
|---|---|
| `pnpm test:frontend` | 144 文件 / 1430 测试全绿（收尾轮基线 140/1346，只增不减） |
| `pnpm check:frontend` | 通过（ESLint `--max-warnings 0` + tsc） |
| `pnpm build:frontend` | 通过（>500kB chunk 警告为既有已知状况） |
| `cargo fmt --check --all` | 通过（修复批次新测试一处长行经 `cargo fmt` 修复后复检通过） |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | 通过（exit 0） |
| `cargo test --workspace --all-features` | 133 套件 / 850 测试全绿（基线 132/823，只增不减） |
| `node scripts/i18n-cjk-audit.mjs` | 通过（user-visible 0，豁免清单不变 52 行） |
| `node scripts/verify_frontend_lifecycle_scripts.mjs` | 通过（57 个包身份） |
| `pnpm audit:frontend` | 通过（exit 0，全等级 0） |
| `git diff --check` | 干净（仅 autocrlf 换行提示，非空白错误） |
| `node scripts/ci-local.mjs` | 十步全部通过 |
| e2e 定向 | `import-flow` + `llm-settings` + `discovery-cards` 63 项全绿；`native-flow-preview` 14/14、`overview-preview` 26/26、`library-card-view` 12/12（合并批 52 项）；`library-toolbar-notices` 7/7、`library-preview` 5/5、`library-scale-300` 4/4、`page-top-alignment` 6/6、`ui-foundations` 9/9、`keyboard-accessibility` 3 项、`app-shell-layout` 7/7；本轮新增 UI 回归在上述壳层/技能库批次中全绿 |
| `pnpm --dir apps/desktop tauri build` | exe 与 NSIS 安装包 + updater 产物均已生成（`target/release/bundle/nsis/SkillHub_0.1.0_x64-setup.exe`）；末尾签名步骤因缺 `TAURI_SIGNING_PRIVATE_KEY` 返回 1——已知现状，不算失败，不构成安装真机验收 |

### 契约迁移与取证记录（本轮）

1. **表格横向滚动（M-21-1 争议重新取证）**：既有"末列可达"自动化只在默认列集 + 窄窗口场景为真；全列开启 + 1280px 下 `table-layout:fixed` 纯 % 列宽使表格根本不溢出（`scrollWidth==clientWidth`，14 列被均分压扁），Chromium 固定布局忽略 `min-width` 与 `width:max(...)`——真机反馈"缺横向滚动条、右侧列不可达"属实。修复：每列 rem 比例+地板（总宽 1656px），区域 `scrollbar-width:auto`；行为级 e2e 锁定（800 默认列溢出可滚、1280 全列溢出滚右后末列完全可见、1280 默认列不溢出）。
2. e2e `app-shell-layout` 已迁移为 M-17-1 新契约：侧栏内折叠按钮 40px 并与顶栏控件中心线对齐，展开态 2.75rem Logo 与按钮不重叠，收起态侧栏为 3rem、Logo 进入内容区顶栏并位于按钮视觉右侧，随后按实际存在的历史控件与标题向右排列；内容区顶栏从侧栏右边开始。当前壳层几何 7/7 通过。
3. e2e `overview-preview` 两条失败为**分支既有**（commit `4569d75` 引入：标签环图 chart-link 改变 Tab 序；hero 密度档 96px vs 2026-09-13 验收后 5rem 自然排版），与本轮改动无关（OverviewPage/overview.css/theme.css 零工作树改动，失败机理经探针取证）。按契约迁移更新期望：键盘序按当前已验收 DOM（radios→chart-link→pending 链接，保留 pending 可达实质断言）、hero ≥80px 锁定 5rem 契约；两行结构等实质断言不变。
4. e2e `native-flow-preview` mock 随 `SkillRepoView` 载荷换型更新，并补 `__TAURI_INTERNALS__.metadata`（壳层 WindowControls 依赖 `currentWindow.label`）。
5. `SkillFilters.test` 三条 base.css 死规则断言迁移至 skills.css 现行契约（用例数不减）；base.css query-tools 死规则已删主块与三处媒体查询；与活选择器混写的残留（base.css 约 L2370-2411、L4693-4695）记录为后续清理待办——整块删除会使 save-view 输入与触屏目标高度样式回归，需先拆分共享选择器。（该残留已于 2026-09-14 遗留风险清理轮拆分删除，见下节。）
6. 本轮各次 Playwright 运行均正常退出，未复现此前记录的清理阶段挂起现象。

### 遗留风险清理轮（2026-09-14 晚间，TDD 红→绿）

| 事项 | 修复方式 | 证据 |
|---|---|---|
| GR-10 性能预算并行冷负载偶发超时 | 预算断言的度量对象是"缓存后的首页可达性"，但多 worker 并行冷启动时首次加载承担模块转换冷成本导致 DCL 抖过 2000ms。修复：正式计时前加一次预热导航，**预算阈值保持 2000ms 不变** | `library-scale-300` 单文件连跑两次 4/4 全绿 |
| base.css 残留死选择器（与活类并组的元素级 query-tools 规则，主块 + `pointer: coarse` 媒体查询） | 逐组拆分共享选择器列表：活选择器（`__save-view input`、`__announcement`、`__boundary`、触屏高度清单）声明体保持原样，死选择器删除；`base.test` 与 `SkillFilters.test` 的锁从"不含 `> section`"强化为"不含任何 query-tools 痕迹"（红：2 失败 → 绿） | 定向 4 文件 59/59；base.css 内 0 残留 |
| RepoManagerPage 启停/移除无防抖 | ref 重入守卫 + `togglingKey`/`removing` busy 态：Switch 在途禁用；`ConfirmDialog` 新增可选 `confirmDisabled` 透传确认钮 | 新增 2 组件用例 + `ConfirmDialog` 1 用例（红 3 失败 → 绿） |
| D3 指纹未覆盖 `custom_headers` | `LlmConnectionIdentity` 增 `custom_headers_digest`（对头的规范 JSON 做 SHA-256）：头名/值/凭据引用/sensitive 任一变化即失效，**原始头值不进入持久化指纹串**；注意：指纹形态变更使既有已存结果一次性全部按"无结果"读取（失效方向安全，下次测试即重建） | core 新增 2 单测 + application 新增 1 facade 用例（红 2 失败 → 绿） |

遗留风险清理轮验证：`pnpm test:frontend` 144 文件 / 1426 测试全绿（只增不减）、`pnpm check:frontend` 通过、`cargo test -p skillhub-core --lib llm::connection` 4/4、`cargo test -p skillhub-application --test facade_llm_admin` 15/15、`cargo fmt --check --all` 通过（新测试 use 树经 fmt 修正一次）、`library-scale-300` e2e 两次 4/4、`ci-local.mjs` 十步复跑通过。

### 仍余留（不阻塞，已记录）

- 页面自带 h1 与顶栏 h1 并存（迁移前即如此）——属跨页信息架构议题，随后续页面级任务处理，不作为风险修补。
- "无凭据引用草稿"的理论误导路径（现有 UI 不会触发）；`delete_llm_provider` 无事务包裹（与仓库既有模式一致）。
- 真机待验收：《人工验收清单-2026-09-13》G 节 D1—D5 与 M-17-1 新布局原子用例。

## 收尾轮验证（2026-09-13：审查遗留 + vitest 4 升级）

| 事项 | 命令/证据 | 结果 |
|---|---|---|
| TDD 红 | `pnpm vitest run src/features/import/ImportWizard.test.tsx src/platform/directoryPicker.test.ts`（新增 5 用例先跑） | 红：5 失败（对话框大小写重复未去重 ×1、POSIX 变体被折叠 ×1、`sameSourcePath` 未导出 ×3） |
| TDD 绿 | 同上（实现 `sameSourcePath` + 接入 `pickLocalDirectory`/`addManualSource` 后） | 54/54 通过 |
| vitest 4 全量 | `pnpm --dir apps/desktop test --run`（vitest 3.2.7 → 4.1.11） | 140 文件 / 1346 测试一次通过，无需改任何测试 |
| lint/typecheck | `pnpm --dir apps/desktop check` | `vite.config.ts` 需改用 `vitest/config` 的 `defineConfig`（vitest 4 不再增强 vite `UserConfig`，否则 tsc TS2769）；改后通过 |
| 生产构建 | `pnpm --dir apps/desktop build` | 通过（>500kB chunk 警告为既有已知状况） |
| audit 阈值恢复 | `pnpm audit --json --registry=https://registry.npmjs.org`（默认阈值） | 全等级 0：GHSA-82fw-gwwq-j7x9（vitest + `@vitest/mocker`，moderate，修复版本 ≥4.1.11）已消除；`audit:frontend` 移除 `--audit-level=high` 后复跑通过 |
| 生命周期策略 | `node scripts/verify_frontend_lifecycle_scripts.mjs` | 57 个包身份全过。注意：升级后必须**删除 `node_modules` 全新安装**（`pnpm install --force` 不清除 `.pnpm` 虚拟仓库残留，pathval@2.0.1/tinyexec@0.3.2 残留会误报）；随后从 `docs/dependency-policy.md` 删除两行过期记录 |
| 本地 CI | `node scripts/ci-local.mjs` | 十步全部通过（含默认阈值 audit、vitest 4 全量前端、Rust 132 套件 / 823 测试） |
| 导入相关 e2e | `playwright test --config=tests/e2e/playwright.config.ts`：`import-flow`（27）与 `native-flow-preview`（41） | 全部通过 |

## 计划 §7 全量验证电池（2026-09-13 执行，代码状态为本轮当前工作树）

| 命令 | 结果 | 说明 |
|---|---|---|
| `pnpm --dir apps/desktop test --run` | 通过（1433/1433，144 文件） | 前端全量单测/组件测试，含详情通知路由边距、Lucide 折叠图标、概览固定布局、协议下拉、供应商能力和初始化全局任务状态断言；原 3 项基线失败已修复（见下方“已知基线失败”） |
| `cd apps/desktop; .\\node_modules\\.bin\\vitest.cmd run src/features/onboarding/ScanStep.test.tsx src/features/bootstrap/backgroundScan.test.ts src/app/AppShell.test.tsx` | 通过（3 文件 / 26 项） | 锁定扫描按钮即时置灰与“正在扫描…”文案、无总数时不确定进度、后台交接开始时间保持，以及顶栏任务入口在视图切换后/通知中心前的全局可达性 |
| `cd apps/desktop; .\\node_modules\\.bin\\vitest.cmd run src/features/import/ImportWizard.test.tsx src/app/TaskStatusIndicator.test.tsx` | 通过（2 文件 / 47 项） | 锁定批量导入继续使用全局任务跟踪、顶栏详情显示真实 `1/42` 进度，以及导入页保留原有进度/摘要并提示可离开、到顶栏和通知中心查看 |
| `pnpm --dir apps/desktop check` | 通过 | ESLint `--max-warnings 0` + `tsc --noEmit` |
| `pnpm --dir apps/desktop build` | 通过 | Vite 生产构建（既有 >500kB chunk 警告为已知状况，路由懒加载拆分后入口 gzip ~203 kB） |
| `pnpm test:e2e` | 通过（372 passed / 0 failed，约 2.9 分钟） | Playwright 全量：壳层几何、九页顶对齐、导入/部署/移除、9 主题矩阵、宽度矩阵（800/1024/1280/1440、最小高 600）、双语核心流、键盘焦点、减少动效、60+ 候选与 300 Skill 规模、品牌素材解码 |
| `cargo fmt --check --all` | 通过 | Rust 格式 |
| `cargo clippy --locked --workspace --all-targets --all-features -- -D warnings` | 修复后通过（`-j 1` 复跑确认） | 本地 CI 首次暴露两处 `clippy::bool_assert_comparison`：`skillhub-adapters/tests/llm_protocol_contract.rs`（`assert_eq!(x, true)`）与 `skillhub-storage/tests/llm_provider_repository.rs`（`assert_eq!(x, false, "…")`），已改为 `assert!(x)` / `assert!(!x, "…")`。默认并行度下会间歇命中 `target\debug\deps\*.d` 的 Windows 写入拒绝（`os error 5`，每次不同文件）；`-j 1` 串行复跑 0 错误、正常 Finished |
| `cargo test --workspace --all-features` | 通过（132 套件 / 823 测试，0 失败） | 全部 crate：导入冲突（M-14 完全重复强制决策）、初始化扫描并发、LLM 协议族契约、迁移、恢复语义等。原 2 项 `skillhub-testkit` Windows 符号链接失败已修复（见下方“已知基线失败”）；本轮 LLM 定向套件（15/5/25/19/9/23）全部通过。全量运行偶发 `target\debug\deps` 写入拒绝抖动（`os error 5`），重跑或 `ci-local.mjs` 自动重试可消除 |
| `node scripts/i18n-cjk-audit.mjs` | 通过（user-visible 0） | CJK 硬编码审计，豁免清单不变 |
| `node scripts/verify_frontend_lifecycle_scripts.mjs` | 通过（59 个包身份全部显式复核） | 生命周期脚本策略 |
| `git diff --check` | 干净 | 空白错误 |
| 生成绑定同步 | 无漂移 | `cargo test generate_bindings`（src-tauri crate）通过，`apps/desktop/src/api/bindings.ts` 与 Rust 契约一致 |
| `pnpm tauri build`（生产/可安装构建） | exe 与 NSIS 安装包均已生成（含 updater 产物）；最后签名步骤因缺少 `TAURI_SIGNING_PRIVATE_KEY` 返回 1 | 编译、打包与图标 bundle 已验证；命令非零仅对应 M-09 签名阻塞，**不构成安装真机验收**（M-07 仍需真机） |
| `cargo test -p skillhub-adapters --test llm_protocol_contract --test llm_runner_contract; cargo test -p skillhub-core --test llm_provider_domain; cargo test -p skillhub-desktop generate_bindings -- --exact` | 通过（10 + 16 + 7 + 1 测试） | 锁定 OpenAI Chat/Responses、Anthropic、Gemini、Azure 请求形状，结构化 Markdown JSON 解析、空模型草稿可拉取列表、空模型列表提示、本地 `/v1` 推导、HTTP allowlist 和绑定无漂移 |

## 二次验收反馈对应的自动化结论

- 详情页 toast 在浮动铃铛右侧保留独立缓冲，普通页面不增加额外通知入口；无 Router 的通知组件复用场景仍可正常渲染。
- 任务栏图标资源已重新生成，`icon.png` 与 `icon.ico` 首帧四角透明、中心图形不透明，受控母本与 Tauri bundle 副本一致；这仍不替代安装后 Windows 任务栏复验。
- 概览标签分布使用 ECharts 环图固定区域，并保留文字等价列表；图表配色读取当前主题 token，不因主题切换回退到固定颜色。
- GLM 底层诊断已确认普通计费与 Coding Plan 的模型目录及 Chat Completions 均可返回；本轮解析器兼容 GLM 返回的 Markdown JSON 包裹。应用内按钮仍需真实桌面复验；DeepSeek 仅完成 CC Switch 端点/后缀对齐与 mock 契约验证，未把真实密钥写入文档或测试产物。

## 本轮真实服务诊断（不计入真机按钮验收）

- LM Studio `http://169.254.83.107:1234/v1/models` 返回 200，并列出 `qwen3.5-4b`；同机回环地址的结构化 Chat Completions 探测返回成功；该链路本地地址现已纳入本地 HTTP allowlist，应用按钮仍需真实桌面复验。
- GLM 官方 `/api/paas/v4/models` 与 Coding Plan `/api/coding/paas/v4/models` 均返回 200（各 10 个模型）；使用 `glm-4.6` 的同请求格式结构化 Chat Completions 探测返回 `{"ok":true}`。GLM API key 仅按用户授权用于这两次底层诊断，未写入日志或文档。
- 以上是底层服务诊断，不替代目标应用中的“获取模型列表”和“测试此配置”按钮验收；本轮因 CUA 未能枚举桌面窗口，未伪造按钮通过证据。
- 本轮再次运行 `pnpm test:e2e` 时 372 个用例均已逐项报告 `ok`，但 Playwright 在用例完成后的 web server 清理阶段未退出，命令被中止，因此不把这次运行记为新的最终通过；上表保留前一轮有正常退出码的 372 passed 记录。

## 矩阵覆盖对照（计划 §7"此外必须验证"）

| 要求 | 覆盖 |
|---|---|
| 9 主题 × 关键页面 | `workflow-pending`/`workflow-recovery`/`import-flow`/`overview-preview` 等 9 主题矩阵用例（1280×900） |
| 简中/英文 | `bilingual-core-flows.spec.ts`（12 用例）+ 单元层中英键集合一致性锁定 |
| 减少动效 有效/无效 | 系统通道 `emulateMedia({ reducedMotion: "reduce" })`（keyboard/settings/overview/ui-foundations 4 处）+ 用户开关通道单元测试 |
| 键盘/焦点 | `keyboard-accessibility.spec.ts` + 壳层 Tab 顺序用例 + 组件层焦点回退测试 |
| 800/1024/1280/1440 宽 ×600/900 高 | 各页宽度矩阵 + `app-shell-layout` 最小高 600 无横溢 |
| 全屏宽屏概览两行指标 | `overview-preview` 1440px 两行契约用例（hero 第一行跨 4 列、统计第二行、密度阶梯 ≥96/64px） |
| 50+ 导入候选 | `import-flow` "keeps 60+ candidates usable"（内部滚动+操作区不被遮挡） |
| 300 Skill | `library-scale-300.spec.ts`（渲染与交互计时断言） |
| 长路径/长名称/长错误 | `import-flow` 长路径换行无横溢 + 忽略项长值/错误拆分既有用例 |
| 通知 2 秒（fake timers） | `notifications.test.tsx`（`NOTICE_TOAST_DURATION_MS=2000`，可控时钟，无任意 sleep） |
| 图标 bundle/config + 生产构建 | `tauriIconAssets` 结构测试 + `brand-assets.spec.ts`（素材真实加载/解码）+ `pnpm tauri build` |

## 分任务证据要点

- **全局通知（T1）**：`notify/dismiss/notices/unreadCount/markAllRead/clear` 契约有契约测试；toast 2 秒自动消退、危险常驻、历史抽屉筛选/全部已读/清空、关闭按钮 40px 方块与焦点回退均有断言；后台扫描经 `BackgroundScanBridge` 交接有壳层集成测试。
- **M-31/M-25（T2）**：并发测试用顺序断言+多尝试（非时序窗口）；选择后台后完成动作立即退出向导进入概览（组件+e2e）；重新发现完成动作在固定底部操作区（几何断言）。
- **M-31 全局任务状态（本轮）**：ScanStep 锁定立即反馈、阶段/已用时和真实进度语义；`backgroundScan` 锁定交接不重置开始时间；AppShell 锁定全局入口顺序与任务详情抽屉挂载。原生 `run_scan` 当前不提供总数，因此自动化与真机均不得把不确定进度描述为百分比。
- **批量导入全局任务状态（本轮）**：`ImportWizard` 的 `operationTracker` 进度与全局顶栏/详情抽屉复用同一数据源；页面原有提交进度与摘要不替换，后台提示明确指向顶栏任务状态和通知中心。
- **M-13/M-05/M-16（T3）**：M-13 根因证据链（UI draft→IPC→profile→protocol adapter）记录于 task-3-report.md；错误文案断言界面不出现键名/`[object Object]`/英文残片；AI 四态切换即读配置，不可用不伪造 AI 结果。
- **M-29/M-14（T4）**：已选来源列表增删/去重/聚焦（含 case-variant 大小写折叠红绿测试）；每目录失败+单独重试（≥44px 命中区域 e2e 几何断言）；完全重复导入强制冲突决策（core+facade+组件三层）。
- **M-21 系列（T5）**：表格独立滚动容器+末列可达、批量删除底部抽屉、别名双名展示、用途编辑、偏好误报根因修复均有组件/e2e 断言。
- **M-18/新增03/M-30（T6）**：本地发现区固定首屏（真实视口 above-the-fold 用例）；图标 bundle 配置结构测试；品牌 SVG 真实加载/解码 + 来源许可记录。
- **Wave 3**：详情路由浮动铃铛（≥40px + 可打开历史抽屉 e2e）；子路由 fallback 回归（router.test）；概览两行指标契约（CSS 契约测试 + 1440px e2e 几何）。

## LLM 供应商能力实现验证（2026-09-13，计划 Task 10 定向电池）

计划《`2026-09-13-llm-provider-capability-implementation-plan.md`》Task 10 的定向验证命令，全部在当前工作树执行并通过：

| 命令 | 结果 | 说明 |
|---|---|---|
| `cargo fmt --all -- --check` | 通过 | 曾因 `facade_llm_admin.rs` 两行字段需并作一行而失败，`cargo fmt --all` 修复后通过 |
| `cargo test -p skillhub-core --test llm_provider_domain` | 通过（15） | 兼容档/结构化策略/推理策略 serde 往返；默认 `Generic` 不猜测；非法组合返回 `llm.protocol_incompatible` 且无网络调用；`generic` 可显式覆盖、内置档拒绝越界覆盖 |
| `cargo test -p skillhub-storage --test llm_provider_repository` | 通过（5） | 旧 JSON 缺字段可读；已知 ID 迁移正确、未知回退 `Generic`；endpoint/model/凭据引用/headers/enabled 不变；迁移幂等且保存后含显式字段 |
| `cargo test -p skillhub-adapters --test llm_protocol_contract` | 通过（25） | 同协议不同供应商得到不同参数；纯文本计划不带结构化字段；结构化计划跟随能力策略；DeepSeek 三面、GLM 产品线、Kimi 分离；聚合器用自身档；本地运行时免凭据；`generic` 不从端点推断 |
| `cargo test -p skillhub-adapters --test llm_runner_contract` | 通过（19） | 三级连接测试：模型级为纯文本探测、结构化级为严格探测、端点不可达跳过能力级、可达未授权在结构化级前停止、文本可答而结构化失败仍标记可调用、结构化探测使用兼容档策略 |
| `cargo test -p skillhub-application --test facade_llm_admin` | 通过（9） | 保存→列出→管理三点兼容档一致；历史行列出时携带迁移后兼容档；草稿密钥不进 provider JSON；清凭据不改 `enabled` |
| `cargo test -p skillhub-desktop generate_bindings` | 通过 | 生成绑定无漂移（`bindings.ts` 由生成器更新，无手工改动） |
| `cargo test -p skillhub-desktop` | 通过（23） | 桌面 IPC 契约 |
| `pnpm --dir apps/desktop test -- LlmProvidersSettings` | 通过（46） | 协议下拉按 `supported_protocols` 过滤、切换协议不覆盖地址/模型/密钥、移除“需开启路由”、`generic` 高级结构化覆盖、三级报告、清凭据后“已启用+未配置”、无路由文案 |
| `pnpm --dir apps/desktop check` | 通过 | ESLint `--max-warnings 0` + `tsc --noEmit` |
| `pnpm --dir apps/desktop build` | 通过 | 经根脚本 `build:frontend` 执行，约 42.5s：`tsc --noEmit` 零错误 + `vite build`（约 25.2s）+ `ensure_frontend_dist_placeholder.mjs`；>500kB chunk 警告为已知状况。注：此前一次后台任务包装（PowerShell 管道）未返回，改为直接运行同一命令后正常 |
| `git diff --check` | 干净 | 空白错误 |

前端新增/更新单测：`llmApi.test.ts`（7，兼容档贯通草稿与请求规划）、`ConnectionReportList.test.tsx`（新增 5，三级报告、结构化失败不改写为“协议不兼容”、原始错误码仅在折叠诊断内）、`LlmProvidersSettings.test.tsx`（46 项通过，含本轮新增的产品线预设分离、高级项仅 `generic` 显示、三级报告、清凭据状态等行为断言）。端到端 `tests/e2e/llm-settings.spec.ts` 已同步三级语义与文案（`Online`→`Cloud`、`Model connection available`→`Model callable`，并新增“模型级未通过时结构化级显示未验证”断言），修正 Task 8 文案变更造成的 e2e 偏差；该文件单文件复跑 **7/7 通过**。

全量 e2e 复跑（`node node_modules/@playwright/test/cli.js test --config=tests/e2e/playwright.config.ts`）：已完成 **209/372** 用例且 **0 失败**，随后 Playwright 在运行中停滞不再推进（与 2026-09-13 早前记录的“用例完成后 web server 清理阶段不退出”同属本机环境问题），因此**不将这次运行记为最终通过**。LLM 相关 e2e 证据以 `llm-settings.spec.ts` 单文件 7/7 通过为准；全量 372 的最终通过仍待环境稳定后复跑。

三层连接测试语义（自动化层断言）：服务可达=任意 HTTP 响应即算可达；模型可调用=低 token 纯文本探测；结构化兼容=按兼容档策略的真实结构化探测。未执行层级显示“未验证”而非失败；模型级未通过即不下探结构化级。

真实服务诊断（Task 9，脱敏，见《供应商兼容矩阵-2026-09-13》§5）：DeepSeek Chat、GLM Coding Plan Chat、LM Studio 本机链路在服务/模型/结构化三级全部通过；DeepSeek 清除凭据后模型级 `llm.auth_failed` 且不下探结构化级。该诊断经最终应用调用路径发起，脚本与凭据未提交，**不替代**应用内按钮与凭据落盘的真机验收。

### 已知基线失败（2026-09-13 晚间已全部修复）

`4569d75` 基线引入的 5 项失败（前端 3 + `skillhub-testkit` 2）已在 UI 收尾任务中修复，修复方式与验证：

- `src/styles/base.test.ts` ×2：断言仍为 2.5rem（40px），而 `base.css` 在 `4569d75` 已收敛为 1.75rem（28px，M-17-1“按钮太大”验收修复）。属测试断言未随设计变更更新——已把断言同步为 1.75rem，用例名同步为 28px。
- `src/app/router.test.tsx` ×1：`isolates deterministic Skill detail preview data from production` 断言详情预览不出现“Skill library”标题。`4569d75` 按新增01-4 验收反馈恢复了详情路由标准顶栏（shell 标题随 `resolveRouteTitleKey` 返回 “Skill library”，同文件单元断言已固化该行为），旧断言是顶栏隐藏时代的遗留——已改为正向断言 shell 标题可见。
- `skillhub-testkit --test workspace` ×2（`fixture_copy_rejects_nested_destination_symlink_escape` 两例）：Windows 上创建的目录符号链接会被本机安全层在创建后立即移除（`symlink_dir`/`mklink` 均报成功但条目不可见），逃逸前提无法建立。已在 Windows 分支创建后用 `symlink_metadata` 复核符号链接确实存在，不可见时按“环境不支持符号链接”跳过（与无 `SeCreateSymbolicLinkPrivilege` 的早退语义一致）；能真实持有符号链接的机器仍执行完整断言。防护逻辑本身（`validate_destination`）无需改动。

## 本地 CI（`.\scripts\ci-local.ps1` → `scripts\ci-local.mjs`）执行结果（2026-09-13）

### 整体复跑中新增发现并修复的两个问题（2026-09-13 深夜）

1. **`app_update_download` 手写 mock 的 keep-alive 复用缺陷（flaky 根因）**：整体复跑中 `manifest_propagates_signature_sidecar_rate_limit` 偶发真实断言失败（期望 `SourceSearchRateLimited`，得到 `ApplicationUpdateUnavailable`）。根因是 mock 响应未声明 `Connection: close` 且每个 mock 只 accept 一次连接——客户端把第一个连接当 keep-alive 复用发第二个请求（signature sidecar），而 mock 已停止读取该连接，请求在半开连接上失败并被正确分类为“更新不可用”。已给 `app_update_download.rs` 两处与 `llm_runner_contract.rs` 一处手写 mock 统一补 `Connection: close` + `flush` + `shutdown`（其余 mock 此前已带 close）。复跑三个受影响套件 48 测试全过。此缺陷只在全量并发跑时偶发，与限流分类逻辑无关（产品代码未改）。
2. **`audit:frontend` 在 npmmirror 镜像下不可用**：`pnpm audit` 默认走本机配置的 npmmirror registry，该镜像不提供 npm 的 audit bulk 端点（`ERR_PNPM_AUDIT_ENDPOINT_NOT_EXISTS`）。已改为显式 `--registry=https://registry.npmjs.org`。切换到官方 registry 后暴露真实审计结果：js-yaml 4.3.1（eslint 传递依赖，dev-only）有 high 级 CPU 耗尽告示——已通过 `pnpm-workspace.yaml` `overrides` 钉到 `~4.3.2`（patch 级修复）；vitest 3.2.7 存在 moderate 级路径遍历（GHSA-82fw-gwwq-j7x9，dev-only 测试工具，修复需升级 vitest 4 大版本，不在本轮范围）——`audit:frontend` 阈值相应设为 `--audit-level=high`，vitest 升级列为独立待办，不静默豁免。

| 步骤 | 结果 | 说明 |
|---|---|---|
| 1 Rust formatting | 通过（1.7s） | |
| 2 Dependency and license policy | 通过（7.2s） | `advisories ok, bans ok, licenses ok, sources ok`（伴随重复依赖与 yanked 警告，非失败） |
| 3 Rust lints | 通过（2.2s，修复后二次运行） | 首次运行暴露上表两处 `bool_assert_comparison`（已修复）；修复后通过。默认并行度曾偶发 `target\debug\deps\*.d` 写入拒绝 |
| 4 Rust tests | **通过**（修复基线失败后复跑：132 套件 / 823 测试 / 0 失败） | 编译期随机命中 `target\debug\deps` 写入拒绝（`os error 5`）、`link.exe` 1104 的抖动与并行度相关；`ci-local.mjs` 已加入特征匹配自动重试（仅 `os error 5`/`LNK1104`/`拒绝访问`，最多 3 次，第 3 次起以 `CARGO_BUILD_JOBS=1` 串行编译，真实失败不重试） |
| 5—10 | 全部通过 | 步骤 7 `audit:frontend` 原因 npmmirror 缺 audit 端点失败，改显式官方 registry + `--audit-level=high`（js-yaml 钉 `~4.3.2`，vitest moderate 升级待办）后通过；步骤 8—10（lint/typecheck、`test:frontend` 140 文件 / 1341 测试、生产构建）全绿 |

最终判定（2026-09-13 深夜）：本地 CI 十步整体跑通——修复 5 项基线失败、mock keep-alive 缺陷与 audit 端点问题后，步骤 4 在默认并行度下**一次通过、未触发重试**，全程 3 分 21 秒。`ci-local.mjs` 的特征匹配重试（最多 3 次，第 3 次起串行）保留为环境抖动兜底。

环境根因：本机对 `target/` 下文件的删除/替换偶发被安全删除层拦截或受文件句柄占用（`[safe-delete] … Some operations were aborted`），导致 rustc 替换既有产物时出现 `os error 5`/`LNK1104`；仓库根目录的临时文件删除正常。该抖动与代码无关、换一次进程即消失，`scripts/ci-local.mjs` 已在脚本层加入特征匹配自动重试兜底。另经探针确认：本机环境会**静默剥离刚创建的符号链接**（`symlink_dir`/`mklink` 报成功但条目不可见），依赖符号链接的行为无法在本机真实验证（已记入 macOS 真机验收边界）。

## 不能替代人工的边界

## 本次复验修复的补充证据（2026-09-13）

- `pnpm --dir apps/desktop test --run` 针对壳层、侧栏、概览、通知和 LLM 设置共 83 项通过；覆盖详情标准顶栏、独立折叠控件、概览剩余高度布局和标签切片显式配色。
- 概览补充短窗口 CSS 契约：`max-height: 70rem` 时撤销满高网格约束，部署区使用自然行高度，防止与标签/待办栏压叠。
- LLM 设置组件 38 项测试通过，接口格式在中英文中均不再宣称“需开启路由”。
- `cargo test -p skillhub-adapters --test llm_protocol_contract --test llm_runner_contract`（26 项）及 `cargo test -p skillhub-core --test llm_provider_domain`（7 项）通过；覆盖模型列表 `/v1/models` 优先级、空模型草稿可请求模型目录、60 秒配置探针、协议请求形状及产品线预设分离。
- 另有经用户授权的底层 HTTP 诊断：GLM Coding Plan 的 OpenAI Chat、模型列表、Anthropic 均为 HTTP 200；LM Studio 局域网地址的模型列表和严格 JSON Chat 均为 HTTP 200。诊断不写入密钥或响应正文，也不替代桌面按钮或凭据落盘验收。

- 测试夹具覆盖的文件系统行为不能证明真实磁盘权限、符号链接/目录联接、大小写与只读目录行为（人工清单 M-01～M-02）。
- LLM 相关测试全部使用本地 mock/虚构密钥；真实凭据落盘与两级连接（人工清单 M-13 发布阻塞部分）未自动化。
- 在线发现/仓库扫描测试不访问真实网络；真实 URL、权限与失败原因需真机取证（人工清单 M-18）。
- 缩放、最小窗口、读屏、真实字体渲染、安装/卸载/签名/备份恢复链路全部未自动化；`pnpm tauri build` 成功不构成安装验收。
- 9 主题/双语/减少动效的自动化为渲染层断言，观感与可读性仍需人工确认。

## 复跑入口

```bash
pnpm --dir apps/desktop test --run
pnpm --dir apps/desktop check
pnpm --dir apps/desktop build
pnpm test:e2e
cargo fmt --check --all
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --workspace --all-features
node scripts/i18n-cjk-audit.mjs
node scripts/verify_frontend_lifecycle_scripts.mjs
git diff --check
```

E2E 产物输出到任务专用临时目录时使用 `--output=<目录>`，勿覆盖 `test-results/`；`/test-results/` 与 `/apps/desktop/test-results/` 均已由 `.gitignore` 忽略。
