# 自动化测试说明（2026-09-16）

更新时间：2026-09-16（关系闭环计划 Task 5—9 全量验证电池 + 分任务证据）
代码基线：分支 `feat/v0.2.0-product-completion` 当前工作树。
说明：本文件只记录自动化命令、结果、覆盖范围与边界；真实 Windows/macOS 桌面、文件系统、系统能力和真实服务证据以《人工验收清单-2026-09-16》为准。自动化通过不构成真机通过。

## 2026-09-16 全量验证电池（主工作区复跑，代码冻结状态）

| 命令 | 结果 |
|---|---|
| `pnpm test:frontend` | 150 文件 / 1542 测试全绿（只增不减） |
| `pnpm check:frontend` | 通过（ESLint `--max-warnings 0` + tsc） |
| `pnpm build:frontend` | 通过（>500kB chunk 警告为既有已知状况） |
| `cargo fmt --check --all` | 通过 |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | 通过（exit 0） |
| `cargo test --workspace --all-features` | 138 个测试二进制 / 1028 测试全绿、0 失败（基线 136 套件，只增不减） |
| `node scripts/i18n-cjk-audit.mjs` | 通过（user-visible 0，豁免清单不变 52 行） |
| `node scripts/verify_frontend_lifecycle_scripts.mjs` | 通过（57 个包身份） |
| `pnpm audit:frontend` | 通过（ci-local 第 7 步，全等级 0） |
| `node scripts/ci-local.mjs` | 十步全部通过（exit 0；rustls 0.23.45 锁定更新后复跑） |
| E2E 全量（`tests/e2e/playwright.config.ts`） | 369 passed / 0 failed（含新增 import-governance 1 项、relationship-views 2 项） |
| 定向 E2E（import-flow / agents-projects-preview / detail-preview / skill-detail-zones / discovery-cards） | 88 项全绿（合并 Task 6/7 后先行复跑） |
| `SKILLHUB_WRITE_BINDINGS=1 cargo test -p skillhub-desktop generate_bindings` 后 `git diff --exit-code -- apps/desktop/src/api/bindings.ts` | 无漂移（bindings 全程仅由生成器更新） |
| `git diff --check` | 干净 |

环境记录：ci-local 第 2 步（cargo deny advisories）在电池期间暴露新披露的 RUSTSEC-2026-0285（rustls 0.23.43），按官方建议 `cargo update -p rustls` 升至 0.23.45 后复跑通过；仅 Cargo.lock 变更，无代码改动（提交 `0d0dfec2`）。

## 2026-09-16 Task 10 冲突组生产写入链路验证（用户裁决新增）

- TDD 红→绿三切片（`fa0353eb` core 纯函数、`c5462ff1` application 持久化+裁决合并、`1f781fc0` 端到端测试）：先写失败测试（含经 `GetRelationshipOverview(scope=skill)` 的端到端断言）再实现。
- 确定性规则：ExactContent→`duplicate_same_content`/`same_skill_version`/user_decision=None；同名不同内容→`same_name_different_content`/`uncertain`，KeepIndependent→distinct_skill、Reuse/Establish/TakeOver→same_skill_version、Copy 类→None（留待裁决不代用户决定）；`conflict_id=import-conflict:<slug>:<normalized-runtime>` 跨来源/重复导入稳定。
- 定向：`cargo test -p skillhub-core --test import_conflicts` 13、`facade_import_conflicts` 7（含端到端与重复导入/裁决保护/Skip 不触碰）、`facade_relationship_governance` 43；三 crate `--all-features` 97 套件/724 通过。
- 合并后主工作区复跑全量：cargo test workspace 138 二进制 0 失败、fmt/clippy `-D warnings` exit 0、前端 1542、check、ci-local 十步 exit 0、E2E 369、bindings 无漂移、`git diff --check` 干净。
- 边界：0013/0014/0015 SQL、前端、bindings、AI 链路零改动（纯写入方）；已登记取舍——ExactContent 走指纹优先匹配，`names_match` 恒 true 在"同名不同 runtime 名"的罕见形态下略宽，但不参与确定性判定输入（`classify_conflict_evidence` 仅按指纹判 same_skill_version）。

## 分任务证据要点（2026-09-15—16 关系闭环 Task 5—8）

### Task 5 导入关系分组确认

- TDD 红→绿：先写六类分类、判定顺序（同名冲突 → 已验证副本 → 完全重复 → 共享目录 → 已知来源不建组 → 识别未知）、结构化影响事实、纯函数无 IO 等失败测试（core `import_conflicts` 4 项新增断言红），再实现 `ImportSourceFacts` 输入与 `classify_governance`。
- 定向：`cargo test -p skillhub-core --test import_conflicts`（8/8）、`--test import_flow`（3/3）、`cargo test -p skillhub-application --test facade_relationship_governance`（37/37，含共享目录分组、同名冲突待办映射、完全重复映射、观察副本分组 4 项新增）；前端 4 文件 81 项 → 修复轮后 6 项面板测试。
- 独立复审两轮：第一轮 Standards+Spec 双 BLOCKER（i18n 双向破坏、三类分组不符 §4.1/§4.2、待办映射错位）→ 修复（提交 `56d35dd8`、`07e756ea`）→ 第二轮复审 APPROVE。
- 修复轮同时修正：无治理分组时流程步骤错位（4 步/5 步统一由 `flowStepIndex` 计算）、摘要状态误用"失败"标签（新增"待处理"attention 语义）。

### Task 6 复制副本/共享引用转管理链接

- TDD 红→绿 8 个切片（`7e88b15c`…`6279753e`）：核心转换计划 6 项红测试、体积精确探针、prepare 可行性闸门、commit 暂建链接重排、storage 影响范围修复、端到端场景、外部修改钉住、clippy 收敛。
- 定向：`cargo test -p skillhub-core --test deploy_flow`（9）+ `--test undeploy_delete`（13）；`facade_relationship_governance` 43/43（37 项既有零降级 + 6 项新增）；`cargo test -p skillhub-core -p skillhub-application -p skillhub-storage --all-features` 698/0（另 adapters deployment_filesystem 8）。
- 必测场景全落：copy→link 全周期含回退、shared reference→link（含未确认共享影响拒绝提交、共享本体指纹不变）、取消保留副本、链接能力不足不降级（Unix 只读父目录真实验证；root 进程诚实跳过并输出说明）、目标被外部修改（prepare/commit 双重）、同卷/跨卷 junction 拒绝。
- 独立复审两轴 MINOR 无阻断；整合期在主工作区发现子代理遗留 clippy 违规（测试内 `set_readonly(false)`），改为 `PermissionsExt` 恢复权限（`8cbde6f5`）。

### Task 7 Agent 页目录矩阵与 Skill 详情关系视图

- 5 个切片提交（`bc162fdc`…`7fac60cb`）+ 复审修复（`4e749fda`）。定向：agents/skill-detail/relationshipGovernance/DiscoveryRoute/i18n 192 项；前端全量 1530。
- 复审修复（TDD 红→绿）：共享消费者只认 supported 能力 + 活动关系（unknown/unsupported 不推断，unknown/unsupported 各有排除断言）、其他 Agent 关系行显式标注归属、治理待办按关系计数、冲突成员路径分隔符 i18n（en locale 断言无中文顿号）。
- bindings.ts 与 Rust 零改动（纯前端消费既有契约）。

### Task 8 可选 AI 冲突分析

- 6 个切片提交（`74fcc28b`…`6aa5ffb1`）+ 截断诚实上报修复（`f25ef846`）。定向：`facade_ai` 10/10（未配置/能力关/裁决隔离/失败留痕/四范围/总数上报）、core `conflict_analysis` 6/6（短结论截断、范围外丢弃、成员命中、置信度钳制、总数）、storage `conflict_analysis_repository` 3（round-trip/缺失/v14→v15 升级）、`semantic_duplicate` 4。
- 复审处置：Standards APPROVE；Spec BLOCKER 三项中"16 组静默截断"已修（`total_case_count` 贯通 core→facade→bindings→面板 + 覆盖提示 i18n + 三层测试）；"采纳标记无 UI""治理面板冲突区无生产宿主"与"冲突组生产无写入方"属基线既有缺口，已登记为仍需（见《开发状态-2026-09-16》）。
- aiAvailable 真实信号：与设置页共用 `usableLlmProviderLabel` 判定（已启用 + 本地或凭据已配置）；查询失败按不可用处理；不可用时按钮不渲染并显示诚实提示。

### Task 9 端到端回归与文档

- 新增 E2E：`import-governance.spec.ts`（治理阶段 5 步流程、确认门槛禁用、单项覆盖优先、AI 不可用诚实提示、待办入口与原始副本保留文案）、`relationship-views.spec.ts`（Agent 目录矩阵角色/识别/Skill 数/共享消费者/归属标注/技术详情隔离、移除影响预览；Skill 详情关系行与可回退备份说明）。
- 预览桩扩展：导入 mock facade 治理场景（`?scenario=governance`，无供应商 → 真实 AI 不可用信号）；Agent/Skill 详情预览桩提供确定性关系事实与移除影响。

## 不能替代人工的边界

- 全部自动化为渲染层/集成测试证据。真实磁盘权限、符号链接/目录联接、Windows junction、只读目录、跨卷、大小写与重启动行为以《人工验收清单-2026-09-16》为准。
- 本机安全策略静默剥离符号链接时，依赖真实链接的测试按既有策略条件跳过并有输出说明；跳过不计为通过。
- AI 冲突分析测试全部使用本地 echo/mock runner 与虚构密钥；真实 LLM 链路语义复用既有语义重复分析能力，未新增网络路径。
- Windows junction 执行器为既有桩（恒报不可用）：跨卷 junction 拒绝规则已在纯计划层测试锁定，真实 junction 行为待具备权限的 Windows 真机复验。

## 复跑入口

```bash
pnpm test:frontend
pnpm check:frontend
pnpm build:frontend
pnpm test:e2e
cargo fmt --check --all
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --workspace --all-features
node scripts/i18n-cjk-audit.mjs
node scripts/verify_frontend_lifecycle_scripts.mjs
node scripts/ci-local.mjs
SKILLHUB_WRITE_BINDINGS=1 cargo test -p skillhub-desktop generate_bindings
git diff --exit-code -- apps/desktop/src/api/bindings.ts
git diff --check
```

E2E 产物输出到任务专用临时目录时使用 `--output=<目录>`，勿覆盖 `test-results/`。
