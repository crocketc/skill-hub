# 自动化测试说明（2026-09-17）

更新时间：2026-09-17。本文记录本分支已执行的自动化证据、入口审计和不能替代人工的边界。自动化通过不构成 Windows/macOS 真机通过。

## 本轮验证

| 命令 | 结果 |
|---|---|
| `pnpm --dir apps/desktop test --run src/features/discovery/LocalDiscoveryWorkbench.test.tsx` | 14/14 通过；新增 discovery rescan tracker 回归测试先红后绿 |
| `pnpm --dir apps/desktop test --run src/features/agents/AgentListPage.test.tsx` | 12/12 通过 |
| `pnpm --dir apps/desktop test --run src/features/overview/OverviewPage.test.tsx` | 25/25 通过 |
| `pnpm --dir apps/desktop test --run src/features/relationships/responsiveLayout.test.ts src/features/relationships/decisions/useConflictDecisions.test.tsx` | 4/4 通过 |
| `pnpm exec playwright test --config=tests/e2e/playwright.config.ts tests/e2e/overview-preview.spec.ts --grep "800x600|stacks chart" --workers=1` | 2/2 通过 |
| `pnpm exec playwright test --config=tests/e2e/playwright.config.ts tests/e2e/overview-preview.spec.ts --grep "compact overview panels" --workers=1` | 1/1 通过 |
| `pnpm --dir apps/desktop check` | lint/typecheck 通过 |
| `git diff --check` | 通过；仅有 Git 的 CRLF 转换提示 |

此前全量电池和关系 Task 5—10 证据保留在归档快照 `2026-09-16-Task10收口前快照`，本轮没有借定向测试冒充全量复跑。

## 异步入口审计（125 次 / 26 个非测试源文件）

判定口径：`API边界` 是 facade/native API 内部调用，不单独创建顶栏任务；`纯浏览` 只读查询，不记操作日志；`已桥接` 是用户可触发的长流程并进入 `runTrackedOperation`；`未桥接` 是仍需后续收口的用户写入/即时命令。一个用户入口应有至少一个成功、失败、取消或恢复测试；本轮新增测试覆盖 Agent 与 discovery 重扫描。

| 文件 | 调用点（行） | `runTrackedOperation` | 分类 |
|---|---:|:---:|---|
| `deployment/reconcileNativeApi.ts` | 40 | 否 | API边界 |
| `backup/nativeApi.ts` | 6, 11, 16, 20, 25, 30, 44, 49, 69, 74 | 否 | API边界；页面恢复提交已桥接 |
| `deployment/nativeApi.ts` | 147, 151 | 否 | API边界；页面部署提交已桥接 |
| `bootstrap/api.ts` | 84, 96, 135, 142, 152, 162, 176, 227 | 否 | API边界/启动读取 |
| `app/router.tsx` | 144 | 否 | 纯浏览/路由读取 |
| `agents/nativeApi.ts` | 152, 181, 192 | 否 | API边界；Agent 重扫描入口已桥接 |
| `discovery/api.ts` | 83, 134, 141, 151, 161, 171, 180, 200, 210, 220 | 否 | API边界；本机发现重扫描入口已桥接 |
| `import/nativeApi.ts` | 354, 365, 409, 420 | 否 | API边界；导入提交使用手工 tracker，仍需统一桥接审查 |
| `markdown/nativeApi.ts` | 54, 70, 83 | 否 | API边界 |
| `skills/nativeApi.ts` | 217, 237, 249, 259, 334, 359, 362, 367, 375, 379 | 否 | API边界；页面批量长流程已桥接 |
| `projects/nativeApi.ts` | 42, 62, 84, 92, 103 | 否 | API边界 |
| `security/nativeApi.ts` | 63, 75, 110, 124, 134, 144, 154 | 否 | API边界；安全分析入口部分已桥接 |
| `skill-detail/nativeApi.ts` | 149, 175, 280, 289, 305, 322, 335, 353, 514, 536, 570, 583 | 否 | API边界；即时写入/分析入口未统一 |
| `pending/nativeApi.ts` | 36, 56, 61, 64, 68, 72, 78, 103, 123 | 否 | API边界 |
| `removal/nativeApi.ts` | 42, 46, 53, 66, 73, 85, 119 | 否 | API边界；页面移除长流程需逐入口核对 |
| `relationships/governance/nativeApi.ts` | 60, 75, 86, 98, 109 | 否 | API边界；治理页面调用已桥接 |
| `relationships/decisions/decisionsApi.ts` | 54, 64 | 否 | API边界；同步决定按即时命令处理 |
| `settings/AiNetworkSettings.tsx` | 24 | 否 | 未桥接即时写入 |
| `operations/nativeApi.ts` | 35 | 否 | API边界/纯浏览 |
| `settings/api.ts` | 203, 215, 224, 233 | 否 | API边界 |
| `settings/AutomationSettings.tsx` | 15 | 否 | 未桥接即时写入 |
| `settings/llmApi.ts` | 196, 208, 217, 220, 223, 226, 239, 265 | 否 | API边界 |
| `settings/GeneralSettings.tsx` | 28, 39 | 否 | 未桥接即时写入 |
| `settings/nativeApi.ts` | 40, 74, 122, 136, 146, 152, 162 | 否 | API边界 |
| `settings/ViewSettings.tsx` | 15 | 否 | 未桥接即时写入 |
| `settings/SettingsPage.tsx` | 211 | 否 | 未桥接即时命令 |

审计结论：数量是源码命中数量，不是 125 个页面入口。纯浏览查询不应进入操作日志；native API 不应重复包一层 tracker。后续需把“用户可见写入入口”拆成单入口测试矩阵，优先覆盖设置即时写入、Skill 详情写入/分析、移除和导入手工 tracker 收口。

## 已登记的测试边界与已知波动

- Windows junction、符号链接权限、只读目录、跨卷、大小写和重启动仍必须真机验证。
- `symlink_physical_id_for_path` 在 Windows 真实实现与现有期望值存在 mismatch，不能用类型检查或 macOS 通过掩盖。
- 前端全量 5-worker E2E 中 markdown editor 1024px textbox 存在偶发既有超时；单 spec 重跑通过，未添加任意等待或跳过。
- `OverviewPage` 的“configured/discovered agent calibers distinct”曾出现单测超时；单文件重跑通过，未降低断言。

