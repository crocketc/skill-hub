# 自动化测试说明（2026-09-18）

更新时间：2026-09-23（当前收口版）。本文件只保留当前自动化命令、结果和不能替代人工的边界；旧批次日志与历史基线已归档至 `docs/development/archive/2026-09-21-人工验收清单整理/`。

> 自动化通过不构成 Windows/macOS release 真机通过。人工验收只按《人工验收清单-2026-09-18.md》执行并记录。

## 2026-09-22 release 记录

- `pnpm --dir apps/desktop tauri build --no-bundle` 通过，未使用临时 override；产物 `target/release/skillhub-desktop`，构建时间 `2026-09-22 20:18:26`，源码 HEAD `286199928fad1be0d08894570e629f65dc831be7`。
- 当前工作区在 2026-09-22 22:38 再次完成前端生产构建和 Tauri 无打包构建；未留下临时 override 配置。该产物包含提交前的 DEV-55 收口；实现已提交为 `fa4c2ee2`，人工验收前仍需从该源码 HEAD 重建。
- `b9cb7c30` 的最终 macOS release 已于 `2026-09-22 23:37:25 +0800` 重建成功；未使用临时 override，产物为 `target/release/skillhub-desktop`。
- `dfa4839f` 的最终 macOS release 已于 `2026-09-23 00:28:15 +0800` 重建成功；未使用临时 override，产物为 `target/release/skillhub-desktop`。
- 进程可启动并打开本地数据库，但未出现可被桌面自动化层识别的窗口；未取得窗口尺寸、截图或页面交互证据，不将本轮人工项写成通过。

## 当前自动化结果

| 命令 | 结果 |
|---|---|
| `pnpm --dir apps/desktop install --frozen-lockfile` | 通过。 |
| `pnpm --dir apps/desktop test` | 通过：169 个测试文件、1845 个测试。 |
| `pnpm --dir apps/desktop lint` | 通过，`--max-warnings 0`。 |
| `pnpm --dir apps/desktop typecheck` | 通过。 |
| `pnpm --dir apps/desktop build` | 通过，Vite 生产构建完成。 |
| `pnpm --dir apps/desktop tauri build --no-bundle` | 通过；最终 `b9cb7c30` 构建未使用临时 override，产物为 `target/release/skillhub-desktop`。 |
| `cargo test --workspace` | 通过。Windows 符号链接能力守卫按 RC-15 规则跳过，不将跳过场景写成通过。 |
| `cargo fmt --all -- --check` | 未通过，原因是既有未触碰文件与当前 rustfmt 版本存在格式差异；未做无关全仓重排。 |
| `pnpm test:e2e` | 通过：396/396，5 workers。 |
| `pnpm test:e2e`（2026-09-23 复跑） | **未全绿**：398 条当前回归产生导入治理、本地流程与 onboarding 等非概览失败产物；尚未完成归因，不能将历史 396/396 视为当前 HEAD 的全量绿证据。DEV-66 只以其 35/35 相关 E2E 和定向检查作为自动化通过依据。 |
| `git diff --check` | 通过。 |
| `pnpm --dir apps/desktop test --run src/features/discovery/RepoDiscovery.test.tsx src/features/discovery/RepoManagerPage.test.tsx` | 通过：2 个文件、29 个测试；覆盖共享仓库卡片渲染、状态和操作。 |
| `pnpm --dir apps/desktop test --run src/features/overview/OverviewPage.test.tsx src/features/overview/OverviewPreview.test.tsx` | 通过：2 个文件、31 个测试；覆盖 CSS/预览契约。 |
| `pnpm test:e2e -- tests/e2e/overview-height.spec.ts` | 通过：7/7；覆盖 `800×600`、Windows 175% 缩放后的 `1462×866` 和 `1920×1080`。断言页面根无纵向滚动、所有概览区块完整可见且无模块内部滚动。 |
| DEV-77 定向 Vitest | 通过：`chartContainerResize`、概览预览、概览页、部署图共 4 文件 / 36 测试；覆盖容器观察、清理和 4 列指标轨道契约。 |
| DEV-77 概览回归 | 通过：`overview-height`、`overview-preview`、`app-shell-layout` 共 47/47；覆盖 `800×600`、`1462×866`、`1920×1080`、窗口化自然流、最大化指标带填满及部署/标签 SVG 均在自身卡片内。 |
| DEV-77 Tauri release | 通过：`pnpm --dir apps/desktop tauri build --no-bundle --config src-tauri/tauri.build.override.json`；前端产物已先经生产构建校验，临时 override 已删除；`target/release/skillhub-desktop.exe` 于 `2026-09-23 11:12:17 +0800` 生成。 |
| DEV-78 | 待开发，暂无新增自动化结果。现有 `tag_chart()` 和 `deployment_chart(Project)` 的测试尚未覆盖“无标签总数守恒”“多标签不重复计数”“Agent 目录排除项目维度”。 |
| DEV-79 | 待开发，暂无新增自动化结果。现有图谱测试尚未覆盖节点/边标签可读性、Agent 徽标缩放、拖动文本选择抑制，以及按 Skill 持久化完整视图状态。 |
| DEV-80 | 待开发，暂无新增自动化结果。`DeploymentDialog`、`BatchDeploymentPage` 及计划展示仍直接使用 `target.label`，现有 AgentPresentation 回归未覆盖技能抽屉进入部署流程后的目标选择、预览、结果和无障碍名称。 |
| `pnpm --dir apps/desktop test --run` | 通过：170 个测试文件、1864 个测试；包含 DEV-73 AgentPresentation 及全部消费点回归。 |
| DEV-55 定向回归 | 已通过：`ImportWizard.test.tsx` 70/70、`ImportSummary.test.tsx` 9/9、`DiscoveryRoute.test.tsx` 8/8、关系治理页 28/28，以及应用层关系治理 59/59；确认无治理决定仍可复制到集中库并保留来源。默认并发全量 Vitest 会在既有 `libraryViewContext` 测试中产生未隔离的错误输出，不能作为本轮全绿证据。 |
| DEV-67 标题去重 | 已通过：`RelationshipsLayout.test.tsx` 5/5、`AppShell.test.tsx` 21/21；确认关系页不再渲染重复的模块标题，仅保留三个子页签。 |
| DEV-76 导入恢复门禁 | 已通过：`cargo test -p skillhub-application` 全量；安全门禁拦截的公共外观测试断言操作不产生恢复候选，`facade_operation_journal` 7/7。 |

## 覆盖结论

- DEV-2—65：代码实现和自动化验证已完成；Vitest、Rust workspace、Playwright、lint、typecheck 和构建结果均已记录。
- DEV-1：自动化不能替代 macOS 第二个真实可写卷的跨卷写入、回滚和权限证据。
- DEV-8：自动化不能替代官方 WorkBuddy logo 素材确认和 macOS/Windows 真实视觉验收。
- 远程来源、基础安全门禁、取消/重试、来源版本事实和关系图投影已有代码/自动化覆盖；真实 release 操作链仍待人工执行。
- DEV-55 自动化仅证明“导入不插入治理步骤、可在完成页进入独立治理、后端不要求治理决定、现有迁移顺序仍受测试保护”；不能替代真实 Agent 目录、链接权限、备份恢复和 macOS/Windows release 操作证据。默认并发全量 Vitest 的 `libraryViewContext` 隔离错误须另行定位，不能用定向通过掩盖。
- DEV-76 自动化仅证明无写入的安全拦截和输入无效不会形成启动恢复门禁；不能替代真实 release 中失败导入、关闭重启、操作记录呈现和成功导入保留的人工证据。
- DEV-66 不再以 `max-height` 推断最大化。窗口适配层将 Tauri 原生最大化状态映射到根样式类；状态切换后图表会重新量尺。DEV-77 进一步以容器 `ResizeObserver` 覆盖异步最大化布局完成时的首次量尺，并断言 4 张指标填满指标带、部署/标签 SVG 不越出各自卡片。最新相关 E2E 47/47、DEV-77 定向 Vitest 36/36 通过。
- DEV-78 尚未实现；现有自动化不能证明标签分类和等于技能总数，也不能证明 Agent 目标不会进入项目部署维度。实现前必须先冻结多标签技能的互斥归类规则，再补后端聚合和概览 E2E。
- DEV-79 尚未实现；现有图谱单测覆盖确定性布局和基础交互，但不能证明真实节点文本尺寸、边标签空间、浏览器文本选择行为或跨页面/跨 Skill 的状态恢复。
- DEV-80 尚未实现；现有 AgentPresentation 测试不能证明技能抽屉到部署流程的目标选择、批量部署、预览/结果链路都使用品牌 + 类型/共享目录展示，也不能证明主展示不泄露裸技术 ID。

## 人工边界

自动化不覆盖或不能最终证明：真实用户目录、真实 Agent 文件保护、真实权限/只读目录、跨卷行为、Windows/macOS 窗口与主题视觉、Dock 图标光学尺寸、人工可理解性以及截图证据。DEV-66 的 31/31 测试覆盖概览高度/溢出契约和预览布局，但不能替代真实最大化窗口、多设备和 release 视觉验收；DEV-67—72 的关系定向测试和 Playwright 回归不能替代真实窗口中的视觉缩放、居中、无滚动和窄屏收纳验收；DEV-73 的全量 170/1864 测试覆盖 AgentPresentation 及主要消费点，但不能替代真实 release 的品牌 logo、类型可理解性和共享目录人工验收；DEV-74 的 29/29 targeted tests 已覆盖仓库发现配置区与仓库管理页的共享卡片投影、状态和操作，但仍不能替代多视口 release 视觉验收。DEV-75 的类型术语自动化仍需 release 视觉确认。发现问题时，保留现场并回查归档后再登记分类。
