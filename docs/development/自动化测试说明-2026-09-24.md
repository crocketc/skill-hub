# 自动化测试说明（2026-09-24）

更新时间：2026-09-24。本文件只保留当前自动化命令、结果和不能替代人工的边界；上一轮批次记录已归档至 `docs/development/archive/2026-09-24-import-deployment-relationship-governance/`。

> 自动化通过不构成 Windows/macOS release 真机通过。人工验收只按《人工验收清单-2026-09-24.md》执行并记录。

## 当前自动化结果（2026-09-24，分支 `feat/v0.2.0-product-completion`）

| 命令 | 结果 |
|---|---|
| `cargo test --locked --workspace` | 通过：1270 通过 / 0 失败。 |
| `cargo clippy --locked --workspace --all-targets --all-features -- -D warnings` | 通过。 |
| `cargo deny check advisories bans licenses sources` | **licenses 未通过**：`notify v8.2.0` 的 `CC0-1.0` 不在允许清单（经 `gix` 依赖链引入）。在干净 HEAD 复验同样失败，属既有依赖策略边界，本轮未变更任何依赖；advisories/bans/sources 通过。 |
| `cargo fmt --all -- --check` | 未通过：既有未触碰文件的 rustfmt 版本差异（工具边界，本轮未触碰相关文件，未做无关重排）。`git diff --check` 通过。 |
| `pnpm install --frozen-lockfile --ignore-scripts` | 通过。 |
| `pnpm verify:lifecycle` / `pnpm audit:frontend` | 通过。 |
| `pnpm --dir apps/desktop test --run` | 通过：1933 测试。 |
| `pnpm check:frontend`（lint `--max-warnings 0` + typecheck） | 通过。 |
| `pnpm build:frontend` | 通过（仅既有大 chunk 提示）。 |
| `pnpm test:e2e` | **通过：404/404（2.6 分钟，5 workers）**。当前 HEAD 首次全量全绿。 |

> `pnpm ci:local` 会在第一个失败阶段（Rust formatting 的既有工具差异）停止；其余阶段已按 `scripts/ci-local.mjs` 的定义逐条手动执行并记录如上（clippy、Rust 测试、依赖/生命周期/审计、前端 lint/typecheck/测试/构建均通过，licenses 策略为既有失败），不因此把 ci:local 写成整体通过。

## 本轮新增/变更的测试范围

- **跨层回归（计划 15.8）**：v18 夹具升级、三个崩溃恢复检查点、watcher 根目录不匹配、部署确认伪造/过期负向路径均有既有测试；本轮补齐 `QueryOpenImportBatch` 的唯一空白——facade 重启后打开批次仍可恢复、提交后从开放列表消失（`facade_import_conflicts.rs`）。
- **E2E 契约更新**（对齐 pair 部署契约与品牌展示约定）：
  - `workflow-deployment.spec.ts`：处置分组（blocked 不阻塞其它 pair、确认回退先重新生成最终预览）、五桶摘要、单项排除后重生成、预览失败无分组无提交。
  - `import-governance.spec.ts`：治理与向导分离后的完成页深链（`task:preview-governance`）、整理来源副本 CTA、原始副本保持不变。
  - `relationship-views.spec.ts`、`onboarding-preview.spec.ts`、`agents-projects-preview.spec.ts`：品牌 + 用户可理解类型的可访问名（如 `OpenAI · 终端`），无裸 `client_id`/技术 ID。
  - `native-flow-preview.spec.ts`：原生 IPC 桩补 `get_deployment_batch_preview` / `commit_deployment_preview`，治理链接计数口径改为需处理状态并集（needs_validation + needs_attention + blocked）。
  - `overview-preview.spec.ts`：治理入口新契约 `Open needs-governance relations (N to handle)` 与带状态并集的深链；标签面板无内部滚动的现行等价断言（fb57b7b6 起为静态面板）。
  - `overview-height.spec.ts`：10/10（800×600、1462×866、1920×1080、窗口化对照、指标带填满、SVG 边界）。
- **修复的真实缺陷测试**：`committedBlocked` 文案键解析（`DeploymentResults.test.tsx` 红灯先行）、项目注册复选框列宽（E2E DEV-14 长标签场景）、800×600 最大化 hero 跨列与极矮密度（`overview-height`）、对比度工具 `color(srgb)` 解析与禁用控件豁免。

## 不能替代人工的边界

- 自动化不覆盖：真实用户目录与 Agent 文件保护、真实权限/只读目录、Windows 目录联接与 macOS 符号链接的真实落盘、跨卷行为、真实窗口/主题视觉、长路径与批量耗时、通知中心人工可读性。
- E2E 使用 DEV 预览路由与 mock 门面（`native-flow-preview` 经页内 IPC 桩），只能证明前端契约与几何，不能证明原生 IPC 与数据库的真实往返。
- 部署预览的确认指纹/过期裁决由后端测试覆盖语义，真实时间推进与多进程并发仍需 release 人工验证。
- 概览高度契约已覆盖三类有效视口，但仍不能替代真实 DPI 缩放下的最大化/窗口化人工复验（判据见人工清单流程 A）。
