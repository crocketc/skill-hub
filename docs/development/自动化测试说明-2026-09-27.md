# 自动化测试说明（2026-09-27）

> 最新状态：DEV-101 启动清扫修复完成——`cargo test --locked --workspace` 151 个套件全绿；签名 release 重建于 2026-09-27 00:27（target/release/skillhub-desktop.exe，SHA-256 73A257169BCD0A35C9656D9718313ADADF92E2DFA01280297EC858AC21FA2212，含 DEV-96—101 全部修正）。**用户已于 2026-09-27 真机确认打开正常、恢复闸门不再出现。**前端全量 1989/1989（2026-09-26）与定向 E2E 全绿；人工验收流程 I/J 待执行。

更新时间：2026-09-27。本文件只保留当前自动化命令、结果和不能替代人工的边界。上一快照（2026-09-26，含 DEV-87—99 的逐批命令记录）已归档至 `docs/development/archive/2026-09-27-验收反馈修复批次/`。

> 自动化通过不构成 Windows/macOS release 真机通过。人工验收只按《人工验收清单-2026-09-27.md》执行并记录。

## 当前自动化结果（2026-09-27，分支 `feat/v0.2.0-product-completion`）

| 命令 | 结果 |
|---|---|
| `cargo test --locked --workspace` | 通过：151 个套件全绿 / 0 失败（含 DEV-100 profile 断言更新、DEV-101 新增清扫用例）。 |
| `cargo fmt --all -- --check` | 未通过：既有未触碰文件的 rustfmt 版本差异（工具边界，未触碰相关文件，未做无关重排）。`git diff --check` 通过。 |
| `cargo deny check licenses` | 既有失败：`notify v8.2.0`（经 `gix` 引入）的 `CC0-1.0` 不在允许清单；干净 HEAD 同样失败，属依赖策略边界。 |
| `pnpm --dir apps/desktop test --run` | 通过：1989/1989（178 文件，2026-09-26 基线；DEV-99/101 改动后相关套件复跑全绿）。 |
| `pnpm check:frontend` | 通过（ESLint 与 TypeScript）。 |
| `pnpm test:e2e`（定向） | workflow-operations + workflow-deployment 54/54；import-deploy + native-flow-preview + workflow-recovery 35/35。全量 404/404 基线为 2026-09-25。 |
| `pnpm --dir apps/desktop tauri build`（带签名） | 通过；产物见文件头。 |

## DEV-100/101 本轮测试变更（2026-09-26/27）

| 范围 | 变更 |
|---|---|
| `builtin_profiles.rs` | junction 真机验证名单扩充（+cursor.editor/cli、github-copilot.cli/ide、google.gemini-cli、kimi.code、trae.code、agent-skills.shared-directory）；内置目录计数 5→6（豆包 AppData 内置目录登记）；expectations 夹具同步豆包四路径。 |
| `facade_operation_journal.rs` | 新增 `stale_planned_and_prepared_rows_settle_as_rolled_back_on_startup`：跨会话 planned/prepared 行结算为 rolled_back 且闸门放行（Clean）；applying 行不受清扫影响并继续闸住（无 recovery_data 报 InProgress，NeedsRecovery 保留给携带可回滚数据的行）。 |
| 存量测试对齐 | `facade_repairs_unfinished_operation_and_requires_valid_ignore_removal` 改为构造 facade 后种行（跨会话 planned 行不再成为候选）；操作时间线/恢复相关前端夹具 kind 对齐真实契约（`import_skill` 等）并补 `object_name`。 |

## 运行注意事项

- **E2E 必须用 `pnpm test:e2e`**（根目录脚本带 `--config=tests/e2e/playwright.config.ts`）。直接 `npx playwright test` 不加载 baseURL，会大面积报「Cannot navigate to invalid URL」——是调用方式错误，不是产品缺陷。
- 单测需在 `apps/desktop` 目录下运行 vitest；从仓库根会解析到无 jsdom 配置的 vitest。

## 不能替代人工的边界

- 自动化不覆盖：真实用户目录与 Agent 文件保护、真实权限/只读目录、Windows 目录联接与 macOS 符号链接的真实落盘、跨卷行为、真实窗口/主题视觉、长路径与批量耗时、通知中心人工可读性。
- E2E 使用 DEV 预览路由与 mock 门面（`native-flow-preview` 经页内 IPC 桩），只能证明前端契约与几何，不能证明原生 IPC 与数据库的真实往返。
- DEV-101 的真实进程崩溃时序无法在自动化中稳定制造；闸门在真实 applying 中断（含 recovery_data 回滚）下的行为仍按流程 I 真机验收。
