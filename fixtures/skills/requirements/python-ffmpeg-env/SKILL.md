---
name: media-helper
requires:
  - python>=3.11
  - env: OPENAI_API_KEY
---

# Media helper

This Skill uses Python for metadata processing.

Install the optional helper packages before use.

The MCP server is configured by the Agent.

The following example invokes a media command when rendering audio.

Use the ffmpeg command only when rendering audio.

<!-- reference clue follows -->

The ffmpeg binary is called by this example.
