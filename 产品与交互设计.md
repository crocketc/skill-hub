# SkillHub 产品与交互设计

> 文档状态：主要产品结构与交互范围已确认，待用户整体验收
>
> 日期：2026-08-21
>
> 上游依据：[需求文档](./需求文档.md) 与 [Agent 平台兼容性调研](./Agent平台兼容性调研.md)
>
> 当前边界：本文定义信息架构、页面职责、核心流程和交互规则；不确定视觉颜色、品牌风格、图标、字号、间距、技术栈、数据结构或具体文件实现。

---

## 1. 设计目标

SkillHub 面向同时包含普通用户和开发者的个人本地 Skill 管理场景。产品交互需要满足：

1. 用户不需要理解 Git、软链接、Agent 目录结构或命令行，也能完成核心管理。
2. Skill、Agent、项目、来源、版本和部署关系清楚可追溯。
3. 批量效率与高风险逐项确认并存。
4. 文件已经放入目标目录，不等于 Agent 已加载、调用或验证可用；界面不得制造此类结论。
5. 所有页面使用同一份状态和关系数据，不在概览、列表或详情中维护第二套结果。
6. 功能面向 100～500 个 Skill 和数量更多的本地项目，默认使用高密度信息结构。

---

## 2. 信息架构与导航

### 2.1 固定左侧导航

核心导航从上到下为：

1. 概览
2. 技能库
3. 发现
4. Agent
5. 项目
6. 待处理

左侧底部固定：

1. 操作记录与恢复
2. 设置

“导入”不是独立导航项，而是技能库与发现页面中的主操作。所有入口支持带上下文跳转；从概览或待处理进入对象列表时自动应用对应筛选，返回时保留原位置和筛选条件。

### 2.2 中文术语

- 面向普通用户的中文界面使用“技能”或“技能库”。
- 第一次需要解释时写作“技能（Skill）”。
- `Skill`、`SKILL.md`、Agent 名称、命令、字段、路径和原始错误保留原文。
- 主要操作使用“添加到 Agent/项目”“从 Agent/项目移除”；技术详情中可以显示“部署/解除部署”。

### 2.3 页面层级

所有核心对象采用三层信息结构：

1. 列表或工作台：高密度浏览、筛选和批量操作。
2. 快速抽屉：查看当前对象的重要摘要并执行常用操作。
3. 完整详情页：展示全部信息、依据、关系和历史，是权威信息入口。

---

## 3. 全局交互规则

### 3.1 搜索、筛选与保存视图

- 技能库和项目页都支持组合筛选、排序和保存筛选视图。
- 保存视图只保存条件、排序和列设置，不复制对象。
- Skill 标签和项目标签分别管理；同名标签不会跨对象自动传播。
- 项目采用多标签和保存视图，不建立复杂的多层文件夹树。
- 保存视图、列设置、密度和抽屉配置属于用户偏好，支持恢复默认。

### 3.2 批量选择

- 明确区分“当前页面可见对象”和“全部筛选结果”。
- 选择后显示浮动批量操作区。
- 执行前汇总可执行、跳过、冲突和需要逐项处理的对象。
- 高风险对象不得隐藏在一个总确认中。
- 部分失败必须分别显示成功、跳过、失败和仍待处理项。

### 3.3 状态表达

生命周期、部署、版本、基础安全检查、AI 安全检查、所有权和待处理问题分开显示。

- 不生成单一健康分。
- “未知”“无数据”“未检查”不显示为失败或零。
- “弃用”不等于版本过旧。
- 状态使用文字和图标，不只依赖颜色。
- 列表只显示重要摘要，详情展示原因、影响、证据和操作入口。

### 3.4 风险与确认

- 只读操作直接执行。
- 低风险且可撤销的操作可以完成后提供撤销。
- 影响版本或部署的操作先展示变化摘要。
- 删除、覆盖、强制导入、高风险部署和批量破坏性操作必须明确预览并确认。
- 技术路径、链接或复制方式默认折叠，普通语言结果优先。

---

## 4. 首次使用与初始化

### 4.1 分步向导

初始化使用可跳过的短流程：

1. 选择集中库方式：新建、使用已有集中库、从备份恢复。
2. 确认集中库位置。
3. 发现 Agent，可跳过。
4. 扫描已有 Skill，可跳过。
5. 导入预览，可全部跳过。
6. 完成摘要。

### 4.2 默认集中库

- 默认根目录为可见的 `~/SkillHub`。
- 默认 Skill 内容目录为 `~/SkillHub/skills`。
- Windows 展示为 `C:\Users\<用户名>\SkillHub\skills`。
- macOS 展示为 `/Users/<用户名>/SkillHub/skills`。
- 正常用户主目录内创建不要求管理员权限。
- 向导展示推荐路径并提供“使用默认位置”和“更改位置”。

推荐目录已经存在时，无论结构是否兼容都必须确认：

- 兼容：展示检测到的 Skill 数量和目录概况，选择使用或更改位置。
- 不兼容：展示冲突内容，选择其他位置或在其中创建新的 Skill 子目录。
- 未确认：不写入、不移动、不导入。

### 4.3 初始化扫描

默认只读扫描：

- 集中库。
- `~/.agents/skills`。
- 已发现 Agent 的已知用户级目录。
- 可访问的内置或插件目录。
- 用户已配置的自定义目录。

不遍历全盘，不自动发现未注册项目。项目级 Skill 只扫描已登记或用户主动选择的项目。扫描结果只进入预览，不自动移动、导入或部署。

### 4.4 跳过行为

- 用户跳过初始化时仍需确认集中库位置。
- 确认后只创建空集中库，其余步骤全部跳过。
- 在集中库确认前关闭，不创建任何内容。
- 初始化完成页展示集中库位置、发现的 Agent、导入数量、跳过项和待处理事项。

---

## 5. 概览

概览采用平衡工作区，不作为操作日志页。

### 5.1 信息范围

- Skill 总数。
- 已注册 Agent 和项目数量。
- 待处理事项。
- 可升级项。
- 冲突。
- 基础与 AI 安全检查问题。
- 失联 Agent、项目或集中库关系。

### 5.2 部署关系柱状图

- 首页使用柱状图展示部署关系数据。
- 支持按 Agent 或项目切换维度。
- 点击柱形进入对应筛选后的技能库或项目列表。
- 图表只汇总真实关系，不根据运行时兼容性或调用情况推断。

### 5.3 明确不展示

- 不展示最近操作时间线。
- 不展示不透明健康分。
- 不维护独立于待处理中心的异常状态。
- 颜色和最终视觉样式不在当前阶段确定。

---

## 6. 技能库

### 6.1 默认列表工作台

默认使用高密度列表，支持：

- 搜索、组合筛选、排序和保存视图。
- 单选、多选和批量操作。
- 列显示、隐藏、顺序和密度调整。
- 列表、卡片、关系矩阵、标签分组和组合视图切换。
- 视图之间共用同一筛选条件和数据。

可配置列包括：

- 名称、显示别名和原始说明。
- 我的用途说明。
- 当前界面语言的描述译文。
- 标签、来源、所有权和生命周期。
- 调用方式。
- Agent 和项目部署摘要。
- 当前版本、可升级和项目固定提示。
- 基础安全检查、AI 安全检查和待处理摘要。

### 6.2 Skill 快速抽屉

行点击打开右侧快速抽屉。固定且不可隐藏：

- 名称、显示别名和说明。
- 主要操作。
- 高风险和待处理摘要。
- 完整详情入口。

可显示、隐藏和调整顺序：

- Agent 与项目关系。
- 版本与更新。
- 来源与许可证。
- 基础与 AI 安全检查。
- 调用方式、运行要求和声明信息。
- 依赖与语义重复。
- 外部变更。
- 有可靠数据时的使用证据摘要。

抽屉配置按用户全局保存，不针对每个 Skill 保存不同布局，并支持恢复默认。

### 6.3 Skill 完整详情

完整详情覆盖：

- 基本信息、显示别名、我的用途说明、标签和备注。
- 原始描述、描述译文和查看原文。
- 身份、来源、作者、许可证和版权。
- 版本时间线、差异、回滚和上游更新。
- Agent、项目和目录关系。
- 调用方式、运行要求和声明适用信息。
- 基础安全检查和 AI 安全检查。
- 确定性重复、语义重复、相关 Skill、组合和依赖。
- 外部变更与操作历史。
- 只有存在可靠数据时才显示使用证据摘要。

支持连续切换相邻 Skill，并保留列表位置和筛选条件。

### 6.4 个人用途说明与描述翻译

“我的用途说明”属于本地管理信息：

- 用户自行填写 Skill 的用途、适用场景和区分说明。
- 支持搜索、单个编辑和批量填写。
- 不写入 `SKILL.md`，不影响 Agent。
- 随完整备份迁移，标准导出默认排除。

描述翻译：

- 支持单个和批量调用当前 LLM 翻译原始描述。
- 默认只发送描述文本，并在调用前说明范围。
- 保存目标语言、模型、时间和对应版本。
- 原始描述变化后旧译文标记为需要更新。
- 用户编辑后的译文标记为“用户修订”，重新翻译前确认覆盖。
- 未配置 LLM 或失败时显示原文，不影响其他功能。

### 6.5 标签与组合

- Skill 支持预置和自定义标签、批量添加与移除、标签分组视图。
- 标签不替代生命周期、安全、版本或部署状态。
- 组合位于技能库“组合”视图，不增加主导航。
- 组合只保存明确 Skill 清单和组合说明。
- 支持创建、编辑、复制、删除、导出及添加到单个或多个 Agent/项目。
- 不支持组合嵌套、动态标签表达式、预绑定目标或版本锁定。
- 删除组合不删除 Skill，也不解除已完成部署。

### 6.6 轻量试用管理

轻量试用采用特殊标签和到期复核，不创建临时目录：

1. 新 Skill 完成正常导入和检查后添加“临时试用”；已有 Skill 可直接添加。
2. 设置复核日期，默认建议 7 天并允许调整。
3. 到期后在待处理中心生成“试用待确认”。
4. 支持转正、延长试用、调整部署和放弃试用。

转正只移除标签和待处理事项；Skill 从导入时已经属于集中库。放弃试用复用归档、解除部署和安全删除流程。试用不表示隔离、已调用、验证通过或验证失败，也不改变更新、安全和部署规则。

---

## 7. 发现与导入

### 7.1 三类查找行为

- 技能库搜索：搜索已经纳入集中库的 Skill，包括正文和管理信息。
- 本机发现：发现 Agent、项目和自定义目录中的外部或关系异常 Skill。
- 联网发现：从 `skills.sh` 等适配来源搜索新的 Skill。

“发现”页面使用“本机”和“联网”两个页签。没有配置 LLM 时联网基础搜索仍可使用；LLM 只可选用于扩展查询和辅助排序。

### 7.2 本机发现

- 展示上次扫描时间和扫描范围。
- 提供重新扫描和选择文件夹。
- 结果分为未纳入管理、已有关联、重复或冲突、疑似 Skill、无法读取。
- 展示位置、来源推测、所有权、内容一致性和命中依据。
- 支持单个或批量进入统一导入流程。

### 7.3 联网发现

- 首个内置来源为 `skills.sh`。
- 展示来源能可靠提供的名称、说明、作者、许可证和热度；缺失数据不显示为零。
- 查看打开来源详情，导入进入统一流程。
- `find-skills` 不作为核心依赖，也不调用 Agent 代替 SkillHub 搜索。

### 7.4 来源输入

统一导入支持：

- 本地文件夹。
- 压缩包。
- 仓库链接。
- Skill 页面链接。
- 已连接的私有来源。
- 本机或联网发现结果。
- 粘贴 `npx skills add ...` 安装命令。

安装命令只解析来源、Skill 选择和目标提示，不执行 Shell，不要求本机安装 Node.js、npm、npx 或 Git。包含管道、重定向、命令连接、其他脚本或未知命令时拒绝解析。

`skills.sh` 链接、仓库目录、直接 `SKILL.md` 和受支持压缩包使用来源适配器；普通网页只有在能确定提取出支持来源时才继续，不执行网页上的安装脚本。

一个来源含多个 Skill 时，先展示清单供用户选择单个、多个或全部；命令中的 `--skill` 只负责默认勾选。

### 7.5 统一导入流程

所有来源进入同一流程：

1. 选择来源。
2. 获取文件。
3. 选择一个或多个 Skill。
4. 自动执行格式、确定性重复和基础安全检查。
5. 按配置选择 AI 安全检查和语义重复分析。
6. 展示导入预览。
7. 处理冲突和所有权方式。
8. 确认导入。
9. 展示成功、跳过、失败和仍待处理项。

预览分组：

- 可直接导入。
- 需要处理。
- 无法导入。

AI 检查默认由用户发起，可在设置中启用导入时自动执行。批量执行前展示数量、文件范围和模型；跳过或失败不影响基础导入能力。

### 7.6 本地导入方式

- 已识别 Agent/项目目录中的用户自有 Skill：优先推荐“纳入管理”，集中库保存内容，原位置成为受管部署关系。
- 普通自有目录：推荐复制并保留原件，也可移入集中库或仅扫描。
- Agent 内置、插件或所有权不明：只能复制，不能移动、替换或接管。
- 保留的外部原件显示为来源副本关系，不作为第二个集中库 Skill。

### 7.7 重复与冲突

- 完全相同：复用已有 Skill、建立来源关联、保留独立副本或跳过。
- 同名不同内容：允许以内部唯一身份在集中库共存，不静默覆盖。
- 不同名语义重复：有 LLM 时展示共同能力、独有能力、包含关系和保留建议，最终由用户决定。
- 内置和插件 Skill 只读；可以强制复制为集中库独立对象，不能覆盖原件。
- 强制导入只表示允许重复对象进入集中库，不表示覆盖 Agent 目标。
- 同一 Agent 的同一层级只能部署一个运行时同名 Skill；需要共存时必须实际重命名独立副本。

---

## 8. Agent 管理

### 8.1 列表与客户端模型

- Agent 按品牌分组，CLI、桌面端和 IDE 插件等实际客户端分别识别。
- 展示已发现客户端、仅发现相关目录、当前不可访问和自定义 Agent 等确定性状态。
- 不展示 Agent 版本、登录、项目授权、模型能力或“Skill 可用”结论。
- 显示全局 Skill、受管部署、外部 Skill 和待处理数量。
- 支持单选或多选 Agent 后批量添加 Skill。

聊天桌面端、Web、移动端、Office/办公插件或云端 Agent 只有公开稳定本地目录时才是文件部署目标。ChatGPT desktop app、Claude Desktop 等没有公开稳定目录的客户端仍保留独立 profile，但只展示能力边界、官方帮助链接和通用标准导出，不建立部署或人工安装状态。

### 8.2 Agent 详情

详情包括：

1. 概况与目录：实际客户端、全局与项目目录、规则依据、重新扫描、重新定位、忽略和自定义目录。
2. 全局 Skill：集中库部署、外部安装、内置和插件 Skill，支持添加、移除、复制或接管允许内容。
3. 关联项目：项目级 Skill 和装配入口。
4. 使用证据分析：实验功能。

### 8.3 使用证据分析

- 模块固定显示“实验功能”和“仅供参考”。
- 默认分析 60 天，允许调整时间范围。
- 展示数据来源、统计范围和覆盖程度。
- 数据充分时才提供保留全局、考虑移入项目或考虑归档的建议。
- 没有可靠数据时显示“暂无可用使用数据”，不显示零调用，不生成建议。
- 建议不自动执行任何移动、归档或删除。
- 关联 Runtime Hook 项目标注“研发中”，当前不提供安装、连接或启用按钮。

---

## 9. 项目管理

### 9.1 高密度项目工作台

项目数量可能明显多于 Agent，默认使用高密度列表工作台：

- 搜索名称、路径、Agent、Skill 和管理信息。
- 收藏、多标签、自定义保存视图和批量操作。
- 固定快捷视图包括全部、收藏、待处理、失联、有更新等。
- 项目与 Agent 关系矩阵作为辅助视图。
- 不建立复杂多层文件夹树；一个项目可以拥有多个标签并出现在多个保存视图中。

项目行展示名称、路径、关联 Agent、Skill 数量、装配进度和重要状态摘要。

### 9.2 项目快速抽屉

固定且不可隐藏：

- 项目名称、路径和访问状态。
- 主要操作。
- 高风险与待处理摘要。
- 完整详情入口。

可显示、隐藏和排序：

- 关联 Agent。
- Skill 数量与装配进度。
- 固定版本和可更新项。
- 共享技能清单。
- 目录与部署关系。
- 外部变更。
- 最近一次重要操作。

项目抽屉与 Skill 抽屉分别配置，按用户全局保存，不为每个项目保存独立布局。

### 9.3 注册项目

1. 用户选择项目根目录。
2. SkillHub 根据目录痕迹和本机已发现客户端列出候选 Agent。
3. 用户确认一个或多个关联 Agent。
4. 扫描项目级 Skill 和已有共享技能清单并预览。
5. 注册只建立管理记录，不自动导入、不写项目文件、不添加 Skill。

### 9.4 项目详情

详情包含：

- 概况：路径、访问状态、关联 Agent、子目录范围和待处理事项。
- 项目 Skill：集中库部署、项目自有、外部、内置和插件 Skill。
- 装配：将复用、获取、添加、跳过、冲突和失败的对象。
- 共享技能清单。
- Agent 与目录关系。

项目移动或失联时提供重新关联、暂时忽略或移除管理关系；绝不删除项目业务目录。

### 9.5 共享技能清单

“共享项目配置”在界面中称为“共享技能清单”，默认关闭。启用时明确提示会向项目写入配置文件。

清单保存：

- 所需 Skill。
- 精确版本和来源。
- 目标 Agent。
- 适用于整个项目或特定子目录的逻辑范围。

不保存：

- 本机绝对路径。
- 凭证。
- 使用证据和操作历史。
- 本机软链接或复制方式。

新设备或新成员取得项目后，SkillHub 读取清单并提示尽力装配；不会自动下载或部署。没有启用时，项目关系只保存在本机 SkillHub 中。

### 9.6 尽力装配

- 优先复用集中库已有精确版本。
- 缺少版本时尝试从已知来源获取。
- 来源、版本或认证失败时逐项说明，不替换为未声明版本。
- 装配前预览新增、复用、跳过、冲突和失败。
- 装配完成只表示文件与管理关系已建立，不表示 Agent 已加载或能够调用。
- 不主动检查或安装 Python、ffmpeg、MCP、插件等运行依赖。

---

## 10. 添加到与移除 Agent/项目

### 10.1 添加流程

可以从技能库、Skill 详情、Agent 详情或项目详情发起，进入同一流程：

1. 选择单个、多个 Skill 或组合。
2. 选择单个、多个或全部已注册且支持文件部署的 Agent，或者选择项目与关联 Agent。
3. 选择版本；Agent 全局默认跟随集中库当前版本，项目可以固定精确版本。
4. 执行路径安全、同名冲突、基础安全、目标可访问性和共享物理目录影响检查。
5. 展示目标、版本、目录类型、实际文件方式和变化摘要。
6. 用户确认后执行。
7. 结果区分成功、跳过、冲突、失败和待处理。

SkillHub 不使用“所有兼容 Agent”，因为无法判断 Skill 运行兼容性。

### 10.2 目标目录与文件方式

- 多个 Agent 分别部署到各客户端自己的已知目录。
- `~/.agents/skills` 只在 Agent 官方支持、用户明确选择或登记为自定义目录时使用，不是通用默认目标。
- 多个逻辑 Agent 指向同一物理目录时只写一次文件，分别保留逻辑关系。
- 默认优先软链接；不可用时使用受管复制兜底。
- 普通界面不要求用户理解底层方式，技术详情可以展开实际路径和方式。

### 10.3 同名目标冲突

- 同一 Agent 的同一目标层级只能添加一个运行时同名 Skill。
- 可以选择使用现有内容、移除可管理的现有内容、改用其他目标、复制并实际重命名、只保留在集中库或取消。
- 界面别名不能绕过运行时同名冲突。
- 全局和项目层级同名时按对应 Agent 已知规则展示优先候选、较低优先级副本、可能共存或无法判断。

### 10.4 从目标移除

Agent 独占目录：

- 删除受管文件夹或仅删除链接，保留集中库。
- 链接删除不得沿链接删除集中库内容。
- 内容被外部修改时，先选择收集修改、保留独立副本、明确放弃修改并移除或取消。
- 出现非受管文件时不删除整个目录。

共享物理目录：

- 保留共享部署并取消操作。
- 从所有受影响 Agent 移除。
- 可能时把其他 Agent 迁移到各自独占目录，再移除共享文件。
- 仅解除管理关系并保留文件时明确提示 Agent 仍可能继续发现，不能显示为已从 Agent 移除。

---

## 11. 版本、更新与外部变化

### 11.1 版本与回滚

- Skill 详情使用版本时间线。
- 支持任意两个版本比较。
- 回滚以旧内容创建新的当前版本，不删除后续历史。
- 回滚前展示受影响的未固定部署；项目固定版本保持不变。
- 回滚后重新执行基础安全检查。

### 11.2 上游更新

1. 显示可更新、已是最新、无来源、来源不可访问、本地已修改或存在冲突。
2. 获取候选版本后展示文件、来源身份、许可证、格式和基础安全差异。
3. 有本地修改时进行原始基线、本地版本和上游版本三方比较。
4. 无冲突内容可以合并；冲突必须由用户处理或交给外部编辑器，不使用 LLM 自动语义重写。
5. 升级后形成新版本并更新未固定受管部署；项目固定版本不变。
6. 高风险、来源变化、合并冲突或获取失败阻止自动升级并进入待处理。

### 11.3 升级策略

- 全局默认自动检查并通知，不自动升级。
- 单个 Skill 可以覆盖全局，支持批量修改。
- 项目固定高于单 Skill 和全局策略。
- 自动升级前创建恢复点。

### 11.4 重命名与来源重新关联

- 所有 Skill 可以设置显示别名。
- 只有用户创建或完全独立 Skill 可以实际重命名。
- 上游跟踪 Skill 必须先复制或派生。
- 实际重命名前展示 Agent、项目、组合和依赖影响，并更新用户确认的 SkillHub 管理引用。
- 来源重新关联先比较当前内容与候选来源，保留旧来源历史，不自动认定相似内容属于同一来源。

### 11.5 外部变更

集中库被修改：比较后选择接受为新版本、恢复、暂缓或精确忽略。

受管部署副本被修改：用户自有 Skill 可收集回集中库形成新版本，也可恢复目标、转为独立副本、解除部署或暂缓。非自有对象不提供收集或覆盖。

文件删除、移动或损坏：提供重新定位、从集中库恢复、解除失效关系或暂时忽略。未处理前暂停相关自动升级和传播，不自动把目标修改同步回集中库。

---

## 12. 调用方式与运行要求

### 12.1 调用方式

- 按 Agent 展示“自动和手动均可”“仅手动”“仅自动或对用户隐藏”。
- 只在平台有确定文件字段时提供修改。
- 常规修改只支持前两种之间切换。
- 平台不支持修改时显示“不支持”。
- 修改前预览文件和受影响目标；修改 Agent 专用变体后形成新版本。
- 不使用“已启用”“一定会自动调用”等无法确定的表述。

### 12.2 运行要求

只展示可追溯信息：

- 已知元数据、标准依赖文件或明确列表中的原作者声明。
- 固定规则识别的命令、环境变量和依赖文件引用线索，并展示文件和位置。
- 用户本地备注，与原始声明分开。

不使用 LLM 推断依赖。检测到 `ffmpeg`、`python` 或环境变量引用，只能显示“发现引用”，不能判断已经安装、版本满足或一定依赖。未发现时显示“未发现明确声明”，不能显示为“没有依赖”。

SkillHub 不主动验证或安装运行依赖，也不生成兼容、部分兼容或不兼容状态。

---

## 13. 安全检查

### 13.1 两项独立结果

基础安全检查与 AI 安全检查是两个独立功能，各自显示：

- 未检查。
- 检查通过。
- 发现问题。
- 检查失败。

运行时临时显示“检查中”。不生成第三个综合结果或安全分数。

### 13.2 明细

每条发现展示：

- 风险类别和级别。
- 文件和位置。
- 固定规则或 AI 判断依据。
- 必要证据，敏感值默认遮盖。
- 影响说明。
- 待处理、确认是预期内容或忽略为误报等处理结果。

全部发现处理完成后，对应检查结果联动为检查通过，明细历史保留。内容、规则、模型或相关范围变化后，对应结果恢复为未检查。

两项检查发现同一文件、位置和实质问题时关联展示，用户处理一次后同步更新；只是相关但证据不同的问题分别处理。

### 13.3 AI 检查边界

- 未配置 LLM 显示“AI 检查未配置”，不属于检查失败。
- 在线调用前展示发送文件范围。
- 基础检查先运行，用于遮盖已识别敏感值并提供规则上下文。
- AI 不评价写作质量、不验证功能、不判断 Agent 兼容性，也不自动修改 Skill。
- 高风险项必须逐项确认。

---

## 14. 待处理中心

使用高密度问题列表，固定快捷视图包括：

- 全部待处理。
- 阻止操作。
- 高风险安全问题。
- 外部变更。
- 冲突。
- Agent/项目失联。
- 失败操作。
- 临时试用待确认。
- 已暂缓。
- 已忽略与处理历史。

每条事项展示问题类型、风险、涉及对象、原因、影响、操作、产生时间和最近检查时间。

- 默认先显示阻止当前操作和高风险事项，再按时间排序。
- 支持按类型、Skill、Agent、项目、风险和来源筛选并保存视图。
- 相同安全操作可以批量处理，高风险仍逐项确认。
- 暂缓只在 SkillHub 中延后，不创建系统后台提醒。
- 忽略绑定精确对象或问题，不支持通配规则。
- 解决后自动移出待处理并进入历史。
- 不支持创建普通待办，不扩展为任务管理系统。

---

## 15. 删除、健康检查与恢复

### 15.1 删除 Skill

删除前重新扫描：

- Agent 和项目部署。
- 项目配置与固定版本。
- 组合。
- 声明依赖和相关 Skill。
- 可发现但用途未知的外部引用。

优先提供归档。用户坚持删除时，按关系选择移除部署、转为独立副本、替换或移除引用、或者取消。未知和非自有内容只提示，不修改。不级联删除其他 Skill。操作前创建恢复点；任何必要关系处理失败时不删除集中库原件。

### 15.2 集中库健康检查

设置页提供完整健康检查，待处理中心承接发现：

- 检查缺失、损坏、无效来源、失效部署、固定关系、未完成操作、孤立记录/文件、失联目标、路径重叠和异常链接。
- 结果分为可自动修复、需要选择、仅提示和检查失败。
- 修复前预览并创建恢复点。
- 只有明确安全的问题可批量修复；未知文件不自动删除。
- 桌面端和 CLI 使用同一规则。
- 不生成独立系统诊断报告，不调用 LLM 做健康判断。

### 15.3 操作记录与恢复

页面包含两个页签。

操作记录：

- 记录导入、接管、添加/移除、升级、回滚、重命名、删除、恢复和装配。
- 时间列表展示时间、操作、对象、范围和结果。
- 结果区分成功、部分成功、失败和已撤销。
- 展开显示成功、跳过、失败和待处理明细。
- 满足条件时提供撤销；不能撤销时说明原因。
- 首页不展示最近操作时间线。

备份与恢复：

- 展示手动备份和重要操作前的自动恢复点。
- 显示时间、原因、范围、大小和敏感内容标记。
- 支持创建完整备份、从文件恢复、换机导入和删除过期备份。
- 恢复先在临时区域验证，恢复前备份当前状态，冲突选择使用备份内容、保留两份或跳过。
- 失败时当前集中库保持不变。
- 换机后重新映射 Agent/项目逻辑目标，由用户确认后再部署。

单 Skill 内容回滚继续位于 Skill 详情，不与整次操作撤销和完整备份恢复混在一起。

---

## 16. 标准导出与卸载前处理

### 16.1 标准导出

- 从技能库批量操作、Skill 详情或组合详情进入。
- 导出标准文件夹或通用 ZIP，可选择当前或历史版本。
- 保留来源、作者、许可证和版权；本地修改明确标记。
- 默认排除个人备注、显示别名、使用证据、操作历史、本机路径、部署关系和私有设置。
- 运行要求与声明信息摘要可以选择附带。
- 疑似真实凭证触发暂停，可先处理、排除对象或明确强制导出。
- 不生成 ChatGPT、Claude Desktop 或其他平台专用导入包，不适配平台上传格式，不提供发布通道。

### 16.2 退出管理与卸载

设置中提供：

- 仅卸载应用，保留集中库、项目配置和现有部署。
- 把受管部署转换为独立副本。
- 从选定或全部目标解除部署。
- 先创建完整备份再退出。

默认不删除集中库或用户创建的 Skill。直接使用系统卸载也默认保留集中库和管理数据，重新安装后可以重新关联。不提供一键清空所有文件。

---

## 17. 设置

设置使用左侧分类，不设置普通模式与开发者模式。

### 17.1 通用

- 界面语言：跟随系统、简体中文、英文。
- AI 输出语言独立设置。
- 主题：跟随系统、浅色、深色。
- 启动、关闭和减少动态效果等基础偏好。

### 17.2 集中库与扫描

- 当前路径、打开目录和健康状态。
- 迁移集中库；不能直接修改路径文本，必须进入备份、路径检查、迁移和关系修复流程。
- 自定义扫描目录和忽略项。
- 重新运行初始化向导。

### 17.3 界面与视图

- 技能库和项目默认视图。
- 列、密度、保存筛选视图。
- Skill 和项目快速抽屉模块配置。
- 恢复默认布局。

### 17.4 更新与自动化

- 全局自动检查与自动升级分别设置。
- 展示单 Skill 覆盖和项目固定数量并跳转管理。
- 更新检查、外部变更和备份自动操作只在应用运行期间执行。

### 17.5 AI、网络与来源

- 配置一个当前默认 LLM，并测试连接。
- 分别设置 AI 安全检查、语义重复、描述翻译和联网搜索辅助。
- 展示每项功能发送的数据范围。
- 管理私有来源连接，凭证进入系统安全存储。
- “关闭所有网络功能”停用联网搜索、来源获取、更新检查和在线 LLM，本地管理保持可用。

### 17.6 备份与数据

- 自动备份保留数量或期限、位置和占用空间。
- 进入完整备份、恢复和迁移。
- 标准导出。
- 清理明确可删除的缓存，不触碰集中库 Skill。
- 退出 SkillHub 管理/卸载前处理。
- 不提供危险的一键清空数据。

Agent 目录、自定义 Agent 和项目关系仍在 Agent/项目页面管理，设置页不重复维护。

---

## 18. 轻量 CLI

- 随桌面应用提供，不要求 Git、Node.js 或其他开发环境。
- 普通用户可以忽略；设置页只提供说明入口，不设置开发者模式。
- 支持列表、状态、扫描、项目尽力装配、添加/移除、对齐、升级、健康检查和 JSON 输出。
- 与桌面端使用同一集中库、规则、互斥保护和操作历史。
- 高风险或破坏性操作必须显式确认；非交互环境缺少确认参数时停止。
- 不执行 Skill 命令、任意脚本，不启动 Agent 或管理 Agent 会话。
- 不要求所有界面偏好都提供 CLI 设置。

---

## 19. 桌面可用性与国际化

- Windows 与 macOS 支持系统缩放和常见高分屏。
- 窗口可调整大小，小窗口仍可完成核心操作。
- 主要查看、筛选、选择、确认和取消支持键盘。
- 键盘焦点清晰，控件具有辅助功能名称。
- 状态不只依赖颜色。
- 跟随系统浅色/深色，也可手动切换。
- 系统减少动态效果时减少非必要动画。
- Windows 与 macOS 快捷键遵循各自习惯。
- 原生 i18n 外置中英文文案；语言切换不修改 Skill、Agent、命令、路径或原始错误。

---

## 20. 当前明确延后

以下内容不在本轮产品与交互确认范围：

- 品牌视觉、颜色、图标、字号和间距。
- 具体默认列宽、卡片尺寸、抽屉宽度和按钮位置。
- 桌面与 CLI 技术栈。
- 集中库内部目录编码、数据库和索引格式。
- 版本历史底层实现。
- 各平台软链接、目录联接和受管复制的最终判定机制。
- 文件监听、任务调度和并发实现。
- Agent 真机运行验证；开发完成后按调研文档执行。
- 独立 Runtime Hook 项目的实现。

这些延后项不得反向删除或改变已经确认的功能范围与用户流程。
