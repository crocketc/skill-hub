# SkillHub Release 人工复验用例

> 适用产物：2026-09-20 新版 `target/release/skillhub-desktop.exe`
>
> 使用方式：按用例顺序执行，在每条用例的“结果/证据”处记录通过、失败、截图路径和异常说明。
>
> 本文件只覆盖当前仍需要 release 真机复验的项目。DEV-25～DEV-50 中已经确认存在代码问题的项目，等待统一开发后再复验；DEV-50 已决定移除 `~/.agents lock` 模块。

## 一、测试素材与准备

### 1. 准-B：制造冲突组

你提供的两个路径可以使用，前提是两个来源都已在「发现 → 本机发现」中启用：

```text
%USERPROFILE%\.claude\skills\pm-meeting-transcript
%USERPROFILE%\.agents\skills\pm-meeting-transcript
```

准备步骤：

1. 保留 `%USERPROFILE%\.claude\skills\pm-meeting-transcript` 作为其中一个版本。
2. 将它复制到 `%USERPROFILE%\.agents\skills\pm-meeting-transcript`，保持文件夹名称完全相同。
3. 只修改第二份 `SKILL.md`，例如增加一行：

   ```md
   <!-- conflict test -->
   ```

4. 启动 SkillHub，进入「发现 → 本机发现 → 重新扫描」。
5. 确认扫描结果中出现两个来源相同名称、内容不同的 `pm-meeting-transcript`。

不要把副本放入集中库，也不要改名为 `pm-meeting-transcript-copy`，否则通常不会形成“同名不同内容”冲突组。

### 2. 准-A：制造部署失败/目标占用

在 Claude Code 的实际 Skill 目录中创建：

```text
%USERPROFILE%\.claude\skills\find-skills\说明.txt
```

如果该目录已经存在，确认其中有外来文件即可，不要覆盖原文件。

复原方式：完成相关用例后删除本次新增的 `find-skills` 外来目录/文件，并重新扫描确认现场恢复。

### 3. 每条用例的记录要求

- 记录实际使用的窗口尺寸。
- 涉及写入、部署、删除、冲突决定的用例，记录操作前后现场。
- 截图至少包含：页面状态、顶栏任务/通知、操作结果或错误文案。
- 失败时记录复现步骤、预期、实际表现和是否影响后续用例。

## 二、核心操作链路

### 用例 1：正常导入与重复导入

覆盖：SR-3

操作：

1. 进入「发现 → 本机发现」。
2. 选择一个候选 Skill，提交导入。
3. 等待顶栏任务完成。
4. 查看技能库和操作记录。
5. 对同一候选再次提交导入。

通过标准：

- 第一次只生成一个导入操作事实。
- 顶栏任务、操作记录、技能库状态一致。
- 原始 Skill 文件仍保留在来源目录。
- 第二次不会重复建立冲突组或重复生成无意义版本。

结果/证据：

```text
结果：
窗口尺寸：
截图/日志：
备注：
```

### 用例 2：冲突人工决定不会被 AI 覆盖

覆盖：SR-4、准-B

前置：完成准-B，并重新扫描出 `pm-meeting-transcript` 冲突组。

操作：

1. 进入「关系 → 冲突处理」。
2. 打开 `pm-meeting-transcript` 冲突组。
3. 选择「保留」或「替换」并保存。
4. 再对同一冲突组运行一次 AI 检查。
5. 返回冲突详情，检查人工决定。
6. 如有「跳过」操作，再验证跳过不会修改已有决定。

通过标准：

- 人工决定成功保存。
- AI 检查完成后，人工决定不被覆盖。
- 「跳过」不修改既有决定。
- 冲突状态、操作记录、详情内容一致。

结果/证据：

```text
结果：
冲突组名称：pm-meeting-transcript
人工决定：
AI 检查后状态：
截图/日志：
备注：
```

### 用例 3：取消 AI 长流程

覆盖：SR-5、DEV-61

前置：准备一个已入库且可查看安全检查结果的 Skill，并确认顶栏任务和操作记录可见。

操作：

1. 从技能库打开该 Skill 的快捷抽屉，检查安全检查区是否同时提供基础安全检查和 AI 安全检查入口。
2. 打开同一 Skill 的完整详情页安全区，确认详情页包含抽屉已有的内容和操作。
3. 分别启动基础安全检查和 AI 安全检查，观察运行中状态。
4. 在 AI 检查运行期间点击「取消检查」。
5. 查看顶栏任务、操作记录、抽屉和详情页状态。
6. 关闭并重新打开抽屉和详情页，确认取消结果仍一致。

通过标准：

- 抽屉和完整详情页都有基础检查、AI 检查及状态反馈；
- 基础检查与 AI 检查相互独立，AI 不替代基础检查结论；
- AI 长流程可取消，终态为「已取消」，不误报失败；
- 顶栏、操作记录、抽屉、详情页状态一致；
- 运行中、失败、未配置、取消、完成状态和键盘/无障碍操作均清晰。

结果/证据：

```text
结果：未验收/当前版本入口缺失
Skill：
截图/日志：
备注：DEV-61 已登记，完成开发后重跑本流程。
```

### 用例 10：技能库安全检查状态与结果分列

覆盖：DEV-62，并联动复验 DEV-48、DEV-61

前置：准备至少 4 个可查看的 Skill，分别覆盖基础/AI 未运行、检查通过或失败、存在待处理/高风险结果、零结果；若当前数据不足，使用测试夹具或记录缺失状态，不以空数据替代验证。

| 步骤 | 操作 | 预期记录 |
| --- | --- | --- |
| 1 | 打开技能库，查看表头、同一页多条 Skill 和列表滚动 | “安全检查”只显示基础/AI 状态标签；新增“检查结果”列独立显示待处理、高风险等结果，不再在状态列混排统计；行高和列宽稳定 |
| 2 | 依次观察未运行、运行中、通过、失败、未配置及零结果条目 | 状态文案一眼可区分；零结果使用紧凑明确空态；结果列来自真实数据，不能所有条目都因固定值显示 0 |
| 3 | 打开其中一条 Skill 的快捷抽屉，再进入完整详情 | 抽屉/详情仍能看到完整检查结果和可用操作；详情覆盖抽屉已有内容与操作，列表拆列不造成信息丢失 |
| 4 | 在窄窗口、键盘遍历和横向滚动下重复观察 | 两列仍有清晰表头、状态与结果不串列，不出现不可达按钮、异常撑高或技术 ID 主视觉 |

结果/证据：

```text
状态列显示：
结果列显示：
验证的检查状态：
验证的结果组合：
抽屉/详情一致性：
窄窗口/键盘/横向滚动：
截图/日志：
备注（若 pending 仍固定为 0）：
```

### 用例 11：同名 Skill 的集中库保留与部署边界

覆盖：DEV-63，并联动 DEV-18、DEV-57

前置：准备两个文件夹名相同、用途或内容不同的 Skill，分别放入可扫描来源；不要直接修改集中库文件。

| 步骤 | 操作 | 预期记录 |
| --- | --- | --- |
| 1 | 扫描并分别导入两个同名 Skill，冲突处理选择“独立导入” | 集中库保留两个独立实体；用来源、用途、版本或别名区分；不把 UUID 作为主展示；原始 `SKILL.md` 内容不被改写 |
| 2 | 分别将两个 Skill 部署到不同 Agent/项目 | 两个目标均可按各自原名称识别；关系记录能区分 Skill 实体和目标 |
| 3 | 尝试将两个 Skill 都部署到同一个 Agent/项目目标 | 第二个相同有效名称在预览阶段阻断；不覆盖已有文件、不静默改文件夹名，提示换目标、只保留一个或使用部署别名（若已实现） |
| 4 | 若产品提供部署别名，分别选择受管复制与链接/联接模式 | 受管复制时副本文件夹名与 `SKILL.md:name` 一致，原集中库版本不变，关系记录原名/部署名且可回滚；链接/联接不得改写原文件，应明确不支持别名 |
| 5 | 重启并查看集中库、Agent/项目、详情和关系治理 | 同名实体、部署名称、来源和关系仍可理解、可追溯；没有裸 UUID、路径内部前缀或名称错配 |

结果/证据：

```text
集中库同名实体：
不同目标部署：
同目标阻断：
部署别名/模式：
SKILL.md 原名与部署名：
原件保护与回滚：
重启后状态：
截图/日志：
备注：
```

### 用例 4：部署失败、撤销与重启恢复

覆盖：SR-6、SR-10、准-A

前置：**目标目录开始时必须为空且可写**；本用例不能沿用“预览前已放入 `说明.txt`”的准-A 占用状态，否则会在规划期直接进入用例 5，无法验证提交后的失败与恢复。需要在预览成功后、确认提交前制造目标占用。

操作：

1. 确认 Claude Code 目标目录中没有 `find-skills`，保持目标空闲。
2. 在技能库选择 `find-skills`。
3. 点击「添加到 Agent/项目」。
4. 选择 Claude Code，进入预览；确认预览成功且“确认添加”可用。
5. 在不返回重新预览的情况下，从外部在目标位置创建同名 `find-skills` 目录或文件，模拟预览后到提交前的占用竞态，并保留一个可识别的外来文件。
6. 返回 SkillHub 点击「确认添加」，观察失败结果、通知中心、顶栏任务和操作记录。
7. 重启应用，观察是否进入恢复闸门。
8. 检查外部制造的文件/目录是否仍在，确认 SkillHub 没有删除或修改它。

通过标准：

- 失败结果按目标逐条展示。
- 文案说明目标、路径和失败原因；Skill 使用用户可读名称，不展示裸 UUID/SkillId。
- 操作记录能看出具体失败目标。
- 失败结果为「已撤销」或等价的已清理状态。
- 重启后不进入恢复闸门。
- 外来文件没有被删除或修改。
- 通知中心和顶栏不出现 `[object Object]`。

结果/证据：

```text
结果：
失败目标：
操作记录：
重启后恢复状态：
现场文件是否保留：
截图/日志：
备注：
```

### 用例 5：规划期拒绝已占用目标

覆盖：D-11、DEV-18-A

前置：保持准-A 的目标占用状态。

操作：

1. 从技能库对同一 Skill 发起部署。
2. 进入预览页面后停止，不确认提交。
3. 检查页面提示、提交按钮和本地文件/数据库现场。

通过标准：

- 预览阶段直接识别目标已占用。
- 显示用户可理解的原因和处理建议。
- 提交按钮不可用。
- 预览阶段不写盘、不新增部署关系。
- 页面不显示裸 SkillId 或 UUID。

结果/证据：

```text
结果：
预览提示：
提交按钮状态：
是否产生写入：
截图/日志：
备注：
```

本次复验记录（2026-09-20）：该截图属于用例 5。规划期识别占用并禁用提交符合预期；但预览提示将内部 Skill UUID 作为主文案，页面未满足“无裸 SkillId/UUID”，DEV-18-A 未通过，需修复后重验。

用例 4 执行状态（2026-09-20）：**未执行到提交阶段**。此前沿用准-A 的“预览前已占用”准备方式，预览阶段即被规划期占用保护阻断，因此该现场不能作为 SR-6/SR-10 的提交后失败证据；需按“预览成功后再制造占用”的竞态步骤重做。

## 三、部署与呈现一致性

### 用例 6：真实部署、目录联接、计数一致与清理

覆盖：DEV-21-A、DEV-22-A

操作：

1. 选择一个可以安全部署的 Skill。
2. 添加到 Claude Code。
3. 在预览中检查部署模式。
4. 提交部署。
5. 依次查看概览、Agent 页、Skill 详情、关系页。
6. 移除部署并确认现场恢复。

通过标准：

- 计划模式按用户能理解的方式显示「链接部署」或「复制部署」；不把“符号链接/目录联接”等实现类型作为主文案。必要的实际链接类型只放在技术细节中。
- 提交后目标目录确实是链接入口（本机 Windows 可表现为 junction/reparse point）；用户能理解链接是复用集中库内容、复制是生成独立副本并占用额外空间。
- 成功结果、Skill 详情和关系治理均提供明确的“查看部署关系/解除部署并恢复原状”入口；这与失败后的恢复闸门是两条不同链路。
- 概览、Agent 页、Skill 详情、关系页的部署关系计数一致，图表维度显示用户可读的 Agent/项目名称，不显示 `deployment.dimension.agent` 等内部键。
- 移除后目标现场恢复，集中库内容不受损。

结果/证据：

```text
结果：
部署模式：
目标类型：
概览计数：
Agent 页计数：
详情页计数：
关系页计数：
移除后现场：
截图/日志：
备注：
```

### 用例 7：用户字段、关系导航、路径和图谱基本呈现

覆盖：DEV-16-A、DEV-23-A、DEV-5-A、J-REL-2

操作：

1. 在技能抽屉编辑别名、用途、标签并保存。
2. 不重启应用，依次查看技能库列表、抽屉、详情页。
3. 进入「关系」，切换图谱、冲突处理、关系治理。
4. 从概览卡片深链进入关系页。
5. 检查 Agent 页、发现页、导入向导、部署目标、冲突页、治理页的路径。
6. 在图谱中查看节点名称、边文本和详情时间。
7. 在技能库列表中查看多标签、长文本和被截断字段，逐项悬停或用键盘聚焦查看完整值。

通过标准：

- 列表、抽屉、详情三处用户字段立即一致。
- 关系三页均可达，计数与概览一致。
- 概览深链进入正确子页。
- 路径不出现 `\\?\`、`\??\` 等内部前缀，斜杠风格统一。
- 图谱节点使用可读 Skill 名称，不显示裸 UUID。
- 图谱详情时间是可读的本地时间。
- 列表标签按独立标签视觉呈现；列表中可能截断的字段均可通过悬停或键盘聚焦查看完整值。

结果/证据：

```text
结果：部分通过
用户字段：三处字段值一致，通过；标签仍为纯文本，且列表字段未统一提供完整值悬停提示，转 DEV-65。
关系页计数：三页可达，计数与概览一致，通过；页签视觉仍为既有 DEV-33 待修复项。
深链结果：概览深链进入正确子页，通过。
路径检查：未通过，仍发现来源路径带 `\\?\`/`\??\` 等内部前缀；DEV-5-A 未通过，联动 DEV-31。
图谱名称/时间：节点名称和详情时间通过；详情内容框仍显示技术标识，J-REL-2 部分通过，联动 DEV-45。
截图/日志：用户 2026-09-20 用例 7 截图。
备注：
```

### 用例 8：800×600 冲突工作台

覆盖：J-REL-3

前置：准-B 冲突组仍存在，或重新制造冲突组。

操作：

1. 将窗口调整为 `800×600`。
2. 进入「关系 → 冲突处理」。
3. 打开 `pm-meeting-transcript` 冲突组。
4. 查看冲突成员、影响预览和操作区域。
5. 尝试使用鼠标和键盘完成一次可用操作，但不要提交不可逆决定，除非已完成证据记录。

通过标准：

- 冲突成员列表可读。
- 操作按钮可达，不被裁切。
- 长文本可以换行或在内部滚动。
- 页面没有根级横向溢出。
- 键盘可以遍历并操作主要控件。

结果/证据：

```text
结果：无法验收（数据前置阻塞）
窗口尺寸：800×600
成员列表：关系 → 冲突处理显示 0 项，但准-B/现场实际存在冲突，无法打开目标冲突组。
影响预览：
横向溢出：
键盘操作：
截图/日志：
备注：未进入布局与操作判据；这不是 J-REL-3 内容布局失败，而是冲突事实未投影到工作台/计数同步异常。与 DEV-58 的冲突落库、查询失效和复扫同步问题联动；DEV-2 仅覆盖自动化同构冲突素材，不能替代真实冲突工作台复验。
```

## 四、只读展示

### 用例 9：调用方式与运行要求不可编辑

覆盖：DEV-28

操作：

1. 打开技能抽屉。
2. 查看“调用方式/运行要求”。
3. 打开同一 Skill 的完整详情页。
4. 查看对应区域并使用键盘遍历。
5. 尝试点击或聚焦所有相关控件。

通过标准：

- 只展示调用方式徽标/文本和运行要求。
- 没有编辑、切换、保存控件。
- 页面没有暗示可在 SkillHub 中修改 Agent 的调用开关。
- 键盘遍历不会进入不可用的伪编辑控件。

结果/证据：

```text
结果：
抽屉状态：
详情状态：
是否存在编辑控件：
键盘遍历：
截图/日志：
备注：
```

## 五、建议执行顺序

建议按以下顺序执行，失败时先记录，不要直接跳过现场清理：

1. 用例 7：先确认基础导航、字段、路径和图谱素材。
2. 用例 1：正常导入与重复导入。
3. 用例 2：准-B 冲突决定。
4. 用例 3：取消 AI 长流程。
5. 用例 4：准-A 失败与恢复。
6. 用例 5：规划期占用阻断。
7. 用例 6：真实部署与计数。
8. 用例 8：窄窗口冲突工作台。
9. 用例 9：调用方式只读展示。
10. 用例 10：技能库安全检查状态与结果分列。
11. 用例 11：同名 Skill 的集中库保留与部署边界。

## 六、现场清理

- 删除准-A 新增的外来 `find-skills` 文件/目录，重新扫描确认恢复。
- 如果准-B 的 `pm-meeting-transcript` 冲突决定写入了数据库，记录决定结果，不要直接删除集中库数据；后续统一清理测试数据。
- 移除用例 6 创建的部署关系。
- 关闭 SkillHub 后再确认目标目录和原始来源目录没有测试残留。
