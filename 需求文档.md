# SkillHub 需求文档

> 文档状态：功能范围基线已确认
>
> 基线日期：2026-08-21
>
> 当前阶段：功能需求范围和开发优先级已确认；产品与交互设计正在固化；技术设计与实现计划尚未开始
>
> 文档原则：本文是当前唯一有效的需求来源。具体技术机制与界面布局只有在明确标注为“已确认需求”时才构成约束。

---

## 1. 产品背景

### 1.1 问题背景

Agent Skill 通常分散在不同 Agent 的全局目录、项目目录、插件目录和其他工具管理目录中。个人用户常见的管理困难包括：

1. 同一个 Skill 在多个位置重复存在，来源和内容不一致。
2. 无法快速知道某个 Agent 或项目最终会使用哪个 Skill。
3. 升级、自定义修改、回滚和项目版本固定依赖人工处理。
4. Agent 内置、插件安装和用户自行导入的 Skill 容易产生同名或功能重复。
5. 删除或迁移 Skill 时，难以判断哪些 Agent、项目和组合仍在使用。
6. 普通用户不理解 Git、软链接、运行目录等技术概念，也可能没有安装相关工具。
7. 多个平台缺少统一的本地搜索、分类、验证、风险提示、备份和恢复入口。

### 1.2 产品目标

SkillHub 是面向个人用户的本地 Agent Skill 全生命周期管理工具，目标是：

- 使用一个集中库统一保存和管理 Skill。
- 统一查看 Skill、Agent、项目、版本、来源和部署关系。
- 支持发现、导入、创建、分类、部署、升级、验证、回滚、移除、备份和迁移。
- 让普通用户通过桌面界面完成核心操作，不要求理解或安装 Git。
- 对所有重要操作提供影响预览、可操作的失败说明和恢复能力。
- 在不同 Agent 平台能力不一致的情况下，明确展示支持范围，不伪造平台能力。

### 1.3 目标用户

- 使用一个或多个 Agent 的个人用户。
- 需要在多个 Agent 或多个项目之间管理 Skill 的开发者。
- 不熟悉 Git、命令行、软链接和 Agent 目录结构的普通用户。
- 希望本地保存数据，不依赖 SkillHub 账号或远程服务的用户。

---

## 2. 产品范围

### 2.1 产品形态

- 桌面应用是主要产品入口。
- 提供轻量 CLI，用于列表、状态、扫描、项目装配、部署、解除部署、对齐、升级和健康检查等自动化场景。
- CLI 不追求与桌面端完全等价。
- 桌面端与 CLI 应使用相同的功能规则和数据，不形成两套管理逻辑。

### 2.2 操作系统

- 正式支持 Windows 和 macOS。
- Windows 是首要体验与验证基准平台。
- Windows 与 macOS 的核心功能目标一致，但允许因操作系统或 Agent 能力不同而存在明确差异。
- Linux、Web 和移动端当前不纳入支持范围。
- 备份、标准导出和项目配置应支持 Windows 与 macOS 之间迁移。

### 2.3 界面语言

- 产品具备原生国际化能力。
- 内置简体中文和英文两种完整界面语言。
- 首次启动默认跟随系统语言，用户可以手动切换。
- Skill 名称、Agent 名称、命令、字段、文件内容和原始错误保持原文，不因切换界面语言而修改。
- 用户填写的别名、说明和备注不自动翻译。
- LLM 生成内容的语言可以独立于界面语言进行选择。

### 2.4 数据与协作边界

- 产品以个人本地管理为核心。
- 不提供 SkillHub 产品账号、角色权限、远程协作服务器或团队管理后台。
- 项目配置可用于个人多项目管理，也可由项目成员共享，但不扩展为团队管理系统。
- 不默认上传 Skill、项目、使用记录、操作历史或诊断数据。

### 2.5 使用规模

- 面向几十到数百个 Skill 的个人本地管理场景。
- 主要设计规模为 100 至 500 个 Skill。
- 不以企业级大规模资产管理为目标。

---

## 3. 核心概念

| 概念 | 定义 |
|---|---|
| 集中库 | 用户选定的唯一 Skill 管理库，保存 Skill 内容、版本和管理关系。 |
| Skill | 按 Agent Skill 约定组织的技能目录或技能包；文件格式正常不代表 Agent 已加载或能够正确执行。 |
| Agent | 可以加载或调用 Skill 的平台。 |
| 项目 | 用户注册到 SkillHub、需要项目级 Skill 管理的本地项目目录。 |
| 目标 | 某个 Agent 的全局 Skill 位置或某个项目中的 Agent Skill 位置。 |
| 部署 | 将指定 Skill 版本放入指定 Agent 或项目的目标目录并建立管理关系。面向普通用户称为“添加到 Agent/项目”；不代表 Agent 已加载或能够调用。 |
| 解除部署 | 移除 SkillHub 管理的目标关系，不等于删除集中库中的 Skill。 |
| 来源 | Skill 的获取位置和身份信息，例如在线来源、本地目录或用户创建。 |
| 所有权 | SkillHub 对对象允许执行的管理范围。 |
| 当前版本 | 集中库中默认使用的 Skill 版本。 |
| 历史版本 | 为对比、回滚或项目固定而保留的只读版本。 |
| 项目固定 | 项目明确使用某个指定版本，不随集中库当前版本自动变化。 |
| 期望状态 | SkillHub 记录的部署、固定版本、策略和关系。 |
| 实际状态 | 文件系统、Agent 目录和项目目录中观察到的真实内容。 |
| 目录关系 | 根据已知 Agent 目录层级规则推算的优先候选、较低优先级副本、可能共存或无法判断关系。 |
| 组合 | 用户命名的一组明确 Skill，用于批量查看或部署。 |
| 待处理事项 | 需要用户关注或选择处理方式的问题。 |

---

## 4. 全局产品规则

### 4.1 单一集中库

- 一个 SkillHub 实例只管理一个集中库。
- 不提供多个集中库之间的切换、合并或同步。
- 推荐集中库根目录为用户主目录下可见的 `SkillHub` 文件夹，默认 Skill 内容目录为 `~/SkillHub/skills`；Windows 对应 `C:\Users\<用户名>\SkillHub\skills`，macOS 对应 `/Users/<用户名>/SkillHub/skills`。
- 默认路径不使用点目录隐藏；在正常用户主目录下创建不要求管理员权限。初始化时必须展示路径并允许更改。
- 推荐目录已存在时，无论结构是否兼容都必须由用户确认后才能接管，不自动覆盖、清空或写入。
- 用户可以迁移集中库位置。
- 集中库被移动、重命名或暂时不可访问时，应暂停依赖集中库的写操作，并提供选择新位置、重新关联和修复受管关系的入口。
- 集中库可以使用自定义路径，但不能与任何已知 Agent 目录或已注册目标发生包含或重叠关系。
- 检测到路径重叠时必须阻止，不能通过“强制继续”绕过。
- 新增 Agent 或项目目标时，也必须重新检查与集中库的路径关系。
- 集中库中的 Skill 以 SkillHub 内部唯一身份区分，物理目录不能只依赖 Skill 名称；同名、不同来源或不同内容的 Skill 可以在集中库中共存，具体目录编码留到技术设计阶段。
- 同一 Agent 的同一全局或项目目标层级不能部署两个运行时同名 Skill；需要共存时必须将其中一个复制或派生为独立 Skill 并实际重命名。

### 4.2 云同步目录

- 允许用户将集中库放在 OneDrive、iCloud Drive、Dropbox 等云同步目录。
- SkillHub 只按本地文件管理，不提供自己的云同步能力。
- 发现仅在线占位文件时，应暂停相关操作并说明原因。
- 不承诺多台设备同时修改集中库时的结果。
- 云服务产生的冲突副本、修改或删除统一进入外部变更处理流程。

### 4.3 双状态模型

- SkillHub 管理记录是期望状态的依据。
- 文件系统和 Agent 目录是实际状态的证据。
- Agent 内置、插件和其他工具管理的 Skill 属于外部观察状态。
- 两者一致时显示正常。
- 缺失、额外、被修改、关联失效或版本不符时显示状态偏差。
- 扫描范围不完整时显示“覆盖范围未知”，不能据此判定对象不存在。
- 对齐和修复不得静默覆盖实际修改。

### 4.4 所有权保护

所有 Skill 分为：

- 用户创建。
- SkillHub 管理。
- 其他工具管理。
- Agent 内置。
- 插件缓存。
- 未知来源。

统一规则：

- 扫描发现默认只读。
- 用户创建或用户明确确认归自己所有的本地 Skill，可以接管管理。
- Agent 内置、插件、其他工具管理和未知来源对象不得自动移动、覆盖或删除。
- 非自有对象只能在用户确认后复制导入为独立 Skill，原件保持不变。
- 非自有对象在界面中明确显示只读原因。

### 4.5 无静默变更

- 不静默覆盖同名内容。
- 不静默替换版本。
- 不静默修改 Skill 语义。
- 不静默传播外部修改。
- 不静默删除集中库、项目目录或非自有内容。
- 重要写操作必须可追踪，并在适用时可撤销或恢复。

### 4.6 网络与隐私

- 核心本地功能在离线状态下可用。
- 提供统一关闭网络的设置。
- 联网操作必须区分在线搜索、来源获取、更新检查、私有来源访问和 LLM 调用。
- 联网前或联网过程中显示原因与目标来源。
- SkillHub 自己管理的凭证保存在操作系统安全凭据存储中，不进入集中库、项目配置、备份或标准导出包。
- 用户直接写入 Skill 文件的疑似凭证属于文件内容，SkillHub 不静默删除；在备份、导出、共享或发送给云端 LLM 前必须单独检查和提示。
- 不默认上传遥测、崩溃数据、原始会话或使用证据。

### 4.7 技术细节延后

以下内容属于后续设计，不在本阶段确定：

- 集中库的具体记录格式。
- 版本历史的底层实现方式。
- 软链接优先、受管复制兜底已经确认为产品策略；不同操作系统和 Agent 实际采用符号链接、目录联接或受管复制的判定与验证机制留到技术设计和真机测试阶段。
- 桌面技术栈和 CLI 技术栈。
- 数据库、索引、文件监听和任务调度实现。

---

## 5. 功能需求

### 5.1 首次使用与初始化

- 提供可跳过的短流程首次使用引导。
- 首先选择新建集中库、使用已有集中库或从备份恢复；集中库是使用 SkillHub 的必要基础，其余 Agent 发现、扫描和导入步骤均可跳过。
- 使用普通语言解释“集中保存”和“添加到 Agent/项目”，默认不展示 Git、软链接等底层概念。
- 提供已确认的 `~/SkillHub/skills` 推荐位置，也允许用户自定义。
- 推荐位置已存在时，即使结构兼容也必须展示检测结果并由用户确认；结构冲突时提供更改位置或在其中创建新技能子目录，不自动接管。
- 自动进行集中库路径安全检查。
- 自动发现已有的受支持 Agent，并区分“发现客户端”和“仅发现相关目录”，展示判断依据。
- 默认扫描集中库、`~/.agents/skills`、已发现 Agent 的已知用户级目录、可访问的内置或插件目录，以及用户已配置的自定义目录。
- 初始化不遍历全盘，也不自动查找未注册项目中的项目级 Skill。
- 只读扫描已有 Skill，按来源、所有权、重复、格式和风险进行分类。
- 不自动导入、移动或部署，必须先由用户预览和选择。
- 支持使用已有集中库或从备份恢复。
- 用户跳过初始化时仍需确认集中库位置；确认后只创建空集中库。集中库确认前关闭则不写入任何内容。
- 初始化中断后可继续，不留下无法识别的半完成状态。
- 初始化完成后显示已创建内容、发现结果和待处理事项。

### 5.2 Agent 管理

#### 5.2.1 内置支持 Agent

- 内置接入是基于官方资料维护的文件管理 profile，内容包括全局目录、项目目录、共享或兼容目录、层级规则、格式差异、刷新说明和可观察的内置或插件目录。
- 开发阶段可以依据官方资料实现 profile，并标记“基于官方资料、待测试”；开发完成进入测试阶段后再逐个平台进行真机验证。
- 内置接入只承诺文件发现、管理和部署能力，不承诺 Agent 已加载、能够调用或正确执行 Skill。
- 平台规则未知时显示“无法判断”，平台明确不支持的文件管理能力显示“不支持”。
- 内置适配规则可以随 SkillHub 更新而扩展。

#### 5.2.2 候选 Agent

- 保留待调研的候选 Agent 池。
- 候选状态不构成功能支持承诺。
- 候选平台可以先提供文档级 profile；是否标记为已测试支持在开发完成后的测试阶段确认。

#### 5.2.3 自定义 Agent

用户可以创建轻量自定义 Agent：

- 设置显示名称。
- 指定全局 Skill 目录。
- 指定一个或多个项目 Skill 目录。
- 支持扫描、导入、部署、解除部署、外部变更检测和基础关系展示。
- 标记为“未验证自定义目标”。

自定义 Agent 不承诺自动发现、平台格式转换、调用策略映射、运行时可用性判断、调用次数采集或完整插件观察能力。

#### 5.2.4 Agent 专用变体

- 一个 Skill 以基础内容为主体，可以包含明确的 Agent 专用变体。
- 变体用于表达不同 Agent 已知且可验证的格式或字段差异。
- 适配只执行明确规则，不自动改写 Skill 的业务语义。
- 生成或更新变体前显示变化，变体随 Skill 版本保存。
- 具体变体目录、字段和生成机制留到后续设计。

#### 5.2.5 同品牌多客户端

- Agent 在界面中按品牌归组，但 CLI、桌面端和 IDE 插件等实际客户端分别识别。
- 多个客户端使用相同目录和规则时合并为一个实际目标；目录或规则不同时分别管理。
- 聊天桌面端、Web、移动端、Office/办公插件或云端 Agent 只有在官方资料或真机验证确认本地目录时，才作为文件部署目标。
- 只支持上传、账号同步、云端管理、插件市场或连接器的客户端，不套用同品牌 CLI/IDE 的本地目录，也不建立部署或人工安装状态；仅保留客户端能力说明、官方帮助链接和通用标准导出入口。
- 用户可以选择品牌下全部已发现客户端，也可以只选择某个客户端。
- 未发现客户端不假装已经部署；后续新发现客户端时只提示，不自动传播已有部署。
- OpenAI 的 ChatGPT desktop app、Codex CLI、Codex IDE extension，Anthropic 的 Claude Code 与 Claude / Claude Desktop 聊天 Skills，以及 CodeBuddy Code 与 WorkBuddy、TraeCode 与 TraeWork、Kimi Code 与 Kimi Work 等能力和目录不同的产品保持独立。

### 5.3 Agent 环境变化

- 应用运行期间或用户手动扫描时，检测受支持 Agent 客户端或相关目录的新增、卸载、移动和变化。
- 找到应用程序、命令行程序或官方安装记录时显示“已发现”；只找到配置或 Skill 目录时显示“发现相关目录”，不据此判断客户端仍已安装。
- 用户可以手动指定便携版或非标准安装位置；同一客户端存在多个安装位置时由用户确认实际目标。
- 新发现 Agent 时提示注册，不自动部署。
- Agent 不可用时提供重新定位、暂时忽略、安全解除部署或移除管理关系。
- 内置适配规则变化时展示影响范围。
- 不识别或管理 Agent 版本、登录状态、项目可信状态、模型能力和运行时兼容性。
- Agent 升级造成目录变化时按实际文件变化处理，不依赖版本号推断。
- 不静默重写 Skill 内容。

### 5.4 项目管理

- 用户通过选择项目根目录注册项目。
- 项目支持收藏、多个自定义标签和保存筛选视图；标签只用于 SkillHub 内部整理，不创建、移动或重命名本机项目目录，也不建立复杂多层文件夹树。
- 注册项目后根据已知平台目录痕迹和本机已发现客户端列出候选 Agent，由用户确认关联；仅存在 `.agents/skills` 时不能据此推断具体 Agent。
- 项目可关联一个或多个 Agent；多个 Agent 指向同一物理目录时分别记录逻辑关系，但不重复导入或部署。
- 项目记录所需 Skill、精确版本和目标 Agent。
- 项目默认只在 SkillHub 内部注册，不自动向项目目录写文件。
- 用户可以启用共享项目配置，界面名称为“共享技能清单”；配置记录所需 Skill、精确版本、来源、内容身份、目标 Agent 和逻辑适用范围，不保存本机绝对路径、凭证、使用证据和本机部署方式。
- 新设备发现共享配置时提示注册和装配，不自动执行。
- 项目根目录中的平台原生目录和 `.agents/skills` 均按对应 Agent 规则扫描；父级、嵌套和仓库根规则不能被统一成一个固定路径。
- 单仓库中的嵌套 Skill 默认作为同一项目的子目录范围，不自动创建新项目；显示其适用于整个项目或特定子目录。
- 用户可以将子目录单独注册为项目；允许父子项目范围重叠，但同一实体文件只识别一次并分别展示项目关系。
- 用户可以选择一个上级文件夹批量发现含共享项目配置或已知 Agent Skill 目录的候选项目；结果先预览，不读取 Agent 对话或项目历史，不持续监控该上级文件夹。
- 项目移动或失联时标记为不可访问，并提供重新关联、确认可能的新位置、暂时忽略或移除管理关系。
- “从 SkillHub 移除项目”不等于删除项目目录。
- 用户可选择保留项目中的 Skill、解除部署后移除管理关系或取消操作。
- SkillHub 绝不删除项目业务目录。

### 5.5 项目尽力装配

- 根据项目配置装配所需 Skill 和精确版本。
- 新成员或新设备取得项目配置后，可以通过一次引导式操作完成尽力装配，不要求理解 Git 或手工复制目录。
- 优先复用集中库已有版本。
- 缺少版本时尝试从已知来源获取。
- 来源无法访问、版本不存在或认证失败时逐项说明。
- 不使用未声明的替代版本。
- 配置使用逻辑 Agent 和项目范围，每台设备按本机系统和实际目录装配。
- 无在线来源、仅存在于本机的 Skill 可以由用户选择携带指定版本副本；不携带时标记为需要手动提供。
- 携带副本前执行格式、重复和安全检查；副本不包含版本历史、操作记录、使用证据或本机管理信息。
- 已直接保存在项目 Skill 目录中的项目自有 Skill 不再生成重复携带副本。
- 装配前预览将添加、复用、跳过、冲突和失败的对象。
- 装配后显示期望状态与实际状态差异。
- SkillHub 不管理 Agent 项目信任状态；若平台可能要求首次确认，只在部署完成说明和故障排查中提供非阻断提示，不保持“未授权”状态。
- 不主动检查或安装 Python、ffmpeg、MCP、插件等运行依赖。

### 5.6 本地发现与扫描

- 扫描集中库、通用用户级 `~/.agents/skills`、已注册 Agent 的已知全局目录、已注册项目的项目目录、可访问的内置或插件目录，以及用户明确选择的路径。
- 项目扫描按平台规则处理原生目录、`.agents/skills`、父级、嵌套和子目录范围，不假定所有平台目录一致。
- 不遍历全盘。
- 用户可以选择一个明确的上级文件夹有限发现其他 Skill 或候选项目；扫描结束后不持续监控该上级文件夹。
- 展示每个发现对象的位置、名称、来源推测、所有权、内容一致性和关联状态。
- 识别格式正常的 Skill、存在格式问题的 Skill、疑似 Skill、链接、实体副本和无法读取对象。
- 对大小写别名、符号链接、目录联接和相同真实路径去重，保留其多个来源或使用关系。
- 只观察可访问的本地内置和插件 Skill，不运行 Agent 命令、不解析私有数据库，也不把官方静态清单当作当前客户端完整事实。
- 发现无必要文件的目录时标记为“疑似非 Skill”，不自动导入。
- 支持手动重新扫描。
- 支持持久忽略单个精确路径或单个 Skill。
- 忽略规则不支持通配符、表达式和复杂条件。

### 5.7 本地搜索与联网搜索

本地搜索：

- 按名称、显示别名、说明、标签、状态、来源、所有权、Agent、项目和部署状态搜索或筛选。
- 支持搜索 Skill 正文和管理侧元数据。
- 搜索结果显示命中原因或片段。
- 支持组合筛选、排序和批量选择。

联网搜索：

- 与本地搜索明确分开。
- skills.sh 作为首个内置在线搜索来源。
- 未来可以扩展其他明确适配的搜索来源。
- LLM 可用于改写查询、扩展关键词和辅助排序。
- find-skills 不作为核心依赖。
- 不配置 LLM 时仍可使用基础在线搜索。

### 5.8 来源获取

- 支持从本地文件夹、压缩包、仓库链接、Skill 页面链接和受支持私有来源获取。
- 支持粘贴明确的 `npx skills add ...` 安装命令；SkillHub 只解析来源、Skill 选择和目标提示，不执行命令，不要求本机安装 Node.js、npm、npx 或 Git。
- 安装命令中出现管道、重定向、命令连接、其他脚本或未知命令时拒绝解析，不能交给系统 Shell 执行。
- `skills.sh`、仓库目录、直接 `SKILL.md` 和受支持压缩包链接通过来源适配器解析；普通网页只有在能确定提取出受支持来源时才继续，不执行网页安装脚本。
- 来源中包含多个 Skill 时先展示检测清单，允许选择单个、多个或全部；命令中的 `--skill` 只作为默认勾选，不默认导入整个仓库。
- 支持常见私有来源的账户连接和认证失败提示。
- 普通用户不需要安装或理解 Git。
- SkillHub 获取的是 Skill 文件及版本信息。
- 不执行网页、README 或 Skill 内容中提供的任意安装脚本或命令。
- 对暂不支持或只能通过特殊命令获取的来源，提示用户先在外部取得文件再导入。
- 不把 SkillHub 扩展为通用命令执行器。

### 5.9 导入、复制与接管

- 导入前进行基础格式检查：确认入口文件可读、YAML/frontmatter 可解析、明确必填字段存在、名称规则正确，并提示明显缺失的相对引用文件。
- 基础格式检查不执行脚本、命令或代码，不判断功能逻辑、依赖是否安装和 Agent 是否能够调用。
- 格式结果使用“格式正常、存在格式问题、疑似 Skill、无法读取”；存在格式问题的内容可以导入查看和修复，无法读取时不能完成依赖内容读取的操作。
- 支持单个和批量导入。
- 导入前执行重复和冲突检查。
- 格式检查、确定性重复检查和基础安全检查在导入前自动执行；LLM 安全检查和语义重复分析默认由用户在预览中选择，也可以在设置中启用“导入时自动进行 AI 检查”。
- 批量 AI 检查前展示对象数量、发送文件范围和模型；未配置 LLM、调用失败或用户跳过时仍可依据基础检查继续导入。
- 非自有对象采用复制导入，原件保留。
- 本地导入方式根据原位置和所有权推荐：已识别 Agent 或项目目录中的用户自有 Skill 优先推荐“纳入管理”，普通自有目录优先推荐复制并保留原件，非自有或所有权不明内容只能复制。
- “纳入管理”把集中库内容与原 Agent/项目位置建立受管部署关系；界面显示一个 Skill 及其部署关系，不把原位置误显示为第二个集中库 Skill。
- 普通自有目录还可以选择移入集中库或仅扫描不导入；移动必须明确确认且不得破坏未知外部引用。
- 保留的外部原件以来源副本关系展示，不重复创建集中库对象。
- 用户明确拥有的本地 Skill 可选择接管。
- 接管前展示原位置后续处理方式：成为受管部署、保留独立副本、移除原件或取消。
- 未完成接管时不能删除原件。
- 所有导入记录来源、导入时间和内容身份。

### 5.10 创建与外部编辑

- 支持从空白或最小模板创建 Skill。
- 支持复制现有 Skill 为完全独立 Skill。
- 支持从现有 Skill 创建保留上游关系的自定义分支。
- 创建后形成初始版本记录。
- 完整内容编辑交给外部编辑器、skill-creator 或其他专用工具。
- 创建完成后提供打开 Skill 文件夹、打开 `SKILL.md` 和选择本机外部编辑器；SkillHub 只提供基础模板和管理信息编辑。
- SkillHub 不发展为完整代码编辑器或 AI Skill 生成平台。
- 外部编辑后的内容通过外部变更流程处理。

### 5.11 来源重新关联

- 对失去来源或来源地址变化的 Skill，支持手动重新关联。
- 重新关联前比较当前内容与候选来源。
- 关联后保留原来源历史。
- 不自动将相似内容认定为同一来源。
- 不提供多个上游来源镜像或自动切换。

### 5.12 元数据与组织

- 支持显示别名、说明、标签、备注、相关 Skill 和用户自定义信息。
- 提供独立的“我的用途说明/备注”，可在技能库、卡片、快速抽屉和详情中展示并参与搜索；它属于 SkillHub 管理信息，不写入 `SKILL.md`，随完整备份迁移，标准 Skill 导出默认排除。
- 支持使用已配置 LLM 对 Skill 原始描述进行单个或批量翻译；默认只发送描述文本，不发送完整 Skill 内容。
- 翻译作为视图层内容保存，不改写原始描述；记录目标语言、模型、时间和对应版本，界面提供查看原文。
- 原始描述变化后旧译文标记为需要更新，不静默重新翻译。用户编辑后的译文标记为“用户修订”，重新翻译前确认是否覆盖。
- 未配置 LLM 或翻译失败时显示原文，不影响其他功能。
- 支持预置标签和自定义标签。
- 标签支持创建、重命名、删除、单个设置和批量设置。
- 技能库支持保存筛选视图，与项目页采用一致交互，但 Skill 标签和项目标签的数据分别管理。
- 删除标签只移除标签关系，不删除 Skill。
- 支持轻量组合的创建、编辑、复制和删除。
- 组合只保存明确 Skill 列表。
- 组合不支持标签表达式、嵌套、目标绑定和版本锁定。
- 支持查看 Skill 的文件数和体积。
- 体积异常时提示，但不提供独立磁盘清理中心。

### 5.13 生命周期状态

Skill 生命周期只有正常、弃用和归档。

- 弃用可设置推荐替代 Skill。
- 归档默认从日常列表隐藏，但可搜索和恢复。
- “临时试用”是可选的 SkillHub 特殊标签并带到期复核行为，不增加新的生命周期状态。
- 生命周期与安全检查、版本和部署状态相互独立。

### 5.14 来源、作者与许可证

- 按版本保存来源地址、作者、导入时间、许可证和版权文件。
- 许可证变化或缺失时提示。
- 复制、派生和导出时保留来源及许可证。
- 修改过的 Skill 在导出时标明已修改。
- 不提供法律意见或许可证兼容性审计。

### 5.15 重复与冲突检查

检查范围包括当前导入批次、集中库、目标 Agent、Agent 全局与项目层级，以及可发现的内置、插件和其他工具 Skill。

- 内容完全一致时可跳过、去重或建立管理关联。
- 同名但内容不同时必须进入冲突处理，不能静默覆盖。
- 不同名称但功能相似时由可选 LLM 进行语义重复分析，展示共同能力、独有能力、包含关系和关键差异。
- LLM 可以建议保留 A、保留 B、保留两者、归档其中一个或人工选择；建议依据可以包含功能覆盖、权限要求、安全检查、来源状态、本地修改和当前部署关系。
- A 包含 B 不自动代表 A 更好；范围更聚焦、触发更准确或权限更少时，可以建议保留 B 或两者共存。
- LLM 不自动合并、删除、归档、重命名或修改 Skill，最终操作由用户确认。
- 内置 Skill 只读，不能覆盖。
- 重复检查覆盖所有可发现的本地内置、插件和其他工具 Skill；无法访问的 Agent 内部或云端能力不纳入确定结论。
- 集中库可保存同名、不同来源的 Skill，并以来源或内部身份区分。
- 强制导入只允许重复内容以独立身份进入集中库，不代表覆盖 Agent 内置、插件或已有目标内容；后续部署仍需单独处理同名冲突。
- “名称加来源”只用于界面辨识，不自动改变 Agent 运行时名称。

部署同名冲突时提供使用现有内容、移除可管理的现有内容、改用其他目标、复制并实际重命名、仅保留在集中库或取消操作。

### 5.16 版本历史与差异

- 每个 Skill 保存当前版本和历史版本。
- 版本记录时间、来源、变更说明和内容身份。
- 支持任意历史版本之间对比。
- 版本历史只增不减；回滚本身形成新的当前版本。
- 删除后的 Skill 可在恢复能力覆盖范围内找回。
- 底层是否使用 Git 不构成用户功能差异。

### 5.17 单 Skill 回滚范围

回滚恢复与版本绑定的 Skill 文件内容、Agent 特定变体、来源与许可证、运行要求和兼容信息声明，以及该版本生成或保存的内容说明。

回滚不恢复标签、显示别名、生命周期、备注、自动升级策略、部署、项目固定、使用证据和忽略设置。

- 安全检查结果与具体 Skill 版本保持历史关联；回滚后按当前规则重新检查。
- 管理状态变化通过操作历史与整次操作撤销处理。

### 5.18 当前版本与项目固定

- 集中库中的当前版本是未固定部署的默认版本。
- 项目可固定到一个只读历史版本。
- 项目固定优先于全局和单 Skill 自动升级策略。
- 项目解除固定后可选择跟随当前版本。
- 不提供任意多个全局当前版本。

### 5.19 上游更新与本地自定义

- 支持检查单个或多个 Skill 的上游更新。
- 显示可更新、已最新、无来源、来源不可访问、存在本地修改和升级受阻。
- 上游跟踪 Skill 被本地修改后，升级时比较原始基线、本地修改和上游变化。
- 无冲突内容可合并，冲突内容必须人工处理。
- 保留来源关系和自定义历史。
- 不静默覆盖本地修改。
- 不自动进行语义重写。

### 5.20 自动检查与自动升级

- 自动检查更新与自动升级是两个独立设置。
- 默认行为为自动检查并通知，不自动升级。
- 支持全局默认、单个 Skill 覆盖和批量修改。
- 优先级为：项目固定或锁定高于单 Skill 设置，单 Skill 设置高于全局设置。
- 仅在应用运行期间检查和处理，不创建后台常驻服务或托盘进程。
- 升级前创建恢复点。
- 升级形成新内容后，基础安全检查和 LLM 安全检查分别恢复为“未检查”；基础检查按规则自动执行。

来源身份变化、高风险待处理项、合并冲突、版本或来源不存在、认证失败、Skill 弃用或归档、项目固定版本等情况必须阻止自动升级。

### 5.21 Skill 重命名

- 所有 Skill 都可以设置显示别名。
- 只有用户创建或完全独立的 Skill 可以实际重命名。
- 上游跟踪 Skill 必须先复制或派生，才能实际重命名。
- 重命名前展示对项目、组合、部署和关联关系的影响。
- SkillHub 管理的引用可以在用户确认后更新。
- 重命名操作可撤销。

### 5.22 部署

- 支持将单个 Skill、多个 Skill 或轻量组合添加到 Agent 或项目。
- 指定部署版本。
- 未固定部署跟随集中库当前版本。
- 项目固定部署保持指定版本。
- 重复执行同一部署操作应保持结果一致。
- 部署前展示目标、版本、冲突、覆盖范围和将发生的变化。
- “添加到指定 Agent”支持单选、多选和选择全部已注册且支持文件部署的 Agent；SkillHub 不使用“兼容 Agent”表述，不判断 Skill 运行兼容性。
- 选择多个 Agent 时分别部署到各客户端自己的已知 Skill 目录，不把 `~/.agents/skills` 当作所有平台的通用共享目标。
- `~/.agents/skills` 只在对应 Agent 官方支持、用户明确选择或登记为自定义目录时使用。
- 多个逻辑 Agent 实际指向同一物理目录时只写入一次，但分别保留逻辑关系。
- 部署默认优先使用软链接；目标、Agent 或系统不支持时使用受管复制兜底，并在技术详情中说明实际方式。
- 已位于共享物理目录的对象解除某个 Agent 关系时，如其他 Agent 仍依赖该实体，不直接删除共享文件；无法只对一个 Agent 隔离时必须展示全部影响。
- Agent 专用变体内容不一致时分别部署到专属目录，不能共用一份共享部署。
- 部署不等于 Skill 已被 Agent 加载、调用、正确执行或已证明安全。
- 各平台软链接、目录联接和受管复制的可用性与回退机制留到技术设计和真机测试阶段。

### 5.23 Agent 目录关系视图

- 按 Agent 和项目合并展示全局、项目、内置、插件及其他工具 Skill。
- 展示名称、来源层级、版本和所有权。
- 对官方明确的目录层级规则标记“优先候选、较低优先级副本、可能共存或无法判断”。
- 基于尚未实测的官方规则时注明“按官方目录规则推算”。
- 平台规则未知时标记为无法判断。
- 部署前预览部署后的目录关系变化。
- 自定义 Agent 只提示可能共存或冲突，不声称知道优先级。
- 不使用“正在使用、已加载、一定会调用”等运行时表述。

### 5.24 调用策略

统一展示自动加手动、仅手动、仅自动或对用户隐藏三类状态。

- 常规设置支持在“自动加手动”和“仅手动”之间切换。
- 支持恢复 Skill 原始设置。
- “仅自动或隐藏”状态可以识别和展示，但不承诺所有平台都能统一设置。
- 只在受支持 Agent 上提供对应修改入口。
- 平台不支持时显示“不支持”。
- 修改必须预览并可恢复。
- 具体字段保存方式和 Agent 变体结构后续设计。

### 5.25 运行要求与兼容信息

- 展示 Skill 在已知元数据、标准依赖文件或明确列表中声明的 Python、ffmpeg、MCP、插件、环境变量等运行要求。
- 使用固定规则识别文件中明确出现的命令、依赖文件和环境变量引用，并标记为“发现引用线索”，展示文件和位置；引用线索不等于完整依赖结论。
- 本模块不使用 LLM 推断依赖。未发现明确声明或引用时显示“未发现明确声明”，不能表述为“没有依赖”。
- 展示 Skill 或来源明确声明的适用 Agent、操作系统和其他兼容信息；用户可以补充本地备注，并与原始声明分开。
- 环境变量只记录名称和用途，不保存值。
- 项目装配时列出运行要求，但不主动验证或安装。
- Agent 在实际调用 Skill 时自行处理其运行依赖。
- 不生成兼容、部分兼容或不兼容状态，不判断 Agent 最低版本、模型能力和实际运行环境。
- 不因兼容信息缺失、可能不匹配或未检查运行依赖而阻止部署。

### 5.26 轻量试用管理

- 轻量试用采用 SkillHub 内置特殊标签“临时试用”和到期复核流程，不创建临时目录、临时副本、测试库或第二个集中库。
- 新发现 Skill 必须先完成正常导入和基础检查，再添加“临时试用”标签；已在集中库的 Skill 可以直接添加标签，并按正常流程部署到 Agent 或项目。
- 添加标签时设置复核日期，默认建议 7 天并允许调整；SkillHub 只在应用运行期间监控，到期后在待处理中心生成“试用待确认”。
- 支持转正、延长试用、调整部署和放弃试用。转正只移除特殊标签和待处理事项，保留集中库内容与部署；放弃试用复用标准归档、解除部署和安全删除流程。
- “临时试用”不属于生命周期状态，不表示隔离、安全、已调用、验证通过或验证失败，也不要求用户反馈效果。
- 试用标签不改变版本、自动升级、安全检查和部署规则；不得自动到期删除内容。

### 5.27 基础安全检查

基础安全检查使用固定代码规则，对全部可代码化安全维度进行检查，包括：

- API Key、Token、私钥、连接字符串和疑似密码等敏感信息。
- 危险或破坏性命令、权限提升、系统修改、持久化和资源滥用。
- 下载后执行、动态执行、未知远程来源、未固定依赖和明显供应链风险模式。
- 路径穿越、异常链接、敏感目录访问、自我复制和目录越界。
- Base64、不可见字符、动态拼接和其他混淆规避特征。
- 明确的提示词注入、上下文泄露、隐藏行为、间接注入、数据外传、过度权限以及 MCP、插件或工具诱导模式。

统一规则：

- 检查只读取内容，可安全解码用于检查但不执行 Skill 代码、命令，不访问发现的 URL，也不验证密钥是否有效。
- 每项发现展示文件、位置、固定规则、证据和风险类别，不直接断言恶意。
- 导入、部署、升级和导出前自动执行，也支持用户手动执行。
- 每次基础检查只有一个检查结果字段，取值为“未检查、检查通过、发现问题、检查失败”；运行过程中临时显示“检查中”，不另设状态字段。
- “检查通过”表示当前版本已完成基础检查且没有尚待处理的发现，不构成安全认证。
- 每条发现可以处于待处理、已确认是预期内容或已忽略为误报；所有发现处理后，检查结果联动为“检查通过”，明细历史保留。
- Skill 内容或基础规则变化后恢复为“未检查”；规则、位置和内容均未变化的精确处理记录可以继承。
- 高风险待处理项退出批量和自动处理，需要逐项查看；明确本地使用时可以强制继续，但不能借此覆盖内置或非自有内容。
- 基础检查持续失败时不无限重试或反复弹窗；提供原因、重试、暂缓和本次明确跳过。自动操作暂停，手动强制继续不改变“检查失败”结果。

包含用户自有 API Key 的 Skill 不被永久禁止：

- 只声明环境变量名称或使用占位符不视为真实密钥。
- 疑似真实 Key 可以在本地导入和部署，首次需要逐项确认；相同内容的本地确认可以复用。
- 分享、标准导出或发送到云端前重新确认；已识别的 Key 默认遮盖后才可发送给云端 LLM。
- 系统不擅自删除、遮盖或改写原 Skill 文件。

不包含病毒扫描、动态沙箱执行、完整恶意代码分析、完整供应链审计、签名认证、合规审查和法律判断。

### 5.28 LLM 安全检查

- LLM 安全检查与基础安全检查是两个独立功能，分别展示结果和明细，不生成第三个综合检查结果。
- 两者共用敏感信息、危险命令、提示词注入、数据外传、过度权限、下载执行、供应链、持久化、混淆规避、间接注入、MCP/插件诱导、上下文泄露、破坏和资源滥用等安全维度。
- 基础检查覆盖固定规则能够确认的部分；LLM 检查补充多文件组合、上下文、隐含意图和规避表达。
- 每次 LLM 检查同样只有一个检查结果字段，取值为“未检查、检查通过、发现问题、检查失败”；运行过程中临时显示“检查中”，不另设状态字段。未配置 LLM 属于功能不可用，不混入检查结果。
- 用户发起 LLM 检查时，过期或未执行的基础检查先自动运行，以便遮盖已识别敏感值并提供规则发现上下文。
- 在线模型调用前展示发送的文件范围；检查模型在无工具、只读且不包含无关本机上下文的环境中分析，将 Skill 内容视为不可信数据。
- 结果必须关联 Skill 版本、检查时间、文件范围、服务和模型；内容、模型或检查规则变化后恢复为“未检查”。
- 两项检查发现同一具体文件、位置和实质问题时关联展示，用户处理一次后同步更新明细；仅相关但不相同的问题分别处理。
- LLM 不评价写作质量、步骤完整性或表达清晰度，不提出修改建议，不判断兼容性、实际可用性和 Agent 调用效果。
- LLM 不自动修改、删除或阻止内容；高风险待处理项需要用户逐项决定。

### 5.29 使用证据

- 在 Agent 提供可靠记录或用户安装关联 Hook 时，可收集 Skill 调用事件。
- 使用证据及其分析统一标注为实验功能，结果仅供参考。
- 关联 Hook 是独立关联项目，当前标注“研发中”；本项目不提供虚假的安装、连接或启用入口。
- 不自动解析原始会话内容。
- 不把不支持记录的 Agent 显示为调用次数零。
- 显示数据来源、统计时间范围和覆盖程度。
- 不提供“用过、有帮助、没有效果”等人工使用反馈，也不由用户手动维护调用次数。

### 5.30 Agent 全局 Skill 使用证据分析

- 针对某个 Agent 的全局 Skill 提供保留、移入项目或归档建议。
- 模块必须显示“实验功能”和“仅供参考”，不得把建议包装成确定结论。
- 默认分析时间窗口为 60 天，并允许用户调整。
- 只有覆盖程度足够时才根据阈值给出低使用建议。
- 没有可靠数据时显示“暂无可用使用数据”，不生成保留、移入项目或归档建议。
- 建议不自动执行删除、归档或移出全局。
- 分析依据主要在 Agent 分析入口展示。
- 单 Skill 详情仅在存在证据时显示摘要。
- 默认阈值的最终数值需在后续阶段结合真实数据验证。

### 5.31 外部变更检测

- 应用运行期间或手动扫描时，检测集中库和受管部署中的修改、删除、移动和损坏。
- 外部变更先形成待处理变更，不直接成为正式版本。
- 显示内容差异和影响范围，并暂停相关自动升级和传播。
- 对集中库变更提供接受为新版本、恢复、暂缓和精确忽略。
- 在用户决定前保留临时安全副本。
- 对受管部署修改提供手动收集、恢复、转为独立副本、暂缓或解除部署。
- 非自有对象只读观察。
- 只有正式接受的新版本才更新未固定部署，项目固定版本不受影响。
- 不提供实时双向同步。

### 5.32 手动收集部署修改

- 当受管部署副本被外部修改时，用户可手动比较并收集回集中库。
- 收集后形成新版本。
- 收集前显示受影响的其他 Agent 和项目。
- 不自动把目标目录修改同步回集中库。
- 不对非自有对象提供收集。

### 5.33 待处理事项中心

统一汇总升级阻断、基础或 LLM 安全检查待处理项、同名或层级冲突、合并冲突、外部变更、Agent 不可用、集中库健康问题、备份失败以及项目装配或批量操作失败。

每项包含涉及对象、原因、影响和可执行操作。

- 支持处理、暂缓和对已知问题进行精确忽略。
- 支持按类型、Agent、项目和风险筛选。
- 支持对安全项目批量处理，高风险项目逐项确认。
- 问题解决后自动更新状态，历史保留。
- 不扩展为通用任务管理系统、后台通知服务或托盘提醒中心。

### 5.34 集中库健康检查与修复

- 检查文件缺失或损坏、无效来源和历史版本、失效部署、项目固定关系、未完成操作、孤立管理记录和孤立文件，以及 Agent 或项目目标失联。
- 修复前预览。
- 明确安全的问题可自动修复。
- 需要判断的问题逐项处理。
- 未知文件不自动删除。
- 桌面端和 CLI 提供相同健康检查能力。
- 不提供独立的诊断日志打包或系统诊断报告导出。

### 5.35 操作历史与撤销

- 记录重要的导入、接管、部署、解除部署、升级、回滚、重命名、删除、恢复和项目装配操作。
- 显示时间、对象、影响范围和结果。
- 操作历史与单 Skill 版本历史分开。
- 支持对满足条件的整次管理操作进行撤销。
- 不提供整个集中库任意时间点的全局回溯。
- 部分失败需要准确记录已完成、已跳过和失败对象。

### 5.36 解除部署与安全脱离

- 支持从单个或全部目标解除部署。
- 解除部署不删除集中库 Skill。
- Agent 独占目标中的受管内容默认从目标目录移除；链接只删除链接本身，不能沿链接删除集中库内容。
- 解除前重新检查目标。目标被外部修改时提供先收集修改、保留独立副本、明确放弃修改并移除或取消；出现非受管文件时不得静默删除整个目录。
- 多个 Agent 共用同一物理目录时，不能把物理文件仍存在的情况显示为“已从某个 Agent 移除”；提供保留共享部署、从全部受影响 Agent 移除、迁移其他 Agent 到独占目录或仅解除管理关系并明确仍可能被发现。
- 用户可选择移除受管部署、把目标内容转换为独立副本，或在明确警告后保留未跟踪内容。
- 操作前展示受影响的 Agent、项目和固定版本。

### 5.37 删除 Skill

删除前检查 Agent 和项目部署、项目配置和固定版本、轻量组合、声明依赖、相关 Skill 和未知外部引用。

用户可选择归档、解除选定或全部部署、把已部署内容保留为独立副本、替换或移除引用及依赖，或者取消删除。

- 不进行级联删除。
- 未知和非自有内容不修改。
- 删除执行前重新扫描部署、引用和依赖关系，避免使用过期结果。
- 整体操作建立恢复点。
- 部分处理失败时不删除集中库原件。

### 5.38 忽略规则

- 支持忽略精确路径、精确 Skill，以及对已知待处理问题暂缓或忽略。
- 不支持通配符、正则表达式、脚本条件和嵌套规则。
- 用户可以查看和取消忽略。

### 5.39 备份、恢复与迁移

- 支持一键完整备份、从备份恢复和换机导入。
- 备份包含 Skill、历史版本、来源、安全检查记录、项目配置、组合、设置、操作历史、自定义 Agent 配置和逻辑上的 Agent/项目版本关系。
- 逻辑部署关系不保存本机绝对路径；换机恢复后根据新设备实际目标重新映射并由用户确认，不自动写入 Agent 或项目目录。
- SkillHub 自己管理的凭证不进入备份。
- 备份前检查 Skill 文件中疑似直接写入的凭证；发现后暂停，让用户选择先处理、排除受影响 Skill，或明确将敏感内容包含在备份中。
- 明确包含敏感内容的备份必须标记并提示妥善保存；SkillHub 不静默修改原 Skill 以移除凭证。
- 恢复前展示冲突，可选择覆盖、保留两份或跳过。
- 恢复失败不能损坏当前集中库。
- 支持 Windows 与 macOS 之间迁移。

### 5.40 自动滚动备份

- 仅在应用运行期间工作。
- 在升级、批量部署、删除、恢复和其他重要操作前自动创建。
- 按保留数量或期限清理旧备份。
- 不创建系统后台服务。
- 不提供云备份服务。

### 5.41 标准导出

- 支持单个 Skill、多个 Skill 和轻量组合导出。
- 导出为标准文件夹或压缩包。
- 支持导出当前版本或历史版本。
- 导出内容不依赖 SkillHub。
- 保留来源和许可证，修改过的内容明确标记。
- 排除 SkillHub 管理的凭证、使用证据、操作历史、本机绝对路径和私有设置。
- Skill 文件中疑似直接写入的凭证触发暂停；用户可以先处理、排除对象或明确强制导出，不能静默删改文件内容。
- 可选附带运行要求和兼容信息摘要。
- 不提供发布通道、市场上传或远程发布。
- 不为 ChatGPT desktop、Claude Desktop 或其他上传型平台生成专用导入包，不适配各平台上传格式；用户可以自行使用通用导出结果，SkillHub 不承诺可被特定平台接受。

### 5.42 LLM 配置

- LLM 完全可选，不配置时所有核心管理功能仍可使用。
- 支持常见在线模型服务、本地模型服务和兼容服务。
- 提供连接测试，只设置一个当前默认模型。
- 凭证保存到操作系统安全凭据存储。
- 每项 LLM 功能说明发送的数据范围。
- LLM 主要用于增强安全检查、语义重复分析和描述翻译；已确认保留的在线搜索查询辅助仍为可选能力。
- LLM 不提供通用内容质量诊断、修改建议、兼容性判断和功能验证。
- LLM 不可用时退回规则能力或明确说明可选功能不可用。
- 不提供通用聊天、模型下载、训练微调、多模型自动路由和复杂成本管理。

### 5.43 轻量 CLI

CLI 支持列出对象、查询状态、扫描、项目尽力装配、部署、解除部署、对齐、更新、健康检查和结构化输出。

- 写操作默认展示预览并要求确认。
- 提供明确的非交互模式。
- 高风险操作仍需显式授权。
- 失败输出必须包含原因和下一步建议。

### 5.44 卸载前处理

提供“退出 SkillHub 管理/卸载前处理”：

- 仅卸载应用并保留集中库、项目配置和现有部署。
- 把受管部署转换为独立副本。
- 解除选定或全部部署。
- 先创建备份再退出管理。
- 取消处理。

统一规则：

- 默认不删除集中库和用户创建的 Skill。
- 不影响 Agent 内置、插件和其他工具管理的 Skill。
- 不删除项目业务目录。
- 凭证是否清除由用户选择，默认询问。
- 直接使用系统卸载时，应用数据和集中库默认保留。
- 重新安装后可以选择原集中库并恢复管理关系。

---

## 6. 界面与交互需求

### 6.1 总体原则

- 面向普通用户使用自然语言。
- “部署/解除部署”在主要界面中表达为“添加到/从……移除”。
- 技术信息按需展开。
- 不设置普通模式和开发者模式两套功能。
- 所有入口使用一致的状态名称、冲突规则和操作结果。
- 空状态、加载状态、无数据状态、无权限状态和失败状态都应提供下一步操作。

### 6.2 主要能力入口

界面需要覆盖概览、Skill 管理、Agent 管理、项目管理、发现与导入、待处理事项、操作历史与恢复、设置与数据管理。

- 使用固定左侧导航。核心区依次为概览、技能库、发现、Agent、项目、待处理；底部为操作记录与恢复、设置。
- 导入是技能库和发现页面中的操作入口，不作为独立主导航。
- 面向中文普通用户的主要界面使用“技能/技能库”；首次出现写作“技能（Skill）”，技术详情保留 Skill、`SKILL.md` 等原文。
- 入口之间支持带上下文跳转。
- 从概览或待处理事项跳转时保留筛选和问题上下文。

### 6.3 Skill 管理能力

- Skill 管理是主要操作区域。
- 支持搜索、筛选、排序、选择和批量操作。
- 支持列表、卡片、矩阵和标签分组等查看形式。
- 不同形式使用同一数据和筛选条件。
- 列表是默认高密度视图；支持保存筛选视图、列显示与顺序、密度及个人视图偏好。
- 批量选择时明确区分当前可见对象和全部筛选结果。

### 6.4 Skill 详情范围

详情覆盖基本信息、身份和来源、作者和许可证、版本和差异、Agent 和项目关系、目录关系、调用策略、兼容信息、运行要求、基础安全检查、LLM 安全检查、语义重复、组合和依赖、外部变更与操作历史。

- 使用证据主要在 Agent 全局 Skill 使用证据分析中展示。
- 只有存在可靠数据时，才在单 Skill 详情显示摘要。
- 采用右侧快速抽屉与独立完整详情页并存。行点击打开抽屉，抽屉提供摘要和常用操作；完整详情页是全部信息的权威入口。
- 快速抽屉按模块支持全局显示、隐藏和排序，不对单个 Skill 保存不同布局；名称说明、主要操作、高风险与待处理摘要、完整详情入口不可隐藏。

### 6.5 多维状态表达

生命周期、部署、版本、基础安全检查、LLM 安全检查和所有权状态必须分开。

- 不设置不透明的单一健康分。
- 列表和卡片只显示当前最重要的问题摘要。
- 详情中显示完整状态及依据。
- “未知”“无数据”“未检查”不能显示为失败或零。
- “弃用”不能与“版本过旧”混为一谈。
- 异常状态提供原因、影响和操作入口。
- 状态不能只依赖颜色。

### 6.6 风险分级操作

- 只读操作直接执行。
- 低风险且可撤销的管理操作可以直接完成，并提供撤销。
- 影响部署或版本的操作先显示变化摘要。
- 删除、覆盖、强制导入、高风险部署和批量破坏性操作必须明确预览并确认。
- 批量操作先汇总可执行、跳过、冲突和需单独处理的对象。
- 高风险对象不能隐藏在一个批量确认中。

### 6.7 过程与结果反馈

- 显示整体进度和当前对象。
- 可安全取消时提供取消，不可取消时说明原因。
- 完成后分别展示成功、跳过、失败和仍待处理的项目。
- 支持重试、查看原因、撤销或进入待处理事项。
- 部分失败不能显示为整体成功。
- 默认显示普通语言结果，路径、差异和底层方式按需展开。

### 6.8 基础桌面可用性

- 适配 Windows 和 macOS 系统缩放及常见高分屏。
- 窗口可调整大小，较小窗口下仍可完成核心操作。
- 主要查看、筛选、选择、确认和取消操作可使用键盘完成。
- 键盘焦点清晰可见。
- 重要控件提供可被系统辅助功能识别的名称。
- 状态使用文字或图标辅助表达，不只使用颜色。
- 保持基本文字对比度。
- 支持跟随系统浅色或深色主题，并允许手动选择。
- 系统启用减少动态效果时，减少非必要动画。
- Windows 和 macOS 快捷键遵循各自习惯。
- 不承诺企业级无障碍认证或完整合规审计。

---

## 7. 已确认的产品与交互方向

本节记录功能需求对交互结构的约束。完整页面结构、流程和模块配置见独立的《产品与交互设计》文档；视觉颜色、字号、间距和品牌风格仍不在当前确认范围。

### 7.1 概览

概览采用平衡工作区，展示 Skill 总数、Agent 和项目数量、待处理事项、可升级项、冲突、安全检查问题和失联目标。概览只做汇总与下钻，不维护第二套状态。

- 总览数字卡片。
- 使用柱状图展示部署关系数据，支持按 Agent 或项目切换维度并下钻。
- 首页不展示最近操作时间线，操作历史统一进入“操作记录与恢复”。
- 点击汇总项后进入对应筛选结果。

### 7.2 Skill 多视图

- 列表：适合高密度查看和批量管理。
- 卡片：适合日常浏览和概要识别。
- 矩阵：适合查看 Skill 与 Agent/项目的部署关系。
- 标签分组：适合按用途聚类查看。

列表可配置的列包括：

- 勾选框。
- 名称和显示别名。
- 简短说明。
- 我的用途说明和描述译文。
- 标签。
- 所有权和来源。
- 生命周期。
- 调用策略。
- 已添加的 Agent 和项目。
- 当前版本、可升级状态和项目固定提示。
- 基础安全检查、LLM 安全检查和待处理问题摘要。
- 行级常用操作和更多操作入口。

筛选维度包括：

- 标签。
- 生命周期。
- 来源和所有权。
- Agent 和项目。
- 调用策略。
- 是否部署。
- 是否可升级或被固定。
- 基础安全检查和 LLM 安全检查结果。
- 是否存在待处理事项。

排序维度保留名称、更新时间、来源、版本和可用使用证据；没有可靠使用证据时不提供误导性排序。

卡片中保留名称、别名、简短说明、标签、主要 Agent/项目关系、重要状态摘要、勾选和常用操作。

批量选择后使用浮动操作区，可包含添加到 Agent/项目、移除、打标签、修改升级策略、升级、翻译描述、导出、归档和删除。删除及其他高风险操作仍遵守逐项检查和风险分级规则。

已确认：

- 列显示、隐藏、顺序和密度调整。
- 视图偏好记忆。
- 保存筛选视图。
- 看板下钻后自动应用筛选。
- 空状态引导。

### 7.3 Skill 详情

- 右侧快速抽屉与独立完整详情页并存。
- 支持连续切换相邻 Skill，并保留当前查看位置。
- 详情内可直接发起与当前状态相关的操作。
- 详情可按基本信息、来源和许可证、版本、Agent 与项目关系、兼容信息与运行要求、基础和 LLM 安全检查、语义重复、关联关系、外部变更和操作历史进行分区。
- 版本区域使用时间线和差异入口。
- Agent 与项目区域使用部署明细表和目录关系说明。
- 快速抽屉模块支持全局显示、隐藏、排序和恢复默认；不为每个 Skill 保存独立布局。

### 7.4 下钻

- 点击概览中的标签、Agent、项目、状态或问题后，进入对应对象列表并自动应用筛选。
- 返回时尽量保留原上下文。

### 7.5 项目工作台

- 项目默认使用高密度列表工作台，支持收藏、多标签、保存筛选视图和批量操作。
- 项目行点击打开可配置快速抽屉，再进入完整项目详情。
- 项目与 Agent 关系矩阵作为辅助视图，不作为默认页。
- 项目快速抽屉的名称、路径、访问状态、主要操作、高风险与待处理摘要、完整详情入口不可隐藏；其他模块可全局显示、隐藏和排序。

### 7.6 Agent、发现与待处理

- Agent 按品牌分组，CLI、桌面端和 IDE 等实际客户端分别识别；客户端没有稳定本地目录时不作为文件部署目标。
- “发现”明确区分本机外部发现和联网发现；集中库内搜索位于技能库页面。
- 待处理采用高密度问题列表，固定快捷视图覆盖阻断操作、安全问题、外部变更、冲突、失联、失败操作、暂缓和历史。
- 使用证据分析在 Agent 详情中标注为实验功能和仅供参考；关联 Hook 标注为研发中。

### 7.7 仍待后续视觉设计

具体表格默认列、卡片尺寸、抽屉宽度、按钮位置、工具栏细节、视觉风格、字号、颜色、图标和间距在后续视觉设计阶段确定，不改变本文已经确认的信息架构和功能流程。

---

## 8. 非功能与质量要求

### 8.1 安全作用域

- 只读写集中库、用户明确选择的导入来源和已注册目标。
- 不遍历全盘。
- 删除部署关系时不得跟随链接误删集中库内容。
- 任何递归删除都必须限定明确对象。
- 普通用户权限下应能使用核心功能，不要求管理员权限作为前提。

### 8.2 可靠性

- 重复执行同一操作应保持结果一致。
- 中断后重试不能产生重复对象或未知半成品。
- 重要写操作先建立恢复点。
- 部分失败必须保持已有成功结果可识别。
- 健康检查能够发现未完成操作。

### 8.3 性能

- 面向 100 至 500 个 Skill 的本地规模。
- 常规列表、筛选和状态查看应保持流畅。
- 扫描、批量导入和批量更新显示进度。
- 不为企业级海量资产建设复杂分布式能力。

### 8.4 应用运行边界

- 自动检查、外部变更检测和滚动备份只在应用运行期间进行。
- 支持用户手动触发扫描和检查。
- 不安装常驻后台服务。
- 不创建托盘常驻监控进程。

---

## 9. 明确不做

- 多集中库管理。
- SkillHub 产品账号、团队角色和远程协作服务器。
- 发布通道、Skill 市场上传或远程发布。
- ChatGPT、Claude Desktop 等上传型平台的专用导入包生成、上传自动化和人工安装状态记录。
- 系统 Git 作为用户前置要求。
- 任意外部命令执行器。
- 自动安装 Skill 运行依赖。
- 完整代码编辑器或完整 AI Skill 创作平台。
- Agent 自动执行测试和自动功能评分。
- 为试用创建临时目录、临时测试库或运行沙箱。
- Agent 版本识别、登录状态、项目可信状态和运行时兼容性判断。
- 用户人工维护“可用、不可用、用过、有帮助”等验证或使用反馈。
- LLM 内容质量评分、写作诊断、修改建议和自动修订。
- 自动解析或上传原始 Agent 会话。
- 双向实时同步、来源镜像或多来源自动切换。
- SkillHub 云同步和多设备并发协作。
- 杀毒、完整恶意代码分析、供应链审计或法律审查。
- 后台常驻服务和系统托盘持续监控。
- 完整 Agent 适配插件生态。
- 整个集中库任意时间点回溯。
- 独立磁盘清理中心。
- 系统诊断日志打包和诊断报告导出。
- Linux、Web 和移动端。

---

## 10. 关联项目

### 多 Agent Runtime Hook

- 作为独立关联项目存在。
- 当前产品标注为“研发中”。
- 用于采集更可靠的 Skill 调用事件。
- SkillHub 不依赖该项目即可使用。
- 配合使用时可提高 Agent 全局 Skill 使用证据分析的准确度。
- Hook 项目需要独立确认支持的 Agent、权限、隐私和事件格式。

---

## 11. 后续阶段事项

### 11.1 功能开发优先级

功能优先级已经确认，统一用于安排开发先后关系，不代表版本拆分、发布批次或范围取舍。所有已经确认的功能仍属于完整开发范围。

排序原则为：技术和数据依赖优先，其次是用户核心价值；自动化、LLM 和数据分析建立在稳定的基础能力之上。不得以优先级较低为由静默删除已确认功能。

#### 11.1.1 优先级一：基础前置

缺少本级能力会使部署、升级、删除或恢复建立在不可靠的数据基础上，因此最先开发：

- 集中库创建、选择、路径安全检查和初始化恢复。
- Skill 核心身份、内容身份、来源、所有权和基础元数据。
- Agent 文件管理 profile、自定义 Agent、通用目录和多客户端识别。
- 项目注册、Agent 关联和项目范围记录基础。
- 本地 Skill 扫描、真实路径去重、链接和未知对象识别。
- 导入、复制、接管和确定性格式检查基础。
- 版本内容保存和历史记录基础。
- 基础安全检查和敏感信息识别引擎。
- 操作记录和恢复点基础。

#### 11.1.2 优先级二：核心闭环

完成本级后，普通用户能够完成 Skill 的主要本地全生命周期管理：

- 本地搜索、来源获取、创建、外部编辑和完整导入流程。
- 元数据、标签、生命周期、来源与许可证、确定性重复和同名冲突、重命名。
- 完整项目管理、共享项目配置、项目尽力装配和项目固定版本。
- 添加到单个或多个 Agent/项目、各客户端目标目录与共享物理目录处理、目录关系展示、解除部署和安全脱离。
- 版本差异、单 Skill 回滚、手动检查更新、上游更新和本地修改合并。
- Agent 环境变化、已部署文件外部变化和手动收集部署修改。
- 基础安全检查的业务流程接入、待处理事项中心、运行要求和声明兼容信息。
- 依赖检查、删除选项、集中库健康检查与修复、完整操作历史和撤销。
- 手动完整备份、恢复、换机迁移和卸载前安全处理。
- 概览、Skill 列表、详情、Agent 和项目入口，以及核心操作反馈。

#### 11.1.3 优先级三：增强管理

本级用于增强自动化、智能分析、批量效率和高级使用方式：

- skills.sh 联网搜索、可选查询辅助，以及本地和联网结果区分。
- 来源重新关联、身份核对和来源恢复。
- 全局、批量和单 Skill 自动检查与自动升级设置。
- LLM 配置、LLM 安全检查、语义重复分析及描述翻译。
- 调用策略、Agent 专用变体和已确认平台差异处理。
- 轻量试用标签与复核闭环、批量发现和批量操作增强、忽略规则。
- 列表和卡片等多视图、视图偏好、列设置、连续查看和高级下钻。
- 自动滚动备份、标准导出和敏感内容导出处理。
- 与桌面端共用规则和数据的轻量 CLI。

LLM 安全检查位于本级是因为它依赖基础安全检查、版本体系、敏感信息遮盖和 LLM 配置，不表示其安全价值较低。

#### 11.1.4 优先级四：后置能力

本级依赖可靠运行数据，不得在数据条件不足时伪造结果：

- 接收 Agent 可靠提供的 Skill 调用事件，并记录数据来源、时间范围和覆盖程度。
- 为独立的多 Agent Runtime Hook 关联项目提供可选接入能力；Hook 本身不在 SkillHub 范围内。
- 按 Agent 和时间范围分析全局 Skill 的可靠使用证据。
- 仅在数据充分时给出保留、移入项目或归档建议。
- 根据实际数据校准默认分析周期和低使用阈值。

#### 11.1.5 横向要求

以下要求不单独排到后期，必须从对应功能首次开发时同步落实：

- Windows 和 macOS 双平台边界。
- i18n 基础和中英文文案外置。
- 文件权限、路径越界、敏感信息和非自有内容保护。
- 普通用户语言、基础桌面可用性和必要的操作界面。
- 每项功能的加载、进度、成功、失败、部分成功和恢复反馈。
- 可测试性、恢复能力和不破坏现有用户数据。

#### 11.1.6 功能章节映射

同一章节包含基础、核心和增强子能力时可以跨级；跨级表示按子能力依赖顺序实现，不表示重复开发。

| 功能章节 | 开发优先级 | 划分说明 |
|---|---|---|
| 5.1 首次使用与初始化 | 一 | 建立集中库、路径安全和初始化恢复基础。 |
| 5.2 Agent 管理 | 一、三 | profile、自定义目标和客户端识别优先；专用变体后置增强。 |
| 5.3 Agent 环境变化 | 二 | 依赖 Agent 基础记录，纳入核心运行闭环。 |
| 5.4 项目管理 | 一、二、三 | 注册和关联基础优先；完整管理与共享配置进入核心；批量发现属于增强。 |
| 5.5 项目尽力装配 | 二 | 构成项目使用核心闭环。 |
| 5.6 本地发现与扫描 | 一 | 是导入、关系判断和外部变化检测的基础。 |
| 5.7 本地搜索与联网搜索 | 二、三 | 本地搜索进入核心；联网搜索和查询辅助属于增强。 |
| 5.8 来源获取 | 二 | 构成普通用户获取 Skill 的核心流程。 |
| 5.9 导入、复制与接管 | 一、二 | 格式检查和导入基础优先；完整冲突处理进入核心。 |
| 5.10 创建与外部编辑 | 二 | 构成核心内容建立流程。 |
| 5.11 来源重新关联 | 三 | 在核心来源管理稳定后增强恢复能力。 |
| 5.12 元数据与组织 | 一、二 | 核心身份和基础元数据优先；完整组织能力进入核心。 |
| 5.13 生命周期状态 | 二 | 构成核心整理和删除前管理流程。 |
| 5.14 来源、作者与许可证 | 一、二 | 来源身份基础优先；完整展示和维护进入核心。 |
| 5.15 重复与冲突检查 | 二、三 | 确定性重复和冲突进入核心；LLM 语义分析属于增强。 |
| 5.16 版本历史与差异 | 一、二 | 版本存储基础优先；完整差异查看进入核心。 |
| 5.17 单 Skill 回滚范围 | 二 | 依赖版本和恢复点基础，构成核心恢复闭环。 |
| 5.18 当前版本与项目固定 | 二 | 构成部署和项目装配核心规则。 |
| 5.19 上游更新与本地自定义 | 二 | 构成核心更新闭环。 |
| 5.20 自动检查与自动升级 | 三 | 自动检查、通知和自动升级策略均建立在手动更新闭环稳定之后。 |
| 5.21 Skill 重命名 | 二 | 纳入核心整理和引用安全处理。 |
| 5.22 部署 | 二 | 构成产品核心价值闭环。 |
| 5.23 Agent 目录关系视图 | 二 | 用于解释部署和冲突结果。 |
| 5.24 调用策略 | 三 | 依赖 Agent profile 和变体机制稳定。 |
| 5.25 运行要求与兼容信息 | 二 | 在核心导入、装配和部署流程中展示声明信息。 |
| 5.26 轻量试用管理 | 三 | 复用导入、部署、待处理和删除闭环的轻量增强。 |
| 5.27 基础安全检查 | 一、二 | 规则引擎优先；流程联动和用户处理进入核心。 |
| 5.28 LLM 安全检查 | 三 | 依赖基础检查、版本和 LLM 配置。 |
| 5.29 使用证据 | 四 | 依赖 Agent 或关联 Hook 提供可靠事件。 |
| 5.30 Agent 全局 Skill 使用证据分析 | 四 | 依赖足够且可解释的使用证据。 |
| 5.31 外部变更检测 | 二 | 构成部署后的核心管理闭环。 |
| 5.32 手动收集部署修改 | 二 | 构成外部变更处理闭环。 |
| 5.33 待处理事项中心 | 二 | 汇总核心流程产生的待处理问题。 |
| 5.34 集中库健康检查与修复 | 二 | 保障核心数据可恢复。 |
| 5.35 操作历史与撤销 | 一、二 | 记录和恢复点基础优先；完整查看与撤销进入核心。 |
| 5.36 解除部署与安全脱离 | 二 | 构成部署生命周期核心闭环。 |
| 5.37 删除 Skill | 二 | 构成安全退出和数据保护闭环。 |
| 5.38 忽略规则 | 三 | 属于扫描和待处理事项的效率增强。 |
| 5.39 备份、恢复与迁移 | 二 | 是个人本地管理的核心数据保障。 |
| 5.40 自动滚动备份 | 三 | 建立在手动备份与恢复验证稳定之后。 |
| 5.41 标准导出 | 三 | 属于共享和迁移之外的增强能力。 |
| 5.42 LLM 配置 | 三 | 为 LLM 安全检查、语义重复分析和描述翻译提供统一入口。 |
| 5.43 轻量 CLI | 三 | 复用已稳定的桌面端规则和数据。 |
| 5.44 卸载前处理 | 二 | 防止卸载破坏部署和集中库数据。 |

界面需求第 6～7 章随对应功能同步实现：核心信息架构和操作界面属于优先级二，高级多视图和偏好能力属于优先级三。安全、可靠性和运行边界第 8～9 章属于横向约束，不得作为低优先级功能推迟。

### 11.2 平台资料维护与测试

- 首轮官方资料调研和逐 Agent 文件管理能力矩阵已经完成。
- 开发阶段按官方资料实现平台 profile，并保留自定义目录兜底，不要求在当前阶段安装全部 Agent。
- 开发完成进入测试阶段后，再逐个平台验证目录发现、部署、解除部署、冲突、刷新和链接行为。
- 维护最后官方资料检查日期、尚未确认规则和后续需要重新测试的触发条件。

### 11.3 产品与交互设计

- 信息架构、导航、主要页面划分、多视图、快速抽屉与完整详情关系已经确认，并记录在独立《产品与交互设计》文档中。
- 首次使用、导入、部署、冲突、项目、待处理、删除、恢复、设置及高风险操作流程已经确认。
- 后续视觉设计继续确认具体列默认值、卡片尺寸、抽屉宽度、按钮布局、图标、颜色、字号和间距，不得改变已确认功能范围。
- 中英文最终文案和状态表达在视觉设计与开发过程中按本文统一术语落实。

### 11.4 技术设计

- 确认集中库内部结构和记录格式。
- 确认版本历史、部署、索引、扫描和外部变更检测机制。
- 确认桌面端和 CLI 技术栈。
- 确认 Windows 与 macOS 平台实现差异。

### 11.5 待验证产品参数

- 使用证据分析的默认阈值。
- 自动滚动备份的默认保留数量或期限。
- 默认扫描频率和更新检查频率。
- 高风险提示和强制确认的具体分级规则。

---

## 12. 文档维护规则

- 本文档是当前唯一有效的功能需求来源。
- 新功能先在需求阶段确认，再加入本文档。
- 功能优先级统一在 11.1 节维护，不在单个功能讨论中零散标注。
- 界面需求、界面形式设想和最终设计必须分层记录。
- 技术实现方案不得反向替代已经确认的产品行为。
- 删除或调整已确认范围时，应同步更新“明确不做”和“后续阶段事项”。
