# SkillHub 需求文档

> 状态：v0.1 草案（功能范围讨论稿）
> 日期：2025-08-19
> 说明：本文档聚焦**功能范围与行为定义**，技术栈、CLI 命名、存储格式细节留待功能范围锁定后另行讨论。

---

## 1. 背景与目标

### 1.1 现状痛点

Agent skills（以 `SKILL.md` 为入口的技能目录）目前散落在多个 agent 的固定扫描目录中，以本机为例：

| 目录 | skill 数 | 归属 |
|---|---|---|
| `~/.agents/skills` | 25 | 跨 agent 标准目录（事实主库） |
| `~/.claude/skills` | 16 | Claude Code 用户级 |
| `~/.codex/skills` | 10 | Codex 用户级 |
| `~/.pi/agent/skills` | 3 | pi 用户级 |
| `~/.cursor/skills` | 2 | Cursor 用户级 |
| `~/.gemini/skills` | 3 | Gemini 用户级 |

核心问题：

1. **重复拷贝**：同一 skill 在多个 agent 目录各存一份实体（如 `vercel-react-best-practices` ×5、`agent-browser` ×4），共 54 份拷贝。
2. **升级割裂**：没有统一升级渠道，各拷贝版本漂移，无法判断哪个是最新。
3. **无法按需派发**：给不同 agent / 项目分发 skill 全靠手工复制。
4. **缺乏元数据**：没有标签、来源、版本记录，无法分类管理。

### 1.2 项目目标

构建一个 **skill 集中管理 + 多 agent 派发同步工具**（工作名 SkillHub）：

- 所有 skill 收敛到**一个用户自定的集中管理库（library）**，该库不被任何 agent 自动扫描。
- 以**软链接**（自动降级策略，见 3.4）将库内 skill 派发到各 agent / 项目目录，做到"一处修改、处处生效"。
- 以**工具侧标签**（不修改 skill 本体）实现分类聚类，按标签一键派发。
- 支持**添加源**（skills.sh 等）安装新 skill，支持**单个 / 一键全部升级**。
- 支持**扫描发现**散落各处的 skill 并收编（adopt）入库。

### 1.3 已确认的关键决策

| 决策点 | 结论 |
|---|---|
| 集中库位置 | 由使用者指定（init 时配置），不硬编码；项目本身只是工具的开发目录 |
| 链接策略 | 自动三级降级：symlink → junction → copy（copy 模式启用 hash 漂移检测） |
| 标签存储 | 只存于工具自身的 manifest，**不修改** 各 skill 的 SKILL.md 本体 |
| 技术栈 | 本阶段不讨论，先锁定功能范围 |

---

## 2. 核心概念

| 概念 | 定义 |
|---|---|
| **library（集中管理库）** | 用户指定的一个目录，存放所有 skill 的唯一实体。内部可按来源分区（如 `library/skills.sh/`、`library/git/`、`library/local/`）。**不得位于任何 agent 的自动扫描路径内**（用户级固定目录、项目内 `.agents/skills`、`.claude/skills` 等） |
| **manifest（清单）** | 工具的唯一事实源，记录每个 skill 的来源（provenance）、内容 hash、标签、部署记录，以及目标目录注册表、标签组合定义。伴随 library 存放 |
| **目标（target）** | 一个可接收 skill 派发的目录，分两类：**用户级**（agent 全局目录，如 `~/.claude/skills`）与**项目级**（项目内目录，如 `<project>/.claude/skills`、`<project>/.agents/skills`）；同一项目可注册多个目标（每 agent 一个）。内置常见目录，支持注册任意自定义路径 |
| **部署（deploy）** | 在目标目录建立指向 library 的链接（或降级拷贝）的动作 |
| **收编（adopt）** | 扫描发现散落 skill，迁移入 library 并（可选）将原位置替换为链接 |
| **标签（tag）** | 工具侧维度对 skill 的分类标记，支持多标签 |
| **元数据（metadata）** | 工具侧维护的 skill 描述信息（含中文用途 / 触发条件等），存于 sidecar 文件，不碰 skill 本体 |
| **组合（bundle）** | 命名了的 skill 集合（通常由标签派生或手工定义），一键派发的基本单元 |
| **链接模式（mode）** | 每个部署点实际使用的同步方式：`symlink` / `junction` / `copy`，逐目标记录于 manifest |

### 2.1 防扫描原则

各 agent 仅扫描固定路径（用户级如 `~/.claude/skills`；项目级如 `<project>/.agents/skills`）。因此：

- library 可使用任意自定义路径（如 `D:/skill-hub-library`），路径不在扫描清单内即天然安全。
- 工具在 init / 配置 library 时须**主动校验**：若路径命中已知扫描路径（或位于某个常开项目的 `.agents` 下），给出警告并阻止或要求确认。
- 内置一份已知扫描路径清单（各 agent 用户级目录 + 常见项目级目录模式），随工具更新维护。

### 2.2 初期适配的 Agent 范围（V1）

初期适配以下 16 个 agent（用户级 + 项目级）：

| # | Agent | 用户级 skill 目录 | 项目级 skill 目录 | 目录机制状态 |
|---|---|---|---|---|
| 1 | Claude Code | `~/.claude/skills` | `<project>/.claude/skills` | 已确认 |
| 2 | Codex | `~/.codex/skills` | 待调研 | 用户级已实测 |
| 3 | pi | `~/.pi/agent/skills` | `<project>/.pi/agent/skills` | 已确认 |
| 4 | Hermes | 待调研 | 待调研 | — |
| 5 | OpenClaw | 待调研 | 待调研 | — |
| 6 | DeepSeek-Harness | 待调研 | 待调研 | — |
| 7 | WorkBuddy | 待调研 | 待调研 | — |
| 8 | Trae | 待调研 | 待调研 | — |
| 9 | Cursor | `~/.cursor/skills` | 待调研 | 用户级已实测 |
| 10 | Gemini CLI | `~/.gemini/skills` | 待调研 | 用户级已实测 |
| 11 | Kimi Code CLI | 待调研 | 待调研 | — |
| 12 | OpenCode | 待调研 | 待调研 | — |
| 13 | ZCode | 待调研 | 待调研 | — |
| 14 | MiMo Code | 待调研 | 待调研 | — |
| 15 | TraeWork | 待调研 | 待调研 | — |
| 16 | QcodeWork | 待调研 | 待调研 | — |

**开发前调研任务（备注）**：开发阶段须逐一调研上述 agent 的 skill 机制，结论沉淀为工具内置的"agent → 目录映射表"（见 3.1）并随版本维护：

1. 用户级 skill 扫描目录路径（含多目录扫描行为与优先级）；
2. 项目内创建 / 扫描项目级 skill 的目录约定；
3. skill 目录结构要求（SKILL.md 格式兼容性、frontmatter 字段差异、多文件支持）；
4. 对软链接 / junction 的兼容性（能否跟随、是否重复加载、copy 模式是否必须）；
5. 用户级与项目级同名 skill 的覆盖优先级；
6. skill 加载机制（全量注入 vs 按需触发——影响派发规模建议与防扫描策略）。

- 调研后确认**不支持 skill 目录机制**的 agent：从适配清单移除，仅可作为"任意自定义路径目标"以 copy 模式管理（是否保留视其可用性决定）。
- 清单外 agent：统一走"任意自定义路径目标"，不承诺内置适配。

---

## 3. 功能需求

> 优先级：P0 = MVP 必须｜P1 = 重要，紧随其后｜P2 = 增值，可延后。

### 3.1 初始化与配置（P0）

- `初始化`：指定（或创建）library 目录，生成 manifest；同时将 library 初始化为 **git 仓库**（已存在则沿用）——收编 / 升级 / 元数据变更由工具自动 commit（消息含操作摘要，可关闭），**版本历史、任意回滚、版本间 diff 均由 git 承担**（B4 已决策，替代独立快照机制）。**git 引擎三层策略（面向无 git 的普通用户）**：①检测到系统 git 则直接使用；②未安装则使用工具**内置的纯 JS git 引擎**（如 isomorphic-git，零安装零配置，能力与①一致，库仍为标准 git 仓库，日后装了 git 可无缝接手）；③均不可用时降级为**内建备份目录机制**（仅升级前快照 + 删除进回收站，覆盖"改坏可恢复"，不提供完整历史与 diff，相关功能调用时明确提示当前模式与能力边界）。
- 支持注册**目标目录**：
  - 内置常见 agent 用户级目标（§2.2 清单内 agent 的用户级目录 + `.agents` 标准目录等），init 时可自动探测存在的目录并列出供勾选；
  - 支持添加**项目级目标**：指定项目根目录后自动探测各 agent 的项目级 skill 目录，勾选注册；亦支持手动指定任意子路径。
  - 内置**agent → 目录约定映射表**（用户级 + 项目级，初期覆盖 §2.2 的 16 个 agent；`.agents/skills` 为跨 agent 标准目录）：自动探测 = 按映射表扫描项目根下已存在的目录；映射表随工具版本维护更新，支持自定义扩展。
  - 支持**任意自定义路径**目标。
  - 用户级与项目级目标在所有后续操作（派发 / 同步 / 升级 / 卸载 / 统计）中一视同仁。
- **LLM 接入配置（可选，P1）**：
  - 专用配置命令设置 API key：内置常用厂商预设（OpenAI / Anthropic / DeepSeek / Kimi / 智谱 / 通义等，选中后仅需填 key）；另支持**自定义厂商**：输入 base URL + API key + 模型名，按 OpenAI 兼容协议调用（本地模型 / 中转服务均可覆盖）；
  - **接入完全可选**：不接入不影响任何核心功能（收编 / 派发 / 升级 / 检索 / doctor 全部可用）；调用依赖 LLM 的功能（enrich、LLM 打标）时明确提示"未接入 LLM，该功能不可用"并给出配置命令入口；
  - API key 存储安全见 4.2。
- 配置可修改（library 迁移场景见 3.9 / 5.3）。

### 3.2 扫描发现与收编（P0）

**扫描**：

- 扫描范围：所有已注册目标目录 + 常见 agent 用户级目录 + 当前项目内的项目级 skill 目录（可扩展：任意指定路径）。
- 输出发现报告：每个 skill 的位置、名称、内容 hash、是否已入库、与库内同名 skill 是否一致。
- **识别链接条目**：目标中已存在的软链接 / junction（含非本工具手工创建的），报告其指向路径与有效性；收编时可将外部链接直接纳管为库链接。

**收编**：

- 对每个发现的 skill 分三类处理：
  1. **库中不存在** → 移入 library（按来源归入 `local/` 分区），原位置替换为链接（可选：保留原样仅入库不链接）。
  2. **库中存在且内容一致** → 删除原拷贝，原位置替换为链接（纯去重，无痛）。
  3. **库中存在但内容不同（冲突）** → 不得静默覆盖。交互处理：并排对比（diff 摘要 / 并存为 `name@origin`）/ 以库为准 / 以发现版本为准入库（此时其它同名拷贝如何处理需联动提示）。
- 全程支持 **dry-run 预览**：先展示"将移动 / 将替换为链接 / 冲突待决"清单，确认后执行。
- 收编操作写入 manifest（来源标记为 `local`，记录原路径，便于追溯与撤销）。

### 3.3 标签与元数据管理（P0）

**标签**：

- 工具侧为 skill 打标签：**一个 skill 可挂多个标签**；添加 / 移除支持单个与批量（按名称 glob（如 `pm-*`）、按检索结果批量（联动 3.5）），交互采用**勾选式多选**（列出 skill 清单 / 标签清单勾选确认，避免手敲名称出错）。
- 标签列表、按标签筛选查看（聚类视图：tag → skills 列表）。
- **预置标签集（8 个）**：`软件开发`、`日常办公`、`内容处理`、`多媒体`、`数据分析`、`行业垂直`、`通用`、`其他`；预设集可在配置中增删改，不影响已打标 skill。
- **自定义标签**：随打随建；提供标签管理：重命名（全局生效）、删除（从所有 skill 上移除该标签，需确认）。
- **手动编辑**：标签及全部元数据既可用 CLI 命令维护，也可直接手改 sidecar 文件（`meta/<name>.yaml`），工具以文件内容为准（加载时重新读取，不以 manifest 缓存覆盖手改内容）。
- **LLM 自动打标（P1，随 enrich）**：大模型阅读 SKILL.md 与生成的中文元数据，从"预设 + 已有自定义"的标签词表中**推荐标签**——只能从现有词表选择、不凭空造新标签（保证词表收敛、聚类不碎片化）；输出建议清单，支持逐条确认 / 批量确认应用，`--auto` 直接应用高置信度建议。
- **通道标签（轻量发布通道，B5 已决策）**：预置 `channel:dev` / `channel:test` / `channel:stable` 标签约定：派发、项目装配（3.6）与一键升级默认排除 `channel:dev`（可显式包含）；开发中的 skill 打上 dev 标签即隔离在库内，不引入独立发布流水线。

**中文元数据**：每个 skill 维护一套工具侧元数据（含中文详情），存储于 sidecar 文件（`library/.skillhub/meta/<name>.yaml`，一 skill 一文件），索引同步至 manifest。**任何情况下不改动 skill 目录内文件**。

- **本体元数据不强制（B2 已决策）**：skill 本体 SKILL.md 的 frontmatter 不作任何强制要求（遵守不修改本体原则）；版本、权限等级等管理字段一律由工具侧元数据承载。自建 skill 自愿写入本体不受限制、不校验。
- **弃用与替代（A6）**：状态置为弃用且设置了替代 skill 后，status / 派发 / 检索中提示"已弃用，推荐替代：xxx"，实现平滑过渡。

| 类别 | 字段 | 来源 |
|---|---|---|
| 基础 | 名称、库内路径、中文名/别名、文件数、体积 | 自动采集 |
| 中文详情 | 用途说明、触发条件、典型使用场景、搜索关键词 | enrich 生成 + 人工可改 |
| 来源版本 | 来源、版本 hash、安装/更新时间、作者、license | 自动采集 |
| 部署 | 已派发目标、各链接模式 | 自动采集 |
| 依赖兼容 | 依赖的外部命令（如 ffmpeg/python）、适用平台、适用 agent | enrich 生成 + 脚本检测 |
| 管理 | 标签、状态（使用中/试用/弃用）、弃用时的替代 skill、个人评级、备注笔记、相关 skill | 人工维护 |
| 使用统计 | 最后使用时间、使用频率 | P2：手动 mark-used 或解析 agent 会话日志（best-effort） |

**enrich（元数据自动生成，P1，需已接入 LLM——见 3.1；未接入时提示配置入口）**：

- 读取 SKILL.md（frontmatter + 正文），LLM 批量生成中文用途 / 触发条件 / 使用场景 / 关键词 / 依赖信息，并输出标签推荐（见上"LLM 自动打标"）。
- 人工修改过的字段视为"锁定"，重新生成不覆盖；无 LLM 环境时字段留空可手填。
- **边界说明**：agent 运行时读取的仍是 SKILL.md 原版 description，中文触发条件仅用于管理侧展示，不影响实际触发行为。

### 3.4 派发与同步（P0，核心）

**部署单元**（任选其一）：

- 单个 skill；
- 一个标签下的全部 skill；
- 一个 bundle（命名组合）；
- "镜像某目标"：使目标目录与期望集合完全对齐（多退少补，见 reconcile）。

**链接建立 —— 自动三级降级**：

1. 优先创建**目录符号链接**（symlink，需系统权限，Windows 需开发者模式 / 管理员）；
2. 失败则降级为 **junction**（Windows 免管理员，仅限目录——skill 恰为目录，适用；注意 junction 为绝对路径绑定）；
3. 再失败降级为 **copy**（实体拷贝 + 记录内容 hash，后续以此 hash 做**漂移检测**）。

- 每个部署点实际使用的模式记录于 manifest，`status` / `doctor` 可见。
- **依赖预检（A3）**：派发前按元数据的依赖字段检查目标机环境（外部命令 / 运行时是否存在），缺失项列出并让用户选择继续或跳过。
- 部署冲突规则：
  - 目标位置已存在**同名的非本工具管理的实体目录** → 提示处理（若内容与库一致则直接去重替换；不一致进入冲突流程，同 3.2）；
  - 目标位置已是本工具部署的链接 → 幂等跳过或按需更新。
- **同步语义**：symlink / junction 模式下 library 内修改天然实时生效；copy 模式提供"重新推送"能力（library → 目标单向覆盖，覆盖前先检测目标端是否被改动过，有改动则提示）。
- **reconcile（对齐）**：将某目标对齐到期望集合——缺的补上、多余的（本工具部署的）移除；`--prune` 才删除多余项，默认只报告。
- **项目级派发交互**（问一次，记住）：
  1. 确定项目根：在项目内运行时默认当前目录，否则选择 / 输入；
  2. 按 3.1 映射表自动探测项目内已存在的 agent skill 目录，多选确认；无任何已存在目录时，从映射表全量列出供勾选；
  3. 选择结果写入该项目配置（见 3.6），后续派发 / 升级 / 对齐默认沿用，不再重复询问；
  4. 同一 agent 可能同时扫描多个标准目录（如 `.claude/skills` 与 `.agents/skills`），多选导致重复加载时给出警告（见 5.8）。
- 全程 dry-run + 确认。

### 3.5 检索、状态与部署明细展示（P0 基础 / P1 全文）

- **检索**：基础筛选（P0）——按名称 / 标签 / 状态过滤；全文检索（P1，A1）——覆盖中文名 / 用途 / 关键词 / SKILL.md 正文，支持组合条件（如 `tag:pm 依赖:ffmpeg`），结果展示命中片段与元数据摘要。

多视角查看部署现状，数据来源 = manifest + **文件系统实时扫描**（不只信 manifest，能发现手工变更）：

- **按目标查看**（status）：对每个注册目标（agent 用户级 / 项目级目录）列出：
  - 已部署 skill 明细：名称、中文名/用途（enrich 后）、指向的库内路径、链接模式（symlink/junction/copy）、内容状态（最新/漂移）；
  - 未管理条目：孤儿 skill、外部手工创建的软链接（显示其指向路径）；
  - 汇总行：已部署数 / 未管理数 / 断链数。
- **按 skill 查看**：反向视图——该 skill 派发到了哪些目标、各是什么模式；未派发任何目标时提示。
- **矩阵视图**：skills × targets 总览表，一眼看清全局分布，支持按标签过滤。
- **项目维度统计**：目标按用户级 / 项目级分组展示；项目 → 其下各 agent 目标 → 明细逐级展开，并给出项目级汇总（期望配置 vs 实际，见 3.6）。
- 展示同时覆盖本工具管理的部署与外部创建的链接/拷贝，外部链接提示可纳管。

### 3.6 项目配置文件（P1）

- 项目根目录生成 `skillhub.yaml`（可提交 git）：声明该项目需要的 skills（按名称 / 标签 / bundle）及使用的 agent 目录集合（派发时选择过的 agent 目录自动记录于此）。
- **一键装配**：新成员克隆项目后执行一条命令，按配置从库派发全部 skill 到项目的各 agent 目录（无库环境时提示先初始化，或按配置内来源信息自动安装，见开放问题 8）。
- 装配采用 reconcile 语义：多退少补对齐配置；`--prune` 才删除多余。
- 状态视图按项目配置分组对比（期望 vs 实际），是团队环境一致性的保障。

### 3.7 添加源安装（P1）

支持三类来源，安装目标统一为 **library**（不直接装进 agent 目录，再由派发功能分发——避免绕过集中管理）：

1. **skills.sh**：包装其官方 CLI（`npx skills add <owner>/<repo>` 形式，开源于 vercel-labs/skills），安装目录指向 library 对应分区；manifest 记录 `owner/repo`、安装时间、内容 hash。
2. **Git 仓库 / 子目录**：给定仓库 URL（可选子路径），克隆或稀疏检出对应 skill 目录入 library。
3. **本地路径**：从任意本地目录导入 skill。

- 安装后可顺手提示打标签 + 选择派发目标。
- 来源信息构成后续升级的依据（provenance）。

### 3.8 升级（P1）

- **单个升级** / **一键全部升级**。
- 升级流程（对每个 skill）：
  1. 依据 manifest 中的 provenance 拉取最新版本；
  2. **漂移检测**：若 library 内版本相对安装时被本地修改过（hash 不符）→ 提示冲突（保留本地 / 覆盖 / 手动合并），不静默覆盖；
  3. **升级前备份**旧版本（保留最近 N 份，可回滚）；
  4. 更新后，symlink / junction 部署点自动生效；copy 部署点自动重推（用户级与项目级目标一致处理）。
- **上游版本标识（A5）**：安装 / 升级时记录上游版本（commit hash / tag / release），status 中展示本地版本 vs 上游最新。
- **升级锁定名单（A5）**：可将指定 skill 锁定在当前版本，不参与"一键全部升级"；锁定项在 dry-run 报告中标注。
- 无法确定来源（`local` 收编）的 skill：跳过并提示"无远程来源，仅本地管理"。
- 全程 dry-run：先列出"可升级 / 已最新 / 有漂移 / 无来源"清单。

### 3.9 体检与维护 doctor（P0 基础 / P1 扩展）

- **断链检测**（P0）：symlink / junction 指向不存在的目标（library 被移动、skill 被删）。
- **孤儿检测**（P0）：目标目录中存在但 manifest 未记录的 skill 目录。
- **漂移检测**（P1）：copy 模式部署点两端 hash 不一致；library 内 skill 相对安装 hash 被修改。
- **依赖失效检测**（P1，A3）：元数据声明的外部依赖（命令 / 运行时）在本机或目标端已不存在。
- **重复拷贝检测**（P0）：目标目录中与库内内容一致的实体目录（可去重收编）。
- **库迁移修复**（P1）：library 路径变更后，一键重建所有链接（junction 绝对路径失效场景的主修复手段）。
- doctor 默认只诊断报告，`--fix` 才执行修复。

### 3.10 移除与卸载（P1）

- 从某目标**取消部署**（undeploy，用户级 / 项目级通用）：移除链接；copy 模式移除拷贝；不动 library。
- **从库中移除** skill：先要求处理所有部署点（级联取消），再删除（入备份），manifest 同步清理。
- 删除均进 dry-run / 确认流程，且可撤销（回收站 / 备份目录机制）。

### 3.11 校验与安全（lint P1 / review P2）

- **lint 静态校验（P1，A2）**：
  - 检查项：SKILL.md 语法与结构完整性、工具侧元数据完整性、skill 内部引用路径合法性、依赖声明存在性、**敏感信息检测**（密钥 / 密码 / token / 内网地址）；
  - 触发时机：收编 / 安装 / 升级后自动执行，也可手动对库或单个 skill 运行；
  - 结果处理：问题分级（错误 / 警告 / 提示）输出报告，默认不阻断，`--strict` 下错误项阻断入库。
- **review 安全审查（P2）**：对（尤其来自第三方的）skill 静态扫描：高亮其中的 bash/网络/写文件等敏感指令与"prompt injection"可疑模式，输出报告，供安装/升级前人工过目。不做拦截，只做提示。

### 3.12 导入导出与备份（P1，A4）

- **单 skill 导出**：打包为 `.zip`（skill 文件 + 工具侧元数据），用于分享；接收方一键导入入库（同名冲突走 3.2 冲突流程，自动按 hash 判重合并）。
- **本地导入**：从任意 zip / 目录导入库。
- **整库备份与恢复**：一键导出完整存档（skills + manifest + 元数据 + git 历史 bundle），新机器一键恢复；导出支持**脱敏选项**（剥离个人路径、备注等隐私字段）。
- 与 git 的分工：git 承担版本历史（B4），整库包承担跨机迁移与冷备份。

### 3.13 桌面版与看板（P1）

**产品形态**：工具最终发布为**桌面版应用**（Windows 优先），页签式界面；架构上**内核与 UI 分离**——库管理引擎（收编 / 派发 / 升级 / doctor / 检索等全部核心逻辑）为独立模块，桌面 UI 为其壳，保证逻辑单源、后续可扩展其它形态（CLI / 其它前端）。

**初始页签规划**（可迭代调整）：

| 页签 | 职责 |
|---|---|
| 看板（Dashboard） | 只读总览与分析，支持下钻跳转（见下） |
| Skills 列表 | 全量明细表：筛选 / 搜索 / 打标 / 派发 / 同步 / 升级等全部操作入口 |
| 目标管理 | Agent / 项目目标的注册、探测、reconcile |
| 健康中心 | doctor 结果、问题清单、一键修复 |
| 设置 | LLM 接入、标签词表、链接策略、忽略名单等 |

**看板设计**（只读为主 + 下钻跳转）：

1. **总览卡片**：skill 总数 / 标签数 / 部署点总数 / 库体积 / 待处理问题数（下钻健康中心）;
2. **可优化提示**：重复拷贝可节省空间、未派发 skill 数、可升级数——把工具价值变成用户看得懂的数字；
3. **分布分析**：标签 / 来源 / 状态 / 通道 / 链接模式分布图；
4. **Agent × Skill 矩阵**：部署全景（用户级与项目级分组），标记重复派发与未派发项；
5. **动态时间线**：最近操作记录与 skill 数量增长趋势（源自 manifest 记录 + git 历史）。

- **下钻交互（核心模式）**：看板为各筛选维度的聚合展示，点击任意图表元素（如某标签、某 agent、某状态）→ **跳转到 Skills 列表页签并自动应用对应筛选**；列表页内完成打标 / 派发等操作（操作仍走同一引擎 + dry-run 确认）。
- **数据同源**：看板数据 = manifest + 文件系统实时扫描（与 status / doctor 同源），不引入第二套事实；完全本地离线，不联网。

---

## 4. 非功能需求

| # | 需求 | 说明 |
|---|---|---|
| N1 | Windows 优先兼容 | 开发与主要使用环境为 Windows（Git Bash / PowerShell）；同时兼容 macOS / Linux（symlink 原生可用） |
| N2 | 防扫描保障 | library 路径校验（2.1）为硬性功能，非可选项 |
| N3 | 破坏性操作安全 | 移动、删除、覆盖、升级均默认 dry-run 预览 → 确认执行；删除可撤销 |
| N4 | 幂等可重入 | 重复执行同一命令结果一致；中断后重跑不产生半成品状态（manifest 原子更新） |
| N5 | 操作可追溯 | manifest 记录每次收编 / 派发 / 升级的时间与动作摘要；升级保留历史版本备份 |
| N6 | 规模无压力 | 数十至数百 skill 规模，常规操作秒级完成，无性能优化诉求 |
| N7 | 中文界面 | 面向个人使用，CLI 输出以中文为主（可再议） |
| N8 | 普通用户权限 | 全功能普通用户权限可用：symlink 特权失败自动降级 junction / copy，**绝不要求提权运行** |
| N9 | 最小作用域 | 只读写"库目录 + 已注册目标目录"，扫描范围固定且可在配置中查看，**绝不遍历全盘** |
| N10 | 链接安全删除 | 删除部署点只删链接本体（不跟随链接），**绝不递归删除链接目标内容** |
| N11 | 离线可用 | 状态 / 检索 / 派发 / 收编 / doctor 全部离线可用；仅安装 / 升级 / enrich 需要网络 |
| N12 | 命令执行透明 | 调用外部命令（npx / git）前完整明示；工具自身**永不执行 skill 内代码** |

### 4.1 权限模型（Windows 优先）

| 操作 | 权限需求 | 失败场景与处理 |
|---|---|---|
| 扫描 / 状态 / 检索 | 目录读权限 | 不可读目录跳过并列出 |
| symlink 派发 | Windows：开发者模式或管理员；macOS / Linux：普通用户 | 失败自动降级 junction（Windows）→ copy |
| junction 派发 | Windows 普通用户免特权（仅本机卷，可跨盘符） | 网络路径不支持 → copy |
| copy 派发 / 收编 / 删除 | 相关目录写权限（用户自有目录通常无碍） | 系统保护 / 只读目录 → 报告并跳过 |
| 安装 / 升级 | 库写权限 + 网络 | 离线时提示 |

已知权限风险场景与对策：

1. 开发者模式未开启且非管理员（symlink 失败）：init 时检测并给出**开启开发者模式的指引**，当次自动降级 junction / copy；
2. 企业组策略禁用符号链接：降级链兜底（同上）；
3. **库位于云同步盘**（OneDrive / 坚果云等）：占位文件与文件锁干扰链接与 hash 计算，init 检测并警告，建议本地磁盘；
4. 跨盘符 / 网络位置：junction 仅本机卷（可跨盘符）、不支持网络路径；网络路径仅 symlink（macOS/Linux）或 copy，部署报告明示；
5. Windows 长路径（>260 字符）：启用长路径 API，避免深层嵌套失败；
6. 管理员权限创建的历史目录普通权限无法修改：报告并提示；
7. 安全软件对批量创建链接的误报：文档说明工具行为特征，必要时提供白名单指引。

### 4.2 安全设计

1. **最小作用域**（N9）：一切读写限于库 + 注册目标；扫描范围在配置中明示、可修改。
2. **删除三重防护**（N10）：lstat 识别链接本体、只删链接不跟随；"原件替换为链接"先入库校验 hash 再删原件；删除进回收站 / 备份目录可撤销。
3. **执行面收敛**（N12）：工具只做文件操作，永不执行 skill 代码；外部命令仅 `npx skills`（skills.sh 官方 CLI，信任由其官方承担，执行前完整展示命令请用户确认）与 `git`。
4. **内容安全**：lint 敏感信息检测（P1）+ review 注入扫描（P2），见 3.11。
5. **网络收敛**（N11）：仅安装 / 升级 / enrich 联网。
6. **数据保护**：manifest 与元数据不存任何凭证；LLM API key 仅存于**用户配置目录**（不进 library、不进项目配置、不随整库导出、不入 git），优先存系统凭据库（Windows 凭据管理器 / macOS Keychain），降级为本地配置文件 + 文件权限保护；整库导出支持脱敏；库 git 远端由用户自行配置，工具**不自动推送远程**。

### 4.3 产品级易用性要求

> 产品形态：桌面版应用为主（见 3.13），以下要求同时适用于桌面 UI 与命令行。

1. **开箱即用上手**：首次启动引导式向导（选库位置 → 探测 agent → 勾选目标 → 首次扫描收编 → 完成报告），不读文档即可完成首次配置；
2. **默认智能**：能自动探测的不询问、问过一次的记住（见 3.4 项目级交互）；危险操作默认 dry-run + 明确 diff 预览；
3. **反馈清晰**：每个操作输出进度与结果汇总报告（做了什么、改了哪些文件），可复查；
4. **错误信息可操作**：失败原因 + 下一步建议，如"symlink 创建失败：请开启 Windows 开发者模式（设置 → 隐私和安全性 → 开发者选项），本次已自动降级为 junction"；
5. **帮助体系**：全中文界面与提示、内置使用引导；健康中心 / doctor 报告附可执行/可复制的修复动作；
6. **环境降级与普通用户零门槛**：目标用户包含非开发者（如日常办公人群），**不假设用户装有 git、更不要求懂 git**——版本历史功能对用户完全无感（工具自动提交，见 3.1 三层策略），且全部为本地操作，**不涉及任何远程仓库 / 账号**；LLM 不接入（enrich / LLM 打标等依赖功能调用时明确提示并给出配置入口，其余功能全部可用）、无网（本地功能全可用）均不影响核心功能；
7. **选型约束**：以上构成技术选型硬约束——桌面应用框架（Windows 优先）+ 内核 UI 分离架构；交互式向导 / 多选勾选 / 进度显示由 GUI 天然满足；是否同步提供 CLI 由开放问题 1 决定。

---

## 5. 边界与冲突场景（行为定义）

1. **同名不同内容**（收编 / 部署 / 升级均可能触发）：永不静默覆盖，必须走冲突流程（对比 → 选择 / 并存 `name@origin`）。
2. **项目级与用户级同名 skill 同时部署**：两者都允许（agent 运行时按项目级优先的惯例解析）；工具在状态视图中并列展示并提示。
3. **library 被移动 / 重命名**：所有 symlink / junction 失效 → doctor 检出 + "重建全部链接"修复；修复前禁止执行依赖链接的其它写操作（快速失败）。
4. **目标目录由其它工具写入**（如用户手动放了新 skill、或 skills.sh CLI 直接安装到 agent 目录）：扫描 / doctor 检出为孤儿，提示收编或忽略（可标记忽略名单）。
5. **同一 skill 重复派发到同一目标**：幂等跳过。
6. **skill 目录无 SKILL.md**：扫描发现时标记"疑似非 skill"，默认不收编，人工确认。
7. **跨盘符 / 网络盘**：junction 与 symlink 的降级判断需覆盖此场景，最终可能落到 copy，须在部署报告中明示模式与含义。
8. **同一项目内多目录重复加载**：项目同时使用多个标准目录（如 `.claude/skills` 与 `.agents/skills`）且同一 skill 派发到多处时，可能被同一 agent 重复加载：派发时警告，并推荐项目内统一使用单一目录（建议跨 agent 标准的 `.agents/skills`）。

---

## 6. 非目标（Non-Goals）

- **不做** skill 内容编辑器 / 生成器（skill 开发交给 skill-creator 等既有工具）。
- **不做**多版本并存（同一 skill 多版本按项目挂载；"一 skill 一实体"的简单性优先，B3 已决策，远期再议）。
- **不做** 远程 / 多机同步（单机工具；云端备份非本期目标）。
- **不做** 运行时拦截（不 hook agent 的加载行为，只管理文件层）。
- **不做** skill 市场 / 发布（只消费 skills.sh、git 等现有源，不自建分发平台）。
- **不修改** 任何 skill 本体文件（含 SKILL.md frontmatter）——标签、元数据一律只在工具侧。

---

## 7. 开放问题（待后续讨论）

1. **技术栈与产品形态（更新）**：已确定最终发布为桌面版（见 3.13），需选定：桌面框架（Tauri / Electron / 原生等）+ 内核语言；内核与 UI 分层方案；是否同步提供 CLI（开发者和自动化场景）；加分项评估：内核若选 Node.js 可内置纯 JS git 引擎（isomorphic-git），让未装 git 的普通用户零安装获得完整版本历史（见 3.1 三层策略）。
2. **manifest 格式**：YAML vs JSON（影响可读性 / 手工编辑友好度）。
3. ~~library 是否用 git 管理~~ **已决策（B4）**：init 初始化为 git 仓库，工具自动 commit，版本历史 / 回滚 / diff 由 git 承担（见 3.1）。
4. **bundle 的定义方式**：纯手工列举 vs 标签表达式（如 `tag:pm OR name:docx`）。
5. **升级备份的保留策略**：份数 / 期限。
6. 是否需要**忽略名单**（ignore list）的持久化配置项（5.4 提及）。
7. ~~enrich 的 LLM 接入方式~~ **已决策**：接入完全可选；常用厂商预设 + 自定义（OpenAI 兼容 base URL + key + 模型名），见 3.1；未接入时依赖功能明确提示。
8. **项目配置文件**：文件名与位置（`skillhub.yaml` vs `.skillhub/` 内）；是否允许配置中内嵌来源信息，让无库的新成员直接按来源安装。
9. **格式自动转换（B1，暂缓）**：仅当 §2.2 调研确认存在 SKILL.md 格式不兼容的适配 agent 时，作为 copy 模式变体设计转换器；调研无此需求则永久搁置。

---

## 8. 功能优先级总览

| 优先级 | 功能 |
|---|---|
| P0 | 初始化与目标注册（git 库）｜扫描发现与收编（含冲突处理）｜标签与元数据管理（含通道标签）｜派发与同步（三级降级 + reconcile + 依赖预检）｜状态展示与基础筛选｜doctor 基础（断链 / 孤儿 / 重复） |
| P1 | 桌面版与看板（页签架构 + 下钻交互）｜全文检索｜enrich 中文元数据生成｜项目配置文件与一键装配｜添加源安装（skills.sh / git / 本地）｜升级（版本标识 / 锁定 / 漂移检测 / 备份）｜lint 校验｜导入导出与整库备份恢复｜doctor 扩展（漂移 / 依赖失效 / 库迁移修复）｜移除与卸载 |
| P2 | 安全审查 review｜使用统计｜bundle 标签表达式｜格式转换（条件触发，见开放问题 9）｜其它增值 |
