# SkillHub 需求文档

> 文档状态：功能范围基线已确认
>
> 基线日期：2026-08-20
>
> 当前阶段：功能需求范围确认完成；功能优先级、产品设计、技术设计与实现计划尚未开始
>
> 文档原则：本文是当前唯一有效的需求来源。具体技术机制与界面布局只有在明确标注为“已确认需求”时才构成约束。

---

## 1. 产品背景

### 1.1 问题背景

Agent Skill 通常分散在不同 Agent 的全局目录、项目目录、插件目录和其他工具管理目录中。个人用户常见的管理困难包括：

1. 同一个 Skill 在多个位置重复存在，来源和内容不一致。
2. 无法快速知道某个 Agent 或项目最终会使用哪个 Skill。
3. 升级、自定义修改、回滚和项目版本固定依赖人工处理。
4. Agent 内置、插件安装和用户自行导入的 Skill 容易产生同名或功能重复。
5. 删除或迁移 Skill 时，难以判断哪些 Agent、项目和组合仍在使用。
6. 普通用户不理解 Git、软链接、运行目录等技术概念，也可能没有安装相关工具。
7. 多个平台缺少统一的本地搜索、分类、验证、风险提示、备份和恢复入口。

### 1.2 产品目标

SkillHub 是面向个人用户的本地 Agent Skill 全生命周期管理工具，目标是：

- 使用一个集中库统一保存和管理 Skill。
- 统一查看 Skill、Agent、项目、版本、来源和部署关系。
- 支持发现、导入、创建、分类、部署、升级、验证、回滚、移除、备份和迁移。
- 让普通用户通过桌面界面完成核心操作，不要求理解或安装 Git。
- 对所有重要操作提供影响预览、可操作的失败说明和恢复能力。
- 在不同 Agent 平台能力不一致的情况下，明确展示支持范围，不伪造平台能力。

### 1.3 目标用户

- 使用一个或多个 Agent 的个人用户。
- 需要在多个 Agent 或多个项目之间管理 Skill 的开发者。
- 不熟悉 Git、命令行、软链接和 Agent 目录结构的普通用户。
- 希望本地保存数据，不依赖 SkillHub 账号或远程服务的用户。

---

## 2. 产品范围

### 2.1 产品形态

- 桌面应用是主要产品入口。
- 提供轻量 CLI，用于列表、状态、扫描、项目装配、部署、解除部署、对齐、升级和健康检查等自动化场景。
- CLI 不追求与桌面端完全等价。
- 桌面端与 CLI 应使用相同的功能规则和数据，不形成两套管理逻辑。

### 2.2 操作系统

- 正式支持 Windows 和 macOS。
- Windows 是首要体验与验证基准平台。
- Windows 与 macOS 的核心功能目标一致，但允许因操作系统或 Agent 能力不同而存在明确差异。
- Linux、Web 和移动端当前不纳入支持范围。
- 备份、标准导出和项目配置应支持 Windows 与 macOS 之间迁移。

### 2.3 界面语言

- 产品具备原生国际化能力。
- 内置简体中文和英文两种完整界面语言。
- 首次启动默认跟随系统语言，用户可以手动切换。
- Skill 名称、Agent 名称、命令、字段、文件内容和原始错误保持原文，不因切换界面语言而修改。
- 用户填写的别名、说明和备注不自动翻译。
- LLM 生成内容的语言可以独立于界面语言进行选择。

### 2.4 数据与协作边界

- 产品以个人本地管理为核心。
- 不提供 SkillHub 产品账号、角色权限、远程协作服务器或团队管理后台。
- 项目配置可用于个人多项目管理，也可由项目成员共享，但不扩展为团队管理系统。
- 不默认上传 Skill、项目、使用记录、操作历史或诊断数据。

### 2.5 使用规模

- 面向几十到数百个 Skill 的个人本地管理场景。
- 主要设计规模为 100 至 500 个 Skill。
- 不以企业级大规模资产管理为目标。

---

## 3. 核心概念

| 概念 | 定义 |
|---|---|
| 集中库 | 用户选定的唯一 Skill 管理库，保存 Skill 内容、版本和管理关系。 |
| Skill | 可被 Agent 识别和调用的技能目录或技能包。 |
| Agent | 可以加载或调用 Skill 的平台。 |
| 项目 | 用户注册到 SkillHub、需要项目级 Skill 管理的本地项目目录。 |
| 目标 | 某个 Agent 的全局 Skill 位置或某个项目中的 Agent Skill 位置。 |
| 部署 | 让指定 Skill 版本可被指定 Agent 或项目使用。面向普通用户称为“添加到 Agent/项目”。 |
| 解除部署 | 移除 SkillHub 管理的目标关系，不等于删除集中库中的 Skill。 |
| 来源 | Skill 的获取位置和身份信息，例如在线来源、本地目录或用户创建。 |
| 所有权 | SkillHub 对对象允许执行的管理范围。 |
| 当前版本 | 集中库中默认使用的 Skill 版本。 |
| 历史版本 | 为对比、回滚或项目固定而保留的只读版本。 |
| 项目固定 | 项目明确使用某个指定版本，不随集中库当前版本自动变化。 |
| 期望状态 | SkillHub 记录的部署、固定版本、策略和关系。 |
| 实际状态 | 文件系统、Agent 目录和项目目录中观察到的真实内容。 |
| 有效状态 | 综合 Agent 层级规则后，最终可能被 Agent 使用的 Skill 状态。 |
| 组合 | 用户命名的一组明确 Skill，用于批量查看或部署。 |
| 待处理事项 | 需要用户关注或选择处理方式的问题。 |
| 功能验证 | 用户对某个 Skill 版本在某个 Agent 上是否可用的人工记录。 |

---

## 4. 全局产品规则

### 4.1 单一集中库

- 一个 SkillHub 实例只管理一个集中库。
- 不提供多个集中库之间的切换、合并或同步。
- 用户可以迁移集中库位置。
- 集中库被移动、重命名或暂时不可访问时，应暂停依赖集中库的写操作，并提供选择新位置、重新关联和修复受管关系的入口。
- 集中库可以使用自定义路径，但不能与任何已知 Agent 目录或已注册目标发生包含或重叠关系。
- 检测到路径重叠时必须阻止，不能通过“强制继续”绕过。
- 新增 Agent 或项目目标时，也必须重新检查与集中库的路径关系。

### 4.2 云同步目录

- 允许用户将集中库放在 OneDrive、iCloud Drive、Dropbox 等云同步目录。
- SkillHub 只按本地文件管理，不提供自己的云同步能力。
- 发现仅在线占位文件时，应暂停相关操作并说明原因。
- 不承诺多台设备同时修改集中库时的结果。
- 云服务产生的冲突副本、修改或删除统一进入外部变更处理流程。

### 4.3 双状态模型

- SkillHub 管理记录是期望状态的依据。
- 文件系统和 Agent 目录是实际状态的证据。
- Agent 内置、插件和其他工具管理的 Skill 属于外部观察状态。
- 两者一致时显示正常。
- 缺失、额外、被修改、关联失效或版本不符时显示状态偏差。
- 扫描范围不完整时显示“覆盖范围未知”，不能据此判定对象不存在。
- 对齐和修复不得静默覆盖实际修改。

### 4.4 所有权保护

所有 Skill 分为：

- 用户创建。
- SkillHub 管理。
- 其他工具管理。
- Agent 内置。
- 插件缓存。
- 未知来源。

统一规则：

- 扫描发现默认只读。
- 用户创建或用户明确确认归自己所有的本地 Skill，可以接管管理。
- Agent 内置、插件、其他工具管理和未知来源对象不得自动移动、覆盖或删除。
- 非自有对象只能在用户确认后复制导入为独立 Skill，原件保持不变。
- 非自有对象在界面中明确显示只读原因。

### 4.5 无静默变更

- 不静默覆盖同名内容。
- 不静默替换版本。
- 不静默修改 Skill 语义。
- 不静默传播外部修改。
- 不静默删除集中库、项目目录或非自有内容。
- 重要写操作必须可追踪，并在适用时可撤销或恢复。

### 4.6 网络与隐私

- 核心本地功能在离线状态下可用。
- 提供统一关闭网络的设置。
- 联网操作必须区分在线搜索、来源获取、更新检查、私有来源访问和 LLM 调用。
- 联网前或联网过程中显示原因与目标来源。
- 凭证保存在操作系统安全凭据存储中，不进入集中库、项目配置、备份或标准导出包。
- 不默认上传遥测、崩溃数据、原始会话或使用证据。

### 4.7 技术细节延后

以下内容属于后续设计，不在本阶段确定：

- 集中库的具体记录格式。
- 版本历史的底层实现方式。
- 部署采用链接、复制或其他机制的具体选择。
- 桌面技术栈和 CLI 技术栈。
- 数据库、索引、文件监听和任务调度实现。

---

## 5. 功能需求

### 5.1 首次使用与初始化

- 提供可跳过的短流程首次使用引导。
- 使用普通语言解释“集中保存”和“添加到 Agent/项目”，默认不展示 Git、软链接等底层概念。
- 提供推荐集中库位置，也允许用户自定义。
- 自动进行集中库路径安全检查。
- 自动发现已有的受支持 Agent，并展示发现依据和覆盖范围。
- 只读扫描已有 Skill，按来源、所有权、重复和风险进行分类。
- 不自动导入、移动或部署，必须先由用户预览和选择。
- 支持使用已有集中库或从备份恢复。
- 初始化中断后可继续，不留下无法识别的半完成状态。
- 初始化完成后显示已创建内容、发现结果和待处理事项。

### 5.2 Agent 管理

#### 5.2.1 内置支持 Agent

- 只有经过验证的平台才能列入内置支持名单。
- 内置适配需说明实际支持的能力，例如全局目录、项目目录、层级规则、调用策略和有效状态判断。
- 不能仅因找到目录就宣称完全兼容。
- 平台不支持的能力显示“不支持”。
- 内置适配规则可以随 SkillHub 更新而扩展。

#### 5.2.2 候选 Agent

- 保留待调研的候选 Agent 池。
- 候选状态不构成功能支持承诺。
- 正式内置名单在平台兼容性调研后确认。

#### 5.2.3 自定义 Agent

用户可以创建轻量自定义 Agent：

- 设置显示名称。
- 指定全局 Skill 目录。
- 指定一个或多个项目 Skill 目录。
- 支持扫描、导入、部署、解除部署、外部变更检测和基础关系展示。
- 标记为“未验证自定义目标”。

自定义 Agent 不承诺自动发现、平台格式转换、调用策略映射、自动兼容性判断、功能验证自动化、调用次数采集或完整适配插件能力。

#### 5.2.4 Agent 专用变体

- 一个 Skill 以基础内容为主体，可以包含明确的 Agent 专用变体。
- 变体用于表达不同 Agent 已知且可验证的格式或字段差异。
- 适配只执行明确规则，不自动改写 Skill 的业务语义。
- 生成或更新变体前显示变化，变体随 Skill 版本保存。
- 变体变化后，相关 Agent 的功能验证需要重新确认或标记为过期。
- 具体变体目录、字段和生成机制留到后续设计。

### 5.3 Agent 环境变化

- 应用运行期间或用户手动扫描时，检测受支持 Agent 的新增、卸载、移动或目录变化。
- 新发现 Agent 时提示注册，不自动部署。
- Agent 不可用时提供重新定位、暂时忽略、安全解除部署或移除管理关系。
- 内置适配规则变化时展示影响范围。
- 适配变化后重新检查兼容性，并在必要时将原功能验证标记为过期。
- 不静默重写 Skill 内容。

### 5.4 项目管理

- 用户通过选择项目根目录注册项目。
- 项目可关联一个或多个 Agent。
- 项目记录所需 Skill、精确版本和目标 Agent。
- 项目配置可被导出或随项目共享。
- 项目配置不包含凭证和本机绝对敏感路径。
- 项目移动或失联时标记为不可访问，并提供重新关联、确认可能的新位置、暂时忽略或移除管理关系。
- “从 SkillHub 移除项目”不等于删除项目目录。
- 用户可选择保留项目中的 Skill、解除部署后移除管理关系或取消操作。
- SkillHub 绝不删除项目业务目录。

### 5.5 项目尽力装配

- 根据项目配置装配所需 Skill 和精确版本。
- 新成员或新设备取得项目配置后，可以通过一次引导式操作完成尽力装配，不要求理解 Git 或手工复制目录。
- 优先复用集中库已有版本。
- 缺少版本时尝试从已知来源获取。
- 来源无法访问、版本不存在或认证失败时逐项说明。
- 不使用未声明的替代版本。
- 装配前预览将添加、复用、跳过、冲突和失败的对象。
- 装配后显示期望状态与实际状态差异。
- 不主动检查或安装 Python、ffmpeg、MCP、插件等运行依赖。

### 5.6 本地发现与扫描

- 扫描已注册的 Agent 全局目录、项目目录和用户明确选择的路径。
- 不遍历全盘。
- 展示每个发现对象的位置、名称、来源推测、所有权、内容一致性和关联状态。
- 识别有效 Skill、疑似 Skill、链接、实体副本和无法读取对象。
- 发现无必要文件的目录时标记为“疑似非 Skill”，不自动导入。
- 支持手动重新扫描。
- 支持持久忽略单个精确路径或单个 Skill。
- 忽略规则不支持通配符、表达式和复杂条件。

### 5.7 本地搜索与联网搜索

本地搜索：

- 按名称、显示别名、说明、标签、状态、来源、所有权、Agent、项目和部署状态搜索或筛选。
- 支持搜索 Skill 正文和管理侧元数据。
- 搜索结果显示命中原因或片段。
- 支持组合筛选、排序和批量选择。

联网搜索：

- 与本地搜索明确分开。
- skills.sh 作为首个内置在线搜索来源。
- 未来可以扩展其他明确适配的搜索来源。
- LLM 可用于改写查询、扩展关键词和辅助排序。
- find-skills 不作为核心依赖。
- 不配置 LLM 时仍可使用基础在线搜索。

### 5.8 来源获取

- 支持从本地文件夹、压缩包、仓库链接、Skill 页面链接和受支持私有来源获取。
- 支持常见私有来源的账户连接和认证失败提示。
- 普通用户不需要安装或理解 Git。
- SkillHub 获取的是 Skill 文件及版本信息。
- 不执行网页、README 或 Skill 内容中提供的任意安装脚本或命令。
- 对暂不支持或只能通过特殊命令获取的来源，提示用户先在外部取得文件再导入。
- 不把 SkillHub 扩展为通用命令执行器。

### 5.9 导入、复制与接管

- 导入前进行基本有效性检查。
- 支持单个和批量导入。
- 导入前执行重复和冲突检查。
- 非自有对象采用复制导入，原件保留。
- 用户明确拥有的本地 Skill 可选择接管。
- 接管前展示原位置后续处理方式：成为受管部署、保留独立副本、移除原件或取消。
- 未完成接管时不能删除原件。
- 所有导入记录来源、导入时间和内容身份。

### 5.10 创建与外部编辑

- 支持从空白或最小模板创建 Skill。
- 支持复制现有 Skill 为完全独立 Skill。
- 支持从现有 Skill 创建保留上游关系的自定义分支。
- 创建后形成初始版本记录。
- 完整内容编辑交给外部编辑器、skill-creator 或其他专用工具。
- SkillHub 不发展为完整代码编辑器或 AI Skill 生成平台。
- 外部编辑后的内容通过外部变更流程处理。

### 5.11 来源重新关联

- 对失去来源或来源地址变化的 Skill，支持手动重新关联。
- 重新关联前比较当前内容与候选来源。
- 关联后保留原来源历史。
- 不自动将相似内容认定为同一来源。
- 不提供多个上游来源镜像或自动切换。

### 5.12 元数据与组织

- 支持显示别名、说明、标签、备注、相关 Skill 和用户自定义信息。
- 支持预置标签和自定义标签。
- 标签支持创建、重命名、删除、单个设置和批量设置。
- 删除标签只移除标签关系，不删除 Skill。
- 支持轻量组合的创建、编辑、复制和删除。
- 组合只保存明确 Skill 列表。
- 组合不支持标签表达式、嵌套、目标绑定和版本锁定。
- 支持查看 Skill 的文件数和体积。
- 体积异常时提示，但不提供独立磁盘清理中心。

### 5.13 生命周期状态

Skill 生命周期只有正常、弃用和归档。

- 弃用可设置推荐替代 Skill。
- 归档默认从日常列表隐藏，但可搜索和恢复。
- “试用”只作为可选标签，不是强制状态。
- 生命周期与验证、安全、兼容、版本和部署状态相互独立。

### 5.14 来源、作者与许可证

- 按版本保存来源地址、作者、导入时间、许可证和版权文件。
- 许可证变化或缺失时提示。
- 复制、派生和导出时保留来源及许可证。
- 修改过的 Skill 在导出时标明已修改。
- 不提供法律意见或许可证兼容性审计。

### 5.15 重复与冲突检查

检查范围包括当前导入批次、集中库、目标 Agent、Agent 全局与项目层级，以及可发现的内置、插件和其他工具 Skill。

- 内容完全一致时可跳过、去重或建立管理关联。
- 同名但内容不同时必须进入冲突处理，不能静默覆盖。
- 不同名称但功能相似时只提示，可由用户继续。
- 语义重复分析可由可选 LLM 增强。
- LLM 不自动合并、删除或弃用 Skill。
- 内置 Skill 只读，不能覆盖。
- 对无法发现的 Agent 内部能力显示检查覆盖范围。
- 集中库可保存同名、不同来源的 Skill，并以来源或内部身份区分。
- “名称加来源”只用于界面辨识，不自动改变 Agent 运行时名称。

部署同名冲突时提供使用现有内容、移除可管理的现有内容、改用其他目标、复制并实际重命名、仅保留在集中库或取消操作。

### 5.16 版本历史与差异

- 每个 Skill 保存当前版本和历史版本。
- 版本记录时间、来源、变更说明和内容身份。
- 支持任意历史版本之间对比。
- 支持标记已人工验证或认可的版本。
- 版本历史只增不减；回滚本身形成新的当前版本。
- 删除后的 Skill 可在恢复能力覆盖范围内找回。
- 底层是否使用 Git 不构成用户功能差异。

### 5.17 单 Skill 回滚范围

回滚恢复与版本绑定的 Skill 文件内容、Agent 特定变体、来源与许可证、运行要求说明、兼容性信息和该版本生成或保存的内容说明。

回滚不恢复标签、显示别名、生命周期、备注、自动升级策略、部署、项目固定、使用证据和忽略设置。

- 安全、验证和信任记录保留历史关联。
- 旧验证结果显示原验证日期。
- Agent 或环境发生变化时标记为过期。
- 管理状态变化通过操作历史与整次操作撤销处理。

### 5.18 当前版本与项目固定

- 集中库中的当前版本是未固定部署的默认版本。
- 项目可固定到一个只读历史版本。
- 项目固定优先于全局和单 Skill 自动升级策略。
- 项目解除固定后可选择跟随当前版本。
- 不提供任意多个全局当前版本。

### 5.19 上游更新与本地自定义

- 支持检查单个或多个 Skill 的上游更新。
- 显示可更新、已最新、无来源、来源不可访问、存在本地修改和升级受阻。
- 上游跟踪 Skill 被本地修改后，升级时比较原始基线、本地修改和上游变化。
- 无冲突内容可合并，冲突内容必须人工处理。
- 保留来源关系和自定义历史。
- 不静默覆盖本地修改。
- 不自动进行语义重写。

### 5.20 自动检查与自动升级

- 自动检查更新与自动升级是两个独立设置。
- 默认行为为自动检查并通知，不自动升级。
- 支持全局默认、单个 Skill 覆盖和批量修改。
- 优先级为：项目固定或锁定高于单 Skill 设置，单 Skill 设置高于全局设置。
- 仅在应用运行期间检查和处理，不创建后台常驻服务或托盘进程。
- 升级前创建恢复点。
- 升级后将相关功能验证标记为需要重新确认或过期。

来源身份变化、高风险、合并冲突、版本或来源不存在、不兼容、认证失败、Skill 弃用或归档、项目固定版本等情况必须阻止自动升级。

### 5.21 Skill 重命名

- 所有 Skill 都可以设置显示别名。
- 只有用户创建或完全独立的 Skill 可以实际重命名。
- 上游跟踪 Skill 必须先复制或派生，才能实际重命名。
- 重命名前展示对项目、组合、部署和关联关系的影响。
- SkillHub 管理的引用可以在用户确认后更新。
- 重命名操作可撤销。

### 5.22 部署

- 支持将单个 Skill、多个 Skill 或轻量组合添加到 Agent 或项目。
- 指定部署版本。
- 未固定部署跟随集中库当前版本。
- 项目固定部署保持指定版本。
- 重复执行同一部署操作应保持结果一致。
- 部署前展示目标、版本、冲突、覆盖范围和将发生的变化。
- 部署不等于 Skill 已被调用、功能验证通过或安全。
- 具体文件机制留到技术设计阶段。

### 5.23 有效 Skill 视图

- 按 Agent 和项目合并展示全局、项目、内置、插件及其他工具 Skill。
- 展示名称、来源层级、版本和所有权。
- 对已知平台规则标记有效、被遮蔽或可共存。
- 平台规则未知时标记为无法判断。
- 部署前预览部署后的有效状态变化。
- 自定义 Agent 只提示可能共存或冲突，不声称知道优先级。

### 5.24 调用策略

统一展示自动加手动、仅手动、仅自动或对用户隐藏三类状态。

- 常规设置支持在“自动加手动”和“仅手动”之间切换。
- 支持恢复 Skill 原始设置。
- “仅自动或隐藏”状态可以识别和展示，但不承诺所有平台都能统一设置。
- 只在受支持 Agent 上提供对应修改入口。
- 平台不支持时显示“不支持”。
- 修改必须预览、可恢复，并在必要时重新验证。
- 具体字段保存方式和 Agent 变体结构后续设计。

### 5.25 运行要求说明与兼容性

- 展示 Skill 明确声明的 Python、ffmpeg、MCP、插件、环境变量等运行要求。
- 系统推测出的要求必须标记为“可能需要”。
- 用户可以补充或修正运行要求说明。
- 环境变量只记录名称和用途，不保存值。
- 项目装配时列出运行要求，但不主动验证或安装。
- Agent 在实际调用 Skill 时自行处理其运行依赖。
- 兼容性独立判断 Agent 最低版本、操作系统和必要模型能力。
- 不因未检查运行依赖而阻止部署。

### 5.26 功能验证与轻量隔离试用

- 功能验证按“Skill 版本 × Agent”记录。
- 状态包括未验证、通过、失败和已过期。
- 验证由用户人工确认，不自动运行 Agent 或自动评分。
- 记录验证时间、简短备注和失败原因。
- Agent、适配规则、版本或关键环境变化后可标记为过期。
- 支持建立临时验证副本或临时测试目标，测试结束后可清理。
- 轻量隔离试用不形成第二个集中库。
- Skill 在验证前仍可被项目或 Agent 使用，是否使用由用户决定。

### 5.27 基础安全检查

基础安全检查只聚焦危险命令或明显危险行为特征，以及疑似 API Key、Token、密码、私钥、内网地址等敏感信息。

- 检查只读取内容，不执行 Skill 代码或命令。
- 显示命中位置和原因，不直接断言恶意。
- 普通风险只提示。
- 高风险不允许静默处理或批量部署，需要逐个确认。
- 强制继续只允许明确的本地使用场景，并需要逐个确认；不能借此覆盖内置或非自有内容。
- 导出时发现疑似敏感信息默认暂停，用户可返回处理或明确强制导出。
- 系统不擅自修改风险内容。
- 导入、升级、部署和导出可以触发基础安全检查，也支持用户手动检查。

不包含病毒扫描、动态沙箱执行、完整恶意代码分析、供应链审计、签名认证、合规审查和法律判断。

### 5.28 AI 全面诊断

- 由用户主动发起，只有配置 LLM 后可用。
- 可分析安全意图、数据外传、提示词注入风险、指令清晰度、缺失步骤、内容矛盾、权限要求、兼容性和改进建议。
- AI 只读取和分析内容，不执行代码。
- 使用在线模型前说明将发送哪些文件或内容。
- 使用本地模型时不上传内容。
- 结果只作辅助判断，不能标记为“已证明安全”。
- 结果关联 Skill 版本、诊断时间和使用模型。
- Skill 版本变化后将旧诊断标记为过期。
- AI 诊断与系统诊断日志报告不是同一功能。

### 5.29 使用反馈与使用证据

- 用户可以选择性记录“用过”“有帮助”“没有效果”等轻量反馈。
- 反馈可以关联 Skill、版本、Agent 和项目。
- 不要求用户每次调用后记录。
- 在 Agent 提供可靠记录或用户安装关联 Hook 时，可收集 Skill 调用事件。
- 不自动解析原始会话内容。
- 不把不支持记录的 Agent 显示为调用次数零。
- 显示数据来源、统计时间范围和覆盖程度。

### 5.30 Agent 全局 Skill 使用证据分析

- 针对某个 Agent 的全局 Skill 提供保留、移入项目或归档建议。
- 默认分析时间窗口为 60 天，并允许用户调整。
- 只有覆盖程度足够时才根据阈值给出低使用建议。
- 没有可靠数据时显示“无法判断”。
- LLM 可用于解释建议，但不负责生成调用次数。
- 建议不自动执行删除、归档或移出全局。
- 分析依据主要在 Agent 分析入口展示。
- 单 Skill 详情仅在存在证据时显示摘要。
- 默认阈值的最终数值需在后续阶段结合真实数据验证。

### 5.31 外部变更检测

- 应用运行期间或手动扫描时，检测集中库和受管部署中的修改、删除、移动和损坏。
- 外部变更先形成待处理变更，不直接成为正式版本。
- 显示内容差异和影响范围，并暂停相关自动升级和传播。
- 对集中库变更提供接受为新版本、恢复、暂缓和精确忽略。
- 在用户决定前保留临时安全副本。
- 对受管部署修改提供手动收集、恢复、转为独立副本、暂缓或解除部署。
- 非自有对象只读观察。
- 只有正式接受的新版本才更新未固定部署，项目固定版本不受影响。
- 不提供实时双向同步。

### 5.32 手动收集部署修改

- 当受管部署副本被外部修改时，用户可手动比较并收集回集中库。
- 收集后形成新版本。
- 收集前显示受影响的其他 Agent 和项目。
- 不自动把目标目录修改同步回集中库。
- 不对非自有对象提供收集。

### 5.33 待处理事项中心

统一汇总升级阻断、功能验证过期、安全风险、兼容性问题、同名或层级冲突、合并冲突、外部变更、Agent 不可用、集中库健康问题、备份失败以及项目装配或批量操作失败。

每项包含涉及对象、原因、影响和可执行操作。

- 支持处理、暂缓和对已知问题进行精确忽略。
- 支持按类型、Agent、项目和风险筛选。
- 支持对安全项目批量处理，高风险项目逐项确认。
- 问题解决后自动更新状态，历史保留。
- 不扩展为通用任务管理系统、后台通知服务或托盘提醒中心。

### 5.34 集中库健康检查与修复

- 检查文件缺失或损坏、无效来源和历史版本、失效部署、项目固定关系、未完成操作、孤立管理记录和孤立文件，以及 Agent 或项目目标失联。
- 修复前预览。
- 明确安全的问题可自动修复。
- 需要判断的问题逐项处理。
- 未知文件不自动删除。
- 桌面端和 CLI 提供相同健康检查能力。
- 不提供独立的诊断日志打包或系统诊断报告导出。

### 5.35 操作历史与撤销

- 记录重要的导入、接管、部署、解除部署、升级、回滚、重命名、删除、恢复和项目装配操作。
- 显示时间、对象、影响范围和结果。
- 操作历史与单 Skill 版本历史分开。
- 支持对满足条件的整次管理操作进行撤销。
- 不提供整个集中库任意时间点的全局回溯。
- 部分失败需要准确记录已完成、已跳过和失败对象。

### 5.36 解除部署与安全脱离

- 支持从单个或全部目标解除部署。
- 解除部署不删除集中库 Skill。
- 用户可选择移除受管部署、把目标内容转换为独立副本，或在明确警告后保留未跟踪内容。
- 操作前展示受影响的 Agent、项目和固定版本。

### 5.37 删除 Skill

删除前检查 Agent 和项目部署、项目配置和固定版本、轻量组合、声明依赖、相关 Skill 和未知外部引用。

用户可选择归档、解除选定或全部部署、把已部署内容保留为独立副本、替换或移除引用及依赖，或者取消删除。

- 不进行级联删除。
- 未知和非自有内容不修改。
- 删除前重新检查状态。
- 整体操作建立恢复点。
- 部分处理失败时不删除集中库原件。

### 5.38 忽略规则

- 支持忽略精确路径、精确 Skill，以及对已知待处理问题暂缓或忽略。
- 不支持通配符、正则表达式、脚本条件和嵌套规则。
- 用户可以查看和取消忽略。

### 5.39 备份、恢复与迁移

- 支持一键完整备份、从备份恢复和换机导入。
- 备份包含 Skill、历史版本、来源、验证记录、项目配置、组合和设置。
- 备份不包含任何凭证。
- 恢复前展示冲突，可选择覆盖、保留两份或跳过。
- 恢复失败不能损坏当前集中库。
- 支持 Windows 与 macOS 之间迁移。

### 5.40 自动滚动备份

- 仅在应用运行期间工作。
- 在升级、批量部署、删除、恢复和其他重要操作前自动创建。
- 按保留数量或期限清理旧备份。
- 不创建系统后台服务。
- 不提供云备份服务。

### 5.41 标准导出

- 支持单个 Skill、多个 Skill 和轻量组合导出。
- 导出为标准文件夹或压缩包。
- 支持导出当前版本或历史版本。
- 导出内容不依赖 SkillHub。
- 保留来源和许可证，修改过的内容明确标记。
- 排除凭证、使用证据、操作历史、本机绝对路径和私有设置。
- 可选附带运行要求和兼容性摘要。
- 不提供发布通道、市场上传或远程发布。

### 5.42 LLM 配置

- LLM 完全可选，不配置时所有核心管理功能仍可使用。
- 支持常见在线模型服务、本地模型服务和兼容服务。
- 提供连接测试，只设置一个当前默认模型。
- 凭证保存到操作系统安全凭据存储。
- 每项 LLM 功能说明发送的数据范围。
- LLM 不可用时退回规则能力或明确说明可选功能不可用。
- 不提供通用聊天、模型下载、训练微调、多模型自动路由和复杂成本管理。

### 5.43 轻量 CLI

CLI 支持列出对象、查询状态、扫描、项目尽力装配、部署、解除部署、对齐、更新、健康检查和结构化输出。

- 写操作默认展示预览并要求确认。
- 提供明确的非交互模式。
- 高风险操作仍需显式授权。
- 失败输出必须包含原因和下一步建议。

### 5.44 卸载前处理

提供“退出 SkillHub 管理/卸载前处理”：

- 仅卸载应用并保留集中库、项目配置和现有部署。
- 把受管部署转换为独立副本。
- 解除选定或全部部署。
- 先创建备份再退出管理。
- 取消处理。

统一规则：

- 默认不删除集中库和用户创建的 Skill。
- 不影响 Agent 内置、插件和其他工具管理的 Skill。
- 不删除项目业务目录。
- 凭证是否清除由用户选择，默认询问。
- 直接使用系统卸载时，应用数据和集中库默认保留。
- 重新安装后可以选择原集中库并恢复管理关系。

---

## 6. 界面与交互需求

### 6.1 总体原则

- 面向普通用户使用自然语言。
- “部署/解除部署”在主要界面中表达为“添加到/从……移除”。
- 技术信息按需展开。
- 不设置普通模式和开发者模式两套功能。
- 所有入口使用一致的状态名称、冲突规则和操作结果。
- 空状态、加载状态、无数据状态、无权限状态和失败状态都应提供下一步操作。

### 6.2 主要能力入口

界面需要覆盖概览、Skill 管理、Agent 管理、项目管理、发现与导入、待处理事项、操作历史与恢复、设置与数据管理。

- 入口之间支持带上下文跳转。
- 从概览或待处理事项跳转时保留筛选和问题上下文。
- 具体采用顶部页签、侧边栏或其他导航方式，后续设计。

### 6.3 Skill 管理能力

- Skill 管理是主要操作区域。
- 支持搜索、筛选、排序、选择和批量操作。
- 支持列表、卡片、矩阵和标签分组等查看形式。
- 不同形式使用同一数据和筛选条件。
- 支持保存或记忆个人视图偏好。
- 批量选择时明确区分当前可见对象和全部筛选结果。

### 6.4 Skill 详情范围

详情覆盖基本信息、身份和来源、作者和许可证、版本和差异、Agent 和项目关系、有效状态、调用策略、兼容性、运行要求、安全检查、AI 诊断、功能验证、组合和依赖、外部变更与操作历史。

- 使用证据主要在 Agent 全局 Skill 使用证据分析中展示。
- 只有存在可靠数据时，才在单 Skill 详情显示摘要。
- 详情采用侧边抽屉还是独立页面，后续设计。

### 6.5 多维状态表达

生命周期、部署、版本、验证、安全、兼容和所有权状态必须分开。

- 不设置不透明的单一健康分。
- 列表和卡片只显示当前最重要的问题摘要。
- 详情中显示完整状态及依据。
- “未知”“无数据”“未检查”不能显示为失败或零。
- “弃用”不能与“版本过旧”混为一谈。
- 异常状态提供原因、影响和操作入口。
- 状态不能只依赖颜色。

### 6.6 风险分级操作

- 只读操作直接执行。
- 低风险且可撤销的管理操作可以直接完成，并提供撤销。
- 影响部署或版本的操作先显示变化摘要。
- 删除、覆盖、强制导入、高风险部署和批量破坏性操作必须明确预览并确认。
- 批量操作先汇总可执行、跳过、冲突和需单独处理的对象。
- 高风险对象不能隐藏在一个批量确认中。

### 6.7 过程与结果反馈

- 显示整体进度和当前对象。
- 可安全取消时提供取消，不可取消时说明原因。
- 完成后分别展示成功、跳过、失败和仍待处理的项目。
- 支持重试、查看原因、撤销或进入待处理事项。
- 部分失败不能显示为整体成功。
- 默认显示普通语言结果，路径、差异和底层方式按需展开。

### 6.8 基础桌面可用性

- 适配 Windows 和 macOS 系统缩放及常见高分屏。
- 窗口可调整大小，较小窗口下仍可完成核心操作。
- 主要查看、筛选、选择、确认和取消操作可使用键盘完成。
- 键盘焦点清晰可见。
- 重要控件提供可被系统辅助功能识别的名称。
- 状态使用文字或图标辅助表达，不只使用颜色。
- 保持基本文字对比度。
- 支持跟随系统浅色或深色主题，并允许手动选择。
- 系统启用减少动态效果时，减少非必要动画。
- Windows 和 macOS 快捷键遵循各自习惯。
- 不承诺企业级无障碍认证或完整合规审计。

---

## 7. 保留的界面形式设想

本节记录已经讨论过、后续设计时应继续评估的界面方向。它们不是最终页面设计。

### 7.1 概览

可考虑展示 Skill 总数、Agent 和项目数量、待处理事项、可升级项、冲突、验证过期、安全风险、失联目标和最近操作。概览只做汇总与下钻，不维护第二套状态。

保留的具体形式包括：

- 总览数字卡片。
- 按标签、来源、生命周期、所有权、版本和部署状态的分布视图。
- Agent 与 Skill、项目与 Skill 的关系矩阵。
- 最近操作时间线。
- Skill 数量或待处理事项变化趋势。
- 点击汇总项后进入对应筛选结果。

是否使用饼图、柱状图、趋势图或其他图表，后续设计时验证。

### 7.2 Skill 多视图

- 列表：适合高密度查看和批量管理。
- 卡片：适合日常浏览和概要识别。
- 矩阵：适合查看 Skill 与 Agent/项目的部署关系。
- 标签分组：适合按用途聚类查看。

列表中保留评估的列包括：

- 勾选框。
- 名称和显示别名。
- 简短说明。
- 标签。
- 所有权和来源。
- 生命周期。
- 调用策略。
- 已添加的 Agent 和项目。
- 当前版本、可升级状态和项目固定提示。
- 验证、安全、兼容和待处理问题摘要。
- 行级常用操作和更多操作入口。

筛选维度保留：

- 标签。
- 生命周期。
- 来源和所有权。
- Agent 和项目。
- 调用策略。
- 是否部署。
- 是否可升级或被固定。
- 验证、安全和兼容状态。
- 是否存在待处理事项。

排序维度保留名称、更新时间、来源、版本和可用使用证据；没有可靠使用证据时不提供误导性排序。

卡片中保留名称、别名、简短说明、标签、主要 Agent/项目关系、重要状态摘要、勾选和常用操作。

批量选择后保留浮动操作区的设想，可包含添加到 Agent/项目、移除、打标签、修改升级策略、升级、导出、归档和删除。删除及其他高风险操作仍遵守逐项检查和风险分级规则。

继续评估：

- 列显示、隐藏和顺序调整。
- 卡片密度和尺寸。
- 视图偏好记忆。
- 行内标签编辑。
- 右键上下文操作。
- 看板下钻后自动应用筛选。
- 空状态引导。

### 7.3 Skill 详情

- 右侧详情抽屉作为保留方案。
- 独立详情页作为备选方案。
- 支持连续切换相邻 Skill，并保留当前查看位置。
- 详情内可直接发起与当前状态相关的操作。
- 详情可按基本信息、来源和许可证、版本、Agent 与项目关系、兼容与运行要求、检查与验证、关联关系、外部变更和操作历史进行分区。
- 版本区域保留时间线和差异入口的设想。
- Agent 与项目区域保留部署明细表和有效状态说明的设想。

### 7.4 下钻

- 点击概览中的标签、Agent、项目、状态或问题后，进入对应对象列表并自动应用筛选。
- 返回时尽量保留原上下文。

### 7.5 具体设计延后

页面数量、导航、具体表格列、卡片密度、抽屉宽度、图表类型、按钮位置、工具栏、视觉风格、字号、颜色和间距均在后续设计阶段确定。

---

## 8. 非功能与质量要求

### 8.1 安全作用域

- 只读写集中库、用户明确选择的导入来源和已注册目标。
- 不遍历全盘。
- 删除部署关系时不得跟随链接误删集中库内容。
- 任何递归删除都必须限定明确对象。
- 普通用户权限下应能使用核心功能，不要求管理员权限作为前提。

### 8.2 可靠性

- 重复执行同一操作应保持结果一致。
- 中断后重试不能产生重复对象或未知半成品。
- 重要写操作先建立恢复点。
- 部分失败必须保持已有成功结果可识别。
- 健康检查能够发现未完成操作。

### 8.3 性能

- 面向 100 至 500 个 Skill 的本地规模。
- 常规列表、筛选和状态查看应保持流畅。
- 扫描、批量导入和批量更新显示进度。
- 不为企业级海量资产建设复杂分布式能力。

### 8.4 应用运行边界

- 自动检查、外部变更检测和滚动备份只在应用运行期间进行。
- 支持用户手动触发扫描和检查。
- 不安装常驻后台服务。
- 不创建托盘常驻监控进程。

---

## 9. 明确不做

- 多集中库管理。
- SkillHub 产品账号、团队角色和远程协作服务器。
- 发布通道、Skill 市场上传或远程发布。
- 系统 Git 作为用户前置要求。
- 任意外部命令执行器。
- 自动安装 Skill 运行依赖。
- 完整代码编辑器或完整 AI Skill 创作平台。
- Agent 自动执行测试和自动功能评分。
- 自动解析或上传原始 Agent 会话。
- 双向实时同步、来源镜像或多来源自动切换。
- SkillHub 云同步和多设备并发协作。
- 杀毒、完整恶意代码分析、供应链审计或法律审查。
- 后台常驻服务和系统托盘持续监控。
- 完整 Agent 适配插件生态。
- 整个集中库任意时间点回溯。
- 独立磁盘清理中心。
- 系统诊断日志打包和诊断报告导出。
- Linux、Web 和移动端。

---

## 10. 关联项目

### 多 Agent Runtime Hook

- 作为独立关联项目存在。
- 用于采集更可靠的 Skill 调用事件。
- SkillHub 不依赖该项目即可使用。
- 配合使用时可提高 Agent 全局 Skill 使用证据分析的准确度。
- Hook 项目需要独立确认支持的 Agent、权限、隐私和事件格式。

---

## 11. 后续阶段事项

### 11.1 下一阶段：功能优先级

- 对全部已确认功能统一划分优先级。
- 确定首批交付范围和后续阶段。
- 当前文档不使用旧的 P0、P1、P2 结论。

### 11.2 平台调研

- 确认首批内置 Agent 名单。
- 确认各 Agent 的全局和项目目录、层级覆盖、重复加载、调用策略和部署兼容性。
- 形成逐 Agent 的能力矩阵。

### 11.3 产品与交互设计

- 确认信息架构、导航和页面划分。
- 验证列表、卡片、矩阵和标签分组。
- 确认 Skill 详情使用抽屉还是独立页面。
- 设计首次使用、导入、部署、冲突、删除和恢复流程。
- 设计中英文文案和状态表达。

### 11.4 技术设计

- 确认集中库内部结构和记录格式。
- 确认版本历史、部署、索引、扫描和外部变更检测机制。
- 确认桌面端和 CLI 技术栈。
- 确认 Windows 与 macOS 平台实现差异。

### 11.5 待验证产品参数

- 使用证据分析的默认阈值。
- 自动滚动备份的默认保留数量或期限。
- 默认扫描频率和更新检查频率。
- 高风险提示和强制确认的具体分级规则。

---

## 12. 文档维护规则

- 本文档是当前唯一有效的功能需求来源。
- 新功能先在需求阶段确认，再加入本文档。
- 功能优先级在下一阶段统一维护，不在单个功能讨论中零散标注。
- 界面需求、界面形式设想和最终设计必须分层记录。
- 技术实现方案不得反向替代已经确认的产品行为。
- 删除或调整已确认范围时，应同步更新“明确不做”和“后续阶段事项”。
