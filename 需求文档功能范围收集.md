# SkillHub 功能清单（简化版）

> 一句话：把散落各处的 agent skill 收拢到一个集中库，打标签分类，按需一键派发给任意 agent / 项目目录。
> 详细设计见《需求文档.md》，本页用于团队传阅 + 征集反馈。

## 一、功能点清单

### 管理

1. **集中管理库** —— 同一个 skill 在多个 agent 目录各存一份实体（本机实测：54 份拷贝、6 个目录、最高重复 5 份），占空间且难以判断哪份是最新。
2. **库路径自定义 + 防扫描** —— 集中库若放在 agent 的固定扫描路径内（如 `~/.claude/skills`、项目内 `.agents/`），会被 agent 全量误加载，拖慢启动、污染上下文。
3. **扫描收编** —— 已散落的存量 skill 迁移入库全靠手工搬，费时且怕搬错。
4. **冲突裁决** —— 多处同名 skill 内容不一致（版本漂移），不知道保留哪份，手工对比困难，怕覆盖丢改动。

### 分类与派发

5. **标签与中文元数据** —— skill 没有任何分类元数据，description 全是英文，看不懂/记不住每个 skill 是干嘛的、什么时候会被触发，不敢乱装乱删；连"这 skill 依赖 ffmpeg 吗"都要翻源码。（元数据支持命令行编辑与直接手改文件两种方式，手改不丢）
6. **标签聚类一键派发 + enrich 自动生成中文详情与打标** —— 给不同 agent / 项目分发 skill 全靠手工复制；中文详情/标签靠 LLM 批量生成（标签限从词表推荐），预设 8 个分类（软件开发/日常办公/内容处理/多媒体/数据分析/行业垂直/通用/其他）可自定义，一 skill 多标签，支持勾选式批量打标；LLM 接入完全可选（支持主流厂商 + 自定义 URL/Key），不接入时相关功能明确提示不可用。
7. **软链接派发** —— 复制式分发导致"改一处要同步 N 处"，library 改完其它端还是旧版。
8. **链接三级降级（symlink→junction→copy）** —— Windows 下软链接需要开发者模式/管理员权限，纯软链接方案在同事机器上直接翻车。
9. **组合（bundle）派发** —— 常用的技能组合（如"PM 套件"）每次都要逐个挑，重复劳动。
10. **reconcile 对齐** —— 目标目录里技能越装越多越来越乱，和期望集合的差集没人清理。
11. **部署明细与状态总览** —— 每个 agent 目录里到底装了哪些 skill、哪些是软链哪些是拷贝，全靠挨个 cd 进去翻；哪些 skill 派发到了哪里没有一张总表，重复装/漏装全靠脑子记；手工建的软链接更是无人知晓。
12. **项目级全周期支持与一键装配** —— 有些 skill 只在特定项目用，放进 agent 全局会污染所有会话；项目里的 skill 派发/同步/升级/删除同样没人管；新成员或新机器配项目环境全靠口头文档，装多装少不一致。理想状态：项目里放一份可提交 git 的配置文件，一条命令装配齐整。

### 安装与升级

13. **添加源安装（skills.sh / git / 本地）** —— 安装渠道分散，各装各的，装完就脱离管理。
14. **一键升级 / 单个升级** —— 没有统一升级渠道，54 份拷贝要逐个目录手动更新，实际上从不升级。
15. **升级漂移检测 + 备份** —— 升级会静默覆盖本地定制改动，丢代码无感知、无法回滚。

### 健康与安全

16. **doctor 体检（断链/孤儿/重复/漂移）** —— 库挪位置后断链、agent 目录里的"野生" skill、悄悄多出来的重复拷贝，全都无人发现，出问题才排查。
17. **移除与卸载 + 弃用管理** —— 想清理某 skill 时要在多个 agent 目录挨个找挨个删，删不干净留残留；旧 skill 直接删了又怕哪天还要用，且不知道该换用哪个替代。
18. **安全审查（可选）** —— 第三方 skill 本质是可注入 prompt 的代码包，安装前无任何风险提示。
19. **全文检索 + 导入导出** —— skill 攒多了找不着（名称 / 标签 / 中文用途 / 正文都要能搜）；换电脑、分享给同事只能手工拷目录，还容易拷漏元数据。
20. **lint 校验 + 依赖预检** —— skill 格式坏了 / 依赖缺了（比如没装 ffmpeg），agent 加载失败才知道；派发到别的机器跑不起来。
21. **升级锁定 + 版本标识** —— 怕一键升级把验证过的版本升坏，想锁定"这个 skill 永不自动升级"。
22. **权限与安全设计** —— 怕工具要管理员权限才能用；怕它扫描 / 误删不该动的文件。（设计目标：普通用户权限全功能可用、只动注册过的目录、删链接绝不误删本体）
23. **产品级易用性** —— 命令行工具学习成本高、同事不愿用。（设计目标：一条命令引导式上手、全中文、报错带解决建议、危险操作先预览）
24. **git 版本历史与一键回滚** —— skill 被改坏、删错、升级出问题后无法恢复，"上次到底改了什么"只能靠记忆。（库自动记录每次变更，可回滚任意版本、对比任意两版差异；无 git 用户零门槛，工具内置引擎自动处理，不需远程账号）
25. **发布通道隔离（channel 标签）** —— 自己开发中/测试中的 skill 和日常稳定用的混在一起，一不留神被批量派发出去。（用 channel:dev/test/stable 标签轻量隔离，默认只派发稳定版）
26. **桌面版 + 看板总览** —— 命令行工具对普通用户门槛高；想了解"我有哪些 skill、都装到哪了、健康状况如何"只能逐个命令查，没有一屏总览。（桌面版应用，看板页签总览/分布/矩阵/趋势，点击下钻到列表页筛选操作）

## 二、初期适配 Agent 范围（V1）

Claude Code｜Codex｜pi｜Hermes｜OpenClaw｜DeepSeek-Harness｜WorkBuddy｜Trae｜Cursor｜Gemini CLI｜Kimi Code CLI｜OpenCode｜ZCode｜MiMo Code｜TraeWork｜QcodeWork

> 开发前将逐一调研以上工具的用户级 / 项目级 skill 目录机制（扫描路径、目录约定、软链接兼容性、同名优先级等），结论内置为工具的目录映射表；确认不支持 skill 机制的工具会从清单移除。清单外工具不承诺内置适配（可作为自定义路径目标管理）。

## 三、反馈征集（请填写）

**A. 你的痛点**：目前管理 agent skill，除了上面这些，你还遇到什么问题？

| # | 痛点描述 | 使用场景（什么情况下遇到） |
|---|---|---|
| 1 | | |
| 2 | | |

**B. 你希望的功能**：如果有个统一管理工具，你最希望它做什么？

| # | 期望功能 | 优先级（高/中/低） | 愿望强度：没有它你会自己想别的办法吗？ |
|---|---|---|---|
| 1 | | | |
| 2 | | | |

**C. 环境信息**（用于评估通用性）：

| 问题 | 你的情况 |
|---|---|
| 你在用哪些 agent？（可从上方 V1 适配清单勾选，也可填写清单外的） | |
| 操作系统？（Windows / macOS / Linux） | |
| skill 大概数量？主要来源？（skills.sh / 自写 / 同事分享 / GitHub） | |
| 你更倾向命令行还是带界面的工具？ | |

**D. 上面 26 个功能点，请勾选你认为必须有的**（填编号即可，如：1, 3, 5, 6）：

> ________________
